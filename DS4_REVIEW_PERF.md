# DS4 sweep review (perf focus) — CastCircle

Exhaustive per-file pass: 308 code files across 8 batches.

## Findings

- [high] ios/LocalPackages/kokoro-ios/Sources/KokoroSwift/BuildingBlocks/LSTM.swift:151-152 — backwardDirection inserts at index 0 of allHidden/allCell arrays per timestep (`allCell.insert(currentCell, at: 0)` / `allHidden.insert(currentHidden, at: 0)`) — each insert shifts all existing elements, making the backward pass O(n²) in sequence length; for a 512-token phoneme sequence this is ~260k element shifts vs. 512 appends — append to a temporary array then reverse once, or index into a preallocated array
- [high] ios/LocalPackages/kokoro-ios/Sources/KokoroSwift/TTSEngine/KokoroTTS.swift:364-367 — createAlignmentTarget builds the alignment matrix with a per-frame Swift loop (`for frame in 0 ..< totalFrames { alignmentArray[phonemeIndex * totalFrames + frame] = 1.0 }`) after constructing per-frame MLXArray scalars via `indices[frame].item()` — each `.item()` call is a synchronous device-to-host round-trip; for a 1000-frame alignment this is 1000 round-trips plus a host-side loop — construct the alignment matrix with MLX tensor operations (e.g. one-hot from the indices array) to keep the computation on-device in a single op
- [high] ios/LocalPackages/parakeet-stt/Sources/ParakeetSTT/ParakeetAlignment.swift:144-163 — mergeLongestContiguous runs a nested O(n×m) loop over overlapA and overlapB tokens to find the longest matching chain, called once per audio chunk during streaming STT — for chunks with large overlap regions (hundreds of tokens) this is tens of thousands of comparisons per chunk, and the inner `matches()` call also does string comparison; use the LCS DP approach (already implemented as fallback) with a guard threshold, or cap the overlap window size
- [high] ios/LocalPackages/parakeet-stt/Sources/ParakeetSTT/ParakeetModel.swift:251-295 — decodeTDT processes the encoder output one frame at a time in a while-loop, calling `eval(jointOut)` (a synchronous MLX device eval) per frame — for a 1000-frame audio this is 1000 sequential device round-trips, each blocking the CPU; batch multiple frames or use MLX's lazy evaluation to defer evals until a batch is ready
- [high] ios/LocalPackages/parakeet-stt/Sources/ParakeetSTT/ParakeetModel.swift:335-375 — decodeRNNT has the same per-frame `eval(jointOut)` pattern as decodeTDT, blocking on each frame's joint network output — same O(frames) sequential device round-trips, making RNNT decoding ~1000× slower than batched inference
- [medium] ios/LocalPackages/kokoro-ios/Sources/KokoroSwift/Utils/AudioUtils.swift:65-67 — writeWavFile copies audio samples one at a time in a Swift loop (`for i in 0 ..< Int(frameCount) { channelData[i] = samples[i] }`) — for a 5-second 24kHz clip (~120k samples) this is 120k indexed writes through a raw pointer; use `memcpy` or `samples.withUnsafeBufferPointer` to copy the entire buffer in one call
- [medium] ios/LocalPackages/parakeet-stt/Sources/ParakeetSTT/AudioUtils.swift:42-44 — writeWavFile copies audio samples one at a time in a Swift loop (`for i in 0 ..< Int(frameCount) { channelData[i] = samples[i] }`) — same per-sample indexed write pattern as KokoroSwift's AudioUtils; use memcpy for the bulk copy
- [medium] ios/LocalPackages/parakeet-stt/Sources/ParakeetSTT/AudioUtils.swift:108-110 — saveAudioArray copies MLXArray samples one at a time into a PCM buffer via `for i in 0 ..< samples.count { channelData[0][i] = samples[i] }` — for large audio arrays this is O(n) indexed writes through a raw pointer; use memcpy or withUnsafeBufferPointer
- [medium] ios/LocalPackages/parakeet-stt/Sources/ParakeetSTT/AudioUtils.swift:271-273 — StreamingWAVWriter.writeChunk copies samples one at a time in a loop (`for i in 0 ..< samples.count { channelData[0][i] = samples[i] }`) — called per audio chunk during streaming writes, adding per-sample overhead on every write call; use memcpy for the bulk copy
- [medium] ios/LocalPackages/parakeet-stt/Sources/ParakeetSTT/DSP.swift:139-153 — melFilters computes the filterbank with a nested O(nFreqs × nMels) loop in Swift, called on every logMelSpectrogram invocation (once per STT chunk) — for nFft=800 (401 freq bins) and nMels=80 this is ~32k iterations per chunk; precompute and cache the filterbank matrix once per config instead of recomputing on every call
- [medium] ios/LocalPackages/parakeet-stt/Sources/ParakeetSTT/DSP.swift:171-177 — reverseArray builds an index array and gathers via indexing (`for i in 0..<len { indices[i] = len - 1 - i }`) then `arr[MLXArray(indices.map { Int32($0) })]` — called during STFT reflect-padding; the index array construction is O(n) in Swift, and the gather is a second pass; use MLX's built-in slice/strided operations to avoid the intermediate index array
- [medium] ios/LocalPackages/kokoro-ios/Sources/KokoroSwift/BuildingBlocks/AdaINResBlock1.swift:75-103 — callAsFunction iterates over 3 conv blocks with per-block Swift-level indexing and repeated `swappedAxes` calls (`conv1[i]`, `adain1[i]`, etc.) — each array subscript on a Swift array of MLX modules is fine, but the 3× repeated axis swaps per block (6 swaps per call) add graph overhead; this is called per decoder layer (4 layers) per audio frame, so the graph construction overhead is non-trivial for long sequences — consider fusing the axis swaps or using a single transposed view
- [low] ios/LocalPackages/kokoro-ios/Sources/KokoroSwift/BuildingBlocks/AdainResBlk1d.swift:109-136 — residual() performs 6 separate `MLX.swappedAxes` calls (swapping axes 2 and 1 back and forth) per residual block, called per decoder layer — each swap creates a new graph node; for 4 decoder layers this is 24 extra graph nodes per forward pass; use a single transposed view or restructure to minimize axis swaps
- [low] ios/LocalPackages/kokoro-ios/Sources/KokoroSwift/Decoder/Generator.swift:160-185 — the upsampling loop in callAsFunction constructs a fresh `LeakyReLU` module (`LeakyReLU(negativeSlope: 0.1)(newX)`) on every iteration of every upsampling stage — module construction per call adds graph overhead; hoist the LeakyReLU instances as stored properties initialized once
- [low] ios/LocalPackages/kokoro-ios/Sources/KokoroSwift/Decoder/Generator.swift:176-183 — the residual block accumulation loop uses optional unwrapping (`xs == nil` / `xs!`) and per-iteration array indexing (`resBlocks[i * numKernels + j]`) — minor overhead per kernel, but called in the hottest loop of audio generation; pre-unwrap or use a non-optional accumulator
- [high] ios/Runner/KokoroVendored/BuildingBlocks/LSTM.swift:151 — backwardDirection inserts each timestep's hidden/cell state at index 0 of an array inside a loop (`allCell.insert(currentCell, at: 0)` and `allHidden.insert(currentHidden, at: 0)`) — O(n²) where n = sequence length, because every insert shifts all existing elements; for a 1000-step sequence this is ~500k element shifts per array, doubling synthesis latency — pre-allocate `[MLXArray](repeating: placeholder, count: seqLen)` and assign by index `allHidden[seqLen - 1 - idx] = currentHidden` instead of inserting at 0
- [high] ios/Runner/KokoroMLXService.swift:268 — writeWAV appends a 2-byte `Data` object per audio sample inside a loop (`data.append(Data(bytes: &int16, count: 2))`) — O(n²) allocation churn where n = number of samples; a 5-second clip at 24kHz is 120k samples, each append copies the growing buffer, adding hundreds of ms to every synthesis — build a `[Int16]` array and convert to `Data` once via `withUnsafeBufferPointer`, or use `Data.withUnsafeMutableBytes` to write directly into a pre-sized buffer
- [high] ios/Runner/KokoroVendored/TTSEngine/KokoroTTS.swift:355 — createAlignmentTarget calls `duration.item()` per phoneme inside `durations.enumerated().map` (line 355) and `indices[frame].item()` per output frame in a sequential loop (line 365) — each `.item()` is a synchronous GPU→CPU round-trip (metal sync), so for a 200-frame synthesis this is 200+ blocking syncs on the synthesis path, adding tens to hundreds of ms per call — extract the entire `durations` array to Swift once via a single `asArray(Int.self)` call before the loop, then index into the Swift array
- [medium] ios/Runner/MisakiVendored/English/EnglishG2P.swift:225 — tokenize aligns preprocessed features to tokens with a nested loop (`for feature in features { for token in mutableTokens { ... } }`) — O(f×t) where f = number of markdown-style pronunciation overrides and t = token count; for text with many `[word](/phoneme/)` overrides this degrades quadratically — build a dictionary keyed by token range start index for O(1) lookup per feature
- [medium] ios/Runner/MisakiVendored/English/Lexicon.swift:463 — getNumber's extend_num splits numbers into words via `num2Words.convert` and then iterates over each word calling `lookup` per word inside a nested loop — for multi-clause numbers (e.g. "1,234,567.89") this produces O(w×l) lookups where w = word count and l = lookup depth; acceptable for single numbers but called per-token in the G2P pipeline — cache `num2Words.convert` results for repeated number strings in a small memo dict
- [high] lib/data/services/ocr_confidence_service.dart:163-169 — stripDiacritics builds a new String by iterating `s.split('')` (creating a List of every code unit) and writing one char at a time to a StringBuffer — called once per word inside scoreLine (line 192), which itself runs per script line during import scoring; for a 2000-line script with 8 words/line that's ~16k split+iterate allocations where a single `s.runes.map(...).join()` or pre-built lookup would suffice — replace `s.split('')` with `s.runes.map((r) => _diacriticMap[r] ?? String.fromCharCode(r)).join()` to avoid the intermediate List allocation per word
- [high] lib/data/services/ocr_confidence_service.dart:176-180 — _isValidWord calls `_checker!.checkBuilder<bool>(stripped, builder: (w, isCorrect) => isCorrect)` per word, constructing a closure on every invocation; the closure is identical each call so it is re-allocated per word — for a 2000-line script with 8 words/line that is ~16k closure allocations; hoist the builder to a static const or a single top-level function reference and pass it, eliminating per-word closure allocation
- [medium] lib/data/services/ocr_confidence_service.dart:114-146 — _buildWhitelist iterates all lines and tokenizes each via _tokenize (which splits, maps, and filters) to count word frequencies, then _buildWhitelist is called once per import; the wordCounts map is built correctly but the tokenization allocates a new List per line — acceptable for a one-shot import, but _tokenize is also called again per line in scoreLine (line 187), so the same lines are tokenized twice during a single import pass — cache the tokenized words from the whitelist pass or have scoreLine reuse a precomputed token list to halve the tokenization cost
- [medium] lib/data/services/script_parser.dart:850-916 — _detectCharacterCue sorts knownCharacters by length on every call (`knownCharacters.toList()..sort((a, b) => b.length.compareTo(a.length))`) and then iterates the sorted list, building a fresh RegExp per character per call; _detectCharacterCue is called once per raw line in _parseLines (line 1161), so for a 3000-line script with 50 characters that is 3000 sorts + 150k RegExp constructions — cache the sorted character list and pre-compile the per-character regexes once (e.g. in _detectCharacters or lazily on first use), keyed by format, so the sort and regex compilation happen once per parse not once per line
- [medium] lib/data/services/script_parser.dart:560-566 — _mergeOcrCharacterNames counts how often each known character name appears as a cue by running `RegExp('^$escaped\\.\\s', multiLine: true).allMatches(rawText).length` per character; for 50 characters on a 3000-line script that is 50 full-text regex scans of the entire rawText — replace with a single pass: split rawText into lines and count cue matches with a single compiled regex or a Map lookup, reducing 50 full scans to one
- [medium] lib/data/services/script_parser.dart:662-715 — _resolveTitleCaseAbbreviations does a nested loop over knownCharacters sorted by length (O(n²) comparisons) plus a separate regex scan of the entire rawText for Enter/Exit stage directions; for large character sets this is quadratic in the number of detected characters — build a prefix trie or sorted list with binary search for the prefix-matching step to reduce O(n²) to O(n log n)
- [medium] lib/data/services/script_parser.dart:1042-1044 — addStageDirection computes the scene number by filtering all result lines for scene headers (`result.where((l) => l.lineType == LineType.header && l.scene.isNotEmpty).length`) on every stage direction that triggers a scene transition — for a script with many scene transitions this is O(n) per transition, making scene numbering O(n²) in the number of scenes — maintain a running scene counter instead of re-scanning the result list each time
- [medium] lib/data/services/script_import_service.dart:529-570 — parseAndMapOcr lowercases the entire rawText into a new list (`rawText.split('\n').map((l) => l.trim().toLowerCase()).toList()`) and then for each parsed line does a linear scan from the cursor looking for a matching raw line using `raw.contains(search) || search.contains(raw)` — the `contains` check on strings is O(m) where m is the string length, and it is called for every (parsed line × raw line candidate) pair; for a 3000-line script this is potentially millions of substring comparisons — pre-normalize once (already done) but use exact or prefix matching with a hash map from lowercased raw line text to its index, reducing the per-line lookup from O(n) linear scan with O(m) string comparisons to O(1) hash lookup
- [medium] lib/data/services/script_import_service.dart:127-143 — in the PDFKit path, _findSourcePageFrom does a linear scan from rawSearchStart over rawLines for each parsed line, using `rawTrimmed.contains(searchText) || searchText.contains(rawTrimmed)` — the `searchText.contains(rawTrimmed)` direction is especially dangerous because a short parsed line (e.g. "I") will match almost every raw line, causing the cursor to advance incorrectly and forcing subsequent lines to scan from near-zero again — use exact equality or a normalized hash map instead of bidirectional contains for reliable O(1) lookup per parsed line
- [medium] lib/data/services/tts_service.dart:433-498 — _splitTextForKokoro runs three sequential passes over the chunks list (sentence split → comma split → force word-split), each creating new intermediate lists and re-iterating; for long monologues (>300 chars with no sentence punctuation) this means the text is split and re-split multiple times — consolidate into a single pass that accumulates words into a buffer and flushes when the 300-char limit is reached, eliminating the redundant intermediate lists and multiple iterations
- [medium] lib/data/services/tts_service.dart:529-595 — _speakWithKokoroMlx calls `await _audioPlayer.stop()` before every chunk's `setFilePath` + `play` (line 558), even for multi-chunk lines where the previous chunk has already finished playing via the `chunkDone` await — the stop() between chunks is redundant since playback completed, and it forces just_audio to reset its internal state unnecessarily on every chunk boundary — only call stop() if the player is still actively playing (check playerState) before setFilePath, or use `setFilePath` with `preload: true` and let the completed state handle the transition
- [medium] lib/data/services/recording_sync_service.dart:538-557 — getCachedRecordings iterates every entry in the global _cache map and calls `File(cached.localPath).existsSync()` per entry to build a production-filtered recording map; this is called on every production switch (app.dart:251-268) and whenever the recordings provider rebuilds; for a user with 500 cached recordings across 50 productions, each production switch does 500 synchronous filesystem stat calls — cache the per-production filtered map and invalidate only when the cache changes for that production, or at minimum cache the existsSync result per path with a short TTL
- [medium] lib/data/services/recording_sync_service.dart:283-310 — syncForProduction builds cloudByLine and myCloudByLine maps by iterating cloudRecordings once, but for each row it calls _parseTimestamp twice (once for the cloudByLine comparison, once for the myCloudByLine comparison) — _parseTimestamp does a DateTime.tryParse on every call, which is relatively expensive; parse the timestamp once per row into a local variable and reuse it for both comparisons, halving the parse calls
- [low] lib/data/services/debug_log_service.dart:160-162 — the ring buffer trims with `while (_entries.length > maxEntries) { _entries.removeAt(0); }` which is O(n) per removal because every remaining element shifts — if the buffer is at capacity and a burst of logs arrives (e.g. 500 entries logged in a tight loop during a crash), each log entry triggers an O(500) shift, making the trim O(n²) in the number of overflow entries — replace removeAt(0) with a ring buffer index or use `_entries.removeRange(0, excess)` to remove all excess entries in one call
- [low] lib/data/services/debug_log_service.dart:273-287 — _loadFromDisk reads the entire log file with `file.readAsString()` and then splits on '\n' into a list, then iterates only the last maxEntries lines — for a 200KB log file (the truncation threshold) this allocates a list of ~5000 strings just to keep the last 500; use a streaming line reader or read the file from the end in chunks to avoid materializing the entire file into memory on every app launch
- [low] lib/data/services/stt_service.dart:299-335 — matchScore allocates a full (m+1)×(n+1) integer matrix via `List.generate(m + 1, (_) => List.filled(n + 1, 0))` for the LCS DP table on every matchScore call; for a typical 15-word expected line and 20-word spoken line that is a 16×21 = 336-element allocation per line scored, and matchScore is called per line during rehearsal scoring — reuse a single pre-allocated buffer (cleared per call) or use a rolling two-row DP to reduce the allocation from O(m×n) to O(n) per call
- [low] lib/data/services/stt_service.dart:338-364 — _wordsMatch does a character-by-character comparison loop with per-character indexing (`a[i]`, `b[i]`) for the same-length case; while not a major bottleneck, the per-character indexing on Dart strings has overhead compared to a single `RegExp` or `String.codeUnitAt` comparison — for the fuzzy match path this is acceptable but could be simplified to reduce per-character overhead in the hot matching loop
- [low] lib/data/services/model_download_service.dart:298-331 — refreshDownloadedStatus iterates all availableModels and for each calls `_filePath(model)` which calls `getApplicationDocumentsDirectory()` (an async platform call) per model — this means 4 sequential platform channel round-trips just to build file paths that all share the same base directory; cache the base documents directory once and construct all paths from it, reducing 4 platform calls to 1
- [low] lib/data/services/model_manager.dart:221-283 — _downloadFile creates a new `HttpClient()` per download call; while this is acceptable for one-off downloads, the client is not reused for keep-alive connections — for the Android ML Kit fallback path which downloads multiple model files, reusing a shared HttpClient would avoid TCP/TLS handshake overhead on each file. (Not on the hot request path, but a measurable constant-factor cost for multi-file model downloads.)
- [high] lib/features/cast_manager/cast_manager_screen.dart:366-371 — VoiceConfigService.assignVoicesFromScript() (full adjacency-graph build + greedy graph coloring) is called inside _buildCharacterCard, which runs once per character in the ListView.builder itemBuilder — O(n^2) in character count: every card re-derives the entire voice assignment from scratch, so a 20-character script runs the O(n * window) adjacency build and O(n * v) greedy coloring 20 times instead of once. — Hoist the call out of _buildCharacterCard: compute autoAssignment once in build() (or cache it in a field keyed by productionId/script revision) and pass the per-character result into _buildCharacterCard.
- [high] lib/features/cast_manager/voice_config_screen.dart:143-148 — Same as cast_manager: VoiceConfigService.assignVoicesFromScript() is called inside _buildCharacterTile, which runs once per character via script.characters.map(...). Each tile rebuilds the full adjacency graph and greedy coloring from scratch — O(n^2) in character count. — Hoist assignVoicesFromScript out of _buildCharacterTile: compute it once in build() and pass the per-character voice into _buildCharacterTile.
- [high] lib/features/cast_manager/cast_manager_screen.dart:86-99 — In _syncCastFromCloud, for each local member the code calls cloudMembers.any(...) to check for a matching cloud record by character+role — O(local * cloud) nested scan over cast member lists that grow with production size. At 50+ cast members this is 2500+ map lookups per cloud sync. — Build a Set or Map keyed by (characterName, role) from cloudMembers once before the loop, then do O(1) lookups per local member.
- [high] lib/data/services/sync_queue.dart:222-225 — In _loadPersisted, the alreadyQueued check runs _pending.any(...) and _failed.any(...) inside a loop over restored jobs — O(restored * (pending + failed)). A queue with 1000 restored jobs and 500 pending/failed does ~1.5M predicate evaluations on app restart. — Build a Set<String> of "productionId:lineId" keys from _pending and _failed once before the loop, then check membership in O(1) per restored job.
- [medium] lib/data/services/voice_config_service.dart:195-204 — _leastUsedVoice iterates over the entire pool and the entire used list (building a counts map each call) every time a voice is needed. It is called from the greedy assignment loop (line 188), making the overall assignment O(c * v) where c = characters and v = voices, but the per-call cost is O(v + u) where u = used voices — and used grows with characters. — Precompute a usage-count map once and update it incrementally as voices are assigned, or pass the counts map in and update it in-place rather than rebuilding it per call.
- [medium] lib/features/bulk_cast_setup_screen.dart:256-295 — _saveCastAssignments iterates over _nameControllers.entries and for each filled entry makes a sequential await supa.createCastInvitation(...) followed by await ref.read(castMembersProvider.notifier).save(member). These are sequential network + DB round-trips per character — N characters = N sequential round-trips. At 20 characters with 200ms cloud latency, that's 4 seconds of serial waiting. — Collect all filled entries into a list, then use Future.wait to issue the cloud invitations in parallel (or batch them), and save local cast members in a single batch if the notifier supports it.
- [low] lib/data/services/stt_vocabulary_service.dart:34 — The _actorCorrections map (productionId:actorId -> {wrong: right}) is only evicted via clearProduction(), which removes entries for a specific productionId prefix. If the user works with many productions without explicitly clearing them, or if clearProduction is never called for old productions, the map grows with distinct actor names across all productions. Unlike correctionCache (line 288) which has a 4000-entry hard cap, _actorCorrections has no max-size bound. — Add a max-entries eviction (e.g., clear oldest when exceeding a threshold) alongside the existing clearProduction path.
- [medium] lib/data/services/stt_vocabulary_service.dart:38 — The _correctionPatterns map (misrecognized word -> compiled RegExp) also has no eviction. While _actorCorrections is bounded by actor count, _correctionPatterns grows with distinct misrecognized words, which can be large if the recognizer is poor. — Add a max-size eviction (e.g., LRU or clear-when-exceeding threshold) similar to correctionCache, since these are just cached RegExp compilations.
- [medium] lib/features/join/join_production_screen.dart:449-451 — The invitation lookup uses _castMembers!.where(...).toList() to find a single matching unclaimed invitation, materializing a full list when only the first match is needed. If _castMembers has 100+ entries, this allocates and iterates the entire list just to find one match. — Use _castMembers!.firstWhere(...) (with an orElse returning null) instead of .where(...).toList(), or break early after finding the first match.
- [low] lib/features/home/home_screen.dart:157,183 — ref.watch(savedCharacterProvider(production.id)) is called inside the itemBuilder of both GridView.builder and ListView.builder. Each watch creates a FutureProvider.family instance per production id; while Riverpod caches these, the watch triggers a rebuild of each _ProductionCard whenever the saved-character preference changes for ANY production in the list, causing unnecessary rebuilds of all cards. — Read savedCharacterProvider once per card via ref.watch in _ProductionCard (already done) — this is acceptable, but the FutureProvider.family creates a new provider key per production id which is fine. No action needed unless rebuilds are observed; this is a low-severity observation.
- [high] lib/features/rehearsal/rehearsal_screen.dart:874 — ListView cacheExtent: 10000 forces Flutter to build ~145 offscreen list items eagerly — every state change (mic level ~12Hz, STT partial results, match-score updates) triggers rebuilding all cached items, causing jank and high memory use during rehearsal — reduce cacheExtent to a bounded value (e.g. 1000) or use addAutomaticKeepAlives only for the current line
- [high] lib/features/production_hub/production_hub_screen.dart:401-411 — _buildSceneList itemBuilder calls script.linesInScene(scene) twice per scene card (once for myLineCount, once for totalDialogue), each scanning the full script.lines list via sublist — with N scenes this is O(N * lines) redundant work on every rebuild, and rebuild fires on every Riverpod watch (character change, mode toggle, etc.) — compute sceneLines once at the top of itemBuilder and reuse for both counts
- [high] lib/features/recording_studio/recordings_browser_screen.dart:596-608 — _resolveRecordingPath falls back to Directory(cacheDir).listSync(recursive: true) on every recording resolution, scanning the entire recording cache directory tree synchronously on the main thread — with hundreds of cached files this blocks the UI and is called per-tile during the async scan and per-playback — maintain an in-memory map of filename→path built once, or use a non-recursive listing with a shallow scan
- [high] lib/features/script_editor/cloud_sync_dialog.dart:22-44 — diffScriptLines does a sequential positional comparison (index i in local vs index i in cloud) rather than a proper diff, so reordering lines marks every line as changed and the dialog renders the full changedDiffs list — for scripts with thousands of lines this produces a massive diff list and slow dialog render — use a proper sequence-diff algorithm (e.g. Myers/O(n log n)) or at minimum build lookup maps to detect moved lines
- [medium] lib/features/recording_studio/recording_studio_screen.dart:253 — script.characters.indexWhere((c) => c.name == character) runs an O(n) linear scan of all characters on every build() call, and build() fires on every Riverpod provider change (recordings, character, script) — with many characters this adds up — build a name→index map once when the script changes and reuse it
- [medium] lib/features/recording_studio/recording_studio_screen.dart:343 — script.lines.indexWhere((l) => l.id == currentLine.id) in _buildContextLines does an O(n) scan of the full script.lines list to find the current line's position, called on every build — with large scripts (thousands of lines) this is wasteful — use a precomputed id→index map or store the index directly
- [medium] lib/features/rehearsal/rehearsal_screen.dart:891 — script.characters.indexWhere((c) => c.name == colorLookupName) runs an O(n) linear scan per list item in the rehearsal ListView builder, so with M characters and N dialogue lines the total is O(N*M) on every rebuild — build a name→colorIndex map once outside the builder and look up in O(1)
- [medium] lib/features/script_editor/scene_editor_screen.dart:66-69 — itemBuilder calls script.linesInScene(scene) on every build for every scene card, each scanning the full script.lines via sublist — with S scenes and L lines this is O(S*L) per rebuild, and the ExpansionTile children also call it — compute sceneLines once per item and cache it
- [medium] lib/features/script_editor/scene_editor_screen.dart:116-118 — scene.characters.map builds character chips, and for each chip calls script.characters.indexWhere((c) => c.name == name), an O(C) scan per character per scene per build — with S scenes and C characters this is O(S*C^2) — build a name→colorIndex map once and look up in O(1)
- [medium] lib/features/script_editor/script_editor_screen.dart:248-254 — the "Low OCR" FilterChip label calls script.lines.where((l) => l.ocrConfidence != null && l.ocrConfidence! < 0.85).length on every build, scanning all lines — this is an O(L) scan repeated on every setState (filter toggle, character select, line edit) — precompute the count once when the script loads and update only when the script changes
- [medium] lib/features/script_editor/script_editor_screen.dart:543-547 — _filteredLines calls lines.indexWhere((l) => l.isForCharacter(_selectedCharacter!)) after already filtering, doing an O(L) scan to find the first character line, then sublist — the subsequent .where() calls also scan the full list multiple times — build the filtered list in a single pass
- [medium] lib/features/script_editor/script_editor_screen.dart:564-565 — _reorderLines calls allLines.indexWhere twice (for movedLine and targetLine), each an O(L) scan of the full line list — with large scripts this is wasteful — build an id→index map once before the two lookups
- [medium] lib/features/script_editor/character_manager_screen.dart:498-500 — scene.characters.map builds character chips and for each calls script.characters.indexWhere((c) => c.name == charName), an O(C) scan per character per scene — same O(S*C^2) pattern as scene_editor — build a name→colorIndex map once and reuse
- [medium] lib/features/script_editor/character_manager_screen.dart:733-765 — _rebuildScript iterates all updatedLines to build charCounts, then iterates all script.scenes and for each scene iterates all updatedLines to rebuild scene characters — this is O(L * S) where S is the number of scenes — for large scripts with many scenes this is slow on every character rename/merge/delete — precompute a scene→lines index or use the scene's startLineIndex/endLineIndex range directly
- [low] lib/features/recording_studio/recordings_browser_screen.dart:98-101 — building recordedCharacters set by iterating recordings.values and calling .add() per entry, then later iterating again for the filter chips — this is a minor double iteration but could be combined into a single pass
- [low] lib/features/rehearsal/rehearsal_screen.dart:1452 — _getRehearsalLines uses identityHashCode(script) as part of the cache key, but ParsedScript is a plain class without overridden hashCode/equality, so identityHashCode is stable per instance but the cache is never invalidated when the script's lines change in-place via ref.read(currentScriptProvider.notifier).state = ... — this is a correctness-adjacent perf issue where stale filtered lists may be reused — use a proper version counter or the script's line count as part of the cache key
