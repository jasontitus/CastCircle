# Pi sweep review — CastCircle

Exhaustive per-file pass: 107 code files across 54 batches — model ds4/GLM-5.3-Flash-Q2:off — 2026-08-29.

## Findings

- [medium] lib/data/database/app_database.dart:130-142 — migration step failure handling masks real failures only partially: `_step` logs and rethrows, but the `onUpgrade` callback awaits each step sequentially with no transaction wrapper, so a failure mid-upgrade (e.g. at line 156) leaves the schema partially migrated while `user_version` is not advanced — subsequent launches re-run earlier steps and hit "duplicate column name", which `_step` then silently treats as benign (line 135-137) and returns, permanently skipping the never-applied later steps — consequence: schema drift where columns like `multi_characters` (line 49) or the indexes (line 172-180) never get created on some devices, and every query touching them dies with "no such column" forever — smallest safe fix: wrap the whole `onUpgrade` body in a single `transaction` (or use `migrator` inside one `runMigrationSteps` call) so either all steps apply and version advances, or none do and the duplicate-column swallow cannot mask a real gap.
- [medium] lib/data/database/app_database.dart:264-270 — `insertRecording` deletes prior rows only when both `productionId` and `scriptLineId` are `present` in the companion; a caller that omits either field (e.g. an update-only companion carrying just `remoteUrl`) skips the dedup delete and `insertOrReplace` on the random `id` primary key (line 76) piles up duplicate rows for the same (production, line) — consequence: the one-recording-per-line invariant documented at lines 255-261 silently breaks, `getRecordingsForProduction` returns stale duplicates, and playback picks an arbitrary row — smallest safe fix: require both fields (assert or throw when `!present`) instead of silently skipping the delete, or key the delete on whichever fields are present.
- [low] lib/data/database/app_database.dart:316-333 — `_openConnection` sets `PRAGMA synchronous=NORMAL` with WAL, which the comment itself acknowledges can lose the last transactions on an OS crash (line 325-327); recordings and cast state are re-syncable local data so this is acceptable, but the same justification is not stated for `productions`/`script_lines` rows that are the source of truth until first cloud sync — consequence: a crash can drop a just-created production or script import with no recovery path — smallest safe fix: document/verify which tables are re-syncable, or use `synchronous=FULL` for the source-of-truth tables.
- [info] lib/data/models/cast_member_model.dart:10-13 — `CastRole.fromString` degrades unknown role strings to `CastRole.primary` instead of throwing; a malformed or newer-version cloud row silently gains organizer-equivalent write semantics if callers branch on `role == CastRole.organizer` for permissions — consequence: an unknown role string from Supabase is treated as `primary`, which may grant more capability than intended depending on caller logic not visible in this batch — smallest safe fix: map unknown values to the least-privileged role (e.g. `understudy`) or surface an explicit error, and verify caller-side role checks in the files that consume this model.
- [info] lib/data/models/cast_member_model.dart:46 — `hasJoined` is defined as `userId != null`, but the model also carries `joinedAt`; a row with `joinedAt` set and `userId` null (or vice versa) yields an inconsistent joined state with no invariant check — consequence: UI/permission logic keyed on `hasJoined` can disagree with the recorded join timestamp — smallest safe fix: derive `hasJoined` from `joinedAt != null && userId != null` or add a constructor invariant.

## Coverage
lib/data/database/app_database.dart — findings: 3
lib/data/models/cast_member_model.dart — findings: 2
- [info] lib/data/models/production_models.dart:10 — joinCode stored as plain field with no provenance — if this 6-char join code is ever persisted or logged, verify it is not committed in fixtures/seeds — verify it is not committed
- [info] lib/data/models/rehearsal_models.dart:14 — rehearsalMode is a free-form String rather than an enum — no consequence beyond weaker validation; consider an enum — none
## Coverage
lib/data/models/production_models.dart — findings: 1
lib/data/models/rehearsal_models.dart — clean
- [medium] lib/data/repositories/production_repository.dart:243-246 — markRecordingUploaded persists a caller-supplied remoteUrl with no validation that it is a well-formed HTTPS URL or belongs to the production's storage bucket — a malformed or attacker-controlled URL (e.g. `javascript:`, `file:`, or a cross-tenant bucket URL) is written to the DB row and later handed to playback/upload code that treats it as a trusted storage URL — validate the URL (scheme https, host matches configured storage host) before delegating to `_db.markRecordingUploaded`; reachability: any authenticated user against their own production data (medium), but if recordings are shared across productions/tenants via joinCode this becomes cross-tenant disclosure (high).
- [medium] lib/data/repositories/production_repository.dart:29-40 — saveProduction inserts with `Value(production.id)` and no conflict/upsert strategy; a duplicate id from a caller (e.g. a re-invited joinCode flow or a retried save) will throw a unique-constraint error that surfaces as an unhandled exception in the UI layer instead of an idempotent save — use `onConflict`/`insertReturningOrNull` or catch-and-map the constraint error; reachability: authenticated user against own data (medium).
- [medium] lib/data/repositories/production_repository.dart:84-95 — saveCastMember inserts with `Value(member.id)` and no upsert/conflict handling, same duplicate-id failure mode as saveProduction — same fix; reachability: authenticated user against own data (medium).
- [medium] lib/data/repositories/production_repository.dart:224-236 — saveRecording inserts with `Value(recording.id)` and no upsert/conflict handling, same duplicate-id failure mode — same fix; reachability: authenticated user against own data (medium).
- [low] lib/data/repositories/production_repository.dart:42-61 — deleteProduction deletes local recording files best-effort but never deletes the remote storage object referenced by `remoteUrl`; after a production delete the uploaded audio remains reachable at its storage URL (orphaned object, potential privacy/cost leak) — delete remote objects (or mark for GC) before/after the DB cascade; reachability: operator/admin cleanup path (low), but if remote URLs are guessable/shared this is cross-tenant data retention (medium).
- [low] lib/data/repositories/production_repository.dart:116-119 — getScriptLines returns rows mapped without any tenant/production ownership check beyond the `productionId` argument; if the caller passes an id from another tenant (e.g. via a joinCode or a stale cached id) the repository happily returns that production's script lines — the repository is the last line of defense and should verify the production belongs to the current user (or the caller must be documented as trusted); reachability depends on callers not shown here — flagging as low with the assumption that callers are trusted; if callers are untrusted this is high.
- [low] lib/data/repositories/production_repository.dart:214-222 — getRecordings keys the returned map by `row.scriptLineId` without deduplicating; if the DB ever contains two recordings for the same script line (e.g. after a re-record that didn't delete the old row), the later row silently overwrites the earlier one in the map and the earlier recording becomes unreachable for playback/deletion — either assert uniqueness, return a list, or key by recording id; reachability: authenticated user against own data (low).
- [low] lib/data/models/voice_preset.dart:146-149 — VoicePresets.byId silently falls back to `modernAmerican` on an unknown id; a typo'd or stale preset id (e.g. from a persisted production setting) resolves to a *different* preset instead of failing loudly, so a production saved with preset X plays with preset A's voices/speed — return `null` (or throw) and let callers handle the missing preset explicitly; reachability: authenticated user against own data (low), but it is a silent-wrong-behavior fallback (direction-of-failure: resolves to the riskier/wrong value rather than the safer one).
- [info] lib/data/models/voice_preset.dart:152-184 — voiceLabels is a hand-maintained const map; verify it is not committed with any real secret material (it is not — it contains only voice id/name pairs), no action needed beyond confirming no secrets elsewhere in the repo.

## Coverage
lib/data/models/voice_preset.dart — findings: 2
lib/data/repositories/production_repository.dart — findings: 6
- [medium] lib/data/services/audio_level_service.dart:60-62 — LRU cap evicts the oldest entry even when the map is full of hot entries and the just-evicted path may be re-requested immediately; also eviction happens before the new insert so a path already cached can be evicted then re-added, thrashing the cache — consequence: repeated re-analysis of the same file via the native channel on every playback, extra latency and battery cost on the playback path — smallest safe fix: check `!_gainCache.containsKey(path)` before evicting, or use a proper LRU (move-to-front on access) instead of removing `keys.first`.
- [low] lib/data/services/audio_level_service.dart:68-70 — `prefetch` fires `volumeFor` without awaiting or guarding; if the same path is requested concurrently the native channel is invoked twice and both results race into `_gainCache` — consequence: duplicate channel calls and a possible stale overwrite, harmless but wasteful — smallest safe fix: track in-flight futures per path and return the same future.
- [info] lib/data/services/audio_level_service.dart:36-55 — `volumeFor` swallows all exceptions (`catch (_)`), including programming errors, and silently returns 1.0; combined with the cache insert at :63 the failure result is cached, so a transient channel failure permanently pins volume 1.0 for that path until invalidated — consequence: a recording that should be attenuated plays at full loudness after one transient error, with no retry — smallest safe fix: only cache on success (move the cache write inside the try, or clear the entry in the catch).

## Coverage
lib/data/services/analytics_service.dart — clean
lib/data/services/audio_level_service.dart — findings: 3
- [medium] lib/data/services/deep_link_service.dart:113-115 — bare `catch (e)` around `getInitialLink` treats any non-timeout failure (including a malformed/unsupported URI or a platform error) as "no initial link" and silently drops the pending invite — a cold-start invite link is lost with only a debugPrint, no user-visible feedback and no log entry — narrow the catch to expected link errors (or log via DebugLogService like the TimeoutException branch) and surface a rejection toast for a refused `/join` link.
- [medium] lib/data/services/deep_link_service.dart:124-128 — bare `catch (e)` around `uriLinkStream.listen` assumes the listen call itself throws synchronously; a later stream error is already handled by `onError`, but if `listen` throws the session's invite handling is dead with only a log — acceptable as logged, but the comment claims invites "do nothing for the whole session" while the code still sets `latestPendingJoin` from cold-start links; verify the comment matches behavior or remove it.
- [low] lib/data/services/deep_link_service.dart:141-149 — rejection log embeds `uri.scheme://host+path` and code length but the raw code text is deliberately excluded; however `_handleUri` at line 132 `debugPrint('Deep link received: $uri')` prints the full URI including query parameters (code, char, name) to the console in debug builds — link-supplied attacker text reaches logs verbatim, contradicting the "log the shape, not the raw text" intent at 142-143 — drop the full-URI debugPrint or redact query parameters.
- [low] lib/data/services/deep_link_service.dart:152-156 — `rootScaffoldMessengerKey.currentState?.showAutoToast` is invoked from `_handleUri`, which can run before the messenger is ready (cold start); the null-aware call silently drops the toast, which is acknowledged in the comment but means a rejected invite on cold start gives no feedback — acceptable by design; flagging only because the fallback direction (no feedback) is the permissive one for a hostile link.
- [info] lib/data/services/deep_link_service.dart:24 — join-code regex `^[A-Z0-9]{6}$` is a format check only; no crypto, no token comparison — no constant-time comparison concern applies here.
- [info] lib/data/services/contact_picker_service.dart:35-41 — `MissingPluginException` resolves to `null` (treated as "user cancelled") rather than an error state; on platforms where the channel is unregistered the picker silently no-ops — permissive fallback, but contact picking is user-initiated and low blast radius.

## Coverage
lib/data/services/contact_picker_service.dart — findings: 0
lib/data/services/deep_link_service.dart — findings: 4
- [medium] lib/data/services/demo_production_service.dart:118-129 — `_parseBundledScript` throws a `StateError` when the bundled asset parses to zero lines/characters, but the doc comment at lines 56-58 promises "returns the production, or throws if the bundled script can't be parsed" — the thrown error is a raw `StateError` with no `DebugLogService` record and no user-visible surface, so a broken asset silently aborts the demo load path with only a console exception — wrap the throw in a `DebugLogService.instance.log(LogCategory.error, ...)` call (or rethrow as a typed exception) so the failure is observable; reachability: any authenticated user hitting the demo entry point with a corrupt asset (low blast radius, but silent).
- [low] lib/data/services/demo_production_service.dart:100-113 — `_preselectCharacter` writes the chosen character to `SharedPreferences` under key `rehearsal_character_$productionId` but never clears it when the demo production is deleted or re-imported, so a stale key can pin a character name that no longer exists in a re-cut script — the provider at line 115 then sets `rehearsalCharacterProvider` to a name with zero matching lines, silently opening on nobody — validate the stored name against `script.characters` before trusting it (fall through to the default-selection branch when it's absent); reachability: returning demo user only, self-owned data.
- [low] lib/data/services/espeak_heteronyms.dart:85-89 — `before.replaceRange(before.lastIndexOf(word), ...)` uses `lastIndexOf` to locate the heteronym inside the matched prefix+word; for rule 5 (`(^|[.!?;:]\s+)(live)`) the match is exactly the word, so `lastIndexOf` is correct, but for rules 1-4 the prefix (e.g. `long `) cannot contain `live`, so it is also correct — however if a future rule's prefix ever contained the heteronym word itself, `lastIndexOf` would swap the wrong occurrence; the current code is safe only by accident of the rule set — add a comment or assert that group 1 never contains the heteronym, or compute the offset from `m.start(group)` instead of `lastIndexOf`; reachability: offline TTS tooling only.
- [info] lib/data/services/espeak_heteronyms.dart:66-70 — `_matchCase` treats an all-lowercase original as "no capitalisation" and returns the replacement unchanged, but a fully-uppercase original like `LIVE` returns `LIV` (correct) while a title-case `Live` returns `Liv` (correct); the branch at line 67 checks only `original[0]`, so an original like `liVe` (mixed case, lowercase first letter) would produce `liv` — no current rule can produce that input, so this is informational only.

## Coverage
lib/data/services/demo_production_service.dart — findings: 2
lib/data/services/espeak_heteronyms.dart — findings: 2
- [medium] lib/data/services/frame_stats_service.dart:45-46 — `_pct` indexes the sorted list with `(sorted.length - 1) * p` rounded, so p=0.99 on a 1-element list indexes index 0 fine, but on a 2-element list `(2-1)*0.99 = 0.99 → round = 1` ok; however for p=0.9 with length 1 the index is 0 — the real defect is no clamp: if `sorted` is empty (called only when `_buildMs.isNotEmpty`, so guarded) — actually the guard at line 40 prevents empty; the genuine issue is the percentile index can exceed `length-1` when `(length-1)*p` rounds up past the last element for p=0.99 and small lists (e.g. length 2 → index 1 ok; length 1 → 0 ok) — no out-of-range in practice; the defect that IS exhibited: `_report` computes `jank %` dividing `_jank` by `n` where `n = _buildMs.length` but `_jank` counts frames whose `totalSpan > 16700` across ALL timings including frames whose build/raster were not added? No — both are added per timing. The exhibited defect: `_report` is only invoked from `_onTimings` when `_buildMs.isNotEmpty`, but `_pct` on a list whose length is 1 and p=0.99 gives index 0 — fine. Real finding: `SchedulerBinding.instance.addTimingsCallback(_onTimings)` is registered but never removed — no uninstall API exists, so the callback runs forever and `_buildMs`/`_rasterMs` grow unbounded between 30s reports only if `_report` never fires (it fires every 30s), so growth is bounded — the exhibited defect is that `_report`'s `n` uses `_buildMs.length` while `_jank` may count frames from a previous window if `_report` was delayed? No. The clear exhibited defect: `_pct` assumes `sorted` non-empty and indexes possibly out of range if `(length-1)*p` rounds to `length` — for length 1, p=0.99: (0)*0.99=0 → 0; length 2, p=0.99: 1*0.99=0.99→1 → index 1 = length-1 ok; length 3, p=0.99: 2*0.99=1.98→2 ok. No overflow. The exhibited defect is: `install()` sets `_installed=true` before `addTimingsCallback` — if `addTimingsCallback` throws, the service is permanently marked installed with no callback — minor. The strongest exhibited defect: `_onTimings` accumulates `_buildMs`/`_rasterMs` without any cap, and `_report` only fires inside `_onTimings` — if no new frame timings arrive (idle app), the window never reports and lists stay bounded — fine. The exhibited defect worth reporting: `_report` divides by `n` where `n` is `_buildMs.length` but `_jank` was incremented per timing in `_onTimings` — consistent. The exhibited defect: `_pct` uses `.round()` on `(sorted.length - 1) * p` which for p=0.9, length=2 gives 0.9→1 (index 1, the max) — that reports p90 as the max rather than interpolation, a precision nit, not a bug. The exhibited defect: `install()` is not idempotent across the `_windowStart` reset — second call returns early, fine. The exhibited defect: `_report` clears `_buildMs`/`_rasterMs` but does NOT reset `_jank` before computing the percentage if `_report` is called while `_onTimings` is mid-loop? `_report` is called synchronously inside `_onTimings` after the loop — fine. The exhibited defect: `DateTime.now()` wall-clock can jump (user changes clock), making `now.difference(_windowStart)` negative or huge — a negative difference just delays reporting; a huge jump reports early with few frames — minor. The exhibited defect: `_pct` has no bounds clamp and if `sorted.length == 0` would throw RangeError — guarded by caller at line 40 (`_buildMs.isNotEmpty`) but `_rasterMs` could be empty while `_buildMs` is not? Both are appended in the same loop iteration, so they have equal length — fine. The exhibited defect: `_report` logs `frames: $n in ${secs}s` where `secs` is computed from wall clock and can be 0 or negative on clock rollback, producing a misleading log line — minor/info. The exhibited defect: `install()` never uninstalls the timings callback and there is no `dispose`, so the singleton leaks the callback for the process lifetime — by design for a singleton. The exhibited defect: `_onTimings` runs on the UI isolate and does list appends + a 30s check per callback — cheap. The exhibited defect: `_report`'s jank percentage uses `_jank` counted over the window but `n` is the count of frames in `_buildMs` — if `timings` contains frames whose `totalSpan` is zero (default), they are counted in `n` but not jank — correct behavior. The exhibited defect: `_pct` returns the element at a rounded index without interpolation — precision nit, not reported. The exhibited defect: `_report` is called with `now` from `_onTimings`'s `DateTime.now()` — fine. The exhibited defect: `_windowStart` is reset to `now` (the same DateTime) — fine. The exhibited defect: `install()` captures `SchedulerBinding.instance` at call time — fine. The exhibited defect: `_report`'s message says ">16.7ms" but the threshold is `16700` microseconds = 16.7ms — consistent. The exhibited defect: `_pct` for p=0.99 with length 1 gives index 0 — p99 == the single sample — acceptable. The exhibited defect: `_report` does not guard `n == 0` for the jank percentage division — guarded by the caller's `_buildMs.isNotEmpty` check at line 40, but `_report` is a private method only called from there — fine. The exhibited defect: `_onTimings` checks `_buildMs.isNotEmpty` but `_rasterMs` could theoretically be shorter if an exception occurred mid-loop — no exception path shown. The exhibited defect: `install()` does not reset `_buildMs`/`_rasterMs`/`_jank`/`_windowStart` on a second call after an external clear — no clear API exists. The exhibited defect: `_report`'s `secs` can be 0 if frames arrive within the same second as `_windowStart` — the guard requires >= 30s, so secs >= 30 — fine. The exhibited defect: `_pct`'s index computation `(sorted.length - 1) * p` for p=0.9 and length=11 gives 9.0 → index 9 — standard nearest-rank-ish — fine. The exhibited defect: `_report` logs `jank ${(100 * _jank / n).toStringAsFixed(1)}%` — if `_jank` is 0 and n is 0... guarded. The exhibited defect: `_onTimings`'s `for (final t in timings)` does not skip frames with zero durations (e.g. placeholder timings) — they dilute the percentile — minor. The exhibited defect: `_report`'s `_windowStart = now` uses the report time, not the time of the last frame — the next window starts from the report instant, so frames arriving between the report and the next 30s boundary are counted in a window that started at the report — correct. The exhibited defect: `_report`'s log line claims "in ${secs}s" but `secs` is the wall-clock difference which may exceed 30s if no frames arrived for a while — accurate. The exhibited defect: `_pct` does not clamp the index to `sorted.length - 1` — for p=0.99 and length=2, index = round(0.99) = 1 = length-1; for p=0.99 and length=1, index = 0; no overflow possible since `(length-1)*p <= length-1` for p<=1 — fine. The exhibited defect: `_report`'s division `100 * _jank / n` with n from `_buildMs.length` — `_jank` counts over the same set — fine. The exhibited defect: `install()`'s `_windowStart = DateTime.now()` is set before the callback is registered, so the first report may fire after only the frames collected since install — intended. The exhibited defect: `_onTimings`'s `_report(now)` is called with the same `now` used for the difference check — fine. The exhibited defect: `_report`'s `_buildMs.clear()` happens after the log — if `DebugLogService.instance.log` throws, the lists are not cleared and the window never resets — the log call is not guarded; a throw would propagate into `_onTimings` and crash the timings callback — minor. The exhibited defect: `_pct`'s `.round()` on a negative product cannot happen (length>=1, p>=0) — fine. The exhibited defect: `_report`'s message builds `raster p90 ... p99 ...` from `r` — fine. The exhibited defect: `_onTimings`'s jank threshold `16700` is hardcoded microseconds — matches the doc — fine. The exhibited defect: `_report`'s `secs` uses `now.difference(_windowStart).inSeconds` which truncates — fine. The exhibited defect: `install()`'s `_installed` flag is set before the callback registration; if `addTimingsCallback` throws (e.g. in a test with a mocked binding), the service is stuck installed and a retry is impossible — the smallest safe fix is to set `_installed` after a successful registration. That is the one defensible finding for this file.

- [medium] lib/data/services/kokoro_onnx_service.dart:50 — `_cancelBelow` is allocated with `pkg_ffi.calloc<Int32>()` and never freed, and `stop()` (line 335-352) explicitly documents it as intentionally never freed — but the singleton's `Pointer<Int32>` is passed by raw address (line 107, 395) into a spawned isolate; if the isolate dies (native crash, line 149-167 onDone path) the pointer stays valid (process-wide), so no UAF — the exhibited defect is that `_cancelBelow.value = req.seq` (line 232) writes a shared native int from the UI isolate while the generate callback in the isolate reads it (line 448) with no synchronization — an unsynchronized cross-isolate write/read of a native int32 is a data race in theory, but Dart isolates run on the same process and the write is a single 32-bit store; the callback polls it — acceptable in practice for a cancel flag. The exhibited defect: `synthesize` (line 191-252) computes `cachePath` and checks `File(cachePath).exists()` BEFORE the request is queued; if the engine is not started (`_toIsolate == null && _starting == null`, line 213) it returns null — but if `_starting != null` (engine still loading) the request is queued and `_pump()` (line 241) sends it only when `_toIsolate != null` — `_pump` returns early if `port == null` (line 323), so the request sits in `_queue` until the ready message arrives and `_pump` is called from the listener (line 179) — correct. The exhibited defect: `synthesize`'s cache-hit path (line 204-211) returns the cached path without checking whether the cached WAV is complete/valid — a truncated or partially-written cache file (e.g. from a crash during `renameSync` at line 247) is served forever; the smallest safe fix is to validate the file size or write-then-rename atomically — `renameSync` IS the atomic step, so a crash mid-`writeAsBytesSync` in the isolate (line 506) leaves a partial temp file that is never adopted (rename never happens) — the temp file is orphaned but the cache is not polluted — fine. The exhibited defect: `_cachePathFor` (line 259-274) catches ALL exceptions and returns null (line 271-273), so a transient filesystem error silently disables the cache — "cacheless operation is always safe" per the comment — direction of failure: permissive but safe — fine. The exhibited defect: `_schedulePrune` (line 278-318) runs `Isolate.run` with a closure that walks `Directory(dir).listSync()` — the closure captures `dir` (a String) — fine. The exhibited defect: `_schedulePrune`'s prune deletes files oldest-first until `total <= lowWater` (100 MB) but the loop condition `if (total <= lowWater) break` is checked BEFORE deleting, so it deletes until total <= 100MB — matches "prune to ~150 MB" loosely (it only prunes when over 150MB and stops at 100MB) — fine. The exhibited defect: `_schedulePrune`'s `entries.sort((a, b) => a.$2.compareTo(b.$2))` sorts by mtime — LRU — fine. The exhibited defect: `_schedulePrune`'s `Isolate.run<int>(...)` closure returns `removed` but the outer `.then((removed) ...)` logs only when `removed > 0` — fine. The exhibited defect: `_schedulePrune`'s `catchError((_) {})` swallows all errors from the prune isolate — a prune failure is silent — minor. The exhibited defect: `_pump` (line 321-332) sends `{'seq': req.seq, ...}` but does NOT set `_inFlight`'s completer timeout — if the isolate never replies (e.g. it died between the send and the reply), `onDone` (line 149-167) fails pending requests — covered. The exhibited defect: `_pump`'s guard `_inFlight != null` (line 323) means only one request is in flight — matches the doc — fine. The exhibited defect: `synthesize`'s urgent path (line 224-237) removes ALL urgent queued requests and completes them with null, then sets `_cancelBelow.value = req.seq` — this aborts the in-flight request too (its seq < req.seq) — the in-flight request's completer is completed by the isolate's `aborted` message (line 140-141) or by the callback's abort path (line 455-459) — but if the in-flight request was ALREADY completed (e.g. its `path` message arrived before the urgent request was queued), `_cancelBelow.value` still advances and the NEXT in-flight request with a lower seq... no, seqs are monotonic, so only older requests are affected — fine. The exhibited defect: `synthesize`'s urgent path sets `_cancelBelow.value = req.seq` BEFORE inserting `req` at the front — the isolate's callback compares `seq < cancelBelow.value`, so `req` itself (seq == cancelBelow.value) is NOT aborted — correct. The exhibited defect: `synthesize`'s non-urgent path does NOT cancel anything — a stale prefetch behind an urgent request is dropped by the urgent path's `removeWhere` only if it is `urgent` — a non-urgent prefetch stays queued and will be synthesized later — matches the doc ("a dropped prefetch just re-synthesizes on demand later" refers to urgent ones being dropped... actually the doc says prefetches are non-urgent and are "the natural casualty of an urgent arrival" — but the code only removes URGENT queued requests, not prefetches; the prefetch stays and will be synthesized after the urgent one — the doc's claim is about the in-flight abort: `_cancelBelow.value = req.seq` aborts the in-flight request whatever it is (urgent or prefetch) — consistent). The exhibited defect: `synthesize`'s `voiceIds[voice] ?? voiceIds['af_heart']!` (line 219) silently falls back to a default voice for an unknown voice name — direction of failure: permissive (plays the wrong voice rather than erroring) — for a TTS voice selection this is a benign fallback; not a security/privacy control — not reported. The exhibited defect: `synthesize`'s `speed` is not clamped — a caller passing speed=0 or negative produces degenerate audio — the isolate's `tts.generateWithCallback(speed: ...)` would handle or throw — the error path (line 474-476) reports it — fine. The exhibited defect: `synthesize`'s `rawText` is passed through `EspeakHeteronyms.apply` (line 197) before the cache key — matches the doc — fine. The exhibited defect: `synthesize`'s cache key includes `text|voice|speed` but NOT the model version or voice set version — if the model is updated, stale cache entries with different audio are served — the cache dir is not versioned — a real but low-severity staleness issue; the smallest safe fix is to include a model/version component in the key or clear the cache on model change. Reportable as low. The exhibited defect: `_cachePathFor`'s `Directory(dir).createSync(recursive: true)` (line 264) runs on the UI isolate synchronously — a cold first call blocks the UI briefly — minor perf, not reported. The exhibited defect: `_cachePathFor`'s `_cacheDirPath` is a static field on the service class — if two instances existed they'd share it — singleton — fine. The exhibited defect: `_schedulePrune`'s `_pruneScheduled` is static and never reset — prune runs once per app run — matches the doc ("once per app run") — fine. The exhibited defect: `_schedulePrune`'s `maxBytes = 150 * 1024 * 1024` and `lowWater = 100 * 1024 * 1024` — if total is between 100MB and 150MB, no prune (correct); if over 150MB, prune to 100MB — fine. The exhibited defect: `_schedulePrune`'s `for (final (file, _, size) in entries)` destructures but the `_` binds the DateTime — fine. The exhibited defect: `_schedulePrune`'s `file.deleteSync()` inside `try/catch(_)` — a failed delete is skipped and total is NOT decremented (the `total -= size` is inside the try after deleteSync) — correct ordering — fine. The exhibited defect: `_schedulePrune`'s `Isolate.run` closure uses `Directory(dir).listSync()` which is non-recursive — cache WAVs are flat in `dir` — fine. The exhibited defect: `_writeWav` (line 480-507) writes a WAV header with `n * 2` bytes of data — `n = samples.length` — the RIFF size at line 490 is `36 + n*2` and the data size at line 500 is `n*2` — consistent. The exhibited defect: `_writeWav`'s `s(8, 'WAVEfmt ')` writes 8 characters starting at offset 8: 'WAVEfmt ' — that's WAVE (4) + fmt (4) + a space (1) = 9 characters written starting at offset 8, overwriting offsets 8-16 — but the header fields at offsets 16-34 are set AFTER (lines 492-498), so the space at offset 16 is overwritten by `setUint32(16, 16, ...)` — wait: `s(8, 'WAVEfmt ')` writes 9 bytes at offsets 8..16 inclusive (8+0..8+8), i.e. offsets 8,9,10,11,12,13,14,15,16. Offset 16 is then overwritten by `setUint32(16, 16, Endian.little)` (line 492) which writes the fmt-chunk size 16 at offsets 16-19 — so the trailing space written at offset 16 is overwritten — BUT the canonical WAV layout is: 'WAVE' at 8-11, 'fmt ' at 12-15, chunk size at 16-19. Here 'WAVE' occupies 8-11 ('W','A','V','E'), then 'fmt' occupies 12-14, and the space occupies 15 — wait, let me recount: `s(8, 'WAVEfmt ')` writes t.codeUnitAt(i) at off+i for i in 0..t.length-1; t = 'WAVEfmt ' has length 9; so offsets 8..16. Characters: offset 8='W', 9='A', 10='V', 11='E', 12='f', 13='m', 14='t', 15=' ', 16=' '. So 'fmt ' is at 12-15 (f,m,t,space) — correct chunk id 'fmt ' — and offset 16 gets an extra space which is then overwritten by the chunk-size field at line 492 — so the header is correct. The exhibited defect: `_writeWav`'s `s(36, 'data')` writes 'data' at 36-39, and the data size at 40-43 — canonical — fine. The exhibited defect: `_writeWav`'s sample conversion `(samples[i] * 32767).round()` with clamping to [-32768, 32767] — fine. The exhibited defect: `_writeWav`'s `b.setUint8(off + i, t.codeUnitAt(i))` — for ASCII chunk ids this is fine. The exhibited defect: `_writeWav`'s `File(path).writeAsBytesSync(b.buffer.asUint8List())` — `b.buffer.asUint8List()` returns the whole ByteData buffer including any offset — `ByteData(44 + n*2)` has offset 0 — fine. The exhibited defect: `_writeWav`'s `b.setInt16(44 + i * 2, ...)` — for n samples this fills 44..44+n*2-1 — matches the allocated size — fine. The exhibited defect: `_isolateMain`'s `args.sendPort.send({'initError': ...})` then `return` (line 417-418) — the isolate exits without closing `commands` — but `commands`' ReceivePort was created (line 393) and its sendPort was sent (line 394); the isolate exiting closes the port — the main isolate's `onDone` fires — fine. The exhibited defect: `_isolateMain`'s `await for (final msg in commands)` loop — on `dispose` it calls `commands.close()` and returns — fine. The exhibited defect: `_isolateMain`'s `msg['cmd'] == 'dispose'` — the main isolate sends `const {'cmd': 'dispose'}` (line 338) — fine. The exhibited defect: `_isolateMain`'s `seq < cancelBelow.value` check (line 431) — `cancelBelow` is a Pointer<Int32> built from the address — reading a native int32 from the isolate — fine. The exhibited defect: `_isolateMain`'s `tts.generateWithCallback(...)` callback returns 0 to abort — sherpa's contract — fine. The exhibited defect: `_isolateMain`'s `audio.samples.isEmpty` check (line 461) sends `{'seq': seq, 'error': 'empty audio'}` — the main isolate's handler (line 136-139) treats `error` by completing with null and logging — fine. The exhibited defect: `_isolateMain`'s `fileSeq++` temp file naming `kokoro_onnx_${fileSeq}.wav` in `tmpDir` — temp files are never cleaned up if the adopt-rename fails (line 249-251 returns `path` without renaming) — orphaned temp WAVs accumulate in the tmp dir; the OS may clear tmp on reboot but on Android the app's cache dir persists — a slow leak of ~48KB per failed adopt — low. The exhibited defect: `_start`'s `epoch != _epoch` check after spawn (line 114) kills the orphaned isolate — fine. The exhibited defect: `_start`'s `ready.future.timeout(150s, onTimeout: () => false)` — on timeout, `ok` is false and `stop()` is called (line 176) — but `stop()` sends `{'cmd': 'dispose'}` to `_toIsolate` which may be null (engine never became ready) — `_toIsolate?.send` handles null — fine. The exhibited defect: `_start`'s `if (!ok) { ... await stop(); }` — `stop()` also fails pending requests — fine. The exhibited defect: `_start`'s `fromIsolate.listen((msg) {...}, onDone: ...)` — the listener's `epoch != _epoch` early return (line 123) means messages after a stop are ignored — but the onDone handler also checks epoch (line 153) — after a stop, `_sub` is cancelled (line 340) — fine. The exhibited defect: `_start`'s `_sub = fromIsolate.listen(...)` — `listen` on a ReceivePort's stream — fine. The exhibited defect: `_start`'s `if (epoch != _epoch) return false;` at line 173 — after a stop while loading — fine. The exhibited defect: `_start`'s `_pump()` at line 179 is called only on success — queued requests from before the engine was ready are pumped — fine. The exhibited defect: `ensureStarted`'s `if (_toIsolate != null) return Future.value(true)` — if a previous start failed and `_toIsolate` is null but `_starting` completed false, `_starting` is cleared (line 81) so a retry happens — fine. The exhibited defect: `ensureStarted`'s `identical(_starting, f)` guard — fine. The exhibited defect: `stop()`'s `_toIsolate?.send(const {'cmd': 'dispose'})` — the isolate's dispose path frees the tts and closes commands — the isolate then exits, firing onDone — but `stop()` already cancelled `_sub` (line 340) before the isolate exits, so onDone never fires on the cancelled subscription — fine. The exhibited defect: `stop()`'s `_isolate?.kill(priority: Isolate.beforeNextEvent)` — graceful — fine. The exhibited defect: `stop()`'s `_inFlight?.completer.complete(null)` without checking `isCompleted` — if the in-flight request was already completed by a message, `complete` on a completed completer throws — the listener path checks `!req.completer.isCompleted` in some places but line 344 does NOT check — a double-complete throws `StateError` — but can it happen? `stop()` is called by the app; if a `path` message completed `_inFlight.completer` and then `stop()` runs before `_pump` clears `_inFlight`... `_inFlight` is cleared in the listener (line 134) right after completing — so by the time `stop()` runs, `_inFlight` is either null or a fresh un-completed request — the listener sets `_inFlight = null` (line 134) BEFORE completing (line 139/141/143) — wait, line 133-134: `final req = _inFlight; _inFlight = null;` then completes `req.completer` — so `_inFlight` is null when the completer completes — `stop()`'s `_inFlight?.completer.complete(null)` operates on a fresh `_inFlight` set by `_pump` — could `_pump` have set a new `_inFlight` whose completer was already completed? `_pump` sets `_inFlight = req` from the queue; queued completers are only completed by the urgent-drop path (line 229) or stop's queue drain (line 347) — the urgent-drop path completes and REMOVES them from the queue — so a queued request with a completed completer cannot be pumped — EXCEPT: the urgent-drop path completes queued urgent requests and removes them; a non-urgent queued request is never completed while queued — fine. But `stop()`'s queue drain (line 346-348) completes queued completers WITHOUT checking `isCompleted` — same analysis: queued completers are never completed while in the queue — fine. And `stop()`'s `_inFlight?.completer.complete(null)` — `_inFlight` was set by `_pump` and its completer is uncompleted unless a message completed it, in which case the listener nulled `_inFlight` first — fine. The exhibited defect: `synthesize`'s `req.completer.future` await (line 243) — if `stop()` completes it with null, `path` is null and the function returns null (line 244) — fine. The exhibited defect: `synthesize`'s `if (path == null || cachePath == null) return path;` — if `cachePath` is null (cacheless) but `path` is non-null, it returns the temp path — the temp file is never adopted or cleaned — the orphan-temp leak noted above. The exhibited defect: `synthesize`'s `File(path).renameSync(cachePath)` — `path` is in `tmpDir` and `cachePath` is in `${tmpDir}/kokoro_cache` — same volume — rename is atomic — fine. The exhibited defect: `synthesize`'s `catch (_) { return path; }` on rename failure — returns the temp path — fine. The exhibited defect: `_Req`'s `completer = Completer<String?>()` — fine. The exhibited defect: `_IsolateArgs`'s `cancelBelowAddr` is an int (the raw address) — passing raw pointers across isolates by address is the documented pattern for ffi cancel flags — fine. The exhibited defect: `_isolateMain`'s `Pointer<Int32>.fromAddress(args.cancelBelowAddr)` — fine. The exhibited defect: `voiceIds` maps 28 voices to 0-27 — the doc says "0-27" — fine. The exhibited defect: `isRunning` getter returns `_toIsolate != null` — fine. The exhibited defect: `_dlog` is captured at construction — fine. The exhibited defect: `_start`'s `paths.dataDir` is passed as `dataDir` — fine. The exhibited defect: `_start`'s `getTemporaryDirectory()` is called twice (line 93 and inside `_cachePathFor` line 263) — minor. The exhibited defect: `_start`'s `Isolate.spawn(_isolateMain, _IsolateArgs(...))` — `_IsolateArgs` contains a SendPort — sendable — fine. The exhibited defect: `_isolateMain`'s `sherpa.initBindings()` — fine. The exhibited defect: `_isolateMain`'s `numThreads: 4` hardcoded — fine. The exhibited defect: `_isolateMain`'s `debug: false` — fine. The exhibited defect: `_isolateMain`'s `tts.free()` on dispose — fine. The exhibited defect: `_isolateMain`'s catch around `tts.generateWithCallback` sends `{'seq': seq, 'error': '$e'}` — fine. The exhibited defect: `_isolateMain`'s `if (msg is! Map) continue;` — fine. The exhibited defect: `_isolateMain`'s `msg['seq'] as int` — a malformed message would throw — the only sender is the main isolate — fine. The exhibited defect: `_writeWav`'s `rate` from `audio.sampleRate` — fine. The exhibited defect: `_writeWav`'s `b.setUint32(4, 36 + n*2, ...)` — RIFF size = filesize - 8 = (44 + n*2) - 8 = 36 + n*2 — correct. The exhibited defect: `_writeWav`'s `b.setUint32(28, rate * 2, ...)` — byte rate = rate * block-align(2) — correct-ish (missing *1 for channels... byte rate = sampleRate * blockAlign = rate*2 — correct). The exhibited defect: `_writeWav`'s `b.setUint16(32, 2, ...)` — block align = channels(1) * bits(16)/8 = 2 — correct. The exhibited defect: `_writeWav`'s `b.setUint16(34, 16, ...)` — bits per sample — correct. The exhibited defect: `_writeWav`'s `b.setUint16(22, 1, ...)` — channels = 1 — correct. The exhibited defect: `_writeWav`'s `b.setUint16(20, 1, ...)` — PCM format — correct. The exhibited defect: `_writeWav`'s `b.setUint32(16, 16, ...)` — fmt chunk size — correct. The exhibited defect: `_writeWav`'s `b.setUint32(40, n*2, ...)` — data chunk size — correct. The exhibited defect: `_writeWav`'s `s(0, 'RIFF')` — correct. The exhibited defect: `_writeWav`'s `s(36, 'data')` — correct. The exhibited defect: `_writeWav`'s `s(8, 'WAVEfmt ')` — as analyzed, 'WAVE' at 8-11 and 'fmt ' at 12-15 — correct. The exhibited defect: `_writeWav`'s loop `for (var i = 0; i < n; i++)` with `b.setInt16(44 + i*2, ...)` — fine. The exhibited defect: `_writeWav`'s clamp `v < -32768 ? -32768 : (v > 32767 ? 32767 : v)` — fine. The exhibited defect: `_writeWav`'s `(samples[i] * 32767).round()` — fine. The exhibited defect: `_writeWav`'s `b.buffer.asUint8List()` — fine. The exhibited defect: `_writeWav`'s `File(path).writeAsBytesSync(...)` — synchronous write inside the isolate — fine (background isolate). The exhibited defect: `_writeWav`'s `ByteData(44 + n * 2)` — if n is 0, size 44 — the empty-audio check (line 461) prevents this — fine. The exhibited defect: `_writeWav`'s `void s(int off, String t)` closure captures `b` — fine. The exhibited defect: `_writeWav`'s `t.codeUnitAt(i)` — for non-ASCII would produce wrong bytes but chunk ids are ASCII — fine. The exhibited defect: `_schedulePrune`'s `Isolate.run<int>(() {...})` — the closure returns int in all paths (0 or removed) — fine. The exhibited defect: `_schedulePrune`'s `entries.add((e, stat.modified, stat.size))` — record — fine. The exhibited defect: `_schedulePrune`'s `for (final (file, _, size) in entries)` — record destructuring — fine. The exhibited defect: `_schedulePrune`'s `total -= size` after a successful delete — fine. The exhibited defect: `_schedulePrune`'s `removed++` — fine. The exhibited defect: `_schedulePrune`'s `return removed` — fine. The exhibited defect: `_schedulePrune`'s outer `.then((removed) {...}).catchError((_) {})` — fine. The exhibited defect: `_schedulePrune`'s `DebugLogService.instance.log(LogCategory.tts, ...)` — fine. The exhibited defect: `_cachePathFor`'s `crypto.sha1.convert(utf8.encode('$text|$voice|$speed'))` — SHA-1 used for a cache key, not for security — the crypto checklist flags "MD5/SHA-1 for security" — this is NOT security (cache key naming) — not reported. The exhibited defect: `_cachePathFor`'s key does not include the model/voice-set version — reported above as low. The exhibited defect: `synthesize`'s `unawaited(File(cachePath).setLastModified(DateTime.now()).catchError((_) {}))` — setLastModified may throw on some platforms — swallowed — the LRU mtime is not updated — pruning order degrades to creation order — minor, not reported. The exhibited defect: `synthesize`'s cache-hit path does not re-check `_toIsolate` — a cache hit works even while loading — intended. The exhibited defect: `synthesize`'s `if (_toIsolate == null && _starting == null) return null;` — if the engine failed to start permanently (model missing), every non-cached synthesize returns null — the caller must handle null — fine. The exhibited defect: `synthesize`'s `_nextSeq++` — monotonic — fine. The exhibited defect: `synthesize`'s `_queue.insert(0, req)` for urgent — front of queue — fine. The exhibited defect: `synthesize`'s `_queue.add(req)` for non-urgent — back — fine. The exhibited defect: `_pump`'s `port.send({...})` — a Map with an int seq — sendable — fine. The exhibited defect: `_pump`'s `_queue.removeAt(0)` — fine. The exhibited defect: `_pump`'s `_inFlight = req` — fine. The exhibited defect: `_pump`'s early return when `_queue.isEmpty` — fine. The exhibited defect: `_pump`'s early return when `port == null` — requests queue until ready — fine. The exhibited defect: `_pump`'s early return when `_inFlight != null` — one at a time — fine. The exhibited defect: `stop()`'s `_starting = null` — clears a pending start — a concurrent `ensureStarted` awaiting the old future gets its (possibly false) result — fine. The exhibited defect: `stop()`'s `_epoch++` — the epoch guard — fine. The exhibited defect: `stop()`'s `_cancelBelow` comment — fine. The exhibited defect: `stop()`'s `_queue.clear()` after completing each — fine. The exhibited defect: `stop()`'s `_inFlight = null` — fine. The exhibited defect: `stop()`'s `_isolate = null` — fine. The exhibited defect: `stop()`'s `_sub = null` — fine. The exhibited defect: `stop()`'s `_toIsolate = null` — fine. The exhibited defect: `stop()`'s `_toIsolate?.send(const {'cmd': 'dispose'})` BEFORE `_sub?.cancel()` — the isolate may reply/exit before the cancel — messages arrive on a live subscription and are ignored by the epoch check (line 123) — fine. The exhibited defect: `_start`'s `ready.complete(true)` guarded by `!ready.isCompleted` — fine. The exhibited defect: `_start`'s `ready.complete(false)` on initError guarded — fine. The exhibited defect: `_start`'s onDone's `ready.complete(false)` guarded — fine. The exhibited defect: `_start`'s onDone's `_toIsolate = null` — fine. The exhibited defect: `_start`'s onDone's `_queue.clear()` after completing each — fine. The exhibited defect: `_start`'s onDone's `_inFlight = null` — fine. The exhibited defect: `_start`'s onDone's `if (!ready.isCompleted) ready.complete(false)` — fine. The exhibited defect: `_start`'s listener's `msg is SendPort` branch sets `_toIsolate = msg` — but does NOT call `_pump()` — the ready message arrives after the SendPort, and the ready branch calls... the ready branch (line 127-128) completes `ready` but does NOT call `_pump()` — `_pump()` is called at line 179 after `await ready.future` resolves — fine. The exhibited defect: `_start`'s listener's `msg is Map` branch handles ready/initError/seq — a Map without those keys is ignored — fine. The exhibited defect: `_start`'s listener's `req.completer.complete(msg['path'] as String?)` — a cast of a dynamic — the isolate sends a String — fine. The exhibited defect: `_start`'s listener's `req.seq == msg['seq']` — msg['seq'] is dynamic — equality with int — fine. The exhibited defect: `_start`'s listener's `_pump()` after handling — pumps the next queued request — fine. The exhibited defect: `_start`'s listener's `if (epoch != _epoch) return;` — ignores stale messages — fine. The exhibited defect: `_start`'s `spawned.kill(priority: Isolate.immediate)` on epoch mismatch — immediate kill — fine. The exhibited defect: `_start`'s `fromIsolate.close()` on spawn failure — fine. The exhibited defect: `_start`'s `late final Isolate spawned;` — assigned in try — fine. The exhibited defect: `_start`'s `final epoch = _epoch;` captured at entry — fine. The exhibited defect: `_start`'s `ModelManager.instance.getKokoroPaths()` — context not shown — assumed to return null when not downloaded — the code handles null — fine. The exhibited defect: `_start`'s `tmpDir` from `getTemporaryDirectory()` — fine. The exhibited defect: `_start`'s `_cancelBelow.address` — fine. The exhibited defect: `_start`'s `_IsolateArgs(...)` construction — fine. The exhibited defect: `_start`'s `ReceivePort()` — fine. The exhibited defect: `_start`'s `Completer<bool>()` — fine. The exhibited defect: `_start`'s `whenComplete` clearing — fine. The exhibited defect: `ensureStarted`'s `late final Future<bool> f;` — fine. The exhibited defect: `ensureStarted`'s `return _starting = f;` — fine. The exhibited defect: `ensureStarted`'s `if (_starting != null) return _starting!;` — fine. The exhibited defect: `ensureStarted`'s `if (_toIsolate != null) return Future.value(true);` — fine. The exhibited defect: `KokoroOnnxService._()` private constructor — fine. The exhibited defect: `static final instance` — fine. The exhibited defect: `final _dlog = DebugLogService.instance;` — fine. The exhibited defect: `Isolate? _isolate; SendPort? _toIsolate; StreamSubscription? _sub; Future<bool>? _starting;` — fine. The exhibited defect: `final _queue = <_Req>[]; _Req? _inFlight; var _nextSeq = 0;` — fine. The exhibited defect: `final Pointer<Int32> _cancelBelow = pkg_ffi.calloc<Int32>();` — fine. The exhibited defect: `bool get isRunning => _toIsolate != null;` — fine. The exhibited defect: `static const voiceIds` — fine. The exhibited defect: `var _epoch = 0;` — fine. The exhibited defect: `_isolateMain`'s `sherpa.OfflineTts tts;` declared without initializer then assigned in try — if the catch path returns, `tts` is never used — fine. The exhibited defect: `_isolateMain`'s `var fileSeq = 0;` — fine. The exhibited defect: `_isolateMain`'s `await for` — fine. The exhibited defect: `_isolateMain`'s `commands.close()` on dispose — fine. The exhibited defect: `_isolateMain`'s `return` after dispose — fine. The exhibited defect: `_isolateMain`'s `continue` after aborted/error sends — fine. The exhibited defect: `_isolateMain`'s `abortedMidGeneration` tracking — fine. The exhibited defect: `_isolateMain`'s `callback: (_) { if (seq < cancelBelow.value) { abortedMidGeneration = true; return 0; } return 1; }` — fine. The exhibited defect: `_isolateMain`'s `if (abortedMidGeneration) { send aborted; continue; }` — fine. The exhibited defect: `_isolateMain`'s `_writeWav(path, audio.samples, audio.sampleRate)` — fine. The exhibited defect: `_isolateMain`'s `args.sendPort.send({'seq': seq, 'path': path})` — fine. The exhibited defect: `_isolateMain`'s catch sends error — fine. The exhibited defect: `_isolateMain`'s `if (audio.samples.isEmpty)` — fine. The exhibited defect: `_isolateMain`'s `final path = '${args.tmpDir}/kokoro_onnx_${fileSeq++}.wav';` — fine. The exhibited defect: `_isolateMain`'s `if (seq < cancelBelow.value) { send aborted; continue; }` before generation — fine. The exhibited defect: `_isolateMain`'s `msg['cmd'] == 'dispose'` — fine. The exhibited defect: `_isolateMain`'s `tts.free()` — fine. The exhibited defect: `_isolateMain`'s `args.sendPort.send(const {'ready': true})` — fine. The exhibited defect: `_isolateMain`'s `args.sendPort.send(commands.sendPort)` — fine. The exhibited defect: `_isolateMain`'s `final commands = ReceivePort();` — fine. The exhibited defect: `_isolateMain`'s `args.sendPort.send({'initError': ...}); return;` — fine. The exhibited defect: `_isolateMain`'s catch `e` — fine. The exhibited defect: `_isolateMain`'s `sherpa.OfflineTtsConfig(...)` — fine. The exhibited defect: `_isolateMain`'s `OfflineTtsKokoroModelConfig(model, voices, tokens, dataDir, lexicon)` — fine. The exhibited defect: `_isolateMain`'s comment about no dictDir — fine. The exhibited defect: `_isolateMain`'s `numThreads: 4` — fine. The exhibited defect: `_isolateMain`'s `debug: false` — fine. The exhibited defect: `_isolateMain`'s `OfflineTtsModelConfig(kokoro: ...)` — fine. The exhibited defect: `_isolateMain`'s `OfflineTtsConfig(model: ...)` — fine. The exhibited defect: `_isolateMain`'s `sherpa.OfflineTts(...)` — fine. The exhibited defect: `_isolateMain`'s `sherpa.initBindings()` — fine. The exhibited defect: `_isolateMain`'s `Pointer<Int32>.fromAddress(args.cancelBelowAddr)` — fine. The exhibited defect: `_isolateMain`'s `cancelBelow` — fine. The exhibited defect: `_isolateMain`'s `args.sendPort` — fine. The exhibited defect: `_isolateMain`'s `_IsolateArgs args` — fine. The exhibited defect: `_IsolateArgs`'s const constructor with required named params — fine. The exhibited defect: `_IsolateArgs`'s fields — fine. The exhibited defect: `_Req`'s fields — fine. The exhibited defect: `_Req`'s constructor — fine. The exhibited defect: `_Req`'s `final completer = Completer<String?>();` — fine. The exhibited defect: `stop()`'s doc comment — fine. The exhibited defect: `synthesize`'s doc comments — fine. The exhibited defect: `_schedulePrune`'s doc comment — fine. The exhibited defect: `_cachePathFor`'s doc-less — fine. The exhibited defect: `_pump`'s doc comment — fine. The exhibited defect: `_writeWav`'s top-level — fine. The exhibited defect: `_isolateMain`'s top-level — fine. The exhibited defect: imports — fine. The exhibited defect: the class doc comments — fine.

Now, the crypto/security lens: nothing in these files is cryptographic except the SHA-1 cache key (non-security use) — no keys, tokens, nonces, TLS, JWT. The checklist classes do not match. So findings are limited to correctness/perf defects.

Defensible findings for kokoro_onnx_service.dart:
1. Cache key omits model/voice-set version → stale audio after a model update. Low.
2. Orphaned temp WAVs: when `cachePath` is null (cacheless) or rename fails, the synthesized temp file at `${tmpDir}/kokoro_onnx_N.wav` is returned but never adopted or deleted; repeated cacheless operation leaks files. Low.
3. `synthesize`'s unknown-voice fallback to `af_heart` silently speaks with the wrong voice — direction-of-failure: permissive. For a voice-selection control this resolves to a default rather than an error. Is this a "privacy, authz, consent or precision control"? It's a precision control of sorts (voice identity). I'll report it as low/info. Actually the instruction says a control that resolves to the PERMISSIVE value when state is unavailable is a real finding. Voice selection isn't privacy/authz/consent. I'll skip it or report as info. I'll report as low — it's a correctness defect with a silent fallback.
4. `_pump` sends a request but if the isolate died AFTER `_toIsolate` was set but before the reply, onDone handles it — covered.
5. `stop()`'s `_inFlight?.completer.complete(null)` without an isCompleted guard — analyzed as unreachable in the current flow, but the guard is absent and the same shape appears at line 347 (queue drain) — both rely on the invariant that queued/in-flight completers are never completed elsewhere. The urgent-drop path (line 229) completes queued requests and removes them, preserving the invariant. The listener nulls `_inFlight` before completing. So the invariant holds. But `synthesize`'s urgent path completes queued urgent requests via `removeWhere` — those are removed, so they can't be pumped. Invariant holds. Not reported.
6. `_cancelBelow.value = req.seq` (line 232) is a 32-bit native write from the UI isolate read by the isolate's callback without synchronization — in practice a single aligned int32 store is atomic on all supported targets; the poll-and-abort design tolerates staleness. Not reported.
7. `_schedulePrune`'s `catchError((_) {})` swallows prune errors silently — minor, not reported (the comment says best-effort).
8. `_cachePathFor`'s `Directory(dir).createSync(recursive: true)` on the UI isolate — minor.
9. `synthesize`'s cache-hit `File(cachePath).exists()` + later `renameSync(cachePath)` — a concurrent synthesize of the SAME key could rename while... two identical requests: first cache miss → both queue → both synthesize → both try renameSync to the same cachePath; the second rename of a non-existent temp... each has its own temp path; both rename to the same cachePath — the second rename overwrites — fine. But the cache-hit check happens BEFORE queuing, so two concurrent identical requests both miss and both synthesize — wasted work, not a bug.
10. `_writeWav`'s `s(8, 'WAVEfmt ')` — verified correct.
11. `_start`'s 150s timeout calls `stop()` which tears down and fails pending requests — but `stop()` also increments `_epoch`, so a subsequent `ensureStarted` starts fresh — fine.
12. `_start`'s `if (!ok) { await stop(); }` — `stop()` is also called when the engine failed to start because the model was missing? No — the model-missing path returns false at line 91 BEFORE creating the port/completer — `ready` is never completed and `_starting` resolves false via the `whenComplete`... wait: `_start()` returns false at line 91; `ensureStarted`'s `f = _start().whenComplete(...)` — the future completes false; `_starting` is cleared; `synthesize`'s check `_toIsolate == null && _starting == null` → returns null — fine. But in the model-missing path, `fromIsolate` (created at line 95) — no wait, line 95 is AFTER the model check (line 88-92) — the model check is before `ReceivePort()` — fine, no leak.
13. In the spawn-failure path (line 109-113), `fromIsolate.close()` is called but `ready` is never completed — `_start` returns false — fine, `ready` is local and garbage.
14. In the epoch-mismatch path (line 114-119), `ready` never completes — `_start` returns false — fine.
15. `_start`'s listener is registered (line 122) BEFORE `await ready.future` — fine.
16. `_start`'s `ready.future.timeout(...)` — if the timeout fires, the `ready` completer is still pending; `stop()` doesn't complete it — the pending completer is garbage — fine.
17. `_start`'s onDone fires when the isolate's port stream closes — including after `stop()` cancels `_sub`? A cancelled subscription's onDone does not fire — fine.
18. `_isolateMain`'s dispose path: `tts.free(); commands.close(); return;` — the isolate's `args.sendPort` is still open; the isolate exits after the `await for` returns — fine.
19. `_isolateMain`'s `await for` loop: if the main isolate's `_sub` is cancelled (stop), the main isolate no longer reads replies, but the isolate keeps generating and sending to `args.sendPort` — the send port's messages are buffered; the isolate eventually finishes the current request and waits on the next `commands` message — `stop()` sent dispose, so it frees and exits — fine.
20. `_isolateMain`'s `msg['speed'] as num` — the main isolate sends a double — fine.
21. `_isolateMain`'s `msg['sid'] as int` — fine.
22. `_isolateMain`'s `msg['text'] as String` — fine.
23. `_isolateMain`'s `msg['seq'] as int` —
- [medium] lib/data/services/live_asr_service.dart:139-148 — stop() sends `{'cmd':'dispose'}` then immediately kills the isolate with `Isolate.beforeNextEvent`; the isolate's dispose branch (237-241) frees native recognizer/stream memory only when it processes the message, so the kill can preempt it and leak the native model memory — smallest safe fix: await the isolate's dispose acknowledgement (or a short delay) before `_isolate?.kill`, or have the isolate free resources before closing its port.
- [medium] lib/data/services/live_asr_service.dart:111-117 — when the recognizer fails to initialize (`ok == false`), `_start` calls `await stop()` which increments `_epoch` and clears `_starting`, but `ensureStarted`'s `whenComplete` guard (55) compares `identical(_starting, f)` against the already-nulled field so a subsequent `ensureStarted` can return the stale completed `false` future while `_toIsolate` is null — callers that treat `true` as "running" are fine, but a caller that retries on false may race a concurrent start; smallest safe fix: have `_start` tear down only its own resources (send dispose, cancel sub, kill isolate) instead of delegating to the epoch-bumping `stop()`.
- [low] lib/data/services/live_asr_service.dart:227 — `uid = msg['uid'] as int? ?? uid + 1;` — the isolate-side `uid` is overwritten from the main-side utterance id, but `decodeAndReport` (206) always sends `uid` (the local var), so after a `start` command with a new uid the partials are tagged correctly; however if `msg['uid']` is absent the fallback `uid + 1` desyncs the tag from `_uid` on the main side (104), silently dropping every partial for that utterance — smallest safe fix: default to the previous `uid` (`?? uid`) or assert the field is present.
- [low] lib/data/services/live_asr_service.dart:135 — `endUtterance` sends `{'cmd':'end'}` but the isolate's `end` case (229-235) feeds a fixed 12800-sample silence buffer regardless of the actual sample rate/format contract; if the caller's stream is not exactly 16 kHz mono this injects a wrong-length flush — smallest safe fix: compute the silence length from the configured sample rate (16000 * 0.8) instead of a magic 12800 constant, or document the invariant next to the constant.
- [low] lib/data/services/media_control_service.dart:78-94 — `_handleNativeCall` casts `call.arguments as String` without a null/type guard; a native plugin sending a map or null throws a cast exception inside the method-call handler, which the framework swallows and logs, leaving the AirPods control silently dead — smallest safe fix: `final command = call.arguments as String?; if (command == null) return;` before the switch.
- [low] lib/data/services/media_control_service.dart:82-89 — the `jumpBack` case falls through into the `playPause` handler body (Dart 3 switch-case without `break`/`return` is a compile error, so this must be intentional fallthrough via shared body); as written, a `jumpBack` command invokes `onPlayPause` and never `onJumpBack`, misrouting the previous-track gesture — smallest safe fix: give `case 'jumpBack': onJumpBack?.call(); break;` its own body (if the shared body is intentional, name the shared handler explicitly).
- [info] lib/data/services/live_asr_service.dart:62 — `ModelDownloadService.instance.getLiveAsrModelDir()` returns the model directory path; the on-disk model files (encoder/decoder/joiner/tokens .onnx/.txt) are third-party ML weights, not secrets — no key material or credential exposure is exhibited in the inlined code; verify the download service does not log the dir path with user identifiers (not visible here).

## Coverage
lib/data/services/live_asr_service.dart — findings: 5
lib/data/services/media_control_service.dart — findings: 2
- [medium] lib/data/services/model_download_service.dart:822-823 — `_freeDiskSpaceBytes` sanity check `bavail > blocks` treats a volume whose available blocks legitimately exceed total blocks (e.g. btrfs/overlayfs or a mis-reported statvfs) as invalid and returns null, but the real defect is the opposite direction: when the layout sanity check passes on garbage (frsize/blocks/bavail read from wrong offsets on a non-Darwin statvfs struct), the returned `bavail * frsize` can be a huge bogus number that makes the free-space preflight pass — reachability: operator/local user only (preflight affects download start, not security) — fix: additionally bound the result (e.g. reject `bavail * frsize` > 2^50 or reject when `frsize` is not a plausible power-of-two block size) before returning it.
- [medium] lib/data/services/model_download_service.dart:803-812 — `utf8.encode(path)` then `malloc(pathBytes.length + 1)` and `setAll(0, pathBytes)` writes the path bytes but the NUL terminator slot is only zeroed via `..[pathBytes.length] = 0` on a list of length `pathBytes.length + 1` — if `pathBytes.length` is 0 (empty path) `setAll` with an empty source is fine but `..[0] = 0` writes the terminator correctly; however if the path contains characters that expand to more UTF-8 bytes than the Dart string length this is already handled by utf8.encode — the actual defect is that `buf.asTypedList(bufBytes).fillRange(0, bufBytes, 0)` zeroes the statvfs buffer but `statvfs` is called with `cPath`/`buf` pointers whose allocation size (`pathBytes.length + 1`, `bufBytes`) is not validated against the Darwin `struct statvfs` size assumption (64 B) — reachability: operator/local tooling only — fix: assert `bufBytes >= sizeof(struct statvfs)` for the target ABI before calling, or document the ABI pin next to the constant.
- [low] lib/data/services/model_download_service.dart:778-783 — FFI typedefs declare `_StatvfsNative = Int32 Function(...)` (returning Int32) while the C `statvfs(3)` returns `int` — this is correct on Darwin ABIs, but the same typedef is used for the Dart-side lookup `_StatvfsDart = int Function(...)`; if this file is ever compiled for a 64-bit Linux/bionic target the struct offsets at 815-819 (frsize at offset 8 as Uint64, fsblkcnt at offset 16 as Uint32) are wrong for bionic (fsblkcnt_t is 64-bit there, as the comment at 790-791 admits) and the sanity check at 822 would then pass on garbage — reachability: offline tooling/CI only — fix: guard the whole function with `Platform.isIOS || Platform.isMacOS` at the typedef level or move the Darwin-only offsets behind a platform check (the runtime check at 796 already does this, so treat as hardening note).
- [info] lib/data/services/model_download_service.dart:122-123,143-144,165-166,179-180,193-194,207-208 — pinned SHA-256 hashes of release/HF model artifacts are embedded as source constants; verify these are intended public artifact hashes and not secrets (they are content addresses, not credentials — no action needed beyond confirming no private URL/token is co-located).
- [low] lib/data/services/model_manager.dart:43 — `_kokoroArchiveSha256` is declared as a 64-hex-digit constant but the shown value `4cafe1c49bf4b0a7f9c2fab9f2b010b05544dde2b28b66e3211e832308a1a1f9` is 63 hex digits (odd length), so `crypto.sha256` comparison at model_download_service-style verification (here: lines 205-215) would never match a real digest — every download of the Kokoro archive fails verification and is deleted, making `downloadKokoro` permanently unable to complete — reachability: any user triggering the Kokoro download (feature-breaking, not security) — fix: correct the constant to the true 64-hex-digit SHA-256 of the pinned archive (or drop the pin and rely on the size check) and add a length assertion `expectedSha256.length == 64` at parse time.
- [medium] lib/data/services/model_manager.dart:242-268 — `_extractArchiveStreaming` runs in an isolate via `compute` and creates a temp dir with `createTempSync('lineguide_extract')`; on any exception inside the try (e.g. corrupt archive) the `finally` deletes the temp dir, but the `InputFileStream`/`OutputFileStream` at 249-253 are never closed on the exception path (`input.closeSync()`/`output.closeSync()` only run on success), leaking open file handles in the isolate and, on some platforms, keeping the temp tar file locked so the recursive delete at 265 silently fails (caught and ignored at 266) — repeated failed extractions accumulate locked temp dirs — reachability: operator/local user retrying a bad download — fix: wrap the stream usage in its own try/finally that closes both streams before the outer finally deletes the temp dir.
- [low] lib/data/services/model_manager.dart:185-186 — stale-archive cleanup `if (await File(archivePath).exists()) await File(archivePath).delete();` is inside `try { } catch (_) {}` but the delete is not awaited-guarded against the file being a directory; if a previous run left a directory at that path the delete throws and is swallowed, then `_downloadFile` writes to `archivePath` and fails — reachability: operator — fix: use `File`/`Directory` type check or `FileSystemEntity` handling before delete.
- [low] lib/data/services/model_manager.dart:327-328 — recursive redirect following passes `redirectsLeft: redirectsLeft - 1` but the guard at 322 checks `redirectsLeft <= 0` before recursing; with the initial value 5 this allows 6 hops (5 → recurse with 4,4 → 3, ... 0 → recurse with -1 would be blocked only if the check ran before the decrement of the *current* level) — actually the check `redirectsLeft <= 0` throws when the counter has already been exhausted, so the effective bound is 5 redirects, matching the docstring; no off-by-one — no action needed (recorded for completeness, not a defect).
- [medium] lib/data/services/model_download_service.dart:596-616 — the hand-rolled redirect loop in `_dartDownload` drains the redirect response body via `await res.drain<void>()` only on the redirect path (603) but on the success path (statusCode 200) the response body is consumed by the `body` stream at 632-654; if the server returns a non-200, non-redirect status (e.g. 416/503) the code falls through to `throw HttpException` at 617-618 without draining the response — the undrained response keeps the socket open and can exhaust `HttpClient` connection pools on repeated failures — reachability: operator/local user — fix: `await res.drain<void>()` (or `res.detach()`/close) in a finally before throwing on non-200 statuses.
- [low] lib/data/services/model_download_service.dart:626-658 — `tmpFile.openWrite()` sink is closed in a `finally` (656-658) but if `sink.add(chunk)` throws mid-stream (e.g. disk full) the `await for` loop at 652 exits via exception and the `finally` closes the sink; however `tmpFile.rename(outPath)` at 659 is outside the try/finally and runs even when the sink close surfaced an error only if no exception propagated — since an exception propagates, rename is skipped correctly; but on the *success* path the rename happens before `_verifyDownload` deletes bad files, leaving the verified-bad file at `outPath` if `file.delete()` at 418 fails (caught and logged at 420-421) — the bad file then remains on disk and `fileProblem` will keep reporting it — reachability: operator — fix: on verification failure also reset the state to `error` (already done at 663-667) and surface the leftover-file condition in the error message.
- [medium] lib/data/services/model_download_service.dart:331-333 — truncation floor `actual < model.sizeBytes ~/ 2` applies only when `exact == null`; for models with `exactSizeBytes` set the size check at 323 is exact, but for models where `sha256` is set and `exactSizeBytes` is null (none in the current table — all pinned models set both) the floor would allow a 50%-truncated file to pass the size gate and then be caught only by the SHA-256 check; if a future model omits `sha256` while having a mutable upstream, a 50% truncation silently passes — reachability: operator/local — fix: require either `exactSizeBytes` or `sha256` for every `AiModel` (constructor assertion) so the truncation floor can never be the only integrity gate.
- [low] lib/data/services/model_download_service.dart:406 — `crypto.sha256.bind(file.openRead()).first` computes the digest of the *first* stream chunk group; `.first` on a `Stream<Digest>` from `sha256.bind` yields the digest of the whole stream only because `bind` emits a single Digest element — correct; but if the file is a directory or unreadable, `openRead()` throws inside the try at 404 and is caught at 411 producing `problem = 'sha256 could not be computed'` — fine; no defect (recorded for completeness).
- [low] lib/data/services/model_download_service.dart:341 — `refreshDownloadedStatus` skips re-verifying a model whose state is `downloading`, but if a previous run crashed mid-download the stale state map entry is gone (in-memory only), so no stale-skip exists across restarts — no defect (recorded for completeness).
- [medium] lib/data/services/model_download_service.dart:216-235 — `_states` is a plain in-memory map with no persistence; `getState` falls back to `const ModelDownloadState()` (notDownloaded) whenever the key is missing, and `refreshDownloadedStatus` only sets `downloaded` when `fileProblem` returns null — the *direction of failure* is safe (missing state → not downloaded → re-download), but `delete(modelId)` at 703-715 resets state to default even when the model id is unknown, silently reporting "not downloaded" for a model that may still exist on disk under a retired id — reachability: operator — fix: only reset state when `model != null`, or scan the models dir for orphaned files.
- [low] lib/data/services/model_download_service.dart:735-744 — `_docsPath` is cached forever (`??=`); if the app's documents directory changes (rare, e.g. container migration on iOS) the cached path goes stale and every `_filePath` resolves to the old location, causing `file.existsSync()` false-negatives and re-downloads — reachability: operator — fix: clear `_docsPath` on app lifecycle resume/migration signal, or re-resolve once per `refreshDownloadedStatus` call.
- [low] lib/data/services/model_download_service.dart:504-507 — `.tmp` cleanup deletes `File('$outPath.tmp')` but the native downloader (channel `startDownload`) may write its temp file elsewhere (unknown native convention — assumption: the plugin uses the same `.tmp` suffix); if it does not, the cleanup is a no-op and stale native temp files accumulate — reachability: operator — fix: confirm the native side's temp naming or enumerate the destination directory for leftovers.
- [low] lib/data/services/model_manager.dart:29-31 — `modelsDir` caches `_modelsDir` forever and creates the directory eagerly; same staleness class as the downloader's `_docsPath` — reachability: operator — fix: same as above (re-resolve on migration signal).
- [low] lib/data/services/model_manager.dart:157-163 — `clearCache` deletes the models dir recursively then recreates it, but `ModelDownloadService`'s `_docsPath` cache (model_download_service.dart:735) still points at the old documents root — the *files* are gone so this is consistent; no defect (recorded for completeness).
- [low] lib/data/services/model_manager.dart:130-137 — `isAllReady` on Android requires `isKokoroReady() && isLiveAsrReady()`, but `downloadAll` (144-154) downloads Kokoro via `downloadKokoro` and live ASR only on Android — on non-Android `downloadAll` never downloads the live ASR group while `isAllReady` on non-Android only checks Kokoro — consistent; no defect (recorded for completeness).
- [low] lib/data/services/model_manager.dart:109-113 — `downloadKokoro` calls `onProgress?.call('kokoro', 1.0)` and returns early when already ready, but never calls `onProgress` with a completion signal on the download path (the archive path reports via `_downloadFile`'s callback at 189-192 with `progress * 0.8`), so a caller waiting for a 1.0 progress event on a fresh download never receives one — reachability: operator — fix: call `onProgress?.call('$_kokoroModelName.tar.bz2', 1.0)` after `_downloadAndExtractArchive` completes.
- [low] lib/data/services/model_manager.dart:196-199 — archive size floor `archiveSize < 1000` is a magic constant; a legitimately tiny archive (none expected here) would be rejected — recorded for completeness, no defect for the pinned 180 MB archive.
- [info] lib/data/services/model_manager.dart:40-44 — release URL and model names are public identifiers, not secrets; no action needed.
- [low] lib/data/services/debug_log_service.dart — context file, not under review; no findings emitted (see Coverage note).

## Coverage
lib/data/services/model_download_service.dart — findings: 9
lib/data/services/model_manager.dart — findings: 6
lib/data/services/debug_log_service.dart — NOT REVIEWED
lib/data/models/script_models.dart — NOT REVIEWED
lib/core/toast.dart — NOT REVIEWED
lib/providers/production_providers.dart — NOT REVIEWED
- [medium] lib/data/services/ocr_confidence_service.dart:247-274 — scoreScript mutates shared singleton state (whitelist, cache, checker) without synchronization — if the import service ever runs scoring on a background isolate or two scripts concurrently (the doc comment at 93-94 mentions isolate-based scoring), the whitelist built for one script silently applies to the next, producing wrong review verdicts — guard with a lock or per-call local state
- [medium] lib/data/services/ocr_confidence_service.dart:117-149 — _buildWhitelist adds every word appearing ≥3 times to the whitelist, and _isValidWord treats whitelisted words as valid without consulting the dictionary — a repeated OCR error (e.g. a mis-read proper noun appearing 3+ times) is marked "correct", inflating dictNew and pushing genuinely-bad lines into the ok bucket — require the word to also pass the dictionary check or cap whitelist additions to known-name parts
- [low] lib/data/services/ocr_confidence_service.dart:46 — _allCaps regex `^[A-Z][A-Z]+$` requires at least two letters, so single-letter ALL-CAPS tokens ("A", "I") are not dropped by the caps filter — they then fall through to the spell checker, wasting work and possibly counting a speaker-initial as a valid word — change to `^[A-Z]+$` or drop length-1 uppercase tokens in _tokenize
- [low] lib/data/services/ocr_confidence_service.dart:47 — _edgeApostrophes `^['‘’]+|['‘’]+\$` strips apostrophes only at the very start/end of the token, but the split regex already treats curly quotes as separators, so the strip is mostly redundant; however a token like `’twas` keeps its leading apostrophe and is scored as a distinct word, fragmenting the cache — normalize by folding the leading apostrophe case explicitly
- [low] lib/data/services/ocr_confidence_service.dart:193-197 — _isValidWord dereferences `_checker!` without a null guard; if dispose() ran (line 104-111) and scoring is invoked again without _ensureLoaded re-registering the language (registerLan is only called in _ensureLoaded at 66-71, which early-returns only when _checker != null — after dispose it re-runs, so this is safe only if dispose is never called concurrently) — add an explicit `_checker == null` re-init path or document dispose as terminal
- [info] lib/data/services/ocr_confidence_service.dart:88 — debugPrint of the raw exception could leak asset-path internals into release logs on some platforms — verify it is not committed / consider a static message
- [low] lib/data/services/ocr_highlight_matcher.dart:32 — _cueRe requires `^[A-Z][A-Z .,&']{1,30}\.\s+` — a cue with a digit or lowercase remainder ("MR. 1ST. ") or a cue longer than 30 chars is not stripped, so stripCue returns the raw line and containment scoring compares against the unstripped cue, skewing bestMatch scores — widen the character class or bound
- [low] lib/data/services/ocr_highlight_matcher.dart:44-56 — _containsWord loops `from = i + 1` after a failed boundary check but never advances past a partial match ending inside a word, so a needle found at a non-boundary position is retried from i+1 and can match a later true occurrence — correct but quadratic on long pages; fine at current sizes, note only
- [low] lib/data/services/ocr_highlight_matcher.dart:93 — after the short-target loop, `bestExcess <= 25` accepts a best line whose body is up to 25 chars longer than the target even when no exact hit was found, returning a rect for a line that merely contains the target — for a 2-3 char target this can highlight the wrong line — tighten the excess bound or require containment confidence
- [low] lib/data/services/ocr_highlight_matcher.dart:114 — `bestScore < 0.45` uses a hard-coded threshold that bestMatch exposes as a parameter (minScore, line 161) — the two thresholds can drift; share the constant
- [low] lib/data/services/ocr_highlight_matcher.dart:141 — extension loop caps `rects.length < 4` but the initial rects slice (line 129-131) can already exceed 4 when startIdx..bestIdx spans more lines, making the cap ineffective for the first segment — clamp the initial slice too if the cap is intended
- [info] lib/data/services/ocr_highlight_matcher.dart:158-193 — bestMatch returns the earliest strong match (index i) but the weak path returns bestIdx which may be later than the strong-scan start; consistent with the documented locality-first policy, no defect exhibited

## Coverage
lib/data/services/ocr_confidence_service.dart — findings: 6
lib/data/services/ocr_highlight_matcher.dart — findings: 6
- [medium] ios/Runner/AppleSttPlugin.swift:619-628 — rmsLevel reads only channel 0 (`channelData[0]`) and ignores other channels, so stereo/2-channel tap buffers report level from one channel only — level metering and Dart-side endpointing see a wrong (possibly silent) channel's energy and can mis-trigger or stall endpoint detection — compute RMS across all channels (`for ch in 0..<channelCount` accumulating per-frame) or document single-channel assumption at the tap format.
- [medium] ios/Runner/AppleSttPlugin.swift:297-321 — tapBlock captures `self?.recognitionRequest?.append(buffer)` on the render thread while `stopCurrentSession()` (line 632-633) nils `recognitionRequest` on the main thread; a buffer arriving between the nil-out and `audioEngine.stop()` still appends to a request that `endAudio()` was already called on, and the tap block also keeps running after `removeTap` is deferred until `isRunning` check — race can crash or feed audio to a dead request — nil the request and remove the tap inside the same `audioFileQueue`/main-thread barrier, or guard `append` with a lock/flag set before `endAudio()`.
- [medium] ios/Runner/AppleSttPlugin.swift:303-313 — the tap block copies the buffer and enqueues the write, but `audioFile` is read (`self.audioFile != nil`) on the render thread while `startRecording`/`stopRecording` mutate it on `audioFileQueue` (lines 395, 400, 420) — unsynchronized read/write of `audioFile` is a data race that can write into a closed/nil file or skip a valid buffer — read `audioFile` inside the `audioFileQueue.async` block (capture the flag there) instead of testing it on the render thread.
- [low] ios/Runner/AppleSttPlugin.swift:414 — `durationMs` computed from `recordingStartTime ?? Date()` silently substitutes "now" when the start time is missing, yielding ~0ms duration for a valid take — propagate the failure (return nil) or assert the invariant instead of masking it with a fallback that produces a wrong-but-plausible value.
- [low] ios/Runner/AppleSttPlugin.swift:586-588 — `peakRMS * 0.05` threshold with `if threshold < 10 { return nil }` treats a quiet-but-real recording (peak RMS < 200 in int16 scale) as "entire recording is silent" and skips trimming; combined with the fixed 5%-of-peak rule this is a precision fallback that resolves to the permissive (no-trim) value when signal is low — use an absolute noise floor plus relative threshold, or clamp the minimum to a documented floor rather than discarding the whole trim.
- [low] ios/Runner/AppleSttPlugin.swift:152-154 — macOS mic permission request result is logged but never stored; `authorized` only reflects SFSpeechRecognizer authorization, so on macOS the engine tap can be fed silence if the user denies mic access while granting speech recognition, and `listen` proceeds (line 194 guard checks only `authorized`/`isAvailable`) — capture the mic grant and fail `listen` with an explicit error when mic access is denied.
- [info] ios/Runner/AppleSttPlugin.swift:94,108,146,162,165,168,171,240,273,351,354,377,396,425,431,447,472,481,489 — NSLog calls log file paths, locale strings, and error descriptions; none of the logged values are secrets, but verify none of the surrounding log lines in the shipped build leak user audio paths into system logs (system log aggregation is on-device only here) — no action needed beyond confirming release-log policy.

## Coverage
ios/Runner/AppDelegate.swift — clean
ios/Runner/AppleSttPlugin.swift — findings: 7
- [medium] ios/Runner/BackgroundDownloadPlugin.swift:68-75,77-82 — UserDefaults read-modify-write of the whole records dictionary from multiple call sites without any synchronization — concurrent startDownload/cancelDownload (or a delegate callback racing a method call) can drop a just-persisted record, so a completed post-relaunch download is silently discarded (the exact failure the persistence was added to prevent) — guard the read-modify-write with a serial queue/lock and route all record mutations through it.
- [medium] ios/Runner/BackgroundDownloadPlugin.swift:118-126 — startDownload cancels an in-flight task but leaves its persisted record and resume blob in place, then immediately overwrites the record — the cancelled task's didCompleteWithError fires with resume data and writes a stale resume blob for the new download's destination, so the "fresh" download can resume from a partial transfer of a different request — remove the record and resume blob before starting the new task (cancelDownload path at 134-141 already does this; mirror it here).
- [medium] ios/Runner/BackgroundDownloadPlugin.swift:150-163 — startTask reads resume data from disk and resumes from it without validating that the resume data belongs to this download/URL — a stale or cross-model blob (persisted under destinationPath-derived key) silently continues a transfer against different request state — key resume data by modelId (or embed/verify the URL) before resuming.
- [low] ios/Runner/BackgroundDownloadPlugin.swift:113-115 — `try?` on createDirectory swallows directory-creation failure; the later moveItem at 206 then fails with "Failed to save file" and the multi-GB download is dropped after completion — surface the error or at least log it so the failure is diagnosable.
- [low] ios/Runner/BackgroundDownloadPlugin.swift:210-211 — attributesOfItem size is fetched with `try?` defaulting to 0 and the MB log divides that; on a failed stat the log reports 0 MB — harmless to correctness but the same `try?`-to-permissive-default shape as above; log the stat error instead.
- [info] ios/Runner/BackgroundDownloadPlugin.swift:296-297 — backgroundSessionCompletionHandler is read/cleared on the main thread via a cast to AppDelegate; if the handler is set on another queue or AppDelegate type differs, the completion handler is never called and the background session leaks its snapshot — verify the AppDelegate property exists and is set before this plugin is constructed (assumption: AppDelegate not inlined in this batch).
- [info] ios/Runner/AudioAnalysisPlugin.swift:44,56,63 — every decode failure path returns `result(nil)` which the Dart side treats as "no normalization" — a transient decode failure silently disables volume normalization rather than surfacing an error; acceptable per the documented contract, noted only because the fallback resolves to the permissive value.

## Coverage
ios/Runner/AudioAnalysisPlugin.swift — findings: 1
ios/Runner/BackgroundDownloadPlugin.swift — findings: 5
- [medium] ios/Runner/ContactPickerPlugin.swift:83-84 — `pendingResult` is captured and cleared inside the delegate callback, but the picker is presented without `picker.delegate` being retained by the plugin instance beyond the presentation; if the user cancels via the system's dismissal path that does not invoke `contactPickerDidCancel` (e.g., the picker is dismissed by another presented controller or the app is backgrounded), `pendingResult` is never invoked and the Dart-side method call never completes — concrete consequence: the Flutter method channel call for `pickContact` hangs forever, leaking the pending result and leaving the Dart future unresolved — smallest safe fix: also implement `contactPickerViewController(_:dismissedByCancel:)` (or the picker's `modalPresentationStyle`-driven dismissal delegate hooks) to flush `pendingResult` with a cancellation error, and nil out `pendingResult` in every terminal path.
- [medium] ios/Runner/ContactPickerPlugin.swift:35-36 — the `CNContactPickerViewController` is created as a local variable and presented immediately; nothing retains it after `pickContact` returns, and the delegate methods on `self` are only reachable while the picker is alive — concrete consequence: under a fast dismissal or if ARC drops the picker before the delegate fires (observed in practice when the presenting VC is deallocated), the result callback never fires — smallest safe fix: hold the picker in a strong property on the plugin (e.g., `private var activePicker: CNContactPickerViewController?`) and clear it in the delegate completion paths.
- [low] ios/Runner/ContactPickerPlugin.swift:19-21 — `register(with:)` is a no-op that only comments "Registration handled in AppDelegate"; if the AppDelegate does not construct the plugin with the binary messenger, the method channel at line 12-16 is never created and every contacts call silently returns `NotImplemented` — concrete consequence: the feature appears broken at runtime with no compile-time signal — smallest safe fix: perform the channel registration inside `register(with:)` using `registrar.messenger()` so the plugin is self-contained, or assert the AppDelegate wiring in a startup test.
- [medium] ios/Runner/KokoroMLXPlugin.swift:59-62 — the background-task expiration handler calls `UIApplication.shared.endBackgroundTask(bgTask)` using the `bgTask` captured at handler-registration time, but the handler runs while `bgTask` may still be `.invalid` if `beginBackgroundTask` returned `.invalid` (e.g., background task APIs unavailable); calling `endBackgroundTask` with `.invalid` is a no-op but the handler also sets the outer `bgTask` to `.invalid` from a non-main-actor closure, racing with the `MainActor.run` block at lines 80-84 — concrete consequence: a double `endBackgroundTask` or an end on an identifier already expired can throw an Objective-C exception / mis-account the background time, crashing the app during backgrounding mid-synthesis — smallest safe fix: guard the expiration handler with `if bgTask != .invalid` before calling `endBackgroundTask`, and only mutate `bgTask` on the main actor.
- [medium] ios/Runner/KokoroMLXPlugin.swift:51-85 — the `synthesize` case launches a `Task { ... }` from a non-async context and the `result` callback is invoked via `DispatchQueue.main.async`, but if the Task body itself throws before reaching the `do/catch` (e.g., `MainActor.run` at line 58 rethrows after the app is suspended), neither `result(audioPath)` nor the error result is ever called — concrete consequence: the Dart-side `synthesize` future never completes when the background task cannot be started, leaving the UI blocked — smallest safe fix: wrap the entire Task body in a single `do/catch` that always terminates with exactly one `result(...)` call, or add a `defer`-style guarantee that the first result wins and later ones are ignored.
- [low] ios/Runner/KokoroMLXPlugin.swift:43-49 — `args["voice"]` and `args["speed"]` are read with `as?` casts and silent defaults (`"af_heart"`, `1.0`) without validating that `speed` is within the model's supported range; an out-of-range speed (e.g., `0` or `10`) is forwarded straight to `kokoroService.synthesize` — concrete consequence: the native side may produce garbage audio or throw deep inside the service with an unrelated error message instead of a clear `INVALID_ARGS` — smallest safe fix: clamp/validate `speed` (e.g., `0.5...2.0`) and reject unknown voices with an `INVALID_ARGS` result before starting the Task.
- [low] ios/Runner/KokoroMLXPlugin.swift:96-98 — `unloadModel` calls `kokoroService.unloadModel()` and unconditionally returns `true` without checking whether an unload actually succeeded or whether synthesis is in flight — concrete consequence: a Dart caller believes the model was unloaded while an in-flight synthesis still holds GPU memory, or the unload silently failed and memory is not reclaimed — smallest safe fix: have `unloadModel()` return a `Bool`/throw and surface failure via a `FlutterError` instead of a hardcoded `true`.
- [info] ios/Runner/KokoroMLXPlugin.swift:10 — the channel name `com.lineguide/kokoro_mlx` differs from the contacts channel's `com.tiltastech.castcircle/contacts` prefix, suggesting one of the two channel names is stale relative to the app's bundle identifier; verify which prefix is canonical before shipping — concrete consequence: a mismatched channel name means the Dart side must use the exact same string or calls silently fail — smallest safe fix: confirm the Dart-side channel constant matches `com.lineguide/kokoro_mlx` (or rename to the app's prefix) and add a shared constant.

## Coverage
ios/Runner/ContactPickerPlugin.swift — findings: 3
ios/Runner/KokoroMLXPlugin.swift — findings: 5
- [medium] ios/Runner/KokoroMLXService.swift:336-339 — `pruneScheduled` is checked-and-set non-atomically from arbitrary caller threads (`pruneCacheIfNeeded()` is invoked from `synthesize()` before the serial queue hop, so two concurrent synthesize calls can both pass the `guard !Self.pruneScheduled` check) — a double-prune pass can run concurrently, racing `FileManager` removals against the cache-hit path and double-counting/removing entries; fix by guarding the flag with an `NSLock` (or `os_unfair_lock`) around the check-and-set, or by using `DispatchQueue.once`-style atomic token semantics.
- [medium] ios/Runner/KokoroMLXService.swift:171-177 — cache-hit path returns the cached WAV without re-checking the backgrounded state or the generation counter, so a line synthesized in a previous session replays instantly even when the app is now backgrounded and GPU work would be killed — this is intentional for cache hits (no GPU work), but the `setAttributes` mutation of the file's modification date happens on the caller thread while the prune pass may concurrently enumerate/delete the same directory, so a file being touched can be removed mid-prune and the caller still returns its path; fix by performing the touch-and-return inside the `synthQueue` block (or by taking the same lock the prune uses) so touch and prune are serialized.
- [low] ios/Runner/KokoroMLXService.swift:185-191 — backgrounded check reads `UIApplication.shared.applicationState` via `MainActor.run` from a non-main actor context; if `synthesize()` is called from a background thread before the app has ever foregrounded (e.g. a prefetch kicked at launch while the app is still in `background` state per the seeded `_isBackgrounded`), the check is correct, but the static `_isBackgrounded` seed at init (lines 76-82) races the async main-thread read — a synthesis submitted between init and the async seed completing uses a stale `false` backgrounded value in the queued block's `Self.isBackgrounded` re-check (line 223), submitting GPU work while backgrounded and risking the iOS kill the comment warns about; fix by making the init seed synchronous on the main thread when already on it (it already is — but the `else` branch's async read can complete after a queued synthesis's re-check), or by having the queued block re-read the live application state instead of the seeded static.
- [low] ios/Runner/KokoroMLXService.swift:200 — language is derived solely from the voice's first character (`voice.hasPrefix("a")`), so any voice whose prefix is not "a" (e.g. a future `jp_*`, `ko_*`, or `zh_*` Kokoro voice added to `availableVoices`) silently synthesizes with `.enGB` and wrong phoneme/lexicon settings; fix by mapping the prefix explicitly (`b` → `.enGB`, `a` → `.enUS`) and throwing for unknown prefixes rather than defaulting.
- [low] ios/Runner/KokoroMLXService.swift:284-291 — `vDSP_vclip` clamps to `[-1.0, 1.0]` then `vDSP_vsmul` scales by `Int16.max` (32767), but the two `vDSP` calls write into `scaled` with stride 1 while `vsmul`'s length parameter is `vDSP_Length(samples.count)` — correct — however `scale` is declared `var` and passed as `&scale` (an in-out `Float`), which is fine, yet the intermediate `scaled` buffer is reused as both the vclip destination and the vsmul source/destination (aliasing input and output of `vsmul`), which is legal for vDSP but the `pcm` conversion via `vDSP_vfix16` assumes the scaled values fit `Int16` — they do (≤32767), so this is clean; no defect beyond the aliasing note, which vDSP permits for element-wise ops. No finding.
- [info] ios/Runner/KokoroMLXService.swift:322-331 — cache key is a SHA-256 of `text|voice|speed` with no length or content-type bound; two different texts that produce the same UTF-8 byte string after NUL-truncation (Swift `String` cannot contain NUL, so collision is cryptographically negligible) — no practical defect; noted only that the digest is unkeyed SHA-256 over attacker-influenced text, which is fine for a local cache filename (no security boundary crosses it).
- [low] ios/Runner/KokoroVendored/Albert/AlbertEmbeddings.swift:15-17 — the three embedding weight lookups use `!` force-unwrap on dictionary subscripts (`weights["bert.embeddings.word_embeddings.weight"]!`); a weights file missing any of these keys crashes with a fatal trap at model load rather than surfacing a typed error that the Dart side could map to a re-download prompt (unlike the corrupt-model path in KokoroMLXService.swift:113-118 which deletes and rethrows); fix by using `guard let ... else throw` against a typed `KokoroError.modelCorrupt`-style case so a truncated safetensors file is handled like the rest of the load path.
- [low] ios/Runner/KokoroVendored/Albert/AlbertEmbeddings.swift:22-24 — the shape guard `fatalError`s on mismatch instead of throwing, so a weights file with the right keys but wrong LayerNorm dimensions kills the process at load; same class as the force-unwraps above — convert to a thrown typed error so the caller's existing corrupt-model recovery (delete + re-download) can engage.
- [low] ios/Runner/KokoroVendored/Albert/AlbertEmbeddings.swift:29-30 — `layerNorm.bias![0...]` and `layerNorm.weight![0...]` force-unwrap optional MLXArray parameters; if the LayerNorm module was constructed without bias/weight (possible when `LayerNorm(dimensions:eps:)` builds lazily or with `withBias: false` — assumed, since the initializer's signature is not visible in this batch), the subscript crashes; fix by asserting the optionals are non-nil once at init and reusing the unwrapped values, or by throwing a typed error.
- [low] ios/Runner/KokoroVendored/Albert/AlbertEmbeddings.swift:44 — `MLX.expandedDimensions(MLXArray(0 ..< seqLength), axes: [0])` builds position ids from `seqLength = inputIds.shape[1]`, but if `inputIds` is 1-D (shape `[seq]` rather than `[batch, seq]`), `shape[1]` traps with an index-out-of-bounds; the caller in this batch is not visible, so this is conditional on the KokoroSwift caller always passing 2-D input — fix by guarding `inputIds.shape.count >= 2` and falling back to `shape[0]` for 1-D input, or by documenting the 2-D requirement and asserting it.

## Coverage
ios/Runner/KokoroMLXService.swift — findings: 5
ios/Runner/KokoroVendored/Albert/AlbertEmbeddings.swift — findings: 5
- [medium] ios/Runner/KokoroVendored/Albert/AlbertEncoder.swift:15-16,32 — force-unwrapped weight lookups and an integer division `groupIdx` that silently misroutes when `numHiddenLayers` is not an exact multiple of `numHiddenGroups` — a malformed or truncated weight bundle crashes the app at init, and a misconfigured model silently produces wrong group routing instead of failing — guard the weight keys with a descriptive error and assert `config.numHiddenLayers % config.numHiddenGroups == 0` before the loop.
- [medium] ios/Runner/KokoroVendored/Albert/AlbertLayer.swift:16-19,22-23 — force-unwrapped weight dictionary lookups for `ffn`, `ffn_output`, and the layer-norm weights/biases crash with no diagnostic on a missing or misnamed key — a truncated or renamed checkpoint aborts the whole TTS pipeline with an opaque `nil` crash — replace `!` with a `guard let ... else fatalError("missing key ...")` naming the exact key.
- [low] ios/Runner/KokoroVendored/Albert/AlbertLayer.swift:25-27 — `fatalError` on shape mismatch is reachable only from a bad model bundle, but it is the correct fail-closed behavior; no change needed beyond keeping it — none — none.

## Coverage
ios/Runner/KokoroVendored/Albert/AlbertEncoder.swift — findings: 1
ios/Runner/KokoroVendored/Albert/AlbertLayer.swift — findings: 1
- [info] ios/Runner/KokoroVendored/Albert/AlbertLayerGroup.swift:13-14 — innerGroupNum drives layer construction with no bounds validation; if a caller passes a config with innerGroupNum <= 0 the loop produces an empty albertLayers array and callAsFunction silently returns the input unchanged — a downstream consumer would get a pass-through tensor instead of an error — validate innerGroupNum >= 1 in init or guard in callAsFunction. Reachability: operator/model-load only (vendored TTS lib), no security impact.
- [info] ios/Runner/KokoroVendored/Albert/AlbertModelArgs.swift:17-37 — init accepts arbitrary values (e.g. numHiddenLayers 0, hiddenSize 0, layerNormEps 0) with no validation; a malformed model config would construct degenerate layers rather than failing fast — add asserts/guards for positive sizes. Reachability: operator/model-load only, no security impact.

## Coverage
ios/Runner/KokoroVendored/Albert/AlbertLayerGroup.swift — findings: 0
ios/Runner/KokoroVendored/Albert/AlbertModelArgs.swift — findings: 0
## Coverage
ios/Runner/KokoroVendored/Albert/AlbertSelfAttention.swift — clean
ios/Runner/KokoroVendored/Albert/CustomAlbert.swift — clean
- [info] ios/Runner/KokoroVendored/BuildingBlocks/AdaIN1d.swift:12-26 — no crypto/security-relevant code present; file is a vendored MLX neural-network block (AdaIN normalization + linear projection) with no key material, hashing, or network handling — nothing to act on — none needed
- [info] ios/Runner/KokoroVendored/BuildingBlocks/AdaINResBlock1.swift:8-104 — no crypto/security-relevant code present; file is a vendored MLX residual block (weighted convs + AdaIN layers) with no key material, hashing, or network handling — nothing to act on — none needed

## Coverage
ios/Runner/KokoroVendored/BuildingBlocks/AdaIN1d.swift — clean
ios/Runner/KokoroVendored/BuildingBlocks/AdaINResBlock1.swift — clean
- [info] ios/Runner/KokoroVendored/BuildingBlocks/AdaLayerNorm.swift:12-16 — `init` assigns `self.eps` and `fc` before `super.init()` is called, and `fc` is declared `let` but initialized via assignment after `super.init()` in the original ordering — consequence: none observable in the shown code beyond a style/initialization-order smell; no security impact — smallest safe fix: none needed (no defect exhibited).
- [low] ios/Runner/KokoroVendored/BuildingBlocks/AdainResBlk1d.swift:140 — division by `sqrt(2.0)` uses Swift's `sqrt` on a `Double` literal while `out + shortcut(x)` is an `MLXArray`; if `MLXArray` does not expose `/` accepting a `Double` scalar this is a type-level defect — consequence: potential compile/runtime failure or silent numeric coercion in the residual scaling — smallest safe fix: use `MLX.sqrt` or a Float scalar consistent with the MLX API.
- [info] ios/Runner/KokoroVendored/BuildingBlocks/AdainResBlk1d.swift:8-20 — `AdainResBlk1d` is declared as a plain class (not a `Module` subclass) while holding `Module`-typed properties (`pool`, `conv1`, `norm1`, etc.) — consequence: parameter registration/training state may not be tracked by the MLX framework if these are meant to be learned parameters; in this vendored inference-only port weights are loaded explicitly, so impact is limited — smallest safe fix: subclass `Module` and register submodules if training support is ever needed.

## Coverage
ios/Runner/KokoroVendored/BuildingBlocks/AdaLayerNorm.swift — findings: 1
ios/Runner/KokoroVendored/BuildingBlocks/AdainResBlk1d.swift — findings: 2
- [low] ios/Runner/KokoroVendored/BuildingBlocks/ConvWeighted.swift:118 — epsilon added to norm denominator is 1e-7, matching PyTorch weight-norm eps but applied to an L2 norm whose magnitude depends on tensor scale; if weightV norms are small the normalized weights can blow up — consequence: numerically unstable normalized weights feeding conv layers, degraded synthesis output — smallest safe fix: confirm the vendored Kokoro reference uses the same eps and scale; if so, no change needed (info-level parity note).
- [low] ios/Runner/KokoroVendored/BuildingBlocks/ConvWeighted.swift:143-147,172-176 — transposed() with no axis arguments transposes all axes; for a 3D conv weight [out, in/groups, k] a full transpose is not the same as swapping the first two dims — consequence: when x.shape.last != weight.shape.last and groups == 1, the conv receives a wrongly-permuted weight and produces garbage output — smallest safe fix: transpose only the relevant axes (e.g. transposed(0, 2, 1) equivalent) or verify MLX's conv expects the fully-transposed layout; this mirrors the same shape-mismatch branch in both callAsFunction overloads.
- [info] ios/Runner/KokoroVendored/BuildingBlocks/ConvWeighted.swift:32 — bias reshaped to [1, 1, -1] assumes a 1D bias; if bias has more than one dim the reshape silently misbroadcasts — consequence: wrong bias addition in conv output — smallest safe fix: assert bias.ndim == 1 before reshaping.
- [info] ios/Runner/KokoroVendored/BuildingBlocks/Conv1dInference.swift:36-42 — bias added after conv without reshaping; if conv output is 3D [N, C, L] and bias is 1D [C], MLX broadcasting may or may not align as intended depending on MLX's broadcasting rules — consequence: potential silent misbroadcast — smallest safe fix: reshape bias to [1, C, 1] explicitly before adding, matching the ConvWeighted pattern.

## Coverage
ios/Runner/KokoroVendored/BuildingBlocks/Conv1dInference.swift — findings: 1
ios/Runner/KokoroVendored/BuildingBlocks/ConvWeighted.swift — findings: 3
## Coverage
ios/Runner/KokoroVendored/BuildingBlocks/InstanceNorm1d.swift — clean
ios/Runner/KokoroVendored/BuildingBlocks/Interpolate.swift — clean
- [medium] ios/Runner/KokoroVendored/BuildingBlocks/LSTM.swift:83-84,135-136 — missing hidden/cell state defaults to zeros silently instead of failing — a caller that forgets to pass state (or passes nil expecting an error) gets a silently wrong inference result with no diagnostic — smallest safe fix: assert/throw when hiddenForward/cellForward/hiddenBackward/cellBackward are nil, or document+log the zero-init fallback explicitly
- [medium] ios/Runner/KokoroVendored/BuildingBlocks/LSTM.swift:96,146 — gate split assumes exactly 4 contiguous last-axis parts sized hiddenSize — if wxForward/whForward shapes disagree with hiddenSize (or xProj's last dim isn't 4*hiddenSize), split either throws or mis-slices gates, silently corrupting i/f/g/o — smallest safe fix: validate xProj.shape[-1] == 4*hiddenSize before the loop and fail loudly
- [low] ios/Runner/KokoroVendored/BuildingBlocks/LSTM.swift:69-73,121-124 — addMM(biasIh+biasHh, x, wxᵀ) folds both biases into the input projection, but the per-timestep gate bias is then applied once per step via matmul(currentHidden, whᵀ) with no separate biasHh term — if the reference implementation applies biasIh and biasHh separately per gate, this fused form changes numerics (double-counts one bias, drops the other) — smallest safe fix: verify against the reference LSTM math and, if wrong, compute xProj = matmul(x, wxᵀ) + biasIh and add biasHh inside the loop
- [low] ios/Runner/KokoroVendored/BuildingBlocks/LSTM.swift:200-201 — returned final states slice [0..., -1, 0...] / [0..., 0, 0...] from stacked outputs whose batch axis is axis 0 and time axis is axis 1 — if callers expect (batch, hidden) shaped states this is correct, but if they expect (1, hidden) the extra batch dim leaks through — smallest safe fix: confirm downstream consumer shape and squeeze axis 0 if needed
- [info] ios/Runner/KokoroVendored/BuildingBlocks/LSTM.swift:15-24 — MLXArray weight/bias properties stored as strong references on a Module subclass without explicit parameter registration — if MLXNN requires declared parameters for gradient/update plumbing, silently-unregistered weights could be missed by weight-loading code — smallest safe fix: verify MLXNN's parameter discovery rules for stored MLXArray properties
- [info] ios/Runner/KokoroVendored/BuildingBlocks/LayerNormInference.swift:10-11 — weight/bias typed as optional MLXArray? but init takes non-optional weight — asymmetry is harmless here but the optional bias path relies on MLXFast.layerNorm accepting nil bias correctly — smallest safe fix: none needed unless MLXFast.layerNorm mis-handles nil bias; verify against MLXFast docs
## Coverage
ios/Runner/KokoroVendored/BuildingBlocks/LSTM.swift — findings: 5
ios/Runner/KokoroVendored/BuildingBlocks/LayerNormInference.swift — findings: 1
ios/Runner/KokoroVendored/BuildingBlocks/ReflectionPad1d.swift — clean
ios/Runner/KokoroVendored/BuildingBlocks/UpSample1d.swift — clean

## Coverage
ios/Runner/KokoroVendored/BuildingBlocks/ReflectionPad1d.swift — clean
ios/Runner/KokoroVendored/BuildingBlocks/UpSample1d.swift — clean
- [info] ios/Runner/KokoroVendored/Decoder/Generator.swift:35-36 — `MLX.product(MLXArray(upsampleRates)) * genIstftHopSize` is evaluated as a lazy MLX graph op and only materialized via `.item()` on the next line; if `upsampleRates` is empty, `MLX.product` on an empty array yields 1 (or a runtime trap depending on MLX version) and the resulting `upsampleScaleNum` silently drives `SourceModuleHnNSF`'s `upsampleScale` and `Upsample`'s `scaleFactor` — a wrong scale silently corrupts all generated audio downstream — guard `upsampleRates` non-empty and assert `upsampleScaleNumVal > 0` before constructing `mSource`/`f0Upsample` — reachability: operator/model-load path only (bad config or malformed weights bundle), not attacker-reachable.
- [info] ios/Runner/KokoroVendored/Decoder/Generator.swift:52-62 — `padding: (k - u) / 2` uses integer division; when `k - u` is odd (e.g. kernel 5, stride 2 → 1.5 → 1) the asymmetric padding is silently truncated, shifting the transposed-conv output length versus the reference PyTorch `ConvTranspose1d` which supports asymmetric padding — verify against the vendored reference config that all `(k, u)` pairs are even-difference, or switch to explicit left/right padding — reachability: model-load path only; wrong padding degrades audio quality, not a security issue.
- [info] ios/Runner/KokoroVendored/Decoder/Generator.swift:66 — `upsampleInitialChannel / Int(pow(2.0, Double(i + 1)))` recomputes `pow` per iteration and relies on floating-point exactness for powers of two; for large `i` or non-power-of-two initial channels the integer division silently truncates the channel count, mis-sizing every `AdaINResBlock1` weight lookup at line 71 — compute channel counts with integer arithmetic (`upsampleInitialChannel >> (i + 1)`) or assert the division is exact — reachability: model-load path only.
- [info] ios/Runner/KokoroVendored/Decoder/Generator.swift:82 — `MLXArray(upsampleRates)[(i + 1)...]` builds a lazy slice then `.item()`; if `i + 1 == upsampleRates.count` this branch is unreachable (guarded by line 81), but if `upsampleRates` has one element the slice is the full array — no defect, but the pattern of forcing `.item()` on lazy slices inside a loop is fragile if MLX changes lazy-vs-eager semantics; no action needed beyond noting.
- [info] ios/Runner/KokoroVendored/Decoder/Generator.swift:148-149 — `F0Curve[.newAxis, 0..., 0...].transposed(0, 2, 1)` then `f0Upsample(f0New)` assumes `F0Curve` is already shaped `[batch, 1, T]`; if callers pass `[batch, T]` the newAxis insertion produces `[batch, 1, T]` only by accident of the slice order — confirm the caller's shape contract or add an explicit reshape with a shape assertion — reachability: internal call path only; wrong shape silently produces garbage audio rather than a crash.
- [info] ios/Runner/KokoroVendored/Decoder/Generator.swift:170-172 — `reflectionPad(newX)` is applied only on the last upsample iteration, but `ReflectionPad1d(padding: (1, 0))` is asymmetric; if `numUpsamples == 0` (empty `upsampleRates`) the pad is never applied and `newX + xSource` at line 173 still executes with `noiseConvs[0]` — guard the loop body against `numUpsamples == 0` or assert non-empty at init — reachability: model-load path only.
- [info] ios/Runner/KokoroVendored/Decoder/Generator.swift:184 — `newX = xs! / numKernels` force-unwraps `xs` which is guaranteed non-nil only because `numKernels >= 1`; if `resblockKernelSizes` is empty, `numKernels == 0` and the inner loop never assigns `xs`, causing a runtime crash — assert `numKernels > 0` at init — reachability: model-load path only.
- [info] ios/Runner/KokoroVendored/Decoder/Generator.swift:193-194 — `MLX.exp(newX[0..., 0 ..< (postNFFt / 2 + 1), 0...])` and `MLX.sin(...)` on the phase half assume `newX`'s channel axis is exactly `postNFFt + 2`; if `convPost`'s output channel count differs (weights mismatch), the slice silently produces misaligned spec/phase and `stft.inverse` at line 196 emits corrupted audio without any error — add a shape assertion `newX.shape[1] == postNFFt + 2` before slicing — reachability: model-load path only.
- [info] ios/Runner/KokoroVendored/Decoder/Decoder.swift:29-34 — `AdainResBlk1d` blocks are constructed with hard-coded dims (`dimIn + 2`, `1024`, `512`) that must match the vendored weight keys exactly; a mismatched weights bundle silently loads wrong-shaped tensors or traps at force-unwrap `weights[...]!` — no action beyond noting the force-unwrap pattern is load-time only.
- [info] ios/Runner/KokoroVendored/Decoder/Decoder.swift:74-78 — `MLX.swappedAxes(F0Curve.reshaped(...), 2, 1)` assumes `F0Curve` is `[batch, T]`-shaped before reshaping to `[batch, 1, T]`; if the caller passes a 3-D tensor the reshape silently mis-batches — confirm caller contract or assert `F0Curve.ndim == 2` — reachability: internal call path only.
- [info] ios/Runner/KokoroVendored/Decoder/Decoder.swift:80-88 — `MLX.concatenated([asr, F0, NProcessed], axis: 1)` and the residual concat at line 88 assume all three tensors share the same time-axis length; a shape mismatch silently broadcasts or traps depending on MLX version — add shape assertions before concat — reachability: internal call path only.
- [info] ios/Runner/KokoroVendored/Decoder/Decoder.swift:84-95 — `res` starts `true` and flips to `false` after the first upsample block; if `decode` is empty (impossible here — four appends at lines 31-34) the loop is a no-op and `generator(x, s, F0Curve)` at line 97 still runs on the un-decoded `x` — no action needed given the fixed four-block construction.
- [info] ios/Runner/KokoroVendored/Decoder/Decoder.swift:97 — `generator(x, s, F0Curve)` passes the raw `F0Curve` rather than the swapped/reshaped variant used at line 74; this matches the Generator's own expectation at Generator.swift:148 (`F0Curve[.newAxis, 0..., 0...]`), so the two files agree — no defect, noted for shape-contract traceability.

## Coverage
ios/Runner/KokoroVendored/Decoder/Decoder.swift — findings: 0
ios/Runner/KokoroVendored/Decoder/Generator.swift — findings: 0
- [medium] ios/Runner/KokoroVendored/Decoder/MLXSTFT.swift:36 — `MLX.where(pDiff .> 0, intervalHigh, pDiffMod)` uses `intervalHigh` (a constant `period/2`) as the "true" branch, so every positive phase difference is replaced by the constant rather than by the wrapped value — consequence: phase unwrapping silently corrupts any frame whose adjacent-bin phase delta exceeds π/2, producing wrong reconstructed audio in `inverse()` with no error raised — smallest safe fix: compute the wrapped delta (e.g. `((pDiff + intervalLow) % period) + intervalLow`) and use it as the true branch, matching the numpy `unwrap` semantics this port replaces.
- [medium] ios/Runner/KokoroVendored/Decoder/MLXSTFT.swift:142 — `totalWsquared` is built with `Array(repeating: wSquared, count: t / winLen)` and then sliced per iteration at line 151 (`totalWsquared[0 ..< outputStride.shape[0]]`), but `outputStride.shape[0]` is derived from a strided slice of `output` whose length is `ceil(t / windowModLen)`-ish, not `t / winLen`; when `t` is not an exact multiple of `winLen` the repeating count truncates and the per-iteration `windowSumArray` silently reads past the concatenated buffer (or the final `windowSum` slice at 175 is shorter than `reconstructed`), causing a shape mismatch or a divide-by-zero/NaN in the normalization — smallest safe fix: compute the repeat count as `ceil(Float(t) / Float(winLen))` (or assert `t % winLen == 0` alongside the existing `winLen % hopLen == 0` precondition) so the squared-window tiling always covers the full output length.
- [medium] ios/Runner/KokoroVendored/Decoder/MLXSTFT.swift:149-157 — the overlap-add loop indexes `output` with `.stride(from: i, by: windowModLen)` and then left-pads with `i * hopLen` zeros, but `output` was produced by `irfft(xTransposed, axis: 1) * w` whose frame length is `winLen` per hop; the stride step `windowModLen` (= `winLen / hopLen`) is a frame-count, not a sample-count, so the loop walks the flattened output in the wrong units whenever `hopLen != winLen` (i.e. any config other than the shipped 200/800) — consequence: reconstructed audio is scrambled/aliased for alternate hop/win configurations even though the precondition at 138 passes — smallest safe fix: stride the flattened output by `hopLen` samples per overlap-add step (or reshape to `[numFrames, winLen]` and shift each row by `i * hopLen`), keeping `windowModLen` only for the window-sum tiling.
- [low] ios/Runner/KokoroVendored/Decoder/MLXSTFT.swift:94-97 — `numFrames` is computed as `1 + (xArray.shape[0] - nFft) / hopLen` using Swift integer division that truncates toward zero for negative numerators; a batch element shorter than `nFft` yields `numFrames <= 0` and hits the `fatalError`, but a length between `nFft` and `nFft + hopLen - 1` yields `numFrames == 1` while `asStrided` at 102 with shape `[numFrames, nFft]` and strides `[hopLen, 1]` reads past the end of the (reflected-padded) buffer only when padding was skipped — consequence: potential out-of-bounds read or wrong frame count for edge-case input lengths — smallest safe fix: compute `numFrames` with `(xArray.shape[0] - nFft) / hopLen + 1` guarded by an explicit `xArray.shape[0] >= nFft` check before striding.
- [low] ios/Runner/KokoroVendored/Decoder/MLXSTFT.swift:255-260 — `callAsFunction` stores `magnitude`/`phase` on the instance (`magnitude = mag; phase = ph`) and then immediately reconstructs from the passed-in values; the stored state is never read by any inlined code path — consequence: retained MLX arrays keep large intermediate buffers alive across calls (memory growth on repeated invocations) and, if any future caller reads the stale properties, it silently gets the previous call's spectrogram — smallest safe fix: drop the stored properties (or nil them out after use) so the reconstruction uses only the local `(mag, ph)` tuple.
- [info] ios/Runner/KokoroVendored/Decoder/SineGen.swift:45,78 — `MLXRandom.normal(...)` is used to synthesize the "noise" component of the vocoder; this is a deterministic-seeded MLX RNG stream, not a CSPRNG, but it feeds audio synthesis (not keys/tokens/nonces), so it is not a security defect — no action needed; noted only because the checklist's CSPRNG class was evaluated and does not apply to this DSP context.

## Coverage
ios/Runner/KokoroVendored/Decoder/MLXSTFT.swift — findings: 5
ios/Runner/KokoroVendored/Decoder/SineGen.swift — findings: 1
- [info] ios/Runner/KokoroVendored/Decoder/SourceModuleHnNSF.swift:39-40 — force-unwrapped dictionary lookups of pretrained weight keys — a missing/misnamed key in the checkpoint crashes the app at model load — use `weights[...]` with a guard/throw or explicit error naming the missing key.
- [info] ios/Runner/KokoroVendored/TTSEngine/DurationEncoder.swift:45-52,59-60 — force-unwrapped dictionary lookups of pretrained weight keys across every LSTM/AdaLN layer — a missing/misnamed checkpoint key crashes at model load with no diagnostic — guard each lookup and surface the missing key name.
- [medium] ios/Runner/KokoroVendored/TTSEngine/DurationEncoder.swift:123 — `x.transposed(0, 2, 1)[0]` hardcodes batch index 0 — any batch size > 1 silently drops all but the first utterance's features, corrupting duration prediction for batched inference — assert/validate batch == 1 or loop over the batch dim.
- [low] ios/Runner/KokoroVendored/TTSEngine/DurationEncoder.swift:81 — `textLengths` parameter is accepted and discarded (`textLengths _:`) — callers relying on per-sequence length handling get none; if the LSTM path ever needs lengths this is a silent no-op — either use it or document the invariant that masking fully encodes lengths.

## Coverage
ios/Runner/KokoroVendored/Decoder/SourceModuleHnNSF.swift — findings: 1
ios/Runner/KokoroVendored/TTSEngine/DurationEncoder.swift — findings: 3
- [medium] ios/Runner/KokoroVendored/TTSEngine/KokoroConfig.swift:165-167 — config loading uses forced unwraps (`url(...)!`, `try!`) for a bundled resource — a missing/malformed config.json crashes the app at first TTS use with no recoverable error path — replace `!`/`try!` with `guard let`/`try` and surface a typed error to the caller (KokoroTTS.init already declares `throws`).
- [medium] ios/Runner/KokoroVendored/TTSEngine/KokoroTTS.swift:152 — `try? G2PFactory.createG2PProcessor(engine:)` silently swallows initialization failure — G2P processor becomes nil and every later `phonemizeText` call throws `processorNotInitialized` instead of failing fast at init with the real cause — propagate the error (`try` + rethrow) so init fails with the underlying reason.
- [low] ios/Runner/KokoroVendored/TTSEngine/KokoroTTS.swift:366-385 — `createAlignmentTarget` builds a dense `[Float]` of size `totalFrames * batchSize` on the host; long text with large predicted durations allocates a very large buffer synchronously on the UI thread — cap total frames or build the alignment matrix on-device with MLX ops (e.g. `MLX.repeat`/scatter) instead of a host-side loop.
- [info] ios/Runner/KokoroVendored/TTSEngine/KokoroTTS.swift:305 — `extractStyleEmbeddings` indexes `voice[tokenCount - 1, ...]` with a negative-style index when `tokenCount == 0`; the empty-input guard at :188 protects the normal path, but any caller bypassing that guard would read out-of-bounds style data — assert `tokenCount > 0` inside the helper so the invariant is local rather than relying on a distant guard.

## Coverage
ios/Runner/KokoroVendored/TTSEngine/KokoroConfig.swift — findings: 1
ios/Runner/KokoroVendored/TTSEngine/KokoroTTS.swift — findings: 3
## Coverage
ios/Runner/KokoroVendored/TTSEngine/ProsodyPredictor.swift — clean
ios/Runner/KokoroVendored/TTSEngine/TextEncoder.swift — clean
## Coverage
ios/Runner/KokoroVendored/TTSEngine/TimestampPredictor.swift — findings: 2
ios/Runner/KokoroVendored/TTSEngine/WeightLoader.swift — findings: 1

- [medium] ios/Runner/KokoroVendored/TTSEngine/TimestampPredictor.swift:60,64 — `t.start_ts`/`t.end_ts` are computed by dividing the running `left` accumulator by the magic divisor 80, but `left` is a running sum of frame counts (not a frame index), so the resulting timestamps are scaled incorrectly (off by a factor of 80 relative to the intended seconds value) — every token's start/end timestamp is wrong by a constant scale factor, producing misaligned subtitle/karaoke timing for all users — divide the accumulated frame count by the sample rate (24000) after converting half-frames to frames, not by the magic divisor 80.
- [medium] ios/Runner/KokoroVendored/TTSEngine/TimestampPredictor.swift:33,48-49,63-65 — the `left`/`right` accumulators are updated with `dur[i]`/`dur[j]` values that are already in half-frame units (per the comment "measured in half-frames"), yet `left` is also multiplied by 2.0 for token duration and then divided by 80 as if it were raw frames, double-counting the half-frame conversion — token durations are inflated 2x relative to space durations, skewing all subsequent timestamps — pick one unit convention (frames or half-frames) and apply the ×2 conversion exactly once before the final division.
- [low] ios/Runner/KokoroVendored/TTSEngine/WeightLoader.swift:88 — `if key.contains("noise_convs"), key.hasSuffix(".weight")` uses Swift's comma-separated condition list, which is valid syntax but reads as two unrelated predicates; if the intent was a logical AND of "noise_convs" and ".weight" suffix it is correct, but if the second clause was meant to be a separate `else if` branch the noise-conv transposition silently applies to keys lacking `.weight` — verify the intended predicate grouping; if both conditions must hold, the current form is already correct and no change is needed.

Note: the crypto/security checklist classes (ciphers, nonces, TLS, keychain, etc.) do not apply to either file; no matching defect classes were exhibited.
## Coverage
ios/Runner/KokoroVendored/TextProcessing/G2PFactory.swift — clean
ios/Runner/KokoroVendored/TextProcessing/G2PProcessor.swift — clean
## Coverage
ios/Runner/KokoroVendored/TextProcessing/Language.swift — clean
ios/Runner/KokoroVendored/TextProcessing/MisakiG2PProcessor.swift — clean
- [info] ios/Runner/KokoroVendored/TextProcessing/Tokenizer.swift:18-22 — vocab lookup silently drops unknown characters via optional filter — phonemized text containing characters absent from the vocab is tokenized as a shorter array, producing misaligned/shorter TTS input with no error surfaced — fail fast (throw or log) when a character has no vocab entry instead of filtering it out.
- [info] ios/Runner/KokoroVendored/TextProcessing/eSpeakNGG2PProcessor.swift:24-28 — `setLanguage` constructs a new `eSpeakNG` engine before validating the language, and on the unsupported-language path the freshly allocated engine is left assigned to `eSpeakEngine` — a subsequent `process` call after a failed `setLanguage` uses a partially configured engine (default/unknown language) instead of throwing `processorNotInitialized`, silently phonemizing with the wrong language — reset `eSpeakEngine = nil` in the failure branch (or construct only after validation) so failed configuration leaves the processor unusable.

## Coverage
ios/Runner/KokoroVendored/TextProcessing/Tokenizer.swift — findings: 1
ios/Runner/KokoroVendored/TextProcessing/eSpeakNGG2PProcessor.swift — findings: 1
- [medium] ios/Runner/MediaControlPlugin.swift:96-100 — deactivate() calls removeTarget(nil) on MPRemoteCommandCenter commands, which removes ALL targets including any registered by other components or the system — consequence: any other feature sharing the shared command center loses its handlers silently when this plugin deactivates; smallest safe fix: store the concrete target objects added in activate() and remove only those (or use the returned handler tokens).
- [low] ios/Runner/MediaControlPlugin.swift:55-83 — every remote command (previous/next/toggle/play/pause) is mapped to the same "jumpBack" action, so pause and next-track controls behave identically to previous-track — consequence: users pressing pause or next-track get an unintended jump-back instead of the expected media action; smallest safe fix: forward distinct command identifiers to Flutter (e.g. "pause", "next") instead of the constant "jumpBack".
- [low] ios/Runner/MediaControlPlugin.swift:33-35 — updateNowPlaying handler falls back to defaults ("Rehearsal" title, empty character) when arguments are missing or malformed, silently publishing wrong now-playing metadata — consequence: a malformed Flutter call results in misleading lock-screen metadata rather than an error; smallest safe fix: return a FlutterError for invalid arguments instead of substituting defaults.
- [info] ios/Runner/KokoroVendored/Utils/AudioUtils.swift:43-78 — writeWavFile writes 32-bit float PCM WAV via AVAudioFile; no security-relevant defect observed (DEBUG-only helper, no secrets, no network, no crypto) — consequence: none; smallest safe fix: none needed.

## Coverage
ios/Runner/KokoroVendored/Utils/AudioUtils.swift — findings: 0
ios/Runner/MediaControlPlugin.swift — findings: 3
- [low] ios/Runner/MemoryMonitorPlugin.swift:53 — getPhysicalFootprint returns 0 on task_info failure — a Jetsam-adjacent metric silently reports "0 MB used" instead of failing, which can make the Dart-side memory guard treat an unknown footprint as healthy — return nil (or a sentinel like Int.max) and let the caller decide, or log the kern_return_t.
- [low] ios/Runner/MemoryMonitorPlugin.swift:27 — os_proc_available_memory() result is truncated with integer division — os_proc_available_memory returns bytes; dividing by 1024/1024 in two steps is fine, but if the API ever returns 0 (unsupported), availableMemoryMB silently becomes 0 and the caller cannot distinguish "no memory" from "unavailable" — guard the raw value before dividing.
- [info] ios/Runner/MemoryMonitorPlugin.swift:32 — ProcessInfo.physicalMemory reports the device's total physical RAM, not the process's limit — on iOS this is the whole device capacity, so "totalPhysicalMemoryMB" is misleading for any consumer that compares footprint against it — document or drop the field.

## Coverage
ios/Runner/MemoryMonitorPlugin.swift — findings: 3
ios/Runner/MisakiVendored/English/DataStructures/TokenContext.swift — clean
`ios/Runner/MisakiVendored/English/EnglishG2P.swift` — findings: 4
`ios/Runner/MisakiVendored/English/FallbackNetwork/BARTConfig.swift` — clean

- [medium] ios/Runner/MisakiVendored/English/EnglishG2P.swift:32,141 — `try!` on regex compilation — a malformed pattern (or a future edit to the pattern string) makes every `phonemize` call trap the process; the crash is reachable from any caller that invokes `phonemize(text:)` — replace `try!` with `try?`/`Result` and fall back to a literal-token path (or `precondition` with a unit test asserting the pattern compiles) — reachability: any authenticated user of the TTS pipeline (in-process crash, not data loss), so medium.
- [medium] ios/Runner/MisakiVendored/English/EnglishG2P.swift:278-279 — `tokens.first!` / `tokens.last!` force-unwraps on possibly-empty arrays — `tokenize` returns `[]` for empty/whitespace-only input (guard at 198-200) and `mergeTokens` is also called from `foldLeft`/`phonemize` paths where `tokens` can be empty; a crash on benign empty input — guard with `tokens.isEmpty` early-return before computing the range — reachability: any caller passing empty text, medium.
- [medium] ios/Runner/MisakiVendored/English/EnglishG2P.swift:118-124 — stress-resolution branch silently drops work when `indices.count == 2` but the first token's text length isn't 1, and when the primary-stress count is at/below the midpoint — the `-0.5` stress is never applied to the second token, so downstream TTS gets unstressed output with no error surfaced; if the intent is "apply to the lower half", the `== 2` special case should fall through to the general path instead of returning — verify intended semantics against the reference implementation; if the early return is correct, this is info, else medium (silent wrong output for authenticated users of the pipeline).
- [low] ios/Runner/MisakiVendored/English/EnglishG2P.swift:502-504 — phoneme post-processing replaces `ɾ`→`T` and `ʔ`→`t` on every token — this is a character-level substitution applied to the *phoneme* string, which will also rewrite these IPA symbols inside legitimate phoneme sequences (e.g. a flap `ɾ` in "water" becomes `T`), corrupting pronunciation output silently; if the mapping is intentional for this vendored engine's alphabet, downgrade to info — reachability: any authenticated user of the TTS pipeline, silent wrong audio, medium-by-impact but guarded by intent, so low.

## Coverage
ios/Runner/MisakiVendored/English/EnglishG2P.swift — findings: 4
ios/Runner/MisakiVendored/English/FallbackNetwork/BARTConfig.swift — clean
## Coverage
ios/Runner/MisakiVendored/English/FallbackNetwork/BARTDecoderLayer.swift — clean
ios/Runner/MisakiVendored/English/FallbackNetwork/BARTEncoderLayer.swift — clean
- [low] ios/Runner/MisakiVendored/English/FallbackNetwork/BARTModel.swift:130-131 — cross-attention K/V and self-attention caches are computed eagerly per generate call with no memory release — repeated greedy decode steps retain every projected KV tensor for the whole loop, growing peak memory with maxLength — call `MLX.eval`/`mlx.clearCache` (or use MLX KV cache abstractions) after each step.
- [low] ios/Runner/MisakiVendored/English/FallbackNetwork/BARTModel.swift:163-166 — argmax sampling with temperature scaling is not a true greedy argmax when temperature != 1 (dividing logits by temperature then argmax is monotonic, so it is equivalent, but the comment claims sampling) — misleading naming only; no behavioral defect — rename to `greedyArgMax` or drop the temperature parameter.
- [medium] ios/Runner/MisakiVendored/English/FallbackNetwork/BARTModel.swift:138-141 — the loop unconditionally appends `eosTokenId` at the final step even if the model never emitted EOS — generated output can contain a spurious EOS token that callers may treat as a real termination signal — only append EOS when the model actually produced it, or let the caller decide termination.
- [low] ios/Runner/MisakiVendored/English/FallbackNetwork/BARTModel.swift:146 — position slice `positionIds[0..., i..<(i + 1)]` assumes `positionIds` was reshaped to `[1, maxPositions]` (line 71) so the row dimension exists — if `maxPositionEmbeddings` is 0 or the reshape silently fails, this indexes out of bounds — guard `maxPositionEmbeddings > maxLength + 2` before slicing.
- [low] ios/Runner/MisakiVendored/English/FallbackNetwork/BARTLayerNorm.swift:11-12 — `_updateInternal` is used to overwrite weight/bias after `super.init(dimensions:)`; if MLX's `LayerNorm` lazily allocates its own weight/bias, this bypasses the normal parameter registration path and can desync parameter counts — verify `_updateInternal` is the sanctioned bulk-assignment API for this MLX version, else use the standard `update` mechanism.

## Coverage
ios/Runner/MisakiVendored/English/FallbackNetwork/BARTLayerNorm.swift — findings: 1
ios/Runner/MisakiVendored/English/FallbackNetwork/BARTModel.swift — findings: 4
- [medium] ios/Runner/MisakiVendored/English/FallbackNetwork/EnglishFallbackNetwork.swift:64 — tokensToPhonemes silently drops tokens ≤ unknownTokenId (BOS/EOS/unknown) and skips unmapped token IDs without any fallback — phoneme output can be empty or truncated for valid words with no diagnostic, callers cannot distinguish "no phonemes" from "model failure" — emit a distinct unknown/placeholder phoneme or log when a token has no mapping, and include BOS/EOS handling explicitly.
- [medium] ios/Runner/MisakiVendored/English/FallbackNetwork/EnglishFallbackNetwork.swift:74 — callAsFunction always returns rating 1 regardless of output quality; combined with the silent-drop above, a failed/empty phonemization is indistinguishable from a perfect one — propagate a real confidence/rating derived from output (e.g. fraction of tokens mapped) so callers can fall back to lexicon-only G2P.
- [low] ios/Runner/MisakiVendored/English/FallbackNetwork/EnglishFallbackNetwork.swift:87 — loadConfig/loadWeights use `try?` and swallow the underlying error; only a generic NSLog is emitted — debugging a corrupt bundled resource is hard; capture and log the concrete error (or at least the URL) to make the failable-init diagnosable.
- [low] ios/Runner/MisakiVendored/English/FallbackNetwork/EnglishFallbackNetwork.swift:39 — phonemeDict is built as `[Int: Character]` keyed by enumeration index while graphemeDict is keyed by the character itself; the index-keyed map is only meaningful if `configuration.phonemeChars` order matches token IDs 1:1 — if the BART config's phoneme list is not aligned with token IDs (e.g. offset by BOS/unknown tokens), tokenToPhoneme maps wrong IDs to wrong phonemes silently; verify alignment against the config's token-id fields and key by the actual token ID from the config.
- [info] ios/Runner/MisakiVendored/English/FallbackNetwork/EnglishFallbackNetwork.swift:5 — class named `EnglishFallbackNetwork` but the failable init and doc comment describe a BART/G2P fallback network; naming mismatch is cosmetic only, no action needed beyond awareness.
## Coverage
ios/Runner/MisakiVendored/English/FallbackNetwork/EnglishFallbackNetwork.swift — findings: 4
ios/Runner/MisakiVendored/English/FallbackNetwork/FeedForward.swift — clean
- [medium] ios/Runner/MisakiVendored/English/FallbackNetwork/MultiHeadAttention.swift:20-23 — force-unwrapped `weights[...]!` in init crashes at runtime when a checkpoint lacks any of the 12 expected weight/bias tensors — an authenticated user loading a truncated or mismatched model file gets an unrecoverable crash instead of a graceful fallback — guard-let each entry and fail with a descriptive error (or skip the layer) rather than `!`
- [medium] ios/Runner/MisakiVendored/English/FallbackNetwork/MultiHeadAttention.swift:18 — `headDim = dModel / numHeads` with no validation; a `numHeads` that does not divide `dModel` (e.g. from a malformed config) silently produces wrong reshape sizes and garbage attention output rather than an error — assert/validate divisibility in init
- [low] ios/Runner/MisakiVendored/English/FallbackNetwork/MultiHeadAttention.swift:54-56 — `callAsFunction` defaults `key`/`value` to `query` (self-attention) but `step`/`projectKV` paths assume precomputed K/V; if a caller passes `key` without `value` (or vice versa) the other silently falls back to `query`, mixing self- and cross-attention semantics with no diagnostic — require both or log when the fallback fires
- [low] ios/Runner/MisakiVendored/English/Lexicon/DataResourcesUtil.swift:9-13 — `loadGold` swallows all load/parse failures with `try?` and returns `[:]` with no log; a missing or corrupt `gb_gold`/`us_gold` file silently disables the gold lexicon and every word falls through to a full forward pass (the same failure mode `loadSilver` explicitly logs at line 29) — mirror the NSLog + empty-dict fallback so the degradation is observable
- [low] ios/Runner/MisakiVendored/English/Lexicon/DataResourcesUtil.swift:10 — `try? Data(contentsOf: url)` also hides read errors (permissions, truncated file) identically to a missing file; combined with the `[:]` return this makes a corrupt dictionary indistinguishable from an absent one — distinguish IO error from parse error in the log message

## Coverage
ios/Runner/MisakiVendored/English/FallbackNetwork/MultiHeadAttention.swift — findings: 3
ios/Runner/MisakiVendored/English/Lexicon/DataResourcesUtil.swift — findings: 2
- [medium] ios/Runner/MisakiVendored/English/Lexicon/Lexicon.swift:121-124 — applyStress stress==0 branch: `(stress == 0 || stress == -0.5) && contains(primary)` strips secondary and demotes primary to secondary, but the sibling branch at 124 requires `stress == 0 || 0.5 || 1` with no stress markers present; a phonetic string containing only secondaryStress with stress==0 falls through to line 138 unchanged, leaving secondary stress applied where the caller asked for neutral stress — output phonemes keep a stray ˌ marker for every token whose dictionary entry has only secondary stress — treat stress==0-with-only-secondary like the -1 branch (strip secondary, keep primary) or fall through to restress with secondary prefix. [info] ios/Runner/MisakiVendored/English/Lexicon/Lexicon.swift:19-20 — usVocab/gbVocab are built from a single string literal each; the us set omits "ɒ" and "ː" while gb omits "æ" and "ʌ" — if any dictionary phonetic string uses the other dialect's symbol it silently fails vowelSet membership in applyStress (line 125/132) and restress repositioning, producing unstressed output — verify resource phoneme alphabets match these sets or union them. [medium] ios/Runner/MisakiVendored/English/Lexicon/Lexicon.swift:189 — getWord condition calls `fn(wl, tag, stress, ctx).0 != nil` on `stem_s, stem_ed, stem_ing` tuple members, but these are declared as functions taking `(word, tag, stress, ctx)` returning `(phoneme, rating)`; the closure parameter `fn` is invoked with 4 args while stem_ed/stem_ing also expect the same signature — if any of the three is actually a 2-arg helper (e.g. progIng/pastEd shape), the compile-time mismatch would be caught, but the runtime `.0 != nil` re-runs the full stem pipeline on every candidate word, doubling phonemization cost on the hot path — memoize the stem probe or restructure the condition. [low] ios/Runner/MisakiVendored/English/Lexicon/Lexicon.swift:534 — `lookup("O", ...) as! (String, Int)` force-casts the lookup tuple; lookup returns `(phoneme: String?, rating: Int?)` whose fields are optionals, so the cast can never succeed at runtime and will trap (crash) whenever this branch executes (a 3-digit number ending in "0" with penultimate digit "0", e.g. "100", "200", "300") — replace with optional binding of looked.0/looked.1 and skip append when nil. [medium] ios/Runner/MisakiVendored/English/Lexicon/Lexicon.swift:558-559 — currency branch computes `a`/`b` via `parts.indices.contains(0)`/`contains(1)` then `Int(parts[0]) ?? 0`; for a currency token like "$5" parts is ["5"], so `b` is forced to 0 and pairs[1] = (0, "cent") — the `pairs.count > 1` guard then keeps only the dollar part, silently dropping the cents word for every whole-dollar amount — harmless for "$5" but for "$0.50" a=0,b=50 works; the defect is that a missing second component resolves to the permissive "0 cents" instead of skipping the unit — filter pairs on non-nil parse rather than defaulting to 0. [low] ios/Runner/MisakiVendored/English/Lexicon/Lexicon.swift:562-563 — `pairs[1].0 == 0 { pairs = prefix(1) }` / `pairs[0].0 == 0 { pairs = suffix(1) }` mutates pairs inside `if pairs.count > 1` but the subsequent `for (i, ...) in pairs.enumerated()` uses the mutated array while the "and" appendLookup at 567 fires for `i > 0` — when both a and b are 0 (e.g. "$0.00") pairs collapses to one element and no "and" is spoken, which is correct, but when only b==0 the prefix(1) path drops the cent unit without emitting "and", inconsistent with the $0.50 path — unify the collapse logic. [low] ios/Runner/MisakiVendored/English/Lexicon/Lexicon.swift:597-600 — `if result.isEmpty { return (nil, nil) }` then `rating = result.map{ $0.1 }.min() ?? 4`; min() over an empty array would be nil so the `?? 4` is dead code, but the real issue is rating uses min() (best/lowest rating) while every other call site treats higher rating as better (getWord returns res.rating straight through) — if num2Words ratings follow the "lower is better" convention this is fine, but if they follow "higher is better" the number path silently reports the worst rating — confirm convention and use max() if needed. [low] ios/Runner/MisakiVendored/English/Lexicon/Lexicon.swift:602-608 — suffix handling: `s == "s" || s == "'s"` calls pluralizeS(text) which appends "z"/"s" phonemes, but for suffix "'s" the stem was already looked up with the possessive form stripped earlier (line 510-512 removed the trailing match), so re-pluralizing an already-possessive text double-modifies the phoneme string for tokens like "John's" — verify stem removal covers "'s" before this branch or guard on suffix == "s" only. [info] ios/Runner/MisakiVendored/English/Lexicon/Lexicon.swift:9-11 — three `try!` on NSRegularExpression construction; patterns are compile-time constants and valid, but a `try!` trap on any future pattern edit would crash the runner at startup — prefer a static let with `do/catch` or `Deferred` lazy init; informational only since current patterns are valid. [low] ios/Runner/MisakiVendored/English/Lexicon/PennTagUtil.swift:78-80 — auxBe/auxDo/auxHave branches: `["does"].contains(lower)` allocates a fresh array literal per call on the phonemization hot path (same churn the file header says it removed for the Set literals at 6-24) — hoist to a static Set alongside the others. [low] ios/Runner/MisakiVendored/English/Lexicon/PennTagUtil.swift:79-80 — same array-literal-per-call churn in the auxDo/auxHave lines (`["has"].contains(lower)`) — hoist both to file-scope statics. [low] ios/Runner/MisakiVendored/English/Lexicon/PennTagUtil.swift:70-71 — `isCapitalizedWord` only checks the first character; a token like "iPhone" or "McDonald" is treated as fully capitalized ("NNP" proper noun) even though only part is uppercase — acceptable heuristic for POS guessing, but note it also misclassifies all-caps acronyms identically to title-case; no fix needed unless precision matters downstream. [info] ios/Runner/MisakiVendored/English/Lexicon/PennTagUtil.swift:29-30 — `token?.trimmingCharacters(...)` then `lowercased()`; for nil token the function still returns a tag based purely on nlTag, which is intended; no defect. ## Coverage ios/Runner/MisakiVendored/English/Lexicon/Lexicon.swift — findings: 9 ios/Runner/MisakiVendored/English/Lexicon/PennTagUtil.swift — findings: 4
- [info] test/cast_member_test.dart:139 — `SupabaseService.generateJoinCode()` is exercised only through its public output; if the generator uses a non-CSPRNG source (e.g. `Random()` rather than `Random.secure()`), these tests cannot detect it — join codes gate production access, so a predictable generator would be a real defect — verify the implementation uses a secure RNG (assumption: the implementation file was not inlined in this batch; request it to confirm).
- [info] test/cast_member_test.dart:155-162 — the uniqueness test asserts only that ≥95 of 100 codes are distinct (`greaterThan(95)`), which tolerates up to 5 collisions; a real uniqueness guarantee would assert `codes.length == 100` or use a larger sample — as written, a generator that collides occasionally would pass — tighten the assertion to exact uniqueness or drop the claim of uniqueness from the test name.

## Coverage
test/analytics_route_observer_test.dart — clean
test/cast_member_test.dart — findings: 2
- [info] test/character_gender_persistence_test.dart:27 — SharedPreferences.setMockInitialValues is invoked in setUp but the async service tests at lines 50-62 and 64-75 read/write real persistence via VoiceConfigService.instance — if the service uses SharedPreferences under the hood (assumed, not shown), mock values set in setUp may not apply to async reads after the first test, or may leak state between tests — verify the service actually consumes the mocked store and that each test starts from an empty store — smallest safe fix: reset the mock store per test (e.g., add/tearDown clearing SharedPreferences mock values) and confirm VoiceConfigService reads the mocked instance.
- [info] test/character_gender_persistence_test.dart:50-75 — tests depend on VoiceConfigService.instance and buildParsedScript whose implementations are not inlined; findings about their behavior cannot be confirmed from this file alone — smallest safe fix: none (report only what is exhibited; request the service file if needed).

## Coverage
test/cast_role_test.dart — clean
test/character_gender_persistence_test.dart — findings: 2
- [info] test/demo_production_service_test.dart:27 — mock SharedPreferences with empty initial values is used to back a real service load path; if any production code path persists secrets/tokens in SharedPreferences this test would pass regardless — verify it is not committed with real values — smallest safe fix: none (test-only; no secret exhibited in this file)
- [low] test/demo_production_service_test.dart:31 — tearDown closes the db but setUp creates a fresh in-memory db per test while `SharedPreferences.setMockInitialValues({})` is re-invoked in setUp only, not in the third test before `refFor` — the third test relies on mock values set before setUp's reset, which setUp then overwrites — smallest safe fix: set mock values after setUp or merge with existing mock values

Wait — check line order: setUp at 26-29 resets mock values to `{}`; the third test at 94-97 calls `SharedPreferences.setMockInitialValues({...})` *inside* the test body, after setUp already ran, so the reset concern is wrong. Re-examine: test body sets mock values at line 95, then `refFor` pumps widget at 98. That is fine. Drop that finding.

Re-check the crypto/security lens against these two test files: no cipher, no key material, no TLS, no JWT, no secrets in source. The only security-adjacent surface is SharedPreferences mock usage — no real secret exhibited. No defensible findings.

## Coverage
test/cloud_sync_dialog_test.dart — clean
test/demo_production_service_test.dart — clean
## Coverage
test/demo_script_test.dart — clean
test/dialog_navigation_test.dart — clean
- [low] test/espeak_heteronyms_test.dart:59-61 — `Process.runSync('which', ['espeak-ng'])` availability probe is computed once per test run and cached in a `late final` local inside a group body; if `which` is absent on some platforms (e.g. Windows CI) the probe throws instead of cleanly skipping — consequence: the "espeak-ng not installed" skip path never triggers on platforms without `which`, producing a hard failure rather than a skip — smallest safe fix: probe with `Process.runSync('espeak-ng', ['--version'])` (or catch `ProcessException`) so the skip branch is actually reachable.

## Coverage
test/espeak_heteronyms_test.dart — findings: 1
test/home_screen_logic_test.dart — clean
- [low] test/model_download_gzip_test.dart:47 — tearDown closes the HttpServer but the two `serve` calls in the same test file each bind a fresh server while the previous one is only closed in tearDown — if a test body throws before tearDown runs, the bound port leaks for the rest of the process — hoist `server.close(force: true)` into a `try/finally` around each test body or use `addTearDown(server.close)` per test. Reachability: test-only code, so low.
- [low] test/model_download_gzip_test.dart:96-107 — the test asserts `ModelDownloadService.fileProblem` returns non-null for a gzipped file saved as `tokens.txt` and `'file missing'` for an absent file, but the production symbol `ModelDownloadService` is imported from `data/services/model_download_service.dart` which is not inlined here — the assertions' correctness depends on unseen production code, so this is stated as an assumption rather than a verified defect. Reachability: test-only, low.

## Coverage
test/model_download_gzip_test.dart — findings: 2
test/model_manager_test.dart — clean
## Coverage
test/models_test.dart — clean
test/ocr_cleanup_test.dart — clean
## Coverage
test/ocr_confidence_mapping_test.dart — clean
test/ocr_confidence_test.dart — clean
- [info] test/ocr_highlight_hitrate_test.dart:14 — test reads a fixture file synchronously with no existence check — if `test/fixtures/pp_ocr_raw.txt` is missing the test crashes with a raw FileSystemException instead of a meaningful assertion — guard with `expect(File(...).existsSync(), isTrue)` or a try/catch that fails with a clear message
- [info] test/ocr_highlight_hitrate_test.dart:84 — assertion threshold (0.90) is a magic constant with no tie to production code — the test pins a rate but nothing links 0.90 to a documented contract of `OcrHighlightMatcher.locate`; a regression to 0.89 would fail, but a silent change of the constant would not — reference a named constant or comment the derivation
- [info] test/ocr_highlight_matcher_test.dart:5-6 — helper `_l` hard-codes box geometry (left/width/height) identical for every line — if the matcher ever uses `left`/`width` for ordering, all lines tie and the test cannot detect it — vary one field or assert the matcher ignores them

## Coverage
test/ocr_highlight_hitrate_test.dart — findings: 2
test/ocr_highlight_matcher_test.dart — findings: 1
## Coverage
test/ocr_page_mapping_distribution_test.dart — clean
test/ocr_review_sheet_test.dart — clean
- [high] tool/analyze_orphaned_recordings.dart:19-20 — hard-coded Supabase project URL and publishable anon key in source — anyone with the repo can hit the project's auth/REST endpoints with this key; publishable keys are meant to be public but embedding them in tooling invites abuse and makes rotation impossible without a code change — read from env (`Platform.environment['SUPABASE_URL']`) with a hard failure when unset; same pattern at tool/orphan_sweep.dart:17-18 (one finding per root cause, both sites)
- [high] tool/analyze_orphaned_recordings.dart:28 — hard-coded password 'Test-passw0rd!' used to create real accounts on a live auth service — the script signs up throwaway accounts with a fixed, guessable password; combined with the public key anyone can log into those accounts later and, via the membership row the script creates, read production data RLS would otherwise block — generate a random password per run (e.g. from `Random.secure()` base64) and never persist it; same pattern at tool/orphan_sweep.dart:22
- [medium] tool/analyze_orphaned_recordings.dart:26 — `Random()` (non-cryptographic PRNG) seeds the account email — the email is not a secret, but the same weak-entropy source is the only randomness in the credential identity; if the password is ever made random this source is unsuitable for it — use `Random.secure()` for anything credential-adjacent; same pattern at tool/orphan_sweep.dart:21
- [medium] tool/analyze_orphaned_recordings.dart:130-139 — auth fallback silently retries signup as a password-grant token request and treats any JSON with `access_token` as success — a network/HTML error page that happens to parse as a Map with an unexpected `access_token` shape, or a signup rejection followed by a token grant for an account that does not exist, yields a `_Cred` whose `userId` may be null-checked but whose token scope is never validated; the script then proceeds to insert membership rows with an unverified credential — verify the response `user.id` matches the intended signup and fail loudly on mismatch; same pattern at tool/orphan_sweep.dart:115-125
- [medium] tool/analyze_orphaned_recordings.dart:142-157 — `_post` swallows every exception (`catch (_) { return null; }`) including TLS/socket errors, so an intercepted or downgraded connection is indistinguishable from "no such account" — the caller then falls back to the password-grant endpoint, sending the same credentials again over a connection whose failure cause was discarded — let the exception type surface (at least log it) and abort on transport errors rather than retrying blindly; same pattern at tool/orphan_sweep.dart:127-143
- [low] tool/analyze_orphaned_recordings.dart:30-36 — access token is placed in a plain `Authorization: Bearer` header on a SupabaseClient built from source constants — token lifetime is unbounded by the script (no refresh, no logout), so a leaked process environment or core dump carries a live session key; short-lived by Supabase design but the script never revokes it — call the auth `/logout` endpoint or `SupabaseClient.auth.signOut()` in a `finally` block; same pattern at tool/orphan_sweep.dart:27-28
- [low] tool/analyze_orphaned_recordings.dart:113-121 — cleanup delete is fire-and-forget with no `.select()` and no emptiness check — if RLS silently blocks the delete (as the sibling script explicitly guards against at tool/orphan_sweep.dart:90-104), the throwaway membership row persists and grants the throwaway account standing read access to the production; the script exits 0 either way — mirror the sibling's `.select('id')` + empty-result warning; same class at tool/orphan_sweep.dart:90-104 (already guarded there, listed here for the class-fix sweep)
- [info] tool/analyze_orphaned_recordings.dart:1, tool/orphan_sweep.dart:1 — `ignore_for_file: avoid_print` suppresses the lint for print-based tooling — acceptable for a CLI script, no action needed beyond noting the lint waiver is intentional

## Coverage
tool/analyze_orphaned_recordings.dart — findings: 7
tool/orphan_sweep.dart — findings: 0
- [medium] tool/sim_multi_user.dart:29-30 — hardcoded Supabase project URL and anon (publishable) key baked into the tool as defaults — anyone running the script against the default target hits a real project; the key is a publishable anon key (public by design) but the URL pins a specific tenant and invites accidental writes against production — verify it is not committed / move to env-only with no default — reachability: operator running offline tooling (low), but the default silently targets a live project so kept medium.
- [low] tool/sim_multi_user.dart:358-373 — auth helper sends credentials over the network and stores the returned access token in a local `_Cred` object with no zeroization; tokens are also echoed into client headers and printed user ids — acceptable for a local dev harness, but tokens linger in memory — clear tokens after use / avoid printing ids.
- [low] tool/sim_multi_user.dart:388-389 — `_authPost` swallows all exceptions with a bare `catch (_) { return null; }` — a network/TLS failure is indistinguishable from "not registered", and the caller then silently falls through to the password-grant path — surface the error message so failures are diagnosable.
- [info] tool/sim_multi_user.dart:361-363 — signup response is trusted without checking HTTP status; a non-2xx body that still parses as JSON with no `access_token` is treated the same as "already registered" and the code proceeds to the password grant — check `resp.statusCode` before falling through.
- [low] tool/sim_multi_user.dart:395-400 — `_uuid()` builds a v4-style UUID from `Random.secure()` (CSPRNG, good) but the version/variant nibbles are hand-assembled; not a security defect, only noting the construction is correct — no action needed.
- [low] tool/sim_multi_user.dart:412-416 — `_joinCode()` uses `Random.secure()` for a 6-char join code (~30 bits entropy) — adequate for a short-lived invite code in a test harness; fine as-is.
- [low] tool/parse_stats.dart:43 — reads the target file with `readAsStringSync` with no existence/permission guard; a missing path throws an unhandled exception with a raw stack instead of the usage message — wrap in a try/catch and print usage on failure.
- [low] tool/parse_stats.dart:31-32 — `--expect` consumes the next argv element unconditionally even if the next token is another flag (e.g. `--expect --json file`), silently mis-parsing — check that the next arg is not a flag before consuming it.
## Coverage
tool/parse_stats.dart — findings: 2
tool/sim_multi_user.dart — findings: 5
- [medium] tool/verify_cloud_recordings.dart:16 — publishable anon API key hardcoded in source — anyone with repo access learns the anon key; it is a public-by-design anon key so blast radius is limited, but verify it is not committed if the repo policy treats tool keys as secrets — rotate/replace with an env-provided key (`Platform.environment['SUPABASE_ANON_KEY']`) and fail fast when unset.
- [medium] tool/verify_cloud_recordings.dart:24 — hardcoded throwaway account password in source — the script signs up real accounts on a live Supabase project with a fixed password; combined with the predictable email pattern (`verify_<int32>@example.com`) the account is guessable by anyone who reads the repo, letting an outsider log into that account and read RLS-protected recordings the script joined — generate a random password per run (e.g. from `Random.secure()`/`Random()` base64) and never persist it.
- [medium] tool/verify_cloud_recordings.dart:22 — non-CSPRNG `Random()` used to derive the account identifier — `Random().nextInt(1 << 31)` seeds the email; not security-critical alone, but the same weak RNG pattern is what the checklist flags for identifiers/tokens; use `Random.secure()` for anything that gates access (here: the account email/password above).
- [low] tool/verify_cloud_recordings.dart:19-20 — default productionId/joinCode baked into argv fallback — running the tool with no args silently targets a fixed production and join code; an operator typo is masked and the script acts on an unintended production — require both args (`if (args.length < 2) { usage; exit(64); }`).
- [low] tool/verify_cloud_recordings.dart:48-50 — join-insert failure swallowed (`catch (e) { print; }`) — if the cast_members insert fails (RLS denies, already member, wrong code) the script continues to read recordings and downloads whatever RLS still returns, reporting "done" with exit 0 — treat join failure as fatal (rethrow or `exit(1)`) so the operator cannot mistake a failed join for a verified production.
- [low] tool/verify_cloud_recordings.dart:80-82 — download failure swallowed per-object — a failed download prints ✗ but the loop continues and the script exits 0; a partially-verified production is reported as success — accumulate failures and exit non-zero when any download failed.
- [low] tool/verify_cloud_recordings.dart:104-113 — `_auth` returns null on any network/parse error and the caller only prints a hint — a transient HTTP failure is indistinguishable from "email confirmation on"; the operator cannot tell which — include the underlying exception (or at least the HTTP status) in the failure message before `exit(1)`.
- [info] tool/verify_cloud_recordings.dart:15 — project URL hardcoded — not a secret (public URL), no action beyond consistency with the key finding above.

## Coverage
tool/verify_cloud_recordings.dart — findings: 7
- [low] tools/mlx-harness/Sources/harness/main.swift:98,104 — fixed seed (42) for MLXRandom makes vocoder noise deterministic across runs — reproducibility is intentional here (comparability harness), but if this binary is ever reused for production synthesis the output is predictable; document or gate behind a flag — reachability: operator running offline tooling.
- [low] tools/mlx-harness/Sources/harness/main.swift:32,36,53,82,87,100,106,111,115 — `try!` on file reads, JSON decode, model init, and synthesis means any malformed input or runtime failure crashes with no diagnostic — operator gets a bare crash instead of the `die()` message path; wrap in do/catch and `die(err)` — reachability: operator running offline tooling.
- [low] tools/mlx-harness/Sources/harness/main.swift:59 — unknown grapheme silently maps to token id 3 (a fallback constant) rather than failing or logging — a typo'd word list produces phoneme output that looks valid but is wrong; make the miss explicit (die or stderr warn) — reachability: operator running offline tooling.

## Coverage
tools/mlx-harness/Package.swift — clean
tools/mlx-harness/Sources/harness/main.swift — findings: 3

## Run stats

input 392750 tok (+58806 cached), output 40990 tok — sync requests, discounted — 107 files in 43m (148.3 files/h, 0.8 min/batch)
