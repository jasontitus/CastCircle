# Pi sweep review (perf focus) — CastCircle

Exhaustive per-file pass: 20 code files across 10 batches — model ds4/GLM-5.3-Flash-Q2:off — 2026-08-30.

## Findings

- [medium] ios/Runner/AppleSttPlugin.swift:619-628 — rmsLevel computes per-sample energy with a scalar Float loop instead of vDSP — the tap fires ~12×/sec at 4096 frames (line 316 comment), so this runs on the render thread every callback; a scalar loop over 4096 samples defeats NEON/vectorization and adds main-thread/render-thread work per buffer, growing with buffer size — replace the loop with `vDSP_vsq`/`vDSP_rmsqv` (Accelerate is already imported at line 9) or compute RMS on the file queue.
- [medium] ios/Runner/AppleSttPlugin.swift:38-55 — copyBuffer deep-copies every tap buffer channel-by-channel with scalar `update(from:count:)` calls inside the tap callback path — at ~12 callbacks/sec × 4096 frames this is per-sample scalar memcpy work on the render thread that scales with channel count and frame length; a failed copy silently drops the buffer (line 304 guard), so repeated allocation churn under memory pressure can also stall recording — allocate the copy once per format (reuse a pool) or use `memcpy`/vDSP copy per channel, and log/handle the nil-copy path instead of silently skipping.
- [medium] ios/Runner/AppleSttPlugin.swift:536-579 — detectSpeechRange reads the entire CAF through AVAssetReader sample-by-sample and builds `windowRMS`/`floats`/`carry` arrays with per-buffer allocations inside a `while let buffer = output.copyNextSampleBuffer()` loop — for a long take (minutes at 48 kHz) this is O(totalSamples) scalar work plus allocation churn per CMSampleBuffer on whatever thread runs the export block (line 438 dispatches it to a global queue, but the loop itself is still unbounded in input size); the 50ms-window RMS could be computed with vDSP on fixed-size chunks and the arrays pre-reserved — preallocate `windowRMS` capacity and reuse one `floats` scratch buffer, or use `AVAudioFile` + vDSP for streaming RMS.
- [low] ios/Runner/AppleSttPlugin.swift:548 — `var floats = [Float](repeating: 0, count: count)` allocates a fresh array per CMSampleBuffer inside the read loop — allocation churn proportional to number of buffers × sample count; hoist a reusable scratch buffer sized to the largest window and reuse it across iterations.
- [low] ios/Runner/AppleSttPlugin.swift:594-599 — first/last speech window search is two linear scans over `windowRMS` — O(n) but n is bounded by recording length; acceptable, no fix needed beyond noting it is already linear (not quadratic), so no action.
- [info] ios/Runner/AppDelegate.swift:38-86 — plugin registration performs 10 sequential registrar lookups and allocations at engine init; this is once-per-launch work on the main thread at startup, not a hot path — no consequence at scale; no fix required.

## Coverage
ios/Runner/AppDelegate.swift — findings: 0
ios/Runner/AppleSttPlugin.swift — findings: 4
- [medium] ios/Runner/AudioAnalysisPlugin.swift:69-77 — per-sample scalar RMS/peak loop over the entire decoded PCM buffer with Swift subscripts and Double accumulation — for a multi-minute line recording at 48 kHz stereo this is tens of millions of subscript reads and per-sample branches on the userInitiated QoS global queue, blocking that thread and delaying the Dart-side result for seconds while the whole buffer is held in one AVAudioPCMBuffer allocation (line 52-53, frameCapacity = full file length) — smallest safe fix: compute RMS/peak with vDSP (vDSP_rmsmq / vDSP.maximum / vDSP.svsMul on the channelData pointers) or at minimum chunk the buffer read into fixed-size windows so peak memory and loop time stay bounded per chunk.
- [medium] ios/Runner/AudioAnalysisPlugin.swift:52-53 — whole-file PCM buffer allocated in one shot (frameCapacity = AVAudioFrameCount(file.length)) — a long recording (e.g. 10 min @ 48 kHz stereo float32 ≈ 46 MB, and grows linearly with duration) is transiently resident plus the copy the loop touches, risking memory-pressure eviction of the app during analysis of large files — smallest safe fix: read the file in fixed-size chunks (e.g. 1 s of frames) and accumulate sumSquares/peak across chunks, bounding the allocation to the chunk size.
- [low] ios/Runner/BackgroundDownloadPlugin.swift:232-252 — lastProgressEmit is a per-modelId dictionary that is never pruned on completion/cancel/error — after many downloads over app lifetime the dict retains one entry per ever-downloaded modelId (small structs, but unbounded growth in a long-lived Runner process) — smallest safe fix: removeValue(forKey:) in didFinishDownloadingTo/didCompleteWithError/cancelDownload alongside activeDownloads cleanup.
- [low] ios/Runner/BackgroundDownloadPlugin.swift:67-75,77-82 — persistDownloadRecord/removeDownloadRecord read-modify-write the entire UserDefaults records dictionary on every start/cancel — with hundreds of persisted model records each startDownload/cancelDownload serializes and rewrites the whole array on the main thread (delegateQueue is .main, line 44) — smallest safe fix: keep records in a small on-disk plist/JSON keyed per modelId, or batch writes; impact is bounded by realistic model counts so low.
## Coverage
ios/Runner/AudioAnalysisPlugin.swift — findings: 2
ios/Runner/BackgroundDownloadPlugin.swift — findings: 2
- [medium] ios/Runner/KokoroMLXPlugin.swift:51-85 — `synthesize` handler runs `Task { ... }` on the plugin's actor (Flutter method handlers are invoked on the main thread), so the `await kokoroService.synthesize` suspension is fine, but the surrounding `Task` body — including the `MainActor.run` hops and any non-`await` setup work — executes on the main actor context of the enclosing `Task`; more importantly, if `KokoroMLXService.synthesize` performs its GPU inference on the calling actor rather than detaching to a background executor, the whole MLX inference (seconds of GPU/CPU work for long text) blocks the main thread and the Flutter UI — consequence: app becomes unresponsive / watchdog kill during synthesis of long inputs; smallest safe fix: wrap the inference call in `Task.detached(priority: .userInitiated)` (or have `KokoroMLXService.synthesize` be `nonisolated` and hop internally) so the heavy work leaves the main actor. Reachability: any authenticated user of the app on-device (local app user), trigger: every `synthesize` channel call; growing input: `text` string length, currently unbounded in this handler (no length cap before dispatching to the service); work per unit: full TTS inference over the text; affected resource: main thread / Flutter UI responsiveness. — assumption: `KokoroMLXService.synthesize` is not visible in this batch; if it already detaches to a background executor this finding collapses to `info`/none.
- [low] ios/Runner/KokoroMLXPlugin.swift:96-98 — `unloadModel` runs `kokoroService.unloadModel()` synchronously on the main thread inside the method handler — if unloading frees MLX weights / GPU buffers it can take hundreds of ms and block the UI; consequence: main-thread stall on every unload call; smallest safe fix: run unload in a `Task` and `result(true)` on completion. Reachability: local app user; trigger: each unloadModel call; bound: model size (fixed weights, not growing input), so low.
- [low] ios/Runner/KokoroMLXPlugin.swift:100-110 — `deleteModel` performs synchronous filesystem delete of the model directory on the main thread in the handler — deleting a multi-GB model directory is I/O-bound main-thread work; consequence: UI stall during model deletion; smallest safe fix: move the delete into a `Task`/background queue and complete the result afterwards. Reachability: local app user; trigger: each deleteModel call; bound: model directory size (fixed, not per-item growth), so low.
- [info] ios/Runner/ContactPickerPlugin.swift:32-45 — `pickContact` presents the picker and stores `pendingResult` without any guard against a second concurrent `pickContact` call; a second call overwrites `pendingResult`, leaving the first Flutter result never resolved (Flutter channel result leak) and the first picker's callback firing on a stale result block — consequence: dropped channel result / possible wrong completion under rapid double-tap; smallest safe fix: guard `pendingResult == nil` (or cancel the in-flight picker) before presenting. This is a correctness/robustness note, not a performance defect; included at info because it is exhibited by the code. Reachability: local app user; trigger: double invocation of pickContact.

## Coverage
ios/Runner/ContactPickerPlugin.swift — findings: 1
ios/Runner/KokoroMLXPlugin.swift — findings: 3
- [info] ios/Runner/KokoroMLXService.swift:344-351 — prune pass enumerates every cache file with per-file resourceValues calls on a global-utility queue — with thousands of cached WAVs the first synthesize call after launch pays an O(n) directory walk with per-file stat work before synthesis can start — smallest safe fix: cap the enumeration (e.g. `contentsOfDirectory` with a size pre-filter or limit entries collected to the oldest N) or run the prune fully detached from the first-synthesis critical path.
- [low] ios/Runner/KokoroMLXService.swift:326-331 — cache key includes full `text` hashed via SHA-256 per synthesize call — work per call is O(len(text)) hashing plus a SHA256 digest; for very long scripts (whole-paragraph lines) this adds measurable per-line latency on the synthesis path, though bounded by line length — smallest safe fix: none needed unless scripts exceed ~10KB lines; otherwise acceptable (reporting as info-level observation only if scale threshold unmet).

Wait — re-checking the gate: the SHA-256 hash of a line of dialogue is microseconds; no realistic consequence at any plausible scale. Drop that line.

## Coverage
ios/Runner/KokoroMLXService.swift — findings: 1
ios/Runner/KokoroVendored/Albert/AlbertEmbeddings.swift — clean
## Coverage
ios/Runner/KokoroVendored/Albert/AlbertEncoder.swift — clean
ios/Runner/KokoroVendored/Albert/AlbertLayer.swift — clean
## Coverage
ios/Runner/KokoroVendored/Albert/AlbertLayerGroup.swift — clean
ios/Runner/KokoroVendored/Albert/AlbertModelArgs.swift — clean
- [info] ios/Runner/KokoroVendored/Albert/AlbertSelfAttention.swift:75-84 — attention computed as hand-rolled matmul/softmax chain on CPU via MLX without fused scaled-dot-product attention — at sequence length L and hidden H, lines 75/84 materialize full (L×L) score and prob tensors plus separate transposed key/value copies (lines 70-74), so memory and compute grow O(L²·H) with allocation churn per layer call; consequence: TTS inference with long scripts allocates and frees large intermediates each layer, raising peak memory and latency on device — smallest safe fix: replace the explicit matmul/softmax/transposes with a fused `MLXFast.scaledDotProductAttention` (or `MLXNN` attention primitive) so scores stay materialized once and transposes become metadata views.
- [info] ios/Runner/KokoroVendored/Albert/AlbertSelfAttention.swift:47-59,87-96 — per-call reshape/transpose of query/key/value into head-split tensors rebuilds shape arrays and copies data every invocation — each `callAsFunction` on every ALBERT layer re-runs three `transposeForScores` calls plus a final reshape loop over shape dims (lines 51-56, 90-94), doing scalar Swift array appends and full tensor copies per layer per token batch; consequence: with many encoder layers and repeated inference calls this adds measurable CPU overhead and allocation churn on the main thread during synthesis — smallest safe fix: precompute static reshape metadata once at init and use `MLX` view/transpose ops (which are lazy) instead of rebuilding `[Int]` shape lists per call.
- [info] ios/Runner/KokoroVendored/Albert/CustomAlbert.swift:29-35 — attention mask is reshaped and negated per forward pass without caching — every `callAsFunction` re-derives `attentionMaskProcessed` from the raw mask even when the mask is identical across repeated synthesis calls with the same token layout; consequence: redundant broadcast-ready tensor construction adds allocation and compute per inference call, noticeable when many short utterances are synthesized back-to-back — smallest safe fix: memoize the processed mask keyed on mask shape/content (or compute it once per session) instead of rebuilding each call.

## Coverage
ios/Runner/KokoroVendored/Albert/AlbertSelfAttention.swift — findings: 2
ios/Runner/KokoroVendored/Albert/CustomAlbert.swift — findings: 1
- [info] ios/Runner/KokoroVendored/BuildingBlocks/AdaINResBlock1.swift:87,94 — scalar transcendental ops (`MLX.sin(...).pow(2)`) inside per-block loop over full activation tensors — each of the 3 residual blocks executes two full-tensor sin+pow passes on the audio-length-sized channel axis; on MLX GPU this is fine, but if these tensors fall back to CPU evaluation (small batch / Metal unavailable) the cost is O(T·C) transcendental evaluations per block per inference — consequence: TTS synthesis latency grows linearly with utterance length and doubles per block when GPU delegate is unavailable — smallest safe fix: none needed if MLX guarantees GPU execution; otherwise precompute on GPU stream or replace sin²·(1/a) with a fused activation to cut two transcendental passes per block — reachability: operator/device-local inference only (low).
- [info] ios/Runner/KokoroVendored/BuildingBlocks/AdaINResBlock1.swift:28-72 — four separate `for` loops over the same 3-element collection during init (convs1, convs2, adain1/adain2, alphas) — redundant iteration is negligible at N=3 and runs once per model construction, not per inference — consequence: none observable at this scale — smallest safe fix: none (reporting only because the loop-over-constant-size pattern is bounded and one-time; no unbounded work exists) — reachability: model-load path only (low).

## Coverage
ios/Runner/KokoroVendored/BuildingBlocks/AdaIN1d.swift — clean
ios/Runner/KokoroVendored/BuildingBlocks/AdaINResBlock1.swift — findings: 2
- [info] ios/Runner/KokoroVendored/BuildingBlocks/AdaLayerNorm.swift:25-30 — variance computed as `mean(centered*centered)` materializes a full extra tensor (`centered` and its square) instead of using a fused variance reduction — extra O(N) allocations and a second full pass over the sequence per AdaIN call, on the audio/TTS render path — use a fused `MLX.variance(x, axes:[-1], keepDims:true)` (or compute mean/var in one reduction) so no intermediate tensor is materialized
- [info] ios/Runner/KokoroVendored/BuildingBlocks/AdainResBlk1d.swift:96-133 — residual/shortcut paths perform 8+ `MLX.swappedAxes` transposes per invocation (each materializing a layout change around every conv/pool op) — repeated O(N) copies of the full activation tensor on every block call in the TTS render loop — keep activations in the layout the conv/pool ops expect (or use layout-agnostic conv wrappers) so the per-op transposes collapse to zero
## Coverage
ios/Runner/KokoroVendored/BuildingBlocks/AdaLayerNorm.swift — findings: 1
ios/Runner/KokoroVendored/BuildingBlocks/AdainResBlk1d.swift — findings: 1
- [medium] ios/Runner/KokoroVendored/BuildingBlocks/ConvWeighted.swift:122-148 — per-forward re-transposition of the normalized weight defeats the frozen-weight cache — `callAsFunction` calls `normalizedWeight()` (cached, line 123) but then, whenever `x.shape.last != weight.shape.last` and `groups == 1`, computes `weight.transposed()` (line 146) fresh on every forward; the transposed tensor is not stored in `cachedNormalizedWeight`, so each synthesis forward re-issues a full weight-tensor transpose (memory copy of the entire conv kernel) on the GPU/CPU per conv layer per call — at TTS inference scale (many conv layers × many synthesis calls per utterance × per user request) this is redundant O(weight-size) work per layer per forward — smallest safe fix: cache the transposed variant alongside the normalized weight (e.g. a second `private var cachedTransposedWeight: MLXArray?` populated in `normalizedWeight()` or a `transposedWeight()` helper with the same memoization) so the transpose happens once at first forward, identical to the existing weight-norm caching pattern.
- [medium] ios/Runner/KokoroVendored/BuildingBlocks/ConvWeighted.swift:150-177 — same un-cached per-forward transpose in the second `callAsFunction` overload — identical defect class to the first overload: line 175 computes `weight.transposed()` per forward with no memoization, re-doing the full kernel transpose every call for the mismatched-channel-shape path — consequence: duplicated O(weight-size) GPU work per conv layer per synthesis forward, multiplied across every conv layer and every generated utterance — smallest safe fix: memoize the transposed weight (single shared `transposedWeight()` helper used by both overloads) so the transpose is computed once and reused, matching the existing `cachedNormalizedWeight` pattern.

## Coverage
ios/Runner/KokoroVendored/BuildingBlocks/Conv1dInference.swift — clean
ios/Runner/KokoroVendored/BuildingBlocks/ConvWeighted.swift — findings: 2

## Run stats

input 56233 tok (+14140 cached), output 3975 tok — sync requests, discounted — 20 files in 4m (257.1 files/h, 0.5 min/batch)
