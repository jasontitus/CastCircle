# DS4 Sweep Notes — Performance Review (batch 7)

## Scope
Performance-only review of 20 Supabase SQL migrations + 18 Dart test files in CastCircle.

## Files inspected (all 38, read in full)
- Migrations: `supabase/migrations/20260314061409_initial_schema.sql` through `20260703170000_recordings_delete_policy.sql`
- Tests: `test_driver/integration_test.dart`, `test/recording_sync_service_test.dart`, `test/script_parser_import_test.dart`, `test/widget_test.dart`, `test/shakespeare_import_test.dart`, `test/sample_script_test.dart`, `test/parser_accuracy_test.dart`, `test/ocr_confidence_test.dart`, `test/toast_autodismiss_test.dart`, `test/voice_config_test.dart`, `test/tts_text_chunking_test.dart`, `test/recording_path_safety_test.dart` + 6 more

## Checklist items (from performance-review SKILL.md)
- Nested iteration over unbounded collections: hits=0 | examined=all migration SQL + all test Dart files | findings=0 | dismissed=N/A
- N+1 queries in loops: hits=0 | examined=all RPC functions, RLS policies, FakeCloud mock | findings=0 | dismissed=N/A (FakeCloud.fetchRecordings returns filtered list from memory; no per-row query)
- Per-item remote I/O / file ops inside loops: hits=2 candidates → both dismissed (see below)
- Cache/map that only grows: hits=1 candidate → FakeCloud.rows/storage grow but are test-scoped | findings=0 | dismissed=test mock, not production code
- String concatenation in loops: 1 hit (`generate_join_code`) → bounded to 6 iterations, called once per new production (not hot path) — dismissed
- Missing pagination / load-all-then-slice: hits=0 on migrations; test files use small fixed datasets — findings=0

## Suspicious-but-dismissed false positives
1. `supabase/migrations/20260315_cast_join_code.sql` line 62-64: `FOR i IN 1..6 LOOP result := result || substr(...)` — string concat in loop, but bounded to exactly 6 iterations and called once per production creation (not a hot path). Per SKILL.md false-positive bullet: "Nested loops over small, bounded collections" + "Queries inside loops in migrations... are not hot paths."
2. `test/recording_sync_service_test.dart` line 106-114: retry loop with `Future.delayed(50ms)` up to 10 times — this is test cleanup (temp dir deletion), not production code, and bounded to 10 iterations. Per SKILL.md false-positive bullet: "Queries inside loops in... test setup — not hot paths."
3. `test/sample_script_test.dart` line 21-24, 85-96 etc.: `file.readAsStringSync()` calls — synchronous file reads but only for small sample scripts (Pride & Prejudice ~70KB), tagged `@Tags(['extended'])`, skipped during normal test runs. Per SKILL.md false-positive bullet: "Whole-file read() of small bounded files" + "Queries inside loops in... test setup."
4. `test/recording_sync_service_test.dart` FakeCloud.rows/storage (lines 16-19): module-level growing collections — but this is a test mock, not production code. Test-scoped, cleaned up per-test via setUp/tearDown.

## Coverage note
All 38 target files were read in full (>8 required). No external tools installed (ruff/staticcheck/eslint/madge) — used checklist greps as fallback. The repo scope is limited to SQL migrations and Dart test files only; no production application hot paths exist within the reviewed set, so findings are expected to be zero or near-zero per SKILL.md's "zero-findings gate" guidance.