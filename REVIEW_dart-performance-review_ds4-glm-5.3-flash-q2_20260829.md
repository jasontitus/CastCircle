# Pi sweep review (perf focus) — CastCircle

Exhaustive per-file pass: 145 code files across 78 batches — model ds4/GLM-5.3-Flash-Q2:off — 2026-08-29.

## Findings

- [medium] lib/data/database/app_database.dart:130-142 — `_step` swallows any error whose message merely contains 'already exists' (e.g. "table already exists", "index already exists" from a failed createIndex) as benign, so a genuinely failed migration step is treated as applied and Drift advances user_version — consequence: the missing column/index is never repaired, every later hot query (getScriptLines/getScenesForProduction/getCastForProduction at :214/:232/:299, which run per production load) degrades to full-table scans or dies with "no such column" at scale, and the migration never re-runs — smallest safe fix: narrow the benign match to the exact duplicate-column/already-applied error codes (e.g. match 'duplicate column name' only, or check SQLite errno/Drift's specific exception type) and rethrow everything else.
- [low] lib/data/database/app_database.dart:316-333 — `_openConnection` runs `PRAGMA journal_mode=WAL` / `synchronous` / `busy_timeout` via `db.execute` inside the LazyDatabase opener on the main isolate before the DB is usable; these are one-time setup statements (not per-request), so the cost is bounded and only paid at startup — consequence: negligible startup latency, no per-request impact — smallest safe fix: none needed; if startup jank is observed, move the PRAGMA setup into `NativeDatabase.createInBackground`'s setup callback already used (it is) — no change required.
- [info] lib/data/database/app_database.dart:220-224 — `insertScriptLines` batches all entries in one `batch()` (good), but `insertAll` with `insertOrReplace` on a table whose primary key is a random `id` (line 52) means re-importing a script inserts brand-new rows rather than replacing existing ones, so the table grows unboundedly with each re-import until `deleteScriptLinesForProduction` is called — consequence: unbounded row growth per re-import if callers don't delete first (caller not visible in this batch; assumption: an import flow re-inserts without a prior delete) — smallest safe fix: verify callers delete-then-insert; if not, add a deterministic primary key or delete-by-production before insertAll.
- [info] lib/data/models/cast_member_model.dart:10-13 — `CastRole.fromString` builds the name map via `asNameMap()` on every call; `asNameMap()` is O(values) per invocation and this is called per cast-member row deserialized (per production load) — consequence: negligible at realistic cast sizes (tens of rows); no measurable impact — smallest safe fix: hoist to a `static final` map if profiling ever shows it.
- [info] lib/data/models/cast_member_model.dart:48-70 — `copyWith` allocates a new instance per field update; called per member edit, not per frame — consequence: negligible — smallest safe fix: none.

## Coverage
lib/data/database/app_database.dart — findings: 3
lib/data/models/cast_member_model.dart — findings: 2
- [info] lib/data/models/production_models.dart:23-42 — copyWith allocates a new Production on every call even when no argument differs; if callers use copyWith in per-frame/per-item update loops (not visible in this batch), each call allocates a fresh object and all its fields — allocation churn proportional to call count with no bound visible here — smallest safe fix: none needed unless a hot caller exists; verify callers before changing (assumption: callers not shown).
- [info] lib/data/models/rehearsal_models.dart:37-38 — struggledLines getter runs a fresh where().toList() on every access, allocating a new list each time lineAttempts is scanned; if UI code reads it per frame (not visible in this batch), each read is O(n) over lineAttempts plus a list allocation — smallest safe fix: none needed unless a hot caller exists; verify callers before changing (assumption: callers not shown).
## Coverage
lib/data/models/production_models.dart — clean
lib/data/models/rehearsal_models.dart — clean
- [low] lib/data/models/voice_preset.dart:148 — `VoicePresets.byId` runs a linear `firstWhere` over the 8-element `all` list on every call — negligible at 8 presets, but if callers resolve presets per line/character during script import or per rebuild in a picker, the scan repeats with no bound on call count — precompute a `Map<String, VoicePreset>` from `all` once (or memoize) and look up by key, falling back to `modernAmerican` on miss.
- [low] lib/data/repositories/production_repository.dart:44-53 — `deleteProduction` performs per-recording async file existence checks and deletes sequentially inside a loop — each iteration is an awaited filesystem round-trip (stat + unlink), so a production with N recordings serializes N×2 I/O syscalls on the caller's isolate; at hundreds of recordings this blocks the delete flow for seconds — batch with `Future.wait` over the recordings (or delete the directory/paths concurrently) so the I/O overlaps instead of serializing.
- [low] lib/data/repositories/production_repository.dart:56-60 — cascade delete issues five sequential awaited DB statements (recordings, script lines, scenes, cast, production) — each is a separate round-trip through Drift; wrapping them in a single `_db.transaction` (as `saveScriptLines`/`saveScenes` already do at lines 142/191) removes four extra round-trips per delete and makes the cascade atomic — same class as the transaction pattern already present in this file.
- [info] lib/data/repositories/production_repository.dart:214-222 — `getRecordings` builds the map with incremental `map[...] = ...` per row after an awaited fetch — allocation churn is trivial (one entry per recording) and the query itself is indexed; no bound problem exhibited, so no action needed beyond noting the shape.

## Coverage
lib/data/models/voice_preset.dart — findings: 1
lib/data/repositories/production_repository.dart — findings: 3
- [low] lib/data/services/audio_level_service.dart:60-62 — cache eviction is FIFO, not LRU: `remove(_gainCache.keys.first)` drops the oldest-inserted entry regardless of recency, so a hot path that repeatedly re-invokes `volumeFor` for a small working set can still evict entries that would be reused, causing repeated MethodChannel `loudness` round-trips (native audio analysis) for the same paths — trigger: every cache miss on a path whose entry was FIFO-evicted despite recent use; growing input: `_gainCache` bounded at 512 entries but eviction order wrong; work per unit: one full native loudness analysis (file decode + RMS scan) per re-miss; affected resource: platform channel + native analyzer CPU on the audio thread/main isolate; consequence: audible-latency volume computation repeated for hot clips once cache churns past 512 distinct paths (e.g. a long production with many re-recorded files); smallest safe fix: on cache hit, re-insert the key (remove+re-add) so `keys.first` is genuinely least-recently-used, or use a proper LRU structure. Reachability: any authenticated user playing recordings on their own device = medium (per rules, but performance-only lens keeps it low since it is a latency-only, self-inflicted cost) — final severity: low.
- [info] lib/data/services/audio_level_service.dart:68-70 — `prefetch` is fire-and-forget but `volumeFor` is async and its returned Future is discarded; if the caller later calls `volumeFor(path)` before the in-flight prefetch resolves, two concurrent `loudness` channel invocations run for the same path (the cache is only populated after the first completes) — trigger: prefetch followed by immediate playback; growing input: none (single path); work per unit: duplicate native analysis; affected resource: platform channel/native analyzer; consequence: wasted duplicate work and a possible transient wrong-volume (1.0 default) on first play; smallest safe fix: store an in-flight `Future<double>` per path and return it from both `prefetch` and `volumeFor`. Reachability: authenticated user on own device; latency-only, so low.
- [low] lib/data/services/audio_level_service.dart:36-55 — on any exception or non-Map/non-finite/non-negative result the fallback resolves to `volume = 1.0` and that 1.0 is then cached at line 63 as if it were a real measurement; a path whose analysis transiently failed (e.g. plugin missing on Android, decode hiccup) is pinned to 1.0 until `invalidate`/eviction, so a genuinely too-loud recording keeps blasting with no retry — trigger: first play of a path whose analysis fails once; growing input: `_gainCache` entry per failed path, bounded at 512; work per unit: none after caching (that is the point); affected resource: playback loudness correctness; consequence: the normalization feature silently no-ops for those clips for the rest of the session; smallest safe fix: do not cache the fallback 1.0 for failure cases (only cache measured values), or cache with a short TTL / retry on next miss. Reachability: authenticated user on own device; feature-degradation not crash, so low. (Direction-of-failure: the fallback picks the safer audible value — playback untouched — but caching it makes the permissive result sticky.)
- [info] lib/data/services/analytics_service.dart:10-11 — `_analytics` getter re-evaluates `firebaseAvailable` and the conditional `FirebaseAnalytics.instance` access on every one of the ~12 log* call sites; if `firebaseAvailable` is a cheap const/flag this is negligible, but if it is a platform check it repeats per event — trigger: each analytics event; growing input: none; work per unit: one boolean check (+ possible instance lookup); affected resource: negligible CPU; consequence: none observable at any realistic event rate; smallest safe fix: hoist into a lazily-initialized field. Not reported as a defect given no evidence of cost — listed here only because the getter is re-evaluated per call; severity info, and only worth acting on if `firebaseAvailable` is non-trivial (assumption: it is defined in main.dart, not visible here).

## Coverage
lib/data/services/analytics_service.dart — findings: 0
lib/data/services/audio_level_service.dart — findings: 3
- [low] lib/data/services/deep_link_service.dart:94 — broadcast StreamController held for the app's lifetime with no subscriber management — every deep-link event is delivered to zero listeners if no widget ever subscribes, and the controller plus its stream stay allocated for the whole session even when unused — smallest safe fix: use a lazily-created broadcast controller or close it when the first/last listener unsubscribes.
- [low] lib/data/services/deep_link_service.dart:119 — `uriLinkStream.listen` subscription is never stored or cancelled in `dispose()` — the platform link stream keeps delivering `_handleUri` (parsing, debugPrint, log writes) after `dispose()` closes only the controller, so late events still run the full parse/log path — smallest safe fix: keep the `StreamSubscription` and cancel it in `dispose()`.
- [info] lib/data/services/deep_link_service.dart:132 — `debugPrint` on every incoming link plus a synchronous `File.writeAsStringSync` per log entry via DebugLogService.logError (debug_log_service.dart:187) runs on the UI isolate at link-arrival time — each rejected/valid link costs a main-thread disk append; at normal invite-tap frequency this is negligible, but a burst of malformed links (e.g. a hostile app-link flood) serializes sync I/O on the main thread — smallest safe fix: route log writes through the periodic flush buffer instead of per-entry sync appends.

## Coverage
lib/data/services/contact_picker_service.dart — clean
lib/data/services/deep_link_service.dart — findings: 3
- [low] lib/data/services/demo_production_service.dart:103-112 — `_preselectCharacter` runs a linear scan + sort of `script.characters` on every demo load, and the sort comparator allocates `compareTo` closures per element — work is O(n log n) per load with n = number of characters in the parsed Hamlet script (tens, bounded by the bundled asset) — consequence is negligible at that scale; only worth noting because the sort is re-run whenever the prefs key is absent (first run per install) — smallest safe fix: none needed at this scale, or memoize the "most lines" character on the parsed script.
- [info] lib/data/services/demo_production_service.dart:119 — `rootBundle.loadString` re-reads and re-parses the entire bundled demo script on every `load()` call (no caching of the parsed `ParsedScript`), so repeated demo entry re-does asset I/O + full text parse each time — trigger is user-driven demo entry, input bounded by the bundled asset, so impact is small; smallest safe fix: cache the parsed script in a static field after first parse.
- [low] lib/data/services/espeak_heteronyms.dart:80-91 — `apply` runs 5 sequential `replaceAllMapped` regex passes over every line of dialogue, each allocating a new output string and a closure per match — trigger is every TTS line of dialogue (hot path during rehearsal playback), input is one line of dialogue (short, bounded), work is 5 regex scans + string rebuilds per line — consequence is minor per-line allocation churn on the TTS prep path, not a bottleneck at line-length scale; smallest safe fix: none needed, or combine rules into a single alternation regex if profiling ever shows it matters.

## Coverage
lib/data/services/demo_production_service.dart — findings: 2
lib/data/services/espeak_heteronyms.dart — findings: 1
- [medium] lib/data/services/frame_stats_service.dart:31-43 — `_onTimings` runs on every frame callback and appends to two growable `List<double>` plus a jank counter, but `_report` is only invoked when the 30s window elapses AND `_buildMs.isNotEmpty`; between reports the lists grow unbounded per frame (60fps → ~1800 entries/s, ~54k doubles per 30s window, and if `_buildMs` is empty while `_rasterMs` is not, `_rasterMs` keeps growing with no report/clear) — allocation churn and GC pressure on the UI isolate during the very frames being measured — smallest safe fix: preallocate fixed-size ring buffers (e.g. `List<double>` with capacity cap or `Float64List` of fixed length with a rolling index) and clear both lists whenever either report fires.
- [low] lib/data/services/frame_stats_service.dart:51-52 — `_report` copies and sorts two lists (`List<double>.of(...)..sort()`) on the UI isolate inside the frame-timing callback path; at ~54k entries per 30s window this is an O(n log n) sort (~hundreds of thousands of comparisons) executed synchronously on the main thread, potentially janking the frame that triggered the report — smallest safe fix: compute percentiles incrementally (e.g. maintain a small histogram or use a running p90 approximation) or move the sort into `Isolate.run` / a compute helper.
- [low] lib/data/services/frame_stats_service.dart:45-46 — `_pct` indexes `sorted[((sorted.length - 1) * p).round()]` without clamping; for p=0.99 and length 1 the index is 0 (fine), but for a list of length 0 the expression `(-1 * p).round()` yields `-1` and throws a RangeError; `_report` is guarded by `_buildMs.isNotEmpty` but `_rasterMs` may be empty while `_buildMs` is not, so `_pct(r, ...)` can crash the report path — smallest safe fix: guard `if (sorted.isEmpty) return 0.0;` at the top of `_pct`.
- [info] lib/data/services/frame_stats_service.dart:24-29 — `install()` registers `addTimingsCallback` but there is no uninstall/teardown path shown; if the service is re-installed after a hot-restart the callback list could accumulate duplicate handlers, doubling per-frame work — smallest safe fix: verify whether `addTimingsCallback` deduplicates; if not, track and remove the callback in a matching `dispose`/`uninstall` (assumption: no teardown exists in this file; caller behavior not visible).
- [medium] lib/data/services/kokoro_onnx_service.dart:203-211 — `synthesize` awaits `File(cachePath).exists()` (an async filesystem round-trip) on every synthesis request before deciding cache hit; for urgent (actor-waiting) requests this adds a disk stat latency ahead of the queue/pump path, and for repeated replays of the same line the stat is re-done each call — smallest safe fix: keep a small in-memory `Set<String>`/`Map` of known-cached keys populated on hit and on adoption (rename), falling back to the stat only on miss.
- [low] lib/data/services/kokoro_onnx_service.dart:208-209 — on every cache hit `synthesize` fires `File(cachePath).setLastModified(DateTime.now())` unawaited; each replay performs a filesystem metadata write (open+utimens) that the comment itself says is only needed for LRU ordering — at high replay rates this is per-request I/O churn on the UI isolate's async I/O thread pool — smallest safe fix: batch mtime touches (e.g. only touch when the entry is older than N seconds, or touch during prune instead of per hit).
- [medium] lib/data/services/kokoro_onnx_service.dart:291-296 — `_schedulePrune`'s `Isolate.run` closure does `Directory(dir).listSync()` and `e.statSync()` for every cached WAV; at steady state the comment says "thousands of files", so this is thousands of synchronous stat syscalls inside the prune isolate each time the cache dir is first created per app run; while it runs in a separate isolate it still burns CPU/IO and delays the prune result — smallest safe fix: use `Directory.list(recursive: false, followLinks: false)` async enumeration with `File.stat()` batched, or track cumulative cache size incrementally (add on write, subtract on delete) so the walk is only needed when the budget is exceeded.
- [low] lib/data/services/kokoro_onnx_service.dart:289-296 — the prune closure builds an `entries` list of `(File, DateTime, int)` records for every cached file and a running `total`; for thousands of WAVs this allocates thousands of tuples plus the sort at line 298 — acceptable inside an isolate, but the allocation is repeated on every prune run — smallest safe fix: reuse a preallocated list or prune with a bounded max-heap of the oldest K entries instead of materializing all entries.
- [low] lib/data/services/kokoro_onnx_service.dart:471-472 — `_writeWav` writes the entire WAV synchronously (`writeAsBytesSync`) inside the synthesis isolate's command loop; for a long line (e.g. 60s at 24kHz mono 16-bit ≈ 2.8 MB) this is a multi-megabyte synchronous write blocking the isolate's event loop between chunk polls, delaying subsequent cancel-poll responsiveness — smallest safe fix: write via `File(path).writeAsBytes(...)` awaited, or stream samples in chunks so the loop can process the next command between writes.
- [low] lib/data/services/kokoro_onnx_service.dart:480-505 — `_writeWav` allocates a `ByteData(44 + n*2)` and converts sample-by-sample with per-sample closure calls (`s()` helper writes byte-by-byte via `setUint8` in a loop); for a 2.8 MB WAV that is ~5.6M `setUint8` calls plus a full-size buffer allocation per synthesis — smallest safe fix: write the 44-byte header once and stream PCM samples with `setInt16` directly into a preallocated `Uint8List` (or use `ByteData` views), avoiding the byte-by-byte string-copy helper.
- [low] lib/data/services/kokoro_onnx_service.dart:501-505 — the sample conversion loop calls `b.setInt16` per sample with clamping branches; for tens of thousands of samples per line this is fine per-call but is repeated for every synthesis including cache misses during prefetch — smallest safe fix: hoist the clamp into a min/max expression and write into a reusable scratch buffer if profiling shows it matters (info-level; only act on measured jank).
- [info] lib/data/services/kokoro_onnx_service.dart:43-44,161-163 — `_queue` is a growable `List<_Req>` used with `insert(0, req)` (front-op) and `removeAt(0)` in `_pump`; queue depth is bounded by user request rate and drained by `_pump`, so unbounded growth is unlikely, but the front-insert is O(n) per urgent request — smallest safe fix: use a `Queue<_Req>` (doubly-linked) or keep insertion at the tail with a priority flag; only worth doing if queue depth routinely exceeds a handful.
- [info] lib/data/services/kokoro_onnx_service.dart:256-257 — `_cacheDirPath` and `_pruneScheduled` are static fields on the service; `_schedulePrune` is guarded so pruning runs at most once per app run, which bounds the walk cost — no action needed beyond the per-run cost noted above.
- [info] lib/data/services/kokoro_onnx_service.dart:50 — `_cancelBelow` is a never-freed FFI allocation (intentional per comment); a single 4-byte leak per process lifetime is negligible — no action.
- [info] lib/data/services/kokoro_onnx_service.dart:57-66 — `voiceIds` is a const map; no per-call allocation — no action.
- [info] lib/data/services/kokoro_onnx_service.dart:191-252 — `synthesize` allocates one `_Req` and one `Completer` per request; request rate is user-driven and each is drained by `_pump`/completer — no unbounded collection — no action.
- [info] lib/data/services/kokoro_onnx_service.dart:268-270 — cache key uses `crypto.sha1.convert(utf8.encode('$text|$voice|$speed'))`; `utf8.encode` allocates a byte list per request proportional to text length; text is a script line (bounded), so cost is negligible — no action.
- [info] lib/data/services/kokoro_onnx_service.dart:122-148 — the listen callback handles messages with `Map` pattern matching and per-message allocation; message rate equals synthesis completions (low) — no action.
- [info] lib/data/services/kokoro_onnx_service.dart:392-478 — `_isolateMain` runs in its own isolate; heavy TTS work is correctly offloaded from the UI isolate — no action.
- [info] lib/data/services/frame_stats_service.dart:16-21 — `_reportEvery` is a const Duration and the window fields are scalars — no action.
- [info] lib/data/services/frame_stats_service.dart:53-60 — the report log line is built with string interpolation once per 30s — negligible — no action.

## Coverage
lib/data/services/frame_stats_service.dart — findings: 4
lib/data/services/kokoro_onnx_service.dart — findings: 9
- [medium] lib/data/services/live_asr_service.dart:216-218 — per-sample Int16→Float32 conversion loop runs on the isolate's event loop for every PCM chunk — at 16 kHz mono a 100 ms chunk is 1600 iterations of `getInt16`+divide per feed, and `feedPcm` is called per mic callback (trigger: every ~100 ms during rehearsal; growing input: chunk size bounded by capture fan-out, work per unit: O(samples)); consequence: decode latency budget on the isolate thread is spent on scalar conversion instead of inference, and the same loop re-runs for the 12800-sample silence pad at line 233-234 (0.8 s of silence = 12800 iterations) on every `endUtterance`; smallest safe fix: convert in bulk via `Int16List.view(msg)` + `Float32List.fromList(...)`/vectorized scale, or precompute the constant silence pad once outside the loop.
- [low] lib/data/services/live_asr_service.dart:200-207 — `decodeAndReport` drains `recognizer.isReady` in a tight while-loop and sends a partial message per decode tick — trigger: every PCM chunk fed; work: O(decode steps) per chunk; consequence: isolate event-loop starvation if the model reports many ready steps per chunk, delaying `start`/`end`/`dispose` command processing; smallest safe fix: yield between decode batches (`await Future.delayed(Duration.zero)` or process commands between ready-batches).
- [low] lib/data/services/live_asr_service.dart:131 — `feedPcm` sends the raw `Uint8List` per mic callback through the isolate port — trigger: every capture callback (~10/s); work: O(chunk bytes) copy per send; consequence: per-message serialization cost on the main isolate for audio-sized buffers; smallest safe fix: batch chunks (e.g. accumulate and send every N ms) or use `TransferableTypedData` to avoid the copy.
- [info] lib/data/services/live_asr_service.dart:111-112 — 30 s timeout on recognizer ready is a fixed bound, not unbounded work — no finding beyond noting the bound exists; consequence: none (bounded); smallest safe fix: none needed.
- [medium] lib/data/services/media_control_service.dart:36,56,69 — three `invokeMethod` round-trips per rehearsal lifecycle (activate/deactivate/updateNowPlaying) with no batching — trigger: once per rehearsal start/end plus per Now Playing update; work: one platform-channel round-trip each; consequence: negligible at current call rate (≤3 per rehearsal), only janks if `updateNowPlaying` is called per line; smallest safe fix: none needed unless callers fire per-line (assumption: caller not visible — if `updateNowPlaying` is called per script line, debounce/batch it).

## Coverage
lib/data/services/live_asr_service.dart — findings: 3
lib/data/services/media_control_service.dart — findings: 0
- [medium] lib/data/services/model_download_service.dart:339-372 — `refreshDownloadedStatus` runs `fileProblem` (which does `existsSync` + `lengthSync` on every model file) plus `_filePath` (a platform-channel round trip to `getApplicationDocumentsDirectory`) per model, sequentially in a loop, on every status refresh — with 6 models that's 6 sequential async channel calls and 12 sync file stats on the UI isolate each time Settings or a refresh triggers it; smallest safe fix is to resolve the documents directory once before the loop (it is already cached in `_docsPath` via `_filePath`, but the `File(...existsSync/lengthSync)` calls remain sync — hoist them into a single `Future.wait` or compute sizes with `File.length()` async) — trigger: every refresh/status poll; growing input: `availableModels` (currently 6, bounded by the const list); work per unit: 1 channel round trip + 2 sync file syscalls; affected resource: UI isolate latency during Settings render.
- [medium] lib/data/services/model_download_service.dart:455-459 — `downloadLiveAsr` builds `needed` by calling `fileProblem(m, File(await _filePath(m)))` per model inside a loop — each iteration awaits a platform-channel documents-directory round trip (via `_filePath`) and does sync `existsSync`/`lengthSync` on the UI isolate; with the 4 live_asr models that's 4 sequential channel calls + 8 sync stats per download attempt; smallest safe fix: resolve `_docsPath` once before the loop and reuse it (or batch the existence checks with `Future.wait`).
- [medium] lib/data/services/model_download_service.dart:465-480 — `_groupReady` repeats the same per-model `_filePath` await + sync `fileProblem` scan on every readiness check, and `isKokoroReady`/`isLiveAsrReady`/`getLiveAsrModelDir` each invoke it — so a single "is ready" gate costs up to 6 sequential channel round trips and 12 sync file syscalls on the UI isolate, and callers (`_tryLoadKokoroIfReady`, `downloadAll` via `isAllReady`) re-run it repeatedly during a download session; smallest safe fix: cache the resolved documents path (already done in `_filePath`) and compute the per-model file stats asynchronously/once per call batch.
- [low] lib/data/services/model_download_service.dart:216,231-235 — `_notify()` iterates `_listeners` and invokes each callback synchronously on the UI isolate; every progress tick (throttled to ~1 MB in `_dartDownload` but unthrottled per native `onDownloadProgress` callback at 239-250) rebuilds every registered listener widget — with N listeners and a 164 MB download that is hundreds of full rebuilds per file; smallest safe fix: throttle native progress notifications the same way the Dart path does (~1 MB deltas) or coalesce with a scheduleMicrotask/frame debounce.
- [low] lib/data/services/model_download_service.dart:107-214 — `availableModels` is a `static const List` so that is fine, but `downloadAll` (694-700) awaits `download(m)` sequentially for all 6 models; each download is a full network transfer, so total wall time is the sum instead of the max — for a fresh install that is ~164 MB + ~14 MB + ~70 MB + ~0.6 MB + ~0.3 MB + ~0.006 MB serialized; smallest safe fix: `Future.wait(availableModels.where(...).map(download))` like `downloadLiveAsr` already does, bounded to the 6-entry const list.
- [low] lib/data/services/model_download_service.dart:596-616 — the redirect-following loop in `_dartDownload` drains each redirect response with `await res.drain<void>()` and re-parses/resolves the URI per hop; bounded by `redirectsLeft = 5` so no unbounded growth, but each hop allocates a fresh `HttpClientResponse` stream subscription and header map on the UI isolate; acceptable at this bound — no fix needed beyond noting the bound is already 5.
- [info] lib/data/services/model_download_service.dart:406 — `crypto.sha256.bind(file.openRead()).first` streams the file, so no whole-body buffering; the digest is computed on the read stream (isolate-managed by package:crypto) — no finding beyond confirming the streaming shape.
- [low] lib/data/services/model_manager.dart:55-62 — `isKokoroReady` performs 5 sequential `await File(...).exists()` calls on the UI isolate each time it is consulted; `isAllReady` (129-137) calls it plus `isLiveAsrReady` (which itself runs `_groupReady` over 4 models with its own channel round trips), and `downloadAll`/`downloadKokoro` re-invoke these gates repeatedly during a download session — each gate is O(models) sync file syscalls per call; smallest safe fix: batch the existence checks with `Future.wait` and cache the models-dir path (already cached in `modelsDir`) so repeated gates cost one stat batch instead of five sequential awaits.
- [low] lib/data/services/model_manager.dart:88-101 — `_reclaimLegacyKokoro` awaits `modelsDir` then checks/deletes each legacy dir sequentially; bounded to 1 legacy dir (`_legacyKokoroDirs` has one entry) so no scale concern — no fix needed.
- [low] lib/data/services/model_manager.dart:242-268 — `_extractArchiveStreaming` runs in `compute` (separate isolate) and streams bzip2→tar→entries, so the heavy work is off the UI isolate; the temp tar file is deleted in `finally` — no unbounded buffering; no finding beyond confirming the offload.
- [low] lib/data/services/model_manager.dart:346-355 — progress callback is throttled to ~1 MB deltas (`lastNotified` guard) so per-chunk `setState` storms are already bounded; no finding.
- [info] lib/data/services/model_manager.dart:206 — archive sha256 is streamed via `crypto.sha256.bind(archiveFile.openRead())` — no whole-body buffering; no finding.

## Coverage
lib/data/services/model_download_service.dart — findings: 7
lib/data/services/model_manager.dart — findings: 5
# dart-performance-review — batch-10 (final amended report)

- [medium] lib/data/services/ocr_confidence_service.dart:117-148 — `_buildWhitelist` re-tokenizes every line of the script on every `scoreScript` call, and `scoreScript` clears `_wordValidCache` (line 255) before each pass, so the memoized spell-checker lookups (line 185-198) never survive across imports — trigger: each script import/OCR review pass; growing input: `lines` (all parsed script lines, unbounded by script length) × words per line; work per unit: regex tokenize + map insert + full dictionary `checkBuilder` lookup per distinct word; affected resource: main isolate CPU during import; consequence: a 900-line script pays a full tokenize+frequency-count pass plus a cold spell-checker cache on every import, and the cache-clearing also means a word whitelisted by a previous import is re-scored from scratch (the doc comment at 251-254 acknowledges the monotonic-growth hazard but the fix chosen re-does all work); smallest safe fix: build the whitelist incrementally (merge new lines' word counts into a persistent `wordCounts` map) and keep `_wordValidCache` keyed per-script (e.g. clear only entries whose validity depended on the previous whitelist) instead of wiping it wholesale.
- [medium] lib/data/services/ocr_confidence_service.dart:257-273 — `scoreScript` allocates a fresh `ScriptLine` via `copyWith` for every non-empty, non-header line even when nothing changed — trigger: every import pass over N lines; growing input: N = all script lines, unbounded; work per unit: one object allocation + field copies per line; affected resource: allocation churn on the import hot path; consequence: for a clean script where `dictNew`/`recConf` yield the same status as before, N short-lived objects are created and immediately discarded, adding GC pressure proportional to script size on the UI isolate; smallest safe fix: compare the computed `display`/`status` against the line's existing `ocrConfidence`/`reviewStatus` and return the original `line` when unchanged (the `copyWith` at script_models.dart:87-119 already supports per-field overrides).
- [low] lib/data/services/ocr_confidence_service.dart:139-143 — `_buildWhitelist` tokenizes via `_tokenize` (regex split + three `where` passes + `replaceAll` per token) and then again inside `scoreLine` (line 204) for the same text, so every line's text is regex-processed twice per import — trigger: each import; growing input: total script text; work per unit: two full regex tokenize passes per line; affected resource: CPU on the import path; consequence: doubles the tokenizer cost for a 900-line script; smallest safe fix: tokenize once in `scoreScript`, pass the token list to both the frequency counter and `scoreLine`.
- [low] lib/data/services/ocr_confidence_service.dart:169-181 — `stripDiacritics` is called per word inside the memoized `_isValidWord` (line 189) but also walks the string char-by-char building a `StringBuffer` even for the common no-diacritic case when a diacritic IS found late in the word; the `sb ??= StringBuffer(s.substring(0, i))` allocates a substring copy of the prefix on first hit — trigger: per distinct word containing any diacritic; growing input: word length (small, bounded); work per unit: one substring allocation + char loop; affected resource: minor allocation churn; consequence: negligible at script scale since `_wordValidCache` memoizes; smallest safe fix: none needed at current scale — noted only because the cache-clearing at line 255 makes this run cold every import (see first finding); no standalone fix required.
- [info] lib/data/services/ocr_confidence_service.dart:77-91 — `ensureVocabLoaded` loads the ~2,936-word theatrical vocab via `rootBundle.loadString` once per service lifetime and `dispose` (line 104-111) resets `_vocabLoadAttempted`, so if `dispose` is called between imports (the doc at 13-14 says "loaded on demand and disposed after use"), the vocab asset is re-read from the asset bundle on every import cycle — trigger: each import following a dispose; growing input: fixed ~2,936-line asset; work per unit: one async asset read + set build; affected resource: asset I/O + set allocation per import; consequence: small but repeated asset I/O on the import path; smallest safe fix: keep the loaded vocab set cached across `dispose` cycles (dispose only the spell checker, not the vocab set and `_vocabLoadAttempted` flag).
- [low] lib/data/services/ocr_highlight_matcher.dart:100-112 — `locate` normalizes each candidate line's text twice per line (`normalize(lines[i].text)` at 101 and `normalize(stripCue(lines[i].text))` at 103), and `_score` (line 199-228) then re-splits `normLine` into a token set (line 215) — trigger: every `locate` call (page-viewer highlight lookup); growing input: `lines` = all OCR lines on the current page (bounded by page size, ~40-50 lines); work per unit: two regex normalize passes + one cue-strip + one token-set build per line; affected resource: CPU on the UI isolate per highlight request; consequence: bounded by page line count so not severe, but the double normalize is pure waste; smallest safe fix: compute `normBody` once, reuse it for both the body-vs-full comparison and `_score`.
- [low] lib/data/services/ocr_highlight_matcher.dart:135-138 — the `consumed` set is built with a spread of `normalize(lines[j].text).split(' ')` per line in the startIdx..bestIdx range, allocating a fresh `List<String>` per line before set-union — trigger: each successful multi-line match; growing input: matched line span (≤ bestIdx-startIdx+1, bounded by page); work per unit: one normalize + split + list alloc per matched line; affected resource: minor allocation churn; consequence: small lists per matched line, bounded by the ≤4-rect extension window; smallest safe fix: reuse a single `Set<String>` and `addAll` the split tokens directly.
- [info] lib/data/services/ocr_highlight_matcher.dart:141-149 — the forward extension loop is bounded by `rects.length < 4` and `remaining.length >= 2`, so the highlight never grows past 4 rects regardless of page size — no unbounded-work finding here; the loop terminates on both bounds and on token exhaustion.
- [low] lib/data/services/ocr_highlight_matcher.dart:172-191 — `bestMatch` normalizes and cue-strips every candidate string on every call with no memoization — trigger: per target line per import pass; growing input: `candidates` = all raw OCR lines of the page/document; work per unit: two regex normalize passes + token-set build per candidate; affected resource: CPU on the import isolate; consequence: O(targets × candidates × normalize-cost) across an import — for a 900-line script that is ~810K normalize passes if every line is matched against every candidate; smallest safe fix: memoize `normalize(stripCue(candidate))` per candidate string (candidates repeat across targets) in a `Map<String, (String, Set<String>)>` passed in or held per import run. (Assumption: callers invoke `bestMatch` per line over the full document — the caller file `lib/data/services/ocr_script_import_service.dart` was requested and is NOT AVAILABLE in this repository, so the per-document vs per-page bound cannot be confirmed; if callers only match within one page, this drops to info.)
- [low] lib/data/services/ocr_highlight_matcher.dart:44-56 — `_containsWord` uses a `while(true)` + `indexOf` scan that can re-scan the haystack from `from = i + 1` after each near-miss; for a needle absent from the haystack this is one full scan (fine), but for a needle present many times with no word-boundary-valid hit it re-scans from each hit — trigger: per candidate per locate/bestMatch call; growing input: haystack = normalized line body (bounded by line length); work per unit: O(hits × needle-length) indexOf scans; affected resource: CPU; consequence: negligible at line-length scale; smallest safe fix: none required at current scale.

## Coverage
lib/data/services/ocr_confidence_service.dart — findings: 5
lib/data/services/ocr_highlight_matcher.dart — findings: 5
- [medium] lib/features/cast_manager/bulk_cast_setup_screen.dart:82-86 — `unassigned` is recomputed on every `build` with an O(characters × castMembers) `any` scan and a fresh `.toList()` allocation, and the whole screen rebuilds whenever `castMembersProvider` or `currentScriptProvider` emits — consequence: with a large script (hundreds of characters) each keystroke-driven rebuild re-walks every character against every cast member and reallocates the list, janking the UI thread on every save/refresh; smallest safe fix: memoize `unassigned` (e.g. compute once per script/castMembers pair via a `select`-style derived provider or cache keyed on script+castMembers identity) instead of rebuilding it in `build`.
- [medium] lib/features/cast_manager/bulk_cast_setup_screen.dart:61-62,159 — `_filledCount` walks all `_nameControllers` values and trims each text on every `ValueListenableBuilder` rebuild of the summary bar, and `_filledTick.value++` fires per keystroke (line 229) — consequence: with many filled-in characters each keystroke re-scans every controller's text (O(n) per keystroke, O(n²) across a typing session) and rebuilds the summary row; smallest safe fix: maintain a running filled counter updated on add/remove/controller-text-change instead of re-scanning all controllers per keystroke.
- [medium] lib/features/cast_manager/bulk_cast_setup_screen.dart:272-311 — `_saveCastAssignments` awaits one Supabase `createCastInvitation` round-trip per filled character sequentially inside a loop — consequence: saving N filled actors costs N sequential network round-trips (N×RTT latency, e.g. 10 actors ≈ 10×~200ms ≈ 2s of spinner time on `_saving`), and each iteration also allocates a fresh `const Uuid().v4()`; smallest safe fix: batch the inserts into one Supabase upsert/rpc call (or `Future.wait` over the per-character requests) so the save is one round-trip instead of N.
- [low] lib/features/cast_manager/bulk_cast_setup_screen.dart:367-373 — `_showInviteLinksSheet` builds the `actors` list by re-walking all `_nameControllers` entries with per-entry trim/isEmpty checks each time the sheet is shown — consequence: trivial at current scale (dozens of characters) but redundant recomputation of data already derivable from `_filledCount`; smallest safe fix: reuse the filled-entries list maintained for `_filledCount` instead of re-scanning controllers.
- [low] lib/features/cast_manager/bulk_cast_setup_screen.dart:30,31,47-59 — `_nameControllers`/`_contactControllers` grow per character via `putIfAbsent` and are only disposed in `dispose()` — consequence: controllers for characters that leave the unassigned list (e.g. after a save assigns them) stay allocated for the screen's lifetime, an unbounded map keyed by character name across repeated script loads; smallest safe fix: prune controllers for characters no longer in `unassigned` when the list is recomputed (or key controllers to the current `unassigned` set).
- [info] lib/features/auth/auth_screen.dart:421 — `sharedPreferencesProvider` write on the skip-auth path is a synchronous-ish plugin call on the UI thread during a navigation; verify the backing store is async (shared_preferences) — consequence: negligible at this scale; smallest safe fix: none needed unless the provider wraps synchronous storage.
## Coverage
lib/features/auth/auth_screen.dart — findings: 1
lib/features/cast_manager/bulk_cast_setup_screen.dart — findings: 5
- [medium] lib/features/cast_manager/cast_manager_screen.dart:103-128 — `_syncCastFromCloud` awaits `notifier.save` per cloud member inside a loop — N sequential round-trips per sync (one per cast member) on the UI isolate; at ~20 members this serializes ~20 awaits before the screen settles, and each `save` likely triggers a provider rebuild — smallest safe fix: batch the members into one notifier call (e.g. `saveAll`) or `Future.wait` with a bounded batch, and let the provider diff once.
- [medium] lib/features/cast_manager/cast_manager_screen.dart:139-147 — per-local `notifier.remove` awaited in a loop — same N sequential awaits/rebuilds class as the save loop; fix: collect ids and issue one bulk removal.
- [low] lib/features/cast_manager/cast_manager_screen.dart:1121-1150 — `_shareCastList` runs two `members.where(...).toList()` scans per character (primary + understudy) — O(chars × members) allocation churn on the share tap; fix: pre-index members by characterName once before the loop (as `_syncCastFromCloud` already does with `cloudKeys`).
- [low] lib/features/cast_manager/cast_manager_screen.dart:393-395 — per-character `recordings.containsKey` inside a `where` over that character's lines, re-evaluated every rebuild of the whole screen (build watches script, production, castMembers, recordings) — O(chars × lines) map lookups per rebuild; fix: hoist a `recordedIds` set once per build and reuse it in the card builder.
- [info] lib/features/cast_manager/cast_manager_screen.dart:193,215 — `_memoAssignment`/`_memoLinesByChar` cache keys use `identityHashCode(script)`/`script.lines` identity, so a provider that rebuilds `ParsedScript` with equal contents but a new instance (e.g. `_toggleGender`'s `copyWith` chain at 1196-1207) invalidates both caches and re-runs the full script walk + voice coloring on the next build — consequence is bounded (one walk per toggle) but worth noting since the memo exists precisely to absorb rebuild churn; fix: key on a cheap content hash or the script's save-generation counter if one exists.
- [low] lib/features/cast_manager/cast_manager_screen.dart:1044-1052 — `_shareInviteCard` rasterizes the off-screen invite card at `pixelRatio: 3.0` and encodes full PNG bytes on the UI isolate (`toImage`/`toByteData` are async but the raster+encode work is main-thread-adjacent in Flutter's pipeline) — a 400×~600 card at 3× is ~1200×1800 px; consequence is a visible hitch on the share tap on low-end devices; fix: drop to `pixelRatio: 1.0`–2.0 or move capture to `Image`-based share without PNG re-encode if the sheet accepts raw bytes.

## Coverage
lib/features/cast_manager/cast_manager_screen.dart — findings: 6
- [medium] lib/features/cast_manager/voice_config_screen.dart:125-139 — `_memoAssignment` caches by a hash of `script.lines` + preset id + gender-override string, but the cache key is recomputed via `Object.hash(identityHashCode(script.lines), ...)` on every build and the memoized result is only invalidated by that hash — if two different scripts hash-collide or the same script object is mutated, the stale assignment map is reused for every character tile — trigger: every rebuild of the character list (each setState, preset tap, dialect toggle) walks `script.characters` and calls `_memoAssignment` per tile; work per call: O(lines) hash walk + O(characters) map build on miss; affected resource: UI thread during rebuilds; consequence: on large scripts (hundreds of lines) each cache miss re-runs the full adjacency walk on the main thread, janking preset taps and dialect toggles; fix: key the cache on the script object identity (`Object` identity or a version counter on the provider) instead of a hash of the lines list, and clear it in `didChangeDependencies`/provider-listener rather than re-deriving per build.
- [medium] lib/features/cast_manager/voice_config_screen.dart:108-111 — `...script.characters.map((char) => _buildCharacterTile(...))` passes `_memoAssignment(script)` as an argument, so the memoized map is evaluated once per character tile inside the spread — trigger: every rebuild with N characters; work per unit: one cache-key hash + map lookup per tile (O(1) if cached, O(lines) on miss); affected resource: UI thread; consequence: N redundant cache-key computations per rebuild instead of one hoisted call, noticeable on scripts with many characters; fix: hoist `final assignment = _memoAssignment(script);` above the `.map` and pass the single result into each tile.
- [low] lib/features/cast_manager/voice_config_screen.dart:335-358 — `VoicePresets.voiceLabels.entries.map(...)` inside the bottom-sheet `ListView(children: ...)` builds the full voice list eagerly (`.toList()` at 358) rather than lazily — trigger: every open of the per-character voice dialog; work per unit: O(voiceLabels) widget construction up front; affected resource: UI thread on dialog open; consequence: dialog open cost grows with the voice-label map size instead of deferring to scroll; fix: use `ListView.builder` with `itemCount: VoicePresets.voiceLabels.length` and index into `entries` lazily.
- [low] lib/features/home/home_screen.dart:242-266 — `ListView.builder` itemBuilder calls `ref.watch(savedCharacterProvider(production.id))` per item, and each provider family instance reads `SharedPreferences.getInstance()` (line 27) — trigger: every rebuild of the production list with N productions; work per unit: one SharedPreferences load per distinct production id per watch (SharedPreferences caches the instance, but the FutureProvider re-runs its body per family key on first watch and on refetch); affected resource: UI thread + prefs I/O; consequence: N provider-family activations on list rebuild; fix: hoist the saved-character lookup into a single map provider keyed by production id, or read prefs once in the screen state and pass values down.
- [low] lib/features/home/home_screen.dart:221,247 — `ref.watch(savedCharacterProvider(production.id))` inside `itemBuilder` subscribes each grid/list item to its own provider family instance; when the productions list is long this creates N active provider subscriptions that all rebuild the item on any prefs change — trigger: N productions × rebuilds; consequence: extra rebuild fan-out on prefs writes; fix: same hoist as above, or `ref.watch` outside the builder via a combined selector.
- [info] lib/features/home/home_screen.dart:586-590 — `_deleteProduction` awaits `SharedPreferences.getInstance()` and two `prefs.remove` calls on the UI thread inside the delete flow; SharedPreferences loads the whole prefs file on first access — trigger: per delete action (user-initiated, rare); consequence: one-time prefs-file load latency on the delete tap; fix: none needed at this scale (single user action, not a hot path) — noted only because the same `getInstance` pattern is repeated per production item at lines 242-266 where it does scale.

## Coverage
lib/features/cast_manager/voice_config_screen.dart — findings: 3
lib/features/home/home_screen.dart — findings: 3
- [medium] lib/features/join/join_production_screen.dart:325-356 — `_buildCharacterOptions()` is called from `build` (line 241) on every setState/rebuild and re-runs the full `_castMembers!.where(...)` filter plus a fresh `List<Widget>` allocation each pass, with no memoization keyed on `_castMembers`/`_prefilledCharacter` — trigger: every keystroke in the name TextField (line 225) and every `_loading`/`_error` setState during lookup/join; growing input: cast member list (unbounded — no cap on `_castMembers` size from `fetchCastMembers`); work per unit: O(n) filter + O(n) widget construction per rebuild; affected resource: UI thread during the join flow; consequence: with a large cast (hundreds of members) each keystroke rebuilds hundreds of RadioListTiles, janking typing; fix: memoize the widget list (cache the built list and invalidate only when `_castMembers` or `_prefilledCharacter` changes) or build the options with a `ValueListenableBuilder`-scoped subtree that rebuilds only on selection change.
- [medium] lib/features/join/join_production_screen.dart:468-470 — `_joinProduction` re-filters `_castMembers!.where(...)` inside the join handler to find the unclaimed invitation, duplicating the O(n) scan already done in `_buildCharacterOptions` — trigger: once per join tap; growing input: cast member list (unbounded); work per unit: O(n) linear scan; affected resource: UI thread; consequence: negligible at small casts but redundant recomputation that grows with cast size; fix: reuse the already-filtered `unclaimedInvitations` list (or a single pass that captures the matching invitation) instead of a second `.where().toList()`.
- [low] lib/features/join/join_production_screen.dart:407-414 — `_lookupCode` scans the full `cast` list linearly to auto-select the prefilled character — trigger: once per successful lookup; growing input: cast list (unbounded); work per unit: O(n) scan with per-element `toUpperCase()` string allocations; affected resource: UI thread; consequence: minor at realistic cast sizes; fix: break out of the scan as soon as a match is found (already done) and skip the scan entirely when `_prefilledCharacter == null` (already guarded) — acceptable as-is; only worth restructuring if casts grow large.
- [low] lib/features/onboarding/model_setup_screen.dart:164-171 — `_downloadAll` polls `_voices.ready`/`_voices.error` in a 500ms `Future.delayed` loop with a 15-minute deadline while the native download listener (`_onServiceState`) is the actual completion signal — trigger: once per download-all tap, up to 1800 iterations; growing input: bounded by the 15-minute deadline; work per unit: one `setState`-free wake-up per 500ms; affected resource: UI isolate timer queue; consequence: a busy-wait that keeps the isolate waking for up to 15 minutes even when the listener would have fired immediately, and if the listener never fires (e.g. listener removed on dispose at line 83) the loop burns the full deadline before timing out; fix: replace the polling loop with a `Completer` completed by `_onServiceState` (with the same timeout), so the wait is event-driven instead of polled.
- [low] lib/features/onboarding/model_setup_screen.dart:186-198 — `track()` (registered as a service listener at line 201) recomputes `availableModels.where(...).toList()` and the full size/progress summation on every service state change during the matching download — trigger: every progress tick from `ModelDownloadService` (potentially many per second during a ~68 MB download); growing input: `availableModels` list (bounded by the shipped model set, but re-scanned per tick); work per unit: O(n) filter + O(n) summation + one `setState` per tick; affected resource: UI thread during an active download; consequence: repeated list scans and setState calls on every progress event can stutter the UI on low-end Android devices; fix: cache the filtered `files` list outside `track` (it is static per install) and throttle progress-driven `setState` (e.g. only update when the computed ratio changes by ≥1%).
- [low] lib/features/onboarding/model_setup_screen.dart:104-116 — `_onServiceState` re-filters `availableModels.where((m) => m.subdir == 'kokoro_mlx').toList()` and re-sums all file sizes/progress on every listener callback — trigger: every native download state change (frequent during a ~180 MB download); growing input: `availableModels` (bounded but re-scanned per event); work per unit: O(n) filter + O(n) summation; affected resource: UI thread; consequence: redundant per-event recomputation during the voices download; fix: hoist the filtered `files` list into a field computed once in `initState` and only recompute the running totals per event.
- [info] lib/features/onboarding/model_setup_screen.dart:130-131 — `SharedPreferences.getInstance().then((p) => p.setBool(...))` fires-and-forgets the consent write with no error handling — trigger: once per download-all tap; consequence: if the future rejects (storage failure) the unhandled rejection is silent and the consent flag may never persist, so the auto-downloader's consent gate (assumed to read this pref in code not shown) would resolve permissive on next launch; fix: `await` the write (or `.catchError`) and log failure via `_dlog`. (Direction-of-failure note: the fallback here is "no consent recorded" — verify the reader's default is false, not true; the reader is not visible in this batch.)
- [info] lib/features/join/join_production_screen.dart:566-569 — `voicePreset` is read from `_foundProduction!['voice_preset']` and persisted via `VoiceConfigService.instance.setPreset` with no null/error guard beyond the `!= null` check — trigger: once per join; consequence: if `setPreset` throws the whole join is caught by the outer catch (line 580) and reported as a generic join failure even though the join itself succeeded; fix: wrap the preset write in its own try/catch so a voice-preset failure cannot mask a successful join. (Performance-neutral; flagged only because the failure direction turns a success into a reported failure.)

## Coverage
lib/features/join/join_production_screen.dart — findings: 4
lib/features/onboarding/model_setup_screen.dart — findings: 5
- [medium] lib/features/production_hub/production_hub_screen.dart:440-580 — ListView.builder itemBuilder rebuilds the full per-scene card subtree (border Container, Wrap of character chips, counts) on every scroll frame for every visible row, and each rebuild re-runs `scene.characters.map(...).toList()` (line 534-562) plus `Theme.of(context)` lookups inside the chip map — work per unit is O(sceneChars) allocations per visible row per rebuild — trigger is every scroll/keyboard/setState frame over `scenes.length` rows — consequence is allocation churn and jank on long cast lists (e.g. 40 scenes × 12 characters) — smallest safe fix: hoist the character-chip `map().toList()` result and per-scene static decorations into cached widgets (e.g. memoize per scene id keyed on `myCharacter`/`_filterAct`), and use `const` constructors for the fixed SizedBox/Icon/Text children.
- [medium] lib/features/production_hub/production_hub_screen.dart:391-410 — `_memoSceneCounts` recomputes the O(totalLines) per-scene dialogue/line scan whenever the `(lines, scenes, myCharacter)` identity key changes, and the cache is a single-entry field so any unrelated rebuild that passes a fresh-but-equal key (e.g. after `_filterAct` setState or a provider state change that recreates the tuple) re-walks every line of every scene — trigger is each hub rebuild with a changed key — growing input is `script.lines` × `script.scenes` with no bound — consequence is repeated O(n) scans on the UI thread during scroll/filter churn — smallest safe fix: key the memo on stable identities (`script` object + `myCharacter` string) and keep a small LRU (e.g. 2-4 entries) instead of one slot, or compute counts once per script version in a provider.
- [low] lib/features/production_hub/production_hub_screen.dart:166 — `debugPrint` of a string built with `script?.lines.length` and provider reads runs on every `build` call; `debugPrint` is throttled in release but the string interpolation itself is per-build work on the UI thread — trigger is every rebuild of the hub screen (scroll, filter, mode toggles) — consequence is minor per-frame allocation/CPU in debug and profile builds — smallest safe fix: guard the log behind `kDebugMode` and a `const` message or move it behind an `assert`.
- [low] lib/features/production_hub/production_hub_screen.dart:889-895 — `_export` performs `getApplicationDocumentsDirectory()` + `Directory.existsSync/createSync` + `File.writeAsString` on the UI isolate each time the export drawer item is tapped; `writeAsString` is async but the `existsSync`/`createSync` pair is synchronous disk I/O on the main thread — trigger is per export tap (user-initiated, low frequency) — consequence is a short main-thread stall on slow disks — smallest safe fix: replace `existsSync`/`createSync` with `exportDir.create(recursive: true)` (idempotent async) or wrap in `Isolate.run`/`to_thread`-equivalent.
- [low] lib/features/onboarding/welcome_screen.dart:24-33 — `maybeOffer` awaits `SharedPreferences.getInstance()` (async plugin round-trip) before deciding to show the walkthrough; this is called at first-run startup so it adds one async plugin hop to the critical first-frame path — trigger is once per install — consequence is a few ms of startup latency, not a hot path — smallest safe fix: none needed at this scale; optionally pre-warm SharedPreferences at app init so the value is already cached when `maybeOffer` runs.
- [info] lib/features/production_hub/production_hub_screen.dart:105-119 — `_checkModels` schedules a 500 ms delayed `_showModelPrompt` via `Future.delayed` whenever models are not ready and the platform is not macOS; each hub entry re-runs `_checkModels` (initState and after `/ai-models` push), so repeated navigation can stack multiple delayed dialogs if `_modelsReady` stays false — trigger is each hub mount with un-downloaded models — consequence is possible duplicate dialog scheduling (UI churn, not CPU) — smallest safe fix: guard the delayed prompt with a `_promptScheduled` flag or cancel the future in `dispose`.

## Coverage
lib/features/onboarding/welcome_screen.dart — findings: 1
lib/features/production_hub/production_hub_screen.dart — findings: 5
- [medium] lib/features/recording_studio/recording_studio_screen.dart:241-244 — `_myLines` memo keyed on script identity only, not on character — when the user switches characters without the script object changing, `linesForCharacter` (a full O(lines) scan with `isForCharacter` per line, script_models.dart:360-365) reruns on every rebuild — at 10 Hz during recording (timer at line 631) that is a full script walk per tick — smallest safe fix: include `_character` in the memo key (`if (_character != character || _myLinesScript != script)` already checks it, but the assignment at 243 runs only when the key differs; the defect is that `_character = character` at 245 is set unconditionally after the memo check, so a same-script/different-character rebuild skips the rescan and serves stale lines — actually the check at 241 does compare `_character != character`, so the rescan fires; the real cost is that the scan itself is O(lines) per rebuild while recording, which is bounded by script size and only re-runs on character/script change — downgrade to info) — [info] lib/features/recording_studio/recording_studio_screen.dart:241-244 — `linesForCharacter` full-script scan reruns whenever character or script identity changes; during recording the 10 Hz timer does not re-trigger it (memo holds), so cost is one O(lines) scan per character switch — acceptable; no fix needed beyond what the memo already does.
- [medium] lib/features/recording_studio/recording_studio_screen.dart:256-259 — `recordedCount` computed per build via `_myLines.where(...).length` allocating a fresh filtered list each rebuild; build() ticks at 10 Hz while recording (timer at 631) because `_recordingDuration` is a ValueNotifier (only the label listens, but `setState` at 637/712/744/731 and `_goToLine`'s setState at 762 still rebuild the whole screen) — each rebuild re-runs the O(_myLines) `where().length` scan plus the O(lines) `indexWhere` at 266 (`script.characters.indexWhere((c) => c.name == character)`) — with a large script (thousands of lines) and a 10 Hz tick this is repeated linear work on the UI thread per tick — smallest safe fix: compute `recordedCount` with a counting loop over the already-memoized `_myLines` (no list allocation) and cache `charIdx`/`charColor` alongside the `_myLines` memo keyed on (character, script).
- [medium] lib/features/recording_studio/recording_studio_screen.dart:358-363 — `_fullIdxById` map rebuilt whenever `_fullIdxScript != script`, but `_buildContextLines` runs on every build (10 Hz while recording via setState-driven rebuilds); the map build itself is O(lines) and only fires on script identity change, so it is memoized correctly — however the loop at 368-373 walks backwards from `fullIdx` through the whole script prefix (`i >= 0` with no early bound beyond `contextLines.length < 2`) — worst case O(fullIdx) per build when the current line is late in the script and preceding lines are stage directions — smallest safe fix: bound the backward walk to a fixed window (e.g. scan at most 4 lines back) since only 2 context lines are shown.
- [low] lib/features/recording_studio/recording_studio_screen.dart:590-594 — `getApplicationDocumentsDirectory()` + `existsSync`/`createSync` directory probe on every `_startRecording` — sync file I/O (`existsSync`, `createSync`) on the UI isolate per recording start; at one call per user-initiated take this is negligible, but `*Sync` file I/O on a serving path is the flagged class — smallest safe fix: hoist the directory creation to initState or a one-time static init.
- [low] lib/features/recording_studio/recording_studio_screen.dart:631-635 — `Timer.periodic` at 100 ms mutates `_recordingDuration.value` for the whole recording duration; each tick fires a ValueListenableBuilder rebuild of only the label (line 452-458), which is correct — but the timer callback also runs `mounted` check and notifier mutation on the UI isolate at 10 Hz for potentially minutes-long takes; this is the documented design (comment at 49-52) and the label listener is scoped — no unbounded work; no finding beyond info — [info] lib/features/recording_studio/recording_studio_screen.dart:631-635 — 10 Hz timer tick is scoped to a single ValueNotifier consumed by one small builder; consequence is negligible; no fix.
- [low] lib/features/recording_studio/recording_studio_screen.dart:715-740 — `_playRecording` calls `AudioLevelService.instance.volumeFor(path)` (async) and `PlaybackSession.ensurePlayback()` per play press; one network/IO round-trip per user action is per-item I/O only at user frequency — no finding.
- [low] lib/features/recording_studio/recording_character_screen.dart:58-70 — `linesByChar` map built per `_CharacterList.build` with an O(lines) walk plus per-character list appends; build runs on every parent rebuild of the screen — the screen rebuilds when `recordingsProvider` or `currentScriptProvider` changes, not at 10 Hz (this screen has no timer), so cost is one O(lines+characters) pass per data change — acceptable; no finding.
- [low] lib/features/recording_studio/recording_character_screen.dart:91-96 — per-item `recordedCount` computed inside `itemBuilder` via `charLines.where((l) => recordings.containsKey(l.id)).length` — allocates a filtered list per visible row and re-runs on every list rebuild; with C characters × L lines this is O(C×L) per full list build (the comment at 46-49 documents the old O(script) scan per row was fixed by hoisting `linesByChar`, but the per-row `where().length` still allocates a list per row) — smallest safe fix: precompute a `recordedIds` Set once per build and count with a loop, or precompute per-character recorded counts in the same hoisted pass as `linesByChar`.
- [low] lib/features/recording_studio/recording_character_screen.dart:92 — `recordings.containsKey(l.id)` inside a `where` per row: `recordings` is a `Map<String, Recording>` so containsKey is O(1); the cost is the per-row list allocation from `where(...).length` (no `isNotEmpty`/counting shortcut) — same class as the previous finding; consequence is allocation churn per visible row on every scroll-triggered rebuild of a row — smallest safe fix: `charLines.where(...).length` → count via `recordedIds` set intersection computed once per build.

## Coverage
lib/features/recording_studio/recording_character_screen.dart — findings: 2
lib/features/recording_studio/recording_studio_screen.dart — findings: 4
- [medium] lib/features/recording_studio/recordings_browser_screen.dart:601-625 — `_resolveRecordingPath` performs up to three sequential async filesystem probes (`File.exists` on the stored path, `File.exists` on the Documents/recordings candidate, plus `_cachedRecordingPaths` directory walk) per recording, and `_scanFileExistence` (lines 195-218) awaits it serially inside a `for` loop over every entry — N recordings × 3+ I/O round-trips each, all on the UI isolate's async queue during first paint of the browser screen; with hundreds of takes this is hundreds of sequential disk hops before the list settles — batch the probes with `Future.wait` over a precomputed candidate list (or resolve lazily per visible tile) instead of a serial loop.
- [medium] lib/features/recording_studio/recordings_browser_screen.dart:631-642 — `_cachedRecordingPaths` walks the entire `recording_cache` directory tree recursively (`dir.list(recursive: true).toList()`) and caches the result in a field that is never invalidated for the screen's lifetime; every new unresolved recording re-runs the full recursive listing (the `_recordingCachePaths` cache is only reused when the *same* screen instance already scanned, but the scan itself re-lists the whole tree whenever the cache is null after a fresh screen or after `_fileResolved` clears), and the result grows unbounded with cache size — cap the walk (e.g. `take`/limit) or resolve cache paths by filename lookup instead of a full recursive enumeration per resolution.
- [low] lib/features/recording_studio/recordings_browser_screen.dart:196-198 — `_scanFileExistence` builds the dedup key by `entries.map(...).join('|')` on every call, allocating an O(n) string per memoized rebuild just to compare against `_scannedKey`; with hundreds of takes this is a fresh multi-KB string per rebuild — compare list identity/length + first/last ids, or hash incrementally, instead of materializing the joined key each time.
- [low] lib/features/recording_studio/recordings_browser_screen.dart:74-83 — `_memoEntries` rebuilds `linesById` (a fresh map over all script lines) and re-sorts `entries` on every cache miss, and the miss happens whenever the script/recordings/filter tuple changes identity even when the resulting list is unchanged; with hundreds of lines × hundreds of recordings this is O(n·m) map construction plus an O(n log n) sort per rebuild — key the memo on the tuple's identity hash or reuse `linesById` when `script.lines` is unchanged.
- [low] lib/features/recording_studio/recordings_browser_screen.dart:108-120 — `recordedCharacters` set construction and the `totalDurationMs` fold re-walk all recordings on every build (not memoized with `_memoEntries`), so each setState (e.g. play/stop toggle at line 51) re-scans the full recordings map — hoist these into the memoized tuple or compute once per recordings-identity change.
- [low] lib/features/rehearsal/rehearsal_history_screen.dart:25-30 — `add` copies the whole state list (`[session, ...state.take(_maxSessions - 1)]`) on every session insert; with `_maxSessions = 100` this is a 100-element list copy per rehearsal-session add, and every `add` also re-creates the state object causing a full provider rebuild of the history screen — acceptable at the stated cap, but if the cap is raised or sessions are added per-line rather than per-session the copy cost grows linearly — use an insert-at-front on a fixed-length structure or document the cap as the bound.
- [info] lib/features/recording_studio/recordings_browser_screen.dart:670 — `File(resolvedPath).lengthSync()` is synchronous file I/O on the UI isolate inside `_playRecording`'s async flow; a large take on slow storage stalls the frame during the play tap — replace with `File.length()` (async) or `FileStat` via `File.stat()`.

## Coverage
lib/features/recording_studio/recordings_browser_screen.dart — findings: 6
lib/features/rehearsal/rehearsal_history_screen.dart — findings: 1
- [medium] lib/features/rehearsal/rehearsal_screen.dart:1055-1060 — per-build O(characters) map construction inside `_buildScriptView` — the map is rebuilt on every rebuild of the script view (each line advance / state flip that reaches this widget), and with ~145 realized rows the work is repeated per frame — hoist the map to a field keyed by script identity, or compute once per scene change — trigger: every rebuild of `_buildScriptView` (per line advance, per STT state flip); growing input: script.characters (unbounded, cast size); work per unit: O(characters) map inserts; affected resource: UI isolate CPU during rebuild; consequence: frame jank on large casts during rapid advances; fix: cache the map alongside `_rehearsalLinesCache` keyed by script identity.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:1092-1247 — per-row `Consumer` closures allocate a fresh builder closure per realized row on every rebuild of the ListView — with cacheExtent 3000 forcing ~30-50 rows built per side plus offscreen realization, each rebuild allocates ~dozens of closures and re-runs the per-row `ref.watch` selects — acceptable at this scale only because selects are cheap; no fix needed beyond noting allocation churn is bounded by cacheExtent — trigger: every rebuild; growing input: realized row count (bounded by cacheExtent 3000); work per unit: one closure + several provider reads per row; affected resource: allocation churn on UI isolate; consequence: minor GC pressure during rapid advances; fix: none required (bounded).
- [medium] lib/features/rehearsal/rehearsal_screen.dart:1688-1698 — `_getRehearsalLines` filtered-list construction walks all dialogue lines with `isForCharacter` per line on every cache miss — cache key includes mode/character/scene so misses are rare, but the miss path is O(dialogueLines) with per-line `multiCharacters.contains` — trigger: cache miss per scene/character/mode change; growing input: dialogueLines (unbounded, script size); work per unit: O(n) scan with per-line list contains; affected resource: UI isolate CPU on scene switch; consequence: one-time hitch on scene change for very long scripts; fix: none required (memoized correctly).
- [medium] lib/features/rehearsal/rehearsal_screen.dart:2428-2431, 2507-2510 — `TtsService.stripStageDirections(line.text)` + `split(RegExp(r'\s+'))` re-run on every confirm-timer tick and every silence-timer re-arm — `_confirmDespiteNoise` re-arms every 800 ms and `_confirmIfActorQuiet` every 150 ms while the actor is mid-line, so the regex split + stage-direction strip runs dozens of times per line — trigger: every timer tick while listening (150-800 ms cadence); growing input: line.text (unbounded, monologue-length); work per unit: regex split + strip = O(text length); affected resource: UI isolate CPU during active listening; consequence: repeated regex work on the UI thread during the hottest listening window; fix: compute wordCount/strip once per line at listen start and cache on the line or in a field.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:2260-2265, 2259 — per-listen regex split + Set conversion of the line's words (`cleanLine.split(...).toSet().toList()`) runs on every `_startListeningForMyLine` — one per actor line, so per-line cost is amortized, but the split/strip/regex work is duplicated again inside `_confirmDespiteNoise`/`_confirmIfActorQuiet` per tick (see prior finding) — trigger: once per actor line; growing input: line.text; work per unit: O(text) regex work; affected resource: UI isolate; consequence: negligible alone, compounds with the per-tick duplication; fix: share the computed hints/wordCount across the line's lifetime.
- [low] lib/features/rehearsal/rehearsal_screen.dart:3197-3233 — `_saveRehearsalCaptures` copies each captured file with `await tempFile.copy(destPath)` sequentially inside a loop — per-item I/O round-trip per capture; with N captured lines this is N sequential copy+delete awaits — trigger: once per scene completion (user-initiated save); growing input: _capturedAudio entries (bounded by lines the actor recorded this session, realistically small); work per unit: one file copy + delete per entry; affected resource: UI isolate awaiting disk I/O; consequence: save dialog latency grows linearly with capture count; fix: batch with `Future.wait` over entries or move to an isolate/background copy.
- [low] lib/features/rehearsal/rehearsal_screen.dart:3252-3255 — `_purgeRehearsalCaptures` uses `File(captured.path).deleteSync()` inside a loop — synchronous file I/O on the UI isolate per capture — trigger: user taps Discard; growing input: _capturedAudio entries; work per unit: one sync delete per entry; affected resource: UI isolate blocked per delete; consequence: brief UI freeze proportional to capture count; fix: use `delete()` async or batch.
- [low] lib/features/rehearsal/rehearsal_screen.dart:3055-3056 — `stale.existsSync()` + `deleteSync()` on the capture path runs synchronously on the UI isolate at every capture start — trigger: every actor line capture start (per line on Android capture path); growing input: none (single file); work per unit: one existsSync + deleteSync; affected resource: UI isolate; consequence: sub-millisecond typically, but sync I/O on a hot per-line path; fix: use async `exists()`/`delete()`.
- [low] lib/features/rehearsal/rehearsal_screen.dart:3107-3108 — `File(path).existsSync()` and `lengthSync()` in `_stopCaptureForLine` run synchronously on the UI isolate at every capture stop — trigger: every capture stop (per actor line); growing input: none; work per unit: two sync file stats; affected resource: UI isolate; consequence: minor per-line sync I/O; fix: async exists/length.
- [info] lib/features/rehearsal/rehearsal_screen.dart:114 — `_ttsPrefetch` map is pruned on use (`.remove` at 2084) and cleared on dispose/scene change (600, 698), so it is bounded — no finding.
- [info] lib/features/rehearsal/rehearsal_screen.dart:181 — `_lineAttempts` grows one entry per actor line attempt; bounded by scene length per session and cleared on restart (2725) — no finding.
- [info] lib/features/rehearsal/rehearsal_screen.dart:94 — `_capturedAudio` grows one entry per captured line, cleared on save/purge — bounded by session capture count — no finding.

## Coverage
lib/features/rehearsal/rehearsal_screen.dart — findings: 9
- [medium] lib/features/script_editor/character_manager_screen.dart:60-63 — `singleLineChars` computed via `script.characters.where(...).toList()` on every `build` (and `hasIssues` only needs `isNotEmpty`) — every rebuild of this screen (setState from `_loadLocales`, `_setCharacterLocale`, gender toggles) re-allocates the filtered list and re-walks all characters just to render a banner; with hundreds of characters and frequent locale/gender updates this is repeated O(n) allocation churn on the UI thread — compute `hasIssues` with `script.characters.any((c) => c.lineCount == 1)` and only materialize the list when the banner is actually shown (or cache it keyed to the script instance).
- [medium] lib/features/script_editor/character_manager_screen.dart:253-256 — `_toggleGender` maps over the entire `script.characters` list and rebuilds a full `ParsedScript` (copying `lines`, `scenes`, `rawText` references into a new state object) on every toggle — each tap on the gender IconButton triggers O(n) list copy plus a whole-provider state replacement, forcing every dependent widget to rebuild; at scale (large casts, rapid toggling) this is per-tap allocation churn and oversized rebuilds — mutate only the affected character (copyWith on the single entry) and reuse the existing `lines`/`scenes`/`rawText` lists in the new `ParsedScript` (they are already reused here, so the main cost is the full characters map; a single-element update avoids the O(n) map).
- [medium] lib/features/script_editor/character_manager_screen.dart:735-739 — `_rebuildScript` recomputes `charCounts` by walking all `updatedLines` and then sorts `charCounts.entries` with a per-comparison `compareTo` on every rename/merge/delete — each character mutation (rename, merge, delete) triggers a full O(lines) count pass plus O(k log k) sort and fresh `ScriptCharacter` allocations for every character, even when only one name changed; for long scripts with many characters this is repeated O(n) work per keystroke-level edit on the UI thread — derive counts incrementally (adjust only the affected character's count) or memoize per script version.
- [medium] lib/features/script_editor/character_manager_screen.dart:769-782 — `_rebuildScript` remaps and re-copies every scene (`remappedScenes.map(...)` producing `copyWith` per scene, then `sceneChars` set/list per scene) on every rename/merge/delete — each edit rebuilds all scene objects and their character sets even though only scenes overlapping the edited range changed; with many scenes this is O(scenes × range) allocation churn per edit on the UI thread — only rebuild scenes whose `[startLineIndex, endLineIndex]` intersects the changed range and reuse the rest.
- [medium] lib/features/script_editor/character_manager_screen.dart:647-669 — `_migrateCastMembers` performs per-member awaits inside a loop: `supa.renameCastCharacter` (network round-trip) and `notifier.save` (local Drift write) sequentially for each affected cast member — a rename affecting many cast rows (e.g. a character name shared across several cast entries) issues one network call + one DB write per member serially, blocking the UI-thread-adjacent async work for n×(RTT+write) — batch the cloud renames with `Future.wait` (bounded) and batch the local saves into one transaction.
- [low] lib/features/script_editor/character_manager_screen.dart:641-645 — `_migrateCastMembers` calls `notifier.loadForProduction(productionId)` (full cast reload from local DB/cloud) on every rename even when the provider already holds this production's cast — each rename pays a redundant full-list load (I/O + parse) before filtering; if the provider caches per production this is wasted work — check whether the notifier already has data for this production id before reloading (or expose a `loadIfMissing`).
- [low] lib/features/script_editor/character_manager_screen.dart:456-457 — `_showCharacterDetail` calls `script.linesForCharacter(char.name)` and `script.scenesForCharacter(char.name)` on every tap, each doing a full O(lines)/O(scenes) scan with `where().toList()` — per-tap O(n) scans allocate fresh lists; for large scripts and repeated taps this is minor but repeated allocation churn — acceptable as-is for a dialog, but could be memoized per (script, character) if the sheet is opened repeatedly.
- [low] lib/features/script_editor/cloud_sync_dialog.dart:22-61 — `diffScriptLines` builds `localById` (O(local)) then walks cloud (O(cloud)) and local again (O(local)) with three list/map allocations per invocation — the diff is recomputed in full every time `showCloudSyncDialog` is called (each sync attempt), allocating `diffs`, `matchedLocalIds`, and a `LineDiff` per line; for scripts with thousands of lines this is O(n) per dialog open on the UI thread before the dialog even shows — acceptable single-shot cost, but if the dialog is re-opened per sync attempt on large scripts consider computing the diff once per sync cycle or streaming counts.
- [low] lib/features/script_editor/cloud_sync_dialog.dart:74-88 — `showCloudSyncDialog` walks all diffs twice (count switch + `changedDiffs` collection) and allocates `changedDiffs` even when the diff is all-unchanged — minor per-open allocation churn; fold the counting and collection into one pass and skip `changedDiffs` allocation when `diffs` is empty.
- [info] lib/features/script_editor/character_manager_screen.dart:110-112 — `ListView.builder` with `itemCount: script.characters.length` is correctly lazy (itemBuilder only builds visible items), so no unbounded widget build issue; noted only because the checklist asks about oversized rebuilds — no action needed.
- [info] lib/features/script_editor/cloud_sync_dialog.dart:143-149 — `ListView.builder` over `changedDiffs` is lazy; the `_buildDiffTile` per-item work is bounded by visible items — no action needed.

## Coverage
lib/features/script_editor/character_manager_screen.dart — findings: 7
lib/features/script_editor/cloud_sync_dialog.dart — findings: 3
- [high] lib/features/script_editor/scene_editor_screen.dart:68-183 — ListView.builder itemBuilder calls script.linesInScene(scene) (context script_models.dart:368-375, which does lines.sublist(start,end)) and a .where().length scan over that sublist on EVERY item build, and the hoisted charIndexByName map (lines 43-46) is rebuilt on every setState/rebuild of the whole screen — work per item is O(lines) sublist allocation + O(scene lines) filter, trigger is every scroll frame/rebuild of the list, growing input is script.lines (unbounded, one per script line) × scenes count — consequence: allocation churn and O(scenes×lines) work on the main isolate per rebuild, jank on long scripts during scroll — smallest safe fix: compute dialogueCount per scene once (memoize on scene id / precompute in the notifier when scenes change) and build charIndexByName once per script instance (memoize like the acts Expando) instead of per build.
- [medium] lib/features/script_editor/scene_editor_screen.dart:122-134 — per-item closure allocates a Chip widget per character inside itemBuilder's children map with a fresh .toList() each rebuild; combined with the O(scenes×chips×characters) indexWhere-style map lookup noted above this is allocation churn per frame — consequence: GC pressure and rebuild cost grows with character count per scene — smallest safe fix: hoist the chip list construction into a memoized per-scene widget or const-construct where possible.
- [medium] lib/features/script_editor/scene_editor_screen.dart:242-245 — _splitScene calls script.linesInScene(scene) then .where().toList() to build dialogueLines on every dialog open; fine at dialog-open frequency, but the same linesInScene sublist is recomputed again inside _performSplit (lines 419-425) after the dialog closes — redundant recomputation of the same O(lines) slice — consequence: duplicated O(lines) work per split action — smallest safe fix: pass dialogueLines/splitAt into _performSplit instead of re-deriving bounds.
- [low] lib/features/script_editor/scene_editor_screen.dart:374,381,408,487 — List<...>.from(...) copies the full scenes/lines lists on every rename/split/merge mutation; each mutation is user-triggered (low frequency) and list sizes are bounded by script size — consequence: minor allocation churn per edit — smallest safe fix: none needed at this scale; if scripts grow, prefer copyWith-style structural sharing.
- [info] lib/features/script_editor/scene_editor_screen.dart:399,481,515 — scheduleScriptSave(ref) is called per mutation; if it serializes the whole ParsedScript (rawText + all lines) synchronously per save, cost grows with script size — assumption: scheduleScriptSave implementation not visible in this batch; verify before claiming — smallest safe fix: debounce/serialize off the UI isolate if it is sync.

## Coverage
lib/features/script_editor/scene_editor_screen.dart — findings: 5
- [medium] lib/features/script_editor/script_editor_screen.dart:118-119,549-606 — `_memoFilteredLines`/`_filteredLines` re-run a full O(n) scan and materialize a fresh `List<ScriptLine>` on every `build` whenever any `setState` fires (filter chips, reorder toggle, line selection at :384, detail-panel save at :535) — the memo key includes only the line-list identity and four flags, but `_selectedLine` changes are not in the key, so every tap that only changes selection still rebuilds the whole filtered list; with a full-length script (hundreds of lines) each keystroke/tap in the edit sheet or each chip toggle re-allocates the list and re-scans all lines on the UI thread — smallest safe fix: exclude selection-only state from the memo key (or cache the filtered list keyed on `script.lines` identity + filter flags only, and let selection be applied at item level), so selection changes reuse the cached list.
- [medium] lib/features/script_editor/script_editor_screen.dart:567-575 — `_memoLowOcrCount` re-scans all `script.lines` on every build that reaches it (every `setState` on this screen) even when the memo hits, because the memo key is only `script.lines` identity — but the count is recomputed whenever the list identity changes, which happens after every edit (`_rebuildScript` builds a new `ParsedScript`), so each edit/save/reorder triggers a fresh O(n) scan plus a new list allocation on the UI thread; consequence: with a 500-line script, every line edit, split, delete, or reorder pays two full scans (`_memoFilteredLines` + `_memoLowOcrCount`) plus `remapScenes` (O(n·scenes)) before the frame can paint — smallest safe fix: compute the low-OCR count once per `ParsedScript` instance (e.g. memoize on the `ParsedScript` object like `acts` does via Expando) instead of per-build.
- [medium] lib/features/script_editor/script_editor_screen.dart:1291-1331,1333-1384 — `_rebuildScript` and `_updateLine` each rebuild the full character list from scratch on every single-line edit: O(n) scan for `charCounts`, O(k log k) sort of characters, plus `ParsedScript.remapScenes` (O(n·scenes) with a per-scene inner loop over the scene's line range) — and `_updateLine` duplicates the entire gender-preservation + charCounts + remapScenes block that `_rebuildScript` already implements, so every keystroke-driven save path pays the same O(n + n·scenes) work twice; consequence: editing one line of a 500-line, 20-scene script costs ~2×(500 + 20×scene-length) allocations and scans on the UI thread per save, and the duplicated block in `_updateLine` (:1346-1368) is a second copy of the same O(n) work — smallest safe fix: route `_updateLine`'s rebuild through `_rebuildScript` (single shared implementation) so the character/scene recomputation runs once per edit.
- [medium] lib/features/script_editor/script_editor_screen.dart:619-620 — `_reorderLines` calls `allLines.indexWhere((l) => l.id == movedLine.id)` and `indexWhere(...targetLine.id)` — two O(n) linear searches per reorder gesture — plus `script.lines.toList()` (:614) and a full O(n) `copyWith` reindex loop (:632-634) on every drag; consequence: each reorder of a 500-line script allocates a full copy of the lines list, runs two linear scans, and re-copies every line with a new `orderIndex` on the UI thread per drag event (ReorderableListView fires `onReorder` per move) — smallest safe fix: build a `Map<String,int>` id→index once per reorder (or per script version) and reuse it for both lookups; the O(n) reindex is inherent to reordering but the two scans and the extra `toList()` copy can be avoided.
- [low] lib/features/script_editor/script_editor_screen.dart:452,830 — `File(pdfPath).existsSync()` is called inside `build`/`_buildDetailPanel` and again inside `_editLine`'s sheet builder — synchronous filesystem stat on the UI thread per rebuild/per sheet open; consequence: on a network-backed or slow volume (or when the path is stale) each rebuild of the detail panel or each edit-sheet open blocks the UI thread for a stat call, and `_buildDetailPanel` re-runs it on every rebuild of the selected-line panel — smallest safe fix: resolve existence once in `_resolvePdfPath` (async) and store the result, or use `File.existsSync` only in initState-time async setup, not per build.
- [low] lib/features/script_editor/script_editor_screen.dart:94 — `_effectivePdfPath` calls `File(stored).existsSync()` on every invocation, and it is invoked from both `build`-adjacent paths (`_buildDetailPanel` :451) and every edit-sheet open (:827) — same sync-stat-on-UI-thread class as above; consequence: repeated synchronous stats per rebuild/sheet open on the main thread — smallest safe fix: cache the resolved path/existence in state after `_resolvePdfPath` and stop re-statting per call.
- [low] lib/features/script_editor/script_editor_screen.dart:1230-1238 — `_applySplit` allocates a fresh `updatedLines` list and copies every line (`l.copyWith` only for the split line, but the loop still walks all n lines and rebuilds the whole `ParsedScript` + `remapScenes`) per split — inherent to the data model, but it runs on the UI thread per user action; consequence: O(n + n·scenes) per split on a large script — smallest safe fix: none needed beyond the shared `_rebuildScript` consolidation already noted; acceptable as-is at realistic script sizes.
- [info] lib/features/script_editor/script_editor_screen.dart:1434-1440 — `_export` writes the export file with `await File(filePath).writeAsString(content)` after building the full export string in memory (`ScriptExporter.toMarkdown/toPlainText` produce the entire script text synchronously) — for a very large script this allocates the whole document as one string on the UI thread before the async write; consequence: a multi-MB export momentarily doubles memory and blocks the UI thread during string assembly — smallest safe fix: stream the export via `File.openWrite`/`writeAsString` with chunked appends if scripts can reach MB scale; at typical script sizes this is acceptable.
- [info] lib/features/script_editor/script_editor_screen.dart:110-113 — `charColors` map is rebuilt on every `build` (every `setState`) by iterating all characters; with a large cast (100+ characters) this is an O(c) map allocation per rebuild — consequence: minor per-rebuild allocation churn on the UI thread — smallest safe fix: memoize keyed on `script.characters` identity.
- [info] lib/features/script_editor/script_editor_screen.dart:1247-1253 — `_lowOcrLines()` re-scans all lines and allocates a new list each call; it is called from the edit-sheet builder (:938) and again after each Remove/Looks-right action (:979,:1000) — O(n) per call on the UI thread, but bounded by script size and only on user actions — consequence: negligible at realistic scale — smallest safe fix: reuse the memoized count/list from `_memoLowOcrCount` if a list form is needed.

## Coverage
lib/features/script_editor/script_editor_screen.dart — findings: 10
- [medium] lib/features/script_import/ocr_review_screen.dart:118-126 — `_orderedCurrentLines`/`_orderedIndexById` are memoized getters, but `_markRemoved` (line 112-116) nulls both caches on every removal, and `_orderedIndexById` is rebuilt O(n) over the whole ordered list each time any code touches it after a removal — consequence: with hundreds of flagged lines and a bulk "Remove these" action (line 449-455 calls `_markRemoved` per line inside one setState loop), each removal invalidates the cache and the next read of `_orderedCurrentLines` re-filters and re-sorts the entire `widget.lines` list (O(n log n)) plus rebuilds the id→index map (O(n)); for a bulk remove of k lines that is O(k·n log n) on the UI thread inside setState — smallest safe fix: in `_removeAllNotScript` mark all removals first and null the caches once after the loop (or make `_markRemoved` only set a dirty flag and let the getters recompute once per frame).
- [medium] lib/features/script_import/ocr_review_screen.dart:241-250 — `_pendingWithPages()` is a non-memoized getter that allocates a fresh list and runs the full predicate scan over `_reviewLines` on every call, and it is called repeatedly per frame: once in `_viewSourcePage` (line 263), again inside the sheet builder on every `setSheetState` (line 282), and again in `_buildSourcePane` (line 644) on every build of the two-pane body — consequence: on a heavily-flagged scan (hundreds of review lines) each rebuild/tap re-runs the O(n) filter plus list allocation several times, adding per-tap jank on the UI thread — smallest safe fix: memoize the pending list keyed on `_removedIds`/`_byId` version (invalidate in `_markRemoved`/`_saveEdit`) instead of recomputing per call.
- [medium] lib/features/script_import/ocr_review_screen.dart:474 — `build` re-filters `_reviewLines` with `.where(...).toList()` on every setState (save, remove, select, context toggle all call setState) — consequence: O(n) allocation churn per interaction on large scans; combined with `_pendingWithPages()` in `_buildSourcePane` (line 644) and `_contextLinesFor` (line 128-144) each card build, a single tap triggers several full-list scans — smallest safe fix: derive the visible review list from the memoized `_orderedCurrentLines`/pending cache rather than re-scanning per build.
- [low] lib/features/script_import/ocr_review_screen.dart:77-80 — `initState` builds two O(n) maps (`_byId`, `_origById`) with map-literal spread over `widget.lines`; fine once, but `_origById` duplicates every line object reference purely for highlight matching — consequence: extra memory for large scripts (thousands of lines) at screen open — smallest safe fix: build `_origById` lazily on first highlight lookup, or store only ids needing originals.
- [low] lib/features/script_import/ocr_review_screen.dart:923-969 — `_buildContextEditor` maps the context window into widgets with per-row `TextField` + `IconButton` allocations each time the context editor is expanded/rebuilt; `_contextControllers` grows via `putIfAbsent` (line 146-148) and is never pruned on removal — consequence: controller map grows unboundedly across a long review session with many expanded context editors (one TextEditingController per touched line id, retained until dispose) — smallest safe fix: dispose/remove the controller for a line when its context editor collapses or the line is removed.
- [info] lib/features/script_editor/validation_panel.dart:23-130 — `validateScript` runs ~8 full passes over `script.lines`/`script.scenes` (separate `.where`/`.length`/`.any` scans) each time the panel is shown; called once per `showValidationPanel` invocation (user-triggered), so cost is O(8n) once per open, not per frame — consequence: negligible at realistic script sizes (hundreds–low thousands of lines); only worth folding into a single pass if profiling shows it — smallest safe fix: none needed; optionally compute dialogue/unattributed/low-confidence counts in one loop.

## Coverage
lib/features/script_editor/validation_panel.dart — findings: 1
lib/features/script_import/ocr_review_screen.dart — findings: 5
- [info] test/cast_member_test.dart:146-171 — test-only loop allocates 500 join codes and splits each into per-char strings purely to assert a fixed alphabet — no production impact; consequence is only test wall-time (500 allocations + 500×6 string splits per run) — smallest safe fix: reduce iteration counts or assert a single generated code's charset; test-only code, no production reachability.

## Coverage
test/analytics_route_observer_test.dart — clean
test/cast_member_test.dart — findings: 1
- [info] test/character_gender_persistence_test.dart:33-34,44,61,74 — `firstWhere` over `script.characters` inside tests is O(n) per lookup, but with 1–2 characters per fixture the work is bounded and test-only — no production hot path is exhibited by the inlined code — none needed (no defect at observed scale).

## Coverage
test/cast_role_test.dart — clean
test/character_gender_persistence_test.dart — clean
## Coverage
test/cloud_sync_dialog_test.dart — clean
test/demo_production_service_test.dart — clean
- [info] test/demo_script_test.dart:16-21 — setUpAll performs sync file I/O (`existsSync`, `readAsStringSync`) plus a full script import on the test process main thread — consequence: test-suite startup latency grows with demo asset size; realistic scale is a small demo file, so impact is bounded — smallest safe fix: none needed (test-only, asset is small); no production serving path affected.
## Coverage
test/demo_script_test.dart — clean
test/dialog_navigation_test.dart — clean
- [info] test/espeak_heteronyms_test.dart:59 — `Process.runSync('which', ['espeak-ng'])` runs synchronous process spawn on the test runner's main thread at group-evaluation time — consequence: blocks the test event loop for the process-launch latency (tens of ms) on every run, and on machines without espeak-ng the `which` probe still spawns a process before the skip guard takes effect; smallest safe fix: use `Process.run` with `await` (or `canRun` from `package:process`) so the probe is async and non-blocking.
- [info] test/espeak_heteronyms_test.dart:63-67 — `ipa()` shells out to `espeak-ng` synchronously (`Process.runSync`) inside loops that call it per line (lines 83, 86, 98, 102) — consequence: each of the ~10 invocations pays full process spawn + TTS synthesis latency serially on the main thread, multiplying test wall time by the spawn cost per line; smallest safe fix: switch to `Process.run`/`await` and, if scale matters, batch lines into one espeak-ng invocation or parallelize with `Future.wait`.
- [info] test/home_screen_logic_test.dart:7-24 — `populatedScript` is rebuilt as a fresh const object per test case rather than shared — consequence: negligible at 4 cases; no measurable impact at this scale; smallest safe fix: none needed (const objects are canonicalized; not a real cost).

## Coverage
test/espeak_heteronyms_test.dart — findings: 2
test/home_screen_logic_test.dart — findings: 1
- [info] test/model_download_gzip_test.dart:65-68 — byte-buffer accumulation via `bytes.addAll(chunk)` inside a stream loop grows an unbounded `List<int>` for the whole response body — consequence: for large model payloads (tokens.txt fixtures scale to MBs in real downloads) each chunk append re-sizes/copy-grows the growable list, O(n) extra allocations and GC churn on the test event loop; smallest safe fix: collect chunks into a `BytesBuilder` (or `body.toBytes()`/`fold`) instead of a growable `List<int>` — trigger: every download test invocation; growing input: response body length, currently bounded only by the local fixture payload (~6.5 KB here, unbounded in principle); work per unit: amortized O(1) per chunk but with repeated array reallocation; affected resource: test isolate heap/event loop.
- [info] test/model_download_gzip_test.dart:33,90,102 — `gzip.encode(payload)` recomputed at three call sites over the same immutable payload — consequence: redundant CPU compression of the same bytes (652-line fixture) repeated per test instead of once; smallest safe fix: compute the gzipped bytes once (top-level `final`) and reuse — trigger: each of the three tests; growing input: payload length (bounded by fixture, ~6.5 KB); work per unit: full gzip encode per call; affected resource: test CPU only, low impact.
- [info] test/model_download_gzip_test.dart:17-18 — `List.generate(652, ...).join('\n')` materializes 652 intermediate strings then a joined whole — consequence: allocation churn proportional to fixture size, negligible at 652 lines; smallest safe fix: none needed at this scale (info-level only) — trigger: test setup once; growing input: 652 lines, bounded by fixture constant; work per unit: O(n) string building; affected resource: transient allocations in test setup.

## Coverage
test/model_download_gzip_test.dart — findings: 3
test/model_manager_test.dart — clean
- [info] test/ocr_cleanup_test.dart:30,116,163,179,197,215 — `firstWhere` without `orElse` inside tests — a mismatched expectation throws a test-failure exception rather than a performance defect; consequence is limited to test diagnostics — smallest safe fix: none needed (test-only, no production impact).
## Coverage
test/models_test.dart — clean
test/ocr_cleanup_test.dart — clean
- [info] test/ocr_confidence_test.dart:8 — test-only binding initialization and vocab injection run per test case; setUp re-creates the theatrical vocab map on every test in the file — consequence: negligible in CI (a handful of tests, tiny maps); no production path affected — smallest safe fix: none needed; noted only because setUp/tearDown churn exists, not a real perf defect
- [info] test/ocr_confidence_test.dart:35,42,47,51,55,60,66,71,77,81,88,94,103,114,122,131,140,143,149,152,155,158,160,177 — every assertion exercises the tokenizer/scorer on single-line inputs; no unbounded loop, no allocation churn beyond per-test constants — consequence: none at this scale — smallest safe fix: n/a

## Coverage
test/ocr_confidence_mapping_test.dart — clean
test/ocr_confidence_test.dart — clean
- [low] test/ocr_highlight_hitrate_test.dart:54-58 — on a matcher miss the test re-runs `OcrHighlightMatcher.locate` against every page's line list in a nested loop (per-line × per-page × per-line-in-page), an O(lines × pages × linesPerPage) fallback scan — at corpus scale (fixture `pp_ocr_raw.txt` grows with the production script) the miss path dominates the test's wall time and blocks the test event loop — smallest safe fix: build a normalized-text → page index once before the loop and look up misses there instead of scanning all pages per miss.
- [info] test/ocr_highlight_hitrate_test.dart:14 — `readAsStringSync` on the fixture is sync file I/O, but it is test-only setup executed once per test run, not a serving path — no production consequence; acceptable as-is.
- [low] test/ocr_highlight_hitrate_test.dart:18,21 — the `linePageMap` and `conf` maps are built with per-iteration closure allocation over the full corpus; for a large fixture this is allocation churn proportional to corpus size, though it runs once per test — smallest safe fix: hoist the closures or build with a plain `for` loop into pre-sized maps.

## Coverage
test/ocr_highlight_hitrate_test.dart — findings: 2
test/ocr_highlight_matcher_test.dart — clean
- [info] test/ocr_page_mapping_distribution_test.dart:12 — `readAsStringSync` on the test main thread — a large fixture file blocks the test event loop during the synchronous read — acceptable in a test-only harness; if the fixture grows, switch to `readAsString` in the async test body.
- [info] test/ocr_page_mapping_distribution_test.dart:16-18 — map literals built with incremental per-element computation over `rawLines.length` — two O(n) map constructions per test run; fine at fixture scale, only worth revisiting if the corpus grows to thousands of lines.
- [info] test/ocr_review_sheet_test.dart:133-135,151-153 — 30 fixed 50ms pumps per close path — deliberate rebuild-storm detector, not a defect; the loop is bounded and test-only.

## Coverage
test/ocr_page_mapping_distribution_test.dart — findings: 2
test/ocr_review_sheet_test.dart — findings: 1
- [low] test/parser_accuracy_test.dart:295-296 — the "Generate parser accuracy report" test writes `sample-scripts/PARSER_ACCURACY_REPORT.md` via `writeAsStringSync` and prints the whole report synchronously; the report is regenerated on every extended-tag run and the write mutates the working tree (comment at 291-292 admits a prior timestamp made runs dirty, the write itself still dirties it) — consequence: each extended run performs sync file I/O + full-report string building on the test event loop and leaves an uncommitted artifact that can make CI dirty; smallest safe fix: write to a temp directory (or `Directory.systemTemp`) and skip the write when not explicitly requested via an env/flag. - [low] test/parser_accuracy_test.dart:263-288 — the report loop re-parses every fixture a second time (each play was already parsed once in the accuracy tests above) with `readAsStringSync` per file and builds the report with `StringBuffer` — consequence: doubles parse work for ~20 full-length play texts (hundreds of KB each) on the test event loop; smallest safe fix: cache the `ParsedScript` per filename in a map filled by the earlier tests, or generate the report in a separate tagged test that parses once. - [info] test/parser_accuracy_test.dart:31 — `file.readAsStringSync()` inside `parseFile` is sync I/O on the test event loop; acceptable for a test-only tagged suite, listed only for completeness. - [info] test/parser_edge_cases_test.dart:149-203 — `SttService.matchScore` is called with tiny strings in tests; no production hot path is exercised here, so no performance defect is exhibited by this file. ## Coverage test/parser_accuracy_test.dart — findings: 2 test/parser_edge_cases_test.dart — findings: 0 ## Follow-up requests - test/parser_accuracy_test.dart NEEDS lib/data/services/script_parser.dart — confirm whether parse() is O(n^2) on large scripts, which would upgrade the double-parse finding's impact.
- [info] test/pdf_export_test.dart:59-64 — test-only character-name assertions use `contains`/`length` on a `Set` built from `script.characters` — if the parser ever emits duplicate character entries the `toSet()` silently dedupes and `length == 4` still passes, masking a regression — consequence: a parser bug producing duplicate `Character` objects would not fail this test, hiding a real defect that would later surface as duplicated dialogue attribution in production imports — smallest safe fix: assert against the production `Character` list length (`script.characters.length`) alongside the set, or use `unorderedEquals` on the full name list rather than a deduped set.

- [info] test/pdf_export_test.dart:314-316 — `firstWhere` without `orElse` on `script.characters` will throw `StateError` if the parser fails to detect a character (e.g. gender-inference regression drops a speaker) — consequence: the test crashes with an unhandled exception rather than a clear failure message, obscuring the actual regression cause during CI triage — smallest safe fix: add `orElse: () => fail('character not found')` or use `singleWhere`/`where(...).first` with an explicit expect.

- [info] test/pdf_import_test.dart:332-333 — same `firstWhere`-without-`orElse` pattern on `script.characters` for `ELIZABETH`/`DARCY` — consequence: a parser regression that drops either character makes the test throw `StateError` instead of reporting which character went missing, slowing diagnosis — smallest safe fix: add `orElse` with a descriptive `fail()` call.

- [info] test/pdf_import_test.dart:29-30 — `['ELIZABETH', 'DARCY', 'JANE', 'BINGLEY'][page % 4]` allocates a fresh 4-element list on every loop iteration (60 iterations) — consequence: negligible at 60 iterations; only matters if this pattern were copied into production code at scale — smallest safe fix: hoist the list to a `const` outside the loop.

## Coverage
test/pdf_export_test.dart — findings: 3
test/pdf_import_test.dart — findings: 3
## Coverage
test/pp_ocr_attribution_test.dart — clean
test/production_repository_test.dart — clean
## Coverage
test/recording_path_safety_test.dart — clean
test/recording_sync_service_test.dart — clean
- [info] test/running_header_test.dart:108-109,138-139 — List.generate builds filler lists inside tests — negligible: bounded 40/30 items, test-only — none needed
## Coverage
test/rehearsal_models_test.dart — clean
test/running_header_test.dart — findings: 1
- [low] test/sample_script_test.dart:24,103,182,216 — `readAsStringSync` on multi-MB fixture files inside `setUpAll`/test bodies blocks the test event loop for the whole read — consequence: slow, janky test startup at corpus scale; smallest safe fix: none needed (test-only, acceptable) — findings: 0
- [low] test/sample_script_test.dart:36-38,80-82,115-117,139-141,146-149,162-164,269-271,278-280,298,325-327,332-334,339-341,346-348,359-361,365 — repeated `.where().toList()` scans over `script.lines`/`script.characters` inside loops and per-test — consequence: O(n) re-scan per assertion, negligible at fixture scale; smallest safe fix: none needed (test-only) — findings: 0
- [low] test/scene_partition_test.dart:79-83 — `dir.listSync()` re-lists the corpus directory per test run — consequence: trivial I/O at corpus scale; smallest safe fix: none needed (test-only) — findings: 0

## Coverage
test/sample_script_test.dart — clean
test/scene_partition_test.dart — clean
- [info] test/scene_remap_test.dart:34-41,116-125 — test fixtures allocate small `List` literals and `ScriptLine`/`ScriptScene` objects per test case — consequence is negligible (≤6 lines, ≤2 scenes per test, run once per test run, not on any serving path) — no fix needed; listed only to confirm the checklist was applied and rejected for lack of scale threshold.
- [info] test/script_editor_walkthrough_test.dart:43-55,66-73 — widget-test fixtures build 1–3 `ScriptLine` objects and pump a full `MaterialApp` per test — consequence is bounded test-only cost (≤3 lines, two pumps, no production caller) — no fix needed; same rejection rationale.

## Coverage
test/scene_remap_test.dart — clean
test/script_editor_walkthrough_test.dart — clean
- [low] test/shakespeare_import_test.dart:701 — `readAsStringSync` on a multi-MB fixture file blocks the test event loop while parsing runs synchronously afterwards — consequence: large fixture loads stall the test runner's main isolate; smallest safe fix: use `readAsString()` with `await` before `parser.parse` — reachability: operator running tests only (low).
- [info] test/script_parser_import_test.dart:258 — test builds a 100-page buffer via incremental `StringBuffer` writes then parses synchronously; work is bounded (~300 lines) and test-only — consequence: negligible; smallest safe fix: none needed.
## Coverage
test/script_parser_import_test.dart — findings: 1
test/shakespeare_import_test.dart — findings: 1
- [info] test/sharing_test.dart:752-776 — test named "large script diff performance" exercises diffScriptLines with 500 lines but asserts only correctness and explicitly omits any wall-clock assertion (comment at 773-775 says no wall-clock assertion), so the suite cannot detect a perf regression in the diff algorithm — the test cannot fail on the defect class it names; assert against a measured bound (e.g. sw.elapsed < threshold) or drop the misleading name — smallest safe fix: add a generous Stopwatch bound or rename the test.
- [info] test/stt_adaptation_test.dart:136-139 — setUp obtains the singleton SttAdaptationService.instance but never resets it between tests, so tests at 154-168 mutate shared singleton state that later tests (e.g. 141-152) may observe, making the suite order-dependent — smallest safe fix: reset or re-create the service state in setUp.

## Coverage
test/sharing_test.dart — findings: 1
test/stt_adaptation_test.dart — findings: 1
- [info] test/stt_vocabulary_service_test.dart:8-13 — setUp clears singleton state via clearProduction before every test — if the singleton holds per-production maps, each test's clearProduction is O(vocabulary size) and repeated across the suite; consequence is negligible at this scale (a handful of lines per test) — smallest safe fix: none needed; noted only as a pattern, no actionable defect at this scale.
- [info] test/stt_vocabulary_service_test.dart:174 — test calls SttVocabularyService.instance directly instead of the setUp-assigned `service` variable — inconsistent access to the singleton; no performance consequence — smallest safe fix: use `service` for consistency (style, not perf; reported at info only because it is not a perf defect).

## Coverage
test/stt_service_test.dart — clean
test/stt_vocabulary_service_test.dart — findings: 2
- [medium] lib/data/services/paddle_ocr_channel.dart:114-128 — `pagesRaw.map(...).toList()` plus nested `linesRaw.map(...).toList()` rebuilds every `PaddlePage`/`PaddleTextBlock` from raw `Map<String,dynamic>` method-channel payloads on the UI isolate each time a full-PDF OCR run completes; for a large script PDF (hundreds of pages × dozens of lines) this is a synchronous deep-map traversal + allocation churn on the main thread right after the native side already did the heavy OCR work, and the result is held in memory for the whole run — smallest safe fix: have the native side return typed/compact payloads (or decode lazily per page as the viewer scrolls) instead of materializing every page's line list up front.
- [low] lib/data/services/paddle_ocr_channel.dart:22,34-37 — `progress` is a process-wide static `ValueNotifier` that is overwritten per run and reset to null in `finally` (line 138); two overlapping `ocrPdf` calls (e.g. user re-imports while a previous run is still in flight) race on the same notifier, so the UI can show page counts from the wrong run and the first run's `finally` clears the second run's live progress — smallest safe fix: key progress per in-flight run (instance field or run-id-scoped notifier) instead of one static slot.
- [low] lib/data/services/pdf_text_channel.dart:31-47 — `extractTextPerPage` returns the whole PDF's per-page text as one `List<String>` from a single method-channel call with no page-range argument; for a long play PDF this buffers the entire extracted text (plus the platform-channel copy) in memory at once on the UI isolate — smallest safe fix: add an optional page-range/limit argument to the native call and fetch pages on demand.

## Coverage
lib/data/services/paddle_ocr_channel.dart — findings: 2
lib/data/services/pdf_text_channel.dart — findings: 1
- [info] lib/data/services/perf_service.dart:10-11 — `_perf` getter re-evaluates `firebaseAvailable` and constructs `FirebasePerformance.instance` on every `startTrace`/`measure` call — if `firebaseAvailable` is a non-trivial check (assumption: it is not a cached const bool, since it is not visible in this file), each trace start pays that cost again; at scale (per-request tracing across many calls) this is repeated recomputation — smallest safe fix: cache the resolved `FirebasePerformance?` in a field after first evaluation.
- [info] lib/data/services/perf_service.dart:25-32 — `measure` wraps every operation in a trace start/stop even when `_perf` is null (unavailable platform), so the wrapper still allocates a `try/finally` closure and an async future chain per call — negligible per call, but on hot paths (per-item measurement in loops) it adds allocation churn with no benefit when tracing is disabled — smallest safe fix: early-return `operation()` directly when `_perf` is null.
- [info] lib/data/services/playback_session.dart:26-35 — `ensurePlayback` reconfigures and re-activates the audio session on every call (per the doc comment this is intentional to override a stale `.record` category), but `AudioSession.instance` and `configure`/`setActive` are platform-channel round-trips; if callers invoke this per playback item rather than per session, each item pays 3+ platform-channel round-trips — smallest safe fix: memoize the last configured state and skip reconfigure when the category is already `.playback` (or when activation succeeded within the current session), keeping the override behavior for the `.record` case.

## Coverage
lib/data/services/perf_service.dart — findings: 2
lib/data/services/playback_session.dart — findings: 1
- [medium] lib/data/services/recording_sync_service.dart:553-573 — getCachedRecordings stats every cache entry's file with existsSync on each call — during a big sync this runs per downloaded file (the docstring at 527-528 says the full-map version is used per downloaded file), so with N cached recordings and M downloads per sync it is O(N·M) synchronous filesystem stats on the UI isolate; at hundreds of cached recordings this blocks rehearsal playback startup — cache a per-entry existence flag invalidated on write/delete, or stat only entries whose path changed since last check.
- [medium] lib/data/services/recording_sync_service.dart:148-173 — _doHydrate reads and jsonDecodes the whole manifest synchronously on the main isolate at startup — manifest grows with every cached recording ever downloaded (unbounded: one entry per lineId across all productions, never pruned), so a long-lived device pays a growing decode+existsSync-per-entry cost on every launch — decode on an isolate or chunk the manifest per production.
- [low] lib/data/services/recording_sync_service.dart:190-201 — _saveManifest re-encodes the ENTIRE cache (jsonEncode over all entries) on every call — clearCache/clearAllCaches/end-of-sync each pay O(total cached recordings) encode+write even when only one entry changed; with hundreds of entries this is a multi-hundred-entry JSON serialize per sync — acceptable at current scale, but the debounced realtime path (183-186) already shows the cost was recognized; consider incremental append or per-production manifests.
- [low] lib/data/services/recording_sync_service.dart:559-572 — getCachedRecordings allocates a fresh Recording object and DateTime.fromMillisecondsSinceEpoch per matching entry on every call — called per downloaded file during sync (per the 527-528 docstring), so M downloads × N matching entries allocations per sync; consequence is allocation churn on the UI isolate during large syncs — reuse the cached Recording or build lazily on demand.
- [info] lib/data/services/recording_sync_service.dart:161 — _doHydrate calls File(cached.localPath).existsSync() per manifest entry inside the restore loop — same existsSync-per-entry shape as getCachedRecordings but only once per launch; impact bounded by manifest size (unbounded over device lifetime) — batch the existence checks or accept as-is.
- [low] lib/data/services/script_export.dart:243-245 — toCueScript materializes script.lines.where(...).toList() then indexes dialogueLines[i-1] — the toList is fine (single pass), but the loop at 247-263 re-walks the full dialogue list per character export; for a full-cast export of a play with thousands of dialogue lines this is O(characters × lines) — export once per character is user-triggered and rare, so impact is bounded; no fix needed unless bulk export is added.

## Coverage
lib/data/services/recording_sync_service.dart — findings: 5
lib/data/services/script_export.dart — findings: 1
- [medium] lib/data/services/script_import_service.dart:132-146 — per-line `copyWith` closure chain allocates a new ScriptLine per parsed line and re-runs `_findSourcePageFrom` (a linear scan over `rawLines` from a cursor) inside a `.map` — for a 1200-line play this is ~1200 allocations plus up to 150-line scans each, on the UI isolate before the isolate-run boundary — hoist the map body into a plain `for` loop building one pre-sized list, and reuse the existing `Isolate.run` worker for the tagging pass — trigger: every PDF import whose PDFKit path succeeds; growing input: parsed line count (unbounded by page count); work per unit: O(window) scan + one object alloc; affected resource: UI isolate latency during import spinner.
- [medium] lib/data/services/script_import_service.dart:547-548 — `Isolate.run(() => parseAndMapOcr(...))` ships the full `rawText` string plus two maps (`lineConfidences`, `linePageMap`) by copy for every import — for a 60-page scanned play that is megabytes serialized/copied twice (send + return of the whole `ParsedScript` with `rawText` retained) on every import — pass the already-materialized `buffer`/maps via `TransferableTypedData` or keep the parse in the same isolate and only ship the parsed result — trigger: every OCR-path PDF import; growing input: raw OCR text length (unbounded by page count); work per unit: full-message copy; affected resource: isolate message cost + import wall time.
- [low] lib/data/services/script_import_service.dart:449-516 — ML Kit fallback loop does per-page `writeAsBytes` + `InputImage.fromFilePath(tempFile.path)` + `processImage` round-trips through one reused temp file, i.e. per-page disk write + channel round-trip with no batching — for a 60-page scan that is 60 sequential write→recognize cycles, each blocking the import future — pipeline render/recognize per page without the intermediate file write (feed bytes directly) or overlap render of page i+1 with recognition of page i — trigger: every image-PDF import on iOS/Android; growing input: page count (unbounded); work per unit: one PNG encode + write + channel call; affected resource: import wall time, disk churn.
- [low] lib/data/services/script_import_service.dart:394-400 — `buffer.writeln` accumulates the entire OCR'd document in a growable `StringBuffer` and the whole `rawText` is retained through `ParsedScript.rawText` — memory grows with document size and is held for the app lifetime of the parsed script — drop `rawText` from the retained `ParsedScript` (or store a lazy getter) since only `parseAndMapOcr` needs it transiently — trigger: every import; growing input: total OCR text (unbounded); work per unit: O(n) retained bytes; affected resource: resident memory per imported script.
- [low] lib/data/services/script_import_service.dart:648-659 — confidence averaging walks `rawLines` from `matchStart` forward calling `matches()` (regex-free but substring-heavy) per parsed line inside the `.map` — bounded by the 150-line window so not unbounded, but the `confidences.reduce`/`length` re-walk plus per-line closure allocation is repeated work on the UI isolate — compute the average inline in the same loop that collects confidences — trigger: every mapped parsed line on the OCR path; growing input: parsed line count × window (bounded 150); work per unit: O(window) compare; affected resource: UI isolate during import.

## Coverage
lib/data/services/script_import_service.dart — findings: 5
- [high] lib/data/services/script_parser.dart:560-571, 583-609, 638-641, 827-829, 1007-1009, 1250-1257, 1275-1307, 1324-1346, 1334, 1346, 1367-1370, 1436, 1450-1455, 1489-1492, 1558-1584, 1596, 1605, 1664, 1729-1730, 1744-1752 — RegExp construction inside hot loops and per-line/per-cue paths — `_detectCharactersStandard`/`_detectCharactersOwnLine`/`_detectCharactersTitleCase`/`_mergeOcrCharacterNames`/`_detectMultiCharacterNames`/`_buildCuePatterns`/`_detectCharacterCue`/`_extractInlineDirection`/`_isEnterExitLine`/`_extractLocation`/`addStageDirection`/`_parseLines`/`_stripInlineHeaders`/`_detectScenes`/`closeScene` each compile `RegExp(...)` literals (and `RegExp.escape`-joined patterns) on every call/iteration; on a full-length import (thousands of lines × per-line cue detection × per-character pattern rebuild) this is O(lines × characters) regex compilations plus O(lines) per-line compiles, tens of thousands of compilations per parse, blocking the UI isolate for seconds on a 1000+ line script — hoist every constant pattern to `static final` (as already done for `_folgerRunningHeaderRe`/`_actHeaderRe`/`_sceneHeaderRe`/`_shiftAnnouncementRe`) and cache the per-character cue pattern list keyed on (knownCharacters.length, _format) which the code already does at 1264-1270 but only for `_cuePatterns`, not for the other per-call compiles.
- [high] lib/data/services/script_parser.dart:855-895 — O(n²) fuzzy-merge loop with per-pair edit distance — `_mergeOcrCharacterNames` iterates every known character against every other (`nameList` × `nameList`) and computes `_editDistance` (an O(len²) DP allocating two `List.filled`/`List.generate` buffers per call, 1144-1145) for each pair; with ~50–200 detected characters this is thousands of O(len²) DP runs per parse, each allocating fresh lists — precompute counts once, skip pairs whose first letters differ before the DP (already guarded at 880 but only after the loop entry), and reuse two preallocated `Int8List`/`List<int>` buffers sized to the max name length instead of `List.generate`/`List.filled` per call.
- [medium] lib/data/services/script_parser.dart:1024-1076 — `_tryGluedMultiCharacterSplit` runs `knownCharacters.any`/`firstWhere` with a `replaceAll` per element inside a per-name loop — for each candidate name it walks the whole cast set calling `c.replaceAll(RegExp(r'[^A-Z]'), '')` (1055-1056, 1070-1071) — O(characters × characters × name-length) string churn per parse; precompute a `Map<String,String>` of stripped names once per parse and look up.
- [medium] lib/data/services/script_parser.dart:960-981 — `_resolveTitleCaseAbbreviations` nested scan of `fullNames` per abbreviation — for each abbreviation it linearly scans `fullNames` twice (965-970, 973-979) with `startsWith` string compares; with many Enter/Exit directions this is O(abbrevs × fullNames) per parse — build a prefix→full map keyed by first letters once.
- [medium] lib/data/services/script_parser.dart:241, 329-331 — `rawText.split('\n')` executed twice per `_detectTitleHeaders` call (241 and 329) plus again in `parse` via `_parseLines` (1444) — three full-document splits of a 1000+ line script allocate three large `List<String>` per parse; split once in `parse` and pass the list down.
- [medium] lib/data/services/script_parser.dart:262-294 — `_detectTitleHeaders` per-line loop compiles `RegExp(r'^\d{1,4}$')` (245), `RegExp(r'[.!?]$')` (266), the ACT/SCENE alternation (267-269), `RegExp(r'^\d+\s+|\s+\d+$')` (278-280) fresh for every one of the thousands of lines — hoist to static finals like the sibling patterns at 1196-1208.
- [medium] lib/data/services/script_parser.dart:225-231 — `isCharacterName` normalizes every known character (two `_normalizeForHeader` calls each doing three `replaceAll` passes) inside the candidate filter loop — O(candidates × characters × name length) per parse; precompute a normalized-name `Set` once.
- [medium] lib/data/services/script_parser.dart:1176-1191 — `_isNoise` runs up to 10 `_noisePatterns.hasMatch` per line plus `_normalizeForHeader` (three replaceAlls) for every line of the script — O(lines × patterns) with per-line allocation of the normalized string; acceptable only because patterns are static, but the `_normalizeForHeader` result is recomputed for the same line text in `_detectTitleHeaders` (279) and `_stripInlineHeaders` paths — compute once per line and share.
- [medium] lib/data/services/script_parser.dart:1688-1788 — `_detectScenes` `closeScene` allocates `sublist` + two `where().toList()` chains per scene boundary — for a script with many scene/act boundaries this is repeated O(scene-size) list copies; build the dialogue list with a single loop or reuse a scratch list.
- [low] lib/data/services/script_parser.dart:162-173 — character line-count loop uses `charCounts[char] = (charCounts[char] ?? 0) + 1` per multi-character entry — fine at this scale; no finding.
- [low] lib/data/services/script_parser.dart:1144-1145 — `_editDistance` allocates two fresh `List<int>` per invocation — only worth fixing as part of the O(n²) merge loop above; standalone it is bounded by name length.
- [info] lib/data/services/script_parser.dart:32-39 — `knownCharacters`, `characterAliases`, `multiCharacterMap` are instance fields on a parser that is presumably re-instantiated per import (assumption: caller constructs a fresh `ScriptParser` per parse; if it is reused across productions the stale cast leaks into the next parse) — verify caller; if reused, clear the three collections at the top of `parse`.
- [info] lib/data/services/script_parser.dart:1802 — file ends at the closing brace of `ScriptParser`; no further findings.

## Coverage
lib/data/services/script_parser.dart — findings: 9
- [medium] lib/data/services/stt_adaptation_service.dart:166-171 — per-sample synchronous `File.existsSync()` stat calls during lazy hydration — `_loadProduction` runs on the UI isolate when `ensureLoaded`/`addSample` first touches a production; each stored sample triggers a blocking `existsSync()` stat on the main thread, so a production with hundreds of accumulated samples (unbounded: samples grow with every recorded line, no cap on `samples` list) stalls startup/first-recording by hundreds of ms — replace with async `File.exists()` batched via `Future.wait` or check existence lazily at training time.
- [medium] lib/data/services/stt_adaptation_service.dart:243-255 — full-profile JSON re-encode and rewrite on every debounced persist — `_persistProduction` serializes every actor profile and every sample (`encode(e.value)` over all `_actorProfiles` entries matching the production, plus the pooled profile) into one `profiles.json` string and rewrites the whole file; with N actors × M samples this is O(total samples) string building + O(file) disk write per persist tick, and the debounce (2s) does not bound total work when recordings are frequent — persist per-actor JSON files or append-only sample log instead of rewriting the aggregate snapshot.
- [low] lib/data/services/stt_adaptation_service.dart:245-246 — `e.key.startsWith('$productionId:')` scan over all actor profiles per persist — `_actorProfiles` is keyed by `productionId:actorId` but the persist loop linearly scans every entry across every production; with many productions hydrated this is O(all profiles) per persist tick — key the map by productionId with a nested actor map, or maintain a per-production index.
- [low] lib/data/services/stt_adaptation_service.dart:283-287 — `getProductionActorProfiles` allocates a fresh list on every call — builds a new `List` via `where().map().toList()` each invocation; called from `recommendStrategy` (line 361) which itself is called per training decision — acceptable at small N but re-allocates per call with no memoization; cache per production or return an unmodifiable view.
- [low] lib/data/services/stt_adaptation_service.dart:320,335-337 — `totalAudioSeconds` recomputed by full `fold` over all samples on every `addSample` — each added sample re-walks the entire sample list (O(M) per recording, O(M²) cumulative across a session) just to recompute a running sum; keep a running total in the profile or update incrementally.
- [low] lib/data/services/stt_adaptation_service.dart:189-193 — merge builds a `Set` of live paths plus two list spreads per hydrated actor — `merge` allocates a set and two intermediate lists per actor profile on every hydration; with many actors this is allocation churn during startup hydration — merge in place or reuse the disk list when live is empty.
- [info] lib/data/services/stt_channel.dart:39,150-152 — `onPcm` delivers raw 16 kHz PCM chunks (~100 ms each) as `Uint8List` through a broadcast-style callback with no backpressure — at 16 kHz mono 16-bit that is ~32 KB per chunk / ~320 KB/s flowing through the Dart isolate while a recording is in progress; if the consumer does not drain promptly, chunks queue on the platform channel and memory grows unboundedly for the session duration — document/enforce a single fast consumer or drop chunks when the handler is null (the comment says chunks are dropped when nobody listens, but the code still invokes `onPcm?.call` per chunk regardless of consumer speed).
- [low] lib/data/services/stt_channel.dart:131-133 — `call.arguments as Map` then per-field casts on every native callback — `onResult` fires per recognized word segment during live listening; each callback allocates a `Map` view and re-casts; negligible per call but this is the hottest callback path in the file — acceptable as-is; no fix needed unless profiling shows channel overhead.

## Coverage
lib/data/services/stt_adaptation_service.dart — findings: 6
lib/data/services/stt_channel.dart — findings: 2
- [medium] lib/data/services/stt_service.dart:336-347 — matchScore runs a full O(m×n) LCS DP on the main isolate for every recognition partial (several times/second while the actor speaks); growing input is expected/spoken word counts (unbounded for long lines), work per unit is m×n cell updates plus two-row buffer swaps — consequence is UI jank during rehearsal on long script lines — smallest safe fix: cap expected/spoken word counts (e.g. bail out or clamp m,n to ~64) or move the DP to an isolate for inputs above a threshold.
- [medium] lib/data/services/stt_vocabulary_service.dart:384-399 — _correctAgainstExpected allocates a full (m+1)×(n+1) Int32List DP matrix per recognition partial on the main isolate; growing input is recognized/expected word counts (unbounded), work per unit is m×n cell updates — consequence is repeated main-thread blocking during live rehearsal on long lines — smallest safe fix: clamp word counts or compute alignment in a worker isolate for large inputs.
- [low] lib/data/services/stt_vocabulary_service.dart:333-345 — _bestVocabularyMatch scans every vocabulary word with an edit-distance DP per candidate on each uncached partial result; growing input is vocab.scanWords length (unbounded across productions), work per unit is O(len) DP per word — consequence is latency spikes when a recognized word misses the memoized correctionCache — smallest safe fix: pre-filter candidates by length window/first-letter bucket before the DP scan.
- [low] lib/data/services/stt_vocabulary_service.dart:49-106 — buildFromScript tokenizes every dialogue line and rebuilds the whole vocabulary object on each call; growing input is lines.length (unbounded), work per unit is tokenize+split per line — consequence is a startup/import-time hitch when a large script is loaded (called once per script load, so bounded per trigger) — smallest safe fix: none needed unless called repeatedly per production; otherwise memoize per productionId.
- [info] lib/data/services/stt_vocabulary_service.dart:32,35,39 — _vocabularies/_actorCorrections/_correctionPatterns are process-global maps with no eviction except clearProduction; growing input is distinct productionId:actorId keys over app lifetime (unbounded if productions are never cleared) — consequence is slow memory growth in a long session — smallest safe fix: verify callers always invoke clearProduction on unload; if not, add an LRU cap.

## Coverage
lib/data/services/stt_service.dart — findings: 1
lib/data/services/stt_vocabulary_service.dart — findings: 4
- [medium] lib/data/services/sync_queue.dart:243-245 — startup restore builds a Set literal from two list comprehensions over every pending+failed job, then `_loadPersisted` re-scans `_pending`/`_failed` per restored job via `queuedKeys.add` (line 249) — O(n²) at startup as the queue grows — trigger: app launch with a large persisted queue; growing input: persisted pending+failed job count, unbounded (no cap on queue size anywhere); work per unit: one set-key build + one linear scan per restored job; affected resource: main isolate CPU during `start()` before first frame; consequence: launch jank/blocking grows quadratically with queue length; smallest safe fix: build the key set once into a `Set<String>` and check `contains` instead of re-adding per job.
- [low] lib/data/services/sync_queue.dart:177-198 — `_persist()` rewrites the entire queue JSON (both lists) on every mutation (enqueue, upload success, upload failure, retry move) and re-loads the file first each time — trigger: every queue mutation; growing input: full pending+failed JSON, unbounded; work per unit: full serialize + full file write + full re-parse; affected resource: disk I/O + main isolate CPU per mutation; consequence: with hundreds of queued jobs each enqueue/upload tick pays a full-file rewrite; smallest safe fix: debounce/append-style persistence or write only the mutated entry.
- [low] lib/data/services/sync_queue.dart:270-272 — `pending`/`failed` getters allocate `List.unmodifiable` copies on every read — trigger: any caller polling these getters (e.g. UI refresh timers); growing input: queue length, unbounded; work per unit: full list copy per read; affected resource: allocation churn on hot UI paths; consequence: repeated polling allocates a fresh list each tick; smallest safe fix: cache an unmodifiable view (`List.unmodifiable` once per mutation) or expose `UnmodifiableListView`.
- [low] lib/data/services/tts_service.dart:107-121 — `setLocale` fetches the full system voice list and logs every matching voice's full map (up to 3 voices' complete metadata) on the main isolate — trigger: every locale change; growing input: system voice list size (device-dependent, typically dozens); work per unit: full list fetch + per-voice map stringify; affected resource: main isolate CPU + log I/O; consequence: locale switch stalls the UI thread briefly and spams the log; smallest safe fix: log only voice names/locales, not full maps, and skip the fetch when the locale is unchanged.
- [low] lib/data/services/tts_service.dart:299-343 — `assignVoice` re-filters the entire `_availableSystemVoices` list with chained `where().toList()` (locale match, exact match, gender pool) on every character assignment — trigger: once per character per rehearsal setup; growing input: full system voice list; work per unit: 3–4 full-list passes + intermediate list allocations; affected resource: main isolate CPU during rehearsal screen build; consequence: with many characters the setup path pays repeated full-list scans; smallest safe fix: memoize the locale-filtered pool per locale and reuse across characters.
- [low] lib/data/services/tts_service.dart:828-843 — `prepareKokoro` allocates a `Future` per chunk plus a `Future.wait` over all of them, and the `.then` callback runs `paths.every` over the whole list — trigger: every prefetch of a long line; growing input: chunk count (bounded by ~300-char chunks per line, so grows with line length, unbounded across lines); work per unit: one future + one method-channel round-trip per chunk; affected resource: platform-channel traffic + allocation churn; consequence: very long lines spawn dozens of concurrent channel calls at once; smallest safe fix: cap concurrent synthesis futures (e.g. batch in groups of 2–3) instead of `Future.wait` over all chunks.

## Coverage
lib/data/services/sync_queue.dart — findings: 3
lib/data/services/tts_service.dart — findings: 3
- [medium] lib/data/services/voice_config_service.dart:148-166 — O(n·w) nested scan building adjacency with per-line list allocations — dialogueLines.length × window × speakers work plus `_charsForLine` allocating a fresh List per line inside the outer loop; at a few thousand script lines this is hundreds of thousands of comparisons plus allocation churn on the UI isolate during import — hoist `_charsForLine` results into a precomputed `List<List<String>>` before the loop and reuse the adjacency map's putIfAbsent pattern; smallest safe fix: compute `aChars`/`bChars` once per line index outside the inner loops.
- [medium] lib/data/services/voice_config_service.dart:180-207 — greedy voice assignment re-reads `assignment.values.toList()` per character via `_leastUsedVoice` fallback — when every pool voice is taken by neighbors, each character triggers a fresh list allocation plus O(pool) counting; with many characters sharing a small pool this is repeated O(n·pool) work — smallest safe fix: maintain a running per-voice usage count map updated as assignments are made and pass it to `_leastUsedVoice` instead of `assignment.values.toList()`.
- [low] lib/data/services/voice_config_service.dart:88-90, 365-383 — `getOverride`/`resolveVoice`/`resolveSpeed` each decode the full overrides JSON blob per call — every per-character voice resolution re-parses `voice_overrides_<productionId>` (jsonDecode of a map sized by cast size) instead of reading one key; called per character per line render this is repeated parse+allocation work on the hot path — smallest safe fix: cache the decoded map per productionId (invalidate on `_saveOverrides`) or read only the requested character's key by storing per-character keys.
- [low] lib/data/services/voice_config_service.dart:325-346 — `_renameLoaded` performs three sequential full-blob read-modify-write cycles (overrides, genders, locales) — each cycle re-decodes and re-encodes its whole JSON map; at large cast sizes this triples serialization work per rename — smallest safe fix: batch the three re-key operations into one pass per blob (they already are one pass each; the cost is inherent) or cache decoded maps between the read and write within the same mutation chain.
- [low] lib/data/services/voice_config_service.dart:212-221 — `_leastUsedVoice` builds a counts map and reduces entries on every fallback call — allocation churn per character in the fallback path; bounded by pool size so consequence is minor — smallest safe fix: reuse a single counts map across calls or compute the least-used voice incrementally.
- [info] lib/data/services/voice_config_service.dart:26-34 — `_mutationChains` grows one entry per productionId and is never pruned — with many productions over a long session the map holds a completed Future per production forever; bounded by production count so impact is small — smallest safe fix: remove the entry once the chain settles (e.g. in a `.whenComplete` that deletes the key if it still points at this run).
- [info] lib/data/services/vision_ocr_channel.dart:19-25, 45-57 — deep `Map<String, dynamic>.from(b as Map)` casts and per-block map traversal on OCR results — for a multi-page PDF the plugin returns page×line maps and this wrapper rebuilds every block as a fresh typed map; work is O(pages×lines) per OCR call and unavoidable given the MethodChannel contract, so consequence is bounded by document size — smallest safe fix: none needed unless profiling shows the cast dominating; could construct `VisionTextBlock` directly from the raw map without the intermediate `Map<String, dynamic>.from`.

## Coverage
lib/data/services/vision_ocr_channel.dart — findings: 1
lib/data/services/voice_config_service.dart — findings: 6
- [low] lib/data/services/wav_silence.dart:70 — whole WAV file is read into memory (`readAsBytes`) and then copied three times (header sublist, audio sublist, padded output via BytesBuilder) on the main isolate — for a multi-MB rehearsal recording this blocks the UI/event loop during the synchronous parse+copy work and doubles peak memory — stream the file (read header incrementally, copy data chunk via `File.openRead`/chunked writes) or at minimum run the copy in an isolate — trigger: once per recording prepended before playback; growing input: unbounded WAV size (only `bytes.length` bounds the chunk walk, no size cap); work per unit: O(file) parse + O(file) copies; affected resource: main isolate CPU + 2–3× file-size allocations; consequence: jank/freeze on large recordings at the moment playback is being prepared.
- [low] lib/data/services/wav_silence.dart:31-48 — chunk-walk loop advances `offset` by attacker/file-controlled `size` with no validation that `size` fits the remaining bytes, so a malformed WAV with a huge chunk size makes `body + size` overflow/wrap and the loop can re-scan or spin over the same region repeatedly (and `sublist(offset, offset+4)` at line 32 can throw RangeError caught by the blanket `catch` at 98, silently returning the source) — worst case is repeated O(file) scans per invocation — clamp `size` to `bytes.length - body` before advancing and break when `offset` stops advancing.
- [info] lib/data/services/wav_silence.dart:98-100 — blanket `catch (_)` returns the unmodified source on any failure (including the RangeError above), so a corrupt WAV silently skips padding with no log — acceptable fallback direction (returns safer original file), noted only because it masks the loop-bounds defect above.

## Coverage
lib/data/services/wav_silence.dart — findings: 3
- [info] integration_test/android_kokoro_rtf_test.dart:28-35 — busy-poll loop with `existsSync` on three files every 3 s for up to 6 minutes — each iteration performs synchronous filesystem stat calls on the test runner's event loop; consequence is negligible on-device (test-only, bounded by the 6-minute timeout and 3 s delay), but the pattern is a sync-I/O poll that would block a serving path if copied into production — smallest safe fix: none needed for the test itself; if reused, replace with a `Completer`/watch-based readiness signal.
- [info] integration_test/android_kokoro_service_test.dart:24-27 — same sync-poll readiness loop (`isKokoroReady()` awaited every 3 s for up to 6 minutes) — bounded by timeout and delay, test-only; consequence is only runner latency, not production cost — smallest safe fix: none needed; production readiness should use a push/event signal rather than polling.
- [info] integration_test/android_kokoro_service_test.dart:55-58 — `Future.wait` over two concurrent `synthesize` calls — bounded to exactly two items by the test, so no unbounded fan-out; consequence: none at this scale — smallest safe fix: none needed; production callers queuing many lines should cap concurrency instead of an unbounded `Future.wait`.

## Coverage
integration_test/android_kokoro_rtf_test.dart — findings: 1
integration_test/android_kokoro_service_test.dart — findings: 2
- [low] integration_test/android_live_matching_test.dart:94-97 — per-chunk `Uint8List.sublistView` allocations in a tight feed loop over the whole resampled wav — for a multi-second utterance this allocates thousands of small views per decode run; consequence is allocation churn on the test main thread during the hot feed path — hoist a single view of the full buffer and pass offsets, or reuse one scratch view per chunk.
- [low] integration_test/android_live_matching_test.dart:186-215 — `_resampleTo16k` runs a per-sample Dart loop with two `getInt16` reads and one `setInt16` write per output sample, synchronously on the main thread — a 4 s 44.1 kHz wav (~176k samples → ~64k output samples) costs ~64k iterations of interpreted arithmetic inside the test's hot path; consequence is a multi-hundred-ms main-thread stall before feeding PCM — move the resample into the recognizer isolate or compute it once outside the timed section; test-only reachability (operator running the integration suite), so low.
- [low] integration_test/android_paddle_ocr_test.dart:73-81 — full-text dump built with `StringBuffer` then written with `writeAsStringSync` on the main thread — for a dense multi-page script the dump can be hundreds of KB and the sync write blocks the test event loop while OCR results are already in memory; consequence is a one-off main-thread stall in a test-only path — switch to `writeAsString` (async) or stream per page; test-only reachability, low.

## Coverage
integration_test/android_live_matching_test.dart — findings: 2
integration_test/android_paddle_ocr_test.dart — findings: 1
- [low] integration_test/android_rehearsal_harness_test.dart:138 — `File(path!).lengthSync()` sync file I/O inside the per-line loop of a serving-path simulation — blocks the test event loop for the duration of a disk read on every line (and again at :293 for the recording path), which at scale of many lines adds measurable wall-clock latency to the latency metric being measured — replace with `File(path).length()` (async) or precompute the byte length once outside the loop.
- [low] integration_test/android_rehearsal_harness_test.dart:268 — `File(wav!).readAsBytesSync()` sync read of the whole synthesized WAV inside the acoustic round-trip loop — blocks the event loop while the entire PCM buffer is read from disk, inflating the measured recognizer latency for that line — use `readAsBytes()` (async) or read once outside the loop.
- [low] integration_test/asr_streaming_macos_test.dart:56 — `File(path).readAsBytesSync()` sync read of each fixture WAV inside the per-candidate/per-line loop — blocks the test event loop for the full file read on every one of the 8 candidate×line iterations, adding disk I/O time to the RTF metric being measured — switch to `readAsBytes()` (async) or read each fixture once outside the candidate loop.
- [info] integration_test/asr_streaming_macos_test.dart:94-104 — LCS `_matchRate` is O(len(expected)×len(got)) with a fresh `List<int>.filled(b.length+1, 0)` allocated per outer iteration — for these short fixture lines the cost is negligible, but if reused on long transcripts (e.g. minutes-long live mic transcripts) the per-row allocation churn and quadratic work would dominate — acceptable as-is for the 4 short fixture lines; flag only if reused on production-length transcripts.
- [info] integration_test/android_rehearsal_harness_test.dart:269-271 — PCM is fed in 3200-sample chunks via `Uint8List.sublistView` per chunk inside the injection loop — each iteration allocates a fresh view object; for a 416-char line (~10 s audio ≈ 320 kB) that's ~100 small allocations per line, negligible per line but repeated for every line in the rehearsal loop — hoist the view creation or feed the whole buffer if the recognizer accepts it, only worth doing if the loop grows to many lines.
## Coverage
integration_test/android_rehearsal_harness_test.dart — findings: 3
integration_test/asr_streaming_macos_test.dart — findings: 2
- [low] integration_test/asr_testwav_transcript_macos_test.dart:43-45 — per-sample loop with `getInt16`/division allocates nothing but runs sync on the test main thread; for a multi-second wav (rate from header, n = (len-44)/2) this is O(n) scalar work per test run — consequence: transcription setup adds tens of ms per invocation, only in a one-off eval test — smallest safe fix: none needed (test-only, bounded by fixture size); if reused, convert via `Float32List.fromList`-style batch or move to isolate.
- [low] integration_test/kokoro_pack_smoke_macos_test.dart:37-46 — `_matchRate` allocates a fresh `List<int>.filled(b.length+1, 0)` row per outer iteration (a.length rows) — O(a*b) allocations churn inside the LCS loop; with `_line` ~24 words and heard text similar length this is trivial, but it re-runs per voice (4 voices) — consequence: negligible at this scale; becomes noticeable only if expected/got grow to thousands of words — smallest safe fix: reuse two preallocated rows and swap, avoiding per-row allocation.
- [low] integration_test/kokoro_pack_smoke_macos_test.dart:50-59 — `_to16k` resamples the full synthesized waveform with a per-sample loop on the main thread; audio.samples for a ~5s utterance at 24kHz is ~120k samples, resampled to ~80k — consequence: ~ms-scale sync CPU per voice, 4 voices per test run, test-only — smallest safe fix: none needed; if promoted to production, offload to isolate/worker.
- [info] integration_test/kokoro_pack_smoke_macos_test.dart:95-112 — loop over 4 voices runs full TTS synthesis + ASR decode serially on the main thread with a 15-minute timeout; each iteration is heavy ONNX inference — consequence: long wall-clock test, but bounded (4 entries, fixed `_line`), test-only — smallest safe fix: none needed; parallelizing would risk resource contention on device.

## Coverage
integration_test/asr_testwav_transcript_macos_test.dart — findings: 1
integration_test/kokoro_pack_smoke_macos_test.dart — findings: 3
- [low] integration_test/kokoro_service_queue_macos_test.dart:35-43 — `_stagePack` copies the entire staged model pack with sync `listSync`/`copySync`/`createSync` file I/O on the test main isolate — blocking work, but test-only and bounded by the shipped pack size — smallest safe fix: none needed (test-only; if desired, switch to async `Directory.list`/`File.copy`).
- [info] integration_test/ocr_dump_macos_test.dart:34 — whole-file `readAsBytes`/`writeAsBytes` buffering of the PDF fixture in memory — allocation churn proportional to file size, but bounded by the single fixture and test-only — smallest safe fix: none needed (test-only dump harness).
- [info] integration_test/ocr_dump_macos_test.dart:57 — `jsonEncode` of the full parsed dump (all lines with text) into one string then one `writeAsString` — builds the entire JSON body in memory before writing; bounded by the P&P fixture and test-only — smallest safe fix: none needed (test-only).

## Coverage
integration_test/kokoro_service_queue_macos_test.dart — findings: 1
integration_test/ocr_dump_macos_test.dart — findings: 2
- [low] integration_test/ocr_highlight_audit_macos_test.dart:93-98 — full-map scan fallback: when a flagged line is not found on its page or ±2 neighbors, `locate` is re-run against every page's line list (`for (final e in byPage.entries)`), O(pages × lines) per miss — with the 82-page fixture and many misses this dominates the 30-minute budget and can push the test past its timeout — smallest safe fix: precompute a text→page index once (or cap the far-scan to a sample) so misses resolve in O(1)
- [low] integration_test/ocr_highlight_audit_macos_test.dart:82-88 — neighbor probing re-runs `OcrHighlightMatcher.locate` over each neighbor page's full line list for every flagged miss (up to 4 pages × lines per miss) — redundant recomputation when the same text appears on many flagged lines; consequence: quadratic-ish work in flagged×lines on a device-bound test — smallest safe fix: memoize `locate` results per (text, page) pair
- [info] integration_test/ocr_highlight_audit_macos_test.dart:35,38 — whole PDF read into memory then re-written (`readAsBytes`/`writeAsBytes`) and OCR results buffered per page in `byPage` map — bounded by the 82-page fixture, no unbounded growth; consequence: none at this scale — smallest safe fix: none needed
- [info] integration_test/ocr_import_macos_test.dart:38 — read-then-write of the full PDF into the sandbox temp dir buffers the whole file in memory; bounded by fixture size, single-shot test — smallest safe fix: none needed

## Coverage
integration_test/ocr_highlight_audit_macos_test.dart — findings: 2
integration_test/ocr_import_macos_test.dart — clean
- [info] integration_test/ocr_page_repeat_macos_test.dart:26 — full PDF file is read into memory and written back byte-for-byte before every test run (`readAsBytes`/`writeAsBytes` on the whole sample script PDF) — for a full-length Pride & Prejudice script PDF this is a multi-MB synchronous-ish buffered copy on the test main thread, adding seconds of setup and allocation churn before the 20-minute timeout budget is even entered — smallest safe fix: copy via `File.copy` (streamed) or skip the copy when the destination already exists.
- [info] integration_test/rehearsal_demo_test.dart:96-100 — the 36-second hold loop pumps every 100 ms and also does a real `Future.delayed` per iteration, i.e. ~360 pump+delay pairs of widget-tree rebuild work on the test main thread for the whole rehearsal scene — this is intentional recorder-ramp scaffolding, not a production hot path, so consequence is limited to long test wall-clock and frame churn — smallest safe fix: none needed (test-only); if desired, batch pumps at 500 ms intervals to cut rebuild count ~5x.
## Coverage
integration_test/ocr_page_repeat_macos_test.dart — findings: 1
integration_test/rehearsal_demo_test.dart — findings: 1
I need to review two files for performance issues:

1. `integration_test/screenshot_test.dart` (195 lines)
2. `integration_test/tts_kokoro_compare_macos_test.dart` (219 lines)

Both are test files. Per the severity guidance: "test-only code = low". Let me analyze each file for performance issues.

**File 1: screenshot_test.dart**

This is an integration test that drives the app through flows and captures screenshots. Let me look for performance issues:

- Line 133-136: `hamlet.scenes.firstWhere((s) => s.characters.contains('HAMLET'), orElse: () => hamlet.scenes.first)` — this is a single call, not in a loop. `firstWhere` with `contains` inside is O(n*m) but it's called once. Not a hot path.

- Line 111: `container.read(productionsProvider.notifier).add(production)` — single call.

- The test does a lot of `pumpAndSettle` calls but that's test infrastructure.

- Line 69: `SharedPreferences.getInstance()` — once.

- Line 78: `AppDatabase.forTesting(NativeDatabase.memory())` — in-memory database, once.

- Line 81: `rootBundle.loadString(_hamletAssetPath)` — loads the full Hamlet script once. That's ~180KB of text, parsed once. Fine for a test.

- Line 83: `importer.importFromText(rawHamlet, title: 'Hamlet')` — parses the script once. This is the heavy work but it's a test.

- Line 92-101: `tester.pumpWidget` with ProviderScope — once.

- Line 109-110: `ProviderScope.containerOf(tester.element(find.byType(CastCircleApp)))` — `find.byType` traverses the widget tree, called a few times. Each call is O(tree size) but it's a handful of calls in a test.

- Line 55: `tester.element(find.byType(Navigator).first)` — called via `_context` multiple times (lines 120, 127, 146, 151, 157, 162, 164, 169, 171, 176, 178). Each `find.byType(Navigator)` is a full tree walk. In a test with ~10 calls, this is negligible. But wait — is this a performance issue worth reporting? The checklist says "firstWhere/contains inside a loop" — `_context` is called repeatedly but not in a loop per se; it's sequential calls in a test. Each is one tree traversal. For a test file, this is low severity at best. Actually, `find.byType(Navigator).first` — `.first` on a finder that matches multiple navigators. In a GoRouter app with nested navigators there could be several. Still, ~11 calls in a test run — negligible.

- Line 183-189: `restore` closure defined and called twice. Fine.

- Line 193: `db.close()` — good, closes the DB.

- Potential issue: the test never pops the last pushed route before ending, but that's not performance.

- Line 40: `pumpAndSettle(const Duration(milliseconds: 500))` then line 48 `pumpAndSettle()` — double settle in `_snap`, called 9 times. Each `pumpAndSettle` pumps frames until no scheduled frame. This is test-harness cost, not app performance. Not really a finding.

- Is there anything that would be a real performance defect class? The test is essentially sequential UI driving. I don't see N+1 queries, allocation churn in loops, unbounded caches, etc.

One thing: line 133-136 `firstWhere` with `orElse: () => hamlet.scenes.first` — if scenes is empty, `hamlet.scenes.first` throws. But that's correctness, not performance. And we're told not to report style/correctness — performance only.

Actually wait — the guidance says report performance only. Let me check the checklist items again for this file:

- "firstWhere/contains inside a loop" — line 133: `firstWhere` with `contains` — but NOT inside a loop. Called once. Not a finding.
- `find.byType` repeated — each is a tree walk, but ~11 times in a test. Micro. The rule: "Reject micro-optimizations without a scale threshold." No scale here.

I think screenshot_test.dart is clean. Hmm, but let me double-check `_snap`: it calls `pumpAndSettle` twice per screenshot (line 40 and 48), plus `convertFlutterSurfaceToImage` once. 9 screenshots × 2 settles. Test-only, negligible.

Actually, one more: line 102 `pumpAndSettle(const Duration(seconds: 2))` — a 2-second settle duration means the test pumps frames for up to 2 seconds of simulated time... actually pumpAndSettle pumps until animations settle, with the given duration per frame step. Not a perf issue in the app.

Verdict: clean.

**File 2: tts_kokoro_compare_macos_test.dart**

This is a benchmark/eval test comparing Kokoro TTS models. Let me look for performance issues:

- Line 48-67: `_matchRate` — implements Smith-Waterman-ish DP (actually it's a simple edit-distance-like DP for word matching). Complexity: O(a.length × b.length). Called once per (model × voice × line) = 2 models × 3 voices × 3 lines = 18 times. Each call allocates `List<int>.filled(b.length + 1, 0)` twice per outer iteration (line 56 initial `prev`, line 58 `cur` per i). So allocation churn: for each i in a.length, allocates a new list of b.length+1. For short lines (a few words), negligible. But wait — the DP here: `prev` and `cur` rows. It allocates `cur` fresh each outer iteration — that's the standard approach, could reuse buffers but for 18 calls with ~10-word strings it's nothing. Test-only, low.

Actually, is there a real issue? The DP is O(n²) per call, 18 calls, tiny inputs. Not a finding per "reject micro-optimizations without a scale threshold."

- Line 70-80: `_to16k` — resamples audio to 16kHz. Allocates `Float32List(n)` once, loops n times doing interpolation. O(n) — fine. Called once per generated audio (18 times). Audio could be seconds long → n ~ tens of thousands to millions. O(n) linear, fine. The interpolation math per sample is constant. OK.

Wait — line 71: `final n = (x.length * 16000 / fromRate).floor();` — if fromRate is 24000, n = x.length * 2/3. Fine.

- Line 82-108: `_writeWav` — builds a ByteData of 44 + n*2 bytes, writes it. For each of 18 WAVs. The loop at line 103-106 writes each sample with `setInt16` — O(n) with per-sample function call overhead. For a few seconds of audio at 24kHz, n ~ 72k samples per line. 18 files. Total ~1.3M setInt16 calls. That's fine for a test (sync CPU work but test-only, runs once). Hmm — "Sync CPU work on the event loop" — this is sync I/O (`writeAsBytesSync` line 107) on the main isolate. But it's a test harness writing WAV files, not a serving path. Test-only = low severity. Is it worth reporting? The checklist has "*Sync file I/O on serving paths" — this is not a serving path. I'd say clean or at most a low/info. Let me think about whether this is a "defensible finding."

The whole file is a benchmark that runs once with a 30-minute timeout. The work is inherently heavy (TTS synthesis, ASR decode). The WAV writing is incidental. I don't think reporting "sync file write in a test" is defensible as a performance issue — it's the test's purpose.

- Line 137: `final reference = <String, Float32List>{}` — holds reference audio for fp32 entries. Bounded: 3 voices × 3 lines = 9 entries max, each a Float32List of audio samples. Bounded by _voices × _lines = 9. Not unbounded. Fine.

- Line 139: `for (final entry in _models.entries)` — 2 models. Inner loops: 3 voices × 3 lines = 9 iterations per model. Total 18 TTS generations + 18 ASR round-trips. Each TTS generation is the expensive part (model inference). That's the test's purpose.

- Line 156: `var rtfSum = 0.0, matchSum = 0.0, count = 0;` — accumulators, reset per model. Fine.

- Line 166: `_writeWav('${outDir.path}/${entry.key}_${v.key}_line$li.wav', ...)` — string interpolation per call, fine.

- Line 170-179: ASR round-trip. `stream.acceptWaveform` with `_to16k(audio.samples, audio.sampleRate)` — resample O(n). Then `while (asr.isReady(stream)) { asr.decode(stream); }` — decode loop until not ready. This is the ASR decode loop — could this be unbounded? `isReady` is a sherpa API; the loop terminates when the model says not ready. Standard streaming ASR pattern. Fine.

- Line 174: `stream.acceptWaveform(samples: Float32List(12800), sampleRate: 16000)` — flush with zeros, standard.

- Line 193-206: correlation computation. `for (var i = 0; i < n; i++)` with 3 multiplications and 3 accumulations per sample — O(n) over the shorter of a/b. Called once per non-fp32 model entry (only for fp16-v1_0, 9 times). n = min(a.length, b.length) — audio length, ~72k samples. 9 × 72k × ~6 ops = ~4M ops. Fine for a test.

Wait, there's a subtle bug-ish thing: line 195 `final n = a.length < b.length ? a.length : b.length;` — uses min length. Then line 196 `lenRatio = a.length / b.length`. Fine.

- Line 203: `corr = sab / math.sqrt(saa * sbb)` — fine.

- Memory: `reference` map holds 9 Float32Lists of full audio. Each ~72k samples × 4 bytes = ~288KB, total ~2.6MB. Bounded. Fine.

- Line 33-36: `_models` — 2 entries. Line 39: `_voices` — 3. Line 41-46: `_lines` — 3. All bounded constants.

- Line 27: `const _eval = '$_ccRepo/.asr-eval';` — fine.

- Line 116-118: `Directory(...)..createSync(recursive: true)` — sync dir creation once. Fine.

- Line 107: `File(path).writeAsBytesSync(b.buffer.asUint8List())` — sync write per WAV, 18 writes. Test-only.

Hmm, is there anything genuinely reportable in this file? Let me reconsider `_matchRate` allocation churn: line 58 `final cur = List<int>.filled(b.length + 1, 0);` allocated inside the outer loop (per i). For a.length iterations. With growable... no, `List.filled` with no growable param defaults growable: true actually. `List<int>.filled(b.length + 1, 0)` — filled's default growable is true. Minor. The allocation churn is a.length allocations of size b.length+1. For the test's 3-word-ish lines... actually the lines are longer: line 42-44 is ~25 words. So a.length ~ 25, b.length ~ heard words ~ similar. 25 allocations of 26 ints per call, 18 calls. Utterly negligible. Not a finding.

What about `_to16k` — line 73-78: per-sample it does `src = i * fromRate / 16000` (float division per sample), `floor()`, `clamp` twice, etc. O(n) with constant work. Fine.

One thing I notice: `_writeWav` line 84: `ByteData(44 + n * 2)` — allocates the whole WAV in memory then writes. For long audio this could be large but bounded by audio length (seconds). Fine.

`print` calls — line 119, 208-210, 213-214. Fine.

Is there any unbounded collection? `reference` map — bounded to 9 entries (only fp32 entries stored: 3 voices × 3 lines = 9). Fine.

The `while (asr.isReady(stream)) asr.decode(stream)` loop — is it potentially infinite? In sherpa's API, `isReady` returns whether there's enough context to decode; the loop terminates. This is the standard usage pattern from sherpa examples. Not a finding.

So both files seem clean from a performance standpoint. But wait — the task says "A file with no real issue produces no finding line." And the coverage section must list both files.

Let me reconsider screenshot_test.dart once more for anything:

- Line 34: `late IntegrationTestWidgetsFlutterBinding _binding;` — checklist mentions "misused `late`". Here `late` is used for a top-level test binding initialized in main before tests run. That's correct usage, not misuse. Not a finding.

- Line 36: `bool _surfaceConverted = false;` — module-level mutable state to avoid re-converting. That's actually a memoization to avoid repeated conversion. Good.

- Line 44-47: the guard `if (!_surfaceConverted)` — caches the conversion. Fine.

- Line 92-101: pumpWidget once.

- Line 111: `container.read(productionsProvider.notifier).add(production)` — one add.

- Line 120: `_context(tester).push('/import')` — `_context` does `find.byType(Navigator).first`. Called ~11 times total across the test. Each finder traversal is O(widget count). Widget count for this app maybe a few hundred. 11 × few hundred = trivial. Not a finding.

- Line 133: `firstWhere` with `contains` — once, over ~20 scenes × ~few characters each. Trivial.

- Line 142-145: `if (hamlet.scenes.isNotEmpty)` guard then set state. Fine.

- Line 183-192: restore closure — 2 calls. Fine.

- Line 193: `db.close()` — good.

- One potential real issue: the test mutates global SharedPreferences (lines 72-73) and restores at the end (191-192) — but if the test fails midway, prefs are left mutated. That's correctness/robustness, not performance. Not reportable under this lens.

- Another: `await prefs.setBool(...)` etc. — each is a round-trip to the platform channel in integration tests... SharedPreferences in integration tests uses the real plugin; each setBool is an async channel call. 4 calls total. Trivial.

So screenshot_test.dart: clean.

tts file: let me scan once more for the checklist classes:

- "Closures/lists/maps allocated per iteration" — `_matchRate` allocates `cur` per outer iteration (line 58). Technically matches the class, but scale threshold: 18 calls × ~25 iterations × 26-element lists. The rule says "Reject micro-optimizations without a scale threshold." There's no scale here — inputs are fixed 3 short lines. I'll not report. Hmm, but should I report it as low/info? The instruction: "Reject micro-optimizations without a scale threshold." I think clean is right.

- "Numeric List<int>... for large/hot arrays" — `List<int>.filled` in _matchRate — small. `Float32List` for audio — appropriate typed list, good.

- "Sync CPU work on the event loop" — `_writeWav` and `_to16k` and the correlation loop are sync CPU on the main isolate. But this is a test benchmark whose whole point is to measure; the sync work is the workload. Test-only = low. Is it defensible? The consequence would be: the test runner's UI/event loop is blocked during WAV writes — but there's no UI being interacted with. I lean clean.

Actually, hold on. Let me reconsider one thing in the tts file: line 104: `(samples[i] * 32767).round().clamp(-32768, 32767)` — per-sample. And line 103-106 loop over n samples with `setInt16` — that's the WAV encode. For a 10-second clip at 24kHz that's 240k iterations; 18 clips → ~4.3M setInt16 calls. Each setInt16 in ByteData is a method call with bounds check. ~4.3M calls ≈ maybe 100-200ms total. Negligible in a 30-minute-timeout test. Clean.

What about `_matchRate`'s DP being O(a×b) — with 25×25 = 625 cells per call × 18 = trivial. Clean.

OK so my conclusion: both files are clean. But that feels like a suspicious outcome for a review task... Let me re-read the files more carefully for anything I might have missed.

screenshot_test.dart again, line by line:

Lines 1-27: imports and comments. Line 24: `import 'package:castcircle/data/models/production_models.dart';` — imports all of production_models (barrel import). Not a perf issue.

Line 32: `const _hamletAssetPath = 'assets/test_scripts/hamlet.txt';`

Line 34: `late IntegrationTestWidgetsFlutterBinding _binding;`

Line 36: `bool _surfaceConverted = false;`

Lines 39-50: `_snap`:
- `pumpAndSettle(Duration(milliseconds: 500))` — pumps frames with 500ms steps until settled.
- conversion guard.
- `pumpAndSettle()` again (default 100ms? default is 30ms per pump... actually default duration for pumpAndSettle is `const Duration(milliseconds: 30)`... no wait, it's `kFastMethod`... whatever).
- `takeScreenshot(name)`.

Called 9 times. Each does 2 pumpAndSettle. Fine.

Lines 54-56: `_context` — `tester.element(find.byType(Navigator).first)`.

Hmm — `find.byType(Navigator).first` — if there are multiple Navigators (GoRouter typically has root Navigator + possibly nested), `.first` picks the first in traversal order (depth-first). If the app uses nested navigators (e.g., a ShellRoute), the first Navigator found might be a nested one rather than the root — pushing on the wrong navigator could... that's correctness, not perf. Skip.

Lines 58-59: main, binding init.

Lines 61-194: the test body.

Line 69: `SharedPreferences.getInstance()` — in integration tests this awaits the platform channel. Once.

Lines 70-73: read prev values, set new. 4 async channel ops. Fine.

Line 78: `AppDatabase.forTesting(NativeDatabase.memory())` — creates in-memory DB. Once. Note: `NativeDatabase.memory()` — fine.

Line 81: `rootBundle.loadString(_hamletAssetPath)` — loads Hamlet (~180KB) once.

Line 82-83: `ScriptImportService()` then `importFromText` — parses the script. Once. This is O(script length) presumably. Fine — done once.

Lines 84-90: constructs Production with `const Uuid().v4()` — wait, `const Uuid()` — Uuid's default constructor... `const Uuid()` — is Uuid's constructor const? In the uuid package, `Uuid()` is not const I think... Actually in uuid ^3/4, `Uuid` class has a non-const constructor. Hmm, `const Uuid().v4()` — if Uuid's constructor isn't const this wouldn't compile. But we're told the file contents are what they are; maybe the version has a const constructor. Not a perf issue anyway. Actually wait — `const Uuid().v4()` — if it compiles, `const Uuid()` is a canonical constant — fine. Not perf.

Line 92-101: pumpWidget with ProviderScope overrides. Once.

Line 102: `pumpAndSettle(Duration(seconds: 2))`.

Line 105: `_snap(tester, '01_home_empty')`.

Line 109-110: `ProviderScope.containerOf(tester.element(find.byType(CastCircleApp)))` — one finder + container lookup.

Line 111: `container.read(productionsProvider.notifier).add(production)` — adds production via notifier. Once. If `add` internally does DB insert + state update — once. Fine.

Line 112-113: set notifier states directly. Note: setting `.state` directly on a notifier... fine.

Line 114: pumpAndSettle(1s).

Line 117: `_snap('02_home_with_production')`.

Line 120: `_context(tester).push('/import')` — hmm, `BuildContext.push`? GoRouter's BuildContext has `push` with a path? Actually GoRouter context has `go`, `push` (which takes a route name or path?). `context.push('/import')` — GoRouter's `push` takes a location string. OK.

Line 121-122: settle + snap.

Line 126: set rehearsalCharacterProvider to 'HAMLET'.

Line 127: `_context(tester).go('/production')`.

Line 128-129: settle + snap.

Lines 133-136: firstWhere over scenes. Once.

Lines 139-145: set mode/character/scene states.

Line 146: push('/rehearsal').

Line 147-148: settle + snap.

Line 151: pop. Line 152: settle.

Lines 153-156: set states.

Line 157: push('/rehearsal') again.

Line 158-159: settle + snap.

Line 162-164: pop, settle, push('/settings').

Line 165-166: settle + snap.

Line 169-171: pop, settle, push('/ai-models').

Line 172-173: settle + snap.

Line 176-178: pop, settle, push('/cast').

Line 179-180: settle + snap.

Lines 183-189: restore closure.

Lines 191-192: restore prefs.

Line 193: `db.close()`.

Line 194: end.

One thing: the test ends without popping the last pushed route ('/cast') and without resetting provider states — but the container is torn down with the test. Not perf.

Another: `await db.close()` at line 193 — but if any earlier step throws, close never runs (no try/finally). In-memory DB, so no leak beyond the test process. Not perf.

So screenshot_test.dart: clean. I'm fairly confident.

tts file once more, line by line:

Lines 1-18: comments/imports.

Line 23-24: `_ccRepo` from environment, default '.'.

Line 27: `_eval`.

Lines 33-36: `_models` map with 2 entries.

Line 39: `_voices` — 3 entries.

Lines 41-46: `_lines` — 3 strings.

Lines 48-67: `_matchRate`:
- `words()` closure: toLowerCase, replaceAll(RegExp), trim, split — allocates a few strings/lists per call. Called 2× per _matchRate call (for expected and got). 18 calls → 36 words() calls. Each on ~25-word strings. Trivial.
- DP: O(a×b) with per-row list allocation. Trivial scale.
- Note: the DP at lines 56-66 — let me verify it's actually correct-ish... not my job (perf only). Actually wait, there's something: line 55 `if (a.isEmpty || b.first.isEmpty) return 0;` — checks b.first.isEmpty but if b is empty entirely, `b.first` throws. Correctness, not perf. Skip.

Lines 70-80: `_to16k`:
- O(n) resample. One allocation of output. Fine.
- Per-sample: `i * fromRate / 16000` — float ops. `clamp(0, x.length - 1)` twice. Fine.

Lines 82-108: `_writeWav`:
- `ByteData(44 + n * 2)` — one allocation sized to output. Fine.
- `s()` closure writes string bytes via loop with `codeUnitAt` — for 'RIFF' (4), 'WAVEfmt ' (8), 'data' (4). Trivial.
- Sample loop lines 103-106: O(n) setInt16. Fine for test scale.
- `writeAsBytesSync` — sync write, once per WAV, 18 total. Test-only.

Lines 110-111: main + binding init.

Line 113: test with 30-min timeout.

Line 114: `sherpa.initBindings()`.

Lines 116-119: temp dir create + print.

Lines 122-134: ASR recognizer config — created once. Note `numThreads: 2` — fine.

Line 137: `reference` map.

Line 139: outer loop over 2 models.

Lines 141-154: TTS config per model — creates a new OfflineTts per model (2 total). Each loads model files from disk (the big cost, once per model). Fine.

Line 156: accumulators.

Line 157: loop over 3 voices.

Line 158: loop over 3 lines → 9 iterations per model.

Lines 159-164: generate + time. `tts.generate(text, sid, speed)` — the heavy inference. 18 total.

Line 166-167: write WAV.

Lines 170-179: ASR round-trip:
- `asr.createStream()` per iteration (18 streams, each freed at line 179). Fine — freed.
- `acceptWaveform` with `_to16k(...)` — O(n) resample per iteration.
- `acceptWaveform(samples: Float32List(12800), ...)` — allocates 12800 floats (51KB) per iteration for the flush — 18 × 51KB = ~1MB total churn. Trivial.
- decode loop.
- `getResult`, `free`.

Line 180: `_matchRate`.

Lines 182-184: accumulate.

Lines 189-206: correlation:
- Only for fp16 entry when reference exists.
- O(min(a.len, b.len)) loop with 6 flops. 9 iterations × ~72k = ~650k iterations × 6 ops. Trivial.

Lines 208-210: print per iteration.

Lines 213-214: print summary.

Line 215: `tts.free()` per model. Line 217: `asr.free()`.

Everything is bounded, once-run, test-only. I really don't see a defensible performance finding in either file.

Hmm wait — one more consideration for the tts file: line 137 `reference` map stores `audio.samples` (the fp32 reference) — but note that `audio.samples` for the fp32 model is stored, and for fp16 the correlation compares. The map holds up to 9 Float32Lists alive for the whole test. Bounded. Fine.

And `_writeWav`'s `s()` closure: `t.codeUnitAt(i)` — for ASCII strings this is fine.

One more possible angle: the checklist mentions "Unbounded Future.wait(items.map(...))" — not present. "StreamController.broadcast" — not present. "HttpClient per request" — not present. "Buffering whole bodies" — `_writeWav` buffers the whole WAV in a ByteData then writes — that IS "buffering whole bodies" in a sense, but for a test writing WAVs of known bounded size, and it's the simplest correct approach. Not defensible.

"Isolate message cost" — no isolates. "Runtime reflection" — no.

What about `print` inside the loop (line 208) — 18 prints. Fine.

OK. Both files clean. Coverage: 2 lines, both "clean".

Wait — but the task framing says "review EVERY file" and expects findings where defensible. If truly nothing, then the report is just the coverage section. Let me make one final pass looking for anything even low-severity that's genuinely defensible...

screenshot_test.dart:
- Repeated `_context(tester)` calls each re-running `find.byType(Navigator)` — 11+ tree walks. Could hoist the BuildContext... but after each push/pop the tree changes, so re-finding is arguably necessary. And scale is trivial. Not defensible.
- `pumpAndSettle` twice in `_snap` (lines 40, 48) — the first settle at line 40 happens before conversion on every screenshot even after conversion is cached; the double-settle is 2× frame pumping per screenshot × 9. Test-harness cost only. Not defensible as a perf defect.

tts file:
- `_matchRate` allocates a fresh `cur` list per outer iteration instead of ping-pong buffers — micro, no scale. Not defensible.
- `_writeWav` builds the entire WAV in memory then writes sync — bounded by clip length; test-only. Not defensible.

Actually, hmm, let me reconsider whether there's ONE thing worth an [info]/[low]: In the tts test, `_to16k` is called on `audio.samples` at line 172 — resamples the full clip. Then at line 174 a flush of 12800 zeros. Then decode. All fine.

What about line 71: `final n = (x.length * 16000 / fromRate).floor();` — `x.length * 16000` — if x.length is large (say 10M samples for a 7-min clip at 24k), x.length*16000 = 1.6e11 — fits in double/int (Dart int is 64-bit) fine. Not an issue.

What about `_matchRate` line 66: `return prev[b.length] / a.length;` — fine.

I'm settling on: both files clean, no findings. Coverage with 2 lines.

Actually — wait. Let me reconsider the instruction: "A request may append CONTEXT FILE sections... Do NOT review them." There are no context files here beyond the two listed. The "Runtime facts" mention indexes for cast_members, join_code_attempts, script_lines, script_scenes — none of these files touch the DB schema queries (screenshot_test uses an in-memory DB via providers, no raw queries visible). So no missing-index claims apply.

One more thought on screenshot_test: line 111 `container.read(productionsProvider.notifier).add(production)` — if `add` writes to the DB (in-memory drift) and also the test later reads... once. Fine.

And: the test at line 78 creates `AppDatabase.forTesting(NativeDatabase.memory())` — drift in-memory DB. Each `pumpAndSettle` after provider changes may re-run queries (e.g., productions list). With one production, trivial.

Final answer: no findings; coverage: both clean.

Hmm, but should I double-check the possibility that I'm expected to flag something in the tts test about the nested loop structure — 2×3×3 = 18 TTS generations each involving full model inference — with a 30-minute timeout. That's the test's design, bounded. No.

And in screenshot_test: `await tester.pumpAndSettle(const Duration(seconds: 2))` at lines 102, 114, 128, 147, 158 — pumpAndSettle with a 2-second step duration pumps frames spaced 2s apart until animations settle — actually the duration param is the time between frames; a larger duration means FEWER pumps for the same simulated time. Not a cost issue.

OK, final: both clean.

## Coverage
integration_test/screenshot_test.dart — clean
integration_test/tts_kokoro_compare_macos_test.dart — clean

Wait, the format says findings first, then Coverage. With no findings, the report is just the Coverage section. Let me also consider whether to include a Follow-up requests section — nothing needed, omit it.

Actually, let me reconsider ONE more time whether either file has anything I'd defend at [low]/[info]...

The strongest candidates:
1. tts `_matchRate` per-row list allocation (line 58) — allocation churn inside loop. Scale: 18 calls, ≤~30×30 cells. Below any threshold. Reject.
2. tts `_writeWav` full-buffer + sync write (lines 84, 107) — sync I/O on the runner's event loop. Scale: 18 writes, each ≤ a few hundred KB. Total < few MB. In a 30-min benchmark. Reject.
3. screenshot `_context` re-finding Navigator 11× — trivial. Reject.
4. screenshot double pumpAndSettle in `_snap` — trivial. Reject.

All rejected per "Reject micro-optimizations without a scale threshold" and "test-only code = low" (and even low needs a real consequence at scale, which none have).

Final report: Coverage only.
## Coverage
test/supabase_join_test.dart — clean
test/supabase_service_test.dart — clean
- [info] test/sync_queue_test.dart:343-403 — persistence-file test writes/reads a real temp JSON file and creates/deletes temp directories per test — under full-suite load the tearDown at 54-64 already documents straggler async persists re-creating files mid-delete, forcing a 200ms delay + second recursive delete — smallest safe fix: use an injectable in-memory persistence sink (or a per-test unique temp dir) instead of a shared system-temp directory with retry-on-collision teardown.
- [info] test/sync_queue_test.dart:66-70 — makeAudioFile writes a 256-byte file per call and tests call it up to 3 times each — trivial cost, only flagged because tearDown's recursive directory delete plus these writes is the sole real I/O in the suite — smallest safe fix: none needed at this scale; keep files tiny or reuse one fixture file.
- [info] test/toast_autodismiss_test.dart:11-57 — two testWidgets each pump a full MaterialApp/Scaffold tree and pumpAndSettle — widget-test overhead only, no production hot path — smallest safe fix: none; this is the minimal harness for the a11y-dismiss behavior under test.

## Coverage
test/sync_queue_test.dart — findings: 2
test/toast_autodismiss_test.dart — clean
## Coverage
test/tts_service_test.dart — clean
test/tts_text_chunking_test.dart — clean
- [info] test/voice_config_test.dart:101-106 — test asserts round-robin uniqueness against a locally generated list rather than a production round-robin helper — the test cannot fail on a production regression and hides a broken assignment path — assert against the production voice-assignment helper (or construct via the preset's real selection API) so the expectation can fail
## Coverage
test/voice_clone_test.dart — clean
test/voice_config_test.dart — findings: 1
- [info] test/wav_silence_test.dart:43 — `List.generate(dataLen, ...)` builds the full WAV data payload in memory per test invocation — test-only code, bounded by `frames` (≤1000 in tests, ~251 KB worst case), no production path — none (test fixture, allocation is bounded and freed per test) — none needed.
- [info] test/wav_silence_test.dart:112,117 — `Uint8List.fromList(List.filled(...))` allocates small fixtures per test — test-only, ≤200 bytes — none — none needed.

## Coverage
test/wav_silence_test.dart — clean
test/welcome_screen_test.dart — clean
- [info] test/widget_test.dart:34-345 — test-only file; no production hot path is exercised; the parser/exporter/validator calls run on a ~32-line sample script, so any allocation or scan cost is bounded by fixture size and cannot grow — no observable production consequence — none needed (test-only, no fix).
## Coverage
test/widget_test.dart — clean
- [medium] lib/features/script_import/pdf_page_view.dart:141-147 — `_cachePut` runs an O(n) scan-and-evict loop over the whole `_pageCache` map on every render/flip, allocating a `toList()` of evictable keys each time just to keep ≤4 entries — per-page-flip allocation churn and O(cache) work for a bounded cache that could be a plain `LinkedHashMap` remove-first — replace with: evict the first key when `length > _maxCachedPages` (insertion-order map guarantees first key is LRU), no intermediate list.
- [medium] lib/features/script_import/pdf_page_view.dart:109-120 — `dispose` copies the entire `_pageCache` into a new list (`values.toList()`) and defers every image's `dispose()` into a post-frame callback that loops over all of them synchronously on the UI thread — with a 4-page cache of multi-MB RGBA images this is a burst of native frees landing in one frame, janking the teardown frame — dispose each image immediately (ownership is already transferred by clearing the map) or dispose them one per frame.
- [low] lib/features/script_import/pdf_page_view.dart:261-262 — `_locateHighlight` re-runs `PaddleOcrChannel.ocrPage` per page flip whenever `_pageOcrCache` misses, and the cache itself is unbounded in page count (grows with every distinct page visited for the session) — repeated OCR round-trips (~seconds each) on every revisit of a page not yet cached, and memory grows with pages browsed — pre-warm/limit the OCR cache alongside `_maxCachedPages` (evict with the page-image cache) so flips back reuse results without unbounded retention.
- [low] lib/features/script_import/pdf_page_view.dart:283-295 — `_locateHighlight` calls `OcrHighlightMatcher.locate` and rebuilds the highlight `setState` on every `_renderPage` completion including pure cache-hit flips (line 164), re-matching the target text against all OCR lines each flip — O(lines) string matching per flip even when nothing changed — memoize per (page, target) pair and skip when rects are already computed for that page.
- [low] lib/features/script_import/script_import_screen.dart:182-201 — `_countsFor` walks all `script.lines` (O(n)) on every `_buildPreview`/`_buildReviewBanner` invocation; the memo key is list identity so any `setState` that rebuilds `_preview`'s lines (e.g. after `_openReview`'s `ParsedScript` rebuild at 438-451) pays a fresh full scan per rebuild — fine at hundreds of lines, but the scan re-runs on every rebuild of the preview screen — cache counts keyed on the `ParsedScript` instance (like `acts`' Expando in script_models.dart:406) instead of line-list identity.
- [low] lib/features/script_import/script_import_screen.dart:238-254 — the characters `ListView` builds all `ListTile`s eagerly inside a non-lazy `ListView(children: ...)` column (plus `...script.lines.take(30).map(...)` at 262) — every rebuild of the preview screen re-allocates the full character list widgets even though only a screenful is visible — use `ListView.builder` with `itemBuilder`/`itemCount` for the character section so off-screen tiles aren't constructed per rebuild.
- [low] lib/features/script_import/script_import_screen.dart:356-359 — `SegmentedButton` segments are rebuilt via `.map(...).toList()` on every `_buildDialectSelector` build (each rebuild allocates fresh `ButtonSegment` widgets for a fixed 2-entry map) — trivial allocation churn on a hot rebuild path — hoist the segments list to a `static const`/top-level final since `_localeLabels` is already `const`.
- [info] lib/features/script_import/pdf_page_view.dart:176-179 — `Pdfrx.getCacheDirectory ??=` assigns a closure that re-awaits `getTemporaryDirectory()` on every cache-directory fetch; if pdfrx invokes the getter per render this re-runs the platform-channel round trip each page render — verify pdfrx's call frequency; if per-render, memoize the resolved path once.

## Coverage
lib/features/script_import/pdf_page_view.dart — findings: 5
lib/features/script_import/script_import_screen.dart — findings: 3
- [medium] lib/features/settings/debug_log_screen.dart:56 — build() calls `_log.entriesForCategory(_filter)` on every rebuild; the service getter returns `List.unmodifiable(_entries)` (debug_log_service.dart:100,229) so each rebuild allocates a fresh defensive copy of up to 500 entries, and the timer at :32-41 fires setState every 2s whenever any entry arrives, so a busy log re-copies the whole ring buffer twice per second — smallest safe fix: cache the filtered list in State and rebuild it only when `_lastEntryCount`/filter changes, or have the service expose the same unmodifiable instance.
- [medium] lib/features/settings/debug_log_screen.dart:68,104,114 — each of the three action handlers (send/share/copy) re-runs `entriesForCategory(_filter)` and `entries.map((e) => e.toLine()).join('\n')` over the full ring buffer (≤500 entries) on tap; `toLine()` string-interpolates every entry and `join` builds a large string on the UI isolate — smallest safe fix: compute the exported text once per filter change and reuse it across the three handlers.
- [low] lib/features/settings/debug_log_screen.dart:216 — `itemBuilder` computes `entries[entries.length - 1 - index]` per visible row; with the list rebuilt every 2s while entries stream in, each rebuild re-derives the reversed index for every cached row — smallest safe fix: pass the reversed list (or a `ReversedIterable` sliver) so itemBuilder indexes directly.
- [low] lib/features/settings/ai_models_screen.dart:246 — `_buildLiveAsrTile` builds a fresh `Map<AiModel, ModelDownloadState>` and folds over it on every build of the screen; the tile is rebuilt whenever the download service notifies (progress callbacks fire frequently during a ~68MB download), so per-notification O(n) map/list churn over the 4-model group — smallest safe fix: memoize the states map keyed by model id and recompute only on service notification with changed statuses.
- [low] lib/features/settings/ai_models_screen.dart:263-268 — progress is recomputed by folding `sizeBytes` over all models on every rebuild during download; with per-byte progress callbacks this runs O(models) per tick — smallest safe fix: cache totalBytes (constant per model set) and accumulate progress incrementally from the service's progress events.

## Coverage
lib/features/settings/ai_models_screen.dart — findings: 2
lib/features/settings/debug_log_screen.dart — findings: 3
- [medium] lib/features/settings/kokoro_debug_screen.dart:56-67 — directory listing runs sequential per-entity async I/O (`exists`, `length`) inside `await for` over `kokoroDir.list`, each `await` a separate event-loop turn — with ~10 model files (kokoro_mlx ships ~10 files incl. 341 MB weights) this is ~20 sequential async round-trips on the UI isolate per refresh; the refresh button (line 144) re-runs it on every tap — consequence: debug screen shows a spinner for tens of ms per file and janks the frame while the user taps refresh repeatedly; smallest safe fix: gather entities first (`final entities = await kokoroDir.list().toList()`), then `Future.wait` the `length()`/`exists()` calls, or compute sizes in one `dart:io` sync pass inside `compute()`/`Isolate.run`.
- [low] lib/features/settings/kokoro_debug_screen.dart:90 — `_log` prepends to `_statusLog` on every call, so the string grows O(n²) in total characters as the session's log lines accumulate (each new line re-copies the whole previous log) and the monospace `Text` at 340-349 rebuilds with the full string on every `setState` — consequence: after a long debug session (hundreds of `_log` calls from init/reload/speak/stop), each log call and each rebuild copies/renders a multi-KB string on the UI thread; smallest safe fix: keep a bounded `List<String>` (e.g. last 100 lines) and render `Text(lines.join('\n'))` only in the log card, or cap the accumulated string length.
- [low] lib/features/settings/kokoro_debug_screen.dart:163-194 — `_debugInfo.entries` map is rendered with per-entry `Padding`+`Row` widgets inside a non-const `Column` that rebuilds wholesale on every `setState` (slider drags at 265-266 fire `setState` per tick) — consequence: dragging the speed slider rebuilds and re-lays-out the entire ListView including the debug-info card and log card each frame; smallest safe fix: extract the status card, log card, and speak controls into separate `const`-friendly child widgets / `ValueListenableBuilder`-scoped rebuilds so slider ticks only rebuild the slider row.
- [low] lib/features/settings/model_download_screen.dart:41-44,63-68 — `_onDownloadUpdate` calls `setState(() {})` on every download-service notification and the `onProgress` callback calls `setState` per progress event per model — consequence: during a ~341 MB multi-model download, progress events arrive many times per second and each one rebuilds the whole screen (ListView with all cards) on the UI thread, causing jank and wasted rebuilds; smallest safe fix: throttle/coalesce progress updates (e.g. only setState when the integer percent changes, or use a `ValueNotifier`/`ValueListenableBuilder` scoped to the progress rows).
- [low] lib/features/settings/model_download_screen.dart:24,137-164 — `_modelProgress` map is rendered as a `for` over `entries` building a `Row`+`LinearProgressIndicator` per model inside the same wholesale-rebuilt ListView — consequence: with N models downloading, every progress tick rebuilds N rows' widgets and re-animates N progress indicators; smallest safe fix: give each model row its own `StatefulWidget`/`ValueListenableBuilder` keyed by model name so only the changed row rebuilds.

## Coverage
lib/features/settings/kokoro_debug_screen.dart — findings: 3
lib/features/settings/model_download_screen.dart — findings: 2
- [low] lib/features/settings/settings_screen.dart:308-317 — FutureBuilder's future is `_getVersionString()`, which is memoized via `_versionFuture ??=` (line 69), so the future is stable across rebuilds and the async work runs once per app run; no per-rebuild PackageInfo round-trip — consequence is negligible — no fix needed (memoization already present).
- [info] lib/features/settings/settings_screen.dart:292-297 — Share.share builds the share text from `SupabaseService.instance.currentUser?.email` at tap time; no per-build work, no loop, no I/O — clean.
- [info] lib/features/settings/settings_screen.dart:324-359 — `_signOut` awaits a SharedPreferences remove and a Supabase signOut only on user tap; single-shot, not a hot path — clean.

## Coverage
lib/features/settings/settings_screen.dart — clean
- [medium] tool/orphan_sweep.dart:37-105 — per-production sequential HTTP round-trips with no pagination or concurrency cap — each production triggers 4+ sequential Supabase REST calls (insert membership, read script_lines, read recordings, delete membership), so a sweep over N productions costs O(N) network round-trips serialized on one client; at hundreds of productions the sweep takes minutes and any single slow/hung request stalls the whole run — smallest safe fix: batch the reads (or fetch all productions' lines/recordings in one paginated query each) and issue the per-production membership insert/delete concurrently with a bounded `Future.wait`/worker pool.
- [medium] tool/orphan_sweep.dart:56-66 — full-table reads of `script_lines` and `recordings` per production with no `.limit()`/pagination — every production pulls its entire lines and recordings result sets into memory as `List<Map<String,dynamic>>`; with large productions (thousands of lines/recordings) this is unbounded allocation per iteration and grows with data size, not with a fixed cap — smallest safe fix: add `.limit(...)`/keyset pagination per query and stream/aggregate counts server-side (e.g. a `count`/RPC) instead of materializing rows.
- [low] tool/orphan_sweep.dart:61,68 — `lineIds` built as a `Set` then `contains` per recording is fine, but `orphans`/`matched` are recomputed via two full `where().toList()` passes over `recs` (lines 67-68, 70-71) — redundant O(N) scans and extra list allocations per production; at thousands of recordings per production this doubles per-production work — smallest safe fix: single pass partitioning into matched/orphans lists (or compute counts only, since only counts are printed).
- [low] tool/orphan_sweep.dart:74-83 — per-orphan `Uri.decodeFull` + `RegExp.firstMatch` + string concatenation inside the grouping loop — regex match and URI decode run per orphan row; with many orphans per production this is repeated CPU work on the event loop for a report that only needs grouped counts — smallest safe fix: precompute the character once per distinct audio_url (or cache decode results in a map) before the loop.
- [low] tool/analyze_orphaned_recordings.dart:63-80 — full unbounded reads of `script_lines` and `recordings` for the production with no `.limit()`/pagination — entire result sets are materialized into `List<Map<String,dynamic>>` (plus `lineIds` set and two `where().toList()` partitions at 79-80); for a production with thousands of lines/recordings this is unbounded per-run allocation and O(N) extra scans — smallest safe fix: add `.limit()`/pagination and compute matched/orphan counts in one pass without materializing both partition lists.
- [low] tool/analyze_orphaned_recordings.dart:93-98,104-108 — per-row `charOf` (RegExp + `Uri.decodeFull`) and string concatenation inside both print/grouping loops — regex compile is hoisted (line 85) but `firstMatch`+decode runs per recording row twice (orphans loop and counts loop); with many recordings this is repeated CPU work on the event loop for report formatting — smallest safe fix: memoize `charOf` per distinct audio_url (map cache) before the loops.
- [info] tool/analyze_orphaned_recordings.dart:19-20 and tool/orphan_sweep.dart:17-18 — hardcoded publishable anon key and project URL in source — verify it is not committed (these are anon/publishable keys by design, but confirm rotation/commit status; do not quote values).
- [info] tool/analyze_orphaned_recordings.dart:130-140 and tool/orphan_sweep.dart:115-125 — `_auth` creates a fresh throwaway account per run (signup then token grant) with no cleanup/teardown of the auth user — each sweep/audit run leaves an orphaned auth user row behind; repeated runs accumulate junk rows in `auth.users` (unbounded growth of the users table across runs) — smallest safe fix: delete the created auth user (admin API) in a `finally` block after the sweep completes.

## Coverage
tool/analyze_orphaned_recordings.dart — findings: 4
tool/orphan_sweep.dart — findings: 5
- [low] tool/parse_stats.dart:43 — `readAsStringSync` on the main entry path buffers the whole script text before parsing — for a large scanned-script OCR dump (hundreds of KB to MB) the synchronous read blocks the event loop during startup and doubles peak memory (raw string + parsed structures) — this is a one-shot CLI harness, not a serving path, so impact is bounded to tool startup latency — smallest safe fix: none needed (CLI-only, single invocation); if desired, switch to `readAsLines`/streamed read, but that is a micro-optimization at this scale.
- [low] tool/parse_stats.dart:67 — `expected.where((e) => matches(n, e)).toList()` runs an O(expected × roster) substring scan per character inside the roster loop — with a large expected set (dozens of names) and a large parsed roster (hundreds of characters) this is O(n·m) string containment work per run — CLI-only, single invocation, so consequence is only harness wall-time on pathological inputs — smallest safe fix: precompute expected names into a lowercase/uppercase set and short-circuit exact-equality hits before the containment scan.
- [info] tool/sim_multi_user.dart:29-30 — hardcoded default Supabase URL and publishable anon key in source — verify it is not committed if this is considered sensitive (it is a publishable client key by name, but still a repo-tracked credential-shaped constant); no performance impact.
- [low] tool/sim_multi_user.dart:419 — `_fakeAudio` allocates a fresh 2048-byte `List` and wraps it in `Uint8List` on every call (three call sites: lines 113, 241, 275) — trivial allocation churn, one-shot CLI, no measurable consequence — smallest safe fix: hoist to a top-level cached constant if repeated at larger sizes.
- [low] tool/sim_multi_user.dart:395-399 — `_uuid` builds each hex segment via `List.generate(...).join()`, allocating a list + string per hex char (5 segments × up to 12 chars) — called only twice per run (lines 88, 111, 193, 240 use it four times total) — negligible; consequence is only microsecond-level overhead — smallest safe fix: use a single `StringBuffer` or a fixed random hex helper.
- [low] tool/sim_multi_user.dart:412-415 — `_joinCode` allocates a `List` + `join` per call, called once per run — negligible — smallest safe fix: none needed.
- [low] tool/sim_multi_user.dart:375-393 — `_authPost` constructs a new `HttpClient` per auth request (two auth calls per run, plus a retry path) — matches the "HttpClient per request" class, but with only 2–4 requests in the whole process the consequence is a few extra TCP handshakes, not a hot-path cost — smallest safe fix: reuse one `HttpClient` across the two `_auth` calls.
- [low] tool/sim_multi_user.dart:385 — `resp.transform(utf8.decoder).join()` buffers the whole auth response body into one string — response is a small JSON token payload (KBs), one-shot CLI — no observable consequence at this scale — smallest safe fix: none needed.
- [info] tool/sim_multi_user.dart:358-372 — `_auth` sends the plaintext password in a JSON body to a URL that may be overridden via `SUPABASE_URL` env — not a performance issue; noted only because the harness prints failure details (line 368-369) that could echo response bodies — no perf consequence.
- [low] tool/sim_multi_user.dart:319-321 — cleanup builds `files` via `.map(...).toList()` then conditionally `.add` — allocation churn is trivial (a handful of storage objects) and runs once per process — smallest safe fix: none needed.
- [low] tool/sim_multi_user.dart:83-84 — early `return` inside `try` when A and B are the same account skips the `finally`-less cleanup path? No — the `finally` block at 315-341 does run on `return`, so cleanup still executes; no defect — no finding beyond this note (not reported as a finding).

Re-examining: the `return` at line 84 is inside the `try` whose `finally` is at 315 — cleanup runs, so no issue. Dropping that line.

Also re-checking `parse_stats.dart` line 48-49: `roster` is built once with spread + sort — fine. Line 83 builds a map literal per JSON encode — one-shot, fine.

`sim_multi_user.dart` line 130: `(lookup is Map && ...)` — single call. Line 291: `firstWhere` on a small list, once. Line 178: `.any` once. All one-shot CLI work — below scale threshold.

## Coverage
tool/parse_stats.dart — findings: 2
tool/sim_multi_user.dart — findings: 5
- [low] tool/verify_cloud_recordings.dart:117,130 — new HttpClient per request in `_post` — each auth attempt (signup then token retry) constructs and tears down a fresh client, adding TCP/TLS handshake latency to an operator-run verification script — hoist a single HttpClient into a top-level field and reuse it across `_post` calls.
- [info] tool/verify_cloud_recordings.dart:16,15 — publishable anon key and project URL hardcoded in source — verify this constant is not committed (it is an anon/publishable key by name, but confirm rotation policy); reference file:line only, do not quote.
- [low] tool/verify_cloud_recordings.dart:65,77 — `createSync`/`writeAsBytesSync` on the main isolate — operator CLI tool, not a serving path; blocking is bounded by ≤3 recordings downloaded fully into memory (`bytes` buffered whole at line 75) before sync write — acceptable for a throwaway verification script; no fix needed unless reused as a library.

## Coverage
tool/verify_cloud_recordings.dart — findings: 3
- [medium] lib/app.dart:258-275 — `ref.listen(currentProductionProvider, ...)` inside `build` re-registers a listener on every rebuild and its callback performs async work (`loadForProduction`, `launchRecordingSync`) with no guard against duplicate firing — every rebuild of the app root (theme change, provider churn, any state update that rebuilds `_CastCircleAppState`) re-runs the production-change branch when `next != null && next.id != prev?.id`, and each firing re-launches a full recordings load + sync — duplicated disk/DB loads and a second concurrent sync loop on every rebuild — hoist the listener into `initState`/`ref.listenManual` (or a `ref.listen` in a `ConsumerState` lifecycle hook) so it registers once, and guard with a `lastHandledProductionId` field.
- [low] lib/app.dart:264-274 — `unawaited(() async { ... }())` allocates a fresh closure and `Future` per production-change event and swallows errors only via the inner try/catch — negligible per-event cost but the closure allocation churn recurs on every rebuild that passes the `next.id != prev?.id` check (see previous finding) — after hoisting the listener to register once, the closure runs once per real production switch, making the churn bounded.
- [info] lib/firebase_options.dart:53,62 — Firebase API keys are embedded in the generated options file — these are client-identifiers, not secrets, but verify it is not committed if the repo policy treats generated config as sensitive; no performance impact.
- [low] lib/app.dart:337 — `_screenNames[path] ?? path` map lookup per route event is fine, but `_logScreen` is called from `didPush`/`didPop`/`didReplace` for every navigation including dialogs that are `PageRoute`s — at high navigation frequency (e.g. rapid rehearsal line stepping) each event does a Firebase analytics call (`logScreenView`) with no batching/debounce — consequence is per-navigation network round-trips to Firebase Analytics; acceptable at human navigation rates, flag only as info-level awareness — actually below threshold, no fix needed.

## Coverage
lib/app.dart — findings: 2
lib/firebase_options.dart — findings: 0
- [medium] lib/main.dart:140-144 — sequential awaited downloads in a loop — each `await modelService.download(model)` serializes network+disk work; with N models the wall time is N×(download+install) and a failure mid-loop leaves earlier models installed but later ones missing with no retry — smallest safe fix: `Future.wait` on the filtered list (or a bounded concurrency helper) so downloads run in parallel and failures are collected.
- [low] lib/main.dart:134-137 — `SharedPreferences.getInstance()` plus `refreshDownloadedStatus()` re-run inside the microtask on every cold start even when consent is false — two async round-trips (prefs read + model status scan) executed before the consent check can short-circuit them — smallest safe fix: read the consent flag first and only call `refreshDownloadedStatus()`/`isKokoroReady()` when `consented` is true.
- [info] lib/main.dart:94-97 — `String.fromEnvironment` defaults embed a Supabase publishable key and project URL in the binary — verify it is not committed (anon/publishable keys are public-by-design, but confirm the default is intended and not a leaked secret).

## Coverage
lib/main.dart — findings: 3
## Coverage
lib/core/constants.dart — clean
lib/core/theme/app_theme.dart — clean
- [info] lib/data/services/voice_clone_service.dart:95 — `await File(path).exists()` inside a loop in `buildProfileFromRecordings` — per-item async filesystem round-trip; with N recording paths this serializes N awaits on the UI isolate, growing with unbounded caller-supplied `recordingPaths` (no cap visible) — consequence: profile build latency grows linearly with clip count and blocks the event loop between awaits at large N — smallest safe fix: batch existence checks with `Future.wait` over `recordingPaths.map(File.new).exists()` or check the directory listing once instead of per-path probes.
- [info] lib/data/services/voice_clone_service.dart:149-150 — `cacheDir.createSync(recursive: true)` and `existsSync` on `_cachePath` — sync filesystem ops on a path that `generateLine` resolves per call (line 122) — consequence: each cache lookup pays a synchronous directory stat/create on the main isolate; at high per-line generation frequency this stalls frame rendering — smallest safe fix: create the directory once at service init (or lazily with `create` async) and have `_cachePath` only join paths.
- [info] lib/data/services/voice_clone_service.dart:138-139 — `clearCache` uses `existsSync` + `delete(recursive: true)` — recursive directory delete runs synchronously on the serving isolate for an entire production's cache — consequence: clearing a large cache (many generated lines) blocks the UI thread for the duration of the walk/delete — smallest safe fix: use `delete` via `await` on an async `Directory` walk or offload to `Isolate.run`/`to_thread` equivalent.
- [info] lib/features/recording_studio/voice_profile_screen.dart:11-36 — placeholder screen rebuilds whole `Scaffold` subtree on every parent rebuild with no `const` on the `Column`/`Text`/`Icon` subtree — consequence: negligible today (static placeholder, no state), only matters if this screen is embedded in a hot rebuild path — smallest safe fix: mark the body subtree `const` (the `Column` children list and `TextStyle`/`EdgeInsets` literals qualify). No scale threshold met; reported at info only because the widget is a placeholder.

## Coverage
lib/data/services/voice_clone_service.dart — findings: 3
lib/features/recording_studio/voice_profile_screen.dart — findings: 1
- [low] test_driver/screenshot_driver.dart:15-16 — `*Sync` filesystem ops (`existsSync`/`createSync`/`writeAsBytesSync`) run on the driver's event loop for every screenshot callback — trigger frequency is one call per screenshot taken by scripts/generate-play-screenshots.sh (bounded by the test suite's screenshot count, not unbounded), work per unit is a full PNG buffer write (screenshots are typically hundreds of KB to a few MB), affected resource is the host event loop during a screenshot run — consequence is that large screenshot writes block the driver loop between callbacks, adding latency to each screenshot step and, at multi-MB sizes, noticeable stalls; smallest safe fix is to switch to async I/O (`dir.exists()`/`dir.create(recursive: true)` awaited once outside the callback, `await file.writeAsBytes(bytes)`) or pre-create the directory once at startup.
## Coverage
test_driver/integration_test.dart — clean
test_driver/screenshot_driver.dart — findings: 1

## Run stats

input 1107229 tok (+120532 cached), output 52672 tok — sync requests, discounted — 145 files in 82m (105.1 files/h, 1.1 min/batch)
