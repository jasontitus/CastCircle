# Pi sweep review — CastCircle

Exhaustive per-file pass: 29 code files across 16 batches — model ds4/GLM-5.3-Flash-Q2:off — 2026-08-30.

## Findings

- [medium] supabase/migrations/20260314061409_initial_schema.sql:147-149 — storage bucket inserted via raw `insert into storage.buckets` inside a schema migration — reruns of the migration (or a failed-then-retried migration) hit a duplicate-key error because the statement is not idempotent despite the `on conflict (id) do nothing` guard depending on the bucket row's `id` colliding with an existing row from a prior run; consequence is migration failure on retry in environments where the bucket already exists with a different id or where the conflict target doesn't match the actual unique key — smallest safe fix: keep the bucket creation in a dedicated idempotent step or use `on conflict (name) do nothing` matching the real unique constraint, and verify rerun behavior before shipping.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:151-163 — storage policies reference `bucket_id = 'recordings'` but the bucket row is created in the same migration with no guarantee of ordering or existence in environments where the bucket was created out-of-band — policy creation succeeds regardless (policies don't validate bucket existence), so a typo'd or missing bucket id silently yields policies that never match any object — smallest safe fix: assert bucket existence in the same transaction or create the bucket before policies with a hard failure on missing row.
- [low] supabase/migrations/20260314061409_initial_schema.sql:24-31 — `handle_new_user` trigger function inserts profile with only `id` and `display_name`, no exception guard for duplicate profile id — a re-triggered signup or pre-existing profile row causes the signup to fail with a unique violation surfaced to the end user — smallest safe fix: add `on conflict (id) do nothing` or catch duplicate in the function body.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:59-67 — `cast_members` unique `(production_id, user_id)` added directly in the initial table creation is fine for a fresh table, but the same migration also creates the RLS policy at 71-79 whose `exists` subquery self-references `cast_members` before the table's own policy set is complete — on fresh databases this is harmless, but on databases where the migration is applied to an existing deployment with rows the recursive policy can deny reads to pre-existing cast rows whose organizer differs — smallest safe fix: confirm this migration only ever runs on empty databases or split policy creation from table creation.
- [high] supabase/migrations/20260314061409_initial_schema.sql:129-135 — recordings insert/update policies check only `auth.uid() = user_id` with no production-membership check — any authenticated user can insert a recording row claiming any `production_id` (including productions they are not a cast member of), and can update their own rows regardless of production; consequence is cross-tenant data pollution and unauthenticated-style write access to other organizers' productions by any signed-in user — smallest safe fix: add an `exists` subquery on `cast_members`/`productions` to both policies, matching the read policy at 119-127.
- [high] supabase/migrations/20260314061409_initial_schema.sql:151-156 — storage insert policy checks only `bucket_id = 'recordings'` and `auth.role() = 'authenticated'` with no ownership or cast-membership predicate — any authenticated user can upload arbitrary objects into the recordings bucket (path names unconstrained), enabling storage squatting, quota exhaustion, and serving of attacker-controlled audio under other productions' paths — smallest safe fix: add a `storage.foldername(name)`-based cast-membership or user-folder predicate to the insert policy.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:158-163 — storage select policy grants read of every object in the recordings bucket to every authenticated user (`bucket_id = 'recordings' and auth.role() = 'authenticated'`) — consequence is any signed-in user can enumerate/download all recordings across all productions, a cross-tenant disclosure — smallest safe fix: scope the select policy to the user's own folder or to productions they are cast in.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:165-170 — storage update policy uses `auth.uid()::text = (storage.foldername(name))[1]` — folder layout is assumed but never established by this migration (no path convention is enforced on insert), so updates succeed only for objects whose first path segment equals the user id; objects uploaded under other layouts become un-updatable by their owner while remaining readable by everyone — smallest safe fix: define and enforce the path convention in the insert policy and align the update policy with it.
- [medium] supabase/migrations/20260314120000_add_script_lines.sql:5-18 — `script_lines` has no `created_at` and `updated_at` defaults to `now()` with no trigger to bump it on update — clients relying on `updated_at` for sync/last-write-wins conflict resolution will see stale timestamps after updates, silently causing lost updates on merge — smallest safe fix: add a `moddatetime` trigger or set `updated_at` in the update policy/application layer.
- [medium] supabase/migrations/20260314120000_add_script_lines.sql:23-31 — "Organizer can manage script lines" policy is `for all` with `using` only and no separate `with check` — for `insert` and `update` the `using` clause is not applied as a write check in all PostgREST/gotrue versions, so rows can be inserted/updated whose `production_id` does not satisfy the organizer predicate when the row is new (insert has no existing row to test) — smallest safe fix: add explicit `with check` mirroring the `using` predicate.
- [low] supabase/migrations/20260314120000_add_script_lines.sql:45 — index `idx_script_lines_production` on `(production_id, order_index)` duplicates the unique constraint index created by `unique (production_id, order_index)` at line 17 — consequence is a redundant index doubling write cost and storage on every script_lines row — smallest safe fix: drop the explicit index or rely on the constraint's index for queries ordered by `(production_id, order_index)`.
## Coverage
supabase/migrations/20260314061409_initial_schema.sql — findings: 8
supabase/migrations/20260314120000_add_script_lines.sql — findings: 3
- [high] supabase/migrations/20260314130000_fix_cast_members_rls.sql:5-11 — migration drops the recursive policy then immediately recreates policies that still reference the same table pair, and the replacement policy "Users can read own cast membership" (auth.uid() = user_id) plus the new productions policy (lines 26-35) reintroduce the exact productions↔cast_members mutual recursion the file claims to fix — any query touching both tables re-enters RLS and Postgres errors "infinite recursion detected" (or silently denies rows), breaking member reads at runtime — replace the cross-referencing USING clauses with SECURITY DEFINER helper functions (as the sibling migration does) instead of re-creating policies that query each other's tables.
- [high] supabase/migrations/20260314130000_fix_cast_members_rls.sql:37-39 — the trailing comment-only block leaves the pre-existing "Organizer can do anything" ALL policy on public.productions in place while the file's stated intent is to drop it, and the sibling migration (20260314140000:29) does drop it — if this migration runs after that one the ALL policy is already gone (fine), but if it runs first/alone the over-broad ALL policy (using true for organizer_id via the old definition) remains and supersedes the tightened SELECT policy, so the fix is a no-op for organizer rows — make the drop explicit (drop policy if exists "Organizer can do anything" on public.productions) rather than describing it in a comment.
- [medium] supabase/migrations/20260314130000_fix_cast_members_rls.sql:5,24 — drop policy if exists without create-if-missing guard makes the migration non-idempotent in the failure path: if the subsequent CREATE fails (e.g. recursion error from the policies above), a rerun drops the old policy again but the table is left with no policy and the transaction (Supabase runs migrations in one transaction) aborts, so reruns cannot recover and the table can be left policy-less in any non-transactional runner — wrap drops/creates so reruns converge, or use a DO block that no-ops when the new policy already exists.
- [medium] supabase/migrations/20260314140000_fix_rls_recursion.sql:7-14,17-24 — the SECURITY DEFINER functions is_production_member/is_production_organizer are created with no explicit search_path setting and no REVOKE; under Supabase's default function-acl security model a caller can change the search_path/GUC of the definer function's session to redirect the unqualified table references (cast_members, productions) to a different schema or a malicious function, and because the function runs as owner with RLS bypassed this can read or assert membership across tenants — add SET search_path = public (or qualify public.cast_members/public.productions inside the body) and consider security definer + strict search_path per Supabase hardening guidance.
- [medium] supabase/migrations/20260314140000_fix_rls_recursion.sql:33-35,59-61 — "Organizer full access" on productions is created FOR ALL with USING (auth.uid() = organizer_id) but no WITH CHECK clause; for ALL policies Postgres uses the USING expression as the WITH CHECK when omitted, which is correct here, however the cast_members ALL policy at 59-61 uses a helper function in USING with no WITH CHECK either — same implicit reuse applies, so this is only a latent risk if a future edit splits USING/WITH CHECK; no separate finding needed beyond noting the pattern is currently safe.
- [medium] supabase/migrations/20260314140000_fix_rls_recursion.sql:38-40,49-51,54-56,67-69,76-78,80-82 — every member-facing policy calls the SECURITY DEFINER helper with auth.uid() as an argument; because the helper is marked STABLE and runs as owner, its result is cached per statement, but the policy expression itself is evaluated per row — on large cast_members/recordings/script_lines tables this is an O(rows) subquery per row unless the planner can hash it, and the helper does not filter on the current row's production_id in the organizer variant at 54-56/59-61 (it passes production_id from the row, which is correct) — no defect exhibited; performance concern only if tables grow large, so no finding.
- [high] supabase/migrations/20260314140000_fix_rls_recursion.sql:29-30 — dropping "Organizer can do anything" ALL policy and "Cast members can read their productions" on productions, then recreating only "Organizer full access" (ALL) and "Members can read productions" (SELECT) removes the old member-read path; but the new member policy at 38-40 depends on is_production_member which queries cast_members, and cast_members' own policies (49-61) depend on is_production_organizer which queries productions — the helper functions bypass RLS (SECURITY DEFINER) so there is no recursion, however the migration never drops the pre-existing recursive policies on cast_members created by the sibling migration 20260314130000 ("Users can read own cast membership", "Organizers can read all cast for their productions", "Cast members can read their productions" on productions); those policies still reference each other's tables directly and will still trigger infinite-recursion errors even after this migration — add drops for the sibling migration's policy names (or fold both migrations into one) so the recursive policies are actually removed.
- [medium] supabase/migrations/20260314140000_fix_rls_recursion.sql:7,17 — create or replace function without a preceding drop and without checking ownership: if an earlier version of these functions exists with a different signature or SECURITY INVOKER, create or replace silently swaps the body but keeps the old owner/SECURITY attr only when identical clause order is used; since the new definition includes security definer, replace works, but if the existing function was created by a different role the owner mismatch can break the definer assumption — low likelihood; no finding beyond info-level note.
- [low] supabase/migrations/20260314140000_fix_rls_recursion.sql:14,24 — functions declared STABLE but they read tables whose contents change during the statement (cast_members inserts by the same user); STABLE is correct for policy use, no defect.
- [info] supabase/migrations/20260314130000_fix_cast_members_rls.sql:39 — the file ends with a comment describing intended behavior ("actually keep it — ALL covers insert/update/delete too") that contradicts the sibling migration which drops that policy; verify which migration is authoritative and that the intended final policy set is committed — verify it is not committed as conflicting duplicate migration content.
## Coverage
supabase/migrations/20260314130000_fix_cast_members_rls.sql — findings: 4
supabase/migrations/20260314140000_fix_rls_recursion.sql — findings: 2
- [high] supabase/migrations/20260315_cast_join_code.sql:29-31 — SELECT policy on productions grants every authenticated user read access to ALL productions (USING clause ignores join_code entirely), not just lookup-by-code — any authenticated user can enumerate and read every production row (title, cast, join codes) — restrict USING to a code-based predicate (e.g. `USING (auth.role() = 'authenticated' AND join_code IS NOT NULL)` scoped to the lookup path) or a dedicated `is_join_code_holder` check so only code-bearing lookups succeed.
- [high] supabase/migrations/20260315_cast_join_code.sql:34-37 — claim-invitation UPDATE policy's WITH CHECK (`auth.uid() = user_id`) is evaluated against the row being written, but the USING clause (`user_id IS NULL`) plus a missing `WITH CHECK` on the new user_id value lets a user re-target any unclaimed row to themselves only if they can already see it; combined with the blanket productions SELECT policy at :29 this is reachable, but the deeper defect is that the policy never verifies the user was actually invited (no comparison against an invitation token/row) — any authenticated user who can SELECT the cast_members row can claim any unclaimed invitation for any production — add an invitation-token or producer-issued claim proof to USING/WITH CHECK.
- [medium] supabase/migrations/20260315_cast_join_code.sql:40-42 — self-join INSERT policy checks only `auth.uid() = user_id` with no cap on row count or production validity — an authenticated user can spam cast_member rows for arbitrary production_ids (subject to whatever SELECT policy exists on productions, assumed permissive per :29) — add a WITH CHECK that the production exists and is joinable, and a uniqueness guard (the dropped unique constraint at :20 is not replaced).
- [medium] supabase/migrations/20260315_cast_join_code.sql:20 — the unique constraint `cast_members_production_id_user_id_key` is dropped but no replacement unique index on `(production_id, user_id)` (treating NULLs as distinct) is created — concurrent self-joins or claim races can insert duplicate memberships, corrupting `is_production_member` semantics at :45-52 — add `CREATE UNIQUE INDEX ... ON cast_members (production_id, user_id) WHERE user_id IS NOT NULL` (or equivalent) in this migration.
- [medium] supabase/migrations/20260315_cast_join_code.sql:9-11 — unbatched backfill UPDATE over the whole productions table inside the same migration as the NOT NULL step at :14 — on a large table this is a long-running rewrite/lock window and blocks concurrent writes; also not rerun-safe if the migration is retried mid-way (rows backfilled before a crash won't re-match `WHERE join_code IS NULL`, silently leaving partial state) — batch the backfill (e.g. 10k rows per UPDATE loop) and/or split into its own migration with a rerun-safe predicate.
- [low] supabase/migrations/20260315_cast_join_code.sql:10 — backfill uses `md5(random()::text)` truncated to 6 chars — only 16^6 ≈ 16.7M distinct values and md5 of random text is not a cryptographically strong generator; collisions across ~thousands of productions are unlikely but the UNIQUE constraint at :6 makes a collision abort the whole UPDATE — use `generate_join_code()` (defined at :55) or a longer alphabet/length for the backfill.
- [low] supabase/migrations/20260315_cast_join_code.sql:55-67 — `generate_join_code()` uses Postgres `random()` (not `gen_random_uuid()`/crypto-grade RNG) and a 32-char alphabet at 6 chars (~1B space) — fine for invitation codes but the function is exposed as callable by clients per the comment at :54, letting any authenticated user burn CPU generating codes — mark it not exposed via PostgREST (`REVOKE EXECUTE` or keep it in a non-exposed schema) if it is only meant for server-side default use (per 20260316 migration).
- [info] supabase/migrations/20260315_cast_join_code.sql:45-52 — `is_production_member` is SECURITY DEFINER and STABLE but has no `SET search_path` — under an attacker-reachable schema/function-name hijack path (if any caller passes attacker-influenced identifiers, or if `public` is on the search_path and a malicious extension/function shadows a name) this could be abused; add `SET search_path = public, pg_temp` — verify actual exposure in callers before treating as exploitable.
- [info] supabase/migrations/20260316_join_code_default.sql:3-4 — the DEFAULT is set to `public.generate_join_code()` evaluated per-row at insert time (correct), but the migration does not backfill any rows that were inserted between 20260315 and this migration with a NULL join_code — if any writer inserted a production without join_code in that window (assumed: none, since 20260315 sets NOT NULL), the column would reject the insert; no action needed beyond confirming no interim writes.

## Coverage
supabase/migrations/20260315_cast_join_code.sql — findings: 8
supabase/migrations/20260316_join_code_default.sql — findings: 1
- [high] supabase/migrations/20260319000001_join_flow_rpc_v2.sql:62-65 — claim_cast_invitation lets any authenticated user claim any unclaimed cast member row — SECURITY DEFINER RPC updates by id alone with no ownership/consent check, so an authenticated user can hijack another user's pending invitation across tenants — add a join-code/production-ownership predicate (e.g. verify caller was invited via lookup_production_by_join_code or restrict to rows where user_id IS NULL AND production belongs to caller's invitation) before the UPDATE
- [medium] supabase/migrations/20260319000001_join_flow_rpc_v2.sql:46-47 — join_production inserts cast_members with user_id = auth.uid()::text but no WITH CHECK against duplicate membership — repeated calls create duplicate rows for the same user/production, and the RLS insert policy (20260318_add_join_code_policy.sql:19-23) only checks auth.uid() = user_id, not uniqueness — add a unique constraint on (production_id, user_id) or an anti-join guard in the RPC
- [medium] supabase/migrations/20260319000001_join_flow_rpc_v2.sql:88-90 — auth_insert_cast policy grants INSERT TO authenticated WITH CHECK (true) — unlike the file-1 policy (auth.uid() = user_id), this fallback lets any authenticated user insert cast member rows attributed to other user_ids, bypassing the self-join restriction — change WITH CHECK to (auth.uid() = user_id) or drop the redundant fallback policy
- [medium] supabase/migrations/20260319000001_join_flow_rpc_v2.sql:93-94 — auth_update_cast policy grants UPDATE TO authenticated USING (true) — any authenticated user can update any cast_members row (e.g. reassign user_id/role), whereas the file-1 policy restricts to own rows (auth.uid() = user_id OR user_id IS NULL) — change USING to (auth.uid() = user_id) or drop the redundant fallback policy
- [low] supabase/migrations/20260319000001_join_flow_rpc_v2.sql:70-74 — GRANT EXECUTE on lookup_production_by_join_code to anon (line 71) while all other RPCs are authenticated-only — unauthenticated (anon) callers can enumerate join codes via row_to_json(p), leaking full production rows to the public role — either grant anon deliberately with a minimal column projection or restrict to authenticated
- [info] supabase/migrations/20260319000001_join_flow_rpc_v2.sql:5-16 — lookup_production_by_join_code returns row_to_json(p) of the whole productions row — verify no secret-bearing columns (owner tokens, private metadata) exist on productions before shipping; if they do, project only public columns
- [low] supabase/migrations/20260318_add_join_code_policy.sql:26-30 — "Users can claim their invitation" UPDATE policy USING (auth.uid() = user_id OR user_id IS NULL) — the user_id IS NULL branch lets any authenticated user claim any unclaimed cast member row (same hijack class as the RPC), since USING(true-ish) on unclaimed rows is the permissive fallback — drop the OR user_id IS NULL branch or gate it on an invitation token the caller proves possession of
- [low] supabase/migrations/20260319000001_join_flow_rpc_v2.sql:77-95 — DO $$ ... EXCEPTION WHEN duplicate_object THEN NULL — swallowing duplicate_object means a rerun that hits a differently-named existing policy silently skips creation, leaving the intended policy absent with no error — re-raise or use CREATE POLICY IF NOT EXISTS semantics (or named policies with ALTER/DROP guards) so reruns converge to the intended state
## Coverage
supabase/migrations/20260318_add_join_code_policy.sql — findings: 1
supabase/migrations/20260319000001_join_flow_rpc_v2.sql — findings: 6
I need to review two SQL migration files against the sql-migration-review defect-class index.

Let me analyze the files:

**File 1: supabase/migrations/20260319100000_add_voice_preset.sql (11 lines)**
- Line 6-8: `ALTER TABLE public.productions ADD COLUMN IF NOT EXISTS voice_preset text, ADD COLUMN IF NOT EXISTS locale text NOT NULL DEFAULT 'en-US';`
- This is an ADD COLUMN on an existing table. In PostgreSQL, `ADD COLUMN` without a non-constant default is metadata-only (fast). But `ADD COLUMN ... NOT NULL DEFAULT 'en-US'` — the default is a constant, so in PG 11+ this is also metadata-only (fast, no rewrite). However, the checklist has "NOT NULL added with existing NULL rows" — with a DEFAULT, existing rows get the default via the missing-missing-value mechanism, so that's fine in PG11+. But wait — the checklist item "[high] NOT NULL added with existing NULL rows" applies when adding NOT NULL without default or when rows could be NULL. Here DEFAULT 'en-US' handles existing rows. Actually in PG < 11, ADD COLUMN with DEFAULT causes a full table rewrite. That's the "Table-rewrite ALTER on a hot table" class. Supabase migrations typically run on PG 12+ (Supabase uses PG 13/14/15), so it's metadata-only. But the checklist says to apply matching classes. Hmm — the defect class "Table-rewrite ALTER on a hot table" is [critical]. Is it exhibited? The code is `ADD COLUMN ... DEFAULT 'en-US'` — on modern PG this is a catalog-only change. The checklist is a heuristic index; I should report only what the code exhibits. The code exhibits an ALTER TABLE ADD COLUMN with NOT NULL DEFAULT on a table that appears to be a hot/production table ("productions"). Without knowing PG version, the risk is a table rewrite on older PG. But I can't verify PG version. The comment says NULL means use defaults — but line 8 sets NOT NULL DEFAULT 'en-US', contradicting line 4's "NULL means use defaults (locale → 'en-US')". Actually line 4 says NULL means use defaults, and the column is NOT NULL DEFAULT 'en-US' — so NULL is impossible; the comment is misleading but that's a doc nit, not really a defect class.

Also: two migrations both add the same `locale` column to `public.productions` with `IF NOT EXISTS`. Migration 20260319100001 adds locale again — redundant with migration 20260319100000 which already adds it. That's "Non-idempotent statements where reruns happen" — actually both use IF NOT EXISTS so they're idempotent. But the duplicate addition across two migrations is a version-ordering/redundancy issue: if migration 20260319100000 runs, 20260319100001 is a no-op; if only 20260319100001 runs (e.g., squashed/reordered), voice_preset is missing. The checklist has "Version ordering conflicts" [medium]. Two migrations in the same batch adding the same column — the second is redundant. That's a real finding: duplicate/redundant migration.

Also "Empty or lying down migration" [high]: migration 20260319100001, if 20260319100000 always runs first, is effectively an empty migration (no-op). It "lies down" — it claims to add locale but it's already added. Hmm, that's the class "Empty or lying down migration". Actually the class means a migration that does nothing or misleads. Migration 2 is a no-op given migration 1. That's [medium]/[high]? The checklist says [high] Empty or lying down migration. But is it exhibited? The second migration's ALTER is redundant — it will always be a no-op when run in order. That's a real smell: either it's dead code or the ordering is ambiguous.

Also line 10-11: comment claims "The lookup_production_by_join_code RPC already uses row_to_json(p), so both new columns are automatically included in join lookups." That's an assumption about another file (the RPC definition) which I cannot see. If the RPC uses row_to_json(p) it would include new columns — plausible. But if the RPC is SECURITY DEFINER and returns row_to_json, adding columns could leak data? Not really. The comment is unverifiable — I could note it as an assumption but it's a comment, not code. The rule: "A finding must be exhibited by the ACTUAL code, not by a comment describing a past or fixed bug." The comment describes behavior of another file. I shouldn't report a finding based on a comment unless it's a defect in this file. Skip.

Now the NOT NULL DEFAULT on locale: line 4 says "NULL means use defaults (locale → 'en-US', preset → locale-based)". But the column is declared NOT NULL — so NULL can never be stored. The comment's semantics ("NULL means use defaults") conflict with the schema (NOT NULL). This is a direction-of-failure / correctness issue: the intended design (per comment) is that NULL means "use default behavior", but the schema forbids NULL, forcing 'en-US' onto every row. Actually wait — with NOT NULL DEFAULT, existing rows get 'en-US' and new rows default to 'en-US'. The comment says NULL means use defaults — meaning if the app doesn't specify, use default. With NOT NULL, the app cannot express "unspecified". Hmm, but the app could just not set the column and get 'en-US'. The comment is about semantics. Is this a defensible finding? The mismatch between documented semantics (NULL → default) and schema (NOT NULL) is a mild inconsistency. The checklist class "NOT NULL added with existing NULL rows" [high] — if productions has existing rows, adding NOT NULL DEFAULT is fine (they get the default). So no.

Actually, let me reconsider "Table-rewrite ALTER on a hot table" [critical]. `ALTER TABLE ... ADD COLUMN ... NOT NULL DEFAULT 'constant'`:
- PG < 11: full rewrite (table lock, long duration on hot table).
- PG ≥ 11: catalog-only.
Supabase projects run PG 14+ typically. I cannot verify the PG version from the inlined code. The checklist is a heuristic; the code exhibits an ADD COLUMN with DEFAULT on a table. Should I report it? The instruction: "Report only what the inlined code exhibits." The code exhibits an ALTER TABLE on public.productions. Whether it rewrites depends on PG version — an assumption. I could state the assumption: "if PG < 11 this rewrites the table". But Supabase is always PG ≥ 14 in current offerings... but migrations could target older. Hmm. I think the defensible finding here is weaker. Let me think about what's most defensible.

The strongest, clearly-exhibited findings:

1. **Duplicate column addition across two migrations** — migration 20260319100001 re-adds `locale` which 20260319100000 already adds (line 8 vs file2 lines 2-3). Both use IF NOT EXISTS so no error, but the second migration is a permanent no-op — dead migration, and if migrations are ever reordered/squashed inconsistently, voice_preset could be missing while locale exists. Class: "Version ordering conflicts" / "Empty or lying down migration". Severity: medium (operator/CI-facing, no data loss). Actually the reachability: migrations run by operator/CI = low per severity guidance ("an operator running a script, CI, or offline tooling = low"). But schema drift could cause runtime errors... The defect is redundant migration — consequence: dead migration, misleading history; if 20260319100000 is ever dropped/squashed, locale exists but voice_preset doesn't, breaking the app's preset feature. Severity: low or medium? It's a maintenance/correctness defect in migration history. I'd say medium is too much; low fits (operator tooling). But "silent data loss"? No. Let me say low... Actually, hmm. The two files are both in this batch, both adding locale. One finding per root cause: file both instances. The root cause is the duplicate definition. I'll file one finding anchored at 20260319100001:2-3 (the redundant one) citing 20260319100000:8.

Wait — actually, is it possible the intent is that 20260319100001 is a separate migration that was authored independently (both dated same timestamp 20260319100000/20260319100001)? The timestamps differ by 1 second. They're sequential. The second is redundant. Yes, finding.

2. **Comment/schema mismatch on NULL semantics** — line 4 of file 1 says NULL means use defaults, but line 8 declares locale NOT NULL DEFAULT 'en-US', making NULL unrepresentable. Also voice_preset is nullable (fine, matches "preset → locale-based" default). The locale NOT NULL contradicts the documented "NULL means use defaults". Consequence: callers cannot express "unspecified locale"; every row is forced to 'en-US' even when the organizer intends locale-based default resolution; if the app layer expects NULL to mean "derive from preset/voice", it will instead read 'en-US'. Hmm — is this defensible? The comment explicitly documents NULL semantics for locale ("locale → 'en-US'"), yet the schema makes NULL impossible. That's an internal inconsistency within the same file. Direction of failure: the schema resolves to the concrete default rather than the documented nullable sentinel. I think this is a legitimate medium/low finding. Severity: it affects app behavior for all users of the productions table... but I can't see the app. The mismatch is exhibited in-file. I'd rate it low-medium. Let me think: consequence — the documented "NULL means use defaults" contract cannot be honored for locale; any code path that tries to store NULL (e.g., "reset to default") will fail with a NOT NULL violation. That's a real behavioral defect. Severity: medium? Reachability: any authenticated user whose flow resets locale → error 500? That's speculative. I'll say low... Hmm. Actually the guidance: "A finding that does not say who can reach it is capped at medium." I can state reachability: any user code path that sets locale to NULL (reset-to-default) hits a NOT NULL violation. That's authenticated-user reachable. Medium seems okay but I'm not certain the app does that. The mismatch is in-file and defensible. I'll go medium? Or low? The consequence is a potential runtime error on a documented operation. I'll say medium is defensible but maybe overreach. Let me be conservative: low. Hmm, but the checklist emphasizes direction-of-failure for defaults. The declared default 'en-US' vs documented "NULL means use defaults" — the schema picks the riskier/less flexible representation. I'll report at low severity with clear reachability caveat... Actually wait. Let me reconsider: is NOT NULL DEFAULT 'en-US' actually wrong? The comment says "NULL means use defaults (locale → 'en-US', preset → locale-based)". Reading carefully: this describes what NULL *would mean* — i.e., if locale is NULL, treat as 'en-US'. The schema implements that by NOT NULL DEFAULT 'en-US' — so NULL never stored, equivalent semantics. It's arguably intentional: avoid NULLs by materializing the default. But then the comment "NULL means use defaults" is describing the *preset* semantics too. Hmm, it's ambiguous. The mismatch is: comment says NULL is meaningful; schema forbids NULL. If the intent were materialized defaults, the comment would say "columns default to...". This is a doc/contract inconsistency. I'll report it as low: "comment documents NULL sentinel semantics but schema declares locale NOT NULL DEFAULT, making NULL unrepresentable". Consequence: reset-to-default code paths (if any — assumption) fail; readers can't distinguish "unspecified" from "explicitly en-US". Low.

3. **ADD COLUMN with NOT NULL DEFAULT on hot table — potential rewrite on PG < 11** — I can't verify PG version. Supabase = PG 15+ nowadays. The checklist class exists but the code only exhibits it conditionally. Per rules: "If a suspicion depends on code you cannot see (another file, a caller), state that assumption inside the finding instead of asserting it." PG version isn't a file... I think I should mention it as an info/low finding with the assumption stated. Hmm. Actually, on PG ≥ 11, ADD COLUMN with a constant DEFAULT is fast and non-blocking except for the brief lock. The real operational concern: ALTER TABLE takes an ACCESS EXCLUSIVE lock on productions — if there's a long-running transaction (e.g., the join-code RPC in flight), the ALTER queues and blocks all reads/writes. That's the "Fast DDL queued behind long transactions" class [high]! That IS exhibited: an ALTER TABLE on a hot table can queue behind long transactions and block traffic. But is that defensible as a finding for a simple ADD COLUMN migration? Every migration does this. The checklist includes it as a class. Hmm — "Fast DDL queued behind long transactions" — the migration is a plain SQL file run by Supabase's migrator; there's no lock timeout / set_lock_timeout shown. The code exhibits an ALTER TABLE with no lock timeout setting. On a hot table with long-running queries, this can block. Is that worth reporting? The checklist says apply matching classes. The class matches: DDL on a table, no CONCURRENTLY/no lock timeout. But ADD COLUMN IF NOT EXISTS is fast DDL; the risk is queueing. I think a defensible medium/low finding: "ALTER TABLE on productions without lock timeout / CONCURRENTLY consideration — queues behind long transactions and blocks all access". Hmm, but ADD COLUMN can't be CONCURRENTLY. The standard mitigation is `SET lock_timeout`. I'll consider reporting at low severity (operator/CI runs migrations). Actually severity guidance: "an operator running a script, CI, or offline tooling = low". Migrations run at deploy time by operator/CI → low. But blast radius: if it blocks, the whole app stalls. "Adjust at most one level — up for silent data loss..." Blocking DDL isn't silent data loss. Low seems right. Or medium? Deploy-time blocking that takes down the app... I'll go low. Hmm, actually many review agents would flag "ADD COLUMN ... NOT NULL DEFAULT on a hot table without lock_timeout" as medium. The reachability is operator/CI (low), adjust up one level for... no. Low.

Wait, but also: is `productions` a hot table? It's the main domain table (productions = shows/events). The migration comment references join_code RPC lookups — so productions is read frequently. Yes, hot.

4. **Two ADD COLUMNs in one statement with IF NOT EXISTS** — idempotent, fine. "Non-idempotent statements where reruns happen" — IF NOT EXISTS makes it idempotent. Good, no finding.

5. **Unnamed constraints** [low]: NOT NULL DEFAULT has no named constraint — NOT NULL constraints get auto names; that's normal, not worth reporting. The checklist has "Unnamed constraints" [low] — typically for CHECK/FK/UNIQUE. NOT NULL DEFAULT 'en-US' — the DEFAULT is not a constraint. Skip.

6. Migration 20260319100001's comment says "Add locale column to productions for syncing dialect to cast members" — same purpose as file 1. Redundant. Covered in finding 1.

7. **Missing migration for rollback / destructive op with no recovery path** — ADD COLUMN is reversible (DROP COLUMN). No destructive op. No finding.

8. **Timestamp ordering**: 20260319100000 then 20260319100001 — sequential, fine.

9. Does migration 1's line 10-11 comment claim the RPC auto-includes new columns — unverifiable, and if wrong, the join lookup would omit locale/voice_preset. But that's a comment about another file. Per rules, I could add a follow-up request for the RPC file... The rule: "if a specific suspicion in this batch cannot be confirmed or refuted without seeing one specific other file". My suspicion: the lookup_production_by_join_code RPC may not actually use row_to_json(p) / may have an explicit column list, meaning new columns aren't returned. But is that a *suspicion I hold*? The comment asserts it; I have no reason to doubt it beyond it being a comment. Reporting a finding based on doubting a comment is weak. But the comment is load-bearing: if the RPC uses an explicit SELECT list (common in SECURITY DEFINER RPCs), the new columns would NOT be included and the feature silently breaks. Hmm. The instruction says findings must be exhibited by actual code, not by a comment describing past/fixed bug. This comment describes current behavior of another module. I could use the follow-up mechanism: request the RPC definition file. Path guess: supabase/migrations/... some earlier migration defining lookup_production_by_join_code, or a function file. I don't know the repo path. The follow-up format: '- <path-in-this-batch> NEEDS <repo-relative-path> — <what seeing it would decide>'. Path-in-this-batch would be one of the two migration files. Repo-relative path: I'd have to guess, e.g. 'supabase/migrations/...lookup_production_by_join_code...'. Guessing a path is risky. Is the suspicion material? The comment claims auto-inclusion; if false, the feature breaks silently. But I have no evidence it's false. I'll skip the follow-up — the comment is plausible (row_to_json(p) does include all columns). Not worth a follow-up round.

Actually, let me reconsider whether to report the "Fast DDL queued behind long transactions" at all. The checklist is a defect-class index for migration review; the class "Fast DDL queued behind long transactions" [high] applies when fast DDL runs without guarding against long-running transactions. The inlined code exhibits bare ALTER TABLE with no lock_timeout. On Supabase, migrations run in a session; without lock_timeout, the ALTER waits indefinitely behind any long transaction, blocking all subsequent queries on productions (ACCESS EXCLUSIVE lock request queues and blocks readers too). This is a genuine, well-known operational defect class. Reachability: deploy-time (operator/CI) → low per guidance, but consequence is app-wide stall. "Adjust at most one level — up for silent data loss, cross-tenant disclosure or unrecoverable state". An indefinite stall isn't unrecoverable. Low. Hmm, but honestly, for a migration review, this is the kind of finding that gets reported at medium. The guidance caps unspecified-reachability at medium and says operator/CI = low. I'll report at low. Or should I report at medium since it can take down production during deploy? The rule says operator running CI = low, adjust at most one level up for unrecoverable state. A stalled deploy is recoverable (kill the migration). Low.

Hmm wait, actually let me reconsider. Both files exhibit the same class (both ALTER TABLE on productions). One finding per root cause: the root cause is "bare ALTER TABLE on hot table without lock_timeout" — but they're two separate migrations... The rule: "when one shared helper, policy or config reaches several call sites, file ONE finding at the cause and list the sites". Two migrations, same pattern. I'll file one finding citing both lines? The format wants one line per finding with a path:line anchor. I'll anchor at 20260319100000:6-8 and mention 20260319100001:2-3 in the consequence. Actually cleaner: anchor at the first, list the second.

Hmm, wait. Let me reconsider the redundancy finding vs the lock finding — they're different root causes, fine.

Let me also double-check: does migration 1 add locale with NOT NULL DEFAULT 'en-US' (line 8) and migration 2 also adds locale NOT NULL DEFAULT 'en-US' (line 3)? Yes, identical definitions. So the second is a pure no-op. Confirmed redundancy.

Also: is there a risk that the two migrations were intended for different tables? File 2 says "productions" too. Same table. Redundant.

Another angle: **"ADD COLUMN IF NOT EXISTS" + separate NOT NULL DEFAULT** — if the column already exists with a different definition (e.g., nullable, no default), IF NOT EXISTS silently skips and the migration lies (claims to add NOT NULL DEFAULT but doesn't enforce). That's the "lying migration" angle: IF NOT EXISTS makes reruns safe but also silently accepts a divergent existing schema. Is that defensible? It's a standard idempotency tradeoff. The checklist has "Non-idempotent statements where reruns happen" — they chose idempotent. I won't report.

What about **missing NOT NULL validation / existing rows**? With DEFAULT, PG 11+ backfills via missing marker — no scan. Fine.

**voice_preset text nullable, no default** — matches "NULL means use defaults". Fine.

**No CHECK on locale format (BCP-47)** — schema doesn't validate 'en-GB' format; app-level concern. Not a migration defect class in the index. Skip. Could mention as info? The checklist doesn't have a class for it. Skip — "Never pad".

**Timestamp without time zone** class — no timestamps here. Skip.

**utf8mb4** — MySQL class, N/A.

**Seed/reference data mixed into schema migrations** — no seed data. Skip.

**Explicit LOCK TABLE** — none. Skip.

**CONCURRENTLY inside a transaction** — none. Skip.

**CREATE INDEX without CONCURRENTLY** — none. Skip.

**Unbatched backfill** — none (no UPDATE). Skip.

**Generated STORED column** — none. Skip.

**Enum addition** — none. Skip.

**Mixed DDL + backfill in one transaction** — migration 1 does two ADD COLUMNs in one statement — that's just DDL, no backfill. Skip.

So my findings:

1. [medium or low] Redundant duplicate migration: 20260319100001 re-adds locale already added by 20260319100000. Severity: low (operator/CI tooling; consequence is dead migration + drift risk if ordering/squashing changes). Hmm — actually, think about consequence more concretely: if someone squashes migrations and keeps only 20260319100001 (or reorders), voice_preset would be missing → app feature broken at runtime. Also the duplicate suggests copy-paste; one of them may have been intended for a different table. I'll say medium? The guidance: operator/CI = low; adjust up only for silent data loss/unrecoverable. Schema drift leading to missing column = runtime errors, recoverable. Low. But hmm, "Version ordering conflicts" is [medium] in the index. The index severities are heuristic; my severity must follow the reachability rules. Operator/CI reachability → low. I'll go low. Hmm, but wait — the redundancy also means: if migration 20260319100000 is edited later to change locale's default (e.g., to NULL-able), migration 20260319100001 won't fix existing DBs... over-thinking. Low.

Actually, let me reconsider once more. Is it possible that these two files are intentionally separate because one is a fix migration re-adding the column for environments where the first failed? Both have IF NOT EXISTS — that's the idempotent-retry pattern. But both were created in the same second (20260319100000/20260319100001) — same batch authoring. A re-add one second later is almost certainly accidental duplication. Finding stands.

2. [low] Comment/schema mismatch: line 4 documents NULL sentinel semantics for locale, but line 8 declares NOT NULL DEFAULT 'en-US' making NULL unrepresentable. Consequence: reset-to-default paths (if the app uses NULL to mean "inherit default") fail with NOT NULL violation; readers can't distinguish unspecified from explicit en-US. Fix: drop NOT NULL (nullable with DEFAULT) or fix the comment. Severity low. Hmm — is this worth reporting? It's an in-file inconsistency, defensible. Yes, low.

Wait, actually — re-read line 4: "NULL means use defaults (locale → 'en-US', preset → locale-based)." This describes the *semantics of NULL* for both columns. voice_preset is nullable → consistent. locale is NOT NULL → inconsistent for locale. So the mismatch is specifically locale. Yes.

3. [low] Bare ALTER TABLE on hot table without lock_timeout — queues behind long transactions (e.g., in-flight join-code RPC transactions), blocking all reads/writes on productions for the deploy duration. Both migrations. Anchor at 20260319100000:6, mention 20260319100001:2. Fix: SET lock_timeout (e.g. '5s') with retry, or run in maintenance window. Severity low (operator/CI).

Hmm, should I also flag the PG<11 rewrite possibility? On PG ≥ 11 ADD COLUMN with constant default is metadata-only. Supabase ≥ PG14. I'll fold a parenthetical into finding 3? No — keep findings tight. Actually the "Table-rewrite ALTER on a hot table" [critical] class: does the code exhibit it? Only on PG<11. I'd be asserting an assumption about the PG version. I could state: "assuming PG ≥ 11 this is metadata-only; on older PG the NOT NULL DEFAULT forces a full rewrite". That's hedging into non-finding. I'll skip it — Supabase targets modern PG, and the checklist class requires the rewrite to actually occur. Better: mention within finding 3 that ADD COLUMN itself is fast; the risk is lock queueing. Good.

4. What about the claim in lines 10-11 (RPC auto-includes columns)? I decided to skip as a finding. Should I use follow-up? The suspicion: "lookup_production_by_join_code may not return the new columns if it uses an explicit column list". This is speculative — the comment says row_to_json(p). row_to_json on a row of productions *does* include all columns. So the comment is technically sound IF the RPC does row_to_json(p). No suspicion worth a follow-up round. Skip.

Any security issues? Migration files, no secrets, no RLS changes. ALTER TABLE public.productions — modifying a public schema table; migrations run as admin. Fine.

One more: **"Empty or lying down migration"** — I'm covering via finding 1 (redundant no-op). Good, one finding per root cause.

Also consider: migration 1 adds TWO columns in one ALTER — if voice_preset addition fails (e.g., lock timeout), locale addition also fails (single statement, atomic). That's fine/atomic — good, not a defect.

Also: **idempotent reruns** — IF NOT EXISTS: rerunning migration 1 after partial... it's one statement, atomic. Fine.

Now severity calibration final:
- Finding 1 (duplicate migration): low. Reachability: operator/CI during deploy. Consequence: dead migration, drift risk. Low.
- Finding 2 (NULL semantics mismatch): low. Reachability: any app code path storing NULL locale — authenticated users; but I must state assumption that such a path exists (can't see app code). Cap: medium if unspecified... I'll state reachability: "any caller that stores NULL (documented reset-to-default) — authenticated user flow, assumed from the documented contract". Hmm, the rule: "A finding that does not say who can reach it is capped at medium." If I say authenticated users, medium allowed. But is it really medium-worthy? The mismatch might be intentional (materialized default). The comment
- [high] supabase/migrations/20260320200000_add_debug_reports.sql:20-22 — SELECT policy `USING (true)` with no role restriction makes every debug report world-readable to any anonymous/unauthenticated PostgREST session — debug reports contain `user_email` (line 5) and full log `content` (line 7), so unauthenticated internet readers can enumerate and dump all users' logs and emails — restrict to `TO authenticated USING (auth.uid() = user_id)` (and a matching owner-only policy for the developer pull path, e.g. via a service-role/edge-function path instead of public read).
- [medium] supabase/migrations/20260320200000_add_debug_reports.sql:15-17 — INSERT policy `WITH CHECK (true)` for `authenticated` does not bind `user_id`/`user_email` to the caller — any authenticated user can forge rows attributing arbitrary `user_id`/`user_email` (spoofed reports under another user's identity, poisoning the developer's log view) — `WITH CHECK (auth.uid() = user_id)` (or `user_id IS NOT NULL AND auth.uid() = user_id`).
- [medium] supabase/migrations/20260320200000_add_debug_reports.sql:4 — `user_id` FK has no `ON DELETE` clause, so deleting an `auth.users` row fails (or, if users are deleted via cascade elsewhere, debug_reports rows orphan with dangling `user_id`) — add `ON DELETE CASCADE` (or `SET NULL`) explicitly; note this is a silent FK behavior choice, not a new cascade on existing data.
- [low] supabase/migrations/20260320200000_add_debug_reports.sql:2-10 — no explicit schema qualification and no `IF NOT EXISTS` on the policy names; rerunning the migration after a partial failure (table exists via `IF NOT EXISTS` but policies created) errors on duplicate policy — make policy creation idempotent (`DROP POLICY IF EXISTS` / `CREATE POLICY ... IF NOT EXISTS` where supported) so reruns are safe.
- [info] supabase/migrations/20260320200000_add_debug_reports.sql:20-22 — the "developer can pull without auth" comment is the stated intent; verify the deployed database actually has this policy and that it is not committed alongside a broader `anon` grant elsewhere — treat as intent confirmation, not a new defect; the severity above already covers the exposure.
## Coverage
supabase/migrations/20260320200000_add_debug_reports.sql — findings: 5
supabase/migrations/20260701090000_add_multi_characters.sql — clean
- [medium] supabase/migrations/20260703100000_purge_test_productions.sql:5-50 — destructive unbatched DELETE of production rows with no recovery path — if any listed UUID turns out to be a real production (typo'd IDs like 'tedt4'/'trst3' at :45-46 suggest sloppy bookkeeping), the data and cascaded children are unrecoverable — guard with a dry-run/verification step or wrap in a reversible transaction with a backup table
- [medium] supabase/migrations/20260703100000_purge_test_productions.sql:5-50 — migration is non-idempotent on rerun semantics and mixes data purge into a schema-migration stream — reruns fail or silently no-op depending on FK state, and the purge is not tied to any schema change — move to a data-fix script or make rerun-safe (e.g. delete only where a marker column confirms test status)
- [low] supabase/migrations/20260703090000_leave_policy_and_audit_cleanup.sql:16-18 — unbatched DELETE over auth.users filtered by email pattern — a broad '%@example.com' match could sweep unintended rows if any real account uses that domain, and the two deletes are not atomic — narrow to the exact seeded test accounts or add a row-count guard
- [low] supabase/migrations/20260703090000_leave_policy_and_audit_cleanup.sql:5-8 — policy drop/create is non-idempotent ordering-sensitive — if the policy name differs in a prior environment the create fails; reruns of the migration pair are not guaranteed safe — use IF EXISTS on the matching name and verify the policy name against the live schema

## Coverage
supabase/migrations/20260703090000_leave_policy_and_audit_cleanup.sql — findings: 2
supabase/migrations/20260703100000_purge_test_productions.sql — findings: 2
- [medium] supabase/migrations/20260703140000_security_lockdown.sql:141-162 — `join_production` SECURITY DEFINER RPC inserts into `cast_members` with no validation that `prod_id` refers to an existing production or that the caller has any relationship to it — any authenticated user can mint a `cast_members` row for an arbitrary production id (including nonexistent uuids if the column allows them, or productions they were never invited to) — consequence: membership table pollution and, because `is_production_member` is keyed off this table, an authenticated user can bootstrap membership in any production they can name an id for, then read that production's cast/script/recordings through the member-scoped policies — smallest safe fix: add an existence/authorization guard inside the definer body (e.g. `if not exists (select 1 from productions p where p.id = prod_id::uuid) then raise exception 'invalid production'; end if;`) and require a valid join code or organizer approval before insert. — reachability: any authenticated user reaching another tenant's data = high, but the shipped-client comment (line 138-140) says role forcing is the only shipped behavior; the insert itself is still reachable by any authenticated caller, so severity high.
- [medium] supabase/migrations/20260703140000_security_lockdown.sql:122-126 — `cast_members` UPDATE policy `using (auth.uid() = user_id or user_id is null)` with `with check (auth.uid() = user_id)` — the `using` clause permits an authenticated user to target *any* row whose `user_id` is null (unclaimed invitation rows), and the `with check` only constrains the *new* row's `user_id`; nothing prevents changing `production_id`, `character_name`, `role`, or `display_name` on someone else's pending invitation — consequence: a user can rewrite an unclaimed invitation row (e.g. set `production_id` to a production they control, or alter `role` before claiming) — smallest safe fix: add `and production_id` ownership check or restrict the update to `set` of only the claim columns via a trigger/`with check` on unchanged `production_id`. — reachability: authenticated user against rows whose `user_id` is null (shared/tenant-visible invitations) = medium.
- [low] supabase/migrations/20260703140000_security_lockdown.sql:37-45 — `recording_object_production` is declared `immutable` but casts `seg::uuid` and catches `others` to return null — `immutable` is a correctness claim the exception handler makes fragile (a path whose first folder segment is not a valid uuid silently yields null, which the policies at 57-79 treat as "not a member", i.e. deny — that direction is safe) — consequence: none for security; only a maintenance hazard if `storage.foldername` behavior changes — smallest safe fix: drop the `immutable` marker or document the null-on-malformed contract. — reachability: none (policy helper only).
- [info] supabase/migrations/20260703140000_security_lockdown.sql:215-216 — `revoke execute ... from public, anon, authenticated` on the two helper functions is immediately undone by the follow-up hotfix migration (20260703150000_fix_helper_grants.sql:15-16) — consequence: the shipped state after both migrations grants EXECUTE to `anon` and `authenticated`, so the lockdown's stated intent ("stop them being callable as a membership oracle") is not achieved; the oracle leak the hotfix accepts is a boolean membership probe requiring both uuids — smallest safe fix: none needed in this pair; note the final state intentionally permits the oracle. — reachability: authenticated users probing their own/other memberships = low (boolean-only, both uuids required).
- [info] supabase/migrations/20260703150000_fix_helper_grants.sql:15-16 — grants EXECUTE to `anon` as well as `authenticated` — consequence: unauthenticated (anon-key) callers can invoke `is_production_member`/`is_production_organizer` directly as a boolean oracle if they can guess/learn two uuids; the migration's own comment (lines 11-14) accepts this as minor — smallest safe fix: grant to `authenticated` only, or wrap the helpers so they are only callable from policy expressions (e.g. `security` invoker check via `current_setting('role')` inside the function). — reachability: unauthenticated internet with anon key = high by the rubric, but the leak is a single boolean requiring knowledge of both uuids and is explicitly accepted in-migration; cap at medium per the "state the reachability" rule and the accepted-risk comment.
## Coverage
supabase/migrations/20260703140000_security_lockdown.sql — findings: 4
supabase/migrations/20260703150000_fix_helper_grants.sql — findings: 1
- [medium] supabase/migrations/20260703160000_drop_last_productions_readall.sql:10 — DROP POLICY IF EXISTS is non-idempotent-safe but the migration is a bare destructive drop with no recovery path and no verification that the other two permissive SELECT policies were already dropped by the lockdown migration — if the lockdown migration was skipped or partially applied, dropping only this third policy leaves the table still readable via the remaining permissive policies, silently preserving the join_code disclosure the comment claims to fix — guard the drop with a DO block asserting the remaining permissive SELECT policies count, or fold the check into the lockdown migration's verification step.
- [medium] supabase/migrations/20260703170000_recordings_delete_policy.sql:11 — the delete policy's organizer branch calls public.is_production_organizer(production_id, auth.uid()) without confirming that helper is SECURITY DEFINER or that it does not itself recurse into RLS-protected tables — if is_production_organizer reads productions under RLS, the USING clause can throw a permission error or silently return false for legitimate organizers, making deletes fail at runtime for the exact users the migration intends to help — verify the helper's definition (SECURITY DEFINER + stable search_path) or inline the organizer check against a table the policy's own privileges already cover.
- [low] supabase/migrations/20260703170000_recordings_delete_policy.sql:5-12 — drop-then-create pair is not rerun-safe as a unit: if the CREATE fails (e.g. is_production_organizer missing at apply time), the migration is left half-applied with the old policy dropped and no new one, and rerunning the migration cannot restore the dropped policy because the drop uses IF EXISTS on a name that no longer exists — wrap both statements in one transaction with a pre-check that the replacement policy text is valid, or use CREATE OR REPLACE-style re-issuance so a failed apply is recoverable.

## Coverage
supabase/migrations/20260703160000_drop_last_productions_readall.sql — findings: 1
supabase/migrations/20260703170000_recordings_delete_policy.sql — findings: 2
- [medium] supabase/migrations/20260813100000_add_script_scenes.sql:24 — `updated_at timestamptz default now() not null` — the NOT NULL qualifier is attached to the column default rather than the column, and Postgres parses `default now() not null` as DEFAULT now() NOT NULL, so this is actually valid; however the column-level NOT NULL is applied at table creation on a new table, which is safe — no consequence — none needed. (No finding; see coverage.)

Wait — re-evaluate: the file is a new-table migration; the checklist classes that apply are: unnamed constraints (unique (production_id, sort_order) is unnamed — low), new index duplicates the unique constraint's index (medium), CREATE INDEX without CONCURRENTLY on a new table is fine (not hot), FK to productions (medium trigger/FK class — FK to hot parent productions with on delete cascade — medium), ON DELETE CASCADE introduced silently (medium), NOT NULL on new table fine.

Let me finalize findings:

1. FK on delete cascade to productions — medium (silent cascade class).
2. Unnamed unique constraint — low.
3. Redundant index idx_script_scenes_production (production_id, sort_order) duplicates the unique constraint index (production_id, sort_order) — medium "New index doesn't match query patterns / duplicates existing".
4. RLS policy "Cast members can read script scenes" uses is_production_member which per the other migration's comment probes cast_members (production_id, user_id) — the index restored in the first migration covers it; fine.
5. The organizer policy is `for all` with USING clause but no WITH CHECK — for `for all` policies, USING applies to SELECT/UPDATE/DELETE and WITH CHECK defaults to USING for INSERT/UPDATE, so it's fine.
6. Cast read policy: `for select using (is_production_member(...))` — fine.
7. Missing WITH CHECK on organizer all-policy: for ALL policies, if WITH CHECK is omitted, USING is used for the check — fine.
8. updated_at has no trigger to update it — not a migration defect class per checklist; skip.
9. First migration: CREATE INDEX without CONCURRENTLY — but it's a migration on Supabase (runs in a transaction/migration window); the checklist says [critical] CREATE INDEX without CONCURRENTLY. On a hot table cast_members, a non-concurrent index build takes an ACCESS EXCLUSIVE lock... Actually CREATE INDEX (non-concurrent) takes SHARE lock blocking writes. In a migration context this is standard, but the checklist flags it as critical for hot tables. cast_members is described as hot (RLS probes it on every row). Migration runs during deploy; writes blocked briefly. Per checklist this is [critical] CREATE INDEX without CONCURRENTLY. I'll report it as critical per checklist but note migration context... The checklist says apply matching classes. I'll report at medium given migration-window reachability? Severity rules: operator/CI = low; adjust. Migration deploy blocking writes is a deploy-time risk — low/medium. The checklist explicitly marks it critical; but severity guidance says cap by reachability. Deploy-time write blocking on a hot table = medium. I'll say medium.

Also first migration: `create index if not exists` — idempotent, fine. No CONCURRENTLY — in Supabase migrations each migration runs in its own transaction; CONCURRENTLY cannot run inside a transaction, so omitting it is actually required. So the "CONCURRENTLY inside a transaction" class doesn't apply; the plain CREATE INDEX class applies but is standard for migrations. I'll flag as low/medium? The checklist is written for interactive agents but says use defect classes. I'll report [low] since migration-window lock is bounded and CONCURRENTLY is impossible in a migration transaction anyway. Actually to be safe: report [medium] "CREATE INDEX without CONCURRENTLY on hot table cast_members — brief write blocking during deploy migration" — but given migrations must be transactional, this is expected. I'll go [low].

Also: the comment says the dropped index was `cast_members_production_id_user_id_key` (a UNIQUE index) and this recreates a non-unique index — fine, matches purpose.

Second migration: `unique (production_id, sort_order)` — unnamed constraint gets auto name; low.

Also potential: sort_order unique per production with default 0 — inserting two scenes with default sort_order 0 would violate unique. That's app-level; not a migration defect class. Skip.

Coverage: both files reviewed.

Final findings list:

- [medium] supabase/migrations/20260813100000_add_script_scenes.sql:15 — FK to productions with ON DELETE CASCADE — deleting a production silently cascades scene rows; standard mirror of script_lines but introduced silently — consequence: bulk deletes on productions also delete scenes (intended per comment) — fix: none needed / document. Hmm, this is intended behavior per comment ("mirrors script_lines"). ON DELETE CASCADE introduced silently class — but it's clearly intended. I'll skip it to avoid padding? The checklist says apply matching classes. It's intended and matches script_lines; not a defect. Skip.

- [medium] line 45-46 — idx_script_scenes_production duplicates the implicit unique index from line 25 (production_id, sort_order) — redundant index, wasted write cost — fix: drop the extra create index or make the unique constraint's index serve lookups.

- [low] line 25 — unnamed unique constraint — auto-named cast_members...; fix: name it.

- [low] line 10-11 — CREATE INDEX without CONCURRENTLY on cast_members — migration-transaction context makes CONCURRENTLY impossible; brief SHARE lock blocks writes during deploy — acceptable; but per checklist report [low].

Also check: `updated_at timestamptz default now() not null` — grammar: column type default expr not null — valid. Fine.

RLS: organizer policy `for all` — USING exists(...) — for INSERT, ALL policy WITH CHECK defaults to USING — fine. Cast select policy fine. No WITH CHECK gap.

One more: policy "Cast members can read script scenes" — is_production_member(production_id, auth.uid()) — matches helper signature per comment. Fine.

Also: no grant/select policy for organizer on select — organizer covered by ALL policy. Fine.

Also: table has RLS enabled but no policy for... e.g., service role bypasses RLS. Fine.

Also: `create table public.script_scenes` without `if not exists` — non-idempotent; migrations rerun concern: Supabase tracks applied migrations, reruns not typical. [medium] Non-idempotent statements where reruns happen — both files use create index if not exists (first) but create table without guard (second). Report [low]/[medium]? Supabase migration runner applies each once; reruns happen on failed/partial deploys. I'll flag [low].

Compose final report.
- [info] macos/Runner/AppDelegate.swift:10-12 — applicationShouldTerminateAfterLastWindowClosed returns true — app quits when the last window closes; for a macOS app with a menu-bar/hidden-window flow this is intentional, but if CastCircle supports closing the window while recording/sharing continues, state is lost — confirm intended behavior with product; if not, return false and rely on explicit quit — verify intended product behavior before changing.

## Coverage
macos/Flutter/GeneratedPluginRegistrant.swift — clean
macos/Runner/AppDelegate.swift — findings: 1
- [medium] macos/Runner/BackgroundDownloadPlugin.swift:56-58 — restarting a download for an existing modelId cancels the old task but leaves its entry in `activeDownloads` until the cancelled task's completion callback fires, and the new entry is written immediately at :61 — if the cancelled task's `didCompleteWithError` (NSURLErrorCancelled early return at :168) fires after the new entry is stored, `removeValue` at :177 deletes the NEW task's entry, so progress/complete events for the restarted download are silently dropped and the delegate's `didFinishDownloadingTo` guard at :95 fails — smallest safe fix: remove the old entry synchronously before storing the new one (or key entries by task identity, not modelId).
- [medium] macos/Runner/BackgroundDownloadPlugin.swift:100-101 — `moveItem` fails if a partial file already exists at `destURL` (e.g. a previous crashed run left a `.tmp`-sibling destination file), so a completed download is reported as `onDownloadError` "Failed to save file" even though the bytes arrived — smallest safe fix: remove the destination before moving, or use `replaceItem` semantics.
- [low] macos/Runner/BackgroundDownloadPlugin.swift:103 — `attributesOfItem(atPath:)` is called on the destination path after the move; if the move succeeded but the file was externally removed, the `try?` yields nil and size silently becomes 0 MB in the log and 0 in the `onDownloadComplete` payload — smallest safe fix: compute size from the moved item's attributes with explicit error handling, or from `totalBytesWritten`.
- [low] macos/Runner/BackgroundDownloadPlugin.swift:104 — size is logged in MB via integer division `size / 1024 / 1024`, so any file under 1 MB logs "0 MB" — smallest safe fix: log bytes or use Double division.
- [medium] macos/Runner/BackgroundDownloadPlugin.swift:41-42 — the `.tmp` sibling is deleted with `try?` before the download starts, but nothing removes the `.tmp` file after a successful move or a cancelled/failed download, so failed/cancelled downloads leak partial `.tmp` files on disk for every retry — smallest safe fix: delete the temp file in the error/cancel completion path.
- [low] macos/Runner/BackgroundDownloadPlugin.swift:25 — the URLSession delegate queue is `.main`, and `didWriteData` progress callbacks (throttled but still frequent) plus `didFinishDownloadingTo` file I/O (`moveItem`, attribute reads) all run on the main thread, stalling the UI during large downloads — smallest safe fix: use a dedicated background delegate queue and hop to main only for `channel.invokeMethod`.
- [low] macos/Runner/BackgroundDownloadPlugin.swift:9-10,177 — `activeDownloads`/`lastProgressEmit` are mutated from the delegate callbacks on the main queue and from `handle` on the platform channel thread; if the channel handler runs off-main this is a data race — smallest safe fix: assert all access on one queue or lock the dictionaries. (Assumption: FlutterMethodChannel handlers on macOS run on the platform main thread, which makes this benign today; flagging as low.)
- [low] macos/Runner/BackgroundDownloadPlugin.swift:66,94,129,164 — `taskDescription` is typed `String?` but is used as the modelId key; a task created without `taskDescription` (never in this code, since :66 always sets it) would silently drop events via the `guard ... else { return }` at :94/:129/:164 — smallest safe fix: none needed; noting the silent-drop shape only because cancel-then-restart at :56-58 can produce a task whose description is set but whose entry was replaced (covered by the first finding).
- [info] macos/Runner/MainFlutterWindow.swift:26-38 — PdfTextPlugin, MemoryMonitorPlugin, VisionOcrPlugin, PaddleOcrPlugin, AppleSttPlugin are referenced but not inlined; cannot verify their init signatures or side effects — verify separately.

## Coverage
macos/Runner/BackgroundDownloadPlugin.swift — findings: 8
macos/Runner/MainFlutterWindow.swift — findings: 0
- [medium] macos/Runner/PdfTextPlugin.swift:55,97,136 — background work dispatched on `DispatchQueue.global` but `result` (a FlutterResult) is invoked from `DispatchQueue.main.async` closures that capture `result` — FlutterResult must be called on the platform (main) thread, which is satisfied, but the *outer* `result` capture inside the async block for the failure path at 59-64/101-105 is invoked on main while the success path at 81-92/120-128 is also on main; however `extractText`'s failure `return` at 64 exits the outer async block without ever calling `result` on the main thread only if the inner main.async already ran — actually the real defect: at 58-65, when `PDFDocument(url:)` fails, the inner `DispatchQueue.main.async` schedules the error result, then `return` — this is fine — but at 96-130 `extractTextPerPage` builds `pages` on a background thread and calls `result` on main; the defect class here is none of the SQL-migration checklist items apply to these Swift files, so no migration-class findings exist — consequence: none — fix: none.
- [low] macos/Runner/MemoryMonitorPlugin.swift:36-47 — `getPhysicalFootprint` returns 0 on `task_info` failure, and the caller at 23 computes `availableMB = totalMB - physicalMB`, so a mach-call failure silently reports the *entire* physical memory as available — consequence: memory-pressure heuristics downstream (assumed caller) see a falsely permissive "plenty free" value instead of an error, direction of failure resolves to the riskier value — fix: return a failure sentinel (e.g. `-1`) or omit the `availableMemoryMB` key when `task_info` fails so consumers can distinguish "unknown" from "unlimited".
- [low] macos/Runner/MemoryMonitorPlugin.swift:38 — `count` is initialized from `MemoryLayout<task_vm_info_data_t>.size / MemoryLayout<natural_t>.size` but `task_vm_info_data_t`'s first field is `mach_msg_type_number_t` (an integer_t-sized member), so the standard idiom divides by `MemoryLayout<natural_t>.size` — this matches the idiom, but `count` is declared `var` and passed as `&count` yet never re-read after the call; harmless — consequence: none — fix: none.
- [info] macos/Runner/PdfTextPlugin.swift:113 — `document.page(at: i)?.string ?? ""` swallows per-page extraction failures as empty strings, so a partially-OCR-able PDF reports `hasAnyText = true` (from other pages) while individual pages silently degrade to `""` in the returned array — consequence: caller cannot distinguish "page has no text" from "extraction failed" — fix: return a nullable/typed per-page status or log extraction failures instead of coalescing to `""`.
- [low] macos/Runner/PdfTextPlugin.swift:142-158 — `checkEmbeddedText` returns `false` when `PDFDocument(url:)` fails to open, conflating "unreadable/corrupt PDF" with "no embedded text" — consequence: the OCR-vs-PDFKit decision (per the comment at 133-135) will route a *corrupt* file to OCR (permissive fallback) rather than surfacing an open error, and a genuinely text-bearing but transiently-locked file gets misrouted — direction of failure resolves to the permissive value — fix: return an optional/three-state result (opened-no-text vs. open-failed) or throw so the caller can decide.
- [low] macos/Runner/PdfTextPlugin.swift:73-78 vs 112-118 — `extractText` skips pages whose `page.string` is nil (`continue`) and joins only non-nil texts, so page boundaries are lost and the joined text at 79 can misalign page indices relative to `extractTextPerPage`'s `pages` array (which keeps `""` placeholders) — consequence: consumers of `extractText`'s flat string cannot map text back to page numbers, and the two methods disagree on representation for the same document — fix: append `""` for nil pages (matching `extractTextPerPage`) or include page markers in the joined output.

## Coverage
macos/Runner/MemoryMonitorPlugin.swift — findings: 2
macos/Runner/PdfTextPlugin.swift — findings: 3
- [medium] macos/Runner/VisionOcrPlugin.swift:44 — scale clamp is applied to the wrong value: `args["scale"] as? Double ?? 2.0` is clamped, but a scale supplied as a non-Double (e.g. Int/NSNumber bridging failure) silently falls back to 2.0 instead of erroring, and the clamp `min(max(..., 0.5), 4.0)` is fine only for the Double path — consequence: a caller passing an out-of-range or wrong-typed scale gets a silently different render scale (up to 4x memory allocation per page) rather than an error; smallest safe fix: validate the scale type explicitly and return `INVALID_ARGS` when `as? Double` fails, keeping the 0.5–4.0 clamp for the accepted Double.
- [medium] macos/Runner/VisionOcrPlugin.swift:100-101 — render dimensions are computed from `bounds.width * scale` with no upper bound on absolute pixel size: a large media-box page (e.g. poster-size PDF) at scale 4.0 produces a bitmap whose width/height can exceed practical CGContext limits, causing `CGContext(data:width:...)` to return nil or `makeImage()` to fail — consequence: page silently counted as failed (`failedPages += 1`) with no error surfaced to the caller, so a valid PDF reports partial OCR success; smallest safe fix: cap width/height to a maximum (e.g. 8192 px) after scaling and log/surface the truncation.
- [medium] macos/Runner/VisionOcrPlugin.swift:93-119 — per-page loop runs Vision OCR serially on the calling (global-queue) thread with no cancellation or memory release between pages: each rendered CGImage and its Vision request are held until the next iteration, so a many-page PDF accumulates pressure and can jetsam the process mid-OCR — consequence: the whole `ocrPdf` call dies without returning any result to the Dart side (the `result` callback is never invoked on process death); smallest safe fix: wrap each page's render+OCR in an autoreleasepool and process pages with a bounded concurrency (or release the CGImage explicitly after `ocrImage` returns).
- [low] macos/Runner/VisionOcrPlugin.swift:182-184 — reading-order sort uses only `boundingBox.origin.y` with no tie-break for equal Y or grouping into visual lines: observations on the same baseline but different X order arbitrarily, and two-column text interleaves — consequence: OCR output text order is wrong for multi-column or same-row fragments, degrading downstream text extraction; smallest safe fix: sort by `(y desc, x asc)` and optionally bucket observations into lines by Y proximity before emitting.
- [low] macos/Runner/VisionOcrPlugin.swift:188 — `observation.topCandidates(1).first` discards the observation when the candidate list is empty without logging or counting it: a page whose recognition yields empty candidates silently drops those lines from the returned blocks — consequence: caller sees fewer lines than the page actually contains with no failure signal; smallest safe fix: count dropped observations and include the count in the per-page result (or log via NSLog like the other failure paths).
- [low] macos/RunnerTests/RunnerTests.swift:7-10 — `testExample` is an empty placeholder that asserts nothing: it cannot fail, so it provides no regression protection for the Runner or the VisionOcrPlugin pipeline — consequence: CI green does not mean the native OCR path works; smallest safe fix: replace with a real test that exercises `VisionOcrPlugin.handle` (e.g. a stub `FlutterMethodCall` for `recognizeText` with a fixture image) or delete the file if no tests are intended.

## Coverage
macos/Runner/VisionOcrPlugin.swift — findings: 5
macos/RunnerTests/RunnerTests.swift — findings: 1
- [medium] supabase/migrations/20260813110000_join_flow_v3_and_policy_fixes.sql:203-205 — unbatched backfill UPDATE over productions re-rolling legacy join codes — on a large productions table this rewrites every matching row in one statement inside the migration transaction, holding row locks and bloating WAL; if the migration fails partway the rerun is safe (regex re-matches only remaining legacy codes) but the initial pass is unbounded — batch the UPDATE (e.g. loop over `WHERE join_code ~ '^[0-9A-F]{6}$' LIMIT 1000` until no rows) or run it as a separate post-deploy job
- [low] supabase/migrations/20260813110000_join_flow_v3_and_policy_fixes.sql:203-205 — no ANALYZE after the join_code re-roll backfill — planner statistics for `productions.join_code` go stale after a bulk rewrite, degrading subsequent code-lookup plans — run `ANALYZE public.productions` after the UPDATE
- [medium] supabase/migrations/20260813110000_join_flow_v3_and_policy_fixes.sql:211 — DROP INDEX without CONCURRENTLY — `drop index if exists public.idx_script_lines_production` takes an exclusive lock on script_lines and can abort in-flight queries on the app's biggest bulk-write path; if the migration runs inside a transaction (Supabase default) CONCURRENTLY is unavailable, so instead schedule the drop in its own non-transactional migration or accept the brief lock explicitly — split into a dedicated migration run outside a transaction, or document/accept the lock window
- [low] supabase/migrations/20260813110000_join_flow_v3_and_policy_fixes.sql:172-176 — insert policy allows role='understudy' but the join RPC at :138 only ever inserts 'actor' — callers with direct-table INSERT can mint 'understudy' rows whose semantics the RPC path never produces; if 'understudy' is meant to be granted only by organizers this is a permissive residual — tighten the WITH CHECK to `role = 'actor'` or add an organizer-grant path; verify intended role semantics first (assumption: no other grant path is visible in this file)
- [low] supabase/migrations/20260813110000_join_flow_v3_and_policy_fixes.sql:147-165 — claim_cast_invitation returns void and silently no-ops when the code is wrong or the row is already claimed — callers cannot distinguish "claimed" from "bad code", which invites retry loops and hides takeover attempts; add a raise/exception or return a status json — return a distinguishable result or raise on zero-row update
- [low] supabase/migrations/20260813110000_join_flow_v3_and_policy_fixes.sql:61-77 — lookup_production_by_join_code returns the full production row as json without projecting columns — if productions carries columns not meant for pre-join callers (organizer-only fields, internal ids) they leak to anyone who can pass the rate limit; project only join-relevant fields via json_build_object — verify productions' column set; if all columns are join-safe this is informational only

## Coverage
supabase/migrations/20260813110000_join_flow_v3_and_policy_fixes.sql — findings: 6
- [medium] supabase/migrations/20260319100001_add_locale.sql:3 — ADD COLUMN with NOT NULL DEFAULT on a hot table rewrites/validates rows under a full-table lock (PostgreSQL <11 semantics; on 11+ the DEFAULT is stored in the catalog but the lock still blocks concurrent writes while the catalog version change forces a connection-wide plan cache reset) — every concurrent production write stalls for the duration of the migration and replicas replay a catalog-invalidating change — use `ALTER TABLE ... ADD COLUMN ... DEFAULT ...` without NOT NULL, or backfill in batches after adding the column nullable — reachability: operator running migrations (low), but the lock stalls all tenant writes during deploy, so medium.
- [medium] supabase/migrations/20260319100001_add_locale.sql:3 — the migration is not rerun-safe in the intended sense: `IF NOT EXISTS` makes the ADD COLUMN idempotent, but the NOT NULL/DEFAULT clause is re-applied on rerun against a table that may already hold NULL locale values from a prior partial state, failing the rerun — a failed deploy cannot be retried without manual cleanup — split into ADD COLUMN IF NOT EXISTS (nullable) + batched `UPDATE ... WHERE locale IS NULL` backfill + `ALTER ... SET NOT NULL` — reachability: operator retry path only (low).
- [medium] supabase/migrations/20260801130000_cast_members_rls_index.sql:10-11 — CREATE INDEX without CONCURRENTLY on a hot table takes an exclusive lock that blocks all reads/writes on cast_members for the index build duration; the RLS helper `is_production_member()` probes this table on every row of productions, cast_members, recordings, script_lines and storage.objects, so the stall multiplies across every RLS check — use `CREATE INDEX CONCURRENTLY IF NOT EXISTS` (and note CONCURRENTLY cannot run inside a transaction block, so the migration must run outside one) — reachability: operator deploy (low) but blocks every authenticated request touching RLS during the build, so medium.
- [low] supabase/migrations/20260801130000_cast_members_rls_index.sql:10-11 — the index is created without `ANALYZE` and the migration file documents the RLS helper as seq-scanning without it; a freshly built index with stale statistics can still cause the planner to prefer seq scans on small estimated row counts — run `ANALYZE public.cast_members` after the build — reachability: operator/CI only (low).

## Coverage
supabase/migrations/20260319100001_add_locale.sql — findings: 2
supabase/migrations/20260801130000_cast_members_rls_index.sql — findings: 2

## Run stats

input 56666 tok (+17424 cached), output 19072 tok — sync requests, discounted — 31 files in 14m (128.3 files/h, 0.9 min/batch)
