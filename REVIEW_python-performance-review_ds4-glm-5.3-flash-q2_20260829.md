# Pi sweep review (perf focus) — CastCircle

Exhaustive per-file pass: 4 code files across 2 batches — model ds4/GLM-5.3-Flash-Q2:off — 2026-08-30.

## Findings

## Coverage
scripts/compare_macbeth_versions.py — clean
scripts/make-demo-script.py — clean
- [medium] scripts/parse_script.py:148-152 — detect_character_cue runs ~27 precompiled regex `pattern.match(line)` probes per input line, and the multi-character fallback at 155-158 additionally compiles a fresh regex per line — on a full-length play (~130k lines per the comment at 145-147) the multi-cue `re.match` with a runtime-built pattern is recompiled on every call — trigger: once per OCR line during parse; growing input: unbounded line count of the raw text file; work per unit: up to 27 regex probes + 1 regex compile; affected resource: CPU on the main thread of the CLI run; consequence: parse of a full play spends a large fraction of its wall time in the fallback regex compile/retry loop; smallest safe fix: hoist the multi-character pattern to a module-level `re.compile` constant alongside `_SORTED_CUE_PATTERNS`.
- [low] scripts/parse_script.py:163-165 — the multi-character cue verification does a linear `char in names_str` scan over all 27 KNOWN_CHARACTERS for every multi-cue line — trigger: per multi-cue dialogue line; growing input: KNOWN_CHARACTERS list length (27, fixed) × multi-cue line count (unbounded); work per unit: 27 substring scans; affected resource: CPU; consequence: negligible at 27 entries but grows if the cast list is extended for other plays; smallest safe fix: precompute a set of known names and check membership of the split name tokens instead of substring scanning the list.
- [low] scripts/parse_script.py:117-119 — `is_noise` re-executes `re.match(pattern, stripped)` against 7 string patterns (not precompiled) for every raw line before cleaning — trigger: once per input line; growing input: unbounded line count; work per unit: 7 regex compiles/matches (Python caches compiled patterns internally, but the loop still re-dispatches 7 matches per line); affected resource: CPU; consequence: measurable but small overhead on ~130k-line inputs; smallest safe fix: precompile NOISE_PATTERNS into a module-level list of compiled objects.
- [low] scripts/parse_script.py:128-132 — `clean_line` builds four fresh regex objects per call, and it is invoked per flushed dialogue, per stage direction, and per candidate line inside the parse loop — trigger: multiple times per input line; growing input: unbounded line count; work per unit: 4 `re.sub` calls with runtime pattern strings; affected resource: CPU; consequence: redundant regex compilation/dispatch dominates on large OCR files; smallest safe fix: hoist the four patterns to module-level compiled constants.
- [low] scripts/parse_script.py:181 — `extract_inline_direction` compiles its pattern per call; it runs once per flushed dialogue block — trigger: per dialogue flush (once per speech, thousands per play); growing input: number of dialogue blocks (unbounded); work per unit: 1 regex compile + match; affected resource: CPU; consequence: minor; smallest safe fix: module-level compiled pattern.
- [low] scripts/parse_script.py:256,278,311 — ACT/SCENE header detection and the continuation-guard `re.match(r"^[^a-zA-Z]*$", line)` compile their patterns per loop iteration — trigger: per input line; growing input: unbounded line count; work per unit: up to 3 regex compiles; affected resource: CPU; consequence: small constant overhead multiplied by ~130k lines; smallest safe fix: precompile at module scope.
- [info] scripts/parse_script.py:394-408 — the `__main__` block writes both markdown and JSON outputs unconditionally on every run even when only stats are wanted, and the JSON dump at 406-408 serializes the full parsed line list with `indent=2` — trigger: each CLI invocation; growing input: full parsed ScriptLine list (unbounded, one entry per script line); work per unit: O(n) serialization with pretty-print inflation; affected resource: disk write + CPU on the operator's machine; consequence: a full play produces a multi-MB pretty-printed JSON write on every run; smallest safe fix: gate the JSON/markdown example writes behind a flag or write compact JSON (`json.dumps(..., separators=(",", ":"))`). Operator-run CLI, so low reachability.
- [medium] scripts/pdf_to_script.py:127-139 — `_detect_characters_from_pdf` scans every page of the document with `page.get_text("dict")` and then re-derives the same per-page block/line/span structures again inside `_extract_folger` (lines 155-196) and again per visual-line group — trigger: once per Folger PDF conversion; growing input: page_count × blocks × lines × spans (unbounded across a full play's PDF); work per unit: full dict-mode text extraction plus Python-level span joins, executed twice over the whole document; affected resource: CPU and peak memory (the dict structures for every page are materialized twice); consequence: doubling of extraction wall time and transient memory for large Folger PDFs; smallest safe fix: extract the dict structures once into a shared per-page cache (or pass the detected char set and page dicts into `_extract_folger`) instead of re-scanning the document.
- [medium] scripts/pdf_to_script.py:155-174 — the ACT-1 header search calls `page.get_text()` (plain extraction) for candidate pages and separately `page.get_text("dict")` in the same loop, and it breaks only on the first hit — but if no page matches, the loop visits every page doing both extraction modes — trigger: once per conversion when the signature is missing or positioned differently; growing input: page_count (unbounded); work per unit: two full text extractions per page; affected resource: CPU; consequence: worst-case full-document double extraction just to locate the start page; smallest safe fix: reuse the dict blocks already collected in the same pass and only fall back to `get_text()` when the cheap positional check is inconclusive.
- [low] scripts/pdf_to_script.py:109-117 — `_is_folger_pdf` extracts plain text for up to 15 pages before `_extract_folger` re-extracts the same pages in dict mode — trigger: once per conversion; growing input: min(15, page_count) pages; work per unit: one plain + one dict extraction per early page; affected resource: CPU; consequence: redundant extraction of the front-matter pages; smallest safe fix: derive the Folger signature from the dict pass already performed in `_detect_characters_from_pdf`/`_extract_folger`, or cache page text.
- [low] scripts/pdf_to_script.py:196-212 — the visual-line grouping allocates a new list per y-group and re-sorts each group by x (`sorted(group, ...)`) for every page — trigger: per page of the Folger PDF; growing input: elements per page (unbounded with dense pages); work per unit: O(elements log elements) sort plus per-group list allocations; affected resource: CPU and allocation churn inside the per-page loop; consequence: noticeable on dense two-column Folger pages; smallest safe fix: sort elements once by (y, x) and slice contiguous y-bands instead of building per-group lists and re-sorting each.
- [low] scripts/pdf_to_script.py:363-373 — `main` splits the entire converted result with `splitlines()` and then runs a regex match per line to count character cues — trigger: once per CLI run; growing input: full output text (unbounded, whole play); work per unit: O(n) split + O(n) regex matches; affected resource: CPU on the operator's machine; consequence: minor for one run, but the stats block re-scans text already processed; smallest safe fix: count cues during extraction or use a single precompiled pattern with a generator over lines. Operator-run CLI, low reachability.
- [info] scripts/pdf_to_script.py:34-63 — KNOWN_CHARACTERS is a hardcoded ~120-name set duplicated between this script and the Dart parser per the docstring; not a performance defect on its own, but the alias/known-name sets in scripts/parse_script.py (27 entries) and here (120 entries) are maintained in two places — flagging only as informational since no runtime cost is exhibited beyond set construction at import. Verify the duplication is intentional.

## Coverage
scripts/parse_script.py — findings: 7
scripts/pdf_to_script.py — findings: 6

## Run stats

input 17838 tok (+2828 cached), output 1841 tok — sync requests, discounted — 4 files in 1m (124.1 files/h, 1.0 min/batch)
