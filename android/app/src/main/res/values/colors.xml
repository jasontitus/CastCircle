<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Matches AppTheme.darkTheme's scaffoldBackgroundColor. The app is
         ThemeMode.dark unconditionally, so the launch window must be dark
         too or light-mode devices flash white before the first frame. -->
    <color name="launch_background">#121212</color>
</resources>
