# Pi sweep review — CastCircle

Exhaustive per-file pass: 10 code files across 6 batches — model ds4/GLM-5.3-Flash-Q2:off — 2026-08-29.

## Findings

- [medium] android/app/src/main/kotlin/com/tiltastech/lineguide/AndroidSttPlugin.kt:101 — `context!!` in "initialize" after detach — if Dart calls the channel during/after `onDetachedFromEngine` (context = null), the platform thread throws KotlinNullPointerException and the MethodChannel result is never delivered — guard with `context ?: run { result.success(false); return }` as `startListening` already does
- [medium] android/app/src/main/kotlin/com/tiltastech/lineguide/AndroidSttPlugin.kt:122 — same `context!!` in "isAvailable" — detach-then-call crashes the platform thread instead of returning availability — same null-guard fix
- [medium] android/app/src/main/kotlin/com/tiltastech/lineguide/AndroidSttPlugin.kt:438 — `durationMs` computed from `recordingStartMs` after `recordingPath`/`captureThread` are nulled but before the async join — if a concurrent `startRecording` resets `recordingStartMs` (or a second stop arrives), the reported duration is wrong; capture `recordingStartMs` into a local alongside `path`/`thread` at 428-431
- [medium] android/app/src/main/kotlin/com/tiltastech/lineguide/AndroidSttPlugin.kt:442-455 — `stopRecording` posts `result.success` from a detached `Thread` after `onDetachedFromEngine` may have destroyed the channel — invoking a result on a dead MethodChannel is a no-op that leaves Dart's future pending forever; guard the posted block with a channel-attached flag (or complete on the engine binding's messenger) — reachability: any authenticated user whose Dart side stops recording right as the engine detaches (hot restart), so medium
- [low] android/app/src/main/kotlin/com/tiltastech/lineguide/AndroidSttPlugin.kt:165 — RMS level mapping `((rmsdB - 2f) / 8f).coerceIn(0f, 1f) * 0.3f` caps speech at 0.3 and treats the whole Android dB range as 0..1 — matches the documented iOS scale, but the comment at 163-164 says speech ≈ 0.05-0.3 while real Android RMS often reads below -2 dB, so silence endpointing may never trigger; verify against the Dart threshold before changing — info-level only if the Dart side is confirmed aligned
- [low] android/app/src/main/kotlin/com/tiltastech/lineguide/AndroidSttPlugin.kt:343 — `drainEncoder` uses a single shared `bufInfo` across loop iterations while `encoder.getOutputBuffer(outIndex)` may return a buffer whose size/flags differ from the just-dequeued info on some OEM codecs — copy `bufInfo` fields into locals per iteration to avoid writing stale metadata to the muxer
- [low] android/app/src/main/kotlin/com/tiltastech/lineguide/AndroidSttPlugin.kt:397 — `encoder.getInputBuffer(inIndex)!!` — non-null assert on a codec API that can return null on some vendor implementations; a crash here kills the capture thread and the recording silently (result already returned true at 321) — replace with a guarded null check that surfaces `result.error` via `mainHandler.post`
- [low] android/app/src/main/kotlin/com/tiltastech/lineguide/AndroidSttPlugin.kt:368-369 — `record.read(pcm, 0, CHUNK_SAMPLES)` returning `n <= 0` hits `continue` with no backoff — a busy/unavailable mic spins the capture thread in a tight CPU loop; add a small sleep or bounded retry before continuing
- [low] android/app/src/main/kotlin/com/tiltastech/lineguide/AndroidSttPlugin.kt:460 — `releaseRecorder` joins the capture thread for 1500 ms on the platform main thread — called from `startRecording` (278) where the comment at 470-471 says the synchronous join is intentional for the mic handoff, but the same helper is also reachable from any future caller; keep the async variant for non-handoff paths only — no change needed now, note for callers
- [info] android/app/build.gradle.kts:90 — `implementation(files("libs/onnxruntime-java-1.22.0.jar"))` is a vendored jar fetched by `scripts/fetch-ort-java.sh` — verify the checked-in jar is not committed (or that its provenance/hash is pinned in the fetch script); cannot confirm from this file alone
- [info] android/app/build.gradle.kts:16-20 — `key.properties` is loaded from the repo root and its passwords flow into `signingConfigs` — verify `key.properties` is gitignored and not committed; the file itself is not in this batch so no secret value is quoted

## Coverage
android/app/build.gradle.kts — findings: 2
android/app/src/main/kotlin/com/tiltastech/lineguide/AndroidSttPlugin.kt — findings: 8
- [medium] ContactPickerPlugin.kt:70-71 — onActivityResult drops the pending Dart result when resultCode is not RESULT_OK and data is null — wait, actually the code handles that: line 73-76 sends success(null). Let me re-check the real defect: line 70 `pendingResult ?: return true` — if pendingResult is null (e.g., a second onActivityResult for the same request code after the result was already consumed, or an activity-result event arriving without a pending call), the method returns true (consumed) without ever resolving the Dart future — but pendingResult null means it was already consumed, so that's fine. The real defect: line 59-61 — `pendingResult` is set but if the user cancels the picker, onActivityResult fires with RESULT_CANCELED and result.success(null) is sent — fine. Actual defect: line 90-93 — the projection queries DISPLAY_NAME and _ID but the selection/sort args are passed as `(null, null, null)` for selection, selectionArgs, sortOrder — wait, query(uri, projection, selection, selectionArgs, sortOrder) — passing null selection is fine. Real defect: line 96 — `contactId` is read from cursor.getString(1) which is _ID column; then used in selection args for Phone/Email queries — fine. Real defect: line 131-135 — `result.success` is called inside the try block after the outer query's `.use{}`; if the outer query returns null (no cursor), the whole block silently skips and result.success is never called — the Dart future never completes. Actually line 90 `contentResolver.query(...)?.use { ... }` — if query returns null, the use block doesn't run, name stays null, and execution continues to line 131 result.success — no, the `?.use` returns null but execution continues past it since it's a statement, not a return. So result.success still runs. OK.

The genuine defect: line 70 — `pendingResult ?: return true` returns true (claiming the result) without completing the pending Dart call in the case where pendingResult was already nulled by a prior onActivityResult for the same requestCode — but that can't happen twice. However: line 52-64 — onMethodCall "pickContact" sets pendingResult then calls startActivityForResult; if the activity result listener never fires (e.g., startActivityForResult throws, or the activity is restarted and the result is delivered to a new plugin instance), pendingResult holds an unresolved MethodChannel.Result and the Dart side hangs forever with no timeout. That's a real hang defect but speculative about restart.

Concrete exhibited defect: line 73-76 — when the user cancels (RESULT_CANCELED) or returns no data, the plugin reports `success(null)` — the Dart side cannot distinguish "user canceled" from "no contact picked"; but that's design, not a bug.

Exhibited defect: line 136-138 — the catch-all `catch (e: Exception)` wraps the whole block including `result.success`; if an exception occurs after pendingResult was nulled but before success/error, it calls result.error — fine. But if `result.success` itself throws (unlikely), error is called on an already-consumed result.

Exhibited defect: line 80-83 — `activity?.contentResolver ?: run { result.success(null); return true }` — if activity is null (detached between pick and result), the picked contact is silently dropped as null instead of an error; direction of failure resolves to the permissive/silent path. That's a real finding: silent data loss on detach.

Exhibited defect: line 114, 127 — `catch (_: SecurityException) {}` swallows the permission failure and degrades to name-only silently — the comment at 98-102 says this is intentional degradation; but the plugin never checks or requests READ_CONTACTS, so phone/email will essentially always be null on modern Android without the permission, silently. The comment documents it; per direction-of-failure this is a permissive fallback but it's documented as intended design. Still worth a low/medium finding: silent degradation without any signal to the caller.

Exhibited defect: line 131-135 — `result.success(mapOf("name" to (name ?: "")))` — when the query succeeded but the cursor had no row (moveToFirst false), name is null and is coerced to "" — indistinguishable from a real empty name; minor.

Exhibited defect: line 60-61 — `Intent(Intent.ACTION_PICK, ContactsContract.Contacts.CONTENT_URI)` with hardcoded requestCode 2001 — if two pickContact calls overlap (second call before first resolves), pendingResult is overwritten at line 59 and the first Dart result is lost forever (never completed). That's a real concurrency defect: overlapping calls clobber pendingResult. Reachability: authenticated user double-tapping; consequence: first future never completes. Severity: medium (own-data, silent hang). Fix: reject a second pickContact while one is pending.

MainActivity.kt: line 11-20 — plugins referenced (AndroidSttPlugin, PaddleOcrPlugin, etc.) are not in the inlined files; can't verify. No exhibited defect in MainActivity itself from the shown code. One possible: registering plugins in configureFlutterEngine after super — standard. No findings.

Also ContactPickerPlugin: line 36-38 — onDetachedFromEngine doesn't clean pendingResult; if engine detaches with a pending result, the Dart result never resolves — same hang class as above; fold into the pendingResult finding.

Line 45, 50 — onDetachedFromActivity sets activity = null but does not remove the activity result listener or resolve pendingResult — same class.

Final findings list:
1. [medium] ContactPickerPlugin.kt:59,70 — overlapping pickContact calls overwrite pendingResult; first Dart result never completes — reject second call while pending.
2. [medium] ContactPickerPlugin.kt:80-83 — activity null on detach silently returns success(null), dropping the picked contact — return error instead.
3. [low] ContactPickerPlugin.kt:114,127 — SecurityException swallowed silently; phone/email degrade to null with no signal — log or return a degraded flag.
4. [low] ContactPickerPlugin.kt:36-38,45,50 — detach paths never resolve a pending result — resolve with error on detach.

Coverage: both files, MainActivity clean.
- [medium] android/app/src/main/kotlin/com/tiltastech/lineguide/PaddleOcrPlugin.kt:122-127 — onMethodCall re-enters itself on the main thread via mainHandler.post while `loading` is true, but the retry loop runs on a bare `Thread` that never checks whether the plugin was detached (`onDetachedFromEngine` nulls sessions and sets `ready=false`); after detach the posted re-entry sees `ready=false` and `loading=false` and calls `result.error("NOT_READY", ...)` on a channel whose handler was already removed — consequence: a stale MethodChannel.Result is completed after teardown and, if the engine re-attaches a new plugin instance, the old result's callback can fire on the new instance's channel state, producing a spurious NOT_READY for a request that was actually served by the new instance — smallest safe fix: capture the plugin instance (`this@PaddleOcrPlugin`) and re-check `binding != null` before posting the retry, and post the re-entry only if the same instance is still attached.
- [medium] android/app/src/main/kotlin/com/tiltastech/lineguide/PaddleOcrPlugin.kt:139-147 — `recognizeText` decodes an arbitrary caller-supplied filesystem path with `BitmapFactory.decodeFile(path)` with no path validation or directory scoping — consequence: any app component (or a remote peer via the Dart-facing channel, which is authenticated-user-reachable) can make the plugin read and OCR arbitrary files (e.g. private media, downloaded documents) and return their text content, an unintended-disclosure primitive if the caller is not the file's owner — smallest safe fix: restrict `path` to the caller's own files dir / a whitelisted content URI, or require the caller to pass an in-memory bitmap instead of a path.
- [medium] android/app/src/main/kotlin/com/tiltastech/lineguide/PaddleOcrPlugin.kt:150-155,158-164 — `ocrPdf` and `ocrPdfPage` open an arbitrary caller-supplied path with `ParcelFileDescriptor.open(File(path), MODE_READ_ONLY)` and render it with `PdfRenderer` — consequence: same unscoped-file-read primitive as recognizeText; a malicious or buggy caller can point the plugin at any readable file on the device (PDF parser on non-PDF data also risks native crashes in libpdfcore) — smallest safe fix: validate the path against the app's scoped storage (e.g. require it to live under `context.getExternalFilesDir()` or come from a `ContentResolver`-opened descriptor) before opening.
- [low] android/app/src/main/kotlin/com/tiltastech/lineguide/PaddleOcrPlugin.kt:70,122-126,139-147,155,164 — every request spawns a bare `Thread` with no cap and no reuse (recognizeText/ocrPdf/ocrPdfPage each create a new thread per call, plus the polling thread per NOT_READY call) — consequence: a burst of imports can create dozens of concurrent ONNX inference threads on top of the session's own intra-op pool, thrashing memory on low-RAM devices and slowing the very pipeline the thread-count tuning at 97 optimizes — smallest safe fix: replace ad-hoc `Thread(...)` with a single-thread or bounded `Executors.newFixedThreadPool`/coroutine dispatcher shared by the plugin.
- [low] android/app/src/main/kotlin/com/tiltastech/lineguide/PaddleOcrPlugin.kt:223-224 — progress is emitted via `channel.invokeMethod("ocrProgress", ...)` from inside `mainHandler.post` while the PDF loop runs on a worker thread, but nothing guards against the plugin being detached mid-loop (`onDetachedFromEngine` nulls `binding` and closes sessions) — consequence: `invokeMethod` on a channel whose messenger is gone throws on the main thread inside the posted lambda, crashing the app during teardown of a long OCR job — smallest safe fix: capture `binding` locally at loop start and skip the progress post (and the final `result.success`) when it has been cleared.
- [low] android/app/src/main/kotlin/com/tiltastech/lineguide/PaddleOcrPlugin.kt:104 — dictionary split uses `split("\n")` without stripping `\r`, so a CRLF-encoded keys.txt yields keys with trailing `\r` — consequence: decoded text gains invisible carriage returns and dictionary index lookups mis-align on Windows-authored assets, silently corrupting recognized strings — smallest safe fix: `split("\r?\n")` or trim each entry.
- [low] android/app/src/main/kotlin/com/tiltastech/lineguide/PaddleOcrPlugin.kt:292-294 — detection resize ratio is computed from `detLimitSide / max(origW, origH)` but the resulting `newW/newH` are rounded independently to 32-multiples, so a tall image can end up with `newH` exceeding the 960 cap (e.g. origH=2000, origW=100: ratio≈0.48 → newH≈960 ok, but origH=3000, origW=60: ratio≈0.32 → newH≈960 ok; the failure case is origH=1930, origW=1930 where ratio=0.4974 → newW=newH≈960 ok — the real overflow is when `roundToInt` of the 32-multiple rounding pushes past 960, e.g. orig long side 1935 → ratio 0.4961 → 960.03 → rounds to 960+32=992) — consequence: the det input tensor exceeds the model's trained 960 limit, degrading detection accuracy or tripping shape checks on some builds — smallest safe fix: clamp `newW`/`newH` to `detLimitSide` after the 32-multiple rounding.
- [low] android/app/src/main/kotlin/com/tiltastech/lineguide/PaddleOcrPlugin.kt:342-343 — `visited = BooleanArray(mW * mH)` and `stack = IntArray(4096)` are allocated per `detectBoxes` call with no reuse; for a 960×960 det map that is ~921k booleans plus per-component stack copies (`copyOf` doubling at 373/381/389/397) — consequence: large transient allocations per page increase GC pressure on the inference-critical path measured at ~420ms — smallest safe fix: hoist the visited/stack buffers into reusable fields sized once per session.
- [low] android/app/src/main/kotlin/com/tiltastech/lineguide/PaddleOcrPlugin.kt:408 — unclip distance uses `detUnclipRatio` (0.4) times area over perimeter, but PP-OCR's unclip is `area * ratio / perimeter` with ratio 1.9–2.2 for DBNet; the 0.4 value here (mirrored from a comment claiming Mac validation) shrinks boxes instead of expanding them — consequence: detected boxes are contracted before recognition, cropping glyph edges and lowering word accuracy versus the reference pipeline the file claims to match — smallest safe fix: verify the constant against the Swift/PP-OCR reference (expected ~1.9) and set `detUnclipRatio` accordingly; if 0.4 is genuinely validated, add a comment citing the source.
- [info] android/app/src/main/kotlin/com/tiltastech/lineguide/PaddleOcrPlugin.kt:101-103 — model and dictionary assets are read via `flutterAssets.getAssetFilePathByName` with no integrity check; a repackaged APK could swap det/rec ONNX weights — consequence: a tampered model could alter OCR output used by downstream flagging logic — smallest safe fix: verify asset checksums against a build-time manifest (or rely on Play signing; note only).
- [info] android/app/src/main/kotlin/com/tiltastech/lineguide/MemoryMonitorPlugin.kt:47-49 — memory figures are computed from `Runtime.totalMemory/freeMemory` (JVM heap) while the keys claim "physicalFootprintMB" — consequence: the reported "physical footprint" is actually the app's Java-heap usage, not the physical memory footprint (native + graphics + heap), so callers relying on the key misinterpret device pressure — smallest safe fix: rename the key or use `ActivityManager.getMemoryInfo`/`Debug.getNativeHeapAllocatedSize` + `totalMem` semantics consistent with the iOS counterpart.

## Coverage
android/app/src/main/kotlin/com/tiltastech/lineguide/MemoryMonitorPlugin.kt — findings: 1
android/app/src/main/kotlin/com/tiltastech/lineguide/PaddleOcrPlugin.kt — findings: 10
- [low] android/app/src/main/kotlin/com/tiltastech/lineguide/PdfTextPlugin.kt:68 — checkPdfReadable is a constant `false` for hasEmbeddedText — every PDF is reported as having no embedded text, so callers relying on this channel skip embedded-text extraction and always re-run OCR — return the actual readability/embedded-text probe result (or surface an explicit "unsupported" error) instead of a hardcoded false
- [low] android/app/src/main/kotlin/com/tiltastech/lineguide/StubPlugins.kt:28 — loadModel stub returns success(false) while unloadModel/deleteModel return success(null) — inconsistent stub semantics make the Dart layer treat a failed load as a completed no-op state — align stub responses (either all success-null or all explicit UNAVAILABLE errors) so fallback logic in TtsService can't mis-detect model state
- [low] android/app/src/main/kotlin/com/tiltastech/lineguide/StubPlugins.kt:92-93 — cancelDownload succeeds unconditionally even though startDownload always errors UNAVAILABLE — cancel of a never-started download reports success, masking missing-state failures in the Dart download controller — return notImplemented or an explicit error for cancelDownload when no download session exists

## Coverage
android/app/src/main/kotlin/com/tiltastech/lineguide/PdfTextPlugin.kt — findings: 1
android/app/src/main/kotlin/com/tiltastech/lineguide/StubPlugins.kt — findings: 2
- [low] android/build.gradle.kts:8-16 — build directory redirected to repo-root `../../build` and clean task deletes `rootProject.layout.buildDirectory` — the clean task deletes the redirected root build dir (repo-root `build/`), which is fine, but the `subprojects` block at 14-17 re-resolves `newBuildDir.dir(project.name)` eagerly at configuration time before `:app`'s own layout is applied, which can collide with the Flutter tooling's own build-dir redirection and produce stale/overlapping outputs — smallest safe fix: keep the stock Flutter `build.gradle.kts` (no custom build-dir redirection) or compute subproject dirs lazily inside each subproject's own configuration block.
- [low] android/build.gradle.kts:18-20 — `evaluationDependsOn(":app")` forces every subproject to evaluate `:app` first — this is the stock Flutter pattern but makes configuration order-dependent and fails hard if `:app` is renamed/absent; consequence is a broken build only, not a runtime defect — smallest safe fix: none needed if `:app` exists (it does, per settings.gradle.kts:31); no change required.
- [info] android/settings.gradle.kts:24-26 — FlutterFire plugin versions (google-services 4.3.15, firebase-perf 1.4.1, crashlytics 2.8.1) are pinned but google-services 4.3.15 is an old release line; not a defect in this file alone — verify the surrounding FlutterFire tooling supports these versions; no code change indicated here.

## Coverage
android/build.gradle.kts — findings: 1
android/settings.gradle.kts — clean
- [medium] android/app/src/main/kotlin/com/tiltastech/lineguide/ContactPickerPlugin.kt:70-71 — pendingResult is overwritten without cancelling the previous one; a second pickContact while the first is still pending silently drops the first caller's result — the first MethodChannel.Result never resolves (Dart future hangs) and the second caller's result is delivered to whichever onActivityResult fires — guard with `if (pendingResult != null) return` or complete-and-null the existing pendingResult before reassigning.
- [medium] android/app/src/main/kotlin/com/tiltastech/lineguide/ContactPickerPlugin.kt:59,70-71 — pendingResult is never cleared on detach (onDetachedFromEngine/onDetachedFromActivity) nor on a cancelled/aborted activity result path other than the handled one; a stale Result held across engine detach can invoke `success`/`error` on a dead channel — null out pendingResult in onDetachedFromEngine and onDetachedFromActivity.
- [low] android/app/src/main/kotlin/com/tiltastech/lineguide/ContactPickerPlugin.kt:61 — startActivityForResult is called without requesting the READ_CONTACTS runtime permission and the plugin never checks it; on API 23+ the ACTION_PICK intent itself grants the picked contact, but the follow-up Phone/Email queries at 104-127 rely on the URI grant which does not extend to those tables — the SecurityException catch at 114/127 silently degrades to name-only, which is the intended fallback, so this is informational unless the app elsewhere assumes phone/email are returned — verify READ_CONTACTS is declared/requested if phone/email are required features.
- [low] android/app/src/main/kotlin/com/tiltastech/lineguide/ContactPickerPlugin.kt:131-135 — result.success is invoked with `name ?: ""` while phone/email stay null; the Dart side must handle the empty-string sentinel, and a picked contact with no display name is indistinguishable from a cancelled pick (both yield a resolved value) — consider returning null for name on cancel and a distinct shape for "picked but unnamed".
- [info] android/app/src/main/kotlin/com/tiltastech/lineguide/ContactPickerPlugin.kt:136-138 — broad `catch (e: Exception)` around the whole read path converts any unexpected failure (e.g. an IllegalStateException from a stale resolver) into a generic CONTACT_ERROR with `e.message` possibly null — acceptable for a picker, but narrowing to the specific expected exceptions would avoid masking programming errors.
- [low] android/app/src/main/kotlin/com/tiltastech/lineguide/MainActivity.kt:11-20 — plugin registration order is fixed at engine configure time with no guard against duplicate registration if configureFlutterEngine is invoked twice (e.g. background engine restart on some devices); FlutterPlugin.onAttachedToEngine for ContactPickerPlugin would re-create the channel and re-set the handler, leaking the previous binding — add an idempotence check or rely on FlutterActivity's single-configure contract (assumed here).
- [info] android/app/src/main/kotlin/com/tiltastech/lineguide/MainActivity.kt:18-20 — KokoroMlxStubPlugin/MediaControlStubPlugin/BackgroundDownloadStubPlugin are registered unconditionally on Android as "iOS-only stubs"; if any of these implement onMethodCall with notImplemented that is fine, but if they silently succeed they mask missing Android implementations — verify their onMethodCall returns notImplemented (not visible in this batch).

## Coverage
android/app/src/main/kotlin/com/tiltastech/lineguide/ContactPickerPlugin.kt — findings: 5
android/app/src/main/kotlin/com/tiltastech/lineguide/MainActivity.kt — findings: 2

## Run stats

input 33748 tok (+5445 cached), output 5832 tok — sync requests, discounted — 12 files in 5m (133.3 files/h, 0.9 min/batch)
