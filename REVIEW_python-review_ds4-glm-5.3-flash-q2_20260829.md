# Pi sweep review — CastCircle

Exhaustive per-file pass: 4 code files across 2 batches — model ds4/GLM-5.3-Flash-Q2:off — 2026-08-30.

## Findings

- [low] scripts/compare_macbeth_versions.py:62-67 — subprocess.run without timeout — if pdf_to_script.py hangs, the comparison script blocks indefinitely; operator-only tooling — add `timeout=120` (and handle TimeoutExpired).
- [low] scripts/compare_macbeth_versions.py:60-71 — missing Folger file triggers a silent regeneration from the PDF, overwriting any prior manual edits to macbeth_folger_converted.txt without confirmation — operator could lose a hand-tuned conversion; require an explicit flag or prompt before regenerating.
- [low] scripts/compare_macbeth_versions.py:124-129 — Scene-1 comparison pairs blocks by index across two differently-structured texts, so a single extra/missing cue in either edition shifts every subsequent row and mislabels all later attributions as mismatches — align by character name or cue sequence rather than positional index.
- [low] scripts/make-demo-script.py:40 — scene-boundary detection treats any ACT/SCENE heading after line 100 as a body heading; a heading quoted inside dialogue or a stage direction after line 100 splits a scene into two bounds, silently truncating the extracted block — anchor bounds to headings at line start with no preceding content, or validate the extracted block against expected cue counts.
- [low] scripts/make-demo-script.py:81-82 — speaker regex `^([A-Z][A-Z\' ]+)\.$` also matches ACT/SCENE headings and all-caps words in stage directions, inflating the reported speaker count/list — exclude ACT/SCENE headings and require the speaker line to be a standalone cue.

## Coverage
scripts/compare_macbeth_versions.py — findings: 3
scripts/make-demo-script.py — findings: 2
- [medium] scripts/parse_script.py:394-397 — CLI writes parsed markdown/JSON into the repo's `examples/` directory derived from the script's own location — consequence: running the tool on any input silently creates/mutates files inside the repository tree (and fails outright if the checkout is read-only), polluting the repo with generated artifacts instead of writing next to the input file — smallest safe fix: write outputs beside the input file (or an explicit `--output` dir) instead of `os.path.dirname(os.path.dirname(os.path.abspath(__file__)))` + `examples/`.
- [low] scripts/parse_script.py:376 — hardcoded default input path `/tmp/pride_full_ocr.txt` — consequence: running the script with no argument reads whatever happens to be at that shared temp path (or crashes with FileNotFoundError), making the tool's behavior machine-dependent — smallest safe fix: require an explicit input path argument (or read stdin) instead of a fixed `/tmp` default.
- [low] scripts/parse_script.py:155-165 — multi-character cue regex accepts any all-caps name list and returns the raw joined string as the character name — consequence: lines like "FIRST WITCH, SECOND WITCH. Text" are attributed to a single concatenated "character" rather than normalized speakers, and unknown all-caps sequences (e.g., a shouted "STOP, LOOK, LISTEN.") are misparsed as dialogue cues — smallest safe fix: require every comma-separated name to be a known character (or normalize each name via CHARACTER_ALIASES) before accepting the cue.
- [low] scripts/parse_script.py:256-273 — ACT header regex `^ACT\s+(I+|[IVX]+|\d+)` matches any line starting with "ACT" followed by numerals, including dialogue lines that begin with the word "ACT" (e.g., "ACT 2 SCENE 3, I say") — consequence: such a line is swallowed as a HEADER record and its dialogue text is dropped from the parsed output — smallest safe fix: anchor the match to end-of-line (`re.match(r"^ACT\s+[\wIVX]+\s*$", line)`) or require the line to be short/uppercase-only before treating it as a header.
- [low] scripts/parse_script.py:278-285 — SCENE header regex uses `re.IGNORECASE` and no end anchor, so any line starting with "scene"/"SCENE" plus a numeral is treated as a scene header — consequence: dialogue beginning with "Scene 5 of the film..." is silently consumed as a header and lost — smallest safe fix: require the header to occupy the whole line (`^\s*SCENE\s+[\dIVX]+\s*$`) before switching scenes.
- [low] scripts/parse_script.py:311 — continuation filter `re.match(r"^[^a-zA-Z]*$", line)` only rejects lines with no letters at all; short all-caps interjections or OCR fragments with one letter are appended into the current speaker's dialogue — consequence: stray fragments (page artifacts, character names repeated as a cue that failed the `.` check) get merged into the previous speech, corrupting dialogue attribution — smallest safe fix: also require the continuation to start lowercase or end without a terminal cue marker before appending.
- [low] scripts/parse_script.py:317-319 — branch `if not current_character and line.startswith("(")` re-adds a line already handled by `is_stage_direction` above only when the earlier branch didn't flush; dead/duplicate path — consequence: harmless in practice but indicates the standalone-direction branch above (301-306) can leave `current_character` set, so a following parenthetical is silently appended to nothing — smallest safe fix: remove the redundant branch or reset `current_character`/`current_dialogue_parts` in the 301-306 branch.
- [low] scripts/pdf_to_script.py:363 — `Path(output_path).write_text(result)` uses the default locale encoding and no explicit encoding — consequence: on Windows (locale cp1252) or under a non-UTF-8 locale, non-ASCII extracted text (em dashes, curly quotes common in Shakespeare PDFs) raises UnicodeEncodeError or silently mangles characters in the output file — smallest safe fix: `Path(output_path).write_text(result, encoding="utf-8")`.
- [low] scripts/pdf_to_script.py:288-291 — orphan dialogue (no current_char, no stage_dir) is emitted anyway via the `elif dialogue_parts:` branch — consequence: text fragments from headers/TOC/running titles on the first pages get printed as bare dialogue lines, polluting the parser input with unattributed speech — smallest safe fix: skip emission when `not current_char and not stage_dir` instead of only `pass`-ing inside the branch.
- [low] scripts/pdf_to_script.py:264 — `int(re.search(r'\d+', act_header).group())` assumes the ACT header contains digits; a Folger variant using Roman numerals ("ACT IV") makes `re.search` return None and raises AttributeError mid-extraction — consequence: conversion aborts for otherwise-valid Folger PDFs — smallest safe fix: guard the search result (`m = re.search(...); num = int(m.group()) if m else roman-parse the trailing token`).
- [low] scripts/pdf_to_script.py:242-243 — character-name match `text in all_chars and 80 <= x <= 95` uses exact string equality against the known set, but Folger margin labels frequently include trailing punctuation/line-wrap splits — consequence: split labels ("MAC") fail to match and the speech gets attributed to the previous speaker or emitted as orphan dialogue — smallest safe fix: normalize the label (strip punctuation, join wrapped uppercase neighbors) before the set lookup.
- [info] scripts/parse_script.py:1-409 — script is a standalone CLI with no network, subprocess, crypto, or deserialization surface; no secrets present — consequence: none beyond the findings above — smallest safe fix: n/a.
- [info] scripts/pdf_to_script.py:1-377 — script reads a local PDF and writes a local text file; no network, subprocess, crypto, or deserialization surface; no secrets present — consequence: none beyond the findings above — smallest safe fix: n/a.

## Coverage
scripts/parse_script.py — findings: 8
scripts/pdf_to_script.py — findings: 5

## Run stats

input 16984 tok (+2178 cached), output 1723 tok — sync requests, discounted — 4 files in 1m (130.9 files/h, 0.9 min/batch)
