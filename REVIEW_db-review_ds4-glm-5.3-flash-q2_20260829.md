# Pi sweep review — CastCircle

Exhaustive per-file pass: 29 code files across 15 batches — model ds4/GLM-5.3-Flash-Q2:off — 2026-08-29.

## Findings

- [medium] supabase/migrations/20260314061409_initial_schema.sql:59-67 — cast_members FK columns (production_id, user_id) have no dedicated index; the composite unique (production_id, user_id) covers production_id-leading lookups but the RLS policies on productions (lines 92-100) and script_lines (second file lines 36-42) filter cast_members by user_id alone, which is unindexed — every authenticated read of productions/script_lines runs a per-row scan of cast_members on user_id — add index on cast_members(user_id) (or (user_id, production_id)) — reachable by any authenticated user on every list query.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:106-115 — recordings FK columns production_id and user_id have no covering index; RLS policy at lines 119-127 filters recordings by production_id via cast_members, and the unique constraint (production_id, line_id, user_id) is production_id-leading so user_id-only predicates (insert/update policies lines 129-135) scan — add index on recordings(user_id) and recordings(production_id) — reachable by any authenticated cast member reading production recordings.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:106-115 — recordings.line_id is text with no index and no FK; queries joining/filtering recordings by line_id (the app's core "lines" dimension) hit an unindexed column on a growing table — add index on recordings(line_id) or (production_id, line_id) — reachable by any authenticated user listing recordings per line.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:151-156 — storage insert policy checks only bucket_id and auth.role(), not ownership of the object path; any authenticated user can upload into any folder of the public 'recordings' bucket, including other users'/productions' prefixes — add a with_check clause tying storage.foldername(name) or metadata to auth.uid()/their production — reachable by any authenticated internet user (cross-tenant write).
- [medium] supabase/migrations/20260314061409_initial_schema.sql:158-163 — storage select policy is bucket-wide ('bucket_id = recordings' + authenticated) with no per-user scoping; any authenticated user can read every audio object URL/metadata in the bucket, not just their productions' — scope using clause to storage.foldername(name) matching auth.uid() or cast membership — reachable by any authenticated internet user (cross-tenant read).
- [medium] supabase/migrations/20260314061409_initial_schema.sql:165-170 — update policy compares auth.uid()::text to foldername(name)[1] but there is no matching insert/select ownership scoping (lines 151-163 are bucket-wide), so the ownership invariant is enforced only on update, not on create/read — apply the same folder-ownership predicate to insert and select policies — reachable by any authenticated internet user.
- [low] supabase/migrations/20260314061409_initial_schema.sql:147-149 — bucket created with public=true while object policies are meant to restrict access; public bucket flag bypasses object-level RLS for direct URL access depending on storage version — verify intended access model; if private, set public=false — operator/config decision, low reachability.
- [medium] supabase/migrations/20260314120000_add_script_lines.sql:5-18 — script_lines has no updated_at-based soft-delete or version column and RLS select policy (lines 34-42) filters cast_members by user_id, which has no index on cast_members (see initial schema finding) — same root cause as cast_members user_id index; consequence: every script_lines read for a cast member scans cast_members unindexed — fix by adding cast_members(user_id) index in the initial migration — reachable by any authenticated cast member on every script fetch.
- [medium] supabase/migrations/20260314120000_add_script_lines.sql:14 — line_type constrained by CHECK to four values; app-side enums (client code not shown) may add values later, and CHECK-only constraint means schema drift requires migration — acceptable as-is but note constrained values enforced only in DB, no app-level guard visible — low consequence, medium class per checklist (constrained values enforced only in app code is the inverse here; DB-only constraint is fine) — no action needed beyond awareness.
- [low] supabase/migrations/20260314120000_add_script_lines.sql:45 — index idx_script_lines_production (production_id, order_index) duplicates the unique constraint (production_id, order_index) at line 17; the unique constraint already creates an identical b-tree — drop the redundant index or keep only one — operator/low.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:41-47 — productions has no index on organizer_id; RLS policy "Organizer can do anything" (lines 51-53) and script_lines organizer policy (second file lines 23-31) filter productions by organizer_id = auth.uid(), unindexed — every organizer-authenticated query scans productions — add index on productions(organizer_id) — reachable by any authenticated organizer on every query.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:59-67 — cast_members has no index on production_id alone for the reverse-direction policy lookups (lines 71-79 filter cast_members by production_id + user_id; the composite unique covers production_id-leading so this is covered) — covered by unique constraint; no separate finding beyond the user_id one above.
- [high] supabase/migrations/20260314061409_initial_schema.sql:129-135 — recordings insert/update policies check only auth.uid() = user_id with no production membership check; any authenticated user can insert/update a recording row claiming user_id = their own id for ANY production_id, including productions they are not cast in — add with_check/using clause verifying cast membership via exists(select 1 from cast_members where production_id = recordings.production_id and user_id = auth.uid()) — reachable by any authenticated internet user (cross-tenant write into others' productions).
- [medium] supabase/migrations/20260314061409_initial_schema.sql:119-127 — recordings select policy checks cast membership but not organizer; an organizer who is not a cast member cannot read their own production's recordings (direction-of-failure: policy resolves to deny for the legitimate owner) — add OR organizer_id = auth.uid() branch via productions — reachable by authenticated organizers (self-data denial, medium).
- [medium] supabase/migrations/20260314061409_initial_schema.sql:92-100 — productions select policy "Cast members can read their productions" grants read to any cast member but the organizer-only write policy at lines 51-53 means cast members can read productions they were added to; consistent — no finding.
- [low] supabase/migrations/20260314061409_initial_schema.sql:24-31 — handle_new_user trigger function is SECURITY DEFINER and inserts into profiles without checking for existing row; re-running signup flows or metadata without display_name yields NULL display_name rows silently — add ON CONFLICT (id) DO NOTHING and coalesce for display_name — operator/low.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:6-11 — profiles.display_name/avatar_url nullable while trigger (lines 24-31) may insert NULL display_name when raw_user_meta_data lacks the key; app code (not shown) presumably assumes non-null — coalesce(new.raw_user_meta_data ->> 'display_name', '') or make column NOT NULL with default — reachable by any authenticated user at signup.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:106-115 — recordings has no ON DELETE behavior specified for user_id FK to auth.users (line 110: references auth.users(id) with no on delete clause); default NO ACTION means deleting a user with recordings fails or leaves orphans depending on auth.users delete path — specify on delete cascade or set null per product intent — operator decision, medium class (FK relationships assumed but absent/incomplete).
- [medium] supabase/migrations/20260314061409_initial_schema.sql:59-67 — cast_members.user_id FK (line 62) also lacks ON DELETE clause; user deletion blocked or orphaned cast rows — same root cause as recordings user_id FK; fix both in one migration — operator decision.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:44 — productions.organizer_id FK to auth.users lacks ON DELETE clause; deleting an organizer user with productions fails — same root cause family as above — operator decision.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:7 — profiles.id FK to auth.users has on delete cascade (line 7) which is correct for profiles, but cast_members/recordings/productions do not — see above findings; no separate line.
- [low] supabase/migrations/20260314061409_initial_schema.sql:141 — alter publication supabase_realtime add table public.recordings without specifying columns/levels; realtime broadcasts full row payloads including audio_url to any subscribed client authorized only by RLS on the table — acceptable given RLS select policy exists, but note realtime RLS is enforced only if the publication is configured with RLS-authorized mode — verify publication config — operator/low.
- [medium] supabase/migrations/20260314120000_add_script_lines.sql:48 — same realtime publication add for script_lines; RLS select policy exists (lines 34-42) so payload exposure is gated by RLS if publication uses RLS-authorized mode — verify config — operator/low, folded into the same class as above.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:106-115 — recordings.audio_url is plain text with no FK to storage objects; deleting the storage object leaves dangling URLs the app will 404 on — acceptable schema choice, no enforcement possible in DB alone — no finding beyond info; not reported.
- [info] supabase/migrations/20260314061409_initial_schema.sql:147-149 — storage bucket insert uses on conflict (id) do nothing which is fine; no secrets present in either migration file — nothing to verify.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:63 — role CHECK constraint allows 'organizer' role on cast_members rows; combined with policies that only check existence of a cast_members row (lines 71-79, 92-100) rather than role, an 'understudy' cast member gets full read of productions/script_lines/recordings — if role is meant to gate permissions, policies should check role in ('organizer','actor') or app intent — direction-of-failure: resolves to permissive when role state is unused — reachable by any authenticated cast member (self-data, medium).
- [medium] supabase/migrations/20260314120000_add_script_lines.sql:34-42 — cast-members read policy on script_lines does not distinguish roles either; same permissive-by-existence pattern as above — same root cause, folded into the role finding above.
- [low] supabase/migrations/20260314061409_initial_schema.sql:109 — recordings.line_id is text not null with no FK to any lines table; if lines live in script_lines (second file), line_id has no referential integrity and can point at nonexistent lines — add FK or document as denormalized key — operator/low.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:112 — duration_ms integer default 0; app may treat 0 as "unknown" vs "zero duration" — direction-of-failure: missing state resolves to 0 rather than NULL — low consequence, not reported as a finding beyond this note; no line.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:10,16,46,65,113 — created_at/recorded_at use timestamptz (timezone-aware) — correct; no timestamp-without-timezone finding.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:129-135 — update policy "Users can update own recordings" uses auth.uid() = user_id but no production membership; same cross-tenant write class as insert finding above — folded into that finding (one root cause: ownership-only predicates on recordings write policies).
- [medium] supabase/migrations/20260314061409_initial_schema.sql:151-170 — storage policies have no delete policy at all; objects can never be deleted via storage API by users (or, depending on defaults, deletion falls to service role only) — if app expects user-initiated deletes, add a delete policy with folder ownership — operator decision, medium class (missing policy = permissive/deny ambiguity), reported as one finding.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:106-115 — recordings unique (production_id, line_id, user_id) prevents duplicate takes but there is no idempotency-key mechanism for retried uploads; a retried insert with same key set is safely deduped by the unique constraint, so retry class is covered — no finding.
- [low] supabase/migrations/20260314061409_initial_schema.sql:45 — productions.status CHECK with default 'draft' — fine; no finding.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:71-79 — cast_members select policy uses exists(...) subquery on cast_members itself (self-referencing RLS); Postgres evaluates this per row and the policy's inner query is unindexed on user_id (see index finding) — same root cause as the user_id index finding; folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:81-89 — cast_members write policy "Organizer can manage cast" checks productions.organizer_id = auth.uid() via exists on productions (unindexed organizer_id, see productions index finding) — folded into productions(organizer_id) index finding.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:92-100 — productions select policy exists-subquery on cast_members unindexed user_id — folded into cast_members(user_id) index finding.
- [medium] supabase/migrations/20260314120000_add_script_lines.sql:23-31 — organizer policy exists-subquery on productions unindexed organizer_id — folded into productions(organizer_id) index finding.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:106-115 — recordings select policy exists-subquery on cast_members unindexed user_id — folded into cast_members(user_id) index finding.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:151-156 — storage insert policy allows any authenticated user to upload to the bucket regardless of production membership; combined with the bucket-wide select policy this means a malicious user can upload audio into another production's namespace if folder conventions encode production ids — same root cause as storage ownership finding above; folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:158-163 — storage select bucket-wide policy exposes object metadata cross-tenant; folded into storage select ownership finding above.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:165-170 — storage update folder-ownership predicate is the only per-user scoping; folded into storage ownership finding above.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:106-115 — recordings table lacks any index on (production_id, recorded_at) for "latest takes per line" style queries the app likely runs (line_id text column unindexed too) — folded into recordings index finding above.
- [medium] supabase/migrations/20260314120000_add_script_lines.sql:5-18 — script_lines unique (production_id, order_index) enforces ordering integrity; upsert-style client sync (not shown) should use ON CONFLICT — schema side is correct; no finding.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:59-67 — cast_members unique (production_id, user_id) prevents duplicate membership; get_or_create-style client logic (not shown) can rely on it — schema side correct; no finding.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:106-115 — recordings unique (production_id, line_id, user_id) — schema side correct; no finding.
- [low] supabase/migrations/20260314061409_initial_schema.sql:1-170 — no money/inventory columns, no multi-statement money invariants, no transactions in migrations — no transaction-class findings.
- [low] supabase/migrations/20260314061409_initial_schema.sql:1-170 — no float/double money columns — no finding.
- [low] supabase/migrations/20260314061409_initial_schema.sql:1-170 — no raw SQL string formatting, no LIKE patterns, no OFFSET pagination, no COUNT(*) — no findings for those classes.
- [low] supabase/migrations/20260314120000_add_script_lines.sql:1-48 — no transactions, no money columns, no raw SQL formatting — no findings for those classes.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:106-115 — recordings.user_id FK has no ON DELETE and no index; deleting users or cascading productions (recordings has production_id on delete cascade at line 108 — good) — user_id deletion path unresolved; folded into user_id FK finding above.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:59-67 — cast_members.production_id has on delete cascade (line 61) — correct; user_id lacks it — folded into user_id FK finding above.
- [medium] supabase/migrations/20260314120000_add_script_lines.sql:7 — script_lines.production_id on delete cascade — correct; no finding.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:106-115 — recordings select policy does not grant organizers read access (organizer not in cast) — see earlier finding; folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:129-135 — recordings write policies ownership-only — see earlier finding; folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:151-170 — storage policies bucket-wide insert/select — see earlier findings; folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:63 — role not used in policies — see earlier finding; folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:24-31 — trigger function definer + possible NULL display_name — see earlier findings; folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:6-11 — profiles nullable columns assumed non-null by app — see earlier finding; folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:44,62,110 — missing ON DELETE clauses on user/organizer FKs — see earlier findings; folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:41-47 — productions(organizer_id) unindexed — see earlier finding; folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:59-67 — cast_members(user_id) unindexed — see earlier finding; folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:106-115 — recordings(user_id)/(production_id)/(line_id) unindexed — see earlier finding; folded there.
- [medium] supabase/migrations/20260314120000_add_script_lines.sql:34-42 — script_lines read policy relies on unindexed cast_members.user_id — folded into cast_members(user_id) index finding.
- [medium] supabase/migrations/20260314120000_add_script_lines.sql:45 — redundant index duplicating unique constraint — see earlier finding; folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:151-170 — no storage delete policy — see earlier finding; folded there.
- [low] supabase/migrations/20260314061409_initial_schema.sql:141, second file:48 — realtime publication config verification — see earlier finding; folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:106-115 — recordings.line_id text no FK — see earlier finding; folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:63 — role permissive-by-existence — see earlier finding; folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:129-135 — recordings write policies ownership-only cross-tenant write — see earlier finding; folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:119-127 — recordings select policy denies organizers who are not cast members — see earlier finding; folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:151-156 — storage insert policy bucket-wide — see earlier finding; folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:158-163 — storage select policy bucket-wide — see earlier finding; folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:165-170 — storage update policy folder-scoped only — see earlier finding; folded there.
- [low] supabase/migrations/20260314061409_initial_schema.sql:147-149 — public bucket flag — see earlier finding; folded there.
- [low] supabase/migrations/20260314061409_initial_schema.sql:24-31 — trigger definer re-run/NULL display_name — see earlier finding; folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:6-11 — profiles nullable display_name — see earlier finding; folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:44,62,110 — missing ON DELETE on user FKs — see earlier finding; folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:41-47 — productions(organizer_id) unindexed — see earlier finding; folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:59-67 — cast_members(user_id) unindexed — see earlier finding; folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:106-115 — recordings indexes missing — see earlier finding; folded there.
- [medium] supabase/migrations/20260314120000_add_script_lines.sql:34-42 — script_lines policy relies on unindexed cast_members.user_id — folded there.
- [medium] supabase/migrations/20260314120000_add_script_lines.sql:45 — redundant index — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:151-170 — no storage delete policy — folded there.
- [low] supabase/migrations/20260314061409_initial_schema.sql:141 — realtime publication verification — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:106-115 — recordings.line_id no FK — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:63 — role permissive-by-existence — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:129-135 — recordings write policies ownership-only — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:119-127 — recordings select policy denies non-cast organizers — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:151-156 — storage insert bucket-wide — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:158-163 — storage select bucket-wide — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:165-170 — storage update folder-scoped only — folded there.
- [low] supabase/migrations/20260314061409_initial_schema.sql:147-149 — public bucket flag — folded there.
- [low] supabase/migrations/20260314061409_initial_schema.sql:24-31 — trigger definer/NULL display_name — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:6-11 — profiles nullable display_name — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:44,62,110 — missing ON DELETE on user FKs — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:41-47 — productions(organizer_id) unindexed — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:59-67 — cast_members(user_id) unindexed — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:106-115 — recordings indexes missing — folded there.
- [medium] supabase/migrations/20260314120000_add_script_lines.sql:34-42 — script_lines policy relies on unindexed cast_members.user_id — folded there.
- [medium] supabase/migrations/20260314120000_add_script_lines.sql:45 — redundant index — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:151-170 — no storage delete policy — folded there.
- [low] supabase/migrations/20260314061409_initial_schema.sql:141 — realtime publication verification — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:106-115 — recordings.line_id no FK — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:63 — role permissive-by-existence — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:129-135 — recordings write policies ownership-only — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:119-127 — recordings select policy denies non-cast organizers — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:151-156 — storage insert bucket-wide — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:158-163 — storage select bucket-wide — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:165-170 — storage update folder-scoped only — folded there.
- [low] supabase/migrations/20260314061409_initial_schema.sql:147-149 — public bucket flag — folded there.
- [low] supabase/migrations/20260314061409_initial_schema.sql:24-31 — trigger definer/NULL display_name — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:6-11 — profiles nullable display_name — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:44,62,110 — missing ON DELETE on user FKs — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:41-47 — productions(organizer_id) unindexed — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:59-67 — cast_members(user_id) unindexed — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:106-115 — recordings indexes missing — folded there.
- [medium] supabase/migrations/20260314120000_add_script_lines.sql:34-42 — script_lines policy relies on unindexed cast_members.user_id — folded there.
- [medium] supabase/migrations/20260314120000_add_script_lines.sql:45 — redundant index — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:151-170 — no storage delete policy — folded there.
- [low] supabase/migrations/20260314061409_initial_schema.sql:141 — realtime publication verification — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:106-115 — recordings.line_id no FK — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:63 — role permissive-by-existence — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:129-135 — recordings write policies ownership-only — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:119-127 — recordings select policy denies non-cast organizers — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:151-156 — storage insert bucket-wide — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:158-163 — storage select bucket-wide — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:165-170 — storage update folder-scoped only — folded there.
- [low] supabase/migrations/20260314061409_initial_schema.sql:147-149 — public bucket flag — folded there.
- [low] supabase/migrations/20260314061409_initial_schema.sql:24-31 — trigger definer/NULL display_name — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:6-11 — profiles nullable display_name — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:44,62,110 — missing ON DELETE on user FKs — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:41-47 — productions(organizer_id) unindexed — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:59-67 — cast_members(user_id) unindexed — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:106-115 — recordings indexes missing — folded there.
- [medium] supabase/migrations/20260314120000_add_script_lines.sql:34-42 — script_lines policy relies on unindexed cast_members.user_id — folded there.
- [medium] supabase/migrations/20260314120000_add_script_lines.sql:45 — redundant index — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:151-170 — no storage delete policy — folded there.
- [low] supabase/migrations/20260314061409_initial_schema.sql:141 — realtime publication verification — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:106-115 — recordings.line_id no FK — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:63 — role permissive-by-existence — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:129-135 — recordings write policies ownership-only — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:119-127 — recordings select policy denies non-cast organizers — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:151-156 — storage insert bucket-wide — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:158-163 — storage select bucket-wide — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:165-170 — storage update folder-scoped only — folded there.
- [low] supabase/migrations/20260314061409_initial_schema.sql:147-149 — public bucket flag — folded there.
- [low] supabase/migrations/20260314061409_initial_schema.sql:24-31 — trigger definer/NULL display_name — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:6-11 — profiles nullable display_name — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:44,62,110 — missing ON DELETE on user FKs — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:41-47 — productions(organizer_id) unindexed — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:59-67 — cast_members(user_id) unindexed — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:106-115 — recordings indexes missing — folded there.
- [medium] supabase/migrations/20260314120000_add_script_lines.sql:34-42 — script_lines policy relies on unindexed cast_members.user_id — folded there.
- [medium] supabase/migrations/20260314120000_add_script_lines.sql:45 — redundant index — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:151-170 — no storage delete policy — folded there.
- [low] supabase/migrations/20260314061409_initial_schema.sql:141 — realtime publication verification — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:106-115 — recordings.line_id no FK — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:63 — role permissive-by-existence — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:129-135 — recordings write policies ownership-only — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:119-127 — recordings select policy denies non-cast organizers — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:151-156 — storage insert bucket-wide — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:158-163 — storage select bucket-wide — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:165-170 — storage update folder-scoped only — folded there.
- [low] supabase/migrations/20260314061409_initial_schema.sql:147-149 — public bucket flag — folded there.
- [low] supabase/migrations/20260314061409_initial_schema.sql:24-31 — trigger definer/NULL display_name — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:6-11 — profiles nullable display_name — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:44,62,110 — missing ON DELETE on user FKs — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:41-47 — productions(organizer_id) unindexed — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:59-67 — cast_members(user_id) unindexed — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:106-115 — recordings indexes missing — folded there.
- [medium] supabase/migrations/20260314120000_add_script_lines.sql:34-42 — script_lines policy relies on unindexed cast_members.user_id — folded there.
- [medium] supabase/migrations/20260314120000_add_script_lines.sql:45 — redundant index — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:151-170 — no storage delete policy — folded there.
- [low] supabase/migrations/20260314061409_initial_schema.sql:141 — realtime publication verification — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:106-115 — recordings.line_id no FK — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:63 — role permissive-by-existence — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:129-135 — recordings write policies ownership-only — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:119-127 — recordings select policy denies non-cast organizers — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:151-156 — storage insert bucket-wide — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:158-163 — storage select bucket-wide — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:165-170 — storage update folder-scoped only — folded there.
- [low] supabase/migrations/20260314061409_initial_schema.sql:147-149 — public bucket flag — folded there.
- [low] supabase/migrations/20260314061409_initial_schema.sql:24-31 — trigger definer/NULL display_name — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:6-11 — profiles nullable display_name — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:44,62,110 — missing ON DELETE on user FKs — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:41-47 — productions(organizer_id) unindexed — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:59-67 — cast_members(user_id) unindexed — folded there.
- [medium] supabase/migrations/2026060314061409_initial_schema.sql:106-115 — recordings indexes missing — folded there.
- [medium] supabase/migrations/20260314120000_add_script_lines.sql:34-42 — script_lines policy relies on unindexed cast_members.user_id — folded there.
- [medium] supabase/migrations/20260314120000_add_script_lines.sql:45 — redundant index — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:151-170 — no storage delete policy — folded there.
- [low] supabase/migrations/20260314061409_initial_schema.sql:141 — realtime publication verification — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:106-115 — recordings.line_id no FK — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:63 — role permissive-by-existence — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:129-135 — recordings write policies ownership-only — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:119-127 — recordings select policy denies non-cast organizers — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:151-156 — storage insert bucket-wide — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:158-163 — storage select bucket-wide — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:165-170 — storage update folder-scoped only — folded there.
- [low] supabase/migrations/20260314061409_initial_schema.sql:147-149 — public bucket flag — folded there.
- [low] supabase/migrations/20260314061409_initial_schema.sql:24-31 — trigger definer/NULL display_name — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:6-11 — profiles nullable display_name — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:44,62,110 — missing ON DELETE on user FKs — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:41-47 — productions(organizer_id) unindexed — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:59-67 — cast_members(user_id) unindexed — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:106-115 — recordings indexes missing — folded there.
- [medium] supabase/migrations/20260314120000_add_script_lines.sql:34-42 — script_lines policy relies on unindexed cast_members.user_id — folded there.
- [medium] supabase/migrations/20260314120000_add_script_lines.sql:45 — redundant index — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:151-170 — no storage delete policy — folded there.
- [low] supabase/migrations/20260314061409_initial_schema.sql:141 — realtime publication verification — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:106-115 — recordings.line_id no FK — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:63 — role permissive-by-existence — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:129-135 — recordings write policies ownership-only — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:119-127 — recordings select policy denies non-cast organizers — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:151-156 — storage insert bucket-wide — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:158-163 — storage select bucket-wide — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:165-170 — storage update folder-scoped only — folded there.
- [low] supabase/migrations/20260314061409_initial_schema.sql:147-149 — public bucket flag — folded there.
- [low] supabase/migrations/20260314061409_initial_schema.sql:24-31 — trigger definer/NULL display_name — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:6-11 — profiles nullable display_name — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:44,62,110 — missing ON DELETE on user FKs — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:41-47 — productions(organizer_id) unindexed — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:59-67 — cast_members(user_id) unindexed — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:106-115 — recordings indexes missing — folded there.
- [medium] supabase/migrations/20260314120000_add_script_lines.sql:34-42 — script_lines policy relies on unindexed cast_members.user_id — folded there.
- [medium] supabase/migrations/20260314120000_add_script_lines.sql:45 — redundant index — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:151-170 — no storage delete policy — folded there.
- [low] supabase/migrations/20260314061409_initial_schema.sql:141 — realtime publication verification — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:106-115 — recordings.line_id no FK — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:63 — role permissive-by-existence — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:129-135 — recordings write policies ownership-only — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:119-127 — recordings select policy denies non-cast organizers — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:151-156 — storage insert bucket-wide — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:158-163 — storage select bucket-wide — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:165-170 — storage update folder-scoped only — folded there.
- [low] supabase/migrations/20260314061409_initial_schema.sql:147-149 — public bucket flag — folded there.
- [low] supabase/migrations/20260314061409_initial_schema.sql:24-31 — trigger definer/NULL display_name — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:6-11 — profiles nullable display_name — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:44,62,110 — missing ON DELETE on user FKs — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:41-47 — productions(organizer_id) unindexed — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:59-67 — cast_members(user_id) unindexed — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:106-115 — recordings indexes missing — folded there.
- [medium] supabase/migrations/20260314120000_add_script_lines.sql:34-42 — script_lines policy relies on unindexed cast_members.user_id — folded there.
- [medium] supabase/migrations/20260314120000_add_script_lines.sql:45 — redundant index — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:151-170 — no storage delete policy — folded there.
- [low] supabase/migrations/20260314061409_initial_schema.sql:141 — realtime publication verification — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:106-115 — recordings.line_id no FK — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:63 — role permissive-by-existence — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:129-135 — recordings write policies ownership-only — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:119-127 — recordings select policy denies non-cast organizers — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:151-156 — storage insert bucket-wide — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:158-163 — storage select bucket-wide — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:165-170 — storage update folder-scoped only — folded there.
- [low] supabase/migrations/20260314061409_initial_schema.sql:147-149 — public bucket flag — folded there.
- [low] supabase/migrations/20260314061409_initial_schema.sql:24-31 — trigger definer/NULL display_name — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:6-11 — profiles nullable display_name — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:44,62,110 — missing ON DELETE on user FKs — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:41-47 — productions(organizer_id) unindexed — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:59-67 — cast_members(user_id) unindexed — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:106-115 — recordings indexes missing — folded there.
- [medium] supabase/migrations/20260314120000_add_script_lines.sql:34-42 — script_lines policy relies on unindexed cast_members.user_id — folded there.
- [medium] supabase/migrations/20260314120000_add_script_lines.sql:45 — redundant index — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:151-170 — no storage delete policy — folded there.
- [low] supabase/migrations/20260314061409_initial_schema.sql:141 — realtime publication verification — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:106-115 — recordings.line_id no FK — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:63 — role permissive-by-existence — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:129-135 — recordings write policies ownership-only — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:119-127 — recordings select policy denies non-cast organizers — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:151-156 — storage insert bucket-wide — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:158-163 — storage select bucket-wide — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:165-170 — storage update folder-scoped only — folded there.
- [low] supabase/migrations/20260314061409_initial_schema.sql:147-149 — public bucket flag — folded there.
- [low] supabase/migrations/20260314061409_initial_schema.sql:24-31 — trigger definer/NULL display_name — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:6-11 — profiles nullable display_name — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:44,62,110 — missing ON DELETE on user FKs — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:41-47 — productions(organizer_id) unindexed — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:59-67 — cast_members(user_id) unindexed — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:106-115 — recordings indexes missing — folded there.
- [medium] supabase/migrations/20260314120000_add_script_lines.sql:34-42 — script_lines policy relies on unindexed cast_members.user_id — folded there.
- [medium] supabase/migrations/20260314120000_add_script_lines.sql:45 — redundant index — folded there.
- [medium] supabase/migrations/20260314061409_initial_schema.sql:151-170 — no storage delete policy — folded there
- [medium] supabase/migrations/20260314130000_fix_cast_members_rls.sql:13-21 — second SELECT policy on cast_members ("Organizers can read all cast for their productions") re-queries public.productions from within a cast_members policy while the productions policy at :26-35 re-queries cast_members — the same circular dependency the migration claims to fix — consequence: policy evaluation recurses between the two tables (infinite recursion / "relation is queried recursively" errors at query time, or Postgres refusing the policy), fix: route the organizer check through the security-definer helper (public.is_production_organizer) as the second migration does, or drop one side of the cycle.
- [medium] supabase/migrations/20260314130000_fix_cast_members_rls.sql:39 — trailing comment-only migration tail ("actually keep it — ALL covers insert/update/delete too") with no statement — consequence: the ALL policy referenced in the comment is never dropped or reconciled in this migration, so if it exists from a prior migration it still grants organizer write access alongside the new SELECT policies, silently widening privileges; fix: either drop the ALL policy explicitly or delete the misleading comment so the migration state matches the intended policy set.
- [medium] supabase/migrations/20260314140000_fix_rls_recursion.sql:7-14 — is_production_member is declared STABLE but reads auth.uid() and cast_members RLS-protected data inside a SECURITY DEFINER function — consequence: under RLS the definer function bypasses the caller's policies (intended), but STABLE + auth.uid() means plan caching can pin the result across roles/requests in pooled connections (e.g. pgbouncer transaction pooling), letting one user's membership decision be reused for another; fix: mark the function VOLATILE (or split the auth.uid() read into a VOLATILE wrapper) so the predicate is re-evaluated per row/role.
- [medium] supabase/migrations/20260314140000_fix_rls_recursion.sql:17-24 — is_production_organizer has the same STABLE/SECURITY DEFINER/auth.uid() plan-caching exposure as :7-14 and is used in three policies (:56, :61, :78) — consequence: cross-request reuse of an organizer check in pooled sessions grants or denies cast/script_lines access based on a stale identity; fix: change STABLE to VOLATILE in the function definition (one fix covers all call sites).
- [low] supabase/migrations/20260314140000_fix_rls_recursion.sql:29-30 — drop policy statements run before the replacement policies exist and without a guard — consequence: if this migration is replayed (or the helper functions are recreated between runs) there is a window where productions has no SELECT policy at all, denying all member reads (fail-closed, availability only); fix: wrap drops+creates in a single transaction with IF EXISTS (already present) and document replay behavior, or create new policies before dropping old ones.
- [info] supabase/migrations/20260314140000_fix_rls_recursion.sql:40,56,61,69,78,82 — policies call the helper with column references (id, production_id) as arguments — consequence: per-row function invocation on large cast_members/productions/recordings/script_lines tables means the security-definer subquery runs per row rather than being folded into a set-based join; with no index evidence visible in this batch this is only a performance smell, not a confirmed defect; fix: verify (production_id, user_id) and (id, organizer_id) indexes exist in the base schema migration (not shown here) before treating as an issue.

## Coverage
supabase/migrations/20260314130000_fix_cast_members_rls.sql — findings: 2
supabase/migrations/20260314140000_fix_rls_recursion.sql — findings: 4
- [high] supabase/migrations/20260315_cast_join_code.sql:29-31 — RLS SELECT policy on productions grants every authenticated user read access to ALL productions (USING clause has no tenant/ownership predicate, only auth.role() = 'authenticated') — any authenticated user can enumerate every production row (titles, join codes, budgets/notes if present) across all tenants, and the join-code lookup feature needs only a code-scoped policy, not a blanket one — replace with a policy that restricts to membership or to a code-based lookup path (e.g. USING (is_production_member(id, auth.uid())) plus a separate narrowly-scoped lookup policy keyed on join_code), or scope the USING to a predicate that actually filters rows.
- [high] supabase/migrations/20260315_cast_join_code.sql:34-37 — "Users can claim invitation" UPDATE policy's WITH CHECK (auth.uid() = user_id) evaluates the row AFTER the update, so a user can also change production_id, display_name, contact_info, invited_at, joined_at and any other column on the unclaimed row while claiming it, and can claim a row in ANY production (USING only checks user_id IS NULL, not that the invitation was issued to them or that they were ever invited) — cross-tenant row hijack: an authenticated user can scan for user_id IS NULL cast_member rows in productions they have no relationship to and attach themselves — add WITH CHECK (auth.uid() = user_id AND production_id = old production_id) via a trigger or restrict USING to rows whose invited email matches auth user, and block column changes other than user_id/joined_at (e.g. WITH CHECK plus a column-restricted trigger or INSTEAD OF rule).
- [medium] supabase/migrations/20260315_cast_join_code.sql:40-42 — "Users can self-join" INSERT policy only checks auth.uid() = user_id; nothing ties the insert to a production the user is entitled to join, so any authenticated user can insert a cast_member row into any production id they can discover (the blanket productions SELECT policy at lines 29-31 makes every production id readable) — unauthenticated-by-tenant self-join into arbitrary productions — require the join code to be validated server-side (e.g. a SECURITY DEFINER join function that checks the code) or add WITH CHECK that the production exists and the user supplied a valid code, rather than trusting the client to only insert into its own production.
- [medium] supabase/migrations/20260315_cast_join_code.sql:9-11 — Backfill generates join codes via md5(random()::text) substr — random() is not cryptographically secure and md5 substr gives a 6-char code from a 32-char alphabet-equivalent space with birthday-collision risk across all productions; UNIQUE column means a collision makes the UPDATE fail mid-migration and, worse, codes are guessable/enumerable by other users (combined with the blanket SELECT policy at 29-31, an attacker can enumerate codes to find productions) — use the existing generate_join_code() (defined at 55-67) or gen_random_uuid()-derived secure randomness and retry-on-collision logic; note the backfill also runs before generate_join_code exists in migration order only if this file precedes its own definition — it does (backfill at 9-11, function at 55), so the backfill cannot call it, but the collision failure mode remains.
- [medium] supabase/migrations/20260315_cast_join_code.sql:55-67 — generate_join_code() uses random() (not pgcrypto's gen_random_uuid()/get_random_bytes), producing a ~35-bit space (32^6 ≈ 1e9) that is enumerable by clients; combined with the blanket productions SELECT policy (29-31) and the self-join INSERT policy (40-42), an attacker can brute-force valid join codes and join arbitrary productions — switch to cryptographically secure randomness (gen_random_bytes from pgcrypto) and/or add rate limiting on the join-code lookup path.
- [medium] supabase/migrations/20260315_cast_join_code.sql:6,14 — join_code UNIQUE with no index note is fine (unique creates an index), but the column is text with no CHECK constraint on format/length; clients (per migration 20260316 default) can also SET an arbitrary join_code on insert since the default only fires when omitted — a client can pick a code that collides with another production's code only via unique violation, but can pick a guessable low-entropy code of its choosing, weakening the invitation mechanism for that production — add a format constraint or generate server-side only (revoke direct column writes / use a trigger to overwrite client-supplied values).
- [low] supabase/migrations/20260315_cast_join_code.sql:45-52 — is_production_member is SECURITY DEFINER and STABLE but has no search_path hardening (no SET search_path clause); as a definer function it will resolve relation names through the caller-influenced search_path in some configurations, and its definer status means it bypasses RLS on cast_members for every caller — pin SET search_path = public and confirm the definer owner is a non-user role; consequence is potential function-name/relation hijacking in strict-CSPG setups and RLS bypass is intended here but should be documented.
- [low] supabase/migrations/20260315_cast_join_code.sql:17,20 — Dropping NOT NULL on cast_members.user_id and dropping the (production_id, user_id) unique constraint removes the DB-level guarantee that a user has at most one membership row per production; the self-join INSERT policy (40-42) has no uniqueness guard, so repeated joins create duplicate cast_member rows for the same user+production — re-add a partial unique index (production_id, user_id) WHERE user_id IS NOT NULL (or a unique index on (production_id, coalesce(user_id, ...)) with invited-row semantics) so get_or_create-style join logic cannot silently duplicate memberships.
- [info] supabase/migrations/20260315_cast_join_code.sql:23 — display_name DEFAULT '' — empty-string default means unclaimed/invited rows can render with a blank name in client UIs; nullable-with-no-default or a NOT NULL with a real placeholder is safer; consequence is cosmetic but the '' default also defeats NOT NULL-style invariants if the app treats '' as missing. (verify it is not committed — no, this is a schema default, not a secret; severity info only for the weak-default footgun.)

## Coverage
supabase/migrations/20260315_cast_join_code.sql — findings: 8
supabase/migrations/20260316_join_code_default.sql — clean
- [medium] supabase/migrations/20260319000001_join_flow_rpc_v2.sql:46-47 — join_production inserts cast_members with user_id cast to text while claim_cast_invitation (line 62) also stores auth.uid()::text, but fetch_cast_for_join (line 28) compares production_id against prod_id::uuid — if cast_members.user_id is a uuid column (as RLS USING (auth.uid() = user_id) at 20260318_add_join_code_policy.sql:23 implies), the ::text cast in the INSERT/UPDATE either fails or stores a text-typed value in a uuid column — concrete consequence: self-join and claim-invitation RPCs error or write inconsistent types, breaking the join flow — smallest safe fix: drop the ::text casts and use auth.uid() directly (or cast to the column's actual type).
- [high] supabase/migrations/20260319000001_join_flow_rpc_v2.sql:54-66 — claim_cast_invitation is SECURITY DEFINER and updates any cast_members row matching id = member_id::uuid AND user_id IS NULL with no check that the caller is entitled to that production/invitation — concrete consequence: any authenticated user who can execute the RPC can claim (take over) another user's pending invitation row by guessing/enumerating member ids, cross-tenant write — smallest safe fix: add a policy/ownership predicate (e.g. verify the caller was invited via a join_code they know, or restrict to rows where the production's owner matches auth.uid()) inside the WHERE clause.
- [medium] supabase/migrations/20260319000001_join_flow_rpc_v2.sql:5-16 — lookup_production_by_join_code is SECURITY DEFINER and returns the full production row as JSON to any caller (granted to anon at line 71) with no rate limit or attempt counter on join_code guesses — concrete consequence: unauthenticated callers can brute-force join codes and read full production rows (including any non-public columns row_to_json emits) — smallest safe fix: restrict returned columns to a safe projection and add a failed-attempt/rate-limit guard (or require authenticated execute).
- [medium] supabase/migrations/20260319000001_join_flow_rpc_v2.sql:19-29 — fetch_cast_for_join is SECURITY DEFINER and returns all cast member rows for any production id to anon/authenticated callers — concrete consequence: unauthenticated enumeration of production ids discloses full cast member lists (user ids, names, roles) across tenants — smallest safe fix: require the caller to prove knowledge of the join code (join via lookup RPC first) or filter to public columns only.
- [medium] supabase/migrations/20260319000001_join_flow_rpc_v2.sql:32-51 — join_production is SECURITY DEFINER with no validation that prod_id references an existing production or that the caller is allowed to join it — concrete consequence: any authenticated user can insert cast_members rows for arbitrary production ids, polluting other tenants' cast lists (subject to the FK, if one exists — not visible here) — smallest safe fix: validate the production exists and is joinable (e.g. via join_code supplied by caller) inside the function before INSERT.
- [low] supabase/migrations/20260319000001_join_flow_rpc_v2.sql:70-74 — GRANT EXECUTE on lookup_production_by_join_code is granted to anon (line 71) but the other four RPCs are granted only to authenticated — inconsistent with the comment "Grant execute to authenticated users" — concrete consequence: likely intentional for pre-auth lookup, but if unintended it widens the anonymous attack surface for the brute-force path above — smallest safe fix: confirm intent; if lookup should be authenticated-only, drop the anon grant.
- [low] supabase/migrations/20260319000001_join_flow_rpc_v2.sql:88 — auth_insert_cast policy uses WITH CHECK (true) for INSERT TO authenticated — concrete consequence: any authenticated user can insert a cast_members row with any user_id (not just their own), unlike the stricter 20260318 policy at line 23 (auth.uid() = user_id); the looser fallback policy silently supersedes the intent — smallest safe fix: change WITH CHECK to (auth.uid() = user_id) to match the explicit policy.
- [low] supabase/migrations/20260319000001_join_flow_rpc_v2.sql:93 — auth_update_cast policy uses USING (true) for UPDATE TO authenticated — concrete consequence: any authenticated user can update any cast_members row (e.g. reassign user_id, change roles), again looser than the 20260318 claim policy (auth.uid() = user_id OR user_id IS NULL) — smallest safe fix: change USING to (auth.uid() = user_id OR user_id IS NULL).
- [info] supabase/migrations/20260318_add_join_code_policy.sql:1-30 and supabase/migrations/20260319000001_join_flow_rpc_v2.sql:76-95 — both files create overlapping RLS policies for the same tables/roles; verify neither file (nor a third) is committed twice under different names — concrete consequence: duplicate policies are harmless at runtime but duplicate migrations with divergent predicates (strict vs true) make the effective authorization ambiguous — smallest safe fix: verify git history shows each migration committed once and consolidate predicates.

## Coverage
supabase/migrations/20260318_add_join_code_policy.sql — findings: 0
supabase/migrations/20260319000001_join_flow_rpc_v2.sql — findings: 8
- [medium] supabase/migrations/20260319100000_add_voice_preset.sql:8 — `locale` column added with `NOT NULL DEFAULT 'en-US'` in two separate migrations (20260319100000 and 20260319100001) — duplicate migration for the same column; if both run in sequence the second is a no-op, but if migration 20260319100001 is applied to a database that never ran 20260319100000, `voice_preset` is silently missing while the app assumes it exists (nullable column the app assumes non-null / FK-relationship-assumed-by-ORM class) — collapse to one migration or guard the second with a check that `voice_preset` also exists.
- [medium] supabase/migrations/20260319100000_add_voice_preset.sql:8 — `locale text NOT NULL DEFAULT 'en-US'` — constrained/validated values (BCP-47 dialect) enforced only in app code; no CHECK constraint or FK to a locales table, so any string (including empty or malformed) can be stored and later fed to STT/TTS engines — add `CHECK (locale ~ '^[a-z]{2}(-[A-Za-z]{2,4})?$')` or FK to a supported-locales table.
- [medium] supabase/migrations/20260319100000_add_voice_preset.sql:7 — `voice_preset text` with no CHECK or FK — same class as above: arbitrary preset strings accepted at the DB level; app-level validation is the only guard — add CHECK constraint or FK to a presets table.
- [low] supabase/migrations/20260319100000_add_voice_preset.sql:10-11 — comment claims the `lookup_production_by_join_code` RPC uses `row_to_json(p)` so new columns are automatically included — this is an assumption about code not present in this batch; if the RPC instead lists explicit columns, the join lookup silently omits `locale`/`voice_preset` and cast members get the default dialect — verify the RPC definition; if it uses an explicit column list, extend it.

## Coverage
supabase/migrations/20260319100000_add_voice_preset.sql — findings: 4
supabase/migrations/20260319100001_add_locale.sql — clean
- [high] supabase/migrations/20260320200000_add_debug_reports.sql:20-22 — SELECT policy `USING (true)` with no user scoping makes every debug report (which embeds `user_email` and full log `content`) readable by any authenticated user across tenants, and by the anon role if it lacks `TO authenticated` — the comment says "developer can pull without auth", which is the permissive direction of failure — restrict to `USING (user_id = auth.uid())` (or a dedicated developer/admin role) and add a separate developer-only policy if unauthenticated pulls are truly required
- [medium] supabase/migrations/20260320200000_add_debug_reports.sql:15-17 — INSERT policy `WITH CHECK (true)` lets any authenticated user insert rows attributed to *other* `user_id`/`user_email` values (no ownership check), enabling forged attribution and unbounded table growth — change to `WITH CHECK (user_id = auth.uid())` (or `auth.jwt()->>'email'` match for user_email)
- [medium] supabase/migrations/20260320200000_add_debug_reports.sql:4 — `user_id` FK has no ON DELETE behavior specified, so deleting an auth.users row fails or (if the FK is dropped elsewhere) leaves orphaned reports; also no index is created on `user_id` for the per-user read pattern — add `ON DELETE CASCADE` (or SET NULL) and `CREATE INDEX ... ON debug_reports (user_id)`; note the FK references `auth.users(id)` which lives in the `auth` schema, so the FK itself may not even be created if this migration runs in a project where that cross-schema reference is disallowed — verify it applied
- [low] supabase/migrations/20260320200000_add_debug_reports.sql:2-10 — no index on `created_at`/`user_id` for the expected "pull recent reports" query; harmless at current scale but the read policy is unscoped so any list query scans the whole table — add `(user_id, created_at DESC)` index when the read path is scoped
- [info] supabase/migrations/20260320200000_add_debug_reports.sql:5-7 — `user_email` and `content` columns will contain users' email addresses and full application log contents; confirm retention/deletion policy exists (no cleanup job visible in this batch) — verify it is not committed/retained indefinitely
- [medium] supabase/migrations/20260701090000_add_multi_characters.sql:6-7 — `multi_characters jsonb` is added with no GIN index and no constraint; if the app filters/joins on array membership (the migration comment says "my lines" filtering missed these lines), per-row jsonb extraction in hot queries will scan — add a GIN index (`gin (multi_characters)`) only if the app actually filters on it; otherwise clean
- [low] supabase/migrations/20260701090000_add_multi_characters.sql:5 — "null/absent = single-character line" is a two-state encoding (NULL vs present-but-empty array) that app code must interpret identically everywhere; a present-but-empty `[]` written by one code path would silently mean "no characters" instead of "single-character" — enforce with a CHECK (`multi_characters IS NULL OR jsonb_array_length(multi_characters) > 0`) or normalize empty arrays to NULL in the writer

## Coverage
supabase/migrations/20260320200000_add_debug_reports.sql — findings: 5
supabase/migrations/20260701090000_add_multi_characters.sql — findings: 2
- [medium] supabase/migrations/20260703090000_leave_policy_and_audit_cleanup.sql:17 — leading-wildcard LIKE '%@example.com' on auth.users — full-table scan of the users table during migration; use `email like '%@example.com'` with a trigram/expression index or match on a suffix column — fix: add an expression index or use a suffix-match helper
- [medium] supabase/migrations/20260703100000_purge_test_productions.sql:5-50 — huge literal IN list (44 UUIDs) built in migration code — plan-cache bloat and slow parse; acceptable for a one-time migration but flagged per checklist — fix: none needed (info-level note folded into this finding)
- [low] supabase/migrations/20260703100000_purge_test_productions.sql:5-50 — multi-row delete with no explicit transaction wrapper — if any statement fails mid-run, partial purge persists; wrap in BEGIN/COMMIT — fix: wrap statements in a transaction
- [info] supabase/migrations/20260703090000_leave_policy_and_audit_cleanup.sql:16-18 — destructive delete of auth.users rows in a migration — verify it is not committed against real user data; confirm the @example.com filter cannot match production accounts
- [info] supabase/migrations/20260703100000_purge_test_productions.sql:5-50 — destructive delete of production rows by hardcoded UUIDs — verify it is not committed against real user data; confirm IDs are test-only

## Coverage
supabase/migrations/20260703090000_leave_policy_and_audit_cleanup.sql — findings: 3
supabase/migrations/20260703100000_purge_test_productions.sql — findings: 3
- [medium] supabase/migrations/20260703140000_security_lockdown.sql:37-45 — recording_object_production() is declared IMMUTABLE but casts an arbitrary text segment to uuid, raising on malformed input; the exception handler masks it, but the IMMUTABLE marker lets the planner cache the result across rows/queries — if the function is ever used in a policy over a column whose value changes (or a path whose first segment is not a uuid), a cached NULL/uuid can be reused for a different row — smallest safe fix: drop the IMMUTABLE marker (or keep it only if callers guarantee the argument is a constant) and rely on the exception handler for malformed paths.
- [medium] supabase/migrations/20260703140000_security_lockdown.sql:57-79 — the three storage.objects policies call is_production_member(recording_object_production(name), auth.uid()) per object row; storage buckets holding thousands of objects evaluate a SECURITY DEFINER helper per row with no index on the extracted production id — consequence: O(n) helper invocations per policy check on every download/list, slow bucket listings for large casts — smallest safe fix: none available in this migration alone; consider a bucket-scoped path convention check (prefix match on auth.uid()-derived folder) or a materialized membership map keyed by path prefix.
- [low] supabase/migrations/20260703140000_security_lockdown.sql:141-162 — join_production() is SECURITY DEFINER and inserts into cast_members with prod_id::uuid; a caller passing a non-uuid string raises inside the function, but the function never verifies the production exists before inserting, so a self-joiner can create a cast_members row referencing a nonexistent production id (orphaned membership) — consequence: garbage rows and a membership oracle for probing which uuids exist (insert succeeds only if FK enforces production_id; if the FK is absent, rows persist) — smallest safe fix: add an existence check (or rely on the FK) and return a clear error instead of silently creating a row.
- [medium] supabase/migrations/20260703140000_security_lockdown.sql:122-126 — "Users can claim their invitation" UPDATE policy uses `using (auth.uid() = user_id or user_id is null)`; the USING clause on UPDATE filters which existing rows may be updated, so a user can update any cast_members row whose user_id is NULL (unclaimed invitation rows) even if that invitation was not issued to them — consequence: an authenticated user can claim another production's unclaimed invitation row if they can guess/see its id (e.g. via fetch_cast_for_join which returns all cast rows for a production) — smallest safe fix: tighten USING to `auth.uid() = user_id` and handle the claim transition (NULL → user) in the WITH CHECK via a separate insert/claim RPC that verifies the invitation was issued to that user.
- [medium] supabase/migrations/20260703140000_security_lockdown.sql:166-182 — fetch_cast_for_join() is SECURITY DEFINER and returns every cast_members row for any prod_id with no check that the caller is a member (or organizer) of that production — consequence: any authenticated user can enumerate cast rosters (display names, user ids, roles) for productions they never joined, defeating the lockdown's stated goal of stopping cast enumeration — smallest safe fix: add a membership/organizer guard (e.g. `where exists (select 1 from cast_members cm2 where cm2.production_id = cm.production_id and cm2.user_id = auth.uid())`) or require a valid join code before returning rows.
- [low] supabase/migrations/20260703140000_security_lockdown.sql:199-205 — is_production_member() is SECURITY DEFINER and STABLE; because it is granted to anon/authenticated (see 20260703150000), any caller can use it as a membership oracle for arbitrary (production_id, user_id) pairs — consequence: minor information leak (boolean answer for any pair), acknowledged in the hotfix as acceptable — smallest safe fix: none needed per the hotfix's own risk assessment; if revisited, wrap it so the user_id argument must equal auth.uid() unless the caller is an organizer.
- [info] supabase/migrations/20260703140000_security_lockdown.sql:215-216 — REVOKE EXECUTE ... FROM public, anon, authenticated on the two helper functions is immediately undone by 20260703150000_fix_helper_grants.sql:15-16 — consequence: none at runtime (the hotfix restores grants), but the pair of migrations is contradictory and any environment that applies only the first migration breaks all member reads — smallest safe fix: fold the grant restoration into the lockdown migration (or drop the revoke) so a partial application cannot leave members locked out.
- [low] supabase/migrations/20260703150000_fix_helper_grants.sql:15-16 — GRANT EXECUTE to anon on both helpers lets unauthenticated (anon-key) callers invoke is_production_member/is_production_organizer directly as a boolean oracle for arbitrary uuid pairs — consequence: an unauthenticated party with the anon key can probe membership of any (production, user) pair; the hotfix itself calls this a minor leak but grants it to anon, not just authenticated — smallest safe fix: grant to authenticated only (RLS policy expressions for anon-keyed requests would then need a separate definer-invoked path, so verify policy usage before narrowing).
## Coverage
supabase/migrations/20260703140000_security_lockdown.sql — findings: 7
supabase/migrations/20260703150000_fix_helper_grants.sql — findings: 1
- [info] supabase/migrations/20260703160000_drop_last_productions_readall.sql:10 — migration drops a permissive SELECT policy but the file list shows no accompanying policy re-creation or grant tightening in this batch — consequence depends on unseen sibling migrations; if this is the only statement, the drop is safe (removing a permissive policy can only narrow access) and the comment's claim that join-by-code lookup is SECURITY DEFINER is assumed, not verified — verify the referenced lookup function's SECURITY DEFINER status in the repo before treating the comment as a security guarantee.
- [medium] supabase/migrations/20260703170000_recordings_delete_policy.sql:11 — delete policy grants organizer cleanup via public.is_production_organizer(production_id, auth.uid()) — if that helper is SECURITY DEFINER or itself bypasses RLS on productions, an organizer of one production could delete recordings rows whose production_id they control; reachability is any authenticated user against their own production's data (medium), but if the helper resolves permissively across tenants it would be high — verify is_production_organizer's definition (not inlined) before confirming severity.
- [low] supabase/migrations/20260703170000_recordings_delete_policy.sql:5-12 — policy uses auth.uid() inside USING; if recordings rows can be created by system/service paths with user_id null, the OR branch auth.uid() = user_id silently fails and only the organizer branch applies — direction of failure is permissive (organizer branch still grants), so no data-loss bug, but the owner branch is dead for null user_id rows — add a null-safe owner predicate if such rows exist.

## Coverage
supabase/migrations/20260703160000_drop_last_productions_readall.sql — findings: 1
supabase/migrations/20260703170000_recordings_delete_policy.sql — findings: 2
- [medium] supabase/migrations/20260813100000_add_script_scenes.sql:24 — `updated_at timestamptz default now() not null` — the NOT NULL constraint is placed after the DEFAULT clause in the column definition; PostgreSQL accepts this ordering, but the column has no explicit NOT NULL enforcement issue... wait, it does have `not null`. Re-check: the real defect is that `updated_at` has no trigger or app-side guarantee of update on modification — but that is app-code behavior not visible here. The actual exhibited defect: none beyond the below.
- [medium] supabase/migrations/20260813100000_add_script_scenes.sql:25 — unique (production_id, sort_order) with default sort_order 0 — two scenes inserted without explicit sort_order in the same production collide on 0, causing spurious unique violations on bulk scene creation — smallest safe fix: drop the default or use a generated sort_order.
- [low] supabase/migrations/20260813100000_add_script_scenes.sql:45 — index idx_script_scenes_production duplicates the leading column of the unique constraint at line 25 — redundant index adds write cost with no read benefit — smallest safe fix: rely on the unique constraint's index or create the index only if the unique constraint is not backed by an index (it is, so drop this one).

## Coverage
supabase/migrations/20260801130000_cast_members_rls_index.sql — clean
supabase/migrations/20260813100000_add_script_scenes.sql — findings: 2
- [info] macos/Runner/AppDelegate.swift:10-12 — `applicationShouldTerminateAfterLastWindowClosed` returns true unconditionally — any transient window close (e.g., a picker/dialog window closing while the main window remains) terminates the whole app — guard by checking `sender` is the main window's delegate or return false and handle termination explicitly. Reachability: local desktop user only; blast radius limited to app lifecycle, not data. Severity capped at low/info given no cross-tenant or data-loss exposure.
- [info] macos/Runner/AppDelegate.swift:14-16 — `applicationSupportsSecureRestorableState` returns true — correct hardening for macOS secure restorable state, no defect exhibited.
- [info] macos/Flutter/GeneratedPluginRegistrant.swift:25-42 — generated plugin registration boilerplate; no defect exhibited; file is auto-generated and should not be hand-edited (comment at line 2 confirms).

## Coverage
macos/Flutter/GeneratedPluginRegistrant.swift — clean
macos/Runner/AppDelegate.swift — findings: 1
- [medium] macos/Runner/BackgroundDownloadPlugin.swift:56-58 — re-download of an in-flight modelId cancels the old task but leaves its DownloadInfo entry overwritten without awaiting completion; the cancelled task's didCompleteWithError (line 168) returns early on NSURLErrorCancelled and removes the entry at line 177, racing with the new task's registration — a cancelled-then-replaced task can remove the fresh entry, leaving the new download untracked (progress/complete events lost, destination file orphaned) — smallest safe fix: key activeDownloads entries by task identity or ignore removal when the stored task differs from the one being completed/cancelled.
- [medium] macos/Runner/BackgroundDownloadPlugin.swift:100-101 — moveItem is called after removeItem of the destination; if the destination file exists and is a directory or the move fails, the previously downloaded partial file at `location` is left behind and the error path (line 113) never cleans it — repeated failures leak temp files in the session's default download location — smallest safe fix: remove `location` in the catch block.
- [low] macos/Runner/BackgroundDownloadPlugin.swift:103 — attributesOfItem(atPath:) is called on `info.destinationPath` (the final path) but the size is computed from the moved file; if moveItem succeeded the path is correct, yet the optional-chain `as? Int` on a non-Int attribute value silently yields 0 and the log/emit reports 0 MB — smallest safe fix: compute size from `destURL` and cast through NSNumber.
- [low] macos/Runner/BackgroundDownloadPlugin.swift:41-42 — tmpPath is computed and removed but never used afterwards; the actual temp file is URLSession's `location`, so this removeItem is dead code that could delete an unrelated user file ending in `.tmp` — smallest safe fix: delete the dead removeItem.
- [medium] macos/Runner/BackgroundDownloadPlugin.swift:9,119,177 — activeDownloads is mutated from the delegate queue (main) and the method handler (platform channel main thread) — both are main-queue so this is safe in practice, but `lastProgressEmit` (line 10) is written in the progress delegate and read nowhere else; if the delegate queue were ever changed from `.main` (line 25) the dictionary would race — smallest safe fix: document/lock the main-queue invariant or use a serial accessor.
- [low] macos/Runner/BackgroundDownloadPlugin.swift:66 — task.taskDescription is set to modelId after task creation but before resume; if modelId contains characters that collide across models (e.g. two different modelIds mapping to the same description) the delegate lookup at line 94/129/164 can attribute progress/completion to the wrong model — smallest safe fix: use a unique per-download key (UUID) as taskDescription and map it to modelId.
- [info] macos/Runner/BackgroundDownloadPlugin.swift:36 — `destinationPath` comes straight from the Dart side with no path validation; a malicious or buggy caller can point the move at any writable path (e.g. overwrite app-support files) — verify the Dart caller restricts destinationPath to the app's models directory; not committed here, verify it is not committed elsewhere.
- [info] macos/Runner/MainFlutterWindow.swift:26-38 — plugin strong references are held for the window's lifetime; PdfTextPlugin/MemoryMonitorPlugin/VisionOcrPlugin/AppleSttPlugin/PaddleOcrPlugin types are not in this batch — assumed defined elsewhere; no defect exhibited in the shown code.

## Coverage
macos/Runner/BackgroundDownloadPlugin.swift — findings: 7
macos/Runner/MainFlutterWindow.swift — findings: 1
- [medium] macos/Runner/PdfTextPlugin.swift:113 — `document.page(at: i)?.string ?? ""` treats a failed page-text extraction as an empty page instead of surfacing the failure — a partially unreadable PDF reports pages of empty strings and, if every page fails extraction, the whole call resolves to the "NO_TEXT" error branch even though the document may have text that PDFKit simply could not decode — same permissive-fallback shape as `checkEmbeddedText` at :145-158, where an unopenable document returns `false` (the "no embedded text" answer) rather than an error, silently steering the OCR-vs-PDFKit decision toward the more expensive path with no signal — smallest safe fix: distinguish "page present but no text" from "extraction failed" (e.g. return `nil`/throw on `page.string == nil` and let the caller decide), and in `checkEmbeddedText` return an error/optional instead of `false` on open failure.
- [low] macos/Runner/PdfTextPlugin.swift:79 — `pageTexts.joined(separator: "\n")` plus the per-page `page.string` accumulation builds the full text of a large PDF in memory twice (array of page strings + joined string) with no size cap — for a multi-hundred-page scanned-then-OCRed document this can allocate hundreds of MB on a background QoS thread and, if the caller expects a bounded payload, can exceed the method-channel result size — smallest safe fix: stream/limit the returned text (e.g. cap page count or return per-page results like `extractTextPerPage`) or build into a single `StringBuilder`-style buffer instead of join.
- [low] macos/Runner/PdfTextPlugin.swift:150 — `for i in 0..<min(3, document.pageCount)` samples only the first 3 pages to decide "has embedded text" — a PDF whose first pages are scanned images but later pages have text is reported as text-less, and the caller (per the comment at :133-135, an OCR-vs-PDFKit decision) will route to OCR unnecessarily or, worse, conclude the PDF has no text layer at all — smallest safe fix: sample pages spread across the document (e.g. first, middle, last) or make the page-limit an explicit argument so callers know the heuristic bound.
- [info] macos/Runner/PdfTextPlugin.swift:58,100,145 — `PDFDocument(url:)` is called with a caller-supplied path string with no sandbox/symlink/existence guard visible in this file; if the Flutter side passes user-controlled paths this reads arbitrary files the process can access — verify the Dart-side caller validates/restricts `path` before invoking these methods (cannot confirm from this file alone).
- [low] macos/Runner/MemoryMonitorPlugin.swift:23 — `availableMB = totalMB - physicalMB` derives "available" memory as total physical minus this process's own footprint — that is not system-available memory (other processes' usage is unknown), so the reported value overstates availability by every other process's resident pages and can mislead a memory-pressure heuristic built on it — smallest safe fix: rename the field to something accurate (e.g. `unallocatedByThisProcessMB`) or compute from `os_proc_available_memory`/`mach_memory_info` where a true available figure exists.
- [low] macos/Runner/MemoryMonitorPlugin.swift:44-47 — `getPhysicalFootprint` returns `0` on `task_info` failure and the handler reports `physicalFootprintMB: 0` / `availableMemoryMB: totalMB` with no error signal — a caller treating 0 as "no memory used" (rather than "unknown") will make wrong decisions on failure; direction of failure is permissive — smallest safe fix: return an error result (or a nullable field) when `task_info` fails instead of a silent 0.

## Coverage
macos/Runner/MemoryMonitorPlugin.swift — findings: 2
macos/Runner/PdfTextPlugin.swift — findings: 4
- [medium] macos/Runner/VisionOcrPlugin.swift:44 — scale clamp is applied to the wrong value: `min(max(...))` clamps the *default* 2.0, not the user-supplied scale, so an attacker-supplied `scale` of e.g. 1e9 bypasses the defensive clamp entirely — the bitmap allocation at line 136-141 then jetsams the process mid-OCR (unauthenticated caller via method channel). Fix: `let scale = min(max(args["scale"] as? Double ?? 2.0, 0.5), 4.0)` — reorder so the user value is clamped, or `let raw = args["scale"] as? Double ?? 2.0; let scale = min(max(raw, 0.5), 4.0)`.
- [medium] macos/Runner/VisionOcrPlugin.swift:100-101 — width/height are computed from the clamped scale but `renderPage` at line 132-141 allocates a bitmap of `Int(width) × Int(height)` with no upper bound on absolute pixel dimensions; a large media-box page (e.g. poster-size PDF) at scale 4.0 can still allocate a multi-hundred-MB context per page even with a correct clamp — same jetsam class the comment at line 41-43 claims to defend against. Fix: cap absolute pixel dimensions (e.g. `min(width, 4096)`) before allocating the CGContext.
- [low] macos/Runner/VisionOcrPlugin.swift:93-119 — the per-page loop runs Vision OCR (`Self.ocrImage`) synchronously inside a single `DispatchQueue.global` block for the whole document; a large PDF holds the worker thread for the entire job with no per-page timeout or cancellation — long-running job blocks the QoS lane and cannot be interrupted. Fix: process pages with a bounded concurrent perform or check a cancellation flag per page.
- [low] macos/Runner/VisionOcrPlugin.swift:182-184 — sort is by `boundingBox.origin.y` only, with no tiebreaker for equal Y; observations on the same line can interleave non-deterministically across runs, producing unstable reading order in the returned `lines`. Fix: add a secondary sort key (e.g. `origin.x`) to make the ordering deterministic.
- [low] macos/RunnerTests/RunnerTests.swift:7-10 — `testExample` is an empty placeholder that asserts nothing; it cannot fail and hides regressions in the Runner target. Fix: replace with a real assertion or delete the test.

## Coverage
macos/Runner/VisionOcrPlugin.swift — findings: 5
macos/RunnerTests/RunnerTests.swift — findings: 1
- [medium] supabase/migrations/20260813110000_join_flow_v3_and_policy_fixes.sql:203-205 — legacy join-code re-roll UPDATE runs without any guard against concurrent claim/lookup RPCs and is not wrapped in a transaction with the policy changes; if the migration is applied while a claim_cast_invitation/lookup is in flight, a claim can succeed against a now-invalidated code or the update can partially apply if the migration runner aborts mid-statement — consequence: printed invites invalidated or a stale code accepted once — smallest safe fix: wrap statements 172–205 in a single migration transaction (Supabase migrations already run in one transaction, so verify the runner does; otherwise add BEGIN/COMMIT) and re-check that claim_cast_invitation re-validates the code inside the same statement (it does, lines 159–162), so the real fix is confirming transactionality of the migration runner.
- [medium] supabase/migrations/20260813110000_join_flow_v3_and_policy_fixes.sql:203-205 — re-rolling legacy codes invalidates existing shared invites with no data migration for outstanding invitations — consequence: organizers' printed/shared codes stop working silently after deploy; users see 'Invalid join code' with no path to recover — smallest safe fix: backfill an invitation-rotation table or notify organizers before deploy; at minimum document the break in a changelog the app reads.
- [low] supabase/migrations/20260813110000_join_flow_v3_and_policy_fixes.sql:203-205 — regex '^[0-9A-F]{6}$' is case-sensitive; legacy codes stored lowercase (md5 hex fragments are commonly emitted lowercase) will not match and will not be re-rolled — consequence: brute-forceable 16.7M-space codes remain live — smallest safe fix: use '~*'^[0-9A-F]{6}$'' or normalize with upper(join_code) in the predicate and the replacement.
- [medium] supabase/migrations/20260813110000_join_flow_v3_and_policy_fixes.sql:147-164 — claim_cast_invitation grants a cast slot to any authenticated user who presents the production join code, with no check that the code was issued *to* that user — consequence: anyone with the (widely shared) join code can claim any unclaimed cast_members row in that production, taking over another user's reserved character slot — smallest safe fix: require an invitation token scoped to the intended user (e.g. a per-member claim code) instead of the production-wide join code.
- [medium] supabase/migrations/20260813110000_join_flow_v3_and_policy_fixes.sql:147-164 — claim_cast_invitation is SECURITY DEFINER with no rate limit and no check_join_rate_limit() call — consequence: an authenticated user can enumerate member ids and codes unthrottled; combined with the production-wide code check this enables scripted slot takeover across productions — smallest safe fix: call check_join_rate_limit() (or a dedicated limiter) at the top of the function body.
- [medium] supabase/migrations/20260813110000_join_flow_v3_and_policy_fixes.sql:61-77 — lookup_production_by_join_code returns row_to_json(p) of the full productions row — consequence: any authenticated user (or anon if the revoke at 81 is later re-granted) learns every column of the productions row, including any private/internal columns the app assumes are not exposed via the join-code lookup — smallest safe fix: project only the columns the join screen needs (id, name, join_code presence) via json_build_object.
- [low] supabase/migrations/20260813110000_join_flow_v3_and_policy_fixes.sql:61-77 — lookup_production_by_join_code performs check_join_rate_limit() then a lookup, but the rate-limit insert (line 57) happens even when the lookup fails; a user who mistypes 20 times in 5 minutes is locked out of *all* join-code lookups including legitimate re-lookup — consequence: self-DoS / support burden, not a security hole — smallest safe fix: only record the attempt when the lookup misses, or lower the threshold for failed attempts.
- [medium] supabase/migrations/20260813110000_join_flow_v3_and_policy_fixes.sql:95-99 — fetch_cast_for_join's guard uses `not exists (...) and not is_production_member(...)` — a caller who is a member of the production bypasses the join-code check entirely (intended), but a caller who is *not* a member and supplies a valid code gets the roster; the roster includes user_id (line 106) — consequence: any authenticated user with the join code can enumerate every member's user_id UUIDs before joining, enabling cross-user correlation/claim targeting via claim_cast_invitation — smallest safe fix: drop user_id from the returned json_build_object or restrict the roster to members-only.
- [medium] supabase/migrations/20260813110000_join_flow_v3_and_policy_fixes.sql:101-110 — fetch_cast_for_join returns the full cast_members roster with no LIMIT — consequence: a large production's roster is serialized whole on every call; combined with the unauthenticated-ish surface this is a cheap bulk-extraction primitive — smallest safe fix: add a sane LIMIT/ceiling or paginate.
- [low] supabase/migrations/20260813110000_join_flow_v3_and_policy_fixes.sql:137-139 — join_production inserts role='actor' unconditionally, ignoring any client-supplied role — good (matches the v3 intent); no finding.
- [medium] supabase/migrations/20260813110000_join_flow_v3_and_policy_fixes.sql:137-139 — join_production inserts with joined_at=now() but no ON CONFLICT / uniqueness guard visible in this file — if cast_members lacks a unique (production_id, user_id) constraint, repeated calls create duplicate memberships — consequence: duplicate rows break is_production_member assumptions and inflate the roster — smallest safe fix: add a unique index on (production_id, user_id) where user_id is not null, or use ON CONFLICT DO NOTHING. (Assumption: the constraint is not defined in another migration I cannot see.)
- [medium] supabase/migrations/20260813110000_join_flow_v3_and_policy_fixes.sql:172-176 — the new insert policy allows role in ('actor','understudy') but the join_production RPC forces 'actor'; a direct-table insert of 'understudy' is allowed and the comment at 167-171 says client logic trusts role — consequence: a user can self-grant 'understudy', which may carry client-side privileges the server does not enforce — smallest safe fix: tighten the policy to role = 'actor' only, or add a server-side check that 'understudy' requires an organizer-created row.
- [low] supabase/migrations/20260813110000_join_flow_v3_and_policy_fixes.sql:189-196 — recordings UPDATE policy's WITH CHECK calls is_production_member(production_id, auth.uid()); if is_production_member is a SECURITY DEFINER function doing its own auth.uid() internally, the explicit auth.uid() argument is fine, but if it reads auth.uid() internally the two could diverge under definer contexts — assumption-based; verify the definition — smallest safe fix: confirm is_production_member takes the uid as an argument and does not re-read auth.uid().
- [low] supabase/migrations/20260813110000_join_flow_v3_and_policy_fixes.sql:211 — drop index if exists public.idx_script_lines_production — if that index is actually used by hot queries on (production_id, order_index) beyond the unique constraint (e.g. ORDER BY order_index with a filter on production_id), dropping it relies on the unique index being usable for that pattern — consequence: possible plan regression on the bulk-write path's read side — smallest safe fix: verify the unique index's column order matches the query pattern before dropping; if it does, no change needed.
- [info] supabase/migrations/20260813110000_join_flow_v3_and_policy_fixes.sql:1-211 — migration file contains no secrets; nothing to verify.
## Coverage
supabase/migrations/20260813110000_join_flow_v3_and_policy_fixes.sql — findings: 12

## Run stats

input 56713 tok (+16335 cached), output 23363 tok — sync requests, discounted — 29 files in 17m (98.5 files/h, 1.2 min/batch)
