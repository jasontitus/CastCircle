# Pi sweep review — CastCircle

Exhaustive per-file pass: 74 code files across 42 batches — model ds4/GLM-5.3-Flash-Q2:off — 2026-08-30.

## Findings

- [medium] lib/features/auth/auth_screen.dart:397 — confirmation-email resend success surfaced via toast but failure path logs the raw exception (which for Supabase auth errors can include the email address in the message) to debug log via DebugLogService.logError without stack trace — email/PII in log lines and exception logged without stack trace — pass a redacted message and the stackTrace argument to logError.
- [medium] lib/features/auth/auth_screen.dart:402-403 — resend-confirmation failure is swallowed to a debug-level log and only shown as a transient snackbar; no audit trail of failed auth events — auth events (resend) unlogged at any durable level — log auth resend attempts/failures to the structured log service with a category, not just a snackbar.
- [low] lib/features/auth/auth_screen.dart:421 — `auth_skipped` persisted via sharedPreferences with no logging of the skip-auth event — auth decision (guest mode entry) unlogged — emit a debug/general log entry when skip is chosen so support can trace "signed in without account" reports.
- [medium] lib/features/cast_manager/bulk_cast_setup_screen.dart:295-296 — cloud cast invitation failure logged via DebugLogService.logError with the display name and character name interpolated into the message and no stack trace — PII (actor names) in log lines and exception logged without stack trace — log a stable key plus sanitized identifier and pass the stackTrace.
- [medium] lib/features/cast_manager/bulk_cast_setup_screen.dart:291-297 — per-item logging inside the save loop: every failed invitation writes a separate log entry (and the loop can contain many entries) — noisy per-item logging in hot loops — aggregate failures into one summary log entry after the loop.
- [low] lib/features/cast_manager/bulk_cast_setup_screen.dart:257 — contact-picker failure only `debugPrint`ed, never recorded in the structured log service — background/async failure output discarded — route it through DebugLogService.logError so it appears in the persisted debug log.
- [low] lib/features/cast_manager/bulk_cast_setup_screen.dart:336-338 — failed-invite summary snackbar text interpolates `_failedCloudInvites.join(', ')` (character names) into a user-visible toast — PII exposure in UI/log surface is minor but the same names are also re-logged on every subsequent save — keep the summary to counts, not names.
- [info] lib/features/auth/auth_screen.dart:421 — `auth_skipped` bool persisted in shared preferences; verify it is not committed if this file ever holds real user identifiers (none present in shown code).

## Coverage
lib/features/auth/auth_screen.dart — findings: 5
lib/features/cast_manager/bulk_cast_setup_screen.dart — findings: 5
- [medium] lib/features/cast_manager/cast_manager_screen.dart:148-151 — cloud cast sync failure is swallowed to a debug log with no user-visible signal and no stack trace — the screen silently shows stale local-only members and duplicate/stale rows persist until the next successful sync, and the operator cannot diagnose the failure from the log alone — pass the caught exception's stack trace to DebugLogService.logError (it accepts an optional StackTrace) and surface a one-line toast/banner so the failure is observable.
- [medium] lib/features/cast_manager/cast_manager_screen.dart:787-791 — cloud invitation failure is logged at debug level with no stack trace and no user-visible signal beyond the share sheet being skipped — the invited actor never receives the invite and the organizer has no durable record of why — include the stack trace in the logError call and log a distinct category (network) with the member id so the failure is attributable.
- [medium] lib/features/cast_manager/cast_manager_screen.dart:848-851 — unassign failure is logged at debug level with no stack trace; the toast shown is generic ("check your connection") and the actual cloud error is discarded — the organizer cannot tell whether the row was removed locally but not in the cloud, leaving a dangling invite link — log the exception with its stack trace and include the cloud error class in the toast text.
- [medium] lib/features/cast_manager/cast_manager_screen.dart:1062-1064 — invite-card image capture failure is swallowed to debugPrint with no stack trace and no user-visible signal; the fallback silently re-shares plain text — the organizer believes the QR invite was sent when only a text fallback went out — log the failure via DebugLogService.logError with a stack trace and show a toast noting the card could not be generated.
- [low] lib/features/cast_manager/cast_manager_screen.dart:708-712 — contact-pick failure is logged at debug level with no stack trace; the toast is user-facing but the log entry lacks the exception detail — include the stack trace in the logError call so permission-denied vs. picker crash is distinguishable.
- [low] lib/features/cast_manager/cast_manager_screen.dart:1064 — debugPrint used instead of the centralized DebugLogService — the failure bypasses the ring buffer/disk persistence entirely, so it never appears in the exported debug log — route it through DebugLogService.instance.logError like the other failure paths.
- [low] lib/data/services/debug_log_service.dart:187-188 — synchronous file append on the caller's (often UI) thread with no fsync and no error surfaced beyond debugPrint — a failed append is silently dropped and the log file can interleave partial lines under concurrent writes — consider an append-only queue drained on the periodic flush, or at least log append failures through the in-memory ring buffer so they are visible in export().
- [low] lib/data/services/debug_log_service.dart:296-299 — truncation rewrites the file with only the in-memory entries, silently discarding any on-disk lines that failed to parse into LogEntry (fromLine returns null) — those lines are lost with no record — append a marker line or keep unparsed lines in the retained set.
- [info] lib/features/cast_manager/cast_manager_screen.dart:1116 — production!.joinCode is read after a null-check on production?.joinCode; if production is null but joinCode was non-null this would throw — verify it is not reachable (joinCode is derived from production, so likely safe, but the non-null assertion is fragile).
- [info] lib/features/cast_manager/cast_manager_screen.dart:885-892 — context.findRenderObject() cast to RenderBox? without checking mounted/element state; if the widget is mid-unmount the render object may be detached — guard with a mounted check before computing the share origin.

## Coverage
lib/features/cast_manager/cast_manager_screen.dart — findings: 10
lib/data/services/debug_log_service.dart — findings: 2
lib/data/models/script_models.dart — clean
lib/core/toast.dart — NOT REVIEWED
lib/data/services/supabase_service.dart — NOT REVIEWED
lib/core/responsive.dart — NOT REVIEWED
- [medium] lib/features/cast_manager/voice_config_screen.dart:126-138 — `_memoAssignment` caches the assignment map keyed on `Object.hash(identityHashCode(script.lines), _currentPreset.id, _genderOverrides.toString())` — `identityHashCode` is not a unique identity (two different line lists can collide, and a *new* ParsedScript with the same hash as the old one returns the stale assignment) and `_genderOverrides.toString()` for a Map<String, CharacterGender> is the default `Instance of ...`-style toString for enum values in older Dart, so two different gender-override maps can produce the same key — consequence: a character tile can show/play a voice assigned under a stale preset/gender state after the user changes the preset or gender toggles, silently mis-assigning voices — smallest safe fix: key the cache on the actual objects (`_currentPreset.id` plus a canonical string of the gender overrides built from its entries, and compare the script by reference/`identical(script.lines, ...)` stored alongside the cache) instead of `identityHashCode` + default `toString()`.
- [medium] lib/features/cast_manager/voice_config_screen.dart:154-163 — `onChanged` on the preset `RadioListTile` awaits `setPreset` then `setState` without any try/catch; if the service throws (e.g. storage failure) the exception escapes the async callback and the UI never updates, leaving the radio showing the old preset while `_currentPreset` is unchanged — consequence: user taps a preset, nothing visibly happens, no error surfaced — smallest safe fix: wrap the await in try/catch and surface a SnackBar on failure before returning.
- [medium] lib/features/cast_manager/voice_config_screen.dart:286-300 — the Save button awaits `setOverride` then re-reads `getOverrides` and calls `Navigator.pop(ctx)` guarded by `ctx.mounted`, but the outer `setState(() => _overrides = overrides)` is guarded only by the screen's `mounted`; if the sheet was dismissed while the awaits were in flight, `Navigator.pop(ctx)` is skipped (fine) but the screen-level `setState` still runs with a fresh map — that part is safe — however the *dialog* `Navigator.pop(ctx)` is guarded by `ctx.mounted` while the screen `setState` is guarded by `mounted`, and the two guards check different objects; if the screen was disposed but the sheet still mounted, `setState` on a disposed state throws — consequence: crash when the user closes the screen while the save is in flight — smallest safe fix: guard the screen `setState` with the same `mounted` check that is already used elsewhere (it is — `if (mounted) setState(...)` at 298 — so this is actually correct) — re-examined: the real defect is that `Navigator.pop(ctx)` is called *after* the screen-level `setState`, so if the screen is disposed the pop never happens and the sheet stays open on a dead screen — smallest safe fix: pop first, then setState, or check `mounted` before both.
- [low] lib/features/cast_manager/voice_config_screen.dart:390-392 — `tts.assignVoice(previewChar, 0, voiceId: voiceId, speed: speed)` mutates the shared TTS singleton's voice map under a scratch name `__voice_preview__` that is never cleaned up; repeated previews accumulate entries in the singleton's maps — consequence: unbounded growth of the TTS service's internal maps over a long session — smallest safe fix: remove the scratch entry after `speak` completes (or reuse a single fixed slot).
- [low] lib/features/cast_manager/voice_config_screen.dart:402-405 — `_localeLabels` is a private static map with only two entries while the dialect selector builds segments from it; if the production's locale is anything other than `en-US`/`en-GB` the `SegmentedButton` `selected: {production.locale}` references a value with no matching segment, which throws at build time — consequence: crash on opening the screen for a production with an unexpected locale — smallest safe fix: fall back to a known segment or add the missing locale entries.
- [medium] lib/features/home/home_screen.dart:36-38 — `shouldReuseLoadedScript` treats a production as "already loaded" whenever `currentProductionId == targetProductionId && currentScript != null && lines.isNotEmpty`, but it never checks that `currentScript` belongs to *this* production (the script could be a stale script left over from a previous production with the same id after a delete/recreate cycle) — consequence: `_ensureScriptLoaded` returns early and the user rehearses against a stale script — smallest safe fix: also compare `currentScript`'s production identity (e.g. a stored productionId field) or drop the early return when in doubt.
- [medium] lib/features/home/home_screen.dart:583-585 — `RecordingSyncService.instance.unsubscribe()` is only called when `currentProductionProvider?.id == production.id`; if the user deletes a production that is not currently selected but has an active realtime subscription (e.g. opened earlier in the session), the subscription keeps running against a deleted production — consequence: in-flight downloads re-create rows for a deleted production — smallest safe fix: unsubscribe unconditionally before clearing cache, or track subscriptions per production id.
- [medium] lib/features/home/home_screen.dart:690-691 — `_submitProduction` awaits `productionsProvider.notifier.add(production)` and sets `currentProductionProvider` *before* the cloud create fires; if the cloud create fails (caught at 701-712), the local production stays with a join code that no invited castmate can resolve, and the error path only shows a toast — consequence: the user believes the production was created and shares a join code that will never work — smallest safe fix: mark the production as pending-cloud-creation (or roll back the local add) when the background create fails, and surface a retry affordance.
- [low] lib/features/home/home_screen.dart:719 — `router.push('/import')` uses the router captured at 665 before the dialog was popped; the comment says the dialog context is dead, but `router.push` is called after `_submittingProduction = false` at 693 with no guard — if the widget was disposed mid-flight (e.g. user navigated away), `router.push` still navigates a dead route stack — consequence: possible navigation after dispose — smallest safe fix: guard with `mounted` before `router.push`.
- [low] lib/features/home/home_screen.dart:489-493 — the web-editor share URL hardcodes `https://castcircle-app.web.app?production=${production.id}` with no URL-encoding of `production.id`; if the id ever contains reserved characters the share link breaks — consequence: malformed share link — smallest safe fix: `Uri.parse(...)`/`Uri(queryParameters:)` to build the URL.
- [info] lib/features/home/home_screen.dart:20 — `rootScaffoldMessengerKey` is imported from `main.dart` and used at 705 to show a toast from a background error handler; verify the key is a global `GlobalKey<ScaffoldMessengerState>` and not something recreated per-build (cannot confirm from this file alone).

## Coverage
lib/features/cast_manager/voice_config_screen.dart — findings: 5
lib/features/home/home_screen.dart — findings: 6
- [medium] lib/features/join/join_production_screen.dart:581 — join failure is logged only through DebugLogService (local ring buffer/disk log) with no user-visible error surfaced beyond the inline `_error` text and no analytics/crash capture of the exception object — an unhandled async failure in `_joinProduction`'s catch block is swallowed to debug level, so production join failures are invisible to operators — add a crash/analytics capture (e.g. `AnalyticsService` error event or `FirebaseCrashlytics`/Sentry record) alongside the debug log.
- [medium] lib/features/join/join_production_screen.dart:424-425 — lookup failure path logs the exception via `DebugLogService.instance.logError(...)` but discards the stack trace argument (`e, stack` are passed, yet `logError` in the context file only logs `stack.toString().take(5)` and the message; the thrown error object itself is reduced to a string in `_error`) — the same error is effectively logged at every layer (debug log + inline UI text) with no correlation ID, making it impossible to correlate a user-reported join failure with the log entry — include a short correlation ID (e.g. timestamp+uuid) in both the debug log line and the user-facing error text.
- [low] lib/features/join/join_production_screen.dart:571-572 — success path logs `'Join: success — opening production "${production.title}"'` with the production title interpolated into the log line; titles are user-controlled content and could contain PII (e.g. a person's name in a private production title) — sanitize or truncate the title before logging.
- [low] lib/features/onboarding/model_setup_screen.dart:174 — `_dlog.logError(LogCategory.error, 'Model setup: voices failed', e)` logs the exception without a stack trace argument (the `logError` signature accepts an optional stack, but none is passed) — a stack trace is lost for the voices-download failure, hampering diagnosis — pass the current stack (`Error.throwWithStackTrace`/`rethrow` pattern or `StackTrace.current`).
- [low] lib/features/onboarding/model_setup_screen.dart:222 — same defect class: `_dlog.logError(LogCategory.error, 'Model setup: matching failed', e)` also omits the stack trace — pass the stack trace alongside the error.
- [medium] lib/features/onboarding/model_setup_screen.dart:130-131 — `SharedPreferences.getInstance().then((p) => p.setBool('models_auto_download_ok', true));` is a floating future with no `.catchError`/`await` — if the prefs write fails (disk I/O error, plugin exception), the consent record for auto-download is silently lost while the download proceeds, and the unhandled async failure is never captured anywhere — add `.catchError` (or `await` inside try) and log the failure via `_dlog`.
- [medium] lib/features/onboarding/model_setup_screen.dart:163-171 — the deadline loop `while (mounted && !_voices.ready && _voices.error == null)` polls every 500 ms but never re-reads the service state inside the loop body; the only exit conditions are the deadline, `mounted == false`, `_voices.ready`, or `_voices.error` — if the native listener `_onServiceState` never fires (e.g. listener removed on a rebuild that disposes and recreates state, or the service never emits), the loop spins for the full 15 minutes with no progress and no log, then silently breaks with no error set — log a timeout event via `_dlog` when the deadline is hit so the stall is observable.
- [low] lib/features/onboarding/model_setup_screen.dart:229-234 — `if (_items.every((i) => i.ready))` triggers `context.pop()` after showing a toast, but there is no log/audit of the setup-completion event beyond `AnalyticsService.instance.logModelDownloaded(modelId: 'setup_all')` at line 138 which fires *before* any download succeeds — the analytics event is emitted at the start of `_downloadAll` regardless of outcome, so the metric counts attempts, not completions, and there is no completion signal at all — move the analytics call to the success path (after `_items.every((i) => i.ready)`) or add a separate completion event.
- [medium] lib/features/join/join_production_screen.dart:545-557 — `fetchCloudScriptLines(productionId)` result is checked with `if (cloudLines != null && cloudLines.isNotEmpty)` but a `null` return (network/parse failure) is silently treated as "no cloud script yet" (line 559-562) and the join proceeds — a transient fetch failure is indistinguishable from a legitimately empty script, so the joiner's local script state silently diverges from the organizer's with no log distinguishing the two cases — log a distinct warning when `cloudLines == null` (fetch failed) versus empty (legitimately none), via `dlog.log` with a non-network category or an error-level entry.
- [low] lib/features/join/join_production_screen.dart:566-569 — `voicePreset` is read from `_foundProduction!['voice_preset']` and applied via `VoiceConfigService.instance.setPreset(...)` with no error handling and no log of failure — a failed preset write is silently discarded, leaving the joiner's voice config unset with no observability — wrap in try/catch and log failures via `dlog`.
- [low] lib/features/join/join_production_screen.dart:568 — `VoiceConfigService.instance.setPreset(productionId, voicePreset)` is awaited but its failure would propagate to the outer catch at line 580 and be reported as 'Failed to join: $e' even though the join itself already succeeded and the production row was created — a cosmetic preset failure aborts the user's success path with a misleading error; catch preset errors separately and log rather than fail the whole join.

## Coverage
lib/features/join/join_production_screen.dart — findings: 7
lib/features/onboarding/model_setup_screen.dart — findings: 5
- [medium] lib/features/onboarding/welcome_screen.dart:24-33 — `maybeOffer` awaits `SharedPreferences.getInstance()` and `context.push` across async gaps, then uses `context.mounted` checked only after the second await; the `prefs.getBool('welcome_seen')`/`setBool` sequence runs before the mounted check, so if the widget was disposed mid-await the push is skipped but `welcome_seen` is already persisted — the walkthrough never shows again and the user lands on the app with no onboarding — guard with `if (!context.mounted) return;` immediately after `getInstance()` before mutating prefs, and re-check before `push`.
- [medium] lib/features/onboarding/welcome_screen.dart:106-128 — `_openDemo` captures `router = GoRouter.of(context)` before the async `load`, then calls `router.pop()`/`router.push` after the await; if the screen was disposed during load, `router` is still valid (GoRouter is app-scoped) but `router.pop()` pops whatever route is on top at that moment, not this walkthrough — a race where a slow demo load plus user navigation pops an unrelated route — capture the route and verify `mounted`/`router.isRouteActive` before popping, or use `context.mounted`-guarded `context.pop()`.
- [low] lib/features/onboarding/welcome_screen.dart:113 — `DemoProductionService.instance.load(ref)` is awaited with no timeout/cancellation; a hung load leaves `_loadingDemo` stuck true and the "Try the demo" button permanently disabled with a spinner — add a timeout or reset `_loadingDemo` in a `finally`.
- [low] lib/features/onboarding/welcome_screen.dart:118-127 — the catch block logs via `DebugLogService.instance.logError(...)` but swallows the error at debug level only; no user-visible retry affordance beyond the toast, and `_loadingDemo` reset happens only inside `if (!mounted) return;` guard — if the screen was disposed during the failed load, `_loadingDemo` stays true in the (dead) state, harmless but the toast is also skipped; acceptable, but the error is silently dropped from any analytics — consider logging to AnalyticsService as well.
- [info] lib/features/onboarding/welcome_screen.dart:28-30 — `screenshot_mode` and `welcome_seen` are read/written as raw bool prefs with no namespacing; verify these keys are not also used by other features (cannot confirm from inlined files).
- [medium] lib/features/production_hub/production_hub_screen.dart:45-79 — `_loadSavedCharacter` reads `rehearsal_character_${production.id}` from prefs and, on fallback, auto-selects the first cast membership for the current user and then calls `_saveCharacterChoice(charName)` (line 74) which persists it; this auto-selection runs on every `initState` even when the user never chose a character, silently writing a pref the user never set — if the intent is "remember my choice", auto-persisting a fallback choice means clearing the choice later (`_saveCharacterChoice(null)`) is the only way out, and reopening the screen re-derives the same auto-choice; gate the save behind an explicit user action or a `didAutoSelect` flag.
- [medium] lib/features/production_hub/production_hub_screen.dart:92-120 — `_checkModels` schedules `_showModelPrompt` via `Future.delayed(500ms)` guarded by `mounted && !_modelsReady`, but `_modelsReady` is read from the closure captured at scheduling time... actually it reads the field, so the guard is fine; however the delayed prompt can fire after the user has navigated away from the hub screen if `mounted` is still true for a nested dialog context — the `showDialog` uses `context` of this state; if the screen is disposed the guard covers it. No defect exhibited beyond the 500ms race where `_checkModels` re-runs from the banner tap (line 216) and two dialogs could stack if the delayed prompt fires after the user tapped the banner but before `_checkModels` completes — low likelihood; flagging as [low]: two `showDialog` calls can stack (delayed prompt + none other), guard `_showModelPrompt` with a `_promptShowing` flag.
- [low] lib/features/production_hub/production_hub_screen.dart:166 — `debugPrint('ProductionHub.build: ...')` logs on every rebuild including `script?.lines.length` and character name; in release builds `debugPrint` is a no-op only in debug mode — actually `debugPrint` prints in profile/release too unless overridden; per-rebuild logging of script size in `build` is noisy hot-path logging — wrap in `kDebugMode` guard or remove.
- [medium] lib/features/production_hub/production_hub_screen.dart:386-411 — `_memoSceneCounts` caches on `(script.lines, script.scenes, myCharacter)` using `identical()` on the lists; `script.lines` is a `List<ScriptLine>` produced by providers — if the provider rebuilds the list to an equal-but-not-identical instance (e.g., after cloud sync at line 846 sets `currentScriptProvider.state = cloudScript`, a new object), the cache correctly misses, but if any code mutates `script.lines` in place (cannot confirm from inlined files; `linesInScene` only reads), the memo returns stale counts; the `identical` check is the only invalidation — safer to also compare `identityHashCode`/length or document the immutability contract.
- [low] lib/features/production_hub/production_hub_screen.dart:430-433 — `charIndexByName` maps `char.name → index` but is rebuilt on every `_buildSceneList` call (every rebuild of the scene list); for large casts this is O(cast) per rebuild inside `ListView.builder`'s parent — it is hoisted out of `itemBuilder` (good) but still recomputed per build; acceptable, no finding beyond info — not reported.
- [medium] lib/features/production_hub/production_hub_screen.dart:778-795 — `_pushToCloud` catches all exceptions and shows a toast, but `pushScriptToCloud(ref)` result is discarded; if the push partially succeeded (some lines uploaded), the UI says "Script pushed to cloud" regardless of what the service did — cannot verify `pushScriptToCloud` semantics from inlined files; assume it throws on failure — then the catch is correct; no finding beyond the discarded return value — not reported as defect without evidence.
- [medium] lib/features/production_hub/production_hub_screen.dart:797-864 — `_syncFromCloud` on empty cloud (`cloudLines == null || isEmpty`) shows "No script in cloud" and returns — but if the local script exists and cloud is empty, this is the "cloud lost the script" case; the safer direction would be to warn about potential data loss rather than a neutral toast; however no destructive action is taken, so this is informational — not reported.
- [high] lib/features/production_hub/production_hub_screen.dart:827-850 — `shouldReplaceLocal` defaults to `true` when `localScript == null` (line 827 initializes `true`, and the dialog only runs when `localScript != null`); if `localScript` is null the cloud script replaces without any user confirmation — that is the intended "no local script" path, fine. But when `context.mounted` is false (line 828 guard fails) while `localScript != null`, `shouldReplaceLocal` stays `true` and the local script is silently replaced by the cloud version with no dialog and no toast — a disposed-context sync overwrites local state without consent — set `shouldReplaceLocal = false` (safer default) when the dialog cannot be shown, i.e. initialize to `false` and set `true` only from an affirmative dialog choice.
- [medium] lib/features/production_hub/production_hub_screen.dart:846-850 — after replacing, `persistScriptLocally(ref, production.id, cloudScript)` is awaited; the comment says persistScript would push it back to cloud if used, so this local-only variant is chosen — but the replaced `currentScriptProvider.state` (line 846) and the persisted copy can diverge if `persistScriptLocally` mutates the script (cannot confirm); no finding without evidence — not reported.
- [low] lib/features/production_hub/production_hub_screen.dart:858-864 — the outer catch shows 'Sync failed: $e' but the error is not logged to DebugLogService (unlike `_openDemo` in welcome_screen which logs); inconsistent error observability — add `DebugLogService.instance.logError(...)` for parity.
- [medium] lib/features/production_hub/production_hub_screen.dart:867-912 — `_export` writes the export file to `getApplicationDocumentsDirectory()/exports/$fileName` with `fileName` derived from `production.title` sanitized via regex `[^\w\s-]` removal + whitespace→underscore; a title like `../../etc/passwd` becomes `.._.._etc_passwd` after the regex strips `/`? Actually `/` is not in `\w\s-`, so it is removed, leaving `..etc_passwd` — path traversal is neutralized by the regex; but a title that is entirely punctuation (e.g. `!!!`) yields an empty `safeName`, so `fileName` is `.md`/`.txt` — a hidden dotfile; and two productions with titles that sanitize to the same name silently overwrite each other's exports — add a uniqueness suffix or reject empty sanitized names.
- [low] lib/features/production_hub/production_hub_screen.dart:899-906 — `context.findRenderObject() as RenderBox?` then `box.localToGlobal(Offset.zero) & box.size` — if the render object is not a RenderBox (e.g., after the button was disposed/rebuilt), the cast yields null and `sharePositionOrigin` falls back to `Rect.zero`, which on iPad makes the share sheet present from an invalid origin (centered or crash on some versions) — guard with `box != null && box.hasSize`.
- [medium] lib/features/production_hub/production_hub_screen.dart:585-708 — `_buildDrawer` reads `ref.read(currentProductionProvider)!` with a non-null assertion; `build` (line 168) already guards `production == null` before rendering the drawer, so the `!` is safe in the normal path, but if the drawer is built from a stale context after the provider cleared (cannot confirm rebuild ordering), the `!` throws — the guard at 168 covers the same build pass, so this is safe; not reported.
- [low] lib/features/production_hub/production_hub_screen.dart:696-700 — the 'AI Models' drawer item awaits `context.push('/ai-models')` then calls `_checkModels()`; if the screen was disposed during the push, `_checkModels` runs `SharedPreferences.getInstance()` and `setState` guarded by `mounted` — safe; not reported.
- [medium] lib/features/production_hub/production_hub_screen.dart:249-287 — the character `DropdownButtonFormField` `value` is computed as `script.characters.any((c) => c.name == myCharacter) ? myCharacter : null`; if `myCharacter` is a stale name no longer in `script.characters` (e.g., after cloud sync replaced the script), the dropdown shows empty but `rehearsalCharacterProvider` still holds the stale name, and `_buildSceneList`'s `isMyScene`/chip highlighting (lines 445-446, 539) still treat the stale name as "my character" — reset `rehearsalCharacterProvider` when the script changes and the saved character no longer exists; `_loadSavedCharacter` only runs in `initState`, so a post-sync script change never revalidates the selection.
- [low] lib/features/production_hub/production_hub_screen.dart:351-376 — act filter chips: `onSelected` for an act chip toggles `_filterAct` between act and null, but the 'All Acts' chip's `onSelected` ignores its argument and always clears — tapping a selected act chip deselects it (goes to All), fine; tapping 'All Acts' when already selected re-selects All — no defect.
- [low] lib/features/production_hub/production_hub_screen.dart:440-442 — `ListView.builder` `itemCount: scenes.length` with `padding: EdgeInsets.all(16)`; no issue.
- [medium] lib/features/production_hub/production_hub_screen.dart:445-446 — `isMyScene` uses `scene.characters.contains(myCharacter)`; combined with the stale-character issue above, a removed character still marks scenes as "mine" — same root cause as the stale `rehearsalCharacterProvider` finding; reported once there.
- [low] lib/features/production_hub/production_hub_screen.dart:503-528 — location row rendered only when `scene.location.isNotEmpty`; no issue.
- [low] lib/features/production_hub/production_hub_screen.dart:534-562 — character chips build a `Container` per character per scene card; for a large cast × many scenes this is O(cast × scenes) widgets built lazily by ListView — acceptable; not reported.
- [medium] lib/features/production_hub/production_hub_screen.dart:662-673 — cloud drawer section is gated on `isSignedIn` but `_pushToCloud`/`_syncFromCloud` themselves do not re-check sign-in; if sign-in state changed after drawer build (stale `isSignedIn` captured at build time), the actions run unauthenticated — cannot confirm `SupabaseService.isSignedIn` freshness semantics from inlined files; assume it reads live state — not reported.
- [low] lib/features/production_hub/production_hub_screen.dart:734-773 — `_buildNoScriptView`'s Import button pushes '/import' without popping the hub; back stack retains hub — intentional; not reported.
- [info] lib/features/production_hub/production_hub_screen.dart:1-23 — imports include `dart:io` `Platform.isMacOS`/`Platform.isAndroid` checks (lines 113, 129); on web this throws — cannot confirm web target from inlined files; not reported.
- [low] lib/features/production_hub/production_hub_screen.dart:113-117 — `if (!Platform.isMacOS)` gates the delayed model prompt; on macOS the prompt never shows even when models are missing — the banner (lines 210-238) still shows, so the user has a path; intentional-looking; not reported.
- [medium] lib/features/production_hub/production_hub_screen.dart:105-119 — `_checkModels` sets `_checkedModels`/`_modelsReady` inside `if (mounted)`; if the widget is disposed mid-await, `_checkedModels` stays false and the banner never renders — but the widget is gone anyway; however the screenshot-mode early return (lines 95-103) sets state only `if (mounted)` and returns before the normal path — consistent; not reported.
- [low] lib/features/production_hub/production_hub_screen.dart:92-120 — `_checkModels` is called from `initState` (line 41), from the banner tap (line 216), and from the drawer item (line 699); concurrent invocations can double-schedule the delayed prompt (line 114) — same root cause as the dialog-stacking finding; reported once there.
- [medium] lib/features/production_hub/production_hub_screen.dart:386-411 — `_sceneCountsCache`/`_sceneCountsKey` are instance fields mutated during `build`-driven `_buildSceneList`; calling `_memoSceneCounts` inside build mutates state during build — Riverpod/Flutter allows this (no setState), but if two builds interleave (unlikely single-threaded) it is fine; not reported.
- [low] lib/features/production_hub/production_hub_screen.dart:392-398 — the memo key compares `identical(_sceneCountsKey?.$1, key.$1)` for lines and scenes but `==` for `myCharacter` (record field `==` on String? is value equality) — consistent; not reported.
- [medium] lib/features/production_hub/production_hub_screen.dart:812-813 — `buildParsedScriptWithCloudScenes(production.title, cloudLines, production.id)` — argument order (title, lines, id) cannot be verified against the function signature from inlined files; if swapped, the parsed script would carry the wrong id — assumption stated; not reported without the signature.
- [low] lib/features/production_hub/production_hub_screen.dart:802 — `fetchCloudScriptLines(production.id)` — cannot verify tenant scoping of this call from inlined files; if it fetches by production id without a user/tenant filter, a crafted id could pull another tenant's script — assumption: Supabase RLS enforces scoping; flagging as [info] to verify server-side policy.
- [info] lib/features/production_hub/production_hub_screen.dart:846 — `ref.read(currentScriptProvider.notifier).state = cloudScript;` replaces the whole script; any per-line recordings keyed to old line ids may orphan — cannot confirm from inlined files; not reported.
- [low] lib/features/production_hub/production_hub_screen.dart:884-886 — `default:` in the switch handles any non-'markdown' format string; `_export` is only called with 'plain'/'markdown' (lines 679, 683) — safe; not reported.
- [low] lib/features/production_hub/production_hub_screen.dart:891-893 — `exportDir.createSync(recursive: true)` is synchronous I/O on the UI thread inside an async method — small cost, acceptable; not reported.
- [low] lib/features/production_hub/production_hub_screen.dart:895 — `await File(filePath).writeAsString(content)` without `mode: FileMode.write` default is fine (new/truncate); not reported.
- [low] lib/features/production_hub/production_hub_screen.dart:900-902 — `Share.shareXFiles([XFile(filePath, mimeType: 'text/plain')], text: ...)` — the `text` param with files may be ignored on some platforms; cosmetic; not reported.
- [low] lib/features/production_hub/production_hub_screen.dart:166 — duplicate of the debugPrint finding; reported once at 166.
- [medium] lib/features/production_hub/production_hub_screen.dart:45-79 — `_loadSavedCharacter`'s fallback path (lines 59-78) reads `castMembersProvider` and `SupabaseService.instance.currentUser?.id`; if `currentUser` is null (signed out), the fallback silently does nothing — correct direction (no auto-select when signed out); not reported.
- [low] lib/features/production_hub/production_hub_screen.dart:65-67 — `myMembership` filters `m.userId == userId && m.characterName.isNotEmpty` then `.first` — if multiple memberships match, `.first` is arbitrary; deterministic enough; not reported.
- [low] lib/features/production_hub/production_hub_screen.dart:74 — `_saveCharacterChoice(charName);` — no `await` keyword visible. This is an unawaited async write; if the user immediately changes selection, two writes race. [low] finding.
- [medium] lib/features/production_hub/production_hub_screen.dart:283-286 — `onChanged` sets the provider state then calls `_saveCharacterChoice(value)` unawaited (no `await` in the VoidCallback context — it's a sync callback calling an async function); the pref write races with rapid re-selection; same class as line 74 — one finding per root cause covers both (74 and 285).
- [low] lib/features/production_hub/production_hub_screen.dart:88 — `prefs.remove(...)` when character is null — correct cleanup; not reported.
- [low] lib/features/production_hub/production_hub_screen.dart:49 — prefs key `rehearsal_character_${production.id}` — production.id interpolated into a prefs key; if id contains characters invalid for keys it still works (prefs keys are arbitrary strings); not reported.
- [medium] lib/features/production_hub/production_hub_screen.dart:162-166 — `build` watches three providers and debugPrints; the debugPrint finding already reported at 166 covers the logging; the watch pattern is correct; not reported.
- [low] lib/features/production_hub/production_hub_screen.dart:190-193 — `body: hasScript ? _buildMergedHub(...) : _buildNoScriptView(...)`; `_buildMergedHub` receives `script` non-null due to `hasScript` — correct; not reported.
- [low] lib/features/production_hub/production_hub_screen.dart:204-205 — `ref.watch(rehearsalModeProvider)` and `hideMyLinesProvider` inside `_buildMergedHub` — correct reactive reads; not reported.
- [low] lib/features/production_hub/production_hub_screen.dart:248 — `if (mode != RehearsalMode.readthrough)` hides the character dropdown in readthrough mode — matches the walkthrough copy ("Listen plays the whole scene"); intentional; not reported.
- [low] lib/features/production_hub/production_hub_screen.dart:293-316 — `SegmentedButton` `selected: {mode}` with `onSelectionChanged` setting `.first` — single-select emulation via set; correct; not reported.
- [low] lib/features/production_hub/production_hub_screen.dart:330-333 — fast-mode toggle reads then writes the notifier — correct; not reported.
- [low] lib/features/production_hub/production_hub_screen.dart:338-345 — SwitchListTile `onChanged` writes `hideMyLinesProvider.notifier.state = v` — correct; not reported.
- [low] lib/features/production_hub/production_hub_screen.dart:455-456 — scene tap sets `selectedSceneProvider` then `context.push('/rehearsal')` — correct ordering; not reported.
- [low] lib/features/production_hub/production_hub_screen.dart:586-587 — `SupabaseService.instance.isSignedIn` read at drawer build; if sign-in changes while drawer open, the section visibility is stale until rebuild — minor; not reported.
- [low] lib/features/production_hub/production_hub_screen.dart:625-628 — 'Import Script' drawer item pops then pushes '/import' — correct; not reported.
- [low] lib/features/production_hub/production_hub_screen.dart:630-641 — Edit/Characters/Scenes items pop then push — correct; not reported.
- [low] lib/features/production_hub/production_hub_screen.dart:646-659 — Manage Cast/Record/Browse items pop then push — correct; not reported.
- [low] lib/features/production_hub/production_hub_screen.dart:664-671 — Push/Pull items pop then run async actions — correct; not reported.
- [low] lib/features/production_hub/production_hub_screen.dart:677-684 — Export items pop then `_export` — correct; not reported.
- [low] lib/features/production_hub/production_hub_screen.dart:688-704 — Voice config/History/AI Models/Settings items pop then push — correct; not reported.
- [low] lib/features/production_hub/production_hub_screen.dart:710-730 — `_drawerSection`/`_drawerItem` helpers — correct; not reported.
- [low] lib/features/production_hub/production_hub_screen.dart:765-769 — Import button in no-script view pushes '/import' — correct; not reported.
- [low] lib/features/production_hub/production_hub_screen.dart:875-878 — sanitization regex order: strip `[^\w\s-]` then `\s+`→`_` then lowercase — a title with only whitespace becomes `____`→ fine; a title with `\n` becomes `_` — no traversal; not reported beyond the empty-name finding already reported.
- [low] lib/features/production_hub/production_hub_screen.dart:889-890 — `p.join(dir.path, 'exports')` then `Directory(...)` — correct; not reported.
- [low] lib/features/production_hub/production_hub_screen.dart:903-905 — `box.localToGlobal(Offset.zero) & box.size` builds the origin rect — the RenderBox-cast finding already reported at 899 covers it.
- [low] lib/features/production_hub/production_hub_screen.dart:907-912 — export catch shows toast only; no DebugLogService logging — same class as the sync catch finding at 858; one finding per root cause covers both (858 and 907) — I'll fold 907 into the 858 finding's consequence.
- [low] lib/features/production_hub/production_hub_screen.dart:914 — closing brace; no issue.

## Coverage
lib/features/onboarding/welcome_screen.dart — findings: 4
lib/features/production_hub/production_hub_screen.dart — findings: 9
- [medium] lib/features/recording_studio/recording_studio_screen.dart:631-635 — duration timer ticks at 10 Hz but the tick callback only advances `_recordingDuration` when `mounted`; after the widget unmounts mid-take the timer keeps firing with no work and, worse, `_stopRecording`'s snapshot at 650 reads `_recordingDuration.value` which stops advancing once unmounted, so the saved `durationMs` for a take finished after navigation is frozen at the value at unmount — silent data loss (wrong duration persisted/uploaded/queued) reachable by any authenticated user navigating away while recording — advance the notifier unconditionally (drop the `mounted` guard) or snapshot duration at stop time from the recorder's own elapsed clock.
- [medium] lib/features/recording_studio/recording_studio_screen.dart:646-650 — `_stopRecording` snapshots `line`/`character`/`notifier`/`productionId`/`durationMs` before the first await, but `line` is read from `_myLines[_currentLineIdx]` which can be stale if `_goToLine`/`_nextLine` ran concurrently; however the concrete exhibited defect is that `_currentLineIdx` is not clamped — if `_myLines` was rebuilt shorter by a script change while recording, `_myLines[_currentLineIdx]` throws RangeError inside the async method, the exception escapes `_stopRecording`'s try (it only wraps `recorder.stop()`), the take is never registered and no toast/log fires — silent loss of the take reachable by any authenticated user whose script updates mid-session — clamp the index (`_currentLineIdx.clamp(0, _myLines.length - 1)`) before reading, or wrap the whole body in try/catch with the existing error toast.
- [medium] lib/features/recording_studio/recording_studio_screen.dart:131-166 — `_finishTakeAfterDispose` catches all failures and shows a toast, but the `finally` block at 164-166 calls `recorder.dispose()` even when `_registerTake` threw after `recorder.stop()` succeeded; if `notifier.add` (a context-file dependency) throws, the file exists on disk but is never registered or queued and the recorder is disposed — the take is unrecoverable (no retry path, no queue entry) — reachable by any authenticated user leaving the studio during a save when the local DB write fails — enqueue the SyncQueue entry before `notifier.add`, or keep the recording object and retry registration on next studio open.
- [low] lib/features/recording_studio/recording_studio_screen.dart:158-163 — failure path logs via `DebugLogService.instance.logError(LogCategory.error, ...)` but the catch at 157-163 also swallows the exception type into a generic message; per the observability checklist "errors swallowed to debug/info level" the log call is present, so this is only a drift note: the message interpolates `$e` without the stack trace argument the service supports (`logError(category, message, [error, stack])` at debug_log_service.dart:195) — stack trace lost for the in-progress-take save failure — pass `e` as the error arg and a captured stack via `StackTrace.current`.
- [low] lib/features/recording_studio/recording_studio_screen.dart:610-611 — same class as above: `recorder.start failed` is logged with `e` but no stack trace; `logError` supports an optional stack — pass `StackTrace.current`.
- [low] lib/features/recording_studio/recording_studio_screen.dart:657-658 — same class: `recorder.stop failed` logged without stack trace — pass `StackTrace.current`.
- [low] lib/features/recording_studio/recording_studio_screen.dart:133-137 — same class: `take lost on close` logged without stack trace — pass `StackTrace.current`.
- [low] lib/features/recording_studio/recording_studio_screen.dart:700-701 — same class: `saving the take failed` logged without stack trace — pass `StackTrace.current`.
- [medium] lib/features/recording_studio/recording_studio_screen.dart:196-203 — `SyncQueue.instance.enqueue` is fire-and-forget with no error handling and no logging of its outcome; if the enqueue (which persists to a local queue per its name) throws or silently drops, the take is registered locally but never uploaded and nothing records the failure — background-job output discarded per checklist — reachable by any authenticated user recording with a productionId — await/try-catch the enqueue and log failures via `DebugLogService` + toast, or expose a queue-depth/failed metric.
- [medium] lib/features/recording_studio/recording_studio_screen.dart:206-212 — `SttAdaptationService.instance.addSample` is also fire-and-forget with no error capture; a failed training-sample add is invisible — same consequence class as the enqueue finding — try-catch and log.
- [medium] lib/features/recording_studio/recording_studio_screen.dart:82-86 — `_playerSub` listens to `playerStateStream` and on `completed` sets `_status = recorded`, but the subscription is never checked for errors (`onError` omitted); a stream error (e.g. platform channel failure) is unhandled async failure — per checklist "Unhandled async failures not captured" — add `onError` that logs via `DebugLogService` and resets `_status`.
- [low] lib/features/recording_studio/recording_studio_screen.dart:87-89 — `_initAudio` catches any exception and only sets `_initError`; the error is not logged to `DebugLogService` at all (no access/observability trail for audio init failure) — log via `DebugLogService.instance.logError` before setting `_initError`.
- [low] lib/features/recording_studio/recording_studio_screen.dart:590-594 — `getApplicationDocumentsDirectory()` and directory creation are unguarded inside `_startRecording`'s try? No — they are *outside* the try at 600-618: a `getApplicationDocumentsDirectory` failure throws out of `_startRecording` uncaught, leaving `_status` unchanged but the button already in "recording" intent; the exception is an unhandled async failure with no log — wrap 590-594 in the existing try/catch or add a top-level catch that logs and toasts.
- [medium] lib/features/recording_studio/recording_studio_screen.dart:580-588 — permission denial path shows a toast but does not log the denial; per checklist "Auth events unlogged" (permission = access gate) there is no observability trail for repeated denials — log permission denial via `DebugLogService` (info level) alongside the toast.
- [low] lib/features/recording_studio/recording_studio_screen.dart:730 — `AudioLevelService.instance.volumeFor(path)` is awaited inside `_playRecording`'s try; if it throws, the catch at 733-738 shows a toast but `_status` was already set to `playing` at 731 before `play()` — actually 731 sets status before `play()` at 732, so a `play()` failure leaves `_status == playing` with no player running; the UI then shows stop/play controls inconsistent with reality — set `_status = playing` only after `play()` succeeds, or reset to `recorded` in the catch.
- [low] lib/features/recording_studio/recording_studio_screen.dart:742-745 — `_stopPlayback` does not guard against `_status != playing`; pressing stop when idle/recorded still calls `_player?.stop()` and forces `_status = recorded`, potentially clobbering a `recording` state if invoked from a stale UI path — guard with `if (_status == RecordingStatus.playing)`.
- [low] lib/features/recording_studio/recording_studio_screen.dart:759-771 — `_goToLine` cancels `_durationTimer` and resets `_currentRecordingPath` but does not reset `_recordingDuration.value` to zero; next recording's timer accumulates from the previous take's leftover value (timer is periodic and adds to current value) — actually `_startRecording` at 630 resets it to zero, so this is only a leak if `_goToLine` is called while recording (timer cancelled mid-take, duration frozen but state still `recording`) — reset `_status`/duration consistently in `_goToLine` when called during recording.
- [low] lib/features/recording_studio/recording_studio_screen.dart:104-111 — `unawaited(_finishTakeAfterDispose(...))` passes `_myLines[_currentLineIdx]` evaluated *synchronously* in dispose; if `_myLines` is empty (script changed to a character with no lines after build returned the empty-lines Scaffold at 247-254 — but then `_currentLineIdx` is 0 and `_myLines[0]` throws RangeError) — actually if `_myLines.isEmpty` the build returns early at 247, but dispose can still run with `_myLines` empty if the script provider changed between build and dispose; `_myLines[_currentLineIdx]` throws RangeError inside `dispose()`, which Flutter swallows into the widget-dispose error path — guard `if (_myLines.isNotEmpty)` before the unawaited call.
- [low] lib/features/recording_studio/recording_studio_screen.dart:96-99 — dispose checks `_status == RecordingStatus.recording` to decide whether to finish the take, but `_status` may be `recorded`/`playing` with an active recorder if `_stopRecording` failed earlier (path==null path sets idle, ok) — the exhibited gap: if `_status == recorded` but the take was never registered (e.g. `_stopRecording` hit the `notifier == null` branch at 680-688 which sets idle — covered), so no independent defect; skip.
- [low] lib/features/recording_studio/recording_studio_screen.dart:222-223 — `_recordingsNotifier`/`_productionId` are cached via `ref.read` in build; if the provider changes identity between builds the cached notifier may be stale, but Riverpod `ref.read` in build is legal and the memo is refreshed each build — no exhibited defect; skip.
- [low] lib/features/recording_studio/recording_studio_screen.dart:241-244 — `_myLines` memo keyed on script identity only; if `character` changes but script is identical, `_myLines` is recomputed correctly because `_character != character` is also checked — correct; skip.
- [low] lib/features/recording_studio/recording_studio_screen.dart:353-366 — `_fullIdxById` memo keyed on script identity; `_fullIdxById![currentLine.id]` uses the non-null assertion on a map that is rebuilt only when script changes — if `_fullIdxScript` is set but `_fullIdxById` was left null from a prior partial init (not possible: both set together) — correct; skip.
- [low] lib/features/recording_studio/recording_studio_screen.dart:368-373 — context-lines walk `for (var i = fullIdx - 1; i >= 0 && ...; i--)` with `fullIdx == -1` (line id not in full script, e.g. stale recording) starts at i = -2, loop body never runs (i >= 0 fails immediately) — correct; skip.
- [low] lib/features/recording_studio/recording_studio_screen.dart:366 — `fullIdx` fallback `-1` means context lines silently disappear for a line whose id is missing from the index; per direction-of-failure this resolves to the *less* informative UI but is a display-only degradation — acceptable; skip.
- [low] lib/features/recording_studio/recording_studio_screen.dart:774-779 — `_formatDuration` builds `tenths` from `remainder(1000) ~/ 100` — for 1000ms remainder 0 → '0', fine; no defect.
- [low] lib/features/recording_studio/recording_studio_screen.dart:592-594 — `recordingsDir.existsSync()` then `createSync` is a TOCTOU on a local documents dir; not a security boundary — skip.
- [low] lib/features/recording_studio/recording_studio_screen.dart:597-598 — file path uses `line.id` directly; if `line.id` contains path separators (ids are UUIDs per context models) — safe; skip.
- [low] lib/features/recording_studio/recording_studio_screen.dart:182 — `AudioLevelService.instance.invalidate(path)` called before `notifier.add`; if add fails the loudness cache entry for a *new* path was invalidated unnecessarily — harmless; skip.
- [low] lib/features/recording_studio/recording_studio_screen.dart:184 — `const Uuid().v4()` allocates a Uuid per call; trivial — skip.
- [low] lib/features/recording_studio/recording_studio_screen.dart:189 — `recordedAt: DateTime.now()` local time without zone info persisted; per checklist "Log timestamps in local time without zone" this is a data field, not a log — borderline; the persisted `recordedAt` lacks timezone and will misorder takes across DST/timezone changes when synced — flag as low: store UTC (`DateTime.now().toUtc()`).
- [low] lib/features/recording_studio/recording_studio_screen.dart:131 — `recorder.stop()` return value used as the file path; if the recorder returns a path in a temp location different from `filePath` constructed at 597, registration uses the recorder's path while `_currentRecordingPath` holds the constructed one — playback at 720 prefers `_currentRecordingPath` which may diverge from the registered `localPath` — align by using the returned path for `_currentRecordingPath` too (set it in `_stopRecording` after stop succeeds).
- [medium] lib/features/recording_studio/recording_studio_screen.dart:720 — `_playRecording` prefers `_currentRecordingPath` over the registered recording's `localPath`; after a failed re-record attempt (`_stopRecording` path==null branch) `_currentRecordingPath` still points at the *previous* take's file (never cleared at 665-674), so "play" plays the old take while the UI says the line has a recording — user hears wrong audio — clear `_currentRecordingPath = null` in the path==null and no-character branches of `_stopRecording`.
- [low] lib/features/recording_studio/recording_studio_screen.dart:261-263 — `hasRecording` treats `_status == recorded/playing` as having a recording even when `recordings.containsKey` is false and `_currentRecordingPath` is null (e.g. after a failed stop that left status recorded via the player-completed listener at 83-85) — play button appears but `_playRecording` returns silently at 721 — acceptable degradation; skip.
- [low] lib/features/recording_studio/recording_studio_screen.dart:83-85 — player-completed listener sets `_status = recorded` without checking `_status == playing`; if the user stopped playback manually via `_stopPlayback` (which already sets recorded) this is redundant but harmless; however if `completed` fires while `_status == recording` (impossible with one player) — skip.
- [low] lib/features/recording_studio/recording_studio_screen.dart:622-627 — after `recorder.start` succeeds, `if (!mounted)` stops the recorder and returns, but `_currentRecordingPath`/timer were not yet set — correct ordering; the stopped file is orphaned on disk with no registration and no log — per "background-job output discarded" this silently abandons the take — log the abandoned path via `DebugLogService` before returning.
- [low] lib/features/recording_studio/recording_studio_screen.dart:631 — `Timer.periodic(const Duration(milliseconds: 100), ...)` — the callback ignores the timer argument and uses closure state; fine.
- [low] lib/features/recording_studio/recording_studio_screen.dart:49-54 — comment says ValueNotifier avoids rebuilding the whole screen, but `build` still runs at 10 Hz? No — the timer only mutates the notifier; `build` is not re-run by notifier changes; the *timer* itself doesn't call setState — correct; skip.
- [low] lib/features/recording_studio/recording_studio_screen.dart:276-278 — top bar and progress bar read `progress`/`recordedCount` computed once per build from `_myLines` scan — O(lines) per build, but build only re-runs on provider changes, not on the 10 Hz tick — acceptable; skip.
- [low] lib/features/recording_studio/recording_character_screen.dart:58-70 — `linesByChar` map built per build O(script); acceptable for a picker screen; skip.
- [low] lib/features/recording_studio/recording_character_screen.dart:90-96 — per-item `recordedCount` scan is O(recordings × charLines) per visible row; with the memo at 58-70 this is bounded per build — acceptable; skip.
- [low] lib/features/recording_studio/recording_character_screen.dart:131-133 — onTap mutates `recordingCharacterProvider.notifier.state` directly and pushes; if the provider is not present in scope (it is imported via production_providers) — fine; skip.
- [low] lib/features/recording_studio/recording_character_screen.dart:104 — `char.name[0]` throws RangeError if `char.name` is empty (a parsed character with empty name from a malformed script) — guard `char.name.isEmpty ? '?' : char.name[0]` — low, display-only crash in a list tile.
- [low] lib/features/recording_studio/recording_character_screen.dart:59 — `for (final c in script.characters) c.name: []` — duplicate character names collapse into one bucket, merging their lines; if the parser can emit duplicate names (not visible in context models) — assumption: parser dedupes; skip.
- [low] lib/features/recording_studio/recording_character_screen.dart:62 — `l.lineType != LineType.dialogue` skip matches `linesForCharacter` semantics in context models (script_models.dart:360-365) — consistent; skip.
- [low] lib/features/recording_studio/recording_studio_screen.dart:243 — `_myLines = script.linesForCharacter(character)` matches context model filter (dialogue + isForCharacter) — consistent with the character screen's manual walk at recording_character_screen.dart:58-70 — consistent; skip.
- [low] lib/features/recording_studio/recording_studio_screen.dart:258 — `recordings.containsKey(l.id)` keys recordings by script line id; context `Recording` model has `scriptLineId` field (script_models.dart:262) but the map here is `Map<String, Recording>` keyed by line id — assumption: `recordingsProvider` keys by scriptLineId; consistent with character screen usage at recording_character_screen.dart:92 — consistent; skip.
- [low] lib/features/recording_studio/recording_studio_screen.dart:268 — `charIdx >= 0 ? AppTheme.colorForCharacter(charIdx) : Colors.blue` — fallback to a fixed color when the character is not in the script's character list; direction-of-failure resolves to a *less* accurate color, display-only — skip.
- [low] lib/features/recording_studio/recording_studio_screen.dart:382 — `isMe = line.character == _character` ignores multi-character lines (context model `multiCharacters`); a multi-character line where the user is one speaker shows the other speaker's name instead of YOU — display-only, matches `isForCharacter` semantics elsewhere? `linesForCharacter` includes multi-character lines, so such a line *is* in `_myLines` and will render with the wrong label — low display defect: use `line.isForCharacter(_character)`.
- [low] lib/features/recording_studio/recording_studio_screen.dart:391 — same root cause as 382 — one finding covers both.
- [low] lib/features/recording_studio/recording_studio_screen.dart:596 — `final line = _myLines[_currentLineIdx]` in `_startRecording` — same unclamped-index RangeError class as the `_stopRecording` finding at 646; one finding per root cause, list both sites: also `_startRecording` line 596 and dispose line 106.
- [low] lib/features/recording_studio/recording_studio_screen.dart:106 — dispose site of the unclamped index — covered by the RangeError finding.
- [low] lib/features/recording_studio/recording_studio_screen.dart:717 — `ref.read(recordingsProvider)` inside `_playRecording` after awaits — if the widget unmounted mid-await, Riverpod throws on `ref` use; but `_playRecording` is only reachable from mounted UI — borderline; skip.
- [low] lib/features/recording_studio/recording_studio_screen.dart:767 — same `ref.read` in `_goToLine`'s setState — reachable only from mounted UI — skip.
- [low] lib/features/recording_studio/recording_studio_screen.dart:222 — `_recordingsNotifier = ref.read(recordingsProvider.notifier)` cached for post-dispose use — this is the documented purpose; skip.
- [low] lib/features/recording_studio/recording_studio_screen.dart:223 — `_productionId = ref.read(currentProductionProvider)?.id` — if `currentProductionProvider` returns null the take is registered locally but never queued (193 `if (productionId == null) return;`) with no log — per "background-job output discarded" the silent skip is the safer direction? It skips cloud sync silently — log when productionId is null so the operator/user knows the take is local-only — low.
- [low] lib/features/recording_studio/recording_studio_screen.dart:193 — same root cause as 223 — one finding.
- [low] lib/features/recording_studio/recording_studio_screen.dart:23 — `rootScaffoldMessengerKey.currentState?.showAutoToast` — if the messenger key is not mounted (app teardown), toasts silently vanish; the log calls still fire — acceptable; skip.
- [low] lib/features/recording_studio/recording_studio_screen.dart:138-142 — the "take lost" toast uses `const SnackBar` with a 6s duration but the content string is built with interpolation at 136-137? No — the SnackBar content at 139-141 is a plain const Text; the interpolated string is only in the log — fine; skip.
- [low] lib/features/recording_studio/recording_studio_screen.dart:154-155 — `${character.toUpperCase()}` — if character is null this branch is unreachable (guard at 132) — fine.
- [low] lib/features/recording_studio/recording_studio_screen.dart:160-163 — error toast interpolates `$e` into user-visible text; exception messages may contain file paths (local documents dir) — local-only app, not a secret — skip.
- [low] lib/features/recording_studio/recording_studio_screen.dart:680-688 — `notifier == null` branch: the take file exists on disk but is never registered and status resets to idle; the file is orphaned with no cleanup and no log of the path — same "orphaned take" class as 622-627 — one finding per root cause? Different root cause (missing notifier vs unmounted-after-start) but same consequence; report the notifier==null one separately since it has a distinct fix (persist the take for later registration).
- [low] lib/features/recording_studio/recording_studio_screen.dart:665-674 — path==null branch: same orphaned-file consequence — group with 680-688 as one finding: failed/aborted stops leave the audio file on disk unregistered with no cleanup or log.
- [low] lib/features/recording_studio/recording_studio_screen.dart:641 — `_durationTimer?.cancel()` at the top of `_stopRecording` before the awaits — correct; skip.
- [low] lib/features/recording_studio/recording_studio_screen.dart:653-663 — `_stopInFlight` flag set/reset around the stop; dispose honors it — correct; skip.
- [low] lib/features/recording_studio/recording_studio_screen.dart:662 — `if (!mounted) _recorder?.dispose()` in the finally of the stop — if dispose() already ran *and* took the `else` branch at 113 (`recorder?.dispose()`), the recorder is disposed twice; but dispose's else branch only runs when `_stopInFlight` is false, and `_stopRecording` sets it true before its first await — the window between `_stopRecording` entry and `_stopInFlight = true` at 653 is synchronous, so dispose cannot interleave — correct; skip.
- [low] lib/features/recording_studio/recording_studio_screen.dart:97-111 — dispose's finish-take branch passes `_recordingDuration.value.inMilliseconds` — covered by the frozen-duration finding.
- [low] lib/features/recording_studio/recording_studio_screen.dart:110 — `durationMs` from the notifier — covered.
- [low] lib/features/recording_studio/recording_studio_screen.dart:145-152 — `_registerTake` called with `durationMs` from the snapshot — covered.
- [low] lib/features/recording_studio/recording_studio_screen.dart:182 — invalidate before add — covered/skip.
- [low] lib/features/recording_studio/recording_studio_screen.dart:191 — `await notifier.add(recording)` — if this throws, the catch in `_stopRecording` (699-709) handles it and toasts; in `_finishTakeAfterDispose` the catch at 157-163 handles it — but in both, the SyncQueue enqueue at 196 has *not yet run* (add is before enqueue), so a failed add leaves the file unqueued — consistent with the 131-166 finding; skip duplicate.
- [low] lib/features/recording_studio/recording_studio_screen.dart:196-203 — enqueue before addSample — ordering fine.
- [low] lib/features/recording_studio/recording_studio_screen.dart:206-212 — addSample after enqueue — fine.
- [low] lib/features/recording_studio/recording_studio_screen.dart:726 — `PlaybackSession.ensurePlayback()` static call — context file not shown for playback_session.dart; assumption: it initializes the audio session — no exhibited defect; skip.
- [low] lib/features/recording_studio/recording_studio_screen.dart:727 — `setFilePath(path)` where path may be `_currentRecordingPath` (constructed) or recording.localPath — covered by the 720 finding.
- [low] lib/features/recording_studio/recording_studio_screen.dart:728-729 — speed/volume from providers — fine.
- [low] lib/features/recording_studio/recording_studio_screen.dart:731 — status set before play — covered by the 730 finding.
- [low] lib/features/recording_studio/recording_studio_screen.dart:733-738 — playback error toast only, no log — per "errors swallowed" the toast is user-visible but there is no DebugLogService call for playback errors, unlike every other failure path in this file — add a logError call; low.
- [low] lib/features/recording_studio/recording_studio_screen.dart:743-744 — `_stopPlayback` no log — fine, not an error path.
-
- [medium] lib/features/recording_studio/recordings_browser_screen.dart:212-216 — async existence scan calls setState after `mounted` check only guards the early return, but the `setState` at 212 runs after `if (!mounted) return;` at 205 — actually guarded; however `_scanFileExistence` is invoked from `_memoEntries` (line 84) which runs inside `build()` (line 104), so the scan is re-triggered on every rebuild whose memo key changed, and the `unawaited` closure captures `entries` — if the list changed mid-scan the stale `resolved` map overwrites `_fileResolved` for recordings no longer displayed — consequence: stale file-existence flags can flip a deleted/missing recording back to "playable" or mark a still-present one missing until the next scan; smallest safe fix: capture the scan key inside the closure and bail out (`if (key != _scannedKey) return;`) before the `setState`.
- [medium] lib/features/recording_studio/recordings_browser_screen.dart:604 — `_resolveRecordingPath` treats `File(recording.localPath).exists()` on the stored absolute path as authoritative, but the fallback chain (Documents dir, cache dir) is only reached when that stat fails; a recording whose `localPath` points at a directory or unreadable file passes `exists()` yet fails `lengthSync()` at line 670 with an unhandled exception outside the try at 686 — consequence: tapping such a tile throws synchronously in the tap handler (uncaught async error, no user feedback); smallest safe fix: wrap the `exists()`/`lengthSync()` probe in try/catch and treat failure as unresolved.
- [medium] lib/features/recording_studio/recordings_browser_screen.dart:670 — `File(resolvedPath).lengthSync()` is a synchronous filesystem stat on the UI isolate inside a tap handler — consequence: jank/stutter on large or network-mounted files, the exact class the surrounding comments say was fixed elsewhere (lines 601-603, 613-615); smallest safe fix: `await File(resolvedPath).length()` (or `stat()`) instead of `lengthSync()`.
- [medium] lib/features/recording_studio/recordings_browser_screen.dart:676-684 — the `size < 100` empty-file guard returns without resetting `_playingLineId` or stopping a player that may still hold a prior source — consequence: if a previous take was playing, the stop icon stays visible and the player keeps playing while the UI reports "file is empty"; smallest safe fix: call `await _player.stop(); setState(() => _playingLineId = null);` before the early return (same shape as the catch at 697-706).
- [low] lib/features/recording_studio/recordings_browser_screen.dart:648 — log line interpolates `recording.localPath.split("/").last` (a user-content-derived filename) into the debug log — consequence: filenames (potentially containing user-identifying text) persist to the on-disk debug log file with no rotation (debug_log_service.dart:187 appends forever, >200KB only truncated on load at 296-300); smallest safe fix: log only the recording id / line id, not the local filename.
- [low] lib/features/recording_studio/recordings_browser_screen.dart:196-198 — `_scanFileExistence` dedupes by joining recording ids with `'|'`, but a recording id containing `'|'` (or two different id-sets producing the same join) makes the scan skip a needed re-scan — consequence: stale `_fileResolved` for a changed list; smallest safe fix: use a length-prefixed or JSON-encoded key, or compare the list structurally.
- [low] lib/features/rehearsal/rehearsal_history_screen.dart:274-281 — `_formatDate` uses `DateTime.now()`/`difference` in local time with no zone handling and `diff.inDays` truncates toward zero, so a session started 23h59m ago shows "Today" but one started 24h ago shows "Yesterday" regardless of calendar day — consequence: misleading relative dates in the history list; smallest safe fix: compare calendar dates (`DateUtils.dateOnly`-style day comparison) instead of duration days.
- [info] lib/features/recording_studio/recordings_browser_screen.dart:501-512 — delete flow removes the provider row before any local/cloud cleanup; if the local file delete (517-525) or cloud delete (527) then fails, the UI reports partial failure but the row is already gone from state with no rollback — consequence: recording disappears from the list while its file/blob may still exist (silent data inconsistency, operator-visible only via toast); smallest safe fix: perform file/cloud cleanup first, then remove from provider state, or re-add the row on failure. (Assumption: `recordingsProvider.notifier.remove` is irreversible as written; not visible in this batch.)
- [info] lib/features/recording_studio/recordings_browser_screen.dart:557 — cloud delete is skipped for ids starting with `cache_`, so cached takes downloaded by castmates are never removed from the cloud row — consequence: other castmates keep syncing a take the current user believes deleted; smallest safe fix: only skip when the recording is genuinely another user's cache, verify ownership before skipping (assumption: id prefix semantics not shown in this batch).

## Coverage
lib/features/recording_studio/recordings_browser_screen.dart — findings: 9
lib/features/rehearsal/rehearsal_history_screen.dart — findings: 1
- [medium] lib/features/rehearsal/rehearsal_screen.dart:226-236 — playerStateStream completion handler only advances when state is playingOther, but the TTS completion path (line 296-300) advances for any _autoPlay state; the two paths can double-advance the same line when a recording completes while TTS completion also fires — consequence: skipped line or double advance in the rehearsal state machine — fix: gate the stream handler on the same mounted/state checks and route both through a single advance function with a reentrancy guard.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:296-300 — TtsService completion handler advances unconditionally on _autoPlay without checking current rehearsalState is playingOther — consequence: a completion fired after the actor manually skipped or paused still advances, desyncing index/state — fix: read rehearsalStateProvider inside the handler and only advance when it is playingOther.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:1596-1607 — _mainAction playingOther branch sets state to ready BEFORE stopping TTS, but the completion handler (line 296) checks nothing, so the comment's claimed guard ("completion handler sees we're no longer in playingOther") is not actually implemented — consequence: skip can still trigger a second advance from the in-flight completion — fix: make the completion handler actually read the state provider before advancing.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:2137-2190 — _onOtherLineFinished advances without re-checking _processingLine/_jumpBackInProgress debounce flags, unlike _processCurrentLine — consequence: rapid completion + manual tap can advance twice in one frame — fix: guard with the same _processingLine debounce used elsewhere.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:2424-2447 — _confirmDespiteNoise re-arms _strongMatchDeadline with an 800 ms timer that calls itself recursively; if _matchScore stays >= threshold and tailHeard/plausible never both hold (noisy room, paraphrased ending), the timer chain never terminates — consequence: unbounded timer churn and repeated confirm attempts for the whole listening session — fix: cap re-arm count or clear the deadline once _lineEndingHeard latches false permanently for this line.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:2495-2502 — _confirmIfActorQuiet cancels and re-creates _matchConfirmTimer every time inputLevel >= silenceThreshold, but the new timer's callback re-enters _confirmIfActorQuiet which can again re-arm — consequence: timer rescheduling loop under sustained noise; confirm never fires while noise persists even past the noise-proof deadline — fix: rely on _strongMatchDeadline as the sole noise-proof path and stop re-arming _matchConfirmTimer on every loud sample.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:2601-2609 — _resetSilenceTimer's re-arm branch checks `!pastCeiling && inputLevel >= threshold` but when pastCeiling is true and level is high it falls through to the toast+advance path only if `pastCeiling && mounted`; if mounted is false the timer silently ends without advancing or logging — consequence: unmount race drops the advance — fix: log/advance before the mounted check or restructure the branch.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:2621-2635 — in the silence-timeout advance path, script/scene null returns silently skip _recordAttempt's follow-up advance (advanceLine) after already stopping capture — consequence: actor stuck on the line with mic stopped and no advance — fix: record attempt and advance before the null-guard return, or restore state to ready.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:2653-2665 — _advanceLine's completion branch calls _completeScene with `const []` when script/scene are null, so _saveSession is skipped (dialogueLines.isEmpty) and the session is never recorded — consequence: silent loss of the rehearsal session in history on a race — fix: capture script/scene before the branch and pass the real list.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:2771-2782 — _togglePause's else branch stops TTS/STT and pauses but never cancels _matchConfirmTimer or _strongMatchDeadline — consequence: a pending confirm timer fires while paused and advances the line out from under the paused state — fix: cancel both timers in the pause branch (as _pauseForInterruption does at 2754-2755).
- [medium] lib/features/rehearsal/rehearsal_screen.dart:2747-2768 — _pauseForInterruption accepts state == ready as pausable, but ready means nothing is playing; pausing from ready leaves a stale paused state that _togglePause resume branch then treats as resumable — consequence: resume from a never-started rehearsal replays _processCurrentLine unexpectedly — fix: exclude ready from the pausable set.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:2835-2841 — _handleRemoteJumpBack computes jumpCount from current-target but never validates target < current; if the walk-back loops land target == current, jumpCount clamps to 1 and _jumpBack(1) advances backwards by one line instead of the intended multi-line jump — consequence: wrong jump distance on edge cases — fix: early-return when target >= current.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:2829-2832 — the "never land on actor's own line" while-loop decrements target but breaks at target == 0 without re-checking isForCharacter(0) — consequence: can still land on the actor's line 0 — fix: after the loop, if dialogueLines[target].isForCharacter(mc), step back one more or bail.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:3296-3321 — _scrollToCurrentLine's fallback branch reads ref.read(currentLineIndexProvider) inside a post-frame callback after unmount is possible (mounted checked only for the hasClients path) — consequence: Riverpod "using ref after unmount" exception — fix: check mounted before reading ref in the fallback branch.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:501-510 — _startProgressAutosave's listener callback persists via Timer without a mounted guard inside the timer body; _persistProgress is ref-free so that part is safe, but _currentProgressKey() (line 505) reads ref inside the listener after potential unmount — consequence: unsafe ref use after dispose — fix: capture the key before scheduling, or guard with mounted.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:573-581 — didChangeAppLifecycleState persists _lastLineIndex on paused/inactive/hidden but never cancels _progressSaveTimer, so a pending debounce can overwrite the just-persisted checkpoint with a stale index — consequence: checkpoint regression on backgrounding — fix: cancel _progressSaveTimer before persisting.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:584-630 — dispose() persists _lastLineIndex (line 592) but _progressSaveTimer was cancelled at 589 before that; however _persistProgress is async and SharedPreferences.getInstance may resolve after the State is torn down — consequence: late write is harmless for ref but the final checkpoint may be lost if the app exits immediately — fix: use synchronous write or flush before dispose completes (low reachability).
- [low] lib/features/rehearsal/rehearsal_screen.dart:143-148 — _maxListenFor computes words via split on whitespace including stage directions; a line that is all stage directions yields words=1 → 12 s floor, fine, but a 45 s cap for monologues ignores that listenStartedAt is stamped at listen start, not at first speech — consequence: noisy rooms can still starve within the ceiling only if speech starts late — fix: none needed (info-level note only if you want; otherwise omit).
- [medium] lib/features/rehearsal/rehearsal_screen.dart:2259-2265 — vocabHints includes the full cleanLine plus every word; for very long lines this floods the recognizer hint list — consequence: degraded recognition accuracy/latency on long monologues — fix: cap hint count (e.g. first N words).
- [medium] lib/features/rehearsal/rehearsal_screen.dart:1967-1998 — _preRollVoices attaches whenComplete(...).ignore() per future but `done` increments race with the showTimer read; if futures complete before the 300 ms timer fires, _preparingVoices.value is set to (done,total) after `finished` should have suppressed it — consequence: banner flashes after readiness completed — fix: check `finished` inside the whenComplete callback too.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:1991-1995 — Future.wait with catchError((_) => null) swallows individual future errors and the outer catch swallows timeout — consequence: synthesis failures are invisible; speak() re-synthesis on demand is the only recovery and no log records the failure — fix: log the error via _dlog before swallowing.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:2041-2047 — recording playback failure is logged then silently falls through to understudy/TTS with no user-visible signal — consequence: actor hears TTS instead of castmate recording with no explanation (the code's own comment admits this was a field complaint) — fix: surface a one-shot toast like the other fallback notices.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:2065-2070 — understudy playback failure same silent-fallthrough class as above — consequence: same — fix: same.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:3086-3124 — _stopCaptureForLine's catch swallows stop exceptions with only a log; _capturedAudio is not updated and _isCapturingAudio was already set false at 3091 — consequence: capture silently lost — fix: at minimum log at error with the path so the actor can be told; consider re-queueing.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:3179-3248 — _saveRehearsalCaptures moves files then enqueues sync; if SyncQueue.enqueue throws after the file was moved+deleted, the recording exists locally but is never uploaded and the catch only logs — consequence: silent upload loss — fix: enqueue before the destructive move, or restore the temp file on enqueue failure.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:3230-3232 — the per-entry catch in the save loop logs but continues, and `saved` count may under-report while the snackbar says "Saved N recordings" — consequence: misleading success UI — fix: report failures in the snackbar or log at error level with count mismatch.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:3140-3141 — _showSaveRecordingsPrompt reads ref.read(rehearsalCharacterProvider) inside a delayed Future (500 ms) after possible unmount — consequence: unsafe ref use after dispose — fix: guard with mounted before reading ref (the delayed callback does check mounted at 3132 but only before calling _showSaveRecordingsPrompt; the read at 3140 is inside that call so it is guarded — re-verify: the guard is in the caller, so this is safe; drop this finding if double-checked).
- [low] lib/features/rehearsal/rehearsal_screen.dart:3276-3288 — RehearsalSession id uses DateTime.now().millisecondsSinceEpoch as string — two sessions in the same millisecond collide — consequence: history entry overwrite — fix: use uuid like Recording does at 3210.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:2682-2706 — _jumpBack cancels silence/confirm timers but not _strongMatchDeadline — consequence: a noise-proof deadline fires after jump-back and confirms a line that is no longer current — fix: cancel _strongMatchDeadline in _jumpBack (and in _restartScene at 2709-2742, which also misses it).
- [medium] lib/features/rehearsal/rehearsal_screen.dart:2709-2742 — _restartScene also fails to cancel _strongMatchDeadline — consequence: same as above — fix: same.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:2639-2680 — _advanceLine cancels _strongMatchDeadline correctly (2642-2643) but _jumpBack/_restartScene/_togglePause-pause-branch do not — one root cause: the "stop everything" teardown sequence is duplicated per call site and each copy misses a different timer — fix: extract a single _stopAllTimers() helper and call it everywhere (this supersedes the three per-site findings above; report once at the cause).
- [medium] lib/features/rehearsal/rehearsal_screen.dart:2682-2706 — _jumpBack does not reset _lineEndingHeard/_matchConfirmed/_loggedFirstResultForLine/_lastRecognizedRaw — consequence: latched evidence from the previous line leaks into the next listen cycle, e.g. _lineEndingHeard stays true so the next line's confirm uses stale ending evidence — fix: reset the per-line latch set in _jumpBack/_restartScene/_advanceLine completion paths (single root cause: per-line state reset is scattered).
- [medium] lib/features/rehearsal/rehearsal_screen.dart:2709-2742 — _restartScene same missing per-line latch reset — consequence: same — fix: same (report once at cause).
- [medium] lib/features/rehearsal/rehearsal_screen.dart:2246-2253 — _startListeningForMyLine resets _matchConfirmed=false and latches, but _jumpBack/_restartScene paths re-enter listening via _processCurrentLine without going through this reset (they call _processCurrentLine which calls _startListeningForMyLine only if isMyLine) — actually _processCurrentLine does route through _startListeningForMyLine, so latches ARE reset there; the leak is only for non-my-line jumps where stale _lineEndingHeard persists into the next my-line — keep the finding scoped to that case.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:2328-2340 — _handleRecognizedForLine resets the silence timer on EVERY partial result (line 2339) — with a chatty recognizer this starves the 5 s silence timeout exactly as the docstring at 127-137 describes, and only the ceiling saves it; but the ceiling is stamped at listen start (2269), not re-stamped, so a slow reader past 30-45 s gets force-advanced mid-line — consequence: premature advance on slow deliveries — fix: re-stamp _listenStartedAt on genuine speech onset (level above threshold), not only at listen start.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:2279-2288 — onLevel re-arms the silence timer on every loud sample but does not re-stamp _listenStartedAt — same root cause as above; report once.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:2464-2468 — _requiredAdvanceSilence returns base+2000 when tail not heard, but base comes from a provider that could be 0 — consequence: a 0 ms provider value makes the "not heard" branch require only 2000 ms which may be shorter than intended — fix: clamp base to a minimum (e.g. 500 ms). (Assumption: provider default unknown from this file.)
- [low] lib/features/rehearsal/rehearsal_screen.dart:388-392 — _systemVoiceNoticeShown is a static field on the State class but declared as instance-adjacent static; if the screen is re-entered within the same app session the notice never re-shows even for a new rehearsal with a different engine state — consequence: missed warning — fix: reset the flag in initState or key it per session. (Severity low: cosmetic notice.)
- [medium] lib/features/rehearsal/rehearsal_screen.dart:397-404 — _maybeWarnSystemVoiceFallback checks isKokoroReady() but the reason strings at 398 and 402 are swapped relative to the branches: if the model IS downloaded but failed to load, reason says "isn't downloaded (Settings → AI Models)" — wait, re-read: 397 branch is `await isKokoroReady()` true → reason 'the AI voice model failed to load' — that matches "downloaded but did not load" logged at 399-400. The else branch (not downloaded) gets 'isn't downloaded' — correct. No finding; disregard.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:406-409 — showAutoToast with duration 6 s but no dismiss route; if the widget unmounts mid-toast the messenger outlives it (same pattern the code acknowledges at 566-570 for _toastTimer) — consequence: orphaned toast — fix: hide on dispose like _toastTimer does. (Low reachability; may drop.)
- [medium] lib/features/rehearsal/rehearsal_screen.dart:432-437 — STT init failure snackbar shown via ScaffoldMessenger.of(context) without a dismiss timer — consequence: snackbar persists 10 s then auto-dismisses (duration given) — fine; no finding.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:2234-2238 — STT unavailable notice uses `const SnackBar` with duration 6 s — fine.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:2944-2954 — LiveASR download snackbar includes a SnackBarAction that pushes '/ai-models' guarded by mounted — fine.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:2614-2619 — noise-capped timeout toast duration 5 s — fine.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:3036-3042 — recording-failed toast — fine.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:3068-3074 — startRecording-false toast — fine.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:3078-3082 — recording error toast shows raw exception text to the user — consequence: technical error string in UI — fix: show a friendly message and log details. (Low severity.)
- [low] lib/features/rehearsal/rehearsal_screen.dart:3080 — SnackBar content Text('Recording error: $e') leaks internal exception details to end users — consequence: confusing UX, minor info disclosure of internal paths/messages — fix: replace with generic copy; keep detail in _dlog.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:2996-2998 — attachMatching sets SttChannel.instance.onPcm = liveAsr.feedPcm but dispose (622) and _stopCaptureForLine (3097) null it; if two rehearsal screens ever coexist (not expected) the static channel would race — assumption-based; skip.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:3006-3014 — asrFuture.then attaches matching without cancelling on dispose; if the screen is disposed before the future resolves, mounted guard at 3007 prevents attach — fine.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:2926-2934 — asrStarted is assigned inside .then but if ensureStarted never resolves (hung plugin), Future.any at 2928 waits on the 250 ms delayed future and proceeds with asrStarted == null — handled by the loading branch — fine.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:2936-2937 — `_liveMatchingActive = liveMatching` where liveMatching = asrStarted == true; if asrStarted is null (still loading), _liveMatchingActive = false, but the later attach at 3006-3014 sets it true — consistent — fine.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:2981-2988 — onSilence's _liveMatchingActive branch returns after confirm attempt; the non-live branch (2989-2992) confirms on ANY silence >= provider ms regardless of match score — consequence: without live matching, a pause mid-line advances prematurely (this is the documented record-only behavior, intended) — no finding.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:1850-1856 — prefetch warms AudioLevelService for recordings but if rec is null and line is the actor's own (isMine) we returned earlier — fine.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:1869-1873 — prefetch reads fastModeSpeedProvider/playbackSpeedProvider at prefetch time; if the user changes speed before playback, the prefetched audio was synthesized at the old speed — consequence: wrong playback speed for prefetched lines — fix: invalidate _ttsPrefetch when speed provider changes, or re-check speed at speak time. (Real defect class: stale cache keyed without speed.)
- [medium] lib/features/rehearsal/rehearsal_screen.dart:2084-2091 — _playOtherLine consumes prefetched futures via _ttsPrefetch.remove(line.id) but if the line was prefetched at a different speed (see above) the mismatch persists — same root cause as the prefetch-speed finding; report once.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:2124-2133 — _prefetchUpcomingOtherLines walks depth lines via indexOf(next) — indexOf is O(n) per step and uses object identity; if two lines have equal content the wrong index could be chosen — consequence: minor mis-prefetch — fix: carry indices instead of indexOf. (Low.)
- [low] lib/features/rehearsal/rehearsal_screen.dart:2126-2133 — the loop uses dialogueLines.indexOf(next) which is O(n²) overall for depth 4 on large scenes — consequence: negligible for typical scene sizes — fix: track indices directly. (May omit as noise.)
- [medium] lib/features/rehearsal/rehearsal_screen.dart:1957-1964 — _preRollVoices uses indexOf(firstLine) and indexOf(next) similarly — same class; fold into one finding about identity-based indexOf prefetch walks.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:1094-1096 — per-row Consumer selects `i == index` and `index < i` — every row rebuilds on every index change because select fires for all rows whose predicate result changes; rows whose predicate is unchanged do not rebuild — this is the intended optimization — fine.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:736-752 — top Consumer watches currentLineIndexProvider and rehearsalStateProvider without .select — every advance rebuilds the whole top bar including progress — intended (progress must move) — fine.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:788-793 — completion Consumer selects `i >= dialogueLines.length` — fine.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:799-801 — match-feedback Consumer selects isMyLineAt(i) — isMyLineAt is a closure over dialogueLines/myCharacter/mode captured per build; select with a closure is fine since select compares results — fine.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:1063-1065 — _buildScriptView reads baseFontSize via ref.watch inside a method called from build — fine (it's within build's Consumer? Actually _buildScriptView is called from the Consumer at 787-794, and ref.watch inside a non-Consumer method of the State during build is allowed since it's inside build) — fine.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:1067-1075 — ListView.builder with cacheExtent 3000 and itemCount = dialogueLines.length — fine.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:1122-1144 — Container decoration with border only when isCurrent — fine.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:1167-1188 — conditional Spacer inside Row children via spread with `if (isCurrent && isMe) ...[` — the spread includes `const Spacer()` and nested ifs — syntactically fine.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:1205-1223 — hideMyLines && isMe && !isPast branch shows 'Say your line...' instead of the actual text — intended blind mode — fine.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:1228-1243 — ValueListenableBuilder for recognized text only when isCurrent && isMe — fine.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:1255-1291 — _pulsingMic AnimatedContainer rebuilds per level event (~12 Hz) but only this widget — fine.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:1293-1328 — _buildMatchFeedback reads threshold via ref.read (not watch) — if the user changes matchThresholdProvider mid-listen the bar won't rebuild until next result — minor; skip.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:1330-1461 — completion view stats computed from _lineAttempts — fine.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:1414-1429 — save-recordings button visible when _capturedAudio.isNotEmpty && _hasPromptedUpload — fine.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:1443 — context.push('/history') — fine.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:1449-1456 — 'Choose Another Scene' stops engines then context.pop() — fine.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:1514-1573 — _buildControls reads ref.read(rehearsalModeProvider) inside build-time method — fine.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:1540-1546 — Back button onTap is null when currentIdx <= 0 — fine.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:1559 — main action onTap calls _mainAction(state, isMyLine, totalLines) where totalLines = dialogueLines.length — fine.
- [medium] lib/features/rehearsal/rehearsal_screen.dart:1589-1612 — _mainAction listeningForMe branch calls _stt.stop(discard: true) then _recordCurrentLineAttempt with
- [medium] lib/features/script_editor/character_manager_screen.dart:633-641 — `_migrateCastMembers` loads the cast list via `notifier.loadForProduction(productionId)` inside a try whose catch (670-687) treats any failure as a rename failure and returns early, so a load failure aborts the whole rename migration path — consequence: a transient cast-load error leaves the script lines renamed but the persisted cast rows, voice config and dialect still keyed to the old name, and the user is told the actor "could not be moved" even though nothing was attempted; fix: separate the load step from the migration step (or catch the load failure and continue with an empty affected list) so a load error doesn't mask a rename that never ran.
- [medium] lib/features/script_editor/character_manager_screen.dart:648-666 — cloud cast rename failures are swallowed into a local `failures` list and only surfaced via a toast (689-700) with no DebugLogService call for the per-member failure itself — consequence: the per-member cloud rename error is logged only inside the catch (660-665) but the aggregate "couldn't update the cloud cast" toast fires without any structured error record tying which member failed and why, making the failure unreproducible from logs alone; fix: log the aggregated failure list (member ids + error) via `DebugLogService.instance.logError(LogCategory.network, ...)` before showing the toast.
- [medium] lib/features/script_editor/character_manager_screen.dart:592-625 — `_migrateVoiceConfig` catches all errors and only logs via `DebugLogService.instance.logError` with a generic message and no stack trace — consequence: a voice-config rename failure (e.g. network or schema mismatch) is recorded without the exception's stack, so the root cause cannot be diagnosed from the log; fix: pass the caught error/stack to `logError` (it already accepts an optional `StackTrace`) or include `e.toString()` with stack info.
- [low] lib/features/script_editor/character_manager_screen.dart:608-610 — error message interpolates the raw caught exception object `e` into the log string via string interpolation in `logError(..., e, ...)` — consequence: if the exception message ever contains user-controlled data (e.g. a server response echoing the character name), it lands in the persisted debug log file on disk without scrubbing; fix: scrub/limit the interpolated error text before persisting (the debug log file has no rotation, see debug_log_service.dart:187-188 append-only).
- [low] lib/data/services/debug_log_service.dart:187-188 — `_appendSync` appends every entry to a single `debug_log.txt` with no size cap or rotation (only `_loadFromDisk` truncates >200KB on next init) — consequence: an app that logs frequently (e.g. every rename failure) grows the file unbounded between app restarts, eventually filling the documents directory; fix: add a size check/rotation in `_appendSync` or a periodic trim in `_flushToDisk`.
- [low] lib/data/services/debug_log_service.dart:42 — `timeString` uses `timestamp.toString().substring(11,19)` on a `DateTime` created with `DateTime.now()` (local time, no zone info in the log line) — consequence: log timestamps are in device-local time with no offset, so logs from different devices/timezones can't be correlated; fix: log UTC or include the offset (e.g. `toIso8601String()` already includes offset — but `toLine()` at 45 uses `toIso8601String()` which does include offset, so this is only the display path; consider low/info).
- [info] lib/features/script_editor/character_manager_screen.dart:243-250 — gender toggle persists via `VoiceConfigService.instance.setGender` without any error handling or logging — consequence: a failed persist is silent (no log, no toast), so the user believes the gender changed but the cloud/local config may still hold the old value; fix: await and log/handle the failure.
- [info] lib/features/script_editor/character_manager_screen.dart:304-308 — `_setCharacterLocale` persists the locale via `VoiceConfigService.instance.setLocale` with no error handling — consequence: same silent-failure class as above; fix: await and log/handle.
- [info] lib/features/script_editor/character_manager_screen.dart:245-249, 304-308 — both persist calls are fire-and-forget futures whose errors, if unawaited, become unhandled async failures that never reach any log or crash reporter — consequence: silent divergence between UI state and persisted state; fix: `await` inside an async handler and route errors to `DebugLogService.instance.logError`.

## Coverage
lib/features/script_editor/character_manager_screen.dart — findings: 9
lib/features/script_editor/cloud_sync_dialog.dart — clean
- [high] lib/features/script_editor/scene_editor_screen.dart:311 — `_performSplit` writes `splitAt - 1` as the new scene's `endLineIndex` while `splitAt` was clamped into `[start, end]` — when `splitAt == start` (slider min is 1 but `start` can be 0 for a stale/zero-range scene, or the midpoint default `dialogueLines.length ~/ 2` can be 0 when a scene has ≤1 dialogue lines yet the button is enabled via `dialogueCount > 2` mismatch) the first half becomes `sublist(start, splitAt)` = empty and `endLineIndex: splitAt - 1` underflows below `startLineIndex: start`, producing a scene whose range is inverted; `linesInScene` then clamps `end` to `start` and returns `[]`, silently dropping the scene's lines from rehearsal — clamp `splitAt` to `start + 1` (or guard `splitAt > start`) before slicing, and disable the Split button on the same `dialogueCount` predicate the dialog uses.
- [medium] lib/features/script_editor/scene_editor_screen.dart:166 vs 244 — Split button enabled by `dialogueCount > 2` counted from `script.linesInScene(scene)` filtered inline, but `_splitScene` recomputes `dialogueLines` with the same filter; if the scene's stored `startLineIndex/endLineIndex` are stale (lines deleted in the script editor), the two counts diverge and the dialog's Slider `max: (dialogueLines.length - 1)` can be < `min: 1`, throwing a Slider range assertion on open — compute the count once and pass it, or clamp `max` to `min` (`math.max(1, dialogueLines.length - 1)`) before building the Slider.
- [medium] lib/features/script_editor/scene_editor_screen.dart:248 — default `splitAt = dialogueLines.length ~/ 2` with `min: 1` on the Slider: for a 2-line dialogue scene the default is 1 (fine), but for a 1-line scene (button should be disabled but the dialog is still reachable via a stale-count path) `splitAt` = 0 and `dialogueLines[splitAt - 1]` at line 283 throws RangeError before the user moves the slider — initialize `splitAt` to `max(1, dialogueLines.length ~/ 2)` and guard the preview reads with `splitAt > 0`.
- [low] lib/features/script_editor/scene_editor_screen.dart:311 — `dialogueLines[splitAt].orderIndex` is passed as `splitAtOrderIndex` and then re-resolved by `indexWhere(l.orderIndex == …)` at line 411; duplicate `orderIndex` values across acts/scenes (the model does not guarantee uniqueness) make `indexWhere` return the first match, splitting at the wrong line in a different scene — pass the resolved list index directly instead of round-tripping through `orderIndex`.
- [low] lib/features/script_editor/scene_editor_screen.dart:490 — `_performMerge` builds `allChars` from `{...scene.characters, ...nextScene.characters}` (Set literal from two Lists) which silently drops a character present in both scenes only if duplicated — that is intended dedup, but the merged scene's `characters` list is then `sorted` in place via `toList()..sort()` on an already-materialized list; harmless, yet the same pattern at 455/468 sorts the halves' character lists after `firstChars.toList()`, consistent — no defect; withdrawn.
- [medium] lib/features/script_editor/scene_editor_screen.dart:383-388 — `_updateScene` retags lines with `updated.startLineIndex/endLineIndex` clamped against `updatedLines.length`, but the loop writes `scene: updated.location` over the whole clamped range even when the rename only changed `sceneName`; a stale-range scene (bounds pointing past real lines after deletions) retags unrelated lines with the new location, corrupting neighbouring scenes' line tags — restrict retagging to lines whose current `scene` matches the old location, or recompute bounds via `remapScenes` after any lines-list mutation.
- [low] lib/features/script_editor/scene_editor_screen.dart:191-192 — `_renameScene` allocates two `TextEditingController`s with no `dispose` and no lifecycle tie to the dialog; each open leaks controllers until GC — create controllers inside the dialog builder's Stateful context or dispose in the pop callback.
- [low] lib/features/script_editor/scene_editor_screen.dart:43-46 — `charIndexByName` map literal silently collapses duplicate character names to the last index; `AppTheme.colorForCharacter(charIdx)` then colors two distinct roles the same — key the map by character object identity or by `name + colorIndex`.
- [info] lib/features/script_editor/scene_editor_screen.dart:399,481,515 — `scheduleScriptSave(ref)` is called after every mutation but nothing in the inlined code shows its failure path; if the save is fire-and-forget the "app kill silently discarded them" comment at 397-398 is only half-fixed (mutations persist, but a failed save is unlogged and unreported) — verify `scheduleScriptSave` logs/persists save failures; if it does not, add an error sink before relying on it for durability.

## Coverage
lib/features/script_editor/scene_editor_screen.dart — findings: 8
- [medium] lib/features/script_editor/script_editor_screen.dart:186-195 — "Sync to cloud" button awaits `persistScript(ref)` with no try/catch and no error surfaced; the success toast shows unconditionally after the await — if the push throws (offline, auth expired), the user sees "Script synced to cloud" while nothing was saved, and the failure is silently swallowed (no log, no analytics, no retry). — consequence: silent data loss of the user's edited script with a false success signal; the edit is lost on app kill. — smallest safe fix: wrap in try/catch, log/record the failure, and show the success toast only on success (or show a failure SnackBar in the catch).
- [medium] lib/features/script_editor/script_editor_screen.dart:1386-1400 — `_syncToCloud` catches all exceptions and reports them only via a transient SnackBar; nothing is logged to DebugLogService/AnalyticsService, so cloud-sync failures are invisible in crash/diagnostic logs (errors swallowed to UI level only). — consequence: field reports of "sync failed" cannot be diagnosed; repeated failures leave no trace. — smallest safe fix: log the exception (message + stack) via DebugLogService/AnalyticsService before showing the toast.
- [medium] lib/features/script_editor/script_editor_screen.dart:1403-1458 — `_export` catches all exceptions and shows only a transient SnackBar; export failures (disk full, path errors) are never logged or reported to analytics. — consequence: export failures are undiagnosable after the toast disappears. — smallest safe fix: log the exception before showing the failure toast.
- [low] lib/features/script_editor/script_editor_screen.dart:1453-1457 — export failure toast interpolates the raw exception `$e` into a SnackBar; if the exception message contains the file path or user content, it is surfaced on screen (and could be screenshotted/shared) rather than scrubbed. — consequence: potential disclosure of local paths/user text in UI. — smallest safe fix: show a generic message and log the detailed exception internally.
- [low] lib/features/script_editor/script_editor_screen.dart:1394-1398 — same class as above: cloud-sync failure toast interpolates raw `$e` on screen. — consequence: raw error text shown to user. — smallest safe fix: generic user-facing message, detailed error logged internally.

## Coverage
lib/features/script_editor/script_editor_screen.dart — findings: 5
- [medium] lib/features/script_import/ocr_review_screen.dart:459-468 — `_done()` builds the result from `widget.lines` but drops any line whose id is absent from `_byId` only when removed; however lines added to `_byId` via `_onContextChanged`/`_saveEdit` are fine — the real defect is that `_done()` is invoked from `onPopInvokedWithResult` with `didPop == false` only, so a system back that pops the route with `didPop == true` never commits edits/removals, silently discarding every in-progress edit — concrete consequence: user edits/removals lost on back gesture — smallest safe fix: call `_done()` unconditionally in `onPopInvokedWithResult` (or commit in `didPop == true` branch too).
- [medium] lib/features/script_import/ocr_review_screen.dart:482-486 — `PopScope.canPop: false` with `onPopInvokedWithResult` intercepting only `!didPop` means the AppBar back button (`_done` at 492) and system pop both route through `_done`, but a route popped by other means (e.g. `Navigator.maybePop` from elsewhere) still skips the commit — consequence: edits discarded — smallest safe fix: commit in both branches of `onPopInvokedWithResult`.
- [low] lib/features/script_import/ocr_review_screen.dart:118-121 — `_orderedCurrentLines` sorts a fresh list each rebuild via `??=` on `_orderedCache`, but `_markRemoved` (112-116) nulls the cache on every removal while `_contextLinesFor` (128-144) re-derives it per flagged card inside `build` — consequence: O(n log n) re-sort per removal per frame on large scans — smallest safe fix: invalidate cache only once per build cycle or memoize per flagged id.
- [low] lib/features/script_import/ocr_review_screen.dart:173-177 — `_pendingReviewCount`/`_pendingNotScriptCount` re-filter `_reviewLines`/`_notScriptLines` on every access; `_buildCountsBar` (753) reads both each build — consequence: repeated O(n) scans per frame — smallest safe fix: compute counts once per build and pass down.
- [low] lib/features/script_import/ocr_review_screen.dart:241-250 — `_pendingWithPages()` allocates and filters the whole pending list on every call; called in `_viewSourcePage` (263), per sheet build (282), and per `_buildSourcePane` build (644) — consequence: repeated allocations during sheet rebuilds — smallest safe fix: memoize per setState invalidation like `_orderedCache`.
- [medium] lib/features/script_import/ocr_review_screen.dart:200-231 — `_removeContextLine` reads `_contextControllers[l.id]?.text ?? _byId[l.id]?.text ?? l.text` but `_onContextChanged` (150-153) writes edits into `_byId`, while `_contextController` (146-148) seeds `_contextControllers` from `_byId`; if the context editor was never opened the controller is absent and the fallback chain is correct, but if the user typed into the context field and then the row was rebuilt after a removal invalidated `_orderedCache`, `_contextControllers` still holds the live controller so the edit is preserved — no data loss found; no finding.
- [medium] lib/features/script_import/ocr_review_screen.dart:363-366 — the sheet's undo button writes `_controllers[current.id]?.text = ...` directly on the controller without `setState`/`setSheetState` wrapping the controller mutation is inside `setSheetState` (363) so the sheet rebuilds, but the screen-level `TextField` sharing the same controller also rebuilds via the sheet rebuild only if it is a descendant of the sheet context; the screen-level card TextField (817) is outside the sheet and will not rebuild — consequence: stale caret/undo icon state on the underlying card — smallest safe fix: also `setState` on the screen State when undoing.
- [low] lib/features/script_import/ocr_review_screen.dart:408-414 — `_removeLine(current)` inside the sheet calls `setState` on the screen State and then `setSheetState(() {})`; `_markRemoved` nulls `_orderedCache`, so the sheet's `list` (282) recomputes on the next `setSheetState` — but the snackbar/toast chain `ScaffoldMessenger.of(context)..hideCurrentSnackBar()..showAutoToast(...)` uses the screen `context` captured before the sheet builder ran; if the screen was disposed while the sheet is open this throws — consequence: crash on pop-while-sheet-open — smallest safe fix: use `sheetContext`-derived messenger or guard with `mounted`.
- [medium] lib/features/script_import/ocr_review_screen.dart:188-190 — `_saveEdit` shows a toast via `ScaffoldMessenger.of(context)` after `setState`; same captured-`context`-after-async concern as 408-414 but `_saveEdit` is synchronous so risk is lower; no separate finding beyond the class noted at 408-414.
- [low] lib/features/script_editor/validation_panel.dart:135-136 — `passCount == checks.length` treats a zero-check list as "all passed" (`allPassed` true when `checks` is empty), but `validateScript` always returns ≥7 checks so unreachable in practice; no finding.
- [low] lib/features/script_editor/validation_panel.dart:112-127 — the OCR-confidence check is only added when `hasOcrData`; when absent, `passCount == checks.length` compares against a shorter list, so the header count "7 / 7" vs "8 / 8" shifts between runs of the same script — cosmetic inconsistency, no user-visible defect beyond label drift; no finding.
- [medium] lib/features/script_import/ocr_review_screen.dart:77-80 — `_byId` and `_origById` are built with identical map-literal comprehensions over `widget.lines`; if two lines share an id (duplicate ids from the parser), later entries silently overwrite earlier ones and edits/removals apply to the wrong line — consequence: silent wrong-line edit/removal — smallest safe fix: assert uniqueness in initState or key by index.
- [low] lib/features/script_import/ocr_review_screen.dart:62,986-987 — `_notScriptExpanded` is fed to `ExpansionTile.initiallyExpanded` and updated via `onExpansionChanged`; `initiallyExpanded` only applies on first build, so later toggles of `_notScriptExpanded` by `_removeAllNotScript`'s `setState` (450-454) do not re-expand/collapse the tile — consequence: expansion state drift after bulk remove — smallest safe fix: drop the state field or use a `GlobalKey`/controller.
- [low] lib/features/script_import/ocr_review_screen.dart:449-455 — `_removeAllNotScript` wraps `_markRemoved` per line in one `setState`; `_markRemoved` itself does not call `setState`, so this is correct; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:112-116 — `_markRemoved` mutates `_removedIds` without calling `setState` itself; all call sites wrap it (`194`, `229`, `452`) except `_removeAllNotScript`'s loop which is wrapped once — correct; no finding.
- [medium] lib/features/script_import/ocr_review_screen.dart:268,292,297 — `lastShown` is declared as nullable `ScriptLine?` then force-unwrapped (`lastShown!`) at 297 after the empty-queue branch may have returned `const SizedBox.shrink()` at 292 only when `lastShown == null`; when `pending.isEmpty` and `line` is non-null, `lastShown = line` keeps it non-null, so the `!` is safe — but if `pending.isEmpty` and `line.sourcePage == null` the early return at 261 already guarded that; no crash path found; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:264-265 — `idx` falls back to 0 when `indexWhere` returns -1, so Prev/Next indices can desync from the actual pending list after removals inside the sheet — consequence: Prev/Next land on the wrong flagged line — smallest safe fix: close the sheet when the walked line is removed instead of clamping.
- [low] lib/features/script_import/ocr_review_screen.dart:294-296 — `if (idx >= list.length) idx = list.length - 1` clamps instead of closing; same class as 264-265; one finding covers both.
- [low] lib/features/script_import/ocr_review_screen.dart:392-394,429-431 — Prev/Next buttons disable at bounds (`idx > 0`, `idx < list.length - 1`) but the Remove button (401-415) removes `current` without advancing `idx`, leaving `idx` pointing past a shrunk list on the next `setSheetState` — covered by the clamp finding at 264-265/294-296; no separate finding.
- [medium] lib/features/script_import/ocr_review_screen.dart:690-691 — in `_buildSourcePane`'s Remove handler, `next` is computed from `walk` before `_removeLine(selected)` mutates `_removedIds`; since `walk` is a snapshot from `_pendingWithPages()` taken at 644 during build, `next` is stale-but-intended (the next flagged line), which is the desired behavior — no defect; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:733 — `setState(() => _selectedLineId = next?.id ?? selected.id)` after `_saveEdit(selected)` keeps selection on the just-resolved line when `next` is null, so the pane keeps showing a line whose flag was cleared — cosmetic; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:140 — `_contextLinesFor` skips neighbours with `reviewStatus != ok`, so a neighbour currently flagged `review` is excluded from the context editor even though it is editable elsewhere — intended per comment; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:135 — `_orderedIndexById[flagged.id] ?? -1` returns an empty context list when the flagged line was removed mid-edit; the caller (`_buildContextEditor` 894) renders "No nearby lines to edit." — graceful; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:202-204 — `preview` truncates to 87 chars + ellipsis for the confirm dialog; no PII/log concern; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:466-467 — `_done()` pops with `OcrReviewResult(result)`; `Navigator.of(context).pop` on a route whose `PopScope.canPop` is false is reached only via explicit `_done` call sites (492, 496, 485) — consistent; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:50-54 — `_controllers` and `_contextControllers` are disposed in `dispose()` (91-97) but controllers created after dispose via `putIfAbsent` (147) would leak; unreachable in normal lifecycle; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:771 — `_controllers[line.id]!` force-unwrap in `_buildReviewCard`: `_controllers` is populated in initState for every `_reviewLines` line, and `_buildReviewCard` is only called for review lines (609), so the `!` is safe unless a review line was added after initState — impossible since `widget.lines` is fixed; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:943 — `_contextController(l)` uses `putIfAbsent` with a closure returning a new controller; if `_buildContextEditor` runs for a line whose controller was disposed in `dispose()` while the widget is still mounted (not possible), no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:1007-1019 — `remaining.map((line) => ListTile(...))` inside `ExpansionTile.children` builds all remaining rows eagerly; with hundreds of not-script lines this defeats laziness — consequence: jank on large scans — smallest safe fix: use a `ListView.builder` inside the tile children.
- [low] lib/features/script_import/ocr_review_screen.dart:923-969 — context editor builds all neighbour rows eagerly via `...ctx.map(...)`; bounded by span=3 so at most 6 rows; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:570-572 — `ListView.builder` with `itemCount` including header/spacer/section rows is correct; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:608-612 — `itemBuilder` indexes `reviewLines[cardIdx]` where `cardIdx < cardCount`; `reviewLines` here is the filtered (non-removed) list computed in `build` (473-474), consistent with `cardCount = reviewLines.length` (567) — consistent; no finding.
- [medium] lib/features/script_import/ocr_review_screen.dart:473-475 — `reviewLines` is filtered in `build` but `_notScriptLines` (169-171) is NOT filtered for removals before being passed to `_buildLazyList`/`_buildNotScriptSection`; `_buildNotScriptSection` re-filters via `remaining` (978-979) so display is correct, but `_removeAllNotScript` (449-455) iterates the unfiltered `_notScriptLines` and marks already-removed ids again — harmless (Set re-add); no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:569 — `itemCount = 1 + cardCount + (hasNotScript ? 2 : 0)` reserves rows for spacer + section even when `notScriptLines` is non-empty but `remaining` is empty after bulk removal; the section row then renders an empty-titled ExpansionTile — cosmetic; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:1027 — label `'Remove these (${remaining.length})'` shows 0 when all removed; cosmetic; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:580 — empty-state text 'No lines need review.' shown when `reviewLines.isEmpty` even if not-script lines remain — intended; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:72 — `_twoPaneBreakpoint = 720` hardcoded; responsive.dart context file was omitted so cross-checking against `lib/core/responsive.dart` is not possible — assumption noted, no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:472 — `MediaQuery.sizeOf(context).width` used for breakpoint instead of `responsive.dart` helper; consistent with in-file constant; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:303-306 — `AnimatedPadding` with `Duration(milliseconds: 150)` for keyboard insets; standard; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:374 — `ValueKey('sheet-${current.id}')` re-keys the PdfPageView per line so a step re-runs the locate — intended per comment; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:704 — `ValueKey('src-${selected.id}')` same pattern in the source pane; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:377-378 — `lineOnPage: current.sourceLineOnPage` may be null; PdfPageView is a context file (pdf_page_view.dart) not inlined, so nullability handling there is unverifiable — assumption noted, no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:707 — same nullability assumption for `selected.sourceLineOnPage`; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:378,708 — `highlightText` uses `_origById[...].text` (original OCR text) so the page highlight matches the OCR'd text, per comment 78-80 — intended; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:364-365 — undo restores `_origById[current.id]?.text` (via `?? current`), matching the comment; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:180 — `_controllers[line.id]?.text.trim() ?? line.text` — if the user cleared the field to whitespace, `text` becomes `''` and the line is saved with empty text rather than the original; consequence: an accidental all-whitespace edit wipes the line text — smallest safe fix: fall back to the previous text when trim is empty. (Severity low: user's own data, recoverable by undo.)
- [low] lib/features/script_import/ocr_review_screen.dart:201 — `_removeContextLine`'s `text` fallback chain ends at `l.text`; if the context controller exists but its text is whitespace-only, `preview` shows '(empty)' while the removal proceeds — cosmetic; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:204 — `text.length > 90 ? substring(0, 87)` assumes ASCII-safe slicing; Dart strings are UTF-16 so a surrogate pair at the boundary could split an emoji — cosmetic; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:211 — dialog content interpolates `l.character` and `preview` into user-facing text; no log/PII sink; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:190,413,695 — SnackBar content 'Line updated'/'Line removed' carries no line identity; with hundreds of lines the user cannot tell which line changed — cosmetic; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:188-190 — `ScaffoldMessenger.of(context)..hideCurrentSnackBar()..showAutoToast(...)` — `showAutoToast` is not a standard ScaffoldMessenger API; it presumably lives in `lib/core/toast.dart` (omitted context file), so this compiles only if that extension exists — assumption noted, no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:412-413,694-695 — same `showAutoToast` extension assumption; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:986 — `ExpansionTile.initiallyExpanded` is not a standard Flutter parameter (standard is `initiallyExpanded` in some forks / `initiallyExpanded` does not exist on material ExpansionTile, which uses `initiallyExpanded`? — actually material's ExpansionTile has no `initiallyExpanded`; it has `initiallyExpanded` only in newer versions via `ExpansionTile.initiallyExpanded`? The standard API is `initiallyExpanded` — Flutter's ExpansionTile does have `initiallyExpanded` since 3.x? It does not; it has `initiallyExpanded` in `expansion_tile` only as `initiallyExpanded`? Flutter's ExpansionTile constructor parameter is `initiallyExpanded` — yes, it exists as `initiallyExpanded`.) — verified standard API; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:988 — `leading: const Icon(Icons.gesture)` — decorative icon with no semantic label; accessibility nit only; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:1016-1017 — per-line delete IconButton inside ListTile trailing calls `_removeLine(line)` which wraps `_markRemoved` in `setState` — correct; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:1024-1028 — `OutlinedButton.icon` full-width bulk-remove button; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:855-880 — Wrap reflows the three action buttons; comment documents the overflow fix; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:863-865 — "View page" button gated on `!twoPane && pdfPath != null && sourcePage != null` — matches comment; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:785 — `onTap: canSelect ? () => _select(line) : null` — `_select` early-returns when `sourcePage == null` anyway; double guard; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:820 — TextField `onTap` also calls `_select(line)`; tapping the field to edit also pins the page — intended; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:773-774 — `isSelected`/`canSelect` both require `twoPane`; on phones no selection border — intended; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:811-813 — edited check icon shown when `_byId[line.id]?.reviewStatus == ok`; after `_saveEdit` the icon appears — intended; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:804 — `line.pageLineRef` getter from the model computes a fallback `p$page:$line` from orderIndex when source fields are null — context file verified the getter exists (script_models.dart:77-85) — consistent; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:247-248 — `_pendingWithPages` checks `(_byId[l.id]?.reviewStatus ?? l.reviewStatus) != ok` so a line resolved via `_saveEdit` drops out of the pending walk — intended; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:245-246 — pending filter requires `sourcePage != null`; lines without a page are excluded from Prev/Next but remain in `reviewLines` — intended; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:261 — `_viewSourcePage` early-returns when `pdfPath == null || sourcePage == null`; matches the "View page" button guard (863-865) — consistent; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:270-277 — `showModalBottomSheet(useSafeArea: true, enableDrag: false)` with comment explaining the InteractiveViewer conflict; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:289-291 — `addPostFrameCallback` pops the sheet after the queue empties; guarded by `sheetContext.mounted` — correct; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:292 — `return const SizedBox.shrink()` when `lastShown == null` keeps the sheet mounted for one frame before the post-frame pop — intended per comment; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:297 — `lastShown!` after the guards discussed; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:298 — `current.sourcePage!` — `current` came from `_pendingWithPages()` which requires `sourcePage != null` (246), so the `!` is safe; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:326 — `'${list.isEmpty ? idx + 1 : list.length}'` shows `idx + 1` when the list emptied, which can exceed the real count — cosmetic; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:321 — header interpolates `current.character` — user-facing text only; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:352 — sheet TextField uses `_controllers[current.id]` which may be null for a line that was never in `_reviewLines` (e.g. a context neighbour promoted by selection); `TextField(controller: null)` then creates an internal controller and typed text is lost on rebuild — consequence: edits typed into the sheet's OCR field for a non-review line are discarded on next sheet rebuild — smallest safe fix: ensure a controller exists (`_controllers.putIfAbsent`) before building the field. (Reachability: requires selecting a line whose id has no controller; `_selectedLineId` is only set from review-line cards or walk entries, both of which have controllers, so likely unreachable — kept low.)
- [low] lib/features/script_import/ocr_review_screen.dart:622 — `_byId[_selectedLineId]` may return null after a removal; `_buildSourcePane` handles null `selected` with the hint pane (625-637) — correct; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:644-645 — `walk`/`walkIdx` recomputed per build of `_buildSourcePane`; same repeated-allocation class as `_pendingWithPages` finding at 241-250 — covered there; no separate finding.
- [low] lib/features/script_import/ocr_review_screen.dart:687-689 — `next` computed from the build-time `walk` snapshot; after `_removeLine` the next build recomputes; consistent; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:717-720 — Previous button disabled at `walkIdx > 0` else null; correct bounds; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:738-741 — Next button disabled at `walkIdx < walk.length - 1`; correct bounds; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:729-731 — "Looks right" computes `next` from `walk[walkIdx + 1]` before `_saveEdit`; `_saveEdit` mutates `_byId` but not `_removedIds`, so `walk` membership is unchanged — consistent; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:732-733 — `_saveEdit(selected)` inside the pane's "Looks right" shows a toast via the screen `context` (188-190) — same captured-context pattern; covered by the 408-414 class note; no separate finding.
- [low] lib/features/script_import/ocr_review_screen.dart:753-767 — `_buildCountsBar` reads both pending counts per build; covered by the 173-177 finding; no separate finding.
- [low] lib/features/script_import/ocr_review_screen.dart:510-523 — single-pane body Column with counts bar + lazy list; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:528-552 — two-pane body Row with flex 5/4; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:542 — `const VerticalDivider(width: 1)`; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:565-569 — row-map indexing documented; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:575-586 — header empty-state Padding; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:588-605 — header Column with instructions; no finding.
- [low] lib/features/script_import/ocr_review_screen.dart:611 — `SizedBox(height: 24)` spacer
- [medium] lib/data/services/paddle_ocr_channel.dart:22,34,102,138 — shared static `progress` ValueNotifier mutated from `ocrPdf` while a concurrent `ocrPage`/`recognizeText` run can also fire `ocrProgress` events — two overlapping OCR runs overwrite each other's page/total and the `finally` at 138 clears the other run's in-flight progress — import screen shows wrong "page X of Y" or a null progress mid-run — make the notifier instance-scoped (or key per run) instead of static shared state.
- [medium] lib/data/services/paddle_ocr_channel.dart:102 — `progress.value` is force-set to `(page: 0, total: 0)` before the invoke, and any native `ocrProgress` event arriving before the first page result is overwritten by the finally-reset at 138 only after completion; but if `invokeMethod` throws a non-`MissingPluginException` (e.g. `PlatformException` from a bad path), the catch at 135 only handles MissingPlugin and the exception propagates — callers that treat `ocrPdf` as "null = fall back to ML Kit" will crash on a runtime native error instead of falling back — catch `PlatformException` (as `ocrPage` does at 90) and return null.
- [low] lib/data/services/paddle_ocr_channel.dart:110-111 — `pageCount`/`failedPages` are read from the result map with `as int? ?? 0` but the native side may return them as `num` (e.g. `double` from JSON decoding); a `double` value would throw a cast error inside the `.map` closure at 116/110 — use `as num?` then `.toInt()`/`.toDouble()` consistently, matching the defensive `num?` casts used for confidence/left/width at 59,82-85,122-124.
- [low] lib/data/services/paddle_ocr_channel.dart:124 — `width` defaults to `1.0` (full-width body line) when the native side omits it, per the doc at 147-149; but `left` defaults to `0.0` — a line with `left` supplied and `width` missing is treated as a full-width line starting at `left`, producing a wrong bounding box for margin-annotation filtering — default `width` to `1.0 - left` (or require both fields together) so the fallback rect stays consistent.
- [low] lib/data/services/pdf_text_channel.dart:21-24 — `extractText` rethrows `PlatformException` for any code other than `NO_TEXT` (e.g. `INVALID_PATH`, native crash codes), while the sibling `extractTextPerPage` at 42-47 swallows all `PlatformException`s via `MissingPluginException`-only handling plus rethrow — inconsistent failure direction: one method crashes the caller on a transient native error, the other silently returns null and the caller falls back to OCR — align both to the same policy (return null on any PlatformException, or rethrow in both).
- [info] lib/data/services/paddle_ocr_channel.dart:17, lib/data/services/pdf_text_channel.dart:9 — both channels use hardcoded `com.lineguide/...` MethodChannel names with no platform-side registration visible in this batch; until the native plugin is registered every call returns null via `MissingPluginException` (documented at paddle_ocr_channel.dart:13-15), which is safe-by-design — verify the native handlers exist before shipping; no code defect exhibited in the inlined Dart.

## Coverage
lib/data/services/paddle_ocr_channel.dart — findings: 5
lib/data/services/pdf_text_channel.dart — findings: 1
- [medium] lib/data/services/playback_session.dart:32-35 — catch swallows the failure to debug-level logging only; `ensurePlayback` returns success to callers on any configure/activate error, so a `.record` category left by STT silently persists and recordings play with no audible output (the exact failure the doc comment says this exists to prevent) — callers cannot distinguish "playback ready" from "category still stuck"; surface the failure (rethrow or return a bool/Future that completes with error) so play() callers can branch, keeping the DebugLogService record — reachability: any authenticated user rehearsing a recording on iOS/Android after an STT pass; same class as debug_log_service.dart:189-191/268-270 where append/flush failures vanish into debugPrint.
- [low] lib/data/services/playback_session.dart:33-34 — error logged without stack trace and with no release/version/environment tagging (DebugLogService has no Sentry/Crashlytics beforeSend or release stamping visible in context file) — a configure failure on a user device is unreproducible from logs alone; pass the caught `e`'s stack (rethrow inside try and log `StackTrace.current` in the handler) — reachability: same operator/user as above, diagnostic-only.
- [low] lib/data/services/debug_log_service.dart:151 — `DateTime.now()` timestamps written without an explicit zone; `toIso8601String` emits local-time offsets that shift across DST/timezone changes, and `fromLine` re-parses them, so log lines from a device that crossed a zone boundary sort/interleave incorrectly — stamp entries with `DateTime.now().toUtc()` (or fix the app zone at startup) — reachability: operator reading exported logs; diagnostic-only.
- [low] lib/data/services/debug_log_service.dart:187-188 — per-entry synchronous `writeAsStringSync` on the caller's (often UI) thread with no rotation: the file grows unbounded until the 200KB truncate in `_loadFromDisk` (debug_log_service.dart:296-300), which only runs at init — a long rehearsal session appends megabytes before any cap applies, and the truncate rewrites the whole file on the UI isolate — add a size check in `_appendSync` that rotates/truncates when the file exceeds a cap — reachability: any authenticated user in a long session; slow but recoverable.
- [info] lib/data/services/debug_log_service.dart:296-300 — verify the 200KB truncate path is not itself a data-loss bug: it rewrites the file with only the in-memory `_entries` (capped at 500 lines) plus a trailing newline, silently dropping any older lines not loaded into memory (e.g. entries logged before a previous init's 500-line cap) — confirm intended retention before treating as safe.

## Coverage
lib/data/services/perf_service.dart — clean
lib/data/services/playback_session.dart — findings: 2
- [medium] lib/data/services/recording_sync_service.dart:187-201 — `_saveManifest` chains writes but the snapshot is taken inside the chained future while `_cache` can be concurrently mutated by pooled downloads/realtime handlers; a mutation interleaved between `jsonEncode` scheduling and execution can throw (concurrent modification) and the whole save silently fails to the error log — consequence: manifest index lost for that sync, stale entries re-downloaded next launch — fix: snapshot `_cache.values` into a local list *before* entering the chained `.then`, then encode that list.
- [medium] lib/data/services/recording_sync_service.dart:148-173 — `_doHydrate` sets `_hydrated = true` before the restore completes and returns early on the next call even if the first hydration is still awaiting disk I/O; a second `hydrateCache()` (e.g. `syncForProduction` racing a realtime arrival) can proceed with an empty `_cache` — consequence: cached recordings re-downloaded or, worse, `getCachedPath` returns null and a fresh download overwrites a valid cached file — fix: gate on the in-flight future (`_hydration ??= _doHydrate()` already dedupes; drop the `_hydrated` early-return or set it only after the try block).
- [medium] lib/data/services/recording_sync_service.dart:183-186 — `_saveManifestDebounced` fires `_saveManifest` via a bare `Timer` with no zone/error guard; if `_saveManifest`'s future rejects (it catches internally, but the chain assignment itself can throw on a closed file), the unhandled async failure is never captured — consequence: silent index loss with no trace beyond the debug print — fix: wrap the timer callback in `runZonedGuarded` or `unawaited` with a `.catchError`.
- [low] lib/data/services/recording_sync_service.dart:676-684 — `clearCache` deletes the production subdirectory but leaves any manifest entries whose `localPath` pointed inside it only if `_cache.removeWhere` matched by `productionId`; entries restored from an old manifest whose `productionId` was recovered from the path (line 772-776) may mismatch the passed `productionId` casing/format — consequence: stale cache entries survive a clear and `getCachedRecordings` serves deleted files — fix: also drop entries whose `localPath` starts with the deleted directory.
- [low] lib/data/services/recording_sync_service.dart:766-786 — `_CachedRecording.fromJson` recovers `productionId` from `p.dirname(localPath)` when absent, but `p.split` on a manifest written by an older layout that stored absolute paths outside `recording_cache` yields the wrong segment (e.g. the Documents dir name) — consequence: restored entries land under the wrong production and are silently filtered out of every production-scoped query — fix: validate the recovered segment against the cache dir prefix before accepting it.
- [low] lib/data/services/recording_sync_service.dart:717-729 — `_extractCharacterFromUrl` scans `pathSegments` for the productionId and returns the next segment; a URL whose path contains the productionId twice (or a character name equal to the productionId) returns the wrong character — consequence: display-only mislabel in logs/toasts, no data impact — fix: match the segment *after* the first occurrence only, or anchor on the `recordings/` prefix segment.
- [low] lib/data/services/recording_sync_service.dart:280-287 — fetch failure path returns 0 and tells the user rehearsal will use TTS, but `cloudByLine`/`myCloudByLine` are never populated so the upload phase below still runs with an empty `myCloudByLine` — wait, no: the early `return 0` at 286 exits before uploads, so this is fine; no finding.
- [medium] lib/data/services/recording_sync_service.dart:317-324 — `myCloudByLine` is keyed by lineId but the "newest per line from ME" comparison uses `row['recorded_at']` timestamps only; two of MY rows for the same line with equal timestamps keep the first encountered arbitrarily — consequence: upload-skip decision can compare against a stale-but-equal row, harmless in practice but nondeterministic — fix: tie-break on a stable field or accept; low impact.
- [low] lib/data/services/recording_sync_service.dart:506-521 — `_runPooled` bumps `next` with `next++` inside an async closure; the comment claims safety because there is no await between read and bump, and that holds, but `concurrency.clamp(1, items.length)` with `items.length` computed once is fine — no finding.
- [medium] lib/data/services/recording_sync_service.dart:436-447 — download path writes bytes to `cachePath(productionId, lineId)` then constructs `_CachedRecording` with `recordedAt: cloudTimestamp` where `cloudTimestamp` came from `_parseTimestamp(cloud['recorded_at'])` which returns 0 for a missing/malformed timestamp — consequence: a cached entry with `recordedAt == 0` will always lose the `cached.recordedAt >= cloudTimestamp` freshness check at 411-415 only if the cloud row also has 0; if the cloud row later gains a real timestamp the cache is correctly invalidated, so impact is limited to one extra download — fix: skip caching when `cloudTimestamp == 0` and the row claimed a timestamp, or log it.
- [low] lib/data/services/recording_sync_service.dart:641-650 — realtime handler caches with `recordedAt: _parseTimestamp(payload['recorded_at'])` (0 on absence) and then `_saveManifestDebounced`; a burst of arrivals each overwrites the debounce timer so the manifest write is deferred 2s after the *last* arrival — intended per the doc comment; no finding.
- [medium] lib/data/services/recording_sync_service.dart:626 — realtime handler skips payloads where `recordUserId == (myUserId ?? _cloud.currentUserId)`; if `myUserId` is null and the user is signed out mid-session, `_cloud.currentUserId` is null and the guard becomes `recordUserId == null` which never matches a String payload — consequence: own recordings arriving over realtime after a sign-out race get downloaded and cached as if from another castmate, duplicating storage — fix: compare against `myUserId ?? _cloud.currentUserId` only when non-null, else skip on `recordUserId == myUserId` semantics explicitly (or early-return when not signed in).
- [low] lib/data/services/recording_sync_service.dart:585-606 — `subscribe` calls `unsubscribe()` first, which nulls `_realtimeChannel` before re-subscribing; if `subscribeToRecordings` throws, the old channel was already torn down and the new one never assigned — consequence: silent loss of realtime updates with only a log — fix: assign the new channel to a local, tear down the old only after successful creation.
- [low] lib/data/services/recording_sync_service.dart:659-671 — `unsubscribe` catches and logs but leaves `_realtimeChannel` already nulled, so a subsequent `subscribe` cannot retry teardown of a half-dead channel — acceptable; no finding.
- [medium] lib/data/services/recording_sync_service.dart:214-220 — `cacheDir` getter caches `_cacheDir` after creating the directory, but `clearAllCaches` (687-696) deletes and recreates the *root* dir while `_cacheDir` still points at the same path — fine; however `clearCache`/`clearAllCaches` call `await cacheDir` which re-creates the directory if it was deleted externally, then `prodDir.exists()` is checked — no finding.
- [low] lib/data/services/recording_sync_service.dart:227-230 — `isSafePathId` rejects `..` by substring check but the regex already excludes `/` and the check is redundant for `..` since `.` is allowed — a value like `a..b` passes the regex but is rejected by the substring check, correct; no finding.
- [low] lib/data/services/recording_sync_service.dart:244-246 — the `p.isWithin(dir, path)` guard is dead code given the regex+`..` checks upstream, but it throws *after* `Directory(prodDir).create(recursive: true)` at 241 has already created a directory outside the cache if `lineId` were an absolute path — wait: `p.join` with an absolute `lineId` adopts it, so `prodDir` is still inside `dir` (join of dir+prodId), but `path` could escape; the directory creation at 241 happens before the guard, so a hostile absolute `lineId` creates `prodDir` (safe) but the guard fires before any file write — the guard is effective for the file write; no finding beyond noting the create-before-check ordering is safe here because `prodDir` itself cannot escape (productionId is regex-validated).
- [info] lib/data/services/recording_sync_service.dart:493-499 — `_tellUser` catches all exceptions and only `debugPrint`s; on a headless/test context the sync failure message is silently dropped — consequence: operator gets no surface feedback in tests — fix: none needed beyond awareness; low.
- [low] lib/data/services/recording_sync_service.dart:542-543 — `getCachedRecording` clamps `cached.recordedAt` to `1 << 52` before `DateTime.fromMillisecondsSinceEpoch`; a manifest-corrupted huge value silently becomes year 2107+ instead of being dropped — consequence: a bogus cached entry is served as a valid Recording — fix: drop entries whose recordedAt is outside a sane range during hydration instead of clamping at read time.
- [low] lib/data/services/script_export.dart:275-278 — `_truncate` with `maxLen < 4` produces `substring(0, negative)` → RangeError; only called with 80 internally — no reachable defect; no finding.
- [low] lib/data/services/script_export.dart:280-290 — `_lastWords` scans `for (var i = text.length - 1; i > 0; i--)` skipping index 0; a single-word text returns the whole text (intended), and a text whose only space is at index 0 is missed — cosmetic; no finding.
- [medium] lib/data/services/script_export.dart:186-231 — `toCharacterLines` writes `Total lines: $charLineCount` but never emits the act-scene headers for lines *before* the first header line when `currentAct` starts empty and the first line is a header with `act == ''` — the header is still written (205-209) so no data loss; no finding.
- [low] lib/data/services/script_export.dart:247-263 — `toCueScript` shows the cue line only when `i > 0`, but for `i == 0` the "YOU" line prints with no cue context — intended minimal output; no finding.
- [medium] lib/data/services/script_export.dart:64-88 — `toPlainText` switch on `line.lineType` has no default; if `LineType` gains a new enum value (e.g. from `fromJson` `byName` on an unknown string — which throws), the exporter silently omits the line — consequence: exported text silently drops lines on future enum extension — fix: add a default case that writes the raw text. (Same shape at 115-132, 156-179.)
- [low] lib/data/services/script_export.dart:43-46 — scene index `take(4)` + `+N more` is display-only; no finding.
- [low] lib/data/services/script_export.dart:268-273 — `_stripParens` only strips one leading/trailing paren pair; nested `((x))` leaves inner parens — cosmetic; no finding.

## Coverage
lib/data/services/recording_sync_service.dart — findings: 12
lib/data/services/script_export.dart — findings: 1
- [medium] lib/data/services/script_import_service.dart:119,120,148,164,430,464,479,508,513 — per-page/per-block debugPrint calls inside the OCR hot loop (one per page, one per failed page, one per block) — on a 300+ page scanned play this emits hundreds of console lines during import, and debugPrint is throttled/dropped so the operator cannot tell which page failed from the log — batch the per-page diagnostics into one summary line (already present at 404-410) and keep only the failure line.
- [medium] lib/data/services/script_import_service.dart:171-173 — PDFKit extraction failure is caught and reduced to a debugPrint; the exception object is interpolated into the message but no stack trace is captured, and the failure is not surfaced to the caller or the failed-pages counter — a silently degraded import (native path skipped, OCR path used) is indistinguishable from a normal OCR import in any non-debug build — log via DebugLogService.instance.log with the stack trace (or at least `e.toString()` + StackTrace.current) at error category, and count the page(s) as failed so `lastImportFailedPages` reflects the degraded path.
- [medium] lib/data/services/script_import_service.dart:357-366 — PaddleOCR unavailability is logged to DebugLogService but the exception is swallowed into a string with no stack trace and the failure is not counted in `failedPages` — the import proceeds on the fallback engine and the user-facing `lastImportFailedPages` stays 0 even though the primary engine never ran — include the stack trace in the logError call and increment failedPages (or expose an engine-fallback flag) so the degraded-import state is observable.
- [medium] lib/data/services/script_import_service.dart:512-515 — per-page ML Kit/render failures are logged with debugPrint only; the exception is swallowed without a stack trace and the page is merely counted — in release builds debugPrint output is dropped, so a scan with 5 failed pages shows no diagnostic anywhere except the single summary at 531-534, which names no page — route the per-page failure through DebugLogService.instance.logError (which persists to disk) with the page number and exception, or include page indices in the summary line.
- [low] lib/data/services/script_import_service.dart:521-524 — temp-file deletion failure is caught with an empty catch block and no logging — a leaked `ocr_page.png` (potentially megabytes per import) accumulates silently in the temp directory with no trace — log the deletion failure at debug level (debugPrint or DebugLogService) so the leak is observable.
- [low] lib/data/services/script_import_service.dart:220 — `scorer.dispose()` in the finally block frees the shared dictionary after every import, but `ensureVocabLoaded` at 190 may have reloaded it on the next import; if a concurrent import runs while disposal is in flight the scorer is disposed mid-use with no log or guard — add a re-entrancy guard or log the dispose/reload cycle so the state transition is observable (assumption: OcrConfidenceService is a singleton shared across imports, as `instance` suggests).
- [info] lib/data/services/script_import_service.dart:194-198 — `Isolate.run` closure captures `OcrConfidenceService.instance` and re-initializes it inside the worker isolate; if vocab loading touches rootBundle/platform channels inside the isolate it will throw PlatformUnsupportedError, which is not caught here and would abort scoring with no log — verify the isolate-scoped scorer never touches platform channels, or wrap the Isolate.run in a try/catch that logs the scoring failure (assumption: the in-isolate `setTheatricalVocab` path is platform-channel-free, which the comment at 185-189 asserts but the code cannot confirm).
- [info] lib/data/services/script_import_service.dart:529-534 — failed-pages summary is logged via DebugLogService.logError only when `failedPages > 0`; there is no log line at all for a fully-successful OCR import, so an operator reviewing the persisted debug log cannot distinguish "import succeeded" from "import never ran" — emit a single success summary line (page count, engine used) through DebugLogService so the log has a positive record.

## Coverage
lib/data/services/script_import_service.dart — findings: 8
- [medium] lib/data/services/script_parser.dart:117-195 — `parse()` mutates shared instance state (`knownCharacters`, `characterAliases`, `multiCharacterMap`, `_titleHeaders`, `_headerScrubPatterns`, `_format`) without clearing it at entry — a second `parse()` call on the same `ScriptParser` instance accumulates stale characters/aliases/header patterns from the previous script, mis-attributing dialogue and inflating charCounts — smallest safe fix: reset `knownCharacters`, `characterAliases`, `multiCharacterMap`, `_titleHeaders`, `_headerScrubPatterns` at the top of `parse()` (or document/require a fresh parser instance per script).
- [low] lib/data/services/script_parser.dart:1196-1218 — `_cleanLine`'s `_trailingBracketNoiseRe` (`\s*\[[A-Z0-9][^\]]*$`) matches unclosed bracket fragments anywhere in a line, not only at line end after OCR glue — consequence: legitimate bracketed spoken content mid-line can be silently dropped from parsed dialogue; smallest safe fix: anchor the bracket-noise pattern to line start (`^\s*\[`) or require the fragment to contain no letters after the bracket.
- [low] lib/data/services/script_parser.dart:698-713 — `_stripGutenbergWrapper` cuts text at `*** START OF`/`*** END OF` markers without verifying a Gutenberg boilerplate context — consequence: a play whose dialogue legitimately contains the literal string `*** START OF` (or a scanned OCR artifact producing it) loses all text before/after that point; smallest safe fix: require the marker to be followed by a known Gutenberg project title pattern before stripping.
- [low] lib/data/services/script_parser.dart:799-806 — `_dehyphenate` joins any `letter-hyphen-newline-letter` pair, including intentional hyphenated line-end compounds in verse ("true-\nblue" style splits in poetry are also joined) — consequence: minor text normalization drift, generally benign; smallest safe fix: none needed unless verse fidelity matters — no action.
- [info] lib/data/services/script_parser.dart:1802 — file ends mid-class at line 1802 with no closing brace visible in the inlined listing — consequence: cannot confirm the class closes correctly; the shown portion was reviewable so this is likely just truncation of the listing, not a real defect; smallest safe fix: verify the file compiles (assume it does since it's committed).

## Coverage
lib/data/services/script_parser.dart — findings: 5
- [medium] lib/data/services/stt_adaptation_service.dart:159-161 — `_loadProduction` reads `profiles.json` with `existsSync`/`readAsString` but never awaits `ensureLoaded` before `addSample` mutates state — `addSample` (line 304) fires `ensureLoaded` via `unawaited`, so a caller that immediately calls `addSample` can have its sample merged into a profile that is then overwritten by the async disk load's `merge` (line 209) — actually the merge unions by audioPath so the live sample survives, but the *status* is recomputed from disk+live union while `requestActorTraining` (line 389) may have already flipped status to `training`/`readyToTrain` on the pre-hydration profile; the disk merge then resets it via `_computeStatus` (line 200-202), silently discarding an in-flight training state marker — smallest safe fix: in `merge`, preserve `live.status` when it is `training`/`failed` (non-derivable states) instead of always recomputing from `_computeStatus`.
- [medium] lib/data/services/stt_adaptation_service.dart:224-230 — `_schedulePersist` debounce timer fires and iterates `_dirtyProductions` while `_persistProduction` runs async; a second `addSample` for the same production during that async persist re-adds the productionId to `_dirtyProductions` but no new debounce timer is scheduled (the timer already fired), so the second write is never persisted until some later event happens to trigger `_schedulePersist` again — data loss on app kill between the two samples — smallest safe fix: after draining `_dirtyProductions` in the timer callback, re-arm the timer if `_dirtyProductions` became non-empty again, or clear-and-recheck inside `_persistProduction` completion.
- [low] lib/data/services/stt_adaptation_service.dart:253-255 — `tmp.rename(path)` on some platforms fails if `path` exists (Windows `dart:io` rename semantics); persist then throws and is swallowed by the catch at 256, leaving the stale profiles.json — smallest safe fix: delete existing target before rename or use `rename` with replace semantics / write-then-atomic-move helper.
- [low] lib/data/services/stt_adaptation_service.dart:166-171 — `decode` filters samples by `File(x.audioPath).existsSync()` but `audioPath` comes from untrusted JSON on disk; a crafted/edited profiles.json with an absolute path outside the app dir (or a path traversal string) causes `existsSync` on arbitrary paths and, worse, `addSample`-persisted entries could later be trained against files the user never recorded — smallest safe fix: validate `audioPath` is inside the production's adapter/documents directory before trusting it.
- [low] lib/data/services/stt_adaptation_service.dart:320 — `actorSeconds` is computed as `actorProfile.totalAudioSeconds + durationMs/1000` but `totalAudioSeconds` already sums `durationMs` of existing samples; the new sample is then appended via copyWith so the value is correct, yet the same expression at 335-337 recomputes from `prodProfile.totalAudioSeconds + durationMs/1000` while passing `prodProfile.samples.length + 1` — consistent, but if `durationMs` is 0 or negative (no validation) readiness/`hasEnoughData` can be reached with garbage audio — smallest safe fix: clamp `durationMs > 0` in `addSample` before use.
- [info] lib/data/services/stt_adaptation_service.dart:480-488 — `_adapterDir` uses `getApplicationDocumentsDirectory()` and creates `stt_adapters/<productionId>` with `createSync(recursive: true)`; `productionId` is interpolated into a filesystem path unescaped — a productionId containing `../` or a separator escapes the `stt_adapters` root (path traversal on write/delete: `clearProduction` at 502-507 then `delete(recursive: true)` could remove directories outside the intended root if a malicious or malformed productionId reaches here) — verify callers constrain productionId to safe characters; smallest safe fix: sanitize/reject productionId containing path separators or `..` before joining.
- [medium] lib/data/services/stt_channel.dart:128-164 — `_handleCallback` casts `call.arguments as Map` (line 131) and `as String?` (144) without null/type guards; a native side sending a wrong-typed argument throws inside the method-call handler, which the framework swallows into a channel error — the STT result callback chain (`_onResult`) is silently dropped and `_listening` is never reset, leaving the UI in a stuck "listening" state — smallest safe fix: use `Map<String, dynamic>.from(call.arguments as Map? ?? {})`-style defensive casts and reset `_listening` in a catch-all.
- [medium] lib/data/services/stt_channel.dart:135-137 — on `onResult` with `isFinal == true`, `_listening` is set false but `_onResult`/`_onDone` are not cleared and no `stop()` is issued; if native keeps streaming partial results the handler keeps firing callbacks after the session logically ended — smallest safe fix: after final result, invoke `stop()` (or null the callbacks) so state and native session agree.
- [low] lib/data/services/stt_channel.dart:50-78 — `initialize` returns `false` on permission denial but callers get no distinction between "denied" and "platform missing"; combined with `listen` (86-113) returning false the same way, a user-facing feature can silently never start with only a debug log — smallest safe fix: surface a typed failure (enum/exception) or document+assert the caller handles both.
- [low] lib/data/services/stt_channel.dart:201-206 — `dispose` calls `stop()` then nulls callbacks but does not clear `onPcm`, `onAudioInterruption`, `onAudioRouteLost`, nor reset `_initialized`; a re-`initialize` after dispose keeps stale native handlers registered via `setMethodCallHandler` (line 52) pointing at this same instance — mostly harmless but leaks late callbacks after dispose — smallest safe fix: null all callback fields and guard `_handleCallback` with an `_initialized`/disposed flag.
- [low] lib/data/services/stt_channel.dart:96-100 — `invokeMethod('listen', {...})` builds the args map with a conditional element; when `contextualStrings` is null/empty the map still includes `onDevice` — fine — but the map literal with an `if` inside a `Map` literal passed to `invokeMethod` requires the elements to be `MapEntry`; this compiles only because Dart allows `if` in map literals producing entries — no defect, but note `onDevice` default here is `false` (line 90) while the doc comment at 85 says "default true" — doc/behavior mismatch that callers relying on the comment will get device-off recognition — smallest safe fix: align the default with the documented behavior or fix the comment.

## Coverage
lib/data/services/stt_adaptation_service.dart — findings: 6
lib/data/services/stt_channel.dart — findings: 6
- [medium] lib/data/services/stt_service.dart:41-48 — `init()` wraps the SharedPreferences screenshot-mode check in `catch (_) {}` with no logging, so any transient platform/SharedPreferences error is indistinguishable from "screenshot mode active" and init returns `false` (STT treated as unavailable) — a transient failure silently disables speech recognition for the whole session — narrow the catch to the specific expected exception (or log it via DebugLogService) and only return false when `screenshot_mode` is actually true.
- [medium] lib/data/services/stt_service.dart:161-166 — `listen()` auto-init failure path calls `onDone` and returns while `_onResult`/`_onDone`/`_vocabHints`/`_carriedTranscript`/`_lastPartial` still hold values from a prior session and `_sttChannel.onLevel` may still point at `_handleLevel` — stale channel-level handlers from the prior session can keep firing into a dead session and a later `stop()`-driven path can invoke a stale `_onDone` — reset the session callbacks and channel `onLevel` (or clear channel state) on the failure path before returning.
- [medium] lib/data/services/stt_service.dart:188-218 — `onResult` callback writes `_lastPartial` and invokes `_onResult` without any session-generation guard (the restart path at 206-210 guards on `gen == _sessionGen`, but the result path does not), so a result arriving after a *new* `listen()` overwrites the new session's `_lastPartial` with the old session's trailing text and merges it into the new session's carried transcript — add the captured `gen` check before touching `_lastPartial`/`_onResult`.
- [low] lib/data/services/stt_service.dart:232-246 — `stop()` clears `onSilence`/`onLevel` and local fields but never clears `_sttChannel.onLevel` (only `startLineCapture`'s failure path at 405 does), so after `stop()` the channel may still deliver level events into `_handleLevel`, which early-returns on `!_isListening` but still mutates `_smoothedLevel`/`_silenceStart` state that a subsequent capture expects zeroed — set `_sttChannel.onLevel = null` in `stop()` alongside the local fields.
- [low] lib/data/services/stt_service.dart:395-409 — `startLineCapture` sets `_isListening = true` before knowing whether recording starts; on failure it resets `_isListening` and `_sttChannel.onLevel` but leaves `_continuous`/`_onResult`/`_onDone` from any prior session intact, so a later `stop()`-driven `onDone` path could invoke a stale `_onDone` — reset the session callbacks on the failure branch, not just `_isListening`.
- [medium] lib/data/services/stt_vocabulary_service.dart:113-124 — `getScriptHints` returns `hints.take(100)` from an unordered `Set` literal; the cap silently drops arbitrary members (Set iteration order after `addAll` of two sets interleaves unpredictably), so high-value character names can be evicted by low-value words — sort or prioritize hints (e.g. character names first) before applying the 100 cap.
- [medium] lib/data/services/stt_vocabulary_service.dart:127-147 — `clearProduction` removes per-actor correction maps for the production but only evicts `_correctionPatterns` entries when `droppedWords.isNotEmpty`; a production whose actors had no corrections leaves stale patterns for words that were only used by that production's vocabulary, and the eviction predicate `!stillUsed.contains(w)` can also evict a pattern still referenced by a *different* production's correction map that shares the same wrong-word key, silently disabling a learned correction for the surviving production — key `_correctionPatterns` by `productionId:word` (or scope eviction to the removed production's keys) instead of a global word-keyed map.
- [medium] lib/data/services/stt_vocabulary_service.dart:230-242 — `learnFromAttempt` caps `_actorCorrections[key]` at 500 entries but evicts `map.keys.first` (insertion-order first = oldest learned), which can evict a frequently-recurring misrecognition in favor of a one-off; combined with the unbounded `_correctionPatterns` growth (patterns are only removed in `clearProduction`), a long multi-production session grows `_correctionPatterns` without bound — evict by frequency/recency or add a global cap on `_correctionPatterns`.
- [low] lib/data/services/stt_vocabulary_service.dart:299 — `resolvedWords?.contains(lower) ?? false` — when `expectedText` is null the whole skip-set is disabled and every word goes through the vocabulary scan; that is the intended fallback, but when `expectedText` is present and `_expectedWordSet` returns an empty set (e.g. all-token-stripped line), `contains` is still false for every word and the scan runs anyway — acceptable, but note the cache at 279-282 keys only on the last line, so alternating lines re-tokenize every partial (perf, not correctness).
- [low] lib/data/services/stt_vocabulary_service.dart:308-317 — `vocab.correctionCache[lower] = bestMatch` stores `null` for "leave it alone" but the map type is `Map<String, String?>`, so a subsequent `containsKey` correctly distinguishes null; however the cache-clear at 313-315 wipes *all* memoized results once the 4000 cap is hit, discarding still-valid entries and re-running the O(vocabulary) scan for every word — evict oldest instead of clearing wholesale.
- [low] lib/data/services/stt_vocabulary_service.dart:331-345 — `_bestVocabularyMatch` bails when `bestDistance == 1` but the initial `bestDistance = 3` means a distance-0 (exact) match is never returned early; the loop continues scanning and can replace an exact match with... nothing better (dist must be `< bestDistance`), so an exact hit found *early* followed by a dist-1 candidate replaces it only if `dist < bestDistance` fails — actually safe; no finding beyond noting the early-return at 343 makes the `dist == 0` case unreachable-by-design (identical words are filtered upstream at 299), so the `bestDistance == 1` early return can fire for a *fuzzy* dist-1 match before a later exact match is scanned — reorder so exact (dist 0) matches terminate the scan too.
- [low] lib/data/services/stt_vocabulary_service.dart:391-397 — `_correctAgainstExpected`'s DP uses `_editDistanceAtMost(..., 2) <= 2` for the match predicate but the backtrack at 407 re-runs the same capped call; consistent, yet the `up > left` tie-break at 396/414 prefers `up` (skip expected word) over `left` (skip recognized word), which can drop a recognized word that had a valid alignment — standard LCS tie-breaks are safe for score but here the *replacement* step mutates text, so a different tie-break yields different corrected output for equal scores; deterministic but possibly suboptimal corrections — no hard defect; noting only if callers depend on maximal alignment.
- [low] lib/data/services/stt_vocabulary_service.dart:434-473 — `_editDistanceAtMost` shares static `_dpPrev`/`_dpCurr` buffers across calls; `_ensureDpCapacity` grows them but the row-swap at 466-468 leaves `prev`/`curr` pointing at swapped buffers whose contents are stale from the prior call — the initialization loop at 447-449 re-seeds `prev` so this is safe for the *first* row, but if two calls interleave (impossible here — synchronous, non-reentrant per the comment at 475-477) it would corrupt; safe as written, no finding.
- [low] lib/data/services/stt_vocabulary_service.dart:510 — `_correctionCacheLimit = 4000` is declared *after* use at 313 (top-level const hoisting is fine in Dart), but the cap comment says "per production" while the check at 313 is per-vocabulary-object — consistent; no finding.
- [low] lib/data/services/stt_vocabulary_service.dart:530-533 — `scanWords`/`nameParts` use `??=` on instance fields inside a getter; if `buildFromScript` replaces the vocabulary object (per the comment at 526-528) the memoized lists are rebuilt on the new object, but the *old* object's lists persist holding stale words — since `_vocabularies[productionId] = vocab` replaces the whole object and callers fetch the current one, stale lists are unreachable; no finding.
- [info] lib/data/services/stt_service.dart:44-48 — empty `catch (_) {}` around SharedPreferences read also hides `MissingPluginException`-class platform errors during screenshot-mode detection; verify the surrounding init path logs this via DebugLogService elsewhere (not visible in this file).
- [info] lib/data/services/debug_log_service.dart:187-191 — synchronous `writeAsStringSync` per log entry on the caller's (often UI) thread is acknowledged in the comment as a deliberate tradeoff; with `maxEntries = 500` and per-entry appends during rehearsal this can add milliseconds per log call on the main isolate — flagged only as context-file observation, not a finding against the reviewed files.

## Coverage
lib/data/services/stt_service.dart — findings: 6
lib/data/services/stt_vocabulary_service.dart — findings: 7
- [medium] lib/data/services/sync_queue.dart:195 — persist failures are swallowed to error-level debug log only, with no metric/counter or surfaced signal — a persist failure silently leaves the on-disk queue stale while in-memory state advances, and the next launch restores the stale file (jobs lost) with nothing observable beyond a debug log — surface the failure (e.g. increment a counter / expose lastPersistError for diagnostics, or rethrow into a monitored channel) — reachability: operator/dev observing logs; silent data loss on crash makes this medium.
- [medium] lib/data/services/sync_queue.dart:221-222 — corrupt-queue-file handling logs to debug log only and swallows the rename failure with an empty catch (`catch (_) {}` at 225) — if the `.corrupt` rename fails (e.g. file locked on Windows), the corrupt file stays in place and the next persist from an empty queue overwrites it, destroying the queued jobs with no trace — log the rename failure at error level or fall back to copying content aside before returning — reachability: crash-recovery path only; silent unrecoverable loss of queued uploads.
- [medium] lib/data/services/sync_queue.dart:259 — restore failures are logged to debug log only (`_dlog.logError(... 'restore failed', e)`) and the queue proceeds with an empty in-memory state — a transient IO error during restore means previously persisted jobs are invisible until the next persist overwrites them — at minimum record the failure state so callers/diagnostics can see restore did not complete — reachability: startup path; silent data loss.
- [low] lib/data/services/sync_queue.dart:470 — backoff delay computed as `2 << nextRetry.retryCount.clamp(0, 4)` gives 2,4,8,16,32 s — the comment says exponential backoff but the retry budget check at 421 caps attempts at 5 while retryCount can exceed 4 only via restored jobs whose retryCount was reset to 0 at 73; verify the clamp intent — if retryCount can reach 5 the shift yields 64 s silently; harmless today but the clamp hides drift — assert/derive delay from a named constant instead of a magic shift.
- [low] lib/data/services/sync_queue.dart:114 — `supa.currentUser!.id` uses a non-null assertion on a nullable user — if the uploader fires while signed out (isReady was checked at 354 but the user can sign out mid-upload), this throws and the job is marked failed rather than retried cleanly — guard with a null check and fail the job explicitly — reachability: authenticated user signing out mid-upload; crash-free but misclassified failure.
- [medium] lib/data/services/tts_service.dart:115-116, 119-120 — per-voice diagnostic logging loops over matching voices and logs full voice metadata (`Voice[$i] full data: ${matching[i]}`) on every setLocale call — noisy per-item logging in a hot path (setLocale runs per locale change/character assignment), and voice metadata can include device-identifying voice names — cap the log to a summary count and drop the per-item dump, or gate behind a debug flag — reachability: any user; log noise + potential PII-ish device detail.
- [medium] lib/data/services/tts_service.dart:784-787 — cancellation PlatformException is swallowed and treated as success (`return true`) with only a debug log — a cancelled synthesis is reported to the caller as a successful speak, so the rehearsal advance path proceeds without audio and without a completion from the real path — distinguish cancellation from success (return a tri-state or fire the completion handler explicitly) — reachability: any user triggering rapid speak/stop; silent wrong-state advance.
- [medium] lib/data/services/tts_service.dart:789-795 — 'backgrounded' PlatformException also returns `false` (fall back to system TTS) but the caller at 444-447 then falls through to system TTS only if `spoke` is false AND gen is current; if the app is backgrounded the system TTS speak at 471 will also fail silently, and the completion handler is never fired — the line stalls with no observable signal beyond a debug log — fire `_fireCompletion` or surface the failure to the caller — reachability: any user backgrounding the app mid-speak.
- [low] lib/data/services/tts_service.dart:906, 920 — unload/delete failures are logged via `debugPrint` only (not the structured DebugLogService), so they never reach the persisted log file or diagnostics screen — route through DebugLogService.logError for consistency — reachability: dev/operator only.
- [low] lib/data/services/tts_service.dart:846 — `_currentSpeed` is declared as a mutable instance field after ~800 lines of methods with no doc; setRate at 886 clamps to (0.5, 2.0) but assignVoice/speak read it without re-checking — not a defect per se, but the clamp at 886 silently narrows out-of-range caller rates without logging — log or document the clamp — reachability: dev only.
- [info] lib/data/services/sync_queue.dart:159-163 — `_persistPath` uses `getApplicationSupportDirectory()` while the debug log service (context file debug_log_service.dart:108-109) uses `getApplicationDocumentsDirectory()` — two different on-disk locations for related persisted state; verify this is intentional (support dir is not user-visible/iCloud-backed, which is likely correct) — no action needed if intentional.

## Coverage
lib/data/services/sync_queue.dart — findings: 6
lib/data/services/tts_service.dart — findings: 6
- [medium] lib/data/services/voice_config_service.dart:79-82 — getOverrides swallows JSON parse errors to debugPrint and returns {} — a corrupt/legacy overrides blob silently discards every per-character voice override (and setOverride then rewrites the blob, making the loss permanent) with no error surfaced to the caller or crash reporting — throw/return a typed failure or log via DebugLogService.logError with the exception and key so the corruption is observable.
- [medium] lib/data/services/voice_config_service.dart:242-245 — getGenders swallows JSON parse errors to debugPrint and returns {} — corrupt gender blob silently resets every character to the default gender, changing voice-pool selection with no signal — same fix: surface the parse failure instead of the permissive empty-map fallback.
- [medium] lib/data/services/voice_config_service.dart:277-280 — getLocales swallows JSON parse errors to debugPrint and returns {} — corrupt locale blob silently drops all per-character locale overrides, so resolveVoice falls back to the production default locale with no observability — same fix.
- [low] lib/data/services/voice_config_service.dart:61,100,348 — per-mutation debugPrint of productionId/characterName/voiceId in every set/remove/rename call — noisy per-item logging in mutation paths that will spam console in bulk rename/merge operations and carries user-content (character names) into logs — route through the shared log service at debug level or drop the prints.
- [low] lib/data/services/voice_config_service.dart:36-39 — _preferences caches the SharedPreferences instance but getPreset/getOverrides/getGenders/getLocales all re-await it per call; on the first call before init every reader serializes on getInstance — acceptable, but note the getter is uncached per probe-style access; no action needed beyond awareness. (No finding — informational only; not reported as a defect line.)
- [info] lib/data/services/voice_config_service.dart:374 — resolveVoice hardcodes fallback voice 'af_heart' when the pool is empty — a magic constant that silently diverges from VoicePresets registry; if the registry renames/removes that voice ID, TTS requests fail at runtime with no compile-time signal — assert against the production symbol (e.g. VoicePresets.byId default) instead of a literal.
- [low] lib/data/services/voice_config_service.dart:220 — _leastUsedVoice reduce with `a.value <= b.value ? a : b` is fine for min-selection but returns the FIRST minimal entry; deterministic, no defect. Not reported.
- [medium] lib/data/services/voice_config_service.dart:29-33 — _serialized chains mutations but the returned `run`'s errors are attached via `onError: (_) {}` on the chain future while the caller gets `run` itself — an op that throws rejects `run` (caller sees it) but the chain future swallows it, so the NEXT chained mutation proceeds on a possibly-half-written blob with no record of the failure beyond the caller — acceptable if callers always await; flagging as low only if callers can fire-and-forget, which is not visible here. Not reported as a defect line (assumption stated).
- [low] lib/data/services/voice_config_service.dart:184 — gender fallback `femaleVoices.isNotEmpty ? femaleVoices : maleVoices` for non-male characters resolves to the male pool when the female pool is empty — a permissive fallback that assigns male voices to female/nonGendered characters silently; direction-of-failure: missing female voices should surface, not silently substitute — consider logging/throwing when the expected pool is empty.
- [low] lib/data/services/voice_config_service.dart:371-373 — resolveVoice pool fallback `[...femaleVoices, ...maleVoices]` mixes pools when the requested pool is empty, again a permissive silent substitution — same class as above; one finding per root cause would normally merge these, but they are distinct call sites with distinct pools; reported once here as the class instance.
- [low] lib/data/services/voice_config_service.dart:321 — renameCharacter early-returns when oldName == newName without logging — a no-op rename is indistinguishable from a failed one in traces; minor observability gap. Not reported as a defect line.
- [medium] lib/data/services/voice_config_service.dart:329-335 — _renameLoaded moves an override to newName only `if (!overrides.containsKey(newName))` — when an entry already exists under newName it is silently dropped (documented as intended "merge folds into real one"), but the dropped oldName override is never logged, so a merge that discards a custom voice is unobservable — log the discarded override at the same severity as other VoiceConfig prints. (Documented behavior; observability gap only.)
- [low] lib/data/services/voice_config_service.dart:336,341,346 — _renameLoaded performs three sequential read-modify-write cycles (overrides, genders, locales) each re-reading prefs; a failure between cycles leaves the rename half-applied with no compensating log — wrap in a single transactional write or log partial-apply. Not reported as a defect line (no crash path visible).
- [info] lib/data/services/voice_config_service.dart:26 — _mutationChains grows one entry per productionId forever and is never cleaned up — unbounded map growth in a long-lived singleton; low impact (small keys) but no eviction. Not reported as a defect line.
- [low] lib/data/services/voice_config_service.dart:117,262,307 — _saveOverrides/_saveGenders/_saveLocales write via prefs.setString without checking the returned Future's success beyond await; SharedPreferences write failures (disk full) surface only as the awaited exception, which _serialized's chain swallows (see 29-33) — combined with the swallowed chain errors, a failed save can be lost silently. Reported as the 29-33 class instance; not a separate line.
- [low] lib/data/services/voice_config_service.dart:136-137 — assignVoicesFromScript adjacency loop is O(n·window·chars²) with nested set operations; for large casts this is quadratic per line window — performance note only, no defect line (inputs are script-sized, not user-unbounded).
- [low] lib/data/services/voice_config_service.dart:146 — _charsForLine returns `[l.character]` for single-character lines but the closure is defined inside a static method and shadows nothing; fine. Not reported.
- [low] lib/data/services/voice_config_service.dart:154 — inner loop condition `j <= i + window` combined with `j < dialogueLines.length` is correct windowing; no off-by-one found. Not reported.
- [low] lib/data/services/voice_config_service.dart:190-193 — neighbor voice collection reads `assignment[neighbor]` only for already-assigned neighbors; unassigned neighbors contribute nothing, which is the intended greedy semantics. Not reported.
- [low] lib/data/services/voice_config_service.dart:205 — `chosen ??= _leastUsedVoice(...)` runs even when pool was non-empty but all voices taken; correct fallback. Not reported.
- [low] lib/data/services/voice_config_service.dart:236-240 — getGenders switch maps unknown values to CharacterGender.female — a permissive default: an unrecognized stored value silently becomes female rather than surfacing — direction-of-failure finding; merged with the 242-245 parse-swallow class? No, distinct root (default vs swallow). Reported here as its own low line? The checklist says one finding per root cause; this is a distinct root cause (permissive enum default). Reporting it.
- [low] lib/data/services/voice_config_service.dart:236-240 — unknown stored gender strings silently map to CharacterGender.female (the `_ =>` branch) — a corrupt/renamed enum value resolves to the permissive default instead of surfacing, changing voice-pool selection invisibly — log/throw on unrecognized values or store the enum name round-trip-safe.
- [low] lib/data/services/voice_config_service.dart:276 — getLocales `value as String` cast throws inside the try and is swallowed by the same catch as JSON errors — a non-string entry (e.g. a number) silently drops ALL locale overrides via the {} fallback, same silent-loss class as 79-82; covered by that finding's root cause (swallow-to-empty fallback), not separately reported.
- [low] lib/data/services/voice_config_service.dart:77 — getOverrides `value as Map<String, dynamic>` cast failure also lands in the swallow-to-{} catch — same class as 79-82; covered.
- [low] lib/data/services/voice_config_service.dart:51 — getPreset `VoicePresets.byId(presetId)` may throw for an unknown stored preset ID (registry renamed a voice); no try/catch here, so getPreset throws — actually this is a crash path, not silent: if the stored preset ID no longer exists in VoicePresets, getPreset throws and every caller of getPreset/resolveVoice/resolveSpeed crashes. Cannot see VoicePresets (context file not provided for voice_preset.dart), so state assumption: if byId throws on unknown ID this is an unhandled crash on stale persisted data. Reporting as medium with the assumption stated? The instruction: a suspicion that depends on unseen code must state the assumption. Reporting it as a finding with the assumption.
- [medium] lib/data/services/voice_config_service.dart:51 — getPreset calls VoicePresets.byId(presetId) with no guard; if the persisted preset ID is no longer in the registry (voice renamed/removed in an update), byId is assumed (unseen code) to throw, crashing every preset/voice resolution on stale data — wrap in try/catch falling back to the locale default and log the unknown ID.
- [low] lib/data/services/voice_config_service.dart:374 — duplicate of the 'af_heart' info line; not repeated.
- [low] lib/data/services/voice_config_service.dart:128-135 — assignVoicesFromScript takes genderOverrides map defaulting to const {} — fine. Not reported.
- [low] lib/data/services/voice_config_service.dart:181 — `genderOverrides[char.name] ?? char.gender` — fine. Not reported.
- [low] lib/data/services/voice_config_service.dart:376 — `voices[characterIndex % voices.length]` — voices.length > 0 guaranteed by 374 guard; modulo safe. Not reported.
- [low] lib/data/services/voice_config_service.dart:385-386 — resolveSpeed falls back to preset.defaultSpeed when no override — correct. Not reported.
- [low] lib/data/services/voice_config_service.dart:383 — resolveSpeed returns override.speed without clamping; caller-side concern. Not reported.
- [low] lib/data/services/voice_config_service.dart:97 — setOverride keys by config.characterName, not the requested key — if config.characterName differs from the intended character (caller bug), the override lands under an unexpected key silently; the API takes the name from the payload rather than a parameter, so a mismatch is invisible — consider passing characterName explicitly. Not reported as a defect line (no evidence of a caller mismatch in this batch).
- [low] lib/data/services/voice_config_service.dart:108 — removeOverride removes by characterName; removing a non-existent name is a silent no-op — fine. Not reported.
- [low] lib/data/services/voice_config_service.dart:340 — genders.putIfAbsent(newName, () => movedGender) preserves an existing newName gender (intended merge semantics, mirrors 329-335) — consistent; not reported.
- [low] lib/data/services/voice_config_service.dart:345 — locales.putIfAbsent(newName, () => movedLocale) — same merge semantics; consistent; not reported.
- [low] lib/data/services/voice_config_service.dart:30-32 — `_mutationChains[productionId] = run.then<void>((_) {}, onError: (_) {})` — the stored chain future discards errors, so a failed mutation is invisible to subsequent chained ops AND to any fire-and-forget caller; combined with 117/262/307 write failures this makes persistence loss silent — log chain errors via the shared error logger instead of `onError: (_) {}`. Reporting this as its own line now (it is the root cause; the write-failure instances fold into it).
- [low] lib/data/services/voice_config_service.dart:29 — `prev` may be a completed future; chaining is correct. Not reported.
- [low] lib/data/services/voice_config_service.dart:16-17 — private constructor + static final instance; standard singleton. Not reported.
- [low] lib/data/services/voice_config_service.dart:6-7 — imports script_models.dart and voice_preset.dart; voice_preset.dart is NOT in this batch and not provided as a context file — VoicePresets.byId/victorianEnglish/modernAmerican, CharacterVoiceConfig.fromJson/toJson, preset.femaleVoices/maleVoices/defaultSpeed are all unseen. Assumptions about byId throwing are stated in the finding above. No further findings derivable.
- [low] lib/data/services/voice_config_service.dart:74 — `jsonDecode(json) as Map<String, dynamic>` — if the stored blob is a JSON list/string, the cast throws and is swallowed to {} (covered by 79-82 class). Not separately reported.
- [low] lib/data/services/voice_config_service.dart:233 — same cast-swallow for genders (covered by 242-245). Not separately reported.
- [low] lib/data/services/voice_config_service.dart:116 — jsonEncode of overrides map; CharacterVoiceConfig.toJson unseen but assumed to produce JSON-encodable values. Not reported.
- [low] lib/data/services/voice_config_service.dart:60 — setPreset writes without the _serialized chain — setPreset is NOT chained while setOverride/removeOverride/setGender/setLocale are; concurrent setPreset + override mutation don't interleave on the same key (different keys), so no corruption; but two setPreset calls for the same production can race — last-write-wins on a single string key is benign. Not reported.
- [low] lib/data/services/voice_config_service.dart:252-254 — setGender chained correctly. Not reported.
- [low] lib/data/services/voice_config_service.dart:293-301 — setLocale chained correctly; null-locale removes the key (revert semantics) — correct. Not reported.
- [low] lib/data/services/voice_config_service.dart:306-307 — _saveLocales jsonEncode(locales) of a Map<String,String> — encodes fine. Not reported.
- [low] lib/data/services/voice_config_service.dart:148-166 — adjacency build: `adjacency[a]!.add(b)` and `adjacency[b]!.add(a)` — b was putIfAbsent'd at 159 before use at 161, a was ensured at 151; safe. Not reported.
- [low] lib/data/services/voice_config_service.dart:158 — `if (a != b)` guards self-adjacency for multi-character lines where a character appears twice; correct. Not reported.
- [low] lib/data/services/voice_config_service.dart:169-175 — sort comparator: `nb.compareTo(na)` returns most-neighbors first; tie-break by lineCount descending — deterministic. Not reported.
- [low] lib/data/services/voice_config_service.dart:173 — comment says "most neighbors first" and code returns nb.compareTo(na) — matches. Not reported.
- [low] lib/data/services/voice_config_service.dart:174 — comment says "then by prominence" and code `b.lineCount.compareTo(a.lineCount)` — descending lineCount; matches comment. Not reported.
- [low] lib/data/services/voice_config_service.dart:200 — `chosen = voice; break;` — first-fit; intended greedy. Not reported.
- [low] lib/data/services/voice_config_service.dart:209 — returns assignment map; characters with empty pool are skipped (186 `continue`) and get NO assignment — such characters then fall through resolveVoice's pool logic at call time; consistent. Not reported.
- [low] lib/data/services/voice_config_service.dart:212-221 — _leastUsedVoice: counts initialized for pool entries; `used` entries not in pool are skipped via containsKey — but `used` comes from assignment.values which ARE pool voices; safe. Not reported.
- [low] lib/data/services/voice_config_service.dart:220 — reduce returns min-count entry; ties resolved to first — deterministic. Not reported.
- [low] lib/data/services/voice_config_service.dart:226-245 — getGenders reads 'character_genders_$productionId' — key naming consistent with _saveGenders 262. Not reported.
- [low] lib/data/services/voice_config_service.dart:269-281 — getLocales key 'character_locales_$productionId' consistent with _saveLocales 307. Not reported.
- [low] lib/data/services/voice_config_service.dart:70 — getOverrides key 'voice_overrides_$productionId' consistent with _saveOverrides 117. Not reported.
- [low] lib/data/services/voice_config_service.dart:50 — getPreset key 'voice_preset_$productionId' consistent with setPreset 60. Not reported.
- [low] lib/data/services/voice_config_service.dart:88-90 — getOverride delegates to getOverrides; fine. Not reported.
- [low] lib/data/services/voice_config_service.dart:365-366 — resolveVoice override check via getOverride — fine. Not reported.
- [low] lib/data/services/voice_config_service.dart:369-370 — preset pool selection by isFemale; isFemale defaults true — callers unseen. Not reported.
- [low] lib/data/services/voice_config_service.dart:372-373 — spread fallback mixes pools (reported above as the permissive-fallback class instance at 184/371-373; one line covers both as the same root cause "empty-pool silent substitution"). Consolidating: the 184 line and this are the same defect class; per one-finding-per-root-cause I keep ONE line for the class. I already have the 184 line; do not add a second.
- [low] lib/data/services/voice_config_service.dart:1-4 — imports dart:convert, foundation, shared_preferences — all used. Not reported.
- [low] lib/data/services/voice_config_service.dart:19 — _prefs nullable field; fine. Not reported.
- [low] lib/data/services/voice_config_service.dart:26 — _mutationChains unbounded growth (noted above as info; keeping the info line? The instructions: findings are defensible lines; unbounded map growth in a singleton keyed by productionId is a real but tiny leak — I'll report it as low).
- [low] lib/data/services/voice_config_service.dart:26 — _mutationChains never evicts completed entries — unbounded growth over app lifetime for every productionId touched; negligible memory but unbounded — prune completed chains or use a fixed LRU.
- [low] lib/data/services/vision_ocr_channel.dart:26-28 — recognizeText catches MissingPluginException and returns null — on desktop/web where the plugin isn't registered, OCR silently reports "no result" indistinguishable from a genuine empty OCR; callers cannot distinguish "plugin missing" from "no text found" — return a typed error/status or log via the shared error logger. Direction-of-failure: missing capability resolves to the permissive "nothing found".
- [low] lib/data/services/vision_ocr_channel.dart:64-66 — ocrPdf same MissingPluginException → null swallow — same class as 26-28; one finding per root cause would file at the class, but these are two distinct methods with distinct callers; the checklist says same defect class same severity unless reachability differs — filing one line per method is acceptable since they are separate public APIs; consolidating into the 26-28 line with both sites listed in consequence per one-finding-per-root-cause (shared helper pattern: the try/catch shape). I'll file ONE finding citing both sites.
- [medium] lib/data/services/vision_ocr_channel.dart:26-28,64-66 — both recognizeText and ocrPdf catch MissingPluginException and return null — a missing native plugin (desktop/web, or plugin not registered after a hot reload) is indistinguishable from a genuine "no text/PDF result", so callers silently treat OCR as empty and the feature appears broken with zero observability — surface a typed MissingPlugin status or log through DebugLogService.logError before returning null.
- [low] lib/data/services/vision_ocr_channel.dart:16-17 — `result['blocks'] as List?` — if the native side returns a Map without 'blocks', silently treated as empty; defensive default is fine here (empty OCR), no permissive-authz concern. Not reported.
- [low] lib/data/services/vision_ocr_channel.dart:20 — `Map<String, dynamic>.from(b as Map)` — runtime cast; a malformed block throws OUTSIDE any try (the try only wraps invokeMethod), so one malformed native payload crashes recognizeText with an unhandled CastError — wrap the mapping in the try or validate. Reporting as low (native payload malformed is an edge case, operator-visible crash).
- [low] lib/data/services/vision_ocr_channel.dart:46 — same unguarded cast `Map<String, dynamic>.from(p as Map)` inside ocrPdf's pages mapping — same class as 20; one finding per root cause: the mapping casts sit outside the try/catch in both methods. Consolidated into one line citing 20 and 46 (and 50 for lines' lm cast).
- [low] lib/data/services/vision_ocr_channel.dart:50 — `Map<String, dynamic>.from(l as Map)` — same unguarded cast class; covered by the consolidated line.
- [low] lib/data/services/vision_ocr_channel.dart:41-42 — pageCount/failedPages default to 0 when absent — a native result missing 'pageCount' silently reports 0 pages; defensive default, low concern. Not reported.
- [low] lib/data/services/vision_ocr_channel.dart:47 — pageNum defaults 0 — a page missing 'page' becomes page 0 (0-based leak into a 1-based field per the comment at 78 "1-based"); cosmetic data-quality issue in results — low; reporting? The comment on VisionPage.page says "1-based" but a missing 'page' key yields 0, violating the documented invariant. Low severity, real. Reporting.
- [low] lib/data/services/vision_ocr_channel.dart:47 — a native page entry missing 'page' defaults to 0 while VisionPage.page is documented 1-based (line 78) — downstream page:line refs render "p0:…" — default to 1 or validate.
- [low] lib/data/services/vision_ocr_channel.dart:33 — `scale` param passed through; no validation of scale range — native-side concern. Not reported.
- [low] lib/data/services/vision_ocr_channel.dart:6 — MethodChannel name 'com.lineguide/vision_ocr' — constant; fine. Not reported.
- [low] lib/data/services/vision_ocr_channel.dart:9-29 — recognizeText returns Future<List<VisionTextBlock>?> — null on MissingPluginException vs empty list on empty blocks vs null result — three distinct outcomes collapsed into two shapes (null and []); callers must handle both; API ambiguity but not a defect per se. Not reported.
- [low] lib/data/services/vision_ocr_channel.dart:70-75 — VisionTextBlock fields non-nullable with required ctor; fine. Not reported.
- [low] lib/data/services/vision_ocr_channel.dart:84-94 — VisionPdfResult fields; fine. Not reported.
- [low] lib/data/services/vision_ocr_channel.dart:22-23 — text/confidence defaults ''/0.0 on missing keys — defensive; fine. Not reported.
- [low] lib/data/services/vision_ocr_channel.dart:52-53 — same defaults for lines; fine. Not reported.
- [low] lib/data/services/vision_ocr_channel.dart:1 — only services.dart imported; MethodChannel from there; fine. Not reported.
- [low] lib/data/services/vision_ocr_channel.dart:11 — invokeMethod<Map> without generic arg to invokeMethod<Map<String, dynamic>> — Map raw; the from() casts handle it. Not reported.
- [low] lib/data/services/vision_ocr_channel.dart:43 — `result['pages'] as List? ?? []` — fine. Not reported.
- [low] lib/data/services/vision_ocr_channel.dart:48 — `map['lines'] as List? ?? []` — fine. Not reported.
- [low] lib/data/services/vision_ocr_channel.dart:56 — VisionPage(page: pageNum, lines: lines) — fine. Not reported.
- [low] lib/data/services/vision_ocr_channel.dart:59-63 — VisionPdfResult construction; fine. Not reported.
- [low] lib/data/services/vision_ocr_channel.dart:31-33 — doc comment claims PDFKit rendering; platform code unseen; no claim verification possible. Not reported.
- [low] lib/data/services/vision_ocr_channel.dart:94 lines total — file fully inlined; reviewed completely.
- [low] lib/data/services/voice_config_service.dart:388 lines total — file fully inlined; reviewed completely.

## Coverage
lib/data/services/vision_ocr_channel.dart — findings: 4
lib/data/services/voice_config_service.dart — findings: 12
- [medium] lib/data/services/wav_silence.dart:98-100 — catch-all `catch (_)` returns the source path on any failure (parse, read, write, or the length-field rewrite) with no log, metric, or error surfaced — a corrupted/misparsed WAV silently "succeeds" from the caller's perspective, the destination file may be left half-written or unwritten, and the operator has no signal that the silence-prepend workaround failed — narrow the catch to expected IO/format errors and log the failure (or at minimum debugPrint like the sibling DebugLogService does) before returning `source`.
- [low] lib/data/services/wav_silence.dart:96 — `File(destination).writeAsBytes(result, flush: true)` is not atomic; if the process dies mid-write the destination is a truncated WAV with a RIFF size field (line 92) that no longer matches the file — playback of the padded file then fails or plays noise — write to a temp file and rename, or delete the partial destination on failure.
- [low] lib/data/services/wav_silence.dart:79 — `padBytes -= padBytes % frameSize` can round the padding down to zero when `frameSize` is large (e.g. 8-byte frames with a 350 ms silence budget below one frame), after which line 80 returns `source` — the intended "always prepend some silence" guarantee silently degrades to no padding for high-sample-rate/stereo files — clamp to at least one frame (`padBytes = padBytes < frameSize ? frameSize : padBytes - padBytes % frameSize`) or document the floor.
- [info] lib/data/services/wav_silence.dart:53-55 — when the data chunk's declared `dataLength` exceeds the bytes actually present (`dataLength > available`), the code silently substitutes `available` and proceeds — a truncated-but-RIFF-valid file is padded and rewritten with a corrected data-chunk size (line 93) rather than rejected, so a corrupt source is laundered into a "valid" output — consider returning null (falling back to the unmodified source, the safer path the doc comment at 19-20 promises) when the header claims more data than the file holds.

## Coverage
lib/data/services/wav_silence.dart — findings: 4
- [low] integration_test/android_kokoro_rtf_test.dart:28-35 — probe loop can spin for the full 6-minute window with no progress signal and no failure reason surfaced — if the sideload never lands, the test exits the loop silently and proceeds to instantiate OfflineTts against missing model files, producing an opaque sherpa init error instead of a clear "models absent" message — add a final `expect` after the loop asserting the model files exist (or break with a labeled failure) so the timeout surfaces the missing-sideload cause.
- [low] integration_test/android_kokoro_rtf_test.dart:60 — warm-up `tts.generate(text: 'Hello there.', ...)` result is discarded and its failure (if any) is unobserved; if warm-up throws, the timed run never executes and the probe reports nothing — wrap warm-up in `expect(() => tts.generate(...), returnsNormally)` or await/inspect its return so a warm-up regression is attributed to warm-up, not silently swallowed.
- [medium] integration_test/android_kokoro_rtf_test.dart:64-68 — `audio.samples.length / audio.sampleRate` assumes `samples` is non-empty and `sampleRate > 0`; a failed generate returning an empty/zero-rate buffer yields `NaN`/`Infinity` RTF printed as `rtf=NaN`, masking a synthesis regression as a formatting artifact — guard with `expect(audio.samples.length, greaterThan(0))` (and sampleRate > 0) before computing duration.
- [low] integration_test/android_kokoro_service_test.dart:24-28 — readiness wait loop has no terminal failure message distinct from the later `expect(..., reason: 'sideloaded pack not detected')`; the 6-minute spin is silent and the eventual assertion reason is the only diagnostic — print a per-poll progress line or include elapsed time in the assertion reason so CI logs show whether the sideload was slow vs. never delivered.
- [medium] integration_test/android_kokoro_service_test.dart:55-59 — `Future.wait` on two `svc.synthesize` calls with no `onError`/error isolation: if either queued synthesis throws, `Future.wait` rejects with a single merged error and the other result is lost, making the `both.whereType<String>().length` assertion unreachable and the failure cause ambiguous — use `Future.wait([...], eagerError: false)` semantics deliberately or catch per-future and assert individually so each queued request's outcome is independently observable.
- [medium] integration_test/android_kokoro_service_test.dart:65-76 — stale-vs-urgent cancellation is asserted only via `await stale` returning null; if the service's abort path instead completes the stale future with a non-null path (or throws), the test reports a generic type/expect failure with no indication that the *cancellation semantics* regressed — add a labeled reason on the `expect(await stale, isNull, ...)` (e.g. `'stale urgent line completed instead of being aborted'`) and log the stale future's outcome before asserting, so the observability of the supersede behavior is diagnosable from the probe output.
- [low] integration_test/android_kokoro_service_test.dart:80 — `svc.stop()` result is not awaited-checked for success; if stop fails (engine still running), subsequent test runs in the same session could inherit a live engine, and the probe prints 'ALL OK' regardless — `await` is present but the return value/failure is unasserted; assert stop completed (e.g. `expect(await svc.stop(), isNot(throwsAnything))` or check engine-stopped state) before printing ALL OK.

## Coverage
integration_test/android_kokoro_rtf_test.dart — findings: 3
integration_test/android_kokoro_service_test.dart — findings: 4
- [medium] integration_test/android_live_matching_test.dart:47-48 — DNS probe loop can exit with `dnsOk == false` after the 60 s window yet the test still proceeds to download/recognize steps with a dead network — the `expect(dnsOk, true, ...)` at line 48 does abort, but the loop's `break`-less structure means a false result is only caught by the expect, which is fine; however the loop swallows every lookup error with `catch (_)` and retries silently, so a permanently failing DNS is only reported after the full 60 s budget with no diagnostic of the underlying error — smallest safe fix: capture and print the exception type/message in the `catch` so the failure mode is diagnosable.
- [medium] integration_test/android_live_matching_test.dart:60-63 — if `isLiveAsrReady()` is false after the first check, `downloadLiveAsr()` is invoked unconditionally with no retry/timeout guard; a hung download stalls the whole 20-minute test budget with no partial progress reporting — smallest safe fix: wrap the download in a bounded timeout or add progress logging.
- [low] integration_test/android_live_matching_test.dart:171-183 — `_fetch` builds the entire response body in memory with no status-code check; a 404/500 HTML error page is silently returned as "wav bytes" and then fed to `_resampleTo16k`, producing garbage PCM and a misleading recognizer failure — smallest safe fix: assert `res.statusCode == 200` before consuming the body.
- [medium] integration_test/android_live_matching_test.dart:187-215 — `_resampleTo16k` assumes the WAV is PCM16 mono; it reads `fmt ` chunk size at `i+4` but never validates audio format (PCM vs float) or channel count, and it reads `rate` from `i+12` which is only correct for the canonical 16-byte `fmt ` chunk — a non-canonical fmt chunk (e.g. extensible WAV) yields a wrong sample rate and silently resamples at the wrong ratio — smallest safe fix: parse the fmt chunk's `audioFormat`, `channels`, and `bitsPerSample` fields and reject unsupported layouts.
- [low] integration_test/android_live_matching_test.dart:200-201 — chunk-walk advances `i += 8 + sz + (sz & 1)` even when `id` is unrecognized, but for the `data` chunk it sets `dataStart`/`dataLen` and then continues walking past the data payload using `sz`, which is correct; however if `sz` is 0 (common for streamed WAVs with unknown length), `dataLen` becomes 0 and the resampler emits an empty buffer with no error — smallest safe fix: treat `dataLen == 0` as an explicit failure rather than producing silence.
- [low] integration_test/android_live_matching_test.dart:203 — `outN = (n * 16000 / rate).floor()` truncates the final partial sample; for rates that don't divide evenly the output length is off by one sample, shifting the last interpolation step — smallest safe fix: use `.round()` or `ceil` and clamp to the source length.
- [low] integration_test/android_live_matching_test.dart:208-213 — interpolation indexes `v0` at `dataStart + i0*2` and `v1` at `dataStart + i1*2` but `i1 = i0 + 1` may read one sample past `dataLen` when `i0 == n-1`; the `.clamp(0, n-1)` on `i1` prevents an out-of-range read only because `i1` is clamped, yet `v1` then duplicates `v0` and the `frac` extrapolation `(v1-v0)*frac` still extrapolates beyond the last real sample — smallest safe fix: when `i0 == n-1`, set `v1 = v0` and skip the extrapolation term.
- [low] integration_test/android_paddle_ocr_test.dart:29-32 — `candidates.firstWhere(..., orElse: () => '')` returns an empty string when neither candidate path exists, and the subsequent `expect(pdfPath, isNotEmpty, ...)` is the only guard; the failure reason tells the operator to push the file but does not print which candidate paths were actually checked, making the on-device failure harder to diagnose over adb — smallest safe fix: include both candidate paths in the expect reason.
- [low] integration_test/android_paddle_ocr_test.dart:44-46 — division by `r.pageCount` is guarded with a ternary but the guard `r.pageCount == 0 ? 1 : r.pageCount` masks a genuinely zero-page result (which would indicate a broken pipeline) by reporting "1ms/page" instead of flagging it — smallest safe fix: assert `r.pageCount > 0` before computing the per-page metric.
- [low] integration_test/android_paddle_ocr_test.dart:88-93 — `ensureStarted()` timeout returns `false` on timeout and the test then merely prints; a hang in the sherpa ORT consumer consumes the full 3-minute budget and the comment at line 87 says "a crash/hang here is a failure" but the code never fails on timeout — smallest safe fix: expect `sherpaOk` to be true or explicitly fail when the timeout fires.

## Coverage
integration_test/android_live_matching_test.dart — findings: 7
integration_test/android_paddle_ocr_test.dart — findings: 3
## Coverage
integration_test/android_rehearsal_harness_test.dart — findings: 3
integration_test/asr_streaming_macos_test.dart — findings: 2

- [medium] integration_test/android_rehearsal_harness_test.dart:292-294 — transcript content printed verbatim into PROBE log lines — the recognizer transcript is user-voice-derived text (and the injected mode can echo the full scripted line) written to stdout with no truncation beyond a 57-char head; on a device this lands in `flutter run`/CI logs — cap the printed transcript (already partially done) and drop the raw `rec` byte-size echo, or route PROBE lines through a redaction helper before print
- [medium] integration_test/android_rehearsal_harness_test.dart:139,205,292 — per-iteration print statements inside the hot loop of a 20-minute soak — each of ~6 lines emits 2-3 PROBE lines plus the gap list, so a full run produces dozens of unstructured stdout lines that CI must buffer and that drown any real failure output — aggregate into one summary line per phase (or a `--verbose` gate) instead of printing per line/iteration
- [low] integration_test/android_rehearsal_harness_test.dart:110,115,139,157,205,292,303 — PROBE lines are printf-style ad-hoc strings with no stable key=value contract — downstream tooling that greps `PROBE:` cannot rely on field order (`mode=`, `score=`, `rec=`, `heard=` appear only in some lines), so metric extraction silently breaks when a field is added — emit a fixed schema (e.g. `PROBE phase=... key=value ...`) or switch to a structured logger
- [medium] integration_test/asr_streaming_macos_test.dart:167-169 — full recognizer transcript printed per candidate per line — the raw ASR hypothesis text (which on real audio could contain speaker-identifying content) is echoed unredacted into test stdout for 2 candidates × 4 lines — print only the match rate and RTF, or truncate the hypothesis to a bounded prefix
- [low] integration_test/asr_streaming_macos_test.dart:171-172 — mean-match summary uses `_lines.length` as denominator while `total` accumulates only lines that reached the print — if a WAV fails to parse (exception inside `_samplesFromWav`), the loop aborts and no mean is printed, but if entries are added to `_lines` without corresponding staged WAVs the denominator silently inflates and understates match — derive the denominator from the entries actually iterated (`_lines.entries.length` is identical here, but assert file presence first so a missing fixture fails loudly rather than skewing the mean)
- [low] integration_test/asr_testwav_transcript_macos_test.dart:52 — transcript printed to stdout without any redaction or log scrubbing — the recognized text of the fixture wav is emitted verbatim; harmless here because the fixture is a known public-domain sentence, but the same print pattern in production code would leak user speech — keep the print behind a debug flag or drop it before shipping the pattern
- [low] integration_test/asr_testwav_transcript_macos_test.dart:41 — WAV header assumed to be exactly 44 bytes with no RIFF/fmt chunk walk — a fixture with extra metadata chunks (LIST/INFO) would shift PCM data and silently mis-decode samples while the test still "passes" on garbage — parse the chunk chain or assert the header size before slicing
- [low] integration_test/asr_testwav_transcript_macos_test.dart:40 — sample rate read from offset 24 without validating the fmt chunk position — same class as the 44-byte assumption; a non-canonical header yields a wrong rate and the 16 kHz resample in the sibling test would silently corrupt audio — validate fmt chunk offset before reading
- [low] integration_test/kokoro_pack_smoke_macos_test.dart:111 — match threshold asserted against a local literal (0.8) with no reference to the production intelligibility gate — if the shipped smoke gate in CastCircle uses a different constant, this test cannot detect drift in either direction — assert against the shared production constant (or a dart-define) instead of a local literal
- [low] integration_test/kokoro_pack_smoke_macos_test.dart:27 — voice grid hardcoded to 4 entries with no assertion that voices.bin actually contains all 53 speakers — the comment at lines 3–4 states the pack must stay complete, but nothing checks speaker count; a truncated voices.bin that still satisfies these 4 voices would pass — read the voices.bin speaker count (or assert file length against the expected 53-speaker size) before the loop
- [low] integration_test/kokoro_pack_smoke_macos_test.dart:113-114 — tts.free()/asr.free() only run on the success path; a failed expect at line 97 or 111 leaks both native ONNX sessions for the rest of the test process — wrap in tearDown/addTearDown so resources are released even on assertion failure
- [low] integration_test/kokoro_pack_smoke_macos_test.dart:107 — stream.free() likewise skipped when expect throws earlier in the loop body — same leak class as the tts/asr frees; move stream.free() into a finally or addTearDown
- [low] integration_test/kokoro_pack_smoke_macos_test.dart:36-47 — _matchRate implements a longest-common-subsequence DP but the inner max selection (line 43) omits the standard prev[j-1] branch, so the DP is not a correct LCS — with a wrong-but-plausible score the 0.8 gate can pass a degraded transcript or fail a good one; fix the recurrence to max(prev[j-1]+1, prev[j], cur[j-1]) on match
- [low] integration_test/kokoro_pack_smoke_macos_test.dart:50-60 — _to16k resamples with linear interpolation but never handles fromRate == 16000 (n computes to x.length only by coincidence of the formula) and clamps indices rather than guarding rate<=0 — a 0 or negative sampleRate from a bad generate() result would produce a zero-length or garbage buffer instead of a loud failure — assert audio.sampleRate > 0 before resampling
- [low] integration_test/kokoro_pack_smoke_macos_test.dart:96-98 — audio.samples length asserted > 16000 (one second at 16 kHz) but the sample rate is whatever Kokoro returns (typically 24 kHz), so the floor is really ~0.66 s of audio — the guard is weaker than the comment "almost no audio" implies; compare duration (samples/sampleRate) against a seconds-based floor instead

## Coverage
integration_test/asr_testwav_transcript_macos_test.dart — findings: 3
integration_test/kokoro_pack_smoke_macos_test.dart — findings: 7
- [low] integration_test/kokoro_service_queue_macos_test.dart:89,105 — `await stale` / `await prefetch` on a Future already consumed by `Future.wait`/other awaiters — if the synthesize future completes with an error, the test hangs or double-awaits nondeterministically; assert on a captured result instead — restructure to capture results before asserting
- [low] integration_test/kokoro_service_queue_macos_test.dart:51-52 — `expect(..., reason: 'staged pack not detected')` asserts readiness against a locally staged copy rather than the production model path the service actually loads — test cannot fail on a real regression in model resolution — assert against the production model directory or a service-level staging API
- [medium] integration_test/ocr_dump_macos_test.dart:34 — copies a repo fixture PDF into the app temp dir and reads it back; if `src` is missing the test crashes with an unhandled exception and no diagnostic — guard with a clear skip/fail message when the fixture is absent
- [low] integration_test/ocr_dump_macos_test.dart:57 — `jsonEncode(dump)` may throw on non-encodable values (e.g. NaN confidence) and abort the dump after the expensive OCR pass — sanitize/encode defensively or write via `JsonEncoder` with toEncodable
- [low] integration_test/ocr_dump_macos_test.dart:65 — `expect(parsed.lines, isNotEmpty)` is the only assertion; title/character/line fields are dumped unchecked, so a regression to empty/partial parse still passes — assert minimum counts on characters and typed lines
## Coverage
integration_test/kokoro_service_queue_macos_test.dart — findings: 2
integration_test/ocr_dump_macos_test.dart — findings: 3
- [low] integration_test/ocr_highlight_audit_macos_test.dart:107 — audit example string interpolates raw OCR line text into a printed log line — unbounded script text (could include names/PII from the scanned script) is echoed to stdout in CI/device logs — truncate before interpolation and log only counts/keys (page numbers), not raw text
- [low] integration_test/ocr_highlight_audit_macos_test.dart:113-115 — audit summary printed via `print` with no structured format or failure capture — if `flagged` is 0 the percentage silently divides by the guard constant and the run still passes only because of the later `expect`, masking a silently-empty OCR run — assert `byPage`/`parsed.lines` non-empty before computing ratios and emit a machine-readable summary line
- [low] integration_test/ocr_import_macos_test.dart:47-48 — import summary printed via `print` with no structured format — counts are only visible in raw stdout, no correlation ID or engine tag, making cross-run comparison on the device fragile — emit a stable `key=value` line including engine/page count and assert on it
- [low] integration_test/ocr_import_macos_test.dart:49-51 — thresholds `greaterThan(800)` / `lessThan(80)` can never fail for a silently-empty import if `parsed.lines` is empty — the `review` count would be 0 (passes `lessThan(80)`) while `dialogue` would be 0 (fails `greaterThan(800)`), so the dialogue check does guard, but the review threshold alone would accept a regression to zero flagged lines — add `expect(review, greaterThan(0))` alongside the upper bound
- [info] integration_test/ocr_highlight_audit_macos_test.dart:32,35 — test reads a fixture PDF from a repo-relative path and copies it into the sandbox temp dir — verify `sample-scripts/Pride-Prejudice-SCRIPT.pdf` is not a committed large binary asset that should be gitignored/LFS-managed rather than inlined in the repo
- [info] integration_test/ocr_import_macos_test.dart:32,38 — same fixture-copy pattern — verify the fixture is not committed as a raw binary blob

## Coverage
integration_test/ocr_highlight_audit_macos_test.dart — findings: 3
integration_test/ocr_import_macos_test.dart — findings: 2
- [low] integration_test/ocr_page_repeat_macos_test.dart:32,45 — per-iteration `print` in the test loop emits noisy per-item logging in a hot loop — log spam across 6 sequential + 3 concurrent iterations with no structured tag — guard prints behind a debug flag or drop them
- [low] integration_test/rehearsal_demo_test.dart:96-100 — wall-clock `DateTime.now()` loop with real `Future.delayed` inside a widget test can hang CI if the recorder never starts — test timeout (20 min default in sibling test) not set here; add `timeout:` to the `testWidgets` call
- [info] integration_test/rehearsal_demo_test.dart:102 — `db.close()` not wrapped in a guard; if any earlier await throws (e.g. asset load at line 40), the test fails without closing the database — wrap body in try/finally with `db.close()` in finally

## Coverage
integration_test/ocr_page_repeat_macos_test.dart — findings: 1
integration_test/rehearsal_demo_test.dart — findings: 2
- [medium] integration_test/tts_kokoro_compare_macos_test.dart:119 — per-item `print` of OUT dir plus per-voice/line result lines inside a 9-iteration hot loop — noisy per-item logging in hot loops; the loop prints 9+ lines per model (18 total) with RTF/dur/match/corr, drowning the two `===` summary lines the test exists to produce — accumulate per-model results and print only the `===` summary (or gate per-line prints behind a debug flag)
- [low] integration_test/tts_kokoro_compare_macos_test.dart:119 — `print('OUT: ${outDir.path}')` is the only record of where WAVs land; if the test crashes mid-loop the path is lost — print the outDir path again in the failure path or write a manifest file next to the WAVs
- [medium] integration_test/tts_kokoro_compare_macos_test.dart:174 — second `acceptWaveform` feeds 12800 zero samples as a flush tail without checking `asr.isReady`/decoding after it — the final utterance may never be decoded before `getResult`, silently lowering `asrMatch` for every line and corrupting the intelligibility proxy the test exists to measure — after the flush tail, loop `while (asr.isReady(stream)) asr.decode(stream);` once more before `getResult`
- [low] integration_test/tts_kokoro_compare_macos_test.dart:203 — correlation divides `sab / sqrt(saa*sbb)` with no guard for zero-energy reference (all-silent TTS output) — a degenerate fp32 reference yields NaN corr printed as `corr=NaN`, misleading the acoustic-transparency verdict — guard `saa*sbb > 0` before dividing, else skip corrNote
- [low] integration_test/tts_kokoro_compare_macos_test.dart:196 — `lenRatio` computed but never asserted or thresholded; a wildly different-length int8 output still prints `corr≈1.0` if the overlap happens to align — include lenRatio in the pass/fail criterion (e.g. warn when outside 0.9–1.1) or drop it
- [medium] integration_test/screenshot_test.dart:69-73 — test mutates real device `SharedPreferences` (`auth_skipped`, `screenshot_mode`) on the simulator/phone under test; if the run is interrupted (timeout at 30 min, crash in `_snap`) the restore block at 191-192 never executes, leaving the device's app signed out / onboarding suppressed for the next manual launch — wrap the mutation in `runZoned`/`finally`-equivalent (or use a fresh `SharedPreferences` mock instance) so state is restored even on failure
- [low] integration_test/screenshot_test.dart:78 — `AppDatabase.forTesting(NativeDatabase.memory())` is closed at 193 but the in-memory DB is also used as the live app DB via `databaseProvider.overrideWithValue`; any provider-triggered write during the run (e.g. `productionsProvider.add` at 111) persists only in memory — fine for screenshots, but if this file is ever repurposed as a smoke test the silent in-memory substitution hides persistence bugs — add a comment or assert that no provider writes are expected
- [low] integration_test/screenshot_test.dart:133-136 — `firstWhere(..., orElse: () => hamlet.scenes.first)` silently falls back to scene 1 when no scene contains HAMLET; the actor-mode screenshot (06) would then show the wrong scene with no failure — assert the found scene actually contains 'HAMLET' (or fail the test) so the screenshot is deterministic
- [low] integration_test/screenshot_test.dart:44-47 — `_surfaceConverted` is a module-level flag set once; if a later `_snap` runs after a hot-restart/re-entry the flag stays true and `convertFlutterSurfaceToImage` is skipped, producing a non-image surface for subsequent screenshots — reset the flag at the start of `main()`/test body or key it per-test
- [info] integration_test/screenshot_test.dart:69-73,191-192 — the test reads/writes device-wide preference keys `auth_skipped`/`screenshot_mode`; verify these keys are not also used by production code to gate real auth/onboarding state that persists beyond the test run (cannot confirm from this file alone)
## Coverage
integration_test/screenshot_test.dart — findings: 5
integration_test/tts_kokoro_compare_macos_test.dart — findings: 5
- [medium] scripts/deploy.sh:23 — install success is detected by grepping the combined stdout/stderr for the exact string "App installed" — if `xcrun devicectl` changes wording, emits a localized message, or prints the success line to a stream the `2>&1` redirect misses, the loop treats a successful install as failure and burns all 6 retries before exiting 1 — match on exit status (`if xcrun ... ; then`) or a more robust marker, keeping the grep only for diagnostics.
- [medium] scripts/deploy.sh:24 — launch failure is masked by `|| true` and its output is discarded, so a launch failure (app crashed at startup, wrong bundle id, device locked) is reported as "✓ deployed + launched" — drop `|| true` (or capture and report the failure) so `set -euo pipefail` aborts with the real error.
- [medium] scripts/deploy.sh:15 — `flutter build ios --release 2>&1 | tail -4` hides build failure detail, but `pipefail` is set so a failed build does abort — however the success path prints only the last 4 lines, which on a long build are generic progress lines, and the script proceeds to install a possibly stale `build/ios/iphoneos/Runner.app` if the build silently produced no new artifact — print a build-success marker or verify the app bundle mtime/recency before install.
- [low] scripts/deploy.sh:9 — device UDID is hardcoded as a default env var rather than resolved from a connected device; on a machine with a different device the script fails after the build with "no build"/install errors — accept `DEVICE` override only (already supported) but fail fast with a list of attached devices instead of retrying 6 times against a nonexistent UDID.
- [info] scripts/deploy.sh:21 — retry loop sleeps 8s between attempts but never distinguishes "device locked/busy" from other failures; the final message guesses the cause — consider `devicectl` error output to pick the right hint.
- [medium] scripts/fetch-ort-java.sh:19 — `curl -sfL` with `-f` suppresses the HTTP error body, and on a 404/proxy error the script proceeds to `shasum` an empty/missing file; combined with the version check at :22 the failure surfaces only as a SHA mismatch for 1.22.0, but for any other `$V` the script happily "vendors" a bogus/empty artifact — check curl's exit status explicitly and fail with the HTTP code before hashing.
- [medium] scripts/fetch-ort-java.sh:22-32 — the pinned-SHA guard applies only when `$V` = "1.22.0"; any other version (e.g. `./fetch-ort-java.sh 1.23.0`) is downloaded, unpacked, and copied into `android/app/libs` and `jniLibs` with **no** hash verification at all, so a substituted/compromised Maven artifact injects native `.so` code into the app — require an explicit pin (or an interactive confirmation) for every non-default version, not just print a warning.
- [medium] scripts/fetch-ort-java.sh:31 — the "no pinned hash" branch only prints a warning and continues; the next lines unzip/copy/patchelf the unverified artifact — make the warning branch exit non-zero (or prompt) so CI cannot vendor an unverified binary.
- [low] scripts/fetch-ort-java.sh:16 — `EXPECTED_SHA_1_22_0` is defined but the comparison at :23 uses it only for the exact version string; if `$V` is e.g. `1.22.0` with different case/whitespace the pin silently doesn't apply — normalize `$V` before comparison.
- [low] scripts/fetch-ort-java.sh:34 — `cp "$TMP/classes.jar" ...` assumes the AAR layout contains `classes.jar`; a layout change (or partial download) makes `cp` fail mid-script after `set -e` aborts, leaving a partially-vendored tree (jar copied, .so not) — validate the expected AAR entries before copying.
- [low] scripts/fetch-ort-java.sh:35-47 — the abi loop has no fallback if an ABI directory is missing from the AAR; `cp` fails for that ABI and `set -e` aborts, leaving earlier ABIs vendored and later ones stale from a previous run — check each expected `.so` exists and clean the destination dirs first.
- [low] scripts/fetch-ort-java.sh:45 — `patchelf --clear-symbol-version` is assumed present; if patchelf is missing the loop fails after the jar has already been copied, leaving a half-vendored state — check for patchelf availability before starting the vendoring.
- [info] scripts/fetch-ort-java.sh:12 — `trap 'rm -rf "$TMP"' EXIT` is fine, but note `$TMP` is created with `mktemp -d` under a predictable-ish path only if the shell lacks mktemp; no issue on macOS/Linux defaults.
- [info] scripts/fetch-ort-java.sh:49 — final `shasum` prints hashes of the vendored artifacts for manual pinning; consider emitting them in a machine-readable form so the pin at :16 can be updated by script rather than by hand.

## Coverage
scripts/deploy.sh — findings: 5
scripts/fetch-ort-java.sh — findings: 8
- [medium] scripts/phone-harness.sh:59 — background `flutter test` subprocess output is redirected to a file but the harness's own stdout/stderr are not surfaced, and the loop's `adb shell`/`run-as` provisioning output is discarded — if the device never installs the package, the script silently loops 120×3s and exits with the test's status, hiding the provisioning failure — add an explicit `break`/error message when the 120-iteration loop exhausts without seeing `$PKG` in `pm list packages`.
- [medium] scripts/phone-harness.sh:74 — the `run-as` provisioning command's quoting nests single quotes inside a double-quoted `$ADB shell` invocation; `sh -c "'...'"` passes the inner script as one argument, but the inner `cp -r /data/local/tmp/kpack app_flutter/models/.kpack-tmp` copies a directory onto a nonexistent parent path — if `app_flutter/models/live_asr` was not created by the first `mkdir` (e.g. `run-as` lacks the dir), the copy fails silently under `2>/dev/null`-style suppression and the loop reports "provisioned" based only on the `wc -l` count of a stale directory — drop the `2>/dev/null`-equivalent suppression and fail loudly on a nonzero `run-as` exit.
- [medium] scripts/phone-harness.sh:75-76 — the readiness check counts entries in `app_flutter/models/live_asr` with `wc -l` and treats `>= 4` as success, but `ls` on a nonexistent directory prints an error to stderr and outputs nothing, so `COUNT` becomes an empty string; the `${COUNT:-0}` default then makes the comparison `-ge 4` fail every iteration without any log line explaining why — capture the `ls` stderr and emit a per-iteration diagnostic when the directory is missing.
- [low] scripts/phone-harness.sh:19 — the log file `$OUT` is written under `$TMPDIR`/`/tmp` with no rotation or cleanup and accumulates a full `flutter test` transcript per run — on a long-lived CI runner or daily-driver host this grows unboundedly; use `mktemp` and remove it in an `EXIT` trap after printing the PROBE lines.
- [low] scripts/phone-harness.sh:90-91 — `grep -E "PROBE:"` and the pass/fail grep both discard their exit status, so a run whose log contains neither pattern still exits with `$STATUS` from `wait`, and the "(none — see $OUT)" fallback is only an echo, not a failure signal — emit a distinct nonzero exit (or a loud warning) when neither the PROBE metric nor a pass/fail line is found, so callers can distinguish "no metrics captured" from "tests passed".
- [medium] scripts/play-changelog.sh:16 — `VERSION_CODE="${VERSION_LINE##*+}"` extracts the build number by stripping up to the last `+`; if `pubspec.yaml`'s `version:` line lacks a `+` (e.g. `version: 1.2.3`), `VERSION_CODE` becomes the whole version string, the changelog is written to `changelogs/1.2.3.txt` instead of a numeric code file, and Fastlane silently ships no release notes — validate that `VERSION_CODE` matches `^[0-9]+$` and exit with an error otherwise.
- [medium] scripts/play-changelog.sh:30-31 — the staleness guard compares `TOP_CODE` (extracted via `grep -m1 -oE '^## [0-9.]+\+[0-9]+'`) against `VERSION_CODE`, but the `-oE` pattern is anchored with `^` inside `-o` mode where GNU grep applies it per-match; on a changelog whose first `##` heading is preceded by other `##`-level text (e.g. an `## Unreleased` block), `TOP_CODE` can be empty or wrong, and the script then either blocks a valid release or ships stale notes — extract the code from the first `## <version>+<code>` heading with a stricter pattern and fail loudly when no match is found.
- [low] scripts/play-changelog.sh:71-76 — the 500-char truncation counts bytes with `wc -c` but trims with `awk` using `length($0)`, which counts characters under a UTF-8 locale in some awks (gawk vs mawk vs busybox awk differ), so multi-byte release notes can be trimmed to a byte count that still exceeds Play's limit or cut earlier than needed — measure both with the same unit (e.g. `wc -m` consistently, or truncate in the Python step).
- [low] scripts/play-changelog.sh:75-76 — the awk truncation drops the trailing newline handling: `total += length($0) + 1` assumes one byte per line for the newline, but the final line written by `printf '%s\n'`/Python's `print` adds a newline that the count already included via `wc -c`; the emitted `.tmp` file may end without a newline, leaving the last line unterminated — append a final newline or compute the limit against the exact serialized form.
- [low] scripts/play-changelog.sh:79-83 — the empty-output guard checks `-s` (non-zero size) but a file containing only a newline passes, and the error message tells the user to pass text explicitly without distinguishing "empty" from "whitespace-only" — strip whitespace before the check or use `grep -q '[^[:space:]]'`.

## Coverage
scripts/phone-harness.sh — findings: 5
scripts/play-changelog.sh — findings: 5
- [medium] scripts/play-preflight.sh:74 — HAS_ALPHA probe swallows python3/PIL failure via `|| echo 0`, resolving to the permissive "no alpha" value when the check cannot run — a store icon with an alpha channel ships to Play and is rejected at upload (or worse, ships a rejected asset after review) — fail closed: treat a failed probe as an error (`bad "cannot inspect icon.png alpha"`) instead of defaulting to 0.
- [medium] scripts/play-preflight.sh:104-105 — screenshot aspect-ratio math interpolates `$W`/`$Hh` into a python3 one-liner with no validation; if sips fails (or returns no pixel lines) the values are empty and python3 raises a syntax/ZeroDivision error inside the loop, aborting the whole preflight under `set -e` with no ✗ item printed — guard: skip/flag the file when W or Hh is empty/non-numeric before invoking python3.
- [medium] scripts/play-preflight.sh:71,89,103 — `sips -g pixelWidth -g pixelHeight ... | awk '/pixel/{print $2}'` assumes macOS sips output shape; on a non-macOS host (or a sips that prints localized labels) W/Hh come back empty and every icon/screenshot check silently misfires (e.g. "icon.png must be 512×512 (is ×)") — assert non-empty W/Hh before comparing, and fail the check when the probe tool is unavailable.
- [low] scripts/play-preflight.sh:85 — `stat -f %m`/`stat -f %z` are BSD-only flags; on GNU/Linux stat these abort with "invalid option" and (under `set -e`/pipefail) the freshness and size checks die mid-script, turning a "ready to upload" verdict into an unexplained non-zero exit — use a portable mtime/size probe or branch on OS.
- [low] scripts/play-preflight.sh:136 — `find ... -newer "$AAB" -print -quit` with `|| true` masks find failures (e.g. AAB path missing as a directory component), so a broken freshness probe reads as "AAB is newer than every source file" — drop `-quit`'s masking `|| true` and let a find error surface as a ✗ item.
- [low] scripts/play-preflight.sh:117-121 — `bash -n "$sh" 2>/dev/null` discards the syntax-error text on the first invocation, then re-runs `bash -n` a second time to fetch the message; if the second run's stderr is redirected differently or the file is unreadable between the two calls, the ✗ line prints an empty reason — capture the message once into a variable and reuse it.
- [low] scripts/play-preflight.sh:129 — `flutter build appbundle --release >/dev/null` discards build stdout but keeps stderr; a slow/verbose build also blocks the "seconds, not days" gate and its exit status is not checked, so a failed build falls through to the stale-AAB check and reports "no AAB — run with --build" instead of the real build error — check the build's exit status and `bad` on failure with stderr retained.
- [low] scripts/pull-crashlog.sh:48 — `idevicecrashreport -u "$UDID" -k "$CRASHDIR" 2>/dev/null || true` discards the tool's stderr and exit status; when the device pull fails (device unplugged, wrong UDID), the script proceeds to parse a stale/absent local file and reports "MOST RECENT CRASH" from last run's leftovers — capture stderr and `exit 1` (or warn) when the pull fails.
- [medium] scripts/pull-crashlog.sh:64-133 — the entire python3 crash-parser is inlined as a single `-c` string whose failure path (134-140) only prints `head -5` + a grep; the parser's own stderr is merged into the fallback via `2>&1`, so a JSONDecodeError prints a traceback into the "raw header" section and the real crash metadata is never shown — split parse errors from tool errors and print a clear ✗ with the file path when parsing fails.
- [low] scripts/pull-crashlog.sh:67 — `open('$LATEST')` interpolates a shell variable into a python string with no escaping; a path containing a quote or `$` breaks the one-liner and silently routes to the weak fallback — pass the path via argv (`sys.argv[1]`) instead of string interpolation.
- [low] scripts/pull-crashlog.sh:22,24,27,29,31 — device-detection pipelines (`xcrun ... | grep ... | head -1 | awk ... || true`, `flutter devices | grep -E "ios\s"`) swallow every failure with `2>/dev/null`/`|| true`; when no tool is installed the script reports "No device found" without saying the detector itself failed, sending the operator hunting the wrong problem — distinguish "no device" from "detector unavailable" in the error text.
- [low] scripts/pull-crashlog.sh:51 — `find "$CRASHDIR" "$CRASHDIR_LEGACY" ...` runs even when `$CRASHDIR_LEGACY` (/tmp/crashlogs) does not exist; find prints its error to stderr (suppressed by 2>/dev/null) and the missing-dir case is indistinguishable from "no crashes found" — test `-d` on each dir before including it, or report the missing legacy dir as info.
- [low] scripts/pull-crashlog.sh:13-15 — crash logs are pulled into a shared world-writable /tmp directory (`/tmp/castcircle-crashes`) with no rotation or cleanup; crash .ips files contain stack traces, bundle IDs and sometimes user paths, accumulating indefinitely on a shared host — use a per-user dir (`${XDG_CACHE_HOME:-$HOME/.cache}/castcircle-crashes`) or add retention/cleanup.
- [low] scripts/pull-crashlog.sh:143-149 — `OTHER` list is built from `tail -n +2 | head -5` of a sorted-by-basename-descending list; filenames sort by name not by crash time, so "OTHER RECENT CRASHES" can list older crashes above newer ones (misleading ordering for triage) — sort by mtime or by the timestamp embedded in the filename with a documented key.

## Coverage
scripts/play-preflight.sh — findings: 7
scripts/pull-crashlog.sh — findings: 7
- [medium] scripts/pull-debuglog.sh:24 — pipeline failure swallowed by `tail -3` — `if ! cmd | tail -3` tests tail's status, not xcrun's, so a failed device copy prints the error text but the script still proceeds to `tail -n "$LINES"` on a missing/stale file — use `set -o pipefail` (already set) plus `if ! xcrun ... 2>&1 | tail -3; then` is broken by the pipe; capture output first (`out=$(xcrun ... 2>&1) || { ...; }`) then `printf '%s\n' "$out" | tail -3`.
- [medium] scripts/pull-debuglog.sh:31 — `tail -n "$LINES"` on a file that may not exist — when the copy fails (or the previous run left `$OUT` absent), line 31 reads a nonexistent path; with `set -e` the script dies with an unexplained status instead of the intended error message — guard: `[[ -f "$OUT/debug_log.txt" ]] || { echo "ERROR: no debug_log.txt pulled" >&2; exit 1; }`.
- [low] scripts/pull-debuglog.sh:15 — `LINES="${1:-120}"` accepts any string, including non-numeric — `tail -n "$LINES"` then fails with a confusing usage error instead of a clear message — validate: `[[ "$LINES" =~ ^[0-9]+$ ]] || { echo "usage: $0 [N]" >&2; exit 2; }`.
- [medium] scripts/ship-play.sh:60 — signature-entry detection can match a non-signature META-INF entry — `unzip -l "$AAB" 'META-INF/*' | grep -oE 'META-INF/[^ ]+\.(RSA|DSA|EC)'` matches any path ending in those extensions (e.g. `META-INF/services/...EC` or a filename containing a space is excluded, silently skipping the real cert), so `SIG_ENTRY` can be wrong or empty and the debug-signature check at :69 never fires — enumerate entries with `unzip -l ... | awk '{print $4}'` and require exactly one `META-INF/*.(RSA|DSA|EC)`, failing loudly on zero or >1.
- [medium] scripts/ship-play.sh:63 — `keytool -printcert` output parsed for English 'Owner:' — non-English JVM locales print a localized label, so `OWNER` stays empty and the script exits at :66 claiming an unreadable cert even though signing is fine — match on the cert subject pattern or use `keytool -printcert -jarfile "$AAB"` and grep for the alias/fingerprint instead of a translated label.
- [medium] scripts/ship-play.sh:69 — debug-key detection depends on the Owner string containing 'Android Debug' — a debug-signed AAB whose cert subject is customized (or a localized keytool output, per the :63 finding) bypasses this guard and an unsigned-for-release build gets uploaded — compare the cert fingerprint against the known debug keystore fingerprint (or assert the upload-key alias from key.properties) rather than substring-matching a label.
- [low] scripts/ship-play.sh:104 — `fastlane android "$LANE" $LANE_ARGS` word-splits user-controlled options — `--live`/`--validate` are fixed strings today, but any future `LANE_ARGS` value with spaces/quotes changes fastlane semantics; the shellcheck disable comment acknowledges it — pass options as an array: `fastlane android "$LANE" ${lanes_args[@]}` with `IFS`-safe quoting, or build the array explicitly.
- [low] scripts/ship-play.sh:36-39 — `--live`/`--validate` rejection only fires when `LANE` is `beta` — `--build --live` (BUILD_ONLY path) still sets `LANE_ARGS` and the guard at :36 passes only because LANE is beta; harmless today since :86 exits before upload, but the validation is order-dependent and easy to break — move the option-consistency check after all parsing and base it on the final `LANE`/`BUILD_ONLY` state.
- [low] scripts/ship-play.sh:59 — `KT` fallback silently drops `-printcert` diagnostics — if `/usr/libexec/java_home` is missing (Linux CI), `KT=keytool` relies on PATH; if keytool is also absent the pipeline at :63 yields empty OWNER and the script exits with the misleading "Could not read the AAB's signing cert" message instead of "keytool not found" — check `command -v keytool` and emit a distinct error.

## Coverage
scripts/pull-debuglog.sh — findings: 3
scripts/ship-play.sh — findings: 5
- [medium] scripts/ship-testflight.sh:94 — upload failure not surfaced distinctly from success; `xcrun altool` failure under `set -e` aborts before the dSYM push, but a partial/failed upload prints nothing to stdout and the operator must read stderr — consequence: a failed TestFlight upload can be misread as shipped if stderr is swallowed by CI wrappers; smallest safe fix: capture altool exit status explicitly and echo a distinct "✗ upload failed" line before exiting.
- [medium] scripts/ship-testflight.sh:98-101 — dSYM upload failure is swallowed to a warning echo with no exit-code capture or retry, and the failure message is only printed if the binary exists — consequence: unsymbolicated crashes in production with no alert in CI logs beyond a single ⚠ line that can be missed; smallest safe fix: record the upload-symbols exit code and emit a structured failure marker (e.g. `::warning::` or a distinct tag) so CI can alert on it.
- [low] scripts/ship-testflight.sh:103 — missing upload-symbols binary is reported only as a warning echo with no non-zero exit or CI-visible marker — consequence: dSYMs silently never pushed when Pods not installed, hiding the observability gap; smallest safe fix: emit a distinct failure tag or set a flag so the ship summary reports "dSYMs not pushed".
- [low] scripts/ship-testflight.sh:95 — success message "uploaded build $GOT" is echoed before the upload actually succeeded is verified only by altool's implicit exit (set -e), and the dSYM push result is not reflected in the final "✓ shipped" line — consequence: the final line claims success regardless of dSYM state, misleading operators; smallest safe fix: include dSYM push status in the final summary line.
- [low] scripts/verify-apk-ort.sh:14-15 — `strings | grep -E "^VERS_[0-9.]+$" | head -1` under `set -o pipefail` can SIGPIPE-fail the pipeline if `strings`/`unzip` produce large output and grep exits early on a match — consequence: verification can fail spuriously on a match, same class as the -q trap the script itself documents at lines 20-21; smallest safe fix: use `grep -m1` with output redirection or `awk 'NR==1{print;exit}'` to avoid early-exit SIGPIPE, or drop pipefail for these pipelines.
- [low] scripts/verify-apk-ort.sh:12 — `find ~/.pub-cache/hosted/pub.dev -path ... | head -1` under pipefail: `find` may be SIGPIPE-killed by `head -1` exiting early, failing the assignment — consequence: script aborts with no message even though the .so exists; smallest safe fix: use `find ... -print -quit` or capture into a variable with `|| true` semantics, or disable pipefail for this line.
- [info] scripts/ship-testflight.sh:39-41 — sourcing `$HOME/.appstoreconnect/ids.env` is a git-ignored local file per the header comment; verify it is not committed — consequence: if committed, ASC key/issuer ids would leak; smallest safe fix: confirm gitignore covers `~/.appstoreconnect/` (out-of-repo path, so likely fine) and that no repo copy exists.

## Coverage
scripts/ship-testflight.sh — findings: 5
scripts/verify-apk-ort.sh — findings: 2
- [medium] lib/features/script_import/pdf_page_view.dart:286-290 — OCR highlight lookup logs the user's `highlightText` target verbatim (up to 40 chars) into the persistent debug log file (`DebugLogService.instance.log` → `_appendSync` → `debug_log.txt` in Documents, append-only, no rotation) — script dialogue text is user content that persists across sessions and is exported via `export()`; consequence: user-typed script text accumulates in an unrotated on-disk log with no scrubbing; smallest safe fix: log only counts/lengths (`rects.length`, `lines.length`) and drop the quoted target substring.
- [medium] lib/features/script_import/pdf_page_view.dart:328 — `_fail` logs the render error message (which embeds the PDF path and page number) plus optional error/stack into the same unrotated persistent debug log on every failed render; consequence: repeated failures (e.g. a missing file) append unbounded lines to `debug_log.txt` with no rotation cap; smallest safe fix: rate-limit or cap `_fail` logging, or route through a rotating sink.
- [low] lib/features/script_import/pdf_page_view.dart:296-304 — OCR failure path logs the exception via `logError` but the catch clause discards the stack trace (only `e` passed, no `stack`), unlike `_fail` which forwards both; consequence: native OCR failures are diagnosed without a stack, slowing triage; smallest safe fix: catch `(e, stack)` and pass the stack to `logError`.
- [low] lib/features/script_import/pdf_page_view.dart:240-242 — `_renderPage`'s catch clause also drops the stack when calling `_fail` (`_fail(generation, msg, e, stack)` is only used at 241 — actually stack IS passed here; the real gap is `_locateHighlight`'s catch at 296) — no independent finding; see previous line.
- [medium] lib/features/script_import/pdf_page_view.dart:109-120 — `dispose` defers native teardown via `addPostFrameCallback` after clearing `_pageCache` and nulling `_doc`, but if the widget is disposed during the same frame the post-frame callback runs with no re-entrancy guard and the deferred `doc?.dispose()`/`image.dispose()` operate on handles already handed to `_renderPage`'s in-flight `await` (line 225-232 caches into `_pageCache` after dispose); consequence: a disposed widget's render completion can re-populate `_pageCache` and cache an image nobody owns, leaking the multi-MB RGBA buffer; smallest safe fix: guard `_cachePut`/`_renderPage` continuation with `mounted` before caching (the `if (mounted)` at 226 only guards the stale branch, not the success path at 238).
- [medium] lib/features/script_import/pdf_page_view.dart:234-238 — success path calls `setState` and `_cachePut` without re-checking `mounted` after the `await pdfImage.createImage()` gap; consequence: if the widget was disposed mid-render, `setState` on an unmounted state throws and the decoded image is cached into a dead instance's map, leaking it; smallest safe fix: `if (!mounted) { image.dispose(); return; }` before the `setState`/`_cachePut` block.
- [medium] lib/features/script_import/pdf_page_view.dart:261-262 — `_locateHighlight` reads `_pageOcrCache[page]` then awaits `PaddleOcrChannel.ocrPage` without a `mounted` re-check before the first `setState` at 256; consequence: a dispose during the OCR await makes `setState` throw on an unmounted state; smallest safe fix: add `if (!mounted) return;` after the await and before each `setState`.
- [low] lib/features/script_import/pdf_page_view.dart:163,173,318 — `_txController.value` is mutated outside `setState` while `_loading`/`_renderError` transitions run; consequence: none observable in isolation (transform reset is idempotent), but if the controller is disposed at 114 before a late `_renderPage` continuation writes it, `value` on a disposed controller throws; smallest safe fix: guard transform writes with `mounted`.
- [low] lib/features/script_import/pdf_page_view.dart:141-147 — LRU eviction iterates `_pageCache.keys` filtered by `!identical(_pageCache[k], _pageImage)`, but `_pageImage` may be null so every entry is "evictable" including the one just put and about to be shown; consequence: under churn the just-decoded on-screen image can be evicted and disposed while still referenced by `RawImage` at 417-420, rendering a blank/crashed page; smallest safe fix: also exclude the key being inserted (`page`) from eviction in this call.
- [low] lib/features/script_import/pdf_page_view.dart:351,362 — prev/next buttons compute enablement from `_currentPage`/`_totalPages` but `_totalPages` is only set inside `_renderPage` after the doc opens (line 190); consequence: before the first render completes the buttons read `Page 1` with `_totalPages == 0`, so next is disabled even when more pages exist — a transient wrong-state, not a crash; smallest safe fix: initialize `_totalPages` from the doc when opened, or disable both until first render.
- [low] lib/features/script_import/pdf_page_view.dart:203-207 — `MediaQuery.maybeOf(context)` is called inside the async `_renderPage` after awaits; consequence: using `context` across async gaps after `dispose` can throw ("Looking up a deactivated widget's ancestor"); smallest safe fix: capture media values before the first await.
- [info] lib/features/script_import/pdf_page_view.dart:286-290 — verify the DebugLogService sink (`debug_log.txt` under app Documents, per context file lines 108-109, 187-188) is not synced/uploaded anywhere; it is append-only with a 200KB truncate in `_loadFromDisk` but no rotation on the write path, so unbounded growth between loads is possible.
- [medium] lib/features/script_import/script_import_screen.dart:588-592 — `_acceptScript`'s failure toast embeds the raw exception `$e` into a user-visible SnackBar; consequence: internal error text (potentially containing file paths or stack fragments) is surfaced to the end user; smallest safe fix: show a generic message and log the detail via `DebugLogService`.
- [medium] lib/features/script_import/script_import_screen.dart:741 — `_pickPdfFile`'s outer catch sets `_error = 'Failed to import PDF: $e'`, rendering the raw exception into the UI error card; consequence: same internal-detail leak to the user surface; smallest safe fix: generic user message, detail only in logs.
- [medium] lib/features/script_import/script_import_screen.dart:778 — `_pickMarkdownFile`'s catch sets `_error = 'Failed to import markdown: $e'` — same raw-exception-to-UI class; smallest safe fix: generic message.
- [medium] lib/features/script_import/script_import_screen.dart:815 — `_pickTextFile`'s catch sets `_error = 'Failed to import script: $e'` — same raw-exception-to-UI class; smallest safe fix: generic message.
- [low] lib/features/script_import/script_import_screen.dart:719-730 — the `UnimplementedError` branch sets `_error` with a multi-line pubspec instruction but never logs via `DebugLogService` before the `logError` at 721-722 does run — no independent finding; the branch is logged, so no defect.
- [medium] lib/features/script_import/script_import_screen.dart:603-637 — `_commitStagedPdf` copies the staged PDF to `Documents/scripts/{productionId}.pdf` via `copy` + `rename` but never checks `mounted`/production identity after the awaits; if the user navigates away and `currentProductionProvider` changed mid-await, the copy lands under the OLD production id and `copyWith(scriptPath: destPath)` is applied to a stale production snapshot pushed into both providers (634-636); consequence: wrong script path recorded on the now-current production, page viewer opens the wrong document; smallest safe fix: re-read `currentProductionProvider` after the awaits and bail if the id changed, or capture the id before the copy and verify.
- [medium] lib/features/script_import/script_import_screen.dart:621-622 — `File(staged).copy('$destPath.incoming')` then `incoming.rename(destPath)`: if `rename` throws (cross-device link, dest locked), the `.incoming` file is left behind and the previous script's PDF is untouched, but `_pdfPendingCommit` stays true so a later `_acceptScript` retry re-copies — acceptable; however if `copy` partially succeeds and `rename` succeeds while the staged source delete at 624 fails, the catch at 625-629 only logs — no data loss; no independent finding beyond the log-only cleanup noted at 627-628.
- [low] lib/features/script_import/script_import_screen.dart:615-618 — directory creation and copy run on the UI isolate via synchronous `existsSync`/`createSync`; consequence: brief jank on large directories; smallest safe fix: use async `Directory.create`.
- [low] lib/features/script_import/script_import_screen.dart:181-201 — `_countsFor` memoizes on `script.lines` identity via `_previewCounts`, but `_openReview` (437-452) replaces `_preview` with a NEW `ParsedScript` whose `lines` is `result.lines`; the memo correctly misses, yet `_buildReviewBanner` (390) destructures `(_, reviewCount, notScriptCount)` and the unused first element shadows nothing — no defect; the memo key is the new list so counts recompute correctly.
- [low] lib/features/script_import/script_import_screen.dart:436-452 — after `Navigator.push` returns, `_openReview` mutates `_preview` without re-deriving `_previewCounts`; `_countsFor` re-runs a full O(n) scan on the next build — acceptable for typical script sizes; no finding.
- [low] lib/features/script_import/script_import_screen.dart:298 — `_saving ? null : () => _acceptScript(script)` guards double-tap, but `_acceptScript` sets `_saving = true` via `setState` at 571 before the first await; if `persistScript` throws synchronously the `finally` at 593-596 resets it — correct; no finding.
- [low] lib/features/script_import/script_import_screen.dart:665,734 — `WakelockPlus.enable()` is called before the OCR await and `disable()` in `finally`, but the `finally` belongs to the inner `try` at 666-735 whose `catch` at 736-744 is OUTSIDE it: an exception thrown between 665 and 666 (none exists) or by `WakelockPlus.enable()` itself leaves the wakelock held; consequence: screen stays awake after a failed import; smallest safe fix: wrap `enable()` in the same try/finally, or disable in the outer `finally` too.
- [low] lib/features/script_import/script_import_screen.dart:684-702 — the staging-copy fallback sets `_importedPdfPath = filePath` (the picker's own temp file) with `_pdfPendingCommit = true`; `_commitStagedPdf` will then copy the PICKER's temp file into Documents and delete it at 624 — if the picker cleans its temp file on its own schedule the commit copy can fail after accept; consequence: accept-time failure with only a log; smallest safe fix: only mark `_pdfPendingCommit` when the staged copy actually succeeded (leave it false in the fallback and let `_commitStagedPdf` no-op).
- [medium] lib/features/script_import/script_import_screen.dart:697-701 — in the staging-failure fallback the code logs via `logError` but still flips `_pdfPendingCommit = true` on a path where the staged file may not exist; combined with the previous finding this is the same root cause (pending-commit flag set on an unverified staged file) — one finding, see 684-702 line above.
- [low] lib/features/script_import/script_import_screen.dart:143-170 — the error card renders `_error!` raw; already covered by the raw-exception-to-UI findings above (741/778/815) — same class, one root cause each per picker; no additional line.
- [low] lib/features/script_import/script_import_screen.dart:366-369 — `presetId` is derived from `locale` with a hard-coded ternary; if `production.locale` is neither 'en-GB' nor 'en-US' the label falls back to the raw locale (328) but the preset silently becomes 'modern_american'; consequence: wrong voice preset for an unexpected locale; smallest safe fix: derive preset from a map with an explicit default and log the unknown locale.
- [low] lib/features/script_import/script_import_screen.dart:371-378 — cloud locale/preset sync is gated on `supa.isSignedIn` with no else-branch logging; consequence: silent divergence between local and cloud state when signed out — acceptable offline behavior; no finding.
- [low] lib/features/script_import/script_import_screen.dart:427-435 — `_openReview` reads `_preview` and `_importedPdfPath` at push time; if `_importedPdfPath` is null the review screen gets a null pdfPath (page viewer then cannot render) — the review screen is out of scope here; assumption noted, no finding without seeing `ocr_review_screen.dart`.
- [low] lib/features/script_import/script_import_screen.dart:554-565 — `_colorForIndex` uses `index.abs() % colors.length`; `int.abs()` of `int.minValue` overflows in Dart (stays negative) so `%` can yield a negative index and throw RangeError; consequence: a pathological `character.hashCode == minInt` crashes the preview build; smallest safe fix: use `(index & 0x7fffffff) % colors.length` or `index.toUnsigned(31)`.
- [low] lib/features/script_import/script_import_screen.dart:522 — `charIndex = line.character.hashCode.abs()` feeds `_colorForIndex` with the same `abs()` overflow edge as 554-565 — same root cause, one finding above covers both call sites.
- [low] lib/features/script_import/pdf_page_view.dart:554-565 — n/a (that symbol lives in script_import_screen.dart; pdf_page_view has no `_colorForIndex`) — no finding.
- [low] lib/features/script_import/script_import_screen.dart:262 — `script.lines.take(30)` previews only 30 lines with a "... and N more" note; consequence: none (intentional preview cap); no finding.
- [low] lib/features/script_import/script_import_screen.dart:267 — `'... and ${script.lines.length - 30} more lines'` computes a negative count if `length < 30`, but the `if (script.lines.length > 30)` guard at 263 prevents it; no finding.
- [low] lib/features/script_import/script_import_screen.dart:471-472 — `counts.entries.toList()..sort((a,b) => b.value.compareTo(a.value))` sorts descending by count only; ties keep insertion order — deterministic; no finding.
- [low] lib/features/script_import/script_import_screen.dart:459 — `genders` map built from `existing` keyed by name; a character removed in review loses gender and falls to `ScriptParser.inferGender` (479) — intended per comment; no finding.
- [low] lib/features/script_import/script_import_screen.dart:362-365 — `onSelectionChanged` writes `currentProductionProvider.notifier.state` directly instead of via the productions notifier; both are updated (364-365) so providers stay in sync; no finding.
- [low] lib/features/script_import/script_import_screen.dart:50-51 — `context.pop()` in the app-bar leading button; if the screen was pushed without a route to pop, `pop()` may throw — assumption: go_router always has a fallback route here; no finding without seeing the router config.
- [low] lib/features/script_import/script_import_screen.dart:583 — `context.push('/production')` after accept; if `persistScript` succeeded but the route push happens after an await without `mounted` re-check (581-583 has one at 582), correct; no finding.
- [low] lib/features/script_import/script_import_screen.dart:574,582,587 — `mounted` re-checks present after each await in `_acceptScript`; correct; no finding.
- [low] lib/features/script_import/script_import_screen.dart:646-650 — `result.files.first.path` null-checked at 649; correct; no finding.
- [low] lib/features/script_import/script_import_screen.dart:754-757, 791-794 — same null-check pattern in markdown/text pickers; correct; no finding.
- [low] lib/features/script_import/script_import_screen.dart:669-670 — `service.importFromPdf(filePath)` result null-checked via `if (!mounted) return;` only — the script value itself is assumed non-null; assumption: `scriptImportServiceProvider` never returns null; no finding without seeing the service.
- [low] lib/features/script_import/script_import_screen.dart:711-718 — `service.lastImportFailedPages` read after the import; if the service resets it on a new import the toast reflects the current run — assumption; no finding.
- [low] lib/features/script_import/script_import_screen.dart:438-451 — `_openReview` constructs `ParsedScript(... rawText: script.rawText)` but drops `title` reuse is present (439); correct; no finding.
- [low] lib/features/script_import/script_import_screen.dart:444-445 — `remapScenes(script.scenes, script.lines, result.lines)` matches context-file semantics (script_models.dart:330-356); correct; no finding.
- [low] lib/features/script_import/script_import_screen.dart:449 — `_recountCharacters(script.characters, result.lines)` matches context-file `ScriptCharacter` shape; correct; no finding.
- [low] lib/features/script_import/pdf_page_view.dart:337 — `_goToPage` guards `page > _totalPages`; if `_totalPages` is 0 (pre-render) both buttons are inert — covered by the 351/362 finding above; same root cause, no extra line.
- [low] lib/features/script_import/pdf_page_view.dart:182-187 — doc reuse keyed on `_docPath != widget.pdfPath`; if the same path is re-picked after the file changed on disk, the stale handle renders old content; consequence: stale page image after an in-place file update; smallest safe fix: also compare a file mtime/size, or always reopen on widget.pdfPath change (already done) plus a content fingerprint.
- [low] lib/features/script_import/pdf_page_view.dart:184 — `_evictAllCached()` disposes all cached images including `_pageImage`'s referent while `RawImage` at 417 may still hold it during a rebuild; consequence: transient blank frame until the new render lands; acceptable; no finding.
- [low] lib/features/script_import/pdf_page_view.dart:176-179 — `Pdfrx.getCacheDirectory ??=` assigns a closure to a global setter-like field each render; if `getCacheDirectory` is a non-nullable function type the `??=` is a one-time assignment — idempotent; no finding.
- [low] lib/features/script_import/pdf_page_view.dart:191-196 — page index bounds checked before `doc.pages[pageIdx]`; correct; no finding.
- [low] lib/features/script_import/pdf_page_view.dart:209-212 — `pdfPage.render(fullWidth/fullHeight)` scaled by `targetW / pdfPage.width`; if `pdfPage.width` is 0 the scale divides by zero producing NaN dimensions; consequence: render call with NaN, likely exception caught by 240; smallest safe fix: guard `pdfPage.width <= 0`.
- [low] lib/features/script_import/pdf_page_view.dart:225-232 — stale-branch caching when `!mounted` disposes the image (229) but when `mounted` and stale it caches into `_pageCache` (227) — a disposed widget's map grows unbounded across re-targets; consequence: minor leak bounded by `_maxCachedPages` eviction which still runs; no finding beyond the mounted-guard finding above (same root cause).
- [low] lib/features/script_import/pdf_page_view.dart:295 — `_scrollToHighlight(rects.first)` called without re-checking `_pageImage` non-null inside; `_scrollToHighlight` itself null-guards at 311-312; correct; no finding.
- [low] lib/features/script_import/pdf_page_view.dart:310-318 — `_scrollToHighlight` uses `_lastViewSize` captured during the last build; if the layout changed since, the pan targets a stale viewport; consequence: highlight may land off-center after a rotation; smallest safe fix: recompute from current constraints or reset `_lastViewSize` on layout change.
- [low] lib/features/script_import/pdf_page_view.dart:398 — `_lastViewSize` is written inside the `LayoutBuilder` builder during build — writing state during build is legal here (no setState), but if two builds race the value is the latest; acceptable; no finding.
- [low] lib/features/script_import/pdf_page_view.dart:424 — `for (final r in _highlightRects ?? const <Rect>[])` iterates rects even when `_locating` is true (stale rects from a previous page could flash); consequence: brief wrong highlight during re-locate; smallest safe fix: gate the rect loop on `!_locating`.
- [low] lib/features/script_import/pdf_page_view.dart:456-458 — the "Couldn't locate" chip condition requires `_highlightRects?.isEmpty ?? false` — when `_highlightRects` is null (locating cleared it at 258) the chip hides even though the lookup finished with no match on the `lines == null` path (273-281 sets `_highlightRects = const []` so chip shows) — consistent; no finding.
- [low] lib/features/script_import/pdf_page_view.dart:448-455 — `_locating` chip is Positioned at top with full width; overlaps the page header row; cosmetic; no finding.
- [low] lib/features/script_import/pdf_page_view.dart:507-533 — `_buildError` retry button calls `_renderPage` directly; if the doc handle is disposed (post-dispose deferred teardown) `_renderPage` reopens — correct; no finding.
- [low] lib/features/script_import/pdf_page_view.dart:526 — retry `onPressed: _renderPage` passes the method tearoff as the async action; fine; no finding.
- [low] lib/features/script_import/pdf_page_view.dart:124-130 — `_evictAllCached` disposes images then clears the map but does NOT dispose `_doc`; caller at 183-186 disposes `_doc` separately — correct pairing; no finding.
- [low] lib/features/script_import/pdf_page_view.dart:132-148 — `_cachePut` evicts while `_pageCache.length <= _maxCachedPages` breaks AFTER the first removal candidate check; the `break` at 145 exits before removing when length is already ≤ max — correct LRU bound; no finding.
- [low] lib/features/script_import/pdf_page_view.dart:133-137 — replaced-image dispose guards `identical(replaced, image)` to avoid disposing the just-inserted image; correct; no finding.
- [low] lib/features/script_import/pdf_page_view.dart:150-152 — `_renderPage` generation increment before any await; stale checks at 189/225/329 use it; correct pattern; no finding.
- [low] lib/features/script_import/pdf_page_view.dart:189 — `if (_isStale(generation)) return;` after doc open but BEFORE `_totalPages` assignment; a stale return leaves `_totalPages` stale from a previous doc — covered by the 351/362 finding; same root cause; no extra line.
- [low] lib/features/script_import/pdf_page_view.dart:336-340 — `_goToPage` does not reset `_highlightRects` when paging; stale rects from the previous page can render on the new page until `_locateHighlight` clears them (253/269); consequence: transient wrong highlight; smallest safe fix: clear `_highlightRects` in `_goToPage`.
- [low] lib/features/script_import/pdf_page_view.dart:252-255 — `_locateHighlight` early-returns when `page != widget.pageNumber` and clears rects only `if (mounted)`; if unmounted the rects stay — harmless on a dead widget; no finding.
- [low] lib/features/script_import/pdf_page_view.dart:251 — `target.trim().isEmpty` early return leaves `_highlightRects` as-is (possibly stale from a prior page); consequence: stale highlight shown for a blank target; smallest safe fix: clear rects on the empty-target path too.
- [low] lib/features/script_import/pdf_page_view.dart:321 — `_isStale` returns true when `!mounted`, so `_fail` at 329-333 skips setState when unmounted — correct; no finding.
- [low] lib/features/script_import/pdf_page_view.dart:330-333 — `_fail` setState guarded by `_isStale` which includes `!mounted`; correct; no finding.
- [low] lib/features/script_import/pdf_page_view.dart:115-120 — post-frame teardown iterates `images` captured before the callback; if `_renderPage`'s stale branch cached a new image into `_pageCache` between dispose and the post-frame callback, that image is NOT in `images` and never disposed — leak; smallest safe fix: in the post-frame callback, dispose from the live `_pageCache` (already cleared at 110, so re-dispose via a snapshot taken inside the callback) — related to the mounted-guard finding; same root cause family, no extra line.
- [low] lib/features/script_import/pdf_page_view.dart:42-47 — `_pageImage`, `_loading`, `_renderError` are instance fields mutated from async continuations; standard pattern; no finding.
- [low] lib/features/script_import/pdf_page_view.dart:59-60 — `_pageCache` is a plain `Map` used as LRU by insertion order; `remove`+reinsert at 155-157 refreshes position; correct; no finding.
- [low] lib/features/script_import/pdf_page_view.dart:69 — `_pageOcrCache` unbounded per page; bounded by page count; no finding.
- [low] lib/features/script_import/pdf_page_view.dart:78 — `_renderGeneration` int overflow practically impossible; no finding.
- [low] lib/features/script_import/pdf_page_view.dart:344-473 — build method constructs the whole column each frame; heavy but static; no finding.
- [low] lib/features/script_import/pdf_page_view.dart:417-420 — `RawImage(image: _pageImage)` with `fit: BoxFit.fill` inside a SizedBox sized to pageW/pageH; correct; no finding.
- [low] lib/features/script_import/pdf_page_view.dart:426-429 — highlight rect offsets (-4/-
- [medium] lib/features/settings/ai_models_screen.dart:36 — `Platform.isAndroid` is not a valid member of `dart:io` Platform (it's `isAndroid` on `default_target_platform`/`Platform` in Flutter via `Platform.isAndroid`? Actually `dart:io` Platform does have `isAndroid`) — wait, `dart:io` `Platform.isAndroid` exists; the real defect is `_checkOnnxStatus`/`_downloadOnnxKokoro`/`_deleteOnnxKokoro`/`_download`/`_delete` call `setState`/`ScaffoldMessenger.of(context)` after `await` guarded only by `mounted` on the State, but `_deleteOnnxKokoro` (line 109-118) and `_download` (line 424-436) use `mounted` correctly — the actual defect: `_downloadOnnxKokoro` line 89 and `_deleteOnnxKokoro` line 114 call `ScaffoldMessenger.of(context)` inside `if (mounted)` but `_onStateChanged` (line 45-47) calls `setState` unconditionally guarded by `mounted` — fine. The genuine defect: line 246 `states` map keyed by `AiModel` object relies on `AiModel` having proper `==`/`hashCode`; if `AiModel` is a plain class without equality override, duplicate model entries would produce separate map keys — assumption: `AiModel` equality is identity, which is fine for distinct models. No finding.
- [medium] lib/features/settings/ai_models_screen.dart:263-268 — progress weighting divides by `totalBytes` computed from `models` list, but `progress` accumulates `part * m.sizeBytes / totalBytes` where `part` for a `downloaded` model is 1.0 and for others is `s.progress` (0–1); if `totalBytes` is 0 (all models have `sizeBytes == 0`), this is a division-by-zero producing NaN — consequence: `LinearProgressIndicator(value: NaN)` throws/clamps unpredictably and the tile shows a broken progress bar — smallest safe fix: guard `totalBytes > 0` before the fold, falling back to unweighted progress.
- [medium] lib/features/settings/ai_models_screen.dart:246-248 — `allDone` is computed as `states.values.every(...)` over an empty `states` map when `models` is empty (no `live_asr` models registered); `every` on an empty iterable returns `true`, so the tile would render "Installed" with a delete menu for a model group that has no files — consequence: user taps Delete and `_service.delete` is never called (loop over empty `models`), leaving UI state inconsistent — smallest safe fix: early-return a disabled/hidden tile when `models.isEmpty`.
- [low] lib/features/settings/ai_models_screen.dart:424-435 — `_download` awaits `_service.download(model)` then reads state and shows a toast only on `error` status; a successful download produces no feedback and, more importantly, if `download` throws, the exception is unhandled (no try/catch), crashing the isolate on unhandled async error — consequence: a network failure during a per-model download surfaces as an unhandled exception rather than the error-state UI that `_buildTrailing` expects — smallest safe fix: wrap in try/catch and set the error state or show a failure toast.
- [low] lib/features/settings/ai_models_screen.dart:109-118 — `_deleteOnnxKokoro` awaits `clearCache()` with no try/catch; a storage failure throws an unhandled async exception and the "deleted" toast never shows — consequence: user believes deletion succeeded only if no crash; on failure the app reports an unhandled error — smallest safe fix: wrap in try/catch and surface failure via `_onnxError`/toast.
- [medium] lib/features/settings/debug_log_screen.dart:32-42 — the periodic 2-second timer calls `setState(() {})` unconditionally when `entryCount` changed, but `_lastEntryCount` is initialized to `-1` and never reset on filter change; switching `_filter` via `_filterChip` (line 301-313) does not reset `_lastEntryCount`, so after a filter change the timer can skip a needed rebuild if the total count is unchanged while the filtered list differs — consequence: stale filtered list shown for up to 2s (minor) — smallest safe fix: reset `_lastEntryCount = -1` in the `onSelected` handler of `_filterChip`.
- [medium] lib/features/settings/debug_log_screen.dart:75-81 — the "Send to developer" action inserts `user_email` and `user_id` into a Supabase `debug_reports` table with no RLS/tenant scoping visible here; the log content (`text`) is the full local debug log which may contain PII (emails, tokens) from other log categories — consequence: sensitive diagnostic data uploaded to a shared table; if RLS is absent, any authenticated user of the same project could read other users' reports — smallest safe fix: verify/enable RLS on `debug_reports` scoped to `auth.uid()` and scrub PII from log lines before insert. (Assumption: table policy not shown in this batch.)
- [medium] lib/features/settings/debug_log_screen.dart:136-140 — `FirebaseCrashlytics.instance.recordError` is called with a fabricated `Exception('Crashlytics test non-fatal')` and `StackTrace.current`; this is test-only debug tooling reachable from the shipped debug screen — consequence: in production builds a user tapping this menu item records a fake non-fatal error into Crashlytics, polluting crash analytics with non-crash noise — smallest safe fix: gate these menu items behind `kDebugMode`/a debug flag rather than `firebaseAvailable`.
- [medium] lib/features/settings/debug_log_screen.dart:159-165 — the `test_fatal` action throws an uncaught `Exception('Crashlytics test fatal error')` from a button handler; in a release build this crashes the app (unhandled zone exception) — consequence: any user (or accidental tap) can crash the app from the debug screen — smallest safe fix: gate behind `kDebugMode` or remove from release builds.
- [medium] lib/features/settings/debug_log_screen.dart:166-171 — `test_native_crash` calls `FirebaseCrashlytics.instance.crash()` which deliberately SIGABRTs the process — consequence: guaranteed app crash reachable from a UI menu item in any build where firebase is available — smallest safe fix: gate behind `kDebugMode`.
- [low] lib/features/settings/debug_log_screen.dart:152-153 — `setCustomKey('test_key', 'test_value_${DateTime.now().millisecondsSinceEpoch}')` sets a Crashlytics custom key with a timestamp value; harmless but pollutes crash reports with a meaningless key — consequence: noise in crash diagnostics — smallest safe fix: remove or gate behind debug flag.
- [low] lib/features/settings/debug_log_screen.dart:44 — `_log.getMemoryUsage()` is called in `initState` without awaiting/using the result; the returned Future is discarded — consequence: unhandled async error if the method-channel throws outside its internal try/catch (it does catch internally, so this is benign; but a discarded Future still surfaces as an unhandled async error if any code path outside the try throws, e.g. `invokeMapMethod` throwing `MissingPluginException` is caught) — smallest safe fix: `unawaited()` it explicitly or drop the call. (Borderline; the internal catch makes this mostly cosmetic.)
- [medium] lib/features/settings/debug_log_screen.dart:223-232 — the FAB "Check memory now" calls `_log.getMemoryUsage()` then logs a message using `mem['physicalFootprintMB']`/`mem['availableMemoryMB']`; `getMemoryUsage` returns `{}` on failure (line 221 of the context service), so the log line reads "nullMB used, nullMB available" — wait, the map access `mem['physicalFootprintMB']` on an empty `Map<String,int>` returns `null`, and string interpolation of `null` yields "null" — consequence: misleading "nullMB used" log entry — smallest safe fix: guard `mem.isNotEmpty` before logging (as `_logMemory` in the service does).
- [low] lib/features/settings/debug_log_screen.dart:107 — `Share.share(text, subject: 'CastCircle $label')` shares the full log text; if the log contains PII (emails from other categories), the user is sharing it externally — consequence: potential PII disclosure via share sheet, though user-initiated — smallest safe fix: none required beyond awareness; optionally scrub. (User-initiated, so low.)
- [low] lib/features/settings/debug_log_screen.dart:116 — `Clipboard.setData(ClipboardData(text: text))` copies the full log including any PII to the clipboard; other apps on the device can read the clipboard — consequence: potential PII exposure to other apps — smallest safe fix: none strictly required; note only. (User-initiated, low.)
- [medium] lib/features/settings/debug_log_screen.dart:56-58 — `build` reads `_log.lastPhysicalMB`/`lastAvailableMB` but `_buildMemoryBar` is only refreshed when `setState` fires (timer or actions); the memory bar shows stale values until some other state change — consequence: memory bar can show outdated figures indefinitely if no timer tick changes the entry count — smallest safe fix: also refresh on memory-stat change (e.g., have the service notify listeners) or accept staleness. (Minor UI staleness; medium is generous — call it low.) — revise: [low].
- [low] lib/features/settings/debug_log_screen.dart:216 — `itemBuilder` computes `entries[entries.length - 1 - index]` for a `reverse: true` ListView; this is correct for reverse lists, but if `entries` is empty the builder is never called (guarded by the `entries.isEmpty` branch) — no finding.
- [low] lib/features/settings/debug_log_screen.dart:295 — `...LogCategory.values.map((c) => _filterChip(c.tag, c))` includes the `error` category chip; selecting it filters to error entries only — no defect.
- [medium] lib/features/settings/debug_log_screen.dart:133,147,159,166 — all four test actions are gated on `firebaseAvailable` (imported from `main.dart`), not on debug mode; combined with the findings above, the whole PopupMenuButton block (lines 131-191) is crash-inducing UI reachable in release builds — this is the same root cause as the individual findings above; file ONE finding at the cause (the missing debug gate) covering lines 133-191 — see consolidated finding below.

Consolidated: the missing `kDebugMode` gate on the Crashlytics test menu (lines 131-191) is one root cause producing four reachable harms (fake non-fatal records, fatal throw, native crash, custom-key noise). Report as one finding at the cause.

Also consolidated: the `_lastEntryCount` staleness and the memory-bar staleness are separate minor issues; keep the timer one, drop the memory-bar one as it's really the same "no listener" shape — actually they're different mechanisms; keep both but the memory-bar one is low.

Let me also re-check the ai_models_screen for the same defect classes:
- `_downloadOnnxKokoro` line 63: `await ModelManager.instance.downloadKokoro(...)` inside try/catch — good.
- `_downloadOnnxKokoro` line 81: `await TtsService.instance.tryLoadKokoro()` inside the same try — good.
- `_deleteOnnxKokoro` line 110: no try/catch — flagged.
- `_download` line 425: no try/catch — flagged.
- `_delete` line 439: `await _service.delete(model.id)` no try/catch — same shape as `_download`; flag it too (same class, separate call site but same root cause pattern "unawaited service call without error handling in async UI handler"). Per the rules, one finding per root cause when a shared helper reaches several call sites — here the root cause is "async UI handlers call service methods without try/catch", reaching `_download` (425), `_delete` (439), `_deleteOnnxKokoro` (110), and the live-ASR delete loop (333-335). File ONE finding listing the sites.

Wait — the live-ASR delete at 331-336: `onSelected: (value) async { if (value == 'delete') { for (...) await _service.delete(m.id); } }` — no try/catch, and no mounted guard after the awaits before... actually there's no UI update after, so no context-use-after-dispose issue, but the unhandled-exception shape applies. Include in the consolidated finding.

Also `_downloadOnnxKokoro`'s PopupMenuButton at 221-229: `onSelected` calls `_deleteOnnxKokoro()` — covered.

One more: `_buildLiveAsrTile` line 331-336 delete loop deletes models sequentially; if one fails mid-loop, later ones still attempt — acceptable.

`_buildOnnxKokoroTile` line 224: `if (value == 'delete') _deleteOnnxKokoro();` — the returned Future is discarded; same unhandled-async shape — include in consolidated finding.

`_downloadOnnxKokoro` is invoked from `onPressed: _downloadOnnxKokoro` (line 233) — has internal try/catch, fine.

`_service.downloadLiveAsr()` at line 345 — `onPressed: () => _service.downloadLiveAsr()` — discarded Future, no try/catch; if it throws, unhandled async error. Include in consolidated finding.

`_service.download(model)` at 425 — covered.

So the consolidated finding: async UI handlers across ai_models_screen (lines 110, 233→54-106 ok, 224, 331-336, 345, 425, 439) call service methods without try/catch; the ones lacking internal handling are 110 (`clearCache`), 224 (`_deleteOnnxKokoro` — which itself lacks handling), 331-336 (delete loop), 345 (`downloadLiveAsr`), 425 (`download`), 439 (`delete`). Root cause: unhandled async exceptions from service calls in UI callbacks. One finding.

Now the NaN/totalBytes finding: `totalBytes` is `models.fold<int>(0, (a,m) => a + m.sizeBytes)`. If any model has `sizeBytes == 0` for all, totalBytes == 0 → division by zero on double arithmetic: `m.sizeBytes / totalBytes` where both are int → int division? `m.sizeBytes` is presumably int, `totalBytes` int → `part * m.sizeBytes / totalBytes` — `part` is double, so `part * m.sizeBytes` is double, `/ totalBytes` int → double division by 0.0 → Infinity/NaN depending on numerator. If part is 0.0 and totalBytes is 0: 0.0/0 → NaN. LinearProgressIndicator with NaN value — Flutter asserts value must be between 0 and 1 (clamps? Actually `LinearProgressIndicator` asserts `!value.isNaN`? In debug it may throw). This is a real edge but requires all four live_asr models to have sizeBytes 0 — unlikely. Downgrade to low? The rule: reachability — it's a UI edge case on-device, authenticated user's own device. I'll keep it [low] with the guard fix. Actually the original severity I wrote was medium; per guidance "an authenticated user against their own data = medium" but this is a UI glitch, not data. [low] is more defensible. Let me set [low].

The empty-models `allDone` finding: also requires `availableModels` to lack live_asr entries — speculative. The code explicitly filters `m.subdir == 'live_asr'`; if the constant list has them (implied by the tile's existence), models is non-empty. This is speculative — drop it? The rule says report only what the code exhibits. The code exhibits `every()` on a possibly-empty map returning true. But whether it's empty depends on `ModelDownloadService.availableModels`, which I cannot see. State the assumption. I'll keep it [low] with the assumption stated.

`_lastEntryCount` filter staleness: real, [low] (UI staleness only, 2s max). Actually the consequence: after changing filter, if total entry count unchanged, no rebuild → the list shown is the OLD filtered list (wrong category) until a new entry arrives or an action triggers setState. That's a visible wrong-list bug. Reachability: any user of the debug screen, own device. [low]–[medium]. It's a correctness defect with no data risk → [low]. Hmm, but "wrong data displayed" — I'll say [low].

PII upload finding: the code exhibits inserting user_email/user_id and full log content into `debug_reports`. Whether RLS exists is unseen. The log content itself is user's own data being uploaded about themselves — that's normal crash-report behavior. The risk is the shared table. State assumption. [medium] per "authenticated user reaching another tenant's data = high" — but this is *writing* own data to a possibly-unscoped table; reading others' requires RLS absence. I'll phrase: if RLS is absent, any authenticated user can read all reports → that would be high; but I can't confirm. Cap at medium with assumption stated. [medium].

Also `supa.currentUser?.id` and `email ?? 'unknown'` — if not logged in, inserts with null user_id; fine.

`'user_id': supa.currentUser?.id` — column name `user_id`; fine.

One more from the debug screen: line 68-70 — `entriesForCategory(_filter)` recomputed inside each action handler; fine.

Line 88: `SnackBar(content: Text('Sent $label log ...'))` — fine.

Line 91-97: catch shows failure — good.

Line 127-129: `await _log.clear(); setState(() {});` — no mounted guard after await; if the screen was disposed during the await, `setState` throws. Same shape at line 85-90 (guarded by `if (mounted)` — good) and line 128 (unguarded). Also line 145, 157, 161, 168 use `if (mounted) setState` — good. Line 128 is the one unguarded instance. Also line 228 `if (mounted) setState(() {})` — good. So: [low] finding at 128 — `setState` after `await _log.clear()` without mounted guard → throws "setState called after dispose" if user navigates away during clear. Small blast radius. [low].

Also ai_models_screen `_delete` line 438-443: `await _service.delete(model.id); if (!mounted) return;` — guarded. `_download` guarded. `_downloadOnnxKokoro` guarded via `if (mounted)`. `_deleteOnnxKokoro` guarded via `if (mounted)`. Good.

`_onStateChanged` line 45-47: `if (mounted) setState(() {})` — guarded.

Timer in debug screen line 35: `if (!mounted) return;` — guarded.

Now the toast context: `showAutoToast` is from lib/core/toast.dart (context file omitted). Not reviewable.

`firebaseAvailable` from main.dart — unseen; assumption it's a bool const.

Now, the observability lens: "Local file logs without rotation" — the debug service (context file, not under review) appends to `debug_log.txt` and truncates at 200KB in `_loadFromDisk` — that's context, not under review. The screens under review don't own that. Skip.

"Errors swallowed to debug/info level" — the screens show errors in UI; fine.

"Sensitive data in span attributes" — the debug upload includes full log content; covered by the PII finding.

"Auth events unlogged" — N/A for these screens.

Let me also check: debug screen line 75-81 insert — `'entry_count': entries.length` — fine.

Line 72-73: `if (!supa.isInitialized) throw Exception('Supabase not initialized');` — caught by outer catch → shows "Send failed" toast. Good.

Line 74: `supa.currentUser?.email ?? 'unknown'` — if not logged in, sends 'unknown' email with null user_id — the report is still uploaded anonymously. Acceptable for debug tooling.

Now severity calibration for the crash-menu finding: reachability — any user of the app (the debug screen route `/kokoro-debug` is pushed from ai_models_screen line 166, so it's reachable in the shipped app). Blast radius: deliberate crash of the whole app + polluted Crashlytics data. That's "any authenticated user" → medium per rules; but "unrecoverable state"? A crash is recoverable (relaunch). Data pollution in Crashlytics is org-wide but not user data loss. I'll keep [medium]. Actually — deliberate `crash()` SIGABRT reachable by any user in release builds is arguably high (guaranteed crash from a menu item). Rules: "unauthenticated internet, or any authenticated user reaching another tenant's data = high; an authenticated user against their own data, or admin-only = medium". A user crashing their own app = their own device → medium. Keep [medium].

The PII/RLS finding: [medium] with assumption.

Final findings list (dedup, one per root cause):

1. [medium] debug_log_screen.dart:131-191 — Crashlytics test menu (fake non-fatal at 136, fatal throw at 164, native crash at 170, custom key at 152) gated only on `firebaseAvailable`, not debug mode — any user in a release build can crash the app from the debug screen and pollute Crashlytics — fix: gate the whole PopupMenuButton behind `kDebugMode`/build flag.

2. [medium] debug_log_screen.dart:75-81 — full local log content (may contain PII/tokens from other categories) plus user_email/user_id inserted into shared `debug_reports` table; RLS unseen — if unscoped, any authenticated user of the project can read all reports — fix: verify/enable RLS scoped to auth.uid() and scrub sensitive categories before upload. Assumption: table policy not in this batch.

3. [medium→low?] ai_models_screen.dart:110,224,331-336,345,425,439 — async UI handlers call service methods (`clearCache`, `delete`, `downloadLiveAsr`, `download`) without try/catch; a storage/network failure surfaces as an unhandled async exception instead of the error-state UI — fix: wrap each in try/catch and surface via state/toast. Severity: on-device, user's own actions → [low]? The consequence is a crash (unhandled exception in async handler → zone error → app may crash or just log). Unhandled async errors in Flutter typically print and can crash in release. I'll say [medium]? Hmm. "an authenticated user against their own data = medium". A failed download crashing the app = own device, medium-ish. But it's a robustness defect, not data. I'll go [low] — no, unhandled exceptions from user-triggered actions in release can terminate the app. I'll pick [medium] for the class since it's user-reachable and can crash. Actually let me be consistent: the crash-menu finding is [medium] for deliberate crash; accidental crash from a failed network call is same blast radius, lower certainty of crash (Flutter's default is to log the error, not crash, unless in a zone that rethrows — in release, unhandled async errors are reported to the zone and typically don't crash). So [low] is more defensible for #3. Go [low].

4. [low] ai_models_screen.dart:263-268 — division by `totalBytes` when all `sizeBytes` are 0 → NaN progress fed to LinearProgressIndicator — fix: guard `totalBytes > 0`.

5. [low] ai_models_screen.dart:246-248 — `every()` over an empty states map (if `availableModels` has no `live_asr` entries — assumption) yields `allDone == true`, rendering an "Installed" tile whose Delete is a no-op — fix: early-return when `models.isEmpty`.

6. [low] debug_log_screen.dart:301-313 + 25 — changing `_filter` doesn't reset `_lastEntryCount`, so the 2s timer can skip the rebuild needed to show the new filtered list when total count is unchanged — fix: reset `_lastEntryCount = -1` on filter change.

7. [low] debug_log_screen.dart:126-129 — `setState` after `await _log.clear()` without mounted guard → "setState after dispose" if the screen is left during the await — fix: add `if (mounted)` guard.

8. [low] debug_log_screen.dart:223-232 — FAB logs `mem['physicalFootprintMB']`/`mem['availableMemoryMB']` without guarding the empty-map failure case → "nullMB used, nullMB available" log entry — fix: guard `mem.isNotEmpty` (mirror `_logMemory` in the service).

9. [low] debug_log_screen.dart:44 — discarded Future from `_log.getMemoryUsage()` in initState — mostly cosmetic since the method catches internally; skip? The rule says don't pad. The internal catch makes it effectively safe. Drop it.

10. Share/clipboard PII — user-initiated, low value; the checklist cares about logging, not user-initiated share. Drop both to avoid padding? The clipboard one is arguably worth an info. I'll drop — user-initiated actions sharing their own data aren't defects.

11. Memory-bar staleness (line 56-58 reading lastPhysicalMB only refreshed on setState) — the timer fires every 2s and calls setState only when count changed; memory stats change without count change → stale bar. This is real but minor UI staleness. [low]. Keep? It's defensible: the bar can show stale memory figures indefinitely. Keep as [low].

Hmm, is #11 the same root cause as #6? No — #6 is filter-change staleness, #11 is memory-stat staleness; both stem from "timer only rebuilds on entry-count change". Could consolidate: the timer's dirty-check (entryCount only) misses memory-stat changes and filter changes. One finding at the timer (lines 32-42) listing both consequences. That's cleaner per "one finding per root cause". Consolidate #6 and #11 into one finding at 32-42: dirty-check uses only entryCount, so (a) filter changes don't reset the baseline → stale filtered list, (b) memory-stat updates never trigger rebuild → stale memory bar. Fix: also compare memory stats / reset baseline on filter change. Good — one finding.

Also check ai_models_screen line 36: `if (Platform.isAndroid) _checkOnnxStatus();` — `Platform.isAndroid` from dart:io is valid. Fine.

Line 145-147: on non-Android, maps all models except live_asr to `_buildModelTile` — includes kokoro tiles? `availableModels` unseen; fine.

Line 166: `context.push('/kokoro-debug')` — route assumed registered elsewhere; fine.

Line 416-
- [medium] lib/features/settings/kokoro_debug_screen.dart:56 — `await for` used on a `Stream` (Directory.list) inside a non-async context is a compile error in Dart; the listing loop never compiles/runs, so the "Model Files on Disk" card can never populate — replace with `await for (final entity in kokoroDir.list(...))` inside an async function (it already is async, so the real defect is the missing `async`/misplaced await semantics) — actually the minimal fix: change line 56 to `await for (final entity in kokoroDir.list(recursive: false)) {` (it is already that) — the true defect is that `files.add` results are discarded when `mounted` is false at line 76, silently dropping the listing — smallest safe fix: compute `files` before the `mounted` check and still assign `_modelFiles` even when unmounted, or guard only the `setState` not the assignment.
- [low] lib/features/settings/kokoro_debug_screen.dart:90 — `_statusLog` grows unboundedly (every action prepends a timestamped line, no cap), and the monospace Text at 340-349 renders the whole string — long debug sessions build an ever-larger string that slows builds and can hit text-clipping — cap the log (e.g. keep last ~50 lines) before assigning `_statusLog`.
- [low] lib/features/settings/kokoro_debug_screen.dart:112 — `int.tryParse(_speakerController.text) ?? 0` silently maps invalid speaker input (e.g. "abc", "-5") to speaker 0 and speaks anyway — validate/parse and log an error instead of defaulting to a valid-but-wrong speaker.
- [low] lib/features/settings/kokoro_debug_screen.dart:115 — `text.substring(0, text.length.clamp(0, 60))` truncates the echoed text to 60 chars but the log line claims "..." even when nothing was truncated — cosmetic only; harmless.
- [info] lib/features/settings/kokoro_debug_screen.dart:48 — `_tts.getDebugInfo()` result is trusted as a `Map<String,String>` with no error path; if the service throws, `_loadDebugInfo`'s outer try/catch only wraps the file-listing block (lines 51-74), so an exception from line 48 escapes `_loadDebugInfo` and the future rejects unobserved — wrap line 48 in the same try/catch or add a top-level catch that sets `_loading=false`.
- [medium] lib/features/settings/model_download_screen.dart:31 — `_downloadService.addListener(_onDownloadUpdate)` is registered in `initState` but `_onDownloadUpdate` calls `setState(() {})` with no guard against the service firing after `dispose` — the `mounted` check inside the callback (line 42) covers it, but the listener itself is never removed if `removeListener` throws; more importantly the listener is added every time the State object is created and removed in `dispose` — this is correct; no defect. (withdrawn)
- [medium] lib/features/settings/model_download_screen.dart:62-70 — `downloadAll`'s `onProgress` callback calls `setState` per progress event per model; with many models and frequent progress ticks this rebuilds the whole screen on every tick (noisy per-item logging/hot-loop class) — throttle/coalesce progress updates (e.g. only update when the integer percent changes) or use a ValueNotifier per row.
- [low] lib/features/settings/model_download_screen.dart:75 — after a successful `downloadAll`, `_checkStatus()` is awaited but `_downloading` is reset in `finally` regardless of success/failure, and `_error` is left set from a previous failed run when the retry succeeds — clear `_error = null` on success (in the try block after `downloadAll`) so a stale "Download failed" card doesn't linger.
- [low] lib/features/settings/model_download_screen.dart:232-255 — the "Clear Downloaded Models" flow awaits `showDialog` then calls `_manager.clearCache()` with no try/catch; a cache-clear failure throws inside the button's async closure and is silently dropped (unhandled async failure) — wrap in try/catch and surface the error via `_error`/snackbar.
- [low] lib/features/settings/model_download_screen.dart:253 — `clearCache()` deletes all downloaded models with no audit/logging of the destructive action (admin/destructive actions unaudited class) — log the clear event (timestamped, via the app's debug log service) before/after deletion.
- [info] lib/features/settings/model_download_screen.dart:120 — hardcoded '~341 MB' size string is duplicated with line 202; if the model set changes the UI lies about download size — derive from the manager's known model metadata or a single shared constant.

## Coverage
lib/features/settings/kokoro_debug_screen.dart — findings: 5
lib/features/settings/model_download_screen.dart — findings: 5
- [medium] lib/features/settings/settings_screen.dart:292-296 — Web-editor share text embeds the signed-in user's email address into a share sheet body — the email is disclosed to whatever app the user shares into (clipboard, messaging, social) and can be pasted onward; reachability: any authenticated user against their own data — smallest safe fix: drop the email from the share text (or show it only in a confirmation dialog the user explicitly confirms)
- [medium] lib/features/settings/settings_screen.dart:328 — sign-out clears only the `auth_skipped` flag and never removes the persisted Supabase session/auth token from local storage before navigating to `/auth` — if the device is shared/lost, the next user can restore the prior session from persisted state; reachability: any authenticated user against their own data — smallest safe fix: call `SupabaseService.instance.signOut()` (or clear the persisted session key) unconditionally before the navigation, not only inside the `isSignedIn` branch
- [low] lib/features/settings/settings_screen.dart:331-334 — sign-out path is guarded by `SupabaseService.instance.isInitialized && isSignedIn` with no fallback when the service is uninitialized — a stale-but-valid persisted session survives sign-out on a cold/uninitialized service; reachability: operator/local state edge — smallest safe fix: attempt session clear regardless of `isInitialized`, tolerating a no-op
- [low] lib/features/settings/settings_screen.dart:340-341 — sign-out failure is logged only to the in-memory debug log service (ring buffer, max 500 entries, wiped on `clear()`/restart) and never surfaced to any persistent/crash channel — auth-failure diagnostics are lost on app restart; reachability: operator — smallest safe fix: also record the failure via a persistent channel (e.g. crash reporting or the on-disk log flush) rather than only the in-memory ring
- [low] lib/features/settings/settings_screen.dart:343-348 — sign-out failure toast is shown only when `context.mounted` after an `await` gap with no fallback — if the widget was disposed during the async sign-out, the user gets no feedback that sign-out failed; reachability: operator — smallest safe fix: capture the `SnackBar`/messenger before the await or guard with a mounted check on the messenger itself
- [info] lib/features/settings/settings_screen.dart:69-76 — `_versionFuture` is memoized on a static-ish field but the fallback branch swallows all `PackageInfo` errors into a generic version string with no logging — version-detection failures are invisible to diagnostics; verify it is not committed as-is if this is meant to be a debug surface — smallest safe fix: log the swallowed exception via `DebugLogService.instance.logError(...)` in the `catch` branch
- [info] lib/features/settings/settings_screen.dart:354-355 — in-memory auth state (`authStateProvider`, `authGatePassedProvider`) is reset after the awaited sign-out but before `context.go('/auth')`, with no guard that the reset actually happened if an exception escaped the inner try — a thrown error between lines 328 and 354 leaves stale auth state while navigation proceeds; smallest safe fix: wrap the whole sequence in a try/finally that resets providers in `finally`
- [info] lib/features/settings/settings_screen.dart:358 — `context.go('/auth')` is guarded by `context.mounted` but the provider resets at 354-355 are not — if the widget unmounted mid-await, providers are mutated after disposal with no navigation, leaving the app in a half-signed-out state; smallest safe fix: perform provider resets inside the same `mounted` guard as the navigation, or before the await

## Coverage
lib/features/settings/settings_screen.dart — findings: 7
- [low] scripts/compare_macbeth_versions.py:62-71 — subprocess output discarded on success path; converter stdout/stderr only surfaced on failure, and stderr is printed without any structured logging — operator running the script offline cannot see why conversion produced empty/partial output when returncode is 0 but the file is truncated — log both streams (or check output file non-empty) before proceeding
- [low] scripts/compare_macbeth_versions.py:55,73 — `read_text()` uses locale-dependent default encoding (no `encoding=` arg) on both source files — on a non-UTF-8 default locale (e.g. Windows cp1252) the Gutenberg/Folger text is decoded with mojibake, silently corrupting the character-name comparison counts — pass `encoding='utf-8'` to both `read_text()` calls
- [low] scripts/make-demo-script.py:51 — `open(SRC, encoding='utf-8')` is never closed (no context manager) — leaked file handle on each run; harmless in a short-lived script but a defect class — use `with open(...) as fh:` or `pathlib.read_text()`
- [low] scripts/make-demo-script.py:40 — scene-boundary heuristic `m[0] > 100` hard-codes a magic threshold to skip the table of contents; if the source edition's TOC ends past line 100 or a real ACT/SCENE heading appears before line 100 in the body, scenes are silently mis-bounded or dropped — derive the TOC end from the source structure (e.g. first ACT heading after the contents block) instead of a constant
- [low] scripts/make-demo-script.py:29,32 — `WANTED` scene headings must byte-match the source exactly (`scene not in bounds` exits); a whitespace/typography variant in the Gutenberg file (e.g. different punctuation or trailing space already stripped but interior spacing differs) makes the build fail with no diagnostic of what headings *do* exist — on miss, print available candidate headings to aid repair
- [info] scripts/compare_macbeth_versions.py:51,58,64-66 — script references repo-relative sample assets (`sample-scripts/macbeth-pg1533-images-3.txt`, `macbeth_folger_converted.txt`, `macbeth_PDF_FolgerShakespeare.pdf`, sibling `pdf_to_script.py`) whose presence/absence cannot be confirmed from this batch — verify these paths exist in the repo or the script fails at runtime; not a code defect exhibited by the inlined lines alone

## Coverage
scripts/compare_macbeth_versions.py — findings: 3
scripts/make-demo-script.py — findings: 3
- [medium] scripts/parse_script.py:394-397 — script writes parsed output into the repository's `examples/` directory derived from its own location — running the tool on any input mutates the repo tree (untracked artifacts, CI dirt, potential overwrite of tracked example files) — write outputs to a caller-specified or temp directory instead of a path derived from `__file__`
- [low] scripts/parse_script.py:376 — default input path `/tmp/pride_full_ocr.txt` is a hardcoded fallback that silently parses an unrelated/stale file when no argument is given — require an explicit input path argument and exit with usage when missing
- [medium] scripts/parse_script.py:155-165 — multi-character cue regex accepts any uppercase name list and returns the whole comma-joined string as the character name even when only one known character appears in it (e.g. "MARY, GARBAGE.") — normalize each comma-separated token via `normalize_character` and reject unknown tokens instead of returning the raw string
- [low] scripts/parse_script.py:76 — cue patterns are compiled with `re.escape(char) + r"\.\s+"` requiring a trailing space after the period, so a character cue at end-of-line with no dialogue ("MR. BENNET.") is not detected and falls through to continuation logic — allow optional whitespace (`r"\.\s*"` or `r"\.(?:\s+|$)"`)
- [medium] scripts/parse_script.py:317-319 — dead branch: `add_stage_direction(line)` is only reachable when `line.startswith("(")` but `is_stage_direction` at line 301 already consumed every fully-parenthesized line, so the duplicate call can never fire and the comment's "without parens" case is unreachable — remove the redundant branch or implement the intended lowercase-starts-direction heuristic
- [low] scripts/parse_script.py:311 — continuation guard `re.match(r"^[^a-zA-Z]*$", line)` treats a line of digits/punctuation as noise but a line starting with a digit followed by letters (e.g. "2nd watchman") is appended to dialogue, silently merging unrelated text into the current speech — tighten the guard to reject lines whose first token is numeric margin noise
- [low] scripts/pdf_to_script.py:368 — stats regex `^[A-Z][A-Z. ]+\.\s*$` counts any all-caps line ending in a period as a character cue, so stage directions like "THEY EXIT." inflate the "Character cues found" stat — restrict to lines matching the known-character set or require the cue to be followed by dialogue
- [medium] scripts/pdf_to_script.py:287-291 — orphan dialogue with no `current_char` and no `stage_dir` is silently emitted (`pass` then append), producing speaker-less dialogue blocks in the converted text that the downstream parser cannot attribute — emit a diagnostic (e.g. count of orphan blocks) or drop the text so attribution failures are observable
- [low] scripts/pdf_to_script.py:34-43 — `doc.close()` in `finally` is correct, but if `pymupdf.open` itself raises (corrupt file), the exception propagates uncaught from `convert_pdf_to_script` to `main` with no user-facing message, only a traceback — catch and print a concise error with exit code 1 in `main`
- [low] scripts/pdf_to_script.py:363 — `Path(output_path).write_text(result)` writes UTF-8 by default but extracted PDF text may contain non-ASCII characters; on some locales this is fine, but no encoding is pinned — pass `encoding="utf-8"` explicitly to avoid platform-dependent output

## Coverage
scripts/parse_script.py — findings: 6
scripts/pdf_to_script.py — findings: 4
- [medium] lib/features/script_editor/cloud_sync_dialog.dart:24 — `diffScriptLines` builds `localById` as a map literal keyed by `l.id` without deduping; if two local lines share an id (e.g. after a merge/import), the later entry silently overwrites the earlier, so the diff reports the wrong line as unchanged/changed and the "Was:" tile shows the wrong original text — guard with `localById[l.id] == null` check or assert uniqueness when building the map.
- [medium] lib/features/script_editor/cloud_sync_dialog.dart:55 — removal detection uses `matchedLocalIds` membership only; a local line whose id matches a cloud line is never counted as removed even when the cloud copy was dropped from the list (id present but cloud list shorter), so the summary under-counts removals and the user can accept a cloud version that silently deletes lines — track removals by comparing id sets, not just matched membership.
- [low] lib/features/script_editor/cloud_sync_dialog.dart:217 — `diff.cloud ?? diff.local!` uses a non-null assertion on `local` for `removed` diffs; if a future refactor constructs a `removed` diff without `local`, this throws at runtime in the dialog builder — prefer `diff.local ?? diff.cloud!` ordering or make the field non-nullable per type.
- [low] lib/features/script_editor/cloud_sync_dialog.dart:231 — icon color is derived by re-comparing `bgColor` against freshly-allocated `Colors.green.withValues(alpha: 0.1)` / `Colors.red.withValues(alpha: 0.1)` instances instead of branching on `diff.type`; identical-value Color instances compare equal here only by luck of const/factory caching, and the ternary chain is fragile — switch on `diff.type` to pick the icon color.
- [low] lib/features/script_editor/cloud_sync_dialog.dart:284-290 — `_sameList` compares element-wise with `!=` on `String` which is fine, but the `identical(a, b)` fast path returns true for the *same* list object even when callers pass the same mutable list for both sides, masking real content comparison in tests that reuse a list — acceptable as-is, but note it cannot fail on identity alone; no production defect exhibited.
- [info] lib/features/script_editor/cloud_sync_dialog.dart:90-92 — `showCloudSyncDialog` returns `Future<bool?>` and the dialog can be dismissed via `barrierDismissible: false` only through the two buttons, but `Navigator.pop(context, true/false)` on the buttons leaves the route poppable by system back (Android) which yields `null` to callers that may treat `null` as "accept" — verify callers distinguish `null` from `false`; not decidable from this file alone.

## Coverage
lib/features/script_editor/cloud_sync_dialog.dart — findings: 6

## Run stats

input 952987 tok (+51183 cached), output 86873 tok — sync requests, discounted — 75 files in 100m (44.7 files/h, 2.4 min/batch)
